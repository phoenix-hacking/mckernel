#ifndef _ASM_X86_ASM_H
#define _ASM_X86_ASM_H

/*
 * Former Linux-style function-like assembler macros have been retired from
 * this header.  The remaining x86_64 C fallback asm users spell their concrete
 * exception-table entries locally so this header no longer owns executable
 * preprocessor behavior.
 */

#endif /* _ASM_X86_ASM_H */
