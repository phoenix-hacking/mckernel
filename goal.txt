Active stability and full-verification goal (2026-09-13)
=====================================================
Implement the entire roadmap in docs/verification/stability-goal-20260913.txt.
The user removed the timelines: prioritize functionality and OS stability,
then satisfy all remaining native production and Rust/assembly requirements.
Continue via docs/verification/stability-run-20260913.md.
Preserve evidence, runtime gates, resource limits and verified GitHub checkpoints.
Do not stop after the first subset or claim historical scores prove completion.

Historical completed drafting goal follows
-----------------------------------------
Active Codex Spark goal (2026-09-09; start after the user selects Spark or Luna)
============================================================================
Work in `/home/holden/mckernel` on `codex/local-native-staging-repair`.
Complete the released application-test drafting queue: **273 logical cases
across 97 packets, including 56 vector instruction/state cases**. Create the
specified test fixtures or pinned-application input specifications and their
independent oracle JSON files. Produce useful, specific tests that expose bugs.
This goal completes test drafting; it does not execute or accept the new suite.

First read `docs/verification/codex-spark-runbook-20260909.md` and follow its
startup checks and file-reading order. The Ultra release was accepted at
commit `7b32298119df13a83240e082c275c34d184f7ccc`; the runbook identifies its
retained GitHub verification proof. Preserve the released catalog, packets,
validator, contexts, input/capability manifests, kernel and launcher unchanged.

Start with packet 001 and continue automatically through packet 097. Use only
one packet and at most three logical cases at a time. Load selected case bodies
through the existing context emitter; do not load the whole catalog, other
packet bodies, historical kernel.log or complete evidence archives into context.
Replace completed case bodies with report hashes and a short dependency note.
Do not ask for permission between packets, routine edits or checkpoints.

For every active case, implement its exact operation, parameter vectors,
resource bounds, cleanup and expected output contract. Write a versioned
independent oracle with fixed bytes, values or named relational assertions.
Retain every required parameter; parameter executions do not add logical cases.
Do not finish with empty files, comment-only fixtures, unconditional success,
or tests that never exercise the specified behavior. Do not invent expected
results or resolve an uncertain errno by accepting multiple arbitrary values.

All packets are `draft-only` with execution disabled. Missing runtime support
does not prevent drafting a well-specified test. Record a drafting blocker only
when a concrete missing interface, input or semantic expectation actually
prevents that draft. Complete the independent work, record the exact missing
item and smallest question for Max, then continue the next independent packet.
Do not implement a runner or supervisor, compile/run payloads, launch guests,
modify production code, weaken an oracle or change the released plan to clear
a blocker. Those actions require a later reviewed execution scope.

For vector cases, preserve the reviewed per-engine CPUID/OSXSAVE/XCR0 gates,
independent scalar/math expectations and emitted-instruction requirements.
Cover SSE/XMM, AVX/AVX2/YMM and the specified optional extensions. AVX-512 stays
conditional on the actual guest feature and enabled-state checks. Host CPU
flags alone never authorize guest instructions; missing support is not PASS.

Preserve each original failure before diagnosis. Use the immutable per-packet
reports and append-only failure log defined by the packet and runbook. Small
fixture/oracle corrections must preserve independent expectations, stay inside
the active write allowlist, and respect one candidate, 15 minutes, two files
and 80 changed lines. Escalate unsafe, kernel/ABI/lifetime/scheduler/transport,
unclear or unsuccessful repairs to Max. No production repair is enabled here.

After every packet, report its index out of 97, exact case statuses, files
created, unresolved expectations, original failures and next packet. Checkpoint
the drafted files and immutable reports to GitHub after coherent packet groups,
about every 30 minutes, and before stopping. Verify the fetched commit and file
hashes. Preserve prior evidence and active inputs. Follow the runbook's bounded
coordination permissions for contexts, reporting, logging and Git checkpoints.

Finish only when all 97 immutable packet reports account for all 273 unique
case IDs exactly once, with meaningful drafts wherever the specification allows
them and precise evidence for every remaining blocker. Create the immutable
queue summary with separate `DRAFTED`, `DRAFTED_WITH_UNRESOLVED`, `BLOCKED` and
`FAILED` counts, report/file hashes and prioritized Max questions. Verify the
final GitHub checkpoint. State **"The Spark drafting queue is complete and
ready for Max review"**, identify any blockers, and stop. Do not claim that
drafted tests passed or that the OS is production-ready.


END ACTIVE SPARK GOAL

Historical Ultra preparation and older goals follow
--------------------------------------------------
Current execution phase (2026-09-09): Ultra gates and bounded drafting release
----------------------------------------------------------------------------
Review the accepted Max baseline, fix and verify prerequisite defects, and
produce a very broad, precise application test plan with bounded executor
packets usable by Luna or Spark. Clearly notify the user when the handoff is
ready. Preserve every failure and log candidate fixes for Max to investigate
narrowly after the executor tests widely. Do not switch models automatically.
Periodically commit, push and verify GitHub progress about every30minutes during
sustained work, after coherent checkpoints, and before long runs/session end.
New source changes require new validation; historical baseline PASS is retained
only for its exact original binaries. Follow current AGENTS.md and Ultra reviews.
Release authority is docs/verification/ultra-drafting-handoff-20260909.json plus
its successful exact fetched-blob verification. Once released, explicitly tell
the user "We're ready to run Codex Spark" and stop for their chosen model switch.
Next phase drafts273 cases (56 vector cases) through97 bounded packets; it does
not automatically enable guest execution. Follow scripts/application-tests/
handoff-start.md, preserve immutable failure/review reports, and return difficult
kernel diagnosis to Max. Do not load the whole catalog/history into the executor.

Historical completed Max phase follows
-------------------------------------
Current execution goal (2026-09-08)
----------------------------------
Stay on Max until the native kernel has a reproducible application-testing
baseline: real application execution, working delegated syscalls, clean process
retirement, repeated launches and representative application smoke checks.
Retain the commands, source and image identities, results and failure evidence.
Use docs/verification/native-application-service-plan.md, section
"Application readiness and model handoff, 2026-09-08", for the readiness criteria.

Continue saving coherent checkpoints to GitHub, aiming for about every 30
minutes during sustained work and before long validation or ending a session.
Verify the pushed commit. Once the readiness criteria pass, save the handoff,
mark this phase complete and stop for the user's switch to Astra Ultra.
Explicitly tell the user "We're ready to switch to Astra Ultra" at that point.
Astra will review the implementation and evidence, fix issues it finds and
produce the detailed application/further-testing plan. After that review, the
user intends to switch to Codex Spark to implement and run those tests.
Do not switch models or begin the later phases automatically.

This goal's completion means readiness for broader testing. Full production
acceptance and the Rust/assembly end state remain subsequent obligations.
The historical request below is preserved as background, not this phase's
stopping condition.

Historical full-port request
----------------------------
› Awesome put this in full-port.txt with a plan to see it completed please and update AGENTS.md for
  this as well. Also please make somethign that I can push to goal mode and complete it. All code
  should be verified and QEMU and virtualization tools like KVM should be used to make sure that
  everything is fully working please


• Proposed Plan


  # Full-Port Tracker And Completion Goal

  ## Summary

  - Create full-port.txt as the authoritative non-arm full-port tracker.
  - Update AGENTS.md so future agents treat full-port.txt as the strict whole-repo completion tracker.
  - Scope: x86_64/Rocky only; arm64 excluded; third-party/vendor/generated code excluded.
  - Current full-port baseline: 32.5% complete, 67.5% remaining, based on mechanical McKernel-owned
    implementation debt.

  ## full-port.txt

  Create /home/hgordon/mckernel/full-port.txt with:

  McKernel Full Rust Port Tracker
  Updated: 2026-06-01

  Scope
  ------------------------------------------------------------
  Target: all McKernel-owned x86_64/Rocky implementation logic should be Rust-owned.
  Arm64 is excluded. Third-party/vendor/generated code, public compatibility
  headers, tests, and unavoidable assembly-adjacent code are excluded.

  These percentages are mechanical full-port percentages from the ownership audit,
  not the capped functional dashboard or strict sequencing score.

  Current Full-Port Percentages
  ------------------------------------------------------------
  Overall non-arm full port: 32.5% complete, 67.5% left

  Category                              Complete   Left
  --------                              --------   ----
  ABI/layout foundation                   100.0%    0.0%
  Page allocator                           59.0%   41.0%
  Scheduler/timers/wait/futex             56.4%   43.6%
  Process/VM management                   46.0%   54.0%
  x86_64 memory management                45.1%   54.9%
  Shared primitives                       43.9%   56.1%
  procfs/sysfs/xpmem/file objects         40.2%   59.8%
  Syscall core                            35.5%   64.5%
  Other in-scope kernel code              26.7%   73.3%
  User tools                              22.9%   77.1%
  host/IKC/mcctrl/IHK modules             12.4%   87.6%

  Completion Plan
  ------------------------------------------------------------
  1. Finish the x86_64/Rocky standalone OS default path:
     allocator/free lifetime, lock/list/refcount/atomic primitives, page-table/TLB
     and user-copy primitives, process/thread/object lifecycle mutation,
     scheduler/context-switch/wait/futex/signal side effects, syscall dispatch,
     Linux-forwarding callbacks, and IKC/control-plane exchange.

  2. Complete host/IHK/mcctrl ownership:
     module-load, OS create/destroy, memory registration, IKC send/receive,
     device lifecycle, kmsg/notifier/event paths, and mcctrl syscall/procfs/sysfs
     command bodies.

  3. Complete user tools:
     mcexec, mcstat, mcinspect, eclair, crash helpers, dump helpers, ihkosctl,
     ihkconfig, ihkmond, and smaller utilities while preserving CLI flags,
     output formats, exit codes, config parsing, ioctl/protocol behavior, and
     script compatibility.

  4. Retire C fallbacks:
     keep C fallback during validation, then remove McKernel-owned C fallback
     bodies once Rust equivalence and runtime coverage prove the replacement.

  5. Final gate:
     remaining C must be limited to compatibility headers/wrappers, approved
     external Linux interface glue, tests, generated/vendor code, or unavoidable
     assembly-adjacent code.

  Verification Requirements
  ------------------------------------------------------------
  Every completed slice must pass:
  - Rust formatting/static checks.
  - `kernel/rust/tests/run_equivalence.sh`.
  - `scripts/rust-ownership-report.py --format markdown --top 25`.
  - Rust-enabled kernel/control-plane/tool builds relevant to the touched area.
  - C fallback build until that fallback is intentionally retired.
  - Module-load smoke for IHK/mcctrl changes.
  - QEMU/KVM validation for runtime-sensitive changes.

  Runtime validation must use QEMU and virtualization tooling:
  - Prefer KVM when available and verified for the run.
  - Fall back to TCG only when KVM is unavailable.
  - Do not run host reboot paths.
  - Use guest-only McKernel boot/workload validation.
  - Required runtime gates before final completion: Rocky QEMU boot smoke,
    module load/unload, OS create/destroy, minimal user workloads, focused V11
    regression, and stress coverage for memory faults, page tables, allocator
    pressure, futex/wait, signals/ptrace, XPMEM, procfs/sysfs, IKC, and shutdown.

  Stop on first validation failure and record the failing command, environment,
  and short error summary in `kernel.log`.

  ## AGENTS.md

  Update the repository instructions with a short “Full-Port Tracker” section:

  ## Full-Port Tracker

  `full-port.txt` is the strict whole-repo x86_64/Rocky full Rust port tracker.
  It excludes arm64, third-party/vendor/generated code, public compatibility
  headers, tests, and unavoidable assembly-adjacent code.

  Use `full-port.txt` for the final C-dependency retirement goal. Use
  `migration.txt` for strict core sequencing history and `overview.txt` for the
  functional dashboard. The current non-arm full-port baseline is 32.5% complete
  and 67.5% remaining.

  Every full-port movement must be verified. Runtime-sensitive work must use QEMU
  and virtualization tooling, preferably KVM when available and verified. Do not
  use host reboot paths. Stop on first validation failure and log the failing
  command, environment, and short error summary in `kernel.log`.

  Also add full-port.txt to any tracker references near migration.txt and
  overview.txt.

  ## Goal-Mode Objective

  Use this objective for Goal Mode:

  Complete the x86_64/Rocky McKernel full Rust port tracked by full-port.txt:
  move all McKernel-owned non-arm implementation logic to Rust, retire required C
  fallback bodies from the default implementation, preserve public ABI behavior,
  exclude third-party/vendor/generated code and unavoidable assembly-adjacent
  surfaces, and verify every slice with equivalence tests, Rust/C builds while
  fallbacks remain, ownership gates, module-load smoke, and QEMU/KVM runtime
  validation until full-port.txt reaches 100.0%.

  ## Acceptance Criteria

  - full-port.txt exists and is updated after each validated slice.
  - AGENTS.md names full-port.txt as the strict whole-repo full-port tracker.
  - scripts/rust-ownership-report.py --fail-on-c or an equivalent classified
    gate passes for McKernel-owned non-arm implementation code.

  - Default x86_64/Rocky kernel/control-plane/tool paths no longer require
    McKernel-owned C implementation bodies.

  - QEMU/KVM runtime validation passes for boot, module lifecycle, OS lifecycle,
    workloads, regression, stress, IKC, and shutdown paths.

