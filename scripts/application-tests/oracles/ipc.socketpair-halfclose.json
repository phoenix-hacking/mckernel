{
  "schema_version": 1,
  "case_id": "ipc.socketpair-halfclose",
  "case_version": 1,
  "artifact_version": "20260909.1",
  "input_fixture": {"argv": ["/apps/ipc.socketpair-halfclose"], "cwd": "/case/work", "environment": {"LANG": "C", "LC_ALL": "C", "TZ": "UTC"}},
  "wait_status": {"kind": "exited", "code": 0},
  "stdout": {"kind": "exact-line-lines", "lines": ["buffered=1 eof=1 reverse=1 valid=1"]},
  "stderr": {"kind": "exact-bytes", "hex": ""},
  "relational_assertions": ["Peer reads buffered LEFT bytes before EOF after one endpoint shuts down writing", "The reverse direction remains usable and carries R"],
  "unresolved_expectations": ["Half-close ordering, EOF lifetime, and descriptor cleanup require Max review."],
  "max_question_for_reviewer": "Confirm shutdown(SHUT_WR) preserves buffered data, then EOF, while retaining reverse-direction reads and writes."
}
