diff --git a/linux/core/host_driver.c b/linux/core/host_driver.c
index 102345b..feebcb6 100644
--- a/linux/core/host_driver.c
+++ b/linux/core/host_driver.c
@@ -34,6 +34,12 @@
 #include <linux/version.h>
 #include <linux/cred.h>
 #include <linux/mutex.h>
+#include <linux/semaphore.h>
+#if defined(__has_include)
+# if __has_include(<linux/panic_notifier.h>)
+#  include <linux/panic_notifier.h>
+# endif
+#endif
 #include <ihk/ihk_host_user.h>
 #include <ihk/ihk_host_driver.h>
 #include <asm/spinlock.h>
@@ -58,6 +64,20 @@
 #define DEV_DEV_NAME "mcd"
 #define OS_DEV_NAME  "mcos"
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 4, 0)
+#define IHK_DEFINE_SEMAPHORE(name) DEFINE_SEMAPHORE(name, 1)
+#define IHK_CLASS_CREATE(name) class_create(name)
+#define IHK_GFP_ATOMIC GFP_ATOMIC
+#elif LINUX_VERSION_CODE >= KERNEL_VERSION(4, 4, 0)
+#define IHK_DEFINE_SEMAPHORE(name) DEFINE_SEMAPHORE(name)
+#define IHK_CLASS_CREATE(name) class_create(THIS_MODULE, name)
+#define IHK_GFP_ATOMIC __GFP_ATOMIC
+#else
+#define IHK_DEFINE_SEMAPHORE(name) DEFINE_SEMAPHORE(name)
+#define IHK_CLASS_CREATE(name) class_create(THIS_MODULE, name)
+#define IHK_GFP_ATOMIC GFP_KERNEL
+#endif
+
 #define OS_DATA_INVALID ((void *)-1)
 #define DEV_DATA_INVALID ((void *)-1)
 
@@ -74,7 +94,7 @@ static struct ihk_host_linux_os_data *os_data[OS_MAX_MINOR];
 static int os_max_minor = 0;
 
 static struct list_head ihk_os_notifiers;
-DEFINE_SEMAPHORE(ihk_os_notifiers_lock);
+IHK_DEFINE_SEMAPHORE(ihk_os_notifiers_lock);
 
 static struct list_head ihk_kmsg_bufs;
 static spinlock_t ihk_kmsg_bufs_lock;
@@ -1650,11 +1670,7 @@ static int __ihk_device_create_os(struct ihk_host_linux_device_data *data,
 		++kmsg_buf_order;
 
 	kmsg_buf_pages = alloc_pages_node(NUMA_NO_NODE,
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(4, 4, 0)
-					  __GFP_ATOMIC |
-#else
-					  GFP_KERNEL |
-#endif
+					  IHK_GFP_ATOMIC |
 					  __GFP_HIGH |
 					  __GFP_COMP | __GFP_NORETRY |
 					  __GFP_NOWARN | __GFP_ZERO,
@@ -2205,7 +2221,7 @@ static int __init ihk_host_driver_init(void)
 		goto ERR;
 	}
 	
-	mcd_class = class_create(THIS_MODULE, DEV_DEV_NAME);
+	mcd_class = IHK_CLASS_CREATE(DEV_DEV_NAME);
 	if (!mcd_class) {
 		printk(KERN_INFO "IHK: Cannot create mcd.\n");
 		goto ERR;
@@ -2216,7 +2232,7 @@ static int __init ihk_host_driver_init(void)
 		printk(KERN_INFO "IHK: Cannot allocate char device number.\n");
 		goto ERR;
 	}
-	mcos_class = class_create(THIS_MODULE, OS_DEV_NAME);
+	mcos_class = IHK_CLASS_CREATE(OS_DEV_NAME);
 	if (!mcos_class) {
 		printk(KERN_INFO "IHK: Cannot create mcos.\n");
 		goto ERR;
diff --git a/linux/driver/smp/arch/x86_64/smp-arch-driver.c b/linux/driver/smp/arch/x86_64/smp-arch-driver.c
index 8c9c996..c796517 100644
--- a/linux/driver/smp/arch/x86_64/smp-arch-driver.c
+++ b/linux/driver/smp/arch/x86_64/smp-arch-driver.c
@@ -130,6 +130,16 @@ static pgd_t *_init_level4_pgt;
 
 static unsigned long *_used_vectors;
 
+static int ihk_uv_uses_non_unique_apic(void)
+{
+#if (!defined(RHEL_RELEASE_CODE) && LINUX_VERSION_CODE < KERNEL_VERSION(5, 9, 0)) || \
+	(defined(RHEL_RELEASE_CODE) && RHEL_RELEASE_CODE < RHEL_RELEASE_VERSION(8, 4))
+	return _get_uv_system_type() == UV_NON_UNIQUE_APIC;
+#else
+	return 0;
+#endif
+}
+
 int ihk_smp_arch_symbols_init(void)
 {
 	_real_mode_header = (void *) kallsyms_lookup_name("real_mode_header");
@@ -322,7 +332,7 @@ int smp_wakeup_secondary_cpu(int apicid, unsigned long start_eip)
 	atomic_set(_init_deasserted, 0);
 #endif
 
-	if (_get_uv_system_type() != UV_NON_UNIQUE_APIC) {
+	if (!ihk_uv_uses_non_unique_apic()) {
 		pr_debug("Setting warm reset code and vector.\n");
 
 		smpboot_setup_warm_reset_vector(start_eip);
diff --git a/linux/driver/smp/smp-driver.c b/linux/driver/smp/smp-driver.c
index 409d6b3..c422756 100644
--- a/linux/driver/smp/smp-driver.c
+++ b/linux/driver/smp/smp-driver.c
@@ -5412,6 +5412,9 @@ static int ihk_smp_symbols_init(void)
 		goto err;
 
 	ihk__free_vmap_area = (void *)kallsyms_lookup_name("free_vmap_area");
+	if (!ihk__free_vmap_area)
+		ihk__free_vmap_area =
+			(void *)kallsyms_lookup_name("free_vmap_area_noflush");
 	if (WARN_ON(!ihk__free_vmap_area))
 		goto err;
 #endif
diff --git a/linux/user/CMakeLists.txt b/linux/user/CMakeLists.txt
index 817b5c1..4329a7e 100644
--- a/linux/user/CMakeLists.txt
+++ b/linux/user/CMakeLists.txt
@@ -4,6 +4,7 @@ include_directories(
 	"${PROJECT_BINARY_DIR}/linux/include"
 	"${PROJECT_BINARY_DIR}"
 )
+include(CheckCSourceCompiles)
 
 execute_process(COMMAND awk -F= "$1 == \"CONFIG_ARM64_64K_PAGES\" { print $2; exit; }" "${KERNEL_DIR}/.config"
 	OUTPUT_VARIABLE CONFIG_ARM64_64K_PAGES OUTPUT_STRIP_TRAILING_WHITESPACE)
@@ -18,8 +19,25 @@ else()
 endif()
 message("PAGE_SIZE: ${PAGE_SIZE}")
 
+set(_ihk_saved_required_libraries "${CMAKE_REQUIRED_LIBRARIES}")
+set(CMAKE_REQUIRED_LIBRARIES ${LIBBFD} ${CMAKE_REQUIRED_LIBRARIES})
+check_c_source_compiles("
+#include <bfd.h>
+int main(void)
+{
+	bfd *abfd = 0;
+	asection *scn = 0;
+
+	return bfd_set_section_size(abfd, scn, 0) ? 0 : 1;
+}
+" IHK_BFD_SET_SECTION_NEEDS_BFD)
+set(CMAKE_REQUIRED_LIBRARIES "${_ihk_saved_required_libraries}")
+
 add_library(ihklib SHARED ihklib.c)
 target_compile_definitions(ihklib PRIVATE -DPAGE_SIZE=${PAGE_SIZE})
+if(IHK_BFD_SET_SECTION_NEEDS_BFD)
+	target_compile_definitions(ihklib PRIVATE -DIHK_BFD_SET_SECTION_NEEDS_BFD)
+endif()
 SET_TARGET_PROPERTIES(ihklib PROPERTIES OUTPUT_NAME ihk)
 target_link_libraries(ihklib ${LIBBFD})
 
@@ -60,4 +78,3 @@ install(FILES "../include/ihk/ihklib.h"
 install(FILES "../include/ihk/affinity.h"
 	DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/ihk")
 
-
diff --git a/linux/user/ihklib.c b/linux/user/ihklib.c
index 3d99534..0e7e501 100644
--- a/linux/user/ihklib.c
+++ b/linux/user/ihklib.c
@@ -31,6 +31,18 @@
 #include <ihk/ihklib.h>
 #include <ihk/ihklib_private.h>
 
+#ifdef IHK_BFD_SET_SECTION_NEEDS_BFD
+#define mc_bfd_set_section_size(abfd, scn, size) \
+	bfd_set_section_size((abfd), (scn), (size))
+#define mc_bfd_set_section_flags(abfd, scn, flags) \
+	bfd_set_section_flags((abfd), (scn), (flags))
+#else
+#define mc_bfd_set_section_size(abfd, scn, size) \
+	bfd_set_section_size((scn), (size))
+#define mc_bfd_set_section_flags(abfd, scn, flags) \
+	bfd_set_section_flags((scn), (flags))
+#endif
+
 //#define DEBUG
 
 int __argc;
@@ -3936,7 +3948,7 @@ int ihk_os_makedumpfile(int index, char *dump_file, int dump_level, int interact
 			goto out;
 		}
 
-		ok = bfd_set_section_size(abfd, scn, cpsize);
+		ok = mc_bfd_set_section_size(abfd, scn, cpsize);
 		if (!ok) {
 			ret = -EINVAL;
 			dprintf("%s: error: "
@@ -3945,7 +3957,7 @@ int ihk_os_makedumpfile(int index, char *dump_file, int dump_level, int interact
 			goto out;
 		}
 
-		ok = bfd_set_section_flags(abfd, scn, SEC_HAS_CONTENTS);
+		ok = mc_bfd_set_section_flags(abfd, scn, SEC_HAS_CONTENTS);
 		if (!ok) {
 			ret = -EINVAL;
 			dprintf("%s: error: "
@@ -3966,7 +3978,7 @@ int ihk_os_makedumpfile(int index, char *dump_file, int dump_level, int interact
 			goto out;
 		}
 
-		ok = bfd_set_section_size(abfd, scn, cpsize);
+		ok = mc_bfd_set_section_size(abfd, scn, cpsize);
 		if (!ok) {
 			ret = -EINVAL;
 			dprintf("%s: error: "
@@ -3975,7 +3987,7 @@ int ihk_os_makedumpfile(int index, char *dump_file, int dump_level, int interact
 			goto out;
 		}
 
-		ok = bfd_set_section_flags(abfd, scn, SEC_HAS_CONTENTS);
+		ok = mc_bfd_set_section_flags(abfd, scn, SEC_HAS_CONTENTS);
 		if (!ok) {
 			ret = -EINVAL;
 			dprintf("%s: error: "
@@ -3996,7 +4008,7 @@ int ihk_os_makedumpfile(int index, char *dump_file, int dump_level, int interact
 			goto out;
 		}
 
-		ok = bfd_set_section_size(abfd, scn, cpsize);
+		ok = mc_bfd_set_section_size(abfd, scn, cpsize);
 		if (!ok) {
 			ret = -EINVAL;
 			dprintf("%s: error: "
@@ -4005,7 +4017,7 @@ int ihk_os_makedumpfile(int index, char *dump_file, int dump_level, int interact
 			goto out;
 		}
 
-		ok = bfd_set_section_flags(abfd, scn, SEC_HAS_CONTENTS);
+		ok = mc_bfd_set_section_flags(abfd, scn, SEC_HAS_CONTENTS);
 		if (!ok) {
 			ret = -EINVAL;
 			dprintf("%s: error: "
@@ -4025,7 +4037,7 @@ int ihk_os_makedumpfile(int index, char *dump_file, int dump_level, int interact
 		goto out;
 	}
 
-	ok = bfd_set_section_size(abfd, scn, mem_size);
+	ok = mc_bfd_set_section_size(abfd, scn, mem_size);
 	if (!ok) {
 		ret = -EINVAL;
 		dprintf("%s: error: "
@@ -4034,7 +4046,7 @@ int ihk_os_makedumpfile(int index, char *dump_file, int dump_level, int interact
 		goto out;
 	}
 
-	ok = bfd_set_section_flags(abfd, scn, SEC_ALLOC|SEC_HAS_CONTENTS);
+	ok = mc_bfd_set_section_flags(abfd, scn, SEC_ALLOC|SEC_HAS_CONTENTS);
 	if (!ok) {
 		ret = -EINVAL;
 		dprintf("%s: error: "
@@ -4060,10 +4072,10 @@ int ihk_os_makedumpfile(int index, char *dump_file, int dump_level, int interact
 		}
 
 		if (interactive) {
-			ok = bfd_set_section_size(abfd, scn, PAGE_SIZE);
+			ok = mc_bfd_set_section_size(abfd, scn, PAGE_SIZE);
 		}
 		else {
-			ok = bfd_set_section_size(abfd, scn, mem_chunks->chunks[i].size);
+			ok = mc_bfd_set_section_size(abfd, scn, mem_chunks->chunks[i].size);
 		}
 		if (!ok) {
 			ret = -EINVAL;
@@ -4073,7 +4085,7 @@ int ihk_os_makedumpfile(int index, char *dump_file, int dump_level, int interact
 			goto out;
 		}
 
-		ok = bfd_set_section_flags(abfd, scn, SEC_ALLOC|SEC_HAS_CONTENTS);
+		ok = mc_bfd_set_section_flags(abfd, scn, SEC_ALLOC|SEC_HAS_CONTENTS);
 		if (!ok) {
 			ret = -EINVAL;
 			dprintf("%s: error: "
