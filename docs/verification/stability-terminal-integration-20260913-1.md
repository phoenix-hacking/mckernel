# Terminal ownership boundary checkpoint

The fifth hard-fault guest exercised the selected real failure before wake
publication. Its full contract remains **FAIL**: the selected application's
`closed` field changed between Terminal and TerminalPlusFive. Every other
inventory row was unchanged. The original launcher became waitable while the
first terminal snapshot was already being collected.

The retained physical diagnosis matches the original request to its consumed
ring packet, permits only the specified response-field changes, verifies an
identical response five seconds later, and observes no new selected wake.
This diagnostic does not override the original inventory failure. All three
QMP captures resumed; actual guest artifact export and shutdown completed.

The additive owner-phase-v2 controller waits for the original launcher to be
waitable and both real output pipes to reach EOF before requesting Terminal.
It preserves WNOWAIT identity, the original input/overall deadlines and the
original post-quiet cleanup. The host runner additionally requires the selected
native application to be closed and quarantined in both terminal snapshots.
The complete inventory equality and physical response oracles remain intact.

The exact controller compiled with the established pinned C flags. Eight
actual Linux scenarios passed against its unchanged boundary method: exit
and EOF, live launcher with EOF, each descendant-held stream, stale start
ticks, nonzero exit, already-expired input deadline, and overall deadline.
The harness retains actual raw waits, repeated WNOWAIT observations, literal
pipe bytes and complete owned cleanup. This is infrastructure validation;
it does not establish a kernel guest or payload result.

Root5 preparation and preflight passed with this controller and the already
compiled runnable-thread payload. The next attempt is fresh guest6, using
the unchanged repaired production inputs and verification module3. Its
invocation is frozen in `evidence/stability-sixth-fault-invocation-20260913.py`.
At this checkpoint guest6 has not run.

The application runner's metadata preflight also passed all twelve pinned
checks. Its reports remain NOT_RUN and acceptance false. New catalog totals
remain three fixtures compiled, zero executed and zero accepted. The remaining
three fault contracts are unreleased drafts; full-ring behavior, invalidation
ownership, generic application collection, lifecycle restoration and the
full Rust/assembly and production qualification requirements remain open.

`stability-terminal-integration-20260913-1.json` maps nine complete, verified
capture archives: guest5 and its parent, physical diagnosis, controller build,
root5 preparation/root/preflight, eight-case boundary checks and metadata12.
Each archive was compared to every original byte, member, type, mode and link.
All original expanded captures remain present. The original failed GitHub
verification and successful retry from checkpoint7 are preserved separately.
