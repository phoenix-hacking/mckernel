# Active McKernel stability and verification run

The user authorized the complete attached roadmap on 2026-09-13 and removed
its three-day schedule. `stability-goal-20260913.txt` preserves the complete
request. Functionality and trustworthy runtime evidence come first; full native
production acceptance and Rust/assembly closure remain mandatory later gates.

## Starting authority

- Repository baseline: `d67c2640d4cadf68367964de8d798ebb2411abe7` on
  `codex/local-native-staging-repair`.
- Actual accepted input pair: Ultra module attempt 1 / image attempt 3 in
  `ultra-draft-inputs-20260909.json`, with full evidence in
  `ultra-final-checkpoint-20260909.json`. All nine selected artifact/record
  identities were rechecked against retained files before implementation.
- The 97 packet reports account for 273 cases. They report six DRAFTED and
  267 DRAFTED_WITH_UNRESOLVED; no new catalog runtime acceptance exists.
- Old progress campaigns remain closed. Native production remains 350/10000
  points; the seven additional MK-LANG checks remain separate and incomplete.

## Ordered work and ownership

1. Reconcile immutable draft reports and input declarations; inventory every
   application, production gate, language gate and tracker in a current ledger.
2. Implement and independently validate execution manifests, process supervision,
   exact pinned-guest reference, result/oracle validation and provenance capture.
3. Replay current-pair original memory/files/threads/signals, both control ABIs,
   and ordinary signal/futex checks with fresh preserved attempts.
4. Implement actual fault supervisor and owner observations; verify all four
   committed-return fault modes and physical full-ring behavior separately.
   Resolve service-error admission/zeroing and invalidation ownership failures.
5. Release at most three reviewed cases per execution packet when all global
   and individual capability gates pass. Expand through the existing family
   order, with independent oracles and required repeats.
6. Continue native shutdown/restoration, missing application capabilities,
   vectors, multicore/NUMA/MPI, Rust/assembly closure, qualification and release
   according to the complete preserved roadmap and original acceptance gates.

The coordinator owns integration, current-state records and Git checkpoints.
Independent lanes own draft reconciliation, supervision and narrow fault review.
Only the coordinator launches serialized container builds/guests. Do not run
host kernel/module operations or use host application execution as guest evidence.

## Runtime and evidence discipline

The original catalog, packets, validator and draft release remain unchanged.
New runtime interfaces and reviewed packet versions carry explicit input hashes,
capability evidence and execution scope. The prerequisite infrastructure tests
are distinct from application acceptance. No missing capability becomes PASS.

Preserve every original failure before diagnosis in `kernel.log` and its full
attempt directory. Use fresh attempts and fresh guests after failures. Normal
cleanup and stable quarantine have different fixed owner contracts. Current
source changes invalidate affected binary evidence until rebuilt and rebound.

Container limits remain four CPUs (2-5), 12 GiB, no swap, 512 tasks and no
network. Initial guests retain four Linux vCPUs/8 GiB/two NUMA nodes and one
McKernel CPU/128 MiB. Existing case limits, 300-second application guests and
600-second original controls remain unchanged. Check scratch and root capacity
before each substantial batch; they share the sparse backing-file budget.

Commit and push coherent work about every 30 minutes and before long runs or
session end; verify fetched GitHub blobs. Preserve active dependencies and all
failure evidence. Audited removal of redundant retained copies and targeted
scratch trim remain authorized. A WIP checkpoint is not goal completion.
