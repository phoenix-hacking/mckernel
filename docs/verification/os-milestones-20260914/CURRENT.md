# Resume cursor

State: milestone plan and Sol/Luna launcher prepared; OS implementation/execution milestones await
dispatch. Current snapshot: 2026-09-14, source parent
`9d90b588bae761a6cea6596827f8cc296a3ee642`, branch
`codex/local-native-staging-repair`. The commit containing this file is the plan
checkpoint. Active execution-run directory: none created by this planning turn.

User-selected coordinator: Sol/medium. Workers: Luna/low for audits and
Luna/medium for specified fixtures. Use a bounded Astra/high independent review
for difficult ownership, concurrency, unsafe code and execution release.
At most three children; one dispatcher owns serialized builds/guests.

Read START.md, then README.md once. Run:

```bash
python3 /home/holden/mckernel/scripts/run_os_goal.py
```

The same command resumes the launcher's exact saved thread. `--status` reads its
local snapshot, `--check` inspects configuration without inference, and `--hours 0`
removes the default 12-hour work window. See LAUNCH.md. The older chat goal is
separate and should remain paused while the launcher works. Planning checks:

```bash
python3 -B docs/verification/os-milestones-20260914/dispatch.py check
python3 -B docs/verification/os-milestones-20260914/dispatch.py task M00-A
```

Next actions:

1. M00-A: preserve and checkpoint preexisting untracked candidate/review files
   listed in source-index.json. Refresh HEAD/diff and active runtime identities.
2. M01-A: independently establish selected-response retention across SELECT,
   cancel/prepare/no-wake/publication/drop; current prepared mode2 guest blocked.
3. M02-A: review the existing Linux collector root-profile/orchestrator for its
   next controlled 25-case infrastructure run. M03-A source work is independent.

Starting counters: 273 drafted cases/97 packets, three compiled, zero catalog
executed/accepted; two of four fault-mode contracts accepted; production 350/10,000
points, six of 130 gates PASS; language gates zero complete, three IN_PROGRESS.
Keep historical scores and exact baseline module2026091301/image3 evidence.

Existing limits and runtime-release contracts remain authoritative. Planning
index checks establish coverage only. Existing goal status was usageLimited;
account quota blocked two prior agents. Start/resume the launcher after quota returns;
do not repeat failed spawns while quota is unavailable. Personal CLI config was
not changed. Its exact-repository trust requirement currently skips project
roles; the launcher supplies worker defaults explicitly. In this chat,
request child models and fresh bounded contexts directly.

After execution starts, the dispatcher updates this small pointer and keeps
append-only task/evidence events in a fresh run directory, including leases,
original failures, exact accepted inputs, remaining quota if available and the
next three ready tasks. Resume that record instead of repeating the full audit.
