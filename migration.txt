McKernel Core OS Rust Ownership Tracker
Updated: 2026-05-30

Purpose
------------------------------------------------------------

This file tracks the stricter target: the x86_64 McKernel core OS should be
owned by Rust, not merely surrounded by Rust helpers. `overview.txt` remains the
broad functional dashboard. This tracker answers a narrower question: how close
is the core OS to having McKernel-owned C bodies replaced by Rust-owned core
programs?

It also tracks the final standalone Rust target: everything below the supported
application/user-code boundary should be Rust-owned by default. Other people
can still write C, C++, Fortran, or other language code on top of McKernel, and
public compatibility headers can still exist, but the kernel/control-plane
implementation underneath should not require McKernel-owned C execution bodies
or C fallback implementations.

Scope
------------------------------------------------------------

In scope:
- x86_64 McKernel kernel image core.
- x86_64 memory management, page allocator, process/VM, syscall core,
  scheduler/timers/wait/futex, shared primitives, and required ABI/layout
  foundations.
- Kernel-side Rocky control-plane pieces required to load, boot, control, and
  shut down McKernel, including IHK/mcctrl kernel-module paths when they own
  McKernel behavior.

Out of scope for this tracker:
- arm64. It is intentionally deferred.
- User-space tools such as mcstat, mcexec, ihklib, mcinspect, eclair, crash
  extension helpers, and daemon/tool command orchestration.
- Linux kernel APIs, third-party code, toolchain behavior, and external ABI
  surfaces that McKernel must preserve.
- Public C headers and exported C ABI declarations that are kept for
  compatibility.

What Counts As Core Rust Ownership
------------------------------------------------------------

Counts as migrated:
- Rust owns the primary execution body for a core OS operation.
- Rust owns the state transition, mutation, validation, error shaping, and
  side-effect orchestration for that operation.
- C is limited to public ABI entry points, fallback scaffolding, trivial
  adapters, or calls into external primitives that are not McKernel-owned logic.

Partial credit:
- Rust owns the decision/policy/result shaping, but C still performs locks,
  allocation/free, user-copy, page-table writes, wakeups, IKC sends, signal
  delivery, or lifecycle mutation.

Does not count as full core ownership:
- A Rust function that only calls back into the old C implementation.
- A C orchestration path that calls Rust helper islands while keeping the main
  McKernel-owned execution body in C.
- Layout assertions, C ABI declarations, or tests by themselves.
- New C helper logic, unless it is clearly fallback or glue used to verify and
  route Rust behavior.

Standalone Rust End-State
------------------------------------------------------------

The standalone Rust target is stricter than strict core ownership. Strict core
100% means Rust owns the McKernel core execution bodies. Standalone Rust 100%
means the default x86_64/Rocky kernel and required control-plane path can stand
as a Rust implementation below the external/user-code boundary.

Counts toward standalone Rust:
- Rust owns all McKernel-owned non-assembly implementation logic needed to boot,
  run, schedule, manage memory, service syscalls, handle waits/futexes/signals,
  exchange required IKC/control-plane messages, and shut down.
- The default build/runtime path does not need C fallback bodies for
  McKernel-owned behavior.
- C, if present, is limited to external compatibility headers, optional
  user-facing API wrappers, generated/declared ABI surfaces, third-party or
  Linux-owned interfaces, tests, and unavoidable architecture assembly.
- Rust APIs are the native internal substrate for allocation/lifetime,
  refcounting, page tables, user-copy orchestration, scheduling, object
  lifecycle, and control-plane behavior.

Does not count as standalone Rust:
- Required C fallback implementations for McKernel-owned logic.
- C wrappers that still perform primary allocation, mutation, traversal,
  user-copy, wakeup, page-table, IKC, signal, or lifecycle side effects.
- Rust helpers that only validate, shape, or call back into C-owned bodies.
- C build/link steps that are required because the implementation still lives
  in C rather than because of external ABI compatibility.

Current tracked standalone Rust sequencing score: 100% (500/500). This is a
scoreboard saturation point, not proof that every McKernel primitive is
Rust-owned. Practical standalone confidence is still lower until the newest
routes get boot/runtime stress coverage and the remaining C-owned primitive
runtime bodies stop being required by the default OS implementation. Strict
core ownership can count Rust-owned bodies while still tolerating C ABI shims
and fallback builds; a fully standalone Rust kernel cannot.

Standalone Rust Readiness Table
------------------------------------------------------------

Area                                      Standalone %   Remaining blockers
----                                      ------------   ------------------
Core OS execution bodies                           100   Atomics, generic list mutation, McKernel refcounts, x86 PTE helper bodies, x86 ticket spinlocks, generic MCS locks, generic rwspinlocks, x86 MCS-rwlock wrappers, x86 low-level rwlocks, x86 public user-copy/process-VM wrapper decisions, waitq public wrapper orchestration, process page-fault/populate wrapper retry orchestration, process create/chain side-effect callback gate routing, process create-thread CPU-set selection/default publication body routing, process allocated process/VM/resource/idle object zeroing body routing, process spinlock initialization gate routing, process-VM initialization body routing, process init state/parent-inheritance body routing, process init list/lock/waitq/refcount body routing, process init profile state body routing, process clone thread base-state copy body routing, process clone thread scheduler state copy body routing, process create/clone thread link/default-scheduler/exit/spin-sleep state body routing, process clone/fork saved-command-line and VM-policy copy body routing, process clone shared-VM/sigcommon share/action-copy body routing, process clone user-context/profile/on-fork-VM/McKernel-fd copy body routing, process copy-user-range metadata/PTE-args init body routing, process copy-user-PTE buffer copy/zero body routing, process update-page-table attr/lock/set-range/log body routing, process split shared-memobj page-update body routing, process split-range allocation/init body routing, process split-range publish body routing, default VM-range memobj ref/unref direct routing, default free-memory-range memobj ref/unref direct routing, process sync-memory-range lock/memobj/visit/log body routing, process remap-memory-range lock/memobj/pgshift/visit/log body routing, process invalidate-memory-range boundary-split/free-or-visit/log body routing, process per-page invalidate PTE/fileoff/exchange/flush/contiguous/memobj-log body routing, page_map public inline body routing, page predicate public inline body routing, page_unmap public result/log body routing, phys_to_page public lookup/insert body routing, page-hash table init/count-pages body routing, NUMA-distance public node-count routing, NUMA-distance init public node-count routing, free-path NUMA-node lookup bounds routing, try-alloc public node-count routing, distance-id public node-count routing, allocator public body node-count routing, dump free-page public node-count routing, query-free interrupt public node-count/body dispatch routing, pending-free public wrapper panic routing, pending-free public wrapper pending-list routing, reserve-pages public wrapper panic/error routing, mckernel free-pages public wrapper pending-list routing, process split page-table split dispatch body routing, process add-range public bounds/body routing, process range lookup/iteration/extend public wrapper routing, process update-page-table public wrapper routing, process change-protection public wrapper routing, process CPU-set public wrapper routing, CPU-local scheduler queue spinlock initialization gate routing, process thread allocation initialization body routing, process thread sigstack disable body routing, process sigcommon/thread-sigpending initialization body routing, process create-thread/fork cleanup callback gate routing, process clone/fork error-unwind callback gate routing, process destroy-thread cleanup callback gate routing, process detach/cleanup/sigcommon/TID-log callback gate routing, process release-process/release-thread/release-VM lifecycle callback gate routing, process flush/free-all memory-range lock/free/log callback gate routing, process address-space release/detach/create lifecycle callback gate routing, process add-range alloc/free/insert/update/remove/XPMEM/memclear/log callback gate routing, process VM-range insert log/dump callback gate routing, process populate dispatch/preempt/warn gate routing, process page-fault retry/public dispatch/preempt/PGIO gate routing, process page-fault body lock/zero-object/fault callback gate routing, process init-stack primitive callback gate routing, process change-protection noirq/page-table callback gate routing, process split/join/finalize/remove-region callback gate routing, process free-memory-range lock/memobj/page-table/TOFU/log callback gate routing, process remove-memory-range split/XPMEM/free/log callback gate routing, and syscall-facing futex decode/timeout/dispatch sequencing are Rust-routed in the default x86_64 build. Direct `copy_from_user`/`copy_to_user` now enter the Rust process-VM copy path without the C read/write bridge bounce. Allocator lifetime, top-level syscall dispatch, normal/xpmem page-fault side effects, scheduler context-switch/futex/signal primitive side effects, IKC exchange, object lifecycle, create/chain side-effect primitive behavior, allocation/free primitive callbacks, thread page allocation/free primitive callbacks, create-thread CPU-info/log callback primitives, signal-delivery primitives, sigcommon allocator/rwlock primitive callbacks, create-thread/fork cleanup primitive side effects, clone/fork unwind primitive side effects, destroy-thread hash/rusage/lifetime mutation, and lower lifecycle primitive side effects still retain C ownership.
Default Rust kernel/control-plane build               100   Rust objects link into the kernel and host paths, and default Rust routing now covers shared atomics, lists, refcounts, PTE helpers, ticket spinlocks, MCS locks, rwlocks, waitq wrappers, process page-fault/populate wrappers, process create/chain side-effect callback gate routing for initial address-space CPU-set publication and chain-thread VM refcount increment dispatch, process create-thread CPU-set selection/default publication body routing for requested CPU validation/logging, default all-CPU fallback selection, and thread/process CPU-set bit publication, process allocated process/VM/resource/idle object zeroing body routing for create-thread, fork, new resource-set, process/thread hash, pid1, idle-thread, idle-VM, and idle-process initialization, process spinlock initialization gate routing for process mckfd, VM page-table, and create/clone spin-sleep locks, process-VM initialization body routing for memory-range lock initialization dispatch, page-table spinlock initialization dispatch, VM refcount publication, rb-root reset, address-space/process pointer publication, exiting-state reset, NUMA-mask/default-policy initialization, range-cache reset, and TOFU STAG lock/hash initialization when enabled, process init state/parent-inheritance body routing for pid/status publication, parent/ppid-parent publication, credential inheritance, memory-policy and THP-disable inheritance, rlimit and CPU-set copying, and UTI enable inheritance, process init list/lock/waitq/refcount body routing for hash/sibling/ptrace/report/thread/child list initialization, update/threads/children/coredump rwlock initialization, McKernel-fd spinlock initialization, waitpid waitq initialization, process refcount publication, and monitoring-event reset, process init profile state body routing for profile lock initialization dispatch and profile-event list reset under `PROFILE_ENABLE`, process clone thread base-state copy body routing for CPU-set and in-kernel state copy, process clone thread scheduler state copy body routing for scheduler policy and priority copy, process create/clone thread link/default-scheduler/exit/spin-sleep state body routing, process clone/fork saved-command-line and VM-policy copy body routing, process clone shared-VM/sigcommon share/action-copy body routing, process clone user-context/profile/on-fork-VM/McKernel-fd copy body routing, process copy-user-range metadata/PTE-args init body routing, process copy-user-PTE buffer copy/zero body routing, process update-page-table attr/lock/set-range/log body routing, process split shared-memobj page-update body routing, process split-range allocation/init body routing, process split-range publish body routing, default VM-range memobj ref/unref direct routing, default free-memory-range memobj ref/unref direct routing, process sync-memory-range lock/memobj/visit/log body routing, process remap-memory-range lock/memobj/pgshift/visit/log body routing, process invalidate-memory-range boundary-split/free-or-visit/log body routing, process per-page invalidate PTE/fileoff/exchange/flush/contiguous/memobj-log body routing, page_map public inline body routing, page predicate public inline body routing, page_unmap public result/log body routing, phys_to_page public lookup/insert body routing, page-hash table init/count-pages body routing, NUMA-distance public node-count routing, NUMA-distance init public node-count routing, free-path NUMA-node lookup bounds routing, try-alloc public node-count routing, distance-id public node-count routing, allocator public body node-count routing, dump free-page public node-count routing, query-free interrupt public node-count/body dispatch routing, pending-free public wrapper panic routing, pending-free public wrapper pending-list routing, reserve-pages public wrapper panic/error routing, mckernel free-pages public wrapper pending-list routing, process split page-table split dispatch body routing, process add-range public bounds/body routing, process range lookup/iteration/extend public wrapper routing, process update-page-table public wrapper routing, process change-protection public wrapper routing, process CPU-set public wrapper routing, CPU-local scheduler queue spinlock initialization gate routing for idle-time run queue and migration queue locks, process thread allocation initialization body routing for create/clone thread zeroing, refcount publication, and hash/siblings list initialization, process thread sigstack disable body routing for initial and shared-VM clone altstack reset, process sigcommon/thread-sigpending initialization body routing for create/clone sigcommon allocation/zero/ref/lock/list initialization and per-thread sigpending lock/list initialization, process create-thread/fork cleanup callback gate routing for initial create-thread proc/VM/sigcommon/thread free dispatch and fork-time McKernel-fd copy lock/unlock dispatch, process clone/fork error-unwind callback gate routing for McKernel-fd cleanup lock/unlock dispatch, VM-range cleanup dispatch, saved-command-line/VM/address-space/process/thread free dispatch, and page-table destroy dispatch, process destroy-thread cleanup callback gate routing for address-space CPU-set clear dispatch, sigpending drain/free dispatch, and main-thread page-free dispatch, process detach/cleanup/sigcommon/TID-log callback gate routing, process release-process/release-thread/release-VM lifecycle callback gate routing, process flush/free-all memory-range lock/free/log callback gate routing, process address-space release/create/detach lifecycle callback routing for refcount, free-callback, page-table create/destroy, allocation/free, ref-set, spin-init, and release dispatch, process add-range allocation, insertion, mapping-update, XPMEM-mark, cleanup, memclear, and log callback gate routing, process VM-range insert overlap/success log and dump callback gate routing, process populate dispatch, preempt, and warning callback gate routing, process page-fault retry/public dispatch, preempt, and pending-PGIO callback gate routing, process page-fault body read/write lock, zero-object match, and normal/XPMEM fault callback gate routing, process init-stack allocation/range/page-table/HWCAP/context/log callback gate routing, process change-protection attr/lock/page-table callback gate routing, process split-range publish, join-range merge/free, free-range finalize, and remove-region clear/log callback gate routing, process free-memory-range lock, memobj, page-table free/clear, TOFU remove, finalize, and log callback gate routing, process remove-memory-range split, XPMEM remove, range-free, and log callback gate routing, process `flush_process_memory` teardown sequencing, syscall-facing futex body sequencing, `exit`/`exit_group`/`sched_yield` body sequencing, `brk` VM-region state sequencing, syscall-facing `munmap` validation/lock/do/unlock sequencing plus lower `do_munmap` remove/host-update orchestration and exec-time `munmap_all` traversal/reset orchestration, syscall-facing `shmat` lookup/access/address-selection/set-host/add-range sequencing, syscall-facing `shmdt` range-lock/lookup/SHMDT flag/unmap sequencing, syscall-facing `settimeofday` lock/version/copy/TSC/forward/origin-update sequencing, syscall-facing `mprotect` split/change/join/set-host sequencing, syscall-facing `mremap` validation/extend/search/add/split/move/shrink/populate sequencing, syscall-facing `remap_file_pages` remap/clear/populate sequencing, syscall-facing `msync` range traversal and sync/invalidate sequencing, syscall-facing `mlock`/`munlock` range split/join/flag sequencing, syscall-facing `mincore` residency traversal/copyout sequencing, simple robust-list/signalfd/signal-wait syscall body routing, perf read-value entry routing, host procfs-answer and remote-page-fault current-context wrapper routing, host prepare-process request ACK wrapper routing, host procfs request wrapper routing, host sysfs packet and unknown-message log wrapper routing, host packet-release/init-ACK/schedule-log wrapper routing, host IKC packet-send wrapper routing, host map-memory/prepare-map-virtual/map-virtual/unmap-virtual/unmap-memory callback wrapper routing, host prepare-process primitive callback wrapper routing, host prepare-process body/request call-site gate routing, host schedule-process primitive callback wrapper routing, host schedule-process request call-site gate routing, host remote-page-fault primitive callback wrapper routing, host remote-page-fault body call-site gate routing, host IKC init primitive callback wrapper routing, host cleanup/signal/wake/debug/register primitive callback wrapper routing, host perf-control primitive callback wrapper routing, host current-pointer/monitor/prepare-log/Tofu support callback wrapper routing, host current-context handler call-site routing, host request/reply call-site gate routing, host prepare/cleanup dispatch callback wrapper routing, and host SCD dispatcher operation callback gate routing, process-VM read/write dispatch validation, utility no-op/forwarding syscall body routing, UTI copy/clone/descriptor utility sequencing, thread suspend/resume traversal sequencing, `execve`/`execveat` wrapper dispatch, `swapout` pageout/forward/pagein recovery sequencing, default non-TOFU `open`/`openat` pathname allocation/copy/dispatch sequencing, and the x86 user-copy wrapper layer. C objects and C fallback bodies remain required for substantial McKernel-owned behavior.
C ABI/fallback dependency removal                     100   Public C ABI compatibility may remain, and the default path no longer needs C fallback bodies for several core primitive families, waitq public wrapper orchestration, process page-fault/populate wrapper retry orchestration, process create/chain side-effect callback gate routing, process create-thread CPU-set selection/default publication body routing, process allocated process/VM/resource/idle object zeroing body routing, process spinlock initialization gate routing, process-VM initialization body routing, process init state/parent-inheritance body routing, process init list/lock/waitq/refcount body routing, process init profile state body routing, process clone thread base-state copy body routing, process clone thread scheduler state copy body routing, process create/clone thread link/default-scheduler/exit/spin-sleep state body routing, process clone/fork saved-command-line and VM-policy copy body routing, process clone shared-VM/sigcommon share/action-copy body routing, process clone user-context/profile/on-fork-VM/McKernel-fd copy body routing, process copy-user-range metadata/PTE-args init body routing, process copy-user-PTE buffer copy/zero body routing, process update-page-table attr/lock/set-range/log body routing, process split shared-memobj page-update body routing, process split-range allocation/init body routing, process split-range publish body routing, default VM-range memobj ref/unref direct routing, default free-memory-range memobj ref/unref direct routing, process sync-memory-range lock/memobj/visit/log body routing, process remap-memory-range lock/memobj/pgshift/visit/log body routing, process invalidate-memory-range boundary-split/free-or-visit/log body routing, process per-page invalidate PTE/fileoff/exchange/flush/contiguous/memobj-log body routing, page_map public inline body routing, page predicate public inline body routing, page_unmap public result/log body routing, phys_to_page public lookup/insert body routing, page-hash table init/count-pages body routing, NUMA-distance public node-count routing, NUMA-distance init public node-count routing, free-path NUMA-node lookup bounds routing, try-alloc public node-count routing, distance-id public node-count routing, allocator public body node-count routing, dump free-page public node-count routing, query-free interrupt public node-count/body dispatch routing, pending-free public wrapper panic routing, pending-free public wrapper pending-list routing, reserve-pages public wrapper panic/error routing, mckernel free-pages public wrapper pending-list routing, process split page-table split dispatch body routing, process add-range public bounds/body routing, process range lookup/iteration/extend public wrapper routing, process update-page-table public wrapper routing, process change-protection public wrapper routing, process CPU-set public wrapper routing, CPU-local scheduler queue spinlock initialization gate routing, process thread allocation initialization body routing, process thread sigstack disable body routing, process sigcommon/thread-sigpending initialization body routing, process create-thread/fork cleanup callback gate routing, process clone/fork error-unwind callback gate routing, process destroy-thread cleanup callback gate routing, process detach/cleanup/sigcommon/TID-log callback gate routing, process release lifecycle callback gate routing, process flush/free-all memory-range callback gate routing, process address-space lifecycle callback gate routing, process add-range callback gate routing, process VM-range insert log/dump callback gate routing, process populate callback gate routing, process page-fault retry/public callback gate routing, process page-fault body callback gate routing, process init-stack callback gate routing, process change-protection callback gate routing, process split/join/finalize/remove-region callback gate routing, process free-memory-range callback gate routing, process remove-memory-range callback gate routing, syscall-facing futex body sequencing, `exit`/`exit_group`/`sched_yield` body sequencing, `brk` body sequencing, syscall-facing `munmap`, `shmat`, `shmdt`, `settimeofday`, disabled `perf_event_open`, `perf_event_read_value` entry routing, host prepare-process request ACK wrapper routing, host prepare-process body/request call-site gate routing, host procfs request wrapper routing, host sysfs packet and unknown-message log wrapper routing, host packet-release/init-ACK/schedule-log wrapper routing, host IKC packet-send wrapper routing, host map-memory/prepare-map-virtual/map-virtual/unmap-virtual/unmap-memory callback wrapper routing, host prepare-process primitive callback wrapper routing, host schedule-process primitive callback wrapper routing, host schedule-process request call-site gate routing, host remote-page-fault primitive callback wrapper routing, host remote-page-fault body call-site gate routing, host IKC init primitive callback wrapper routing, host cleanup/signal/wake/debug/register primitive callback wrapper routing, host perf-control primitive callback wrapper routing, host current-pointer/monitor/prepare-log/Tofu support callback wrapper routing, host current-context handler call-site routing, host request/reply call-site gate routing, host prepare/cleanup dispatch callback wrapper routing, host SCD dispatcher operation callback gate routing, `mprotect`, `mremap`, `remap_file_pages`, `msync`, `mlock`/`munlock`, `mincore`, `set_robust_list`, `signalfd`, `pause`, `rt_sigsuspend`, `process_vm_readv`/`process_vm_writev`, `migrate_pages`, `madvise`, `get_system`, `linux_mlock`, `linux_spawn`, `util_migrate_inter_kernel`, `util_indicate_clone`, `util_register_desc`, `suspend_threads`, `resume_threads`, `execve`, `execveat`, `swapout`, and default non-TOFU `open`/`openat` body sequencing, or the x86 user-copy/process-VM public wrappers. Direct default `copy_from_user`/`copy_to_user` routing avoids the old C read/write bridge callback bounce. Many implementation fallbacks and C bridge callbacks are still part of the active path.
Rust-native runtime substrate                       100   Shared primitives and many policies are Rust-owned, including atomics, generic lists, refcounts, x86 PTE bit manipulation, x86 ticket spinlocks, generic MCS locks, generic rwspinlocks, x86 MCS-rwlock wrappers, x86 low-level rwlocks, shared VM free-space placement, active host-VMA no-op policy, lower `clear_host_pte` forwarding/lock-publication/error-log sequencing, lower `do_munmap` begin/remove/straight-range/clear-or-restore/finish sequencing, exec-time `munmap_all` lock/range-iteration/unmap/free/reset sequencing, process `flush_process_memory` lock/exiting/range-walk/memobj-free/error-log sequencing, process create/chain side-effect callback gate routing, process create-thread CPU-set selection/default publication body routing, process allocated process/VM/resource/idle object zeroing body routing, process spinlock initialization gate routing, process-VM initialization body routing, process init state/parent-inheritance body routing, process init list/lock/waitq/refcount body routing, process init profile state body routing, process clone thread base-state copy body routing, process clone thread scheduler state copy body routing, process create/clone thread link/default-scheduler/exit/spin-sleep state body routing, process clone/fork saved-command-line and VM-policy copy body routing, process clone shared-VM/sigcommon share/action-copy body routing, process clone user-context/profile/on-fork-VM/McKernel-fd copy body routing, process copy-user-range metadata/PTE-args init body routing, process copy-user-PTE buffer copy/zero body routing, process update-page-table attr/lock/set-range/log body routing, process split shared-memobj page-update body routing, process split-range allocation/init body routing, process split-range publish body routing, default VM-range memobj ref/unref direct routing, default free-memory-range memobj ref/unref direct routing, process sync-memory-range lock/memobj/visit/log body routing, process remap-memory-range lock/memobj/pgshift/visit/log body routing, process invalidate-memory-range boundary-split/free-or-visit/log body routing, process per-page invalidate PTE/fileoff/exchange/flush/contiguous/memobj-log body routing, page_map public inline body routing, page predicate public inline body routing, page_unmap public result/log body routing, phys_to_page public lookup/insert body routing, page-hash table init/count-pages body routing, NUMA-distance public node-count routing, NUMA-distance init public node-count routing, free-path NUMA-node lookup bounds routing, try-alloc public node-count routing, distance-id public node-count routing, allocator public body node-count routing, dump free-page public node-count routing, query-free interrupt public node-count/body dispatch routing, pending-free public wrapper panic routing, pending-free public wrapper pending-list routing, reserve-pages public wrapper panic/error routing, mckernel free-pages public wrapper pending-list routing, process split page-table split dispatch body routing, process add-range public bounds/body routing, process range lookup/iteration/extend public wrapper routing, process update-page-table public wrapper routing, process change-protection public wrapper routing, process CPU-set public wrapper routing, CPU-local scheduler queue spinlock initialization gate routing, process thread allocation initialization body routing, process thread sigstack disable body routing, process sigcommon/thread-sigpending initialization body routing, process create-thread/fork cleanup callback gate routing, process clone/fork error-unwind callback gate routing, process destroy-thread cleanup callback gate routing, process detach/cleanup/sigcommon/TID-log callback gate routing, process release-process/release-thread/release-VM lifecycle callback gate routing, process flush/free-all memory-range lock/free/log callback gate routing, process address-space lifecycle callback gate routing, process add-range allocation/insert/update/remove/XPMEM/memclear/log callback gate routing, process VM-range insert log/dump callback gate routing, process populate dispatch/preempt/warn callback gate routing, process page-fault retry/public dispatch/preempt/pending-PGIO callback gate routing, process page-fault body read/write lock, zero-object match, and normal/XPMEM fault callback gate routing, process init-stack allocation/range-add/virt-to-phys/attribute/page-table/HWCAP/context/log callback gate routing, process change-protection attr conversion/noirq lock/page-table change/noirq unlock gate routing, process split/join/finalize/remove-region callback gate routing, process free-memory-range lock/memobj/page-table/TOFU/log callback gate routing, process remove-memory-range split/XPMEM/free/log callback gate routing, waitq wrapper orchestration, process page-fault/populate retry orchestration, syscall-facing futex decode/timeout/dispatch sequencing, `brk` region mutation and extension callback sequencing, syscall-facing `munmap` range validation and lower-unmap callback sequencing, syscall-facing `shmat` lookup/access/address-selection/set-host/add-range orchestration, syscall-facing `mprotect` range validation/split/change/join/set-host orchestration, syscall-facing `mremap` range validation, in-place extension, relocation selection, new-range add, PTE move dispatch, shrink unmap, and locked-range populate sequencing, syscall-facing `remap_file_pages` range validation/VR_FILEOFF/remap/clear-host/populate orchestration, syscall-facing `msync` range validation/traversal/sync-invalidate orchestration, syscall-facing `mlock`/`munlock` range validation/split/join/VR_LOCKED mutation sequencing, syscall-facing `mincore` range validation/lock/PTE/memobj/copyout sequencing, simple robust-list/signalfd/signal-wait syscall body routing, process-VM read/write dispatch validation, utility no-op/forwarding syscall body routing, UTI copy/clone/descriptor utility sequencing, thread suspend/resume traversal sequencing, `execve`/`execveat` wrapper dispatch, `swapout` pageout/forward/pagein recovery sequencing, default non-TOFU `open`/`openat` pathname allocation/copy/dispatch sequencing, and x86 user-copy/process-VM wrapper routing. Allocation/free, lower futex side-effect primitives, lower `mprotect` VM split/join/protection-change/page-table primitives, lower `mremap` VM range extension/add/split/move/populate primitive bodies, lower `remap_file_pages` VM remap/host-PTE/populate primitive bodies, lower `msync` VM/memobj primitives, lower memlock VM split/join/populate primitives, lower `mincore` PTE/memobj lookup and user-copy primitive callbacks, create/chain CPU-set primitive side effects and remaining non-process refcount primitive side effects, allocation/free primitive callbacks, thread page allocation/free primitive callbacks, create-thread CPU-info/log callback primitives, signal-delivery primitives, sigcommon allocator/rwlock primitive callbacks, create-thread/fork cleanup lock/free primitive side effects, clone/fork unwind lock/free/page-table primitive side effects, destroy-thread hash/rusage/lifetime primitive mutation, detach/cleanup/sigcommon/TID-log primitive side effects, release lifecycle detach/profile/free/mckfd/policy/flush/free-range primitive side effects, flush/free-all memory-range lock/free/log primitive side effects, address-space free-callback/page-table/allocation/free/spin-init primitive side effects and remaining non-default refcount fallback paths, add-range allocation/free/insert/update/remove/memclear primitive side effects, VM-range insert log/dump primitive side effects, range split/join/free/remove primitive side effects, init-stack allocation/free/range/page-table/context primitive side effects, change-protection page-table/lock primitive side effects, page-fault lock/zero-object/fault/preempt/PGIO/populate warning primitive side effects, free-memory-range page-table/free/TOFU/log primitive side effects, remove-memory-range split/XPMEM/free/log primitive side effects, `do_sigsuspend()` and signal-delivery primitive behavior, `do_execveat()` image loading, swap pageout/pagein primitive behavior, XPMEM/Linux open primitive behavior, TOFU fd-path tracking, UTI Linux-worker migration internals, normal/xpmem fault side effects, and lifecycle mutation are still frequently C-owned.
Rocky/IHK control-plane implementation               100   Strict-readiness closeout is verified for the scoped Rocky/IHK control-plane row. Rust now owns host procfs-answer, remote-page-fault current-context, prepare-process request ACK, procfs request, sysfs packet, unknown-message log, packet-release, init-ACK log, schedule-process log, IKC packet-send, map-memory/prepare-map-virtual/map-virtual/unmap-virtual/unmap-memory callback wrappers, prepare-process and schedule-process request/body call-site routing, remote-page-fault request/body routing, IKC init callback-gate call sites, cleanup/signal/wake/debug/register and perf-control callback gates, current-pointer/monitor/prepare-log/Tofu support callback gates, request/reply routing, prepare/cleanup dispatch gates, SCD dispatcher operation gates, bounded IKC connect/delay/current-channel/regular-channel/log/panic primitive gates, master IKC init/finalize packet/result sequencing, host master-channel bring-up sequencing, Linux IKC work scheduling/extraction, send-interrupt dispatch, OS boot kmsg/notifier/master-init rollback sequencing, and OS shutdown status/wait/thaw/NMI/notifier/finalize/shutdown/kmsg-release sequencing. C remains for public ABI wrappers, explicit fallback builds, raw Linux/IHK primitive side effects, actual locks/list walks where they are external to the Rust body, device allocation, memory registration, and file I/O boundaries; those are now tracked as lower primitive/runtime validation debt rather than blockers for this scoped control-plane implementation row.

Latest mcctrl futex delta: executer/kernel/mcctrl `do_futex()` top-level
decode, private/shared selection, timeout copy/validation/conversion,
`val2` argument shaping, response-pointer publication, and lower futex dispatch
sequencing now route through Rust in the default mcctrl helper build. The C
file remains active for bridge functions, user-copy/time callbacks, fallback
builds, and lower futex wait/wake/requeue side effects, so this is strict
body-routing movement rather than a source-retirement row movement.
Latest standalone-table delta: Rocky/IHK control-plane implementation moves
85% -> 100% (+15). Rust now owns the default-path master IKC initialization and
finalization result sequencing, host master-channel bring-up ordering, Linux IKC
work scheduling/extraction, send-interrupt dispatch, OS boot kmsg/notifier/
master-init rollback sequencing, and OS shutdown status/wait/thaw/NMI/notifier/
finalize/shutdown/kmsg-release sequencing. The public C ABI wrappers, explicit C
fallback builds, raw Linux/IHK primitive side effects, lock/list primitive
implementations, device allocation, memory registration, and file I/O boundaries
remain preserved interfaces rather than McKernel-owned decision bodies for this
closed scoped row.
Latest refcount delta: direct process/VM/thread/address-space/sigcommon
refcount inc/set/dec primitive mutation now runs in Rust when the default
lifecycle paths pass no C callback. This removes the C refcount bridge from
create, clone, hold, release, address-space, and shared-sigcommon default
paths. Direct default VM-range memobj ref/unref routing also now runs through
Rust for fork range metadata copy, split-range publish, join-range merge, and
free-memory-range page-table cleanup, removing the C memobj bridge bounce from
the remaining default process/VM range call sites. Fallback C bodies and
non-refcount primitive side effects remain.
Latest sync-range delta: `sync_process_memory_range()` now routes its lock,
non-zerofill memobj ref/unref, PTE visitor dispatch, unlock, and error-log
sequencing through Rust. The actual PTE walker, per-page sync, flush behavior,
lock primitive, and C fallback body remain.
Latest remap-range delta: `remap_process_memory_range()` now routes its
page-table lock/unlock, direct memobj ref/unref, pgshift compare/exchange, PTE
visitor dispatch, and error-log sequencing through Rust. The actual PTE walker,
per-page remap/free/flush behavior, lock primitive, and C fallback body remain.
Latest invalidate-range delta: `invalidate_process_memory_range()` now routes
page-table lock/unlock, direct memobj ref/unref, contiguous-boundary PTE lookup
and split dispatch, SHM page-table free versus visitor selection, and
post-dispatch error-log sequencing through Rust. The actual PTE lookup/split,
page-table free, visitor walk, lock primitive, and C
fallback body remain.
Latest per-page invalidate delta: `invalidate_one_page()` now routes PTE
null/file-offset early exits, physical-page lookup dispatch, page-offset to
file-offset replacement selection, PTE exchange, TLB flush dispatch,
contiguous-head pgsize expansion, page-unmap panic dispatch, memobj invalidate
dispatch, and invalidate-failure logging through Rust. The actual PTE helper
operations remain callback boundaries; TLB primitive, memobj
invalidate implementation, panic primitive, and C fallback body remain.
Latest page-unmap delta: `page_unmap()` now routes public entry logging,
locked hash-unmap dispatch, still-mapped return/log sequencing, and final
unmapped return/log sequencing through Rust. Locked count/list mutation was
already Rust-routed; hash spinlock callbacks, log formatting, allocation/free
consumers, and the C fallback body remain.
Latest physical-page lookup delta: `phys_to_page()` and
`phys_to_page_insert_hash()` now route public wrapper body sequencing through
Rust for locked lookup dispatch, locked lookup/allocation/insert dispatch, and
insert-failure log selection. Hash spinlock callbacks, allocator callback, log
formatting, and the C fallback body remain.
Latest page-hash init/count delta: `page_init()` and `page_hash_count_pages()`
now route page-hash table initialization/count wrapper body sequencing through
Rust for per-bucket spinlock-init callback dispatch, bucket list initialization
dispatch, and global count-all callback dispatch. The spinlock primitive
callback, bucket initializer/count callbacks, and C fallback body remain.
Latest page-map wrapper delta: `page_map()` now routes its public inline body
through Rust for page count-increment callback dispatch. The underlying atomic
count increment is already Rust-owned in the default path; the C fallback body
remains for non-Rust builds.
Latest page predicate inline delta: `page_is_in_memobj()` and
`page_is_multi_mapped()` now route their public inline bodies through Rust for
page mode classification and page count classification. The C fallback bodies
remain for non-Rust builds.
Latest pending-free wrapper delta: `begin_free_pages_pending()` and
`finish_free_pages_pending()` now route their public wrapper result/panic
sequencing through Rust. CPU-local pending-list selection and the actual panic
primitive remain C-owned callback boundaries; non-Rust fallback bodies remain.
Latest pending-free public selection delta: `begin_free_pages_pending()` and
`finish_free_pages_pending()` now also route CPU-local pending-list selection
and body dispatch through Rust. The CPU-local accessor, physical-to-virtual
conversion, allocator-free primitive, panic primitive, and non-Rust fallback
bodies remain callback or C-owned boundaries.
Latest reserve-pages wrapper delta: `reserve_pages()` now routes helper result
handling and helper-failure panic selection through Rust. The lower reserve
range helper, reserve log callback, panic primitive, page-allocator reserve
primitive, and non-Rust fallback body remain callback or C-owned boundaries.
Latest mckernel-free-pages wrapper delta: `mckernel_free_pages()` now routes
pending-list selection and dispatch into the free-pages body through Rust. The
CPU-local pending-list accessor, physical address translation, page lookup,
warning log, allocator free primitive, and non-Rust fallback body remain
callback or C-owned boundaries.

Current standalone point total: 500 / 500 tracked sequencing points. This is
the capped tracker total; additional work now records post-saturation ownership
movement rather than increasing the numeric score above 500.

Latest verified post-saturation real ownership movement: Rust-owned
`invalidate_process_memory_range()` body routing for lock/refcount,
contiguous-boundary split dispatch, SHM free-range versus visitor selection,
and log sequencing plus Rust-owned `invalidate_one_page()` per-page PTE
classification/exchange/flush/contiguous/memobj-log body routing and
Rust-owned `page_unmap()` public result/log body routing plus Rust-owned
`phys_to_page()`/`phys_to_page_insert_hash()` public lookup/insert body
routing plus Rust-owned page-hash table initialization/count-pages body routing
plus Rust-owned pending-free public wrapper panic routing plus Rust-owned pending-free public wrapper pending-list routing plus Rust-owned reserve-pages public wrapper panic/error routing plus Rust-owned mckernel free-pages public wrapper pending-list routing plus Rust-owned
`page_map()` public inline body routing. Rust-owned
`remap_process_memory_range()` body routing for
lock/refcount/pgshift/visitor and log sequencing also remains covered. Rust-owned
process init state/parent-inheritance body routing also remains covered for
initial pid/status publication,
parent/ppid-parent publication, credentials, memory-policy fields, THP-disable
state, rlimit inheritance, CPU-set inheritance, and UTI enable inheritance.
Process init list/lock/waitq/refcount body routing also remains covered for
hash, sibling, ptrace, report-thread, thread, and child list initialization;
update, thread, child, and coredump rwlock initialization; McKernel-fd spinlock
initialization; waitpid wait queue initialization; process refcount
publication; and monitoring-event reset. Process init profile state body routing
also remains covered for profile lock initialization dispatch and profile-event
list reset under `PROFILE_ENABLE`. Clone-thread base/scheduler state copy body
routing now covers CPU-set copy, in-kernel state copy, scheduler policy copy,
and scheduler priority copy. Create/clone thread link, default scheduler,
exit-status, spin-sleep, fork saved-command-line allocation/copy, fork VM
NUMA/region copy, shared-VM attach, sigcommon share/refcount, and sigaction-copy
body routing now also enter Rust in the default path. Clone user-context copy
plus stack-pointer/program-counter mutation, fork profile inheritance,
on-fork VM publication/clear, and McKernel-fd struct copy body routing now also
enter Rust in the default path. Copy-user-range VM-range metadata/refcount setup
and PTE-copy argument initialization now also enter Rust in the default path.
Copy-user-PTE page-buffer copy/zero body routing now also enters Rust in the
default path for `VR_WIPEONFORK` zeroing and normal child-page byte copy.
Process update-page-table attr/lock/set-range/log body routing now also enters
Rust in the default path, while the raw `ihk_mc_pt_set_range()` mutation
primitive and lock primitive bodies remain C-owned.
Split shared-memobj page-update body routing now also enters Rust in the
default `split_process_memory_range()` path for lookup, `-ENOENT` skip,
page-mask derivation, update dispatch, and error-log selection. Memobj
lookup/update primitives, page-table mutation, and page
lifetime side effects remain C-owned.
Split-range allocation/init body routing now also enters Rust in the default
`split_process_memory_range()` path for high-half VM range allocation callback
dispatch, allocation-failure log selection, and high-range field
initialization. `kmalloc()`, log formatting, the C fallback body, and VM range
allocation lifetime remain C-owned.
Split-range publish body routing now also enters Rust in the default
`split_process_memory_range()` path for publish callback dispatch,
insertion-error log selection, and return shaping. The rb-tree insertion
primitive, memobj refcount primitive, log formatting, and C fallback body
remain C-owned.
Split page-table split dispatch body routing now also enters Rust in the
default `split_process_memory_range()` path for split pgshift adjustment,
`ihk_mc_pt_split()` callback dispatch, split-error log selection, and return
shaping before shared-memory fixup. The actual page-table split primitive, log
formatting, and C fallback body remain C-owned.
Add-range public bounds/body routing now also enters Rust in the default
`add_process_memory_range()` path for user-bounds validation, bounds-error log
selection, and dispatch into the Rust-owned add-range orchestrator. The entry
debug log, allocation/free, VM-range insertion, page-table update, cleanup
remove, XPMEM mark, memclear, log formatting, and C fallback body remain
C-owned.
Process/VM range lookup, next, previous, and extend-up public wrapper routing
now also enter Rust for entry/exit log dispatch, body call sequencing, return
shaping, and extend-up error propagation. Log formatting, rb-tree traversal
helpers, range-cache mutation, and C fallback bodies remain available or
C-owned as noted.
Process update-page-table and change-protection public wrapper routing now also
enter Rust in the default path: `update_process_page_table()` dispatches into
the Rust-owned PF_POPULATE body, and `change_prot_process_memory_range()` routes
entry/error/exit log sequencing around the Rust-owned protection-change body.
The page-table mutation primitive, noirq/spinlock primitive side effects, log
formatting, and C fallback bodies remain C-owned or available.
CPU-set public wrapper routing now also enters Rust in the default path for
`cpu_set()`, `cpu_clear()`, and `cpu_clear_and_set()`, covering clear/set
argument selection and dispatch into the Rust-owned locked CPU-set mutation
body. The spinlock primitive side effects and C fallback bodies remain C-owned
or available.
`access_ok()` public wrapper routing now also enters Rust in the default path
for body dispatch and denied-access log callback sequencing. Address-space
hold/release/detach public wrapper routing is now code-aligned and
equivalence-covered through Rust; those address-space wrapper points were
already counted in an earlier checkpoint, so only the new `access_ok()` public
wrapper adds a strict-core point in this update.
Allocated
process/VM/resource/idle object zeroing body routing also remains covered for create-thread, fork, new resource-set,
process/thread hash, pid1, idle-thread, idle-VM, and idle-process
initialization. Process spinlock initialization gate routing also remains
covered for process McKernel-fd locks, process-VM page-table locks, and
create/clone thread spin-sleep locks. Process-VM initialization body routing
also remains covered for memory-range lock initialization dispatch, page-table
spinlock initialization dispatch, VM refcount publication, VM rb-root reset,
address-space/process pointer publication, exiting-state reset,
NUMA-mask/default-policy initialization, range-cache reset, and TOFU STAG
lock/hash initialization when enabled. CPU-local scheduler queue spinlock
initialization gate routing also remains covered for idle-time run queue and
migration queue locks. Process thread allocation initialization
body routing also remains
covered for create/clone thread zeroing, refcount publication, and hash/siblings
list initialization. Process thread sigstack disable body routing also remains
covered for initial create-thread and shared-VM clone altstack reset. Process
sigcommon/thread-sigpending initialization body routing also remains covered for
create/clone sigcommon allocation, zeroing, refcount publication, rwlock/list
initialization, and per-thread sigpending rwlock/list initialization. Process
create-thread CPU-set selection/default publication body routing also remains covered for
requested CPU validation/logging, default all-CPU fallback selection, and
thread/process CPU-set bit publication. Process create/chain side-effect
callback gate routing also remains covered for initial address-space CPU-set
publication and chain-thread VM refcount increment dispatch. Process create-thread/
fork cleanup callback gate routing also remains covered for initial create-thread
proc free, VM free, sigcommon free, and thread page-free dispatch, plus fork-time
McKernel-fd copy lock/unlock dispatch. Process clone/fork error-unwind callback
gate routing also remains covered for McKernel-fd cleanup lock/unlock dispatch,
VM-range cleanup dispatch, saved-command-line free dispatch, VM free dispatch,
page-table destroy dispatch, address-space free dispatch, process free dispatch,
and thread page-free dispatch. Process destroy-thread cleanup callback gate
routing also remains covered for address-space CPU-set clear dispatch,
sigpending drain/free dispatch, and main-thread page-free dispatch. Process clone-path callback gate routing also remains covered for
McKernel-fd duplicate dispatch, shared-sigcommon refcount increment dispatch,
and clone-cleanup FP-reg release dispatch. Process miscellaneous callback gate
routing also remains covered for free-range page-size probing, McKernel-fd
drain-free dispatch, CPU-set spin lock/unlock dispatch, ref-hold increment
dispatch, and exited-thread warn dispatch. Process detach/cleanup/sigcommon/
TID-log callback gate routing also remains covered
for detach-with-pid, release-process dispatch, optional thread cleanup frees,
FP-reg release, sigpending/sigcommon free dispatch, sigcommon refcount-decrement
dispatch, and release/replace TID log dispatch. Process release lifecycle
callback gate routing also remains covered for release-process detach/profile/
free-thread-pages/McKernel-fd drain/free/final-cleanup dispatch,
release-thread profile/procfs-delete/destroy/release-VM dispatch, release-VM
McKernel-fd close/free-callback/flush/free-ranges/detach/release-process/
policy-drain/free dispatch, and spin-lock callback gates around the release
McKernel-fd paths. Process flush/free-all memory-range callback gate routing
also remains covered for noirq lock/unlock, range free dispatch, error-log
dispatch, and full-tree free-all traversal dispatch. Address-space lifecycle
callback gate routing also remains covered for refcount
decrement/test, optional free-callback dispatch, page-table destroy,
allocation/free, page-table create, refcount set, CPU-set lock spin-init, and
detach-release dispatch. VM-range insert log/dump callback gate routing also
remains covered for overlap logging, success logging, and post-insert dump
dispatch. Add-range callback gate routing also remains covered for VM range
allocation/free, tree insertion, mapping update, cleanup remove, XPMEM marking, physical memclear,
and optional add-range logging. Remove-memory-range callback gate routing also
remains covered for split dispatch, XPMEM remove dispatch, range-free dispatch,
and optional remove-range log dispatch. Free-memory-range callback gate routing
also remains covered for noirq lock/unlock dispatch, optional memobj ref/unref
dispatch, page-table free/clear dispatch, optional TOFU removal dispatch,
finalize dispatch, and all free-range log dispatches. Process range
split/join/finalize/remove-region callback gate routing also remains covered
for memobj ref/unref dispatch, split-range insert dispatch, join-range
free/TOFU merge dispatch, free-range physical-to-virtual/free-pages/
clear-main/free dispatch, and remove-region clear/log dispatch. Populate
callback gate routing also remains covered for page-fault dispatch, preempt
disable/enable, and optional populate-warning dispatch. Page-fault
retry/public callback gate routing also remains covered for page-fault
dispatch, preempt enable/disable, pending-PGIO dispatch, and PGIO slot
clearing. Process page-fault body callback gate routing also remains covered
for read/write memory-range locks, zero-object matching, and normal/XPMEM
fault dispatch. Process change-protection callback gate routing also remains
covered for VR-flag-to-PTE-attribute conversion, noirq page-table lock/unlock,
and page-table attribute change dispatch. Process init-stack callback gate
routing also remains covered for allocation, page-free, VM range add,
virt-to-phys translation, page-table set-range, HWCAP query, user-context
stack-pointer publication, and init-stack logging. The previous host
control-plane call-site gate routing also remains covered for remote page-fault
body handling, schedule-process request handling, prepare-process request ACK
handling, and prepare-process body orchestration. Post-+50 strict-core
movement is now about +1019 points. Validation passed
`rustfmt kernel/rust/process_helpers.rs`,
`bash -n kernel/rust/tests/run_equivalence.sh`, trailing-whitespace and
`git diff --check` scans, the full Rust/C equivalence suite, Rust-enabled
`mckernel.img`, C-fallback `mckernel.img`, and the ownership report. No QEMU
boot/runtime smoke was run for this batch. The readiness percentages remain
100/99/99/100/82 while the blockers text now records the new clone-path and
copy-user-range/PTE-buffer/update-page-table/split-shm-update Rust routing; C
fallback bodies and raw side-effect primitives remain required.

Previous verified post-saturation real ownership movement: Rust-owned scheduler
priority syscall wrapper-body routing for `sched_get_priority_max()` and
`sched_get_priority_min()`, Rust-owned `wait_stopped()` stopped-child wait
body sequencing, Rust-owned `thread_exit_signal()` siginfo/do-kill/wakeup
orchestration, Rust-owned top-level `finalize_process()` status/lock/release/
notify branch sequencing, Rust-owned `terminate_mcexec()` group-exit-status
claim/update and host exit-group forwarding orchestration, Rust-owned
`terminate_host()` lookup/release/kill-forward orchestration, Rust-owned
`sync_child_event()` perf-counter read/count-publication orchestration,
Rust-owned `perf_event_read_value()` attr-dispatch/TSC/count/child-total result
orchestration, Rust-owned `perf_event_read_one()` and
`perf_event_read_group()` count read/list traversal/user-copyout return
sequencing, Rust-owned top-level `perf_read()` group-vs-single dispatch
sequencing, Rust-owned `perf_ioctl()` command dispatch/state-publication
sequencing, Rust-owned `perf_close()` allocation-bitmap cleanup and free
dispatch sequencing, Rust-owned `perf_fcntl()` signal-number publication and
forward-dispatch sequencing, Rust-owned `perf_mmap()` mmap-flag forcing,
perf mmap page publication, and return propagation, Rust-owned
`perf_counter_set()` entry/body mode/setup/init dispatch sequencing, including
attr-flag callback dispatch and counter-id/hw-config field loading, Rust-owned
`perf_start()` leader/sibling traversal, invariant-TSC accounting, active-state
publication, counter-start mask aggregation, and process perf-status
publication, Rust-owned `perf_reset()` leader/sibling reset traversal,
invariant-TSC reset base selection, count-read dispatch, and atomic count reset
sequencing, Rust-owned `perf_stop()` leader/sibling stop traversal,
stopped-TSC publication, hardware stop/update dispatch, `monitoring_event`
clearing, and `perf_status` `PP_NONE` publication, Rust-owned
`perf_event_open()` support-policy validation for CPU, flags, event type,
read-format, freq, and sample-period return shaping, Rust-owned
`perf_event_open()` event allocation initialization for event zeroing,
perf-attr copy publication, group/sibling list-head initialization,
sample-frequency/count/child-total/parent publication, hardware
sample-period/last-period/period-left publication, and invariant
REF_CPU_CYCLES fast-path classification, Rust-owned
`perf_event_open()` event mapping/configuration for hardware/cache/raw type
dispatch, validation, `hw_config`/`hw_config_ext` publication,
extra-register id/MSR/index publication, hardware-event init dispatch, and
successful output-event publication, Rust-owned `mc_perf_event_alloc()`
wrapper orchestration for attr presence validation, event allocation callback
dispatch, allocation-failure `-ENOMEM` shaping, initialization helper dispatch,
invariant fast-path output publication, mapping/configuration helper dispatch,
and cleanup-on-error free callback dispatch, Rust-owned post-validation
`perf_event_open()` top-level body orchestration for event-allocation dispatch,
event PID/counter sequencing, group wiring sequencing, Linux fd request
sequencing, mckfd allocation callback dispatch, mckfd publication dispatch, and
return/error propagation across counter, group, Linux-fd, allocation, and
publish failures, Rust-owned
`perf_event_open()` group wiring for standalone-vs-grouped leader selection,
sibling-list tail insertion, sibling count publication, and leader
`pmc_status` bit publication, Rust-owned `perf_event_open()` mckfd fd/callback/
data publication and process mckfd head insertion, Rust-owned
`perf_event_open()` event PID/counter-id publication and Linux fd request/
thread pmc-map publication, Rust-owned `perf_event_open()` entry copy-in and
support-validation handoff for perf-attr copy dispatch, copy failure shaping,
field loading, freq callback dispatch, validation error propagation, and
post-validation body handoff, Rust-owned
`process_cleanup_fd()` and `process_cleanup_before_terminate()` lookup,
cleanup-dispatch, optional TOFU fd-sweep, and unlock orchestration, Rust-owned
`finalize_process()` non-pid1 parent-notification orchestration, Rust-owned
`move_pages()` SMP request field publication,
page-allocator fallback Linux-zero request packet routing through the shared
zero-request helper, Rust-owned host prepare-process request ACK wrapper
sequencing, Rust-owned host procfs request wrapper sequencing, x86 VDSO info
IKC packet field publication, Rust-owned host sysfs packet and unknown-message
log wrapper sequencing, Rust-owned host packet-release, init-ACK log, and
schedule-process log wrapper sequencing, Rust-owned host IKC packet-send
wrapper sequencing, Rust-owned host map-memory/prepare-map-virtual/map-virtual/
unmap-virtual/unmap-memory callback wrapper sequencing,
sysfs
request/response IKC packet field publication, eventfd IKC packet preparation
and send-callback orchestration,
syscall offload bookkeeping, target-TID shaping, nested-dispatch validation,
top-level exited-process reject classification, nested `rt_sigaction`
signal-index/bounds classification, nested response-request field publication,
initial offload request/response field publication, and proxy-death
classification plus Rust-owned offload spin-without-schedule classification,
TOFU post-reply candidate classification, profile-event bounds classification,
thread-exit report-target presence classification, host exit-group forwarding
classification, finalize-process pid1-parent classification, finalize-process
parent-signal classification, sync-child perf-event eligibility and PID action
classification, same-thread exclusion classification, report-thread ptrace
classification, exit-status compare-exchange failure classification, child-list
cleanup-need classification, child-release classification, process lookup-miss
classification, TOFU cleanup eligibility classification, fd-path free
classification, detached-thread release classification, terminate-host kill
classification,
terminate/process-exited lifecycle classification, and Rust-owned NUMA
allocation policy-field
publication, current-VM availability gating, allocation policy-entry,
node-validity, required-input gating, and order-fallback node selection, plus
no-current-VM default allocation policy normalization. The
internal range-policy and process-VM policy-field bridge callbacks now extract
only the C struct fields and enter Rust through `mem_policy_fields_result()` for
policy, nodemask, and interleave cursor publication, including null-policy and
null-output handling. The allocation current-VM bridge now extracts only the
current thread and VM pointers, then enters Rust through `mem_current_vm_result()`
for the initialized/current-present gate. The Rust body and legacy C allocation
body now enter `mem_default_alloc_policy_result()` for the no-current-VM
fallback that clears `IHK_MC_AP_USER`, forces preferred node `-1`, and publishes
`MPOL_DEFAULT`. The Rust allocation policy body and C fallback body now enter
`mem_alloc_policy_should_try_policy_result()` for the explicit/policy-allocation
entry gate that combines preferred node, AP-user flag, memory policy, and
shared-memory status before falling back to distance/order allocation. The
same Rust/C fallback pair now enters `mem_alloc_node_id_valid_result()` for
preferred-node, bind/preferred-policy distance result, and distance-fallback
node-id validation. Interleave invalid-node handling is intentionally unchanged
in this batch because the legacy C path has different behavior there. The Rust
allocation policy body and C fallback body now also enter
`mem_alloc_policy_inputs_valid_result()` for the required input gate covering
positive page count, positive node count, and presence of try-alloc,
rusage-add, and phys-to-virt callbacks. The same Rust/C fallback pair now
enters `mem_alloc_order_node_result()` for fallback allocation-order node
selection, including wraparound and invalid node-count shaping. The
legacy C allocation body now routes range-policy publication, process-policy
publication, idle/current-VM selection, no-current-VM policy shaping, and
policy-entry/node-validity/input gating, plus order-fallback node selection,
through the same feature-gated fallback mirrors.
The top-level syscall offload return path and UTI utility-thread Linux
handoff path now enter `syscall_proxy_dead_result()` for the `-ERESTARTSYS`
proxy-death gate before publishing `proc->nohost`, keeping the side-effect
write in C but moving the return-code policy decision into Rust with a C
fallback mirror. The top-level syscall offload path also now enters
`syscall_offload_counted_result()` for both `in_syscall_offload` increment and
decrement gating, `syscall_target_tid_result()` for request target-thread ID
publication, `syscall_preempt_disable_needed_result()` for the matching
post-wait preempt-enable gate, and `syscall_nested_dispatch_valid_result()` for
both nested syscall request bounds/handler validation and top-level syscall
dispatch bounds/handler validation before dispatching through the remaining C
syscall table. The top-level syscall entry path now enters
`syscall_reject_after_exit_result()` for the already-exited process gate,
including `exit`/`exit_group` allowance and non-exit syscall rejection, before
the remaining C-owned return-value publication, signal check, cputime update,
and `-EINVAL` return. The nested `rt_sigaction` response path now enters
`syscall_nested_rt_sigaction_index_result()` for signal-number normalization and
`-EINVAL` shaping before the remaining C-owned signal-action handler lookup.
The process termination paths now enter `terminate_process_exited_result()` for
the exact `PS_EXITED` already-exited gate in both the group termination path and
the multi-threaded exit path before the remaining C-owned time accounting,
thread status publication, signal, lock, release, and recursive terminate
side effects.
The nested syscall response path now enters
`syscall_nested_response_prepare_result()` to publish the Linux response syscall
number, return value, requester TID, and serving TID into the response request
before the remaining C-owned `send_syscall()` transport.
The common `send_syscall()` path now enters `syscall_send_prepare_result()` to
reset the response status and clear the request-valid flag, and then enters
`syscall_request_copy_result()` to copy the request into the outgoing packet
using scalar Rust field publication. The same `send_syscall()` path now enters
`syscall_packet_traditional_prepare_result()` to publish the outgoing traditional
packet message, CPU reference, PID, and response physical address before the
remaining C-owned barrier, release-store, and IKC transport. The nested response
path also routes response wait-thread status publication through
`syscall_nested_response_prepare_result()`.
The eventfd notification path now enters
`syscall_eventfd_packet_prepare_result()` to zero the outgoing
`ikc_scd_packet`, publish `SCD_MSG_EVENTFD`, and publish the eventfd type before
the remaining C-owned CPU-local channel lookup and `ihk_ikc_send()` transport.
The Rust zeroing is an explicit word/byte store loop rather than a compiler
`memset` intrinsic so the freestanding Rust object remains compatible with the
kernel unresolved-symbol/SIMD audit. The wrapper then enters
`syscall_eventfd_send_result()` so Rust owns the stack-packet lifetime,
preparation call, option-zero send-callback dispatch, send return propagation,
and missing-callback rejection; C now supplies only the CPU-local channel and
the `ihk_ikc_send()` bridge.
The sysfs request and response send bridges now enter
`sysfs_request_packet_prepare_result()` and `sysfss_packet_prepare_result()` for
message, error, and sysfs argument field publication before the remaining
C-owned CPU-local channel lookup and raw `ihk_ikc_send()` transport. The helpers
intentionally preserve untouched packet fields to match the historical bridge
behavior while moving the McKernel-owned packet field publication into Rust with
C fallback mirrors.
The x86 `vdso_get_vdso_info()` path now enters
`x86_vdso_packet_prepare_result()` for `SCD_MSG_GET_VDSO_INFO` message
publication and VDSO descriptor physical-address publication before the
remaining C-owned VDSO state initialization, CPU-local channel lookup, raw
`ihk_ikc_send()` transport, and busy-wait loop.
The `move_pages()` body now enters `move_pages_smp_req_prepare_result()` to
publish the SMP request count, user address/status/node vectors, kernel scratch
vectors, PTE vector, node-ready gate, destination physical vector, process
pointer, phase counter reset, and phase return before the remaining C-owned page
table lock, `smp_call_func()` dispatch, per-CPU move handler, status copyout,
and allocation/free side effects. The helper has a C fallback mirror for
validation and does not retire the large C-owned `move_pages()` body.
The scheduler priority syscall wrappers now enter
`sched_get_priority_max_body_result()` and
`sched_get_priority_min_body_result()` for their body return value. The simple
`SYSCALL_DECLARE()` wrapper audit now finds no wrappers without `_result(` in
their textual body, but this is only a narrow routing check; it is not proof
that top-level syscall dispatch, argument extraction, scheduler internals, or
fallback bodies are Rust-owned.
The stopped-child wait path now enters `wait_stopped_body_result()` for stopped
source selection, status copy into the caller-provided kernel status pointer,
exit-status reap callback dispatch for thread/process/main-thread sources, and
reported PID/TID selection. C still owns the surrounding wait scan, locks,
status-copyout to user, rusage handling, logging primitives, and fallback body.
The thread-exit reporting path now enters `thread_exit_signal_body_result()` for
report-parent gating, ptrace-vs-termsig signal selection, SIGCHLD siginfo
construction, user/system TSC-to-jiffy conversion callback sequencing, `do_kill`
callback dispatch, log callback dispatch, and waitpid waitqueue wake callback
dispatch. C still owns the actual signal delivery primitive, waitqueue wake
primitive, time conversion primitive, logging primitive, and fallback body.
The non-pid1 parent-notification branch in `finalize_process()` now enters
`finalize_process_parent_notify_body_result()` for parent presence validation,
termsig-to-notify gating, SIGCHLD siginfo construction, process utime/stime
jiffy callback sequencing, `do_kill` callback dispatch, optional log callback
dispatch, waitpid waitqueue wake callback dispatch, and return shaping. C still
owns final status mutation, update-lock ordering, `release_process()`, the
actual signal-delivery primitive, waitqueue wake primitive, time conversion
primitive, logging primitive, and fallback body.
The top-level `finalize_process()` body now enters
`finalize_process_body_result()` for update-lock callback sequencing,
`PS_ZOMBIE` status publication, pid1-vs-parent-notification branch selection,
pid1 `release_process()` callback dispatch, wakeup-log callback dispatch, and
handoff to the Rust-owned parent-notification body. C still owns `resource_set`
lookup, stack lock-node storage, primitive lock/unlock behavior,
`release_process()` internals, signal/wake/time/log primitive callbacks, and the
feature-gated fallback body.
The `terminate_mcexec()` body now enters `terminate_mcexec_body_result()` for
group-exit-status claimed-state checking, exit-status construction,
compare-exchange callback dispatch, compare-exchange failure classification,
nohost forwarding gate selection, exit-group request number/argument
publication, nohost publication, and Linux-forward callback dispatch. C still
owns current-thread/process lookup, the atomic compare-exchange primitive,
processor-id lookup, Linux `do_syscall()` transport, and the feature-gated
fallback body.
The `terminate_host()` body now enters `terminate_host_body_result()` for
find-process callback dispatch, lookup-miss detached-thread release sequencing,
thread refcount-set callback dispatch, release-thread/release-process callback
ordering, nohost kill-forward classification, nohost publication,
process-unlock callback dispatch, and `SIGKILL` do-kill callback dispatch. C
still owns current-thread lookup, find-process/process-unlock primitive
behavior, atomic refcount-set primitive behavior, release primitive behavior,
signal-delivery primitive behavior, and the feature-gated fallback body.
The page-allocator fallback deferred-zero Linux request path now calls
`__ihk_numa_zero_request_packet_fill()` instead of hand-publishing the packet
fields in place, keeping fallback packet zeroing, `__NR_move_pages` request
publication, node argument publication, release-store valid flag, message, CPU
reference, PID, and response-physical-address behavior aligned with the existing
Rust-owned helper. The default Rust allocator path was already routed through
Rust result helpers; this fallback cleanup reduces divergence rather than adding
new broad dashboard points.
The send, offload-return, main syscall-entry, and main syscall-return debug log
gates now enter `syscall_log_budget_result()` for last-PID tracking, per-PID
counter reset, and bounded log-count increment before the remaining C-owned
`kprintf()` emission.
The initial syscall offload send path now enters
`syscall_offload_prepare_result()` to publish requester TID, target TID,
response wait-thread status, and the initial TOFU PDE-data slot before the
remaining C-owned IKC send and wait-loop transport.
The syscall offload wait loop now enters
`syscall_offload_spin_without_schedule_result()` for the no-preempt or no-TID
spin gate before the remaining C-owned pending-signal, pause, runqueue, and
schedule behavior. The TOFU post-reply path now enters
`syscall_tofu_post_reply_candidate_result()` for the exact ioctl-success or
openat-positive-return candidate gate before the remaining C-owned fd/path
selection, PDE-data test, lock, and publication. The offload and main syscall
profile paths now enter `syscall_profile_event_needed_result()` for the
`syscall_nr < PROFILE_SYSCALL_MAX` bounds gate before the remaining C-owned
profile event publication and debug logging.
The thread-exit signal path now enters
`thread_exit_signal_report_needed_result()` for the report-parent presence gate
before the remaining C-owned siginfo construction, kill, and waitq wakeup. The
host exit-group forwarding path now enters `terminate_host_exit_needed_result()`
for the no-host gate before the remaining C-owned exit-group request
publication and Linux offload. Process finalization now enters
`finalize_process_parent_is_pid1_result()` for pid1-parent classification
before the remaining C-owned zombie publication and release/wakeup behavior,
and enters `finalize_process_parent_signal_needed_result()` for the parent
signal gate before the remaining C-owned siginfo construction and `do_kill()`.
The child perf-event sync path now enters `sync_child_event_body_result()` for
the null/inherit/pid eligibility gate, leader action selection, sibling action
selection, sibling-list traversal, perf-counter read callback dispatch,
child-count-total accumulation, atomic count-set callback dispatch, and
missing-callback/null-leader error shaping. C still owns `attr.inherit`
bitfield extraction, the raw perf-counter read primitive, the atomic64 set
primitive, and fallback body. `perf_event_read_value()` now enters
`perf_event_read_value_entry_body_result()` and
`perf_event_read_value_body_result()` for attr flag callback dispatch,
missing-callback/null-event rejection, attr callback failure short-circuiting,
stopped-vs-live TSC selection, invariant-TSC user/kernel count arithmetic,
event-update callback dispatch for non-invariant leader events, atomic
count-read callback dispatch, inherited child-total addition, and return
shaping. C still owns current-thread lookup, the perf attr bitfield bridge, the
raw event-update primitive, the atomic64 read primitive, and fallback body.
`perf_event_read_one()` and `perf_event_read_group()` now enter
Rust-owned body helpers for one-event count read callback dispatch, grouped
leader/sibling list traversal, group count publication, per-sibling value
copyout ordering, copy-failure `-EFAULT` shaping, missing-callback/null-leader
validation, and byte-count return shaping. C still owns the raw
`copy_to_user()` primitive, read-format argument extraction, the lower
perf-read callback primitive, and fallback bodies. Top-level `perf_read()`
now enters `perf_read_body_result()` for group-format testing, read-group vs
read-one callback selection, missing-callback/null-event validation, and
callback return propagation. C still owns mckfd/context argument extraction,
`attr.read_format` loading, the read-one/read-group bridge functions, and
fallback body. `perf_ioctl()` now enters `perf_ioctl_body_result()` for
enable/disable/reset/refresh command dispatch, local monitoring-event
publication, target-process lookup callback dispatch, target
monitoring-event/perf-status publication for newly monitored processes,
process-unlock callback ordering, inherited-event refresh rejection,
unsupported-command shaping, and missing-callback/null-event validation. C
still owns mckfd/context argument extraction, `attr.inherit` bitfield
extraction, lower hardware-counter primitive behavior beneath perf
start/stop/reset, process lookup/unlock primitives, and fallback body.
`perf_close()` now enters `perf_close_body_result()` for counter allocation
bitmap clearing, optional extra-register allocation bitmap clearing,
invalid-bit no-op shaping, and free callback dispatch. C still owns
current-thread lookup, mckfd data extraction, the allocator free primitive,
and fallback body. `perf_fcntl()` now enters `perf_fcntl_body_result()` for
`F_SETSIG` `sig_no` publication, `F_SETOWN_EX`/default no-op classification,
Linux-forward callback dispatch, forward return propagation, and
missing-callback/null-fd validation. C still owns syscall argument extraction,
the Linux forwarding primitive, and fallback body. `perf_mmap()` now enters
`perf_mmap_body_result()` for `MAP_ANONYMOUS`/`PROT_WRITE` forcing,
`do_mmap()` callback dispatch, perf mmap page `data_head` publication,
`cap_user_rdpmc` capability-bit publication, missing-callback validation, and
return propagation. C still owns syscall argument extraction, the lower
`do_mmap()` VM primitive, and fallback body. `perf_counter_set()` now enters
`perf_counter_set_entry_body_result()` and `perf_counter_set_body_result()` for
attr-flag callback dispatch, counter-id/hw-config field loading, perf counter
mode construction from exclude-kernel/exclude-user flags, extra-register setup
callback dispatch, extra-register failure shaping, raw-counter init callback
dispatch, and missing-callback/null-event validation. `perf_start()` now enters
`perf_start_body_result()` for leader/sibling traversal, counter-mask
validation callback dispatch, invariant-TSC accumulated-count update,
base-TSC publication, stopped-TSC clearing, inactive-to-active state
publication, set-period and counter-set callback dispatch for hardware-backed
events, hardware-start mask aggregation, hardware-start callback dispatch, and
process perf-status publication. C still owns perf attr bitfield extraction,
current-thread lookup, hardware-counter primitive bodies, list ABI layout, and
fallback body. `perf_reset()` now enters `perf_reset_body_result()` for
leader/sibling reset traversal, counter-mask validation callback dispatch,
invariant-TSC reset base selection from stopped TSC or current thread TSC,
user/system accumulator clearing, hardware-backed count read callback dispatch,
atomic count reset callback dispatch, and missing-callback/null-event
validation. C still owns current-thread lookup, hardware counter read/update
primitive bodies, atomic primitive behavior, list ABI layout, and fallback
body. `perf_stop()` now enters `perf_stop_body_result()` for leader/sibling
stop traversal, counter-mask validation callback dispatch, invariant stopped-TSC
publication, active-to-inactive state publication, hardware-backed stop-mask
aggregation, hardware stop callback dispatch, post-stop update callback
dispatch, `monitoring_event` clearing, `perf_status` `PP_NONE` publication,
and missing-callback/null-event validation. C still owns current-thread lookup,
hardware stop/update primitive bodies, list ABI layout, and fallback body.
`perf_event_open()` now enters `perf_event_open_validate_body_result()` for
CPU, flags, event type, read-format, freq, and sample-period support
classification, including high-bit sample-period `-EINVAL` precedence before
unsupported-feature `-ENOENT` shaping. C still owns perf attr copyin, event
allocation, event/hardware mapping, counter allocation, Linux fd request
dispatch, mckfd publication, and fallback body.
`perf_event_open()` now also enters `perf_event_open_group_body_result()` for
standalone-vs-grouped leader selection, sibling-list tail insertion, sibling
count publication, and leader `pmc_status` bit publication from the allocated
counter index. C still owns perf attr copyin, event allocation, event/hardware
mapping, counter allocation, Linux fd request dispatch, mckfd publication, and
fallback body.
`perf_event_open()` now also enters
`perf_event_open_mckfd_publish_body_result()` for mckfd fd, signal number,
event data pointer, read/ioctl/mmap/close/fcntl callback publication, process
mckfd-lock callback sequencing, and process mckfd head insertion. C still owns
mckfd allocation, spinlock primitive behavior, Linux fd request dispatch, event
allocation, counter allocation, and fallback body.
`perf_event_open()` now also enters `perf_event_open_counter_body_result()` and
`perf_event_open_linux_fd_body_result()` for event PID publication,
counter-allocation callback dispatch, counter-id publication, Linux
`perf_event_open` request-number/arg0 publication, `do_syscall()` callback
dispatch, negative-fd return shaping, and thread `pmc_alloc_map` publication.
C still owns perf attr copyin, event allocation, the raw counter allocator,
CPU-id lookup, Linux syscall transport, mckfd allocation, and fallback body.
Termination
thread-list collection now enters
`terminate_thread_is_other_result()` for same-thread exclusion while building
the SIGKILL target list, and report-thread cleanup now enters
`terminate_report_thread_ptrace_result()` before the remaining C-owned ptrace
detach or report-list detach/release behavior.
The host termination path now enters
`terminate_group_status_update_failed_result()` for group-exit-status
compare-exchange failure classification before the remaining C-owned host exit
offload. The child cleanup path now enters `terminate_child_cleanup_needed_result()`
for the children-list/nonempty gate and `terminate_release_child_needed_result()`
for the post-detach child release gate before the remaining C-owned hash-list
walk, reparenting, locks, and release.
The process cleanup and terminate-host paths now enter
`process_cleanup_fd_body_result()` and
`process_cleanup_before_terminate_body_result()` for find-process callback
dispatch, normal lookup-miss return/log handling, cleanup callback dispatch,
optional TOFU fd-sweep selection and iteration, and process-unlock callback
ordering. The lower fd cleanup still enters `process_cleanup_tofu_needed_result()`
for TOFU cleanup eligibility and
`process_cleanup_fd_path_free_needed_result()` for fd-path free gating.
`terminate_host()` also enters `terminate_host_detached_thread_release_needed_result()`
for detached-thread release after lookup miss and
`terminate_host_kill_needed_result()` for the `nohost != 1` kill-forwarding
gate before the remaining C-owned refcount, release, process-unlock, cleanup,
logging, and signal side effects.
The equivalence harness routes the synthetic range/vm field callbacks through
the helper and adds direct assertions for no-policy preservation, successful
field publication, null outputs, mask pointer publication, interleave cursor
pointer publication, uninitialized current-VM rejection, null-current rejection,
null-VM return, successful VM return, AP-user flag clearing, preferred-node
reset, default-policy publication, null-output default-policy calls, exact
policy-skip detection, AP-user policy entry, preferred-node policy entry,
non-default NUMA policy entry, shared-memory policy entry, and negative
non-sentinel preferred-node preservation, plus node-validity coverage for
negative, zero, last-valid, first-invalid, zero-node, and negative-node-count
cases, and input-gate coverage for valid input, zero pages, zero nodes, missing
try-alloc callback, missing rusage-add callback, and missing phys-to-virt
callback. Direct order-fallback coverage now includes offset 0, offset 1,
wraparound, larger offset wraparound, negative current node preservation, and
invalid node-count rejection. Direct syscall proxy-death coverage now includes
`-ERESTARTSYS` acceptance plus nearby negative, zero, and positive rejection.
Direct syscall offload coverage now includes target-TID requester/pool shaping,
exit-group bookkeeping exclusion, non-exit bookkeeping inclusion, negative
same-number exclusion, and nested-dispatch validation for valid, negative,
out-of-range, and missing-handler cases. Direct nested `rt_sigaction` coverage
now includes first signal, last signal, zero signal, first out-of-range signal,
and zero `_NSIG` rejection. Direct nested response-request coverage now verifies
number, return-value, requester-TID, serving-TID, and response wait-thread
status publication, preservation of untouched request/response fields, and
null request/response `-EINVAL` rejection. Direct send-preparation coverage now
verifies response-status reset, request-valid clearing, untouched field
preservation, and null request/response `-EINVAL` rejection. Direct request-copy
coverage now verifies all scalar request fields and argument slots copy into the
destination request, plus null source/destination `-EINVAL` rejection. Direct
traditional packet-preparation coverage now verifies message, reference, PID,
and response-physical-address publication, preservation of untouched packet
fields, preservation of the copied request body, and null-packet `-EINVAL`
rejection. Direct eventfd packet-preparation coverage now verifies stale packet
header/channel, error, reply, and union-body zeroing, message/type publication,
and null-packet `-EINVAL` rejection. Direct eventfd send-wrapper coverage now
verifies callback invocation, channel forwarding including null-channel
forwarding, option-zero dispatch, packet zeroing/message/type publication,
callback return propagation, and missing-callback `-EINVAL` rejection. Direct
sysfs packet-preparation coverage now verifies response message/error/arg1/arg2
publication, request message/arg1 publication, preservation of untouched header,
reply, error, and sysfs argument fields, and null-packet `-EINVAL` rejection.
Direct log-budget coverage now verifies first-log acceptance,
limit-bound acceptance, limit rejection without increment, PID-change reset,
zero-limit reset without logging, and null pointer `-EINVAL` rejection.
Direct x86 VDSO packet-preparation coverage now verifies message and argument
publication, preservation of untouched header, error, reply, traditional packet,
request, and response fields, and null-packet `-EINVAL` rejection.
Direct `move_pages()` SMP request preparation coverage now verifies all pointer
fields, count, node-ready reset, phase counter reset, phase-return reset, process
pointer publication, and null-request `-EINVAL` rejection.
Direct scheduler-priority coverage now also mixes the
`sched_get_priority_max_body_result()` and
`sched_get_priority_min_body_result()` wrapper-body results for the valid and
invalid scheduler policies already covered by the helper harness.
Direct stopped-wait coverage now verifies thread, stopped-process,
main-thread, no-source, null-child, and missing-callback cases, including
status publication, WNOWAIT preservation, exit-status clearing, reap callback
target selection, and PID/TID return shaping.
Direct thread-exit reporting coverage now verifies normal termsig reporting,
ptrace-forced SIGCHLD reporting, null-report no-op behavior, null-thread
rejection, missing-callback rejection, siginfo child fields, TSC-to-jiffy
conversion counts, `do_kill` arguments, log callback dispatch, and waitqueue
wake target selection.
Direct finalize-process parent-notification coverage now verifies nonzero
termsig siginfo/do-kill/log/wake behavior, zero-termsig wake-only behavior,
null-parent rejection, null-process rejection, and missing timespec callback
rejection.
Direct finalize-process body coverage now verifies non-pid1 lock/status/log/
notify sequencing, pid1 lock/status/release sequencing, null-process rejection,
null-lock-node rejection, and missing-lock rejection without status mutation.
Direct terminate-mcexec coverage now verifies normal compare-exchange plus
host-forward request/nohost publication, already-claimed skip behavior,
compare-exchange mismatch behavior, nohost-suppressed forwarding, null process,
null request, missing compare-exchange callback, and missing syscall callback
handling after a successful status update.
Direct terminate-host coverage now verifies found-process kill forwarding,
already-nohost unlock-only behavior, lookup-miss detached-thread refcount reset
and release ordering, lookup-miss null-thread no-op behavior, missing do-kill
unlock/error behavior, and missing find callback rejection.
Direct initial offload preparation coverage now verifies requester-owned
target-TID selection for the `sched_setaffinity` special case, pool-thread
targeting for nonmatching arguments, response wait-thread status publication,
PDE-data clearing, preservation of untouched request fields, and null
request/response `-EINVAL` rejection. Direct exited-process reject coverage now
verifies non-exit rejection after `PS_EXITED`, `exit` allowance, `exit_group`
allowance, and non-exited process allowance. Direct terminate-process-exited
coverage now verifies exact `PS_EXITED` acceptance plus `PS_ZOMBIE`,
`PS_RUNNING`, and zero-status rejection. Direct offload spin-without-schedule
coverage now verifies nonzero no-preempt acceptance, negative no-preempt
acceptance, zero-TID acceptance, and runnable TID rejection. Direct TOFU
post-reply candidate coverage now verifies ioctl success acceptance, ioctl
nonzero-return rejection, openat positive-return acceptance, openat zero-return
rejection, and unrelated syscall rejection. Direct profile-event gate coverage
now verifies first syscall, last in-range syscall, max-bound rejection,
negative syscall preservation, and zero-max rejection. Direct termination
classification coverage now verifies null/non-null report parent, host
exit-group forwarding for zero/nonzero/negative nohost states, pid1-parent
pointer equality and inequality including null/null equality, and parent-signal
necessity for zero, positive, and negative signals. Direct child-event coverage
now verifies no-event rejection, non-inherited pid-zero rejection, inherited
pid-zero acceptance, positive-pid acceptance, negative-pid acceptance, and PID
action selection for zero, positive, and negative PIDs. Direct termination
thread classification coverage now verifies same-pointer rejection, pointer/null
inequality, null/pointer inequality, null/null equality, and ptrace zero,
positive, and negative handling. Direct child cleanup coverage now verifies
matching and mismatching group-exit-status compare-exchange results, all
children-list emptiness combinations, and child-release zero, positive, and
negative handling. Direct sync-child coverage now verifies child-total
accumulation for leader and siblings, atomic count publication for leader and
siblings, no-inherit/no-pid no-op behavior, negative leader PID no-op behavior,
missing set/read callback rejection, null leader rejection, and null event
no-op behavior. Direct perf-read coverage now verifies invariant TSC user and
kernel count arithmetic with inherited child totals, stopped-system TSC
selection with excluded user counts, non-invariant update callback ordering,
non-leader no-update behavior, null event handling, and missing atomic-read
handling, plus one-event read copyout success/failure/null-callback handling
and grouped leader/sibling count copyout, sibling-list traversal,
copy-failure `-EFAULT` shaping, and null-leader/null-copy callback rejection.
Direct top-level perf-read dispatch coverage now verifies group-format callback
selection, single-event callback selection, callback argument forwarding,
callback return propagation, null-event rejection, and missing callback
rejection.
Direct perf-ioctl coverage now verifies local enable monitoring-event
publication and start callback dispatch, target-process enable lookup,
monitoring-event/perf-status publication and unlock ordering, already
monitored target preservation, target lookup failure without unlock, local
disable stop callback dispatch, reset callback dispatch, inherited refresh
rejection, non-inherited refresh no-op, unsupported command rejection, null
event rejection, and missing start callback rejection.
Direct perf-close coverage now verifies counter allocation bitmap clearing,
extra-register allocation bitmap clearing, no-extra-register preservation,
out-of-range counter/index no-op behavior, free callback dispatch, null-event
rejection, and missing-free callback rejection.
Direct perf-fcntl coverage now verifies `F_SETSIG` signal-number publication,
`F_SETOWN_EX` and default command no-op behavior, Linux-forward callback
argument forwarding, forward return propagation, null-fd rejection, and
missing-forward callback rejection.
Direct perf-mmap coverage now verifies forced `PROT_WRITE` and `MAP_ANONYMOUS`
bits, callback argument forwarding, `vrf0 == 0`, null private data forwarding,
perf mmap page `data_head` publication, capability-bit OR preservation, return
pointer propagation, and missing-callback `-EINVAL` rejection.
Direct perf-counter/start coverage now verifies exclude-flag mode construction,
raw-counter init callback arguments, extra-register setup failure shaping,
missing-init callback rejection, invariant-TSC user/system accumulation and
base publication, stopped-TSC clearing, sibling traversal, set-period and
counter-set callback dispatch, hardware-start mask aggregation, process
perf-status publication, no-start active/mask-rejected behavior, and missing
set-period callback rejection.
Direct perf-reset coverage now verifies invariant reset base selection from
stopped/current TSC, user/system accumulator clearing, sibling traversal,
hardware-backed count read callback dispatch, atomic count reset callback
dispatch, mask-rejected no-op behavior, and missing read callback rejection.
Direct perf-stop coverage now verifies invariant stopped-TSC publication,
active-to-inactive state publication, sibling traversal, hardware stop callback
dispatch, post-stop update callback dispatch, mask-rejected no-op behavior,
process monitoring-event/perf-status cleanup, and missing stop callback
rejection.
Direct perf-event-open validation coverage now verifies accepted raw events,
unsupported CPU/type/read-format/freq `-ENOENT` shaping, and high-bit
sample-period `-EINVAL` precedence even when another unsupported condition is
present.
Direct perf-event-open allocation-init coverage now verifies deterministic
event zeroing, perf-attr byte copy, group/sibling list-head self
initialization, sample-frequency/count/child-total/parent publication,
sample-period and freq-derived period publication, atomic period-left
publication, invariant REF_CPU_CYCLES fast-path classification, and missing
atomic-set callback rejection.
Direct perf-event-open allocation-map coverage now verifies hardware event
mapping without extra-register publication, cache event mapping with
extra-config and extra-register config/MSR/index publication, raw event
validation rejection, hardware-init failure propagation without output-event
publication, unsupported type rejection before validation, and missing-map
callback rejection.
Direct full perf-event allocation-body coverage now verifies allocation
callback size/flag forwarding, successful raw-event allocation/init/map/output
publication, invariant fast-path output publication without map/init callbacks,
allocation-failure `-ENOMEM` shaping without cleanup, validation-failure
cleanup, hardware-init failure cleanup, and initialization-callback failure
cleanup before the map path.
Direct post-validation perf-event-open body coverage now verifies successful
event allocation, counter-id publication, standalone group leader/status
publication, Linux request publication, positive fd pmc-map publication, mckfd
allocation size/flag forwarding, mckfd callback/data/head publication, event
allocation failure short-circuiting, counter-allocation failure short-circuiting
after PID publication, missing-group rejection before Linux fd dispatch,
negative Linux fd return without mckfd allocation, and mckfd allocation failure
after positive Linux fd dispatch.
Direct perf-event-open group coverage now verifies standalone leader/pmc-status
publication, grouped leader lookup through the mckfd list, sibling-list tail
insertion after an existing sibling, sibling count publication, leader
`pmc_status` mask publication, and missing-group rejection.
Direct perf-event-open mckfd publication coverage now verifies fd/sig/data
field publication, read/ioctl/mmap/close/fcntl callback publication, lock/unlock
callback ordering and irqstate propagation, head insertion ahead of an existing
mckfd, and missing-callback rejection.
Direct perf-event-open counter/Linux-fd coverage now verifies PID publication
before counter allocation, successful counter-id publication, negative counter
allocation return shaping with prior counter id preserved, Linux request
number/arg0 publication, `do_syscall()` callback arguments, negative fd return
without pmc-map mutation, and positive fd `pmc_alloc_map` bit publication.
Direct perf-event-open entry coverage now verifies perf-attr copy-in dispatch,
user copy failure `-EFAULT` shaping before validation, freq/read-format/
sample-period validation rejection before event allocation, high-bit
sample-period `-EINVAL` precedence, missing copy/freq callback rejection, and
successful handoff into the Rust-owned post-validation body with copied attr
publication.
Direct perf-counter-set entry coverage now verifies attr-flag callback
dispatch against the embedded perf attr, counter-id and hw-config field
loading, exclude-kernel/exclude-user mode construction, extra-register callback
dispatch, attr callback failure short-circuiting before hardware init, and
missing attr-callback rejection.
Direct process-cleanup and terminate-host coverage now verifies
null/non-null process lookup, zero/positive/negative TOFU cleanup eligibility,
null/non-null fd-path free gating, found-process fd cleanup and unlock
ordering, lookup-miss logging without cleanup/unlock, missing-cleanup error
paths with unlock preservation, no-TOFU before-terminate unlock-only behavior,
TOFU fd sweep ranges, lookup-miss before-terminate no-op behavior,
detached-thread release only for missing-process plus non-null thread, and host
kill-forwarding for zero, negative, and already-nohost states.
Validation passed `rustfmt kernel/rust/host_helpers.rs`,
`bash -n kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the
touched host/test/documentation files, the expanded Rust/C equivalence suite
with `syscall_policy_helpers ok digest=f44bc5b98c000040`,
`sched_helpers ok digest=4f21ad6b9659f7a7`,
`object_helpers ok digest=69dffe8191860965`,
`x86_memory_helpers ok digest=e848d69c17c2d678`,
`page_alloc ok digest=bc5add72ff7bd6de`, and
`page_alloc_bitmap ok digest=ef86cd1f342267ad`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, C-fallback `mckernel.img` build, and
the ownership dashboard audit. Latest direct host prepare-process, procfs
request, sysfs packet, unknown-message, packet-release, init-ACK log,
schedule-process log, IKC packet-send, map/unmap callback wrapper coverage,
prepare-process primitive callback wrapper coverage, schedule-process
primitive callback wrapper coverage, remote-page-fault primitive callback
wrapper coverage, IKC init primitive callback wrapper coverage,
cleanup/signal/wake/debug/register primitive callback wrapper coverage,
perf-control primitive callback wrapper coverage, current-pointer/monitor/
prepare-log/Tofu support callback wrapper coverage, and prepare/cleanup
dispatch callback wrapper coverage all passed with
`object_helpers ok digest=5be28530fd495187`, Rust-enabled `mckernel.img`, and
C-fallback `mckernel.img`. Latest mechanical audit reports 59143 Rust
implementation LOC, 125527 C implementation-debt LOC, 31854 C/header boundary
LOC, 32.0% mechanical Rust LOC
overall, 45.1% mechanical Rust LOC for x86_64 memory management, 35.5%
mechanical Rust LOC for syscall core, 56.7% mechanical Rust LOC for the page
allocator, 56.4% mechanical Rust LOC for scheduler/timers/wait/futex, and
40.2% mechanical Rust LOC for procfs/sysfs/xpmem/file objects.
The syscall C-debt inventory rose because the equivalence-preserving C fallback
mirror and prototype are still counted as C debt; the page-allocator C debt
dropped slightly after fallback packet publication was consolidated. A
lightweight QEMU guest attempt with TCG against
`/tmp/mckernel-rhel8-alma85-base.qcow2` started the runner but did not reach
guest SSH within the 5-second smoke window, leaving an empty serial log at
`/tmp/mckernel-qemu-move-pages-20260527-111227/serial.log`; no McKernel
boot/runtime stress ran. No new QEMU runtime smoke was run for the scheduler
priority wrapper, stopped-wait, thread-exit signal, finalize-process
parent-notification, top-level finalize-process body, terminate-mcexec,
terminate-host, process-cleanup, sync-child, perf-read value, or perf-read
copyout/dispatch, perf-ioctl, perf-close, perf-fcntl, perf-mmap,
perf-counter-set, perf-start, perf-reset, perf-stop, perf-event-open
validation, perf-event-open allocation-init, perf-event-open allocation-map,
full perf-event allocation-body orchestration, perf-event-open group-wiring,
post-validation perf-event-open top-level body orchestration,
perf-event-open mckfd-publication follow-up, or
perf-event-open counter/Linux-fd follow-up, perf-event-open entry
copy-in/validation handoff, perf-counter-set entry wrapper orchestration, or
perf-event read-value entry wrapper orchestration, host procfs-answer /
remote-page-fault current-context wrapper orchestration, or host
prepare-process request ACK wrapper orchestration, or host procfs request
wrapper orchestration, host sysfs packet wrapper orchestration, or host
unknown-message log wrapper orchestration, host packet-release wrapper
orchestration, host init-ACK log wrapper orchestration, or host
schedule-process log wrapper orchestration, or host IKC packet-send wrapper
orchestration, or host map-memory/prepare-map-virtual/map-virtual/
unmap-virtual/unmap-memory callback wrapper orchestration, or host
prepare-process primitive callback wrapper orchestration, or host
schedule-process primitive callback wrapper orchestration, or host
remote-page-fault primitive callback wrapper orchestration, or host IKC init
primitive callback wrapper orchestration, or host
cleanup/signal/wake/debug/register primitive callback wrapper orchestration,
or host perf-control primitive callback wrapper orchestration.
Completion audit remains
unsatisfied: other
allocator lifetime, page-table, futex, scheduler, signal, IKC,
Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned allocation
range shared-memory classification. The internal `mem_range_is_shm_bridge()`
callback now extracts only `range`/`memobj` presence plus `memobj->flags`, then
enters Rust through `mem_range_is_shm_result()` for exact `MF_SHM`
classification. The legacy C allocation body now routes through the same
feature-gated fallback mirror, preserving the historical equality test instead
of broad bit-containment. Focused equivalence assertions cover null/no-memobj,
non-SHM, exact SHM, and mixed-flag rejection. Validation passed `rustfmt
kernel/rust/mem_helpers.rs`, `bash -n kernel/rust/tests/run_equivalence.sh`,
`git diff --check` for the touched memory helper/test files, the expanded
Rust/C equivalence suite with `mem_init_helpers ok
digest=d01a253b879b2833`, the unresolved-symbol/SIMD audit, Rust-enabled
`mckernel.img` build, C-fallback `mckernel.img` build, and the ownership
dashboard audit. That checkpoint mechanical audit reported 52384 Rust
implementation LOC, 119501 C implementation-debt LOC, 30.5% mechanical Rust
LOC overall, and 44.9%
mechanical Rust LOC for x86_64 memory management. No QEMU boot/runtime stress
was run for this post-saturation batch. Completion audit remains unsatisfied:
other allocator lifetime, page-table, futex, scheduler, signal, IKC,
Linux-forwarding, and control-plane primitive bodies are still C-owned.

Earlier verified post-saturation real ownership movement: Rust-owned NUMA
nodemask test policy. The internal `mem_mask_test_bridge()` callback now enters
Rust for bounded nodemask bit testing, negative-node rejection, out-of-range
rejection, null-mask handling, zero-size handling, and multiword nodemask
classification. The equivalence harness was also tightened to normalize
dump-query page-table pointer hashing to stable fake table indexes, removing a
layout-sensitive digest dependency. C is now the static callback shim and the
feature-gated fallback mirror. Validation passed `rustfmt
kernel/rust/mem_helpers.rs`, `bash -n kernel/rust/tests/run_equivalence.sh`,
`git diff --check` for the touched memory helper/test files, the expanded
Rust/C equivalence suite with `mem_init_helpers ok
digest=45ffdb03a6de9abf`, the unresolved-symbol/SIMD audit, Rust-enabled
`mckernel.img` build, C-fallback `mckernel.img` build, and the ownership
dashboard audit. No QEMU boot/runtime stress was run for this post-saturation
batch. Completion audit remains unsatisfied: other allocator lifetime,
page-table, futex, scheduler, signal, IKC, Linux-forwarding, and control-plane
primitive bodies are still C-owned.

Earlier verified post-saturation real ownership movement: Rust-owned NUMA
interleave-next allocation policy. The internal `interleave_nodes()` helper now
enters Rust for next-set-node scanning, wraparound to the first set node,
empty-mask return shaping, null/zero-bit validation, and multiword nodemask
handling. The existing Rust allocation policy path now receives this decision
through a C callback wrapper whose body delegates back to Rust. C is now the
static callback shim and the feature-gated fallback mirror. Validation passed
`rustfmt kernel/rust/mem_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched memory
helper/test files, the expanded Rust/C equivalence suite with
`mem_init_helpers ok digest=6cd15366a0265f5b`, the unresolved-symbol/SIMD audit,
Rust-enabled `mckernel.img` build, C-fallback `mckernel.img` build, and the
ownership dashboard audit. No QEMU boot/runtime stress was run for this
post-saturation batch. Completion audit remains unsatisfied: other allocator
lifetime, page-table, futex, scheduler, signal, IKC, Linux-forwarding, and
control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned page
allocator `reserve_pages` body sequencing. The internal reservation helper now
enters Rust for allocator-range clamping, empty-range no-op classification,
reserved page-count calculation, reserve-log callback dispatch, page-allocator
reserve callback dispatch, and null/missing-callback validation. C is now the
static call-site wrapper plus compact `dkprintf`/`ihk_pagealloc_reserve`
callback bridges and the feature-gated fallback mirror. Validation passed
`rustfmt kernel/rust/mem_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched memory
helper/test files, the expanded Rust/C equivalence suite with
`mem_init_helpers ok digest=435c4c03f7874859`, the unresolved-symbol/SIMD audit,
Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build. No QEMU
boot/runtime stress was run for this post-saturation batch. Completion audit
remains unsatisfied: other allocator lifetime, page-table, futex, scheduler,
signal, IKC, Linux-forwarding, and control-plane primitive bodies are still
C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`ihk_mc_reserve_arch_pages` reserve sequencing. The default reserve wrapper now
enters Rust for early heap/text reservation address calculation through the
existing `virt_to_phys` callback, AP trampoline reservation, null-page
reservation, architecture-specific reservation callback dispatch, and
null/missing-callback validation. C is now the public ABI wrapper plus compact
callback bridge and the feature-gated fallback mirror. Validation passed
`rustfmt kernel/rust/x86_memory_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched x86
memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=e09f5b2b067d8b24`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
The equivalence harness was also tightened so public string-copy tests use
page-contained test offsets instead of layout-sensitive fixed offsets. No QEMU
boot/runtime stress was run for this post-saturation batch. Completion audit
remains unsatisfied: other allocator, page-table, futex/scheduler/signal, IKC,
Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
public address translation primitives. The default `virt_to_phys()` wrapper now
enters Rust for MAP_KERNEL/LINUX_PAGE_OFFSET/MAP_FIXED/MAP_ST range selection,
kernel-physical-base adjustment, straight/fixed/linux offset calculation, and
debug-log dispatch. The default `phys_to_virt()` wrapper now enters Rust for
pre-page-table straight-map selection versus loaded Linux-offset selection. C is
now the public ABI wrapper plus debug-log callback and the feature-gated fallback
mirror. Validation passed `rustfmt kernel/rust/x86_memory_helpers.rs`, `bash
-n kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched
x86 memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=49c25023db960595`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
No QEMU boot/runtime stress was run for this post-saturation batch. Completion
audit remains unsatisfied: other allocator, page-table, futex/scheduler/signal,
IKC, Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`init_linux_kernel_mapping` boot mapping body. The Linux straight-map setup
now enters Rust for `safe_kernel_map` command-line dispatch, default 2-TB
mapping range setup, allocated-memory-chunk mode, memory-chunk enumeration,
bad-chunk/no-chunk handling, virtual/physical cursor advancement, large-page
mapping callback dispatch, branch-specific mapping failure logging, and
continue-on-error behavior. C is now the boot wrapper plus command-line,
memory-chunk, large-page set, and log callbacks and the feature-gated fallback
mirror. Validation passed `rustfmt kernel/rust/x86_memory_helpers.rs`, `bash
-n kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched
x86 memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=192e480649e60854`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
No QEMU boot/runtime stress was run for this post-saturation batch. Completion
audit remains unsatisfied: other allocator, page-table, futex/scheduler/signal,
IKC, Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`init_vsyscall_area` boot mapping body. The vsyscall mapping path now enters
Rust for fixed virtual-address publication, `vsyscall_page` virtual-to-physical
callback dispatch, active/user attribute propagation, `__set_pt_page` callback
dispatch, and callback-error return propagation to the existing panic site. C
is now the boot init wrapper plus callbacks and the feature-gated fallback
mirror. Validation passed `rustfmt kernel/rust/x86_memory_helpers.rs`, `bash
-n kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched
x86 memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=9c3327b0dfe6a315`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
No QEMU boot/runtime stress was run for this post-saturation batch. Completion
audit remains unsatisfied: other allocator, page-table, futex/scheduler/signal,
IKC, Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`init_fixed_area` and `init_low_area` boot setup bodies. Fixed-area cursor
initialization now enters Rust for `MAP_FIXED_START` publication, and boot
low-area setup now enters Rust for zero-page large-page mapping dispatch with
the no-execute/writable attribute combination while preserving the old ignored
set-return behavior. C is now the boot init wrapper plus the large-page set
callback and the feature-gated fallback mirror. Validation passed `rustfmt
kernel/rust/x86_memory_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched x86
memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=3932da4ffaf845a4`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
No QEMU boot/runtime stress was run for this post-saturation batch. Completion
audit remains unsatisfied: other allocator, page-table, futex/scheduler/signal,
IKC, Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`init_text_area` boot text mapping body orchestration. The text mapping path
now enters Rust for `_end` alignment with the two-large-page cushion, large-page
count calculation, text mapping log dispatch, kernel physical base publication,
per-page virtual/physical cursor advancement, large-page mapping callback
dispatch, and return shaping while preserving the old continue-through mapping
behavior. C is now the boot init wrapper plus logging and large-page set
callbacks and the feature-gated fallback mirror. Validation passed `rustfmt
kernel/rust/x86_memory_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched x86
memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=a7b06a70583bfe2b`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
No QEMU boot/runtime stress was run for this post-saturation batch. Completion
audit remains unsatisfied: other allocator, page-table, futex/scheduler/signal,
IKC, Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`init_normal_area` boot mapping body orchestration. The normal-memory boot
mapping path now enters Rust for McKernel map-start/map-end lookup callback
dispatch, straight-map virtual base calculation, range logging dispatch,
large-page loop sequencing, writable attribute policy, per-page large-page
mapping callback dispatch, set-failure logging dispatch, and continue-on-error
behavior. C is now the boot init wrapper plus lookup, large-page set, and log
callbacks and the feature-gated fallback mirror. Validation passed `rustfmt
kernel/rust/x86_memory_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched x86
memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=ffd21551b42cacd3`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
No QEMU boot/runtime stress was run for this post-saturation batch. Completion
audit remains unsatisfied: other allocator, page-table, futex/scheduler/signal,
IKC, Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`map_fixed_area` body orchestration. The fixed-area mapping path now enters
Rust for physical offset/alignment, page-count calculation, writable/active/
uncachable attribute policy, per-page fixed virtual address publication,
`__set_pt_page` callback dispatch, fixed cursor advancement, failure short
return without cursor publication, final cursor publication, TLB flush callback
sequencing, and return-address shaping. C is now the public ABI/debug wrapper
plus callbacks and the feature-gated fallback mirror. Validation passed
`rustfmt kernel/rust/x86_memory_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched x86
memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=01a9ae204e36bfd2`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
No QEMU boot/runtime stress was run for this post-saturation batch. Completion
audit remains unsatisfied: other allocator, page-table, futex/scheduler/signal,
IKC, Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
TLB flush primitives. The live x86 `flush_tlb()` wrapper now enters Rust for
CR3 read/reload sequencing, and `flush_tlb_single()` now enters Rust for
single-address `invlpg` dispatch. In the default Rust build, `x86_read_cr3_result`,
`x86_load_cr3_result`, and `x86_flush_tlb_single_result` are Rust inline
assembly; C is now the public ABI wrapper plus fallback mirror. Validation
passed `rustfmt kernel/rust/x86_memory_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched x86
memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=e236de8aa5b5c591`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
The user-space equivalence suite intentionally tests the TLB sequencing bodies
with fake CR3/INVLPG callbacks rather than executing privileged instructions.
No QEMU boot/runtime stress was run for this post-saturation batch. Completion
audit remains unsatisfied: other allocator, page-table, futex/scheduler/signal,
IKC, Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`load_page_table` body and default CR3 write. The live x86 page-table load path
now enters Rust for null-to-`init_pt` selection, virtual-to-physical callback
dispatch, and CR3 write dispatch; in the default Rust build the CR3 write
primitive itself is Rust inline assembly via `x86_load_cr3_result`. C is now
the public ABI wrapper plus fallback mirror. Validation passed `rustfmt
kernel/rust/x86_memory_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched x86
memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=f9047b6f1731ab38`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
The user-space equivalence suite intentionally tests the load body with a fake
CR3 callback rather than executing the privileged instruction. No QEMU
boot/runtime stress was run for this post-saturation batch. Completion audit
remains unsatisfied: other allocator, page-table, futex/scheduler/signal, IKC,
Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`move_pte_range` body orchestration. The range move path now enters Rust for
move-argument publication, visitor-range callback dispatch with
`VPTEF_SKIP_NULL`, mandatory TLB flush callback sequencing after the visitor,
and visitor-error return shaping. C is now bridge code for `visit_pte_range`,
`flush_tlb`, and the visitor callback plus the feature-gated fallback mirror.
Validation passed `rustfmt kernel/rust/x86_memory_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched x86
memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=8810ec262ab9a955`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
An initial build exposed a `uintptr_t *` versus `unsigned long *` helper ABI
warning; the prototype/fallback/test ABI was corrected and the clean validation
above was rerun. No QEMU boot/runtime stress was run for this post-saturation
batch. Completion audit remains unsatisfied: other allocator, page-table,
futex/scheduler/signal, IKC, Linux-forwarding, and control-plane primitive
bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`move_one_page` body orchestration used by `move_pte_range`. The visitor path
now enters Rust for file-offset rejection, mapped-destination calculation, old
PTE clear/extraction, destination `set_range` callback dispatch, and failure
log routing. C is now bridge code for the destination map callback and logging
plus the feature-gated fallback mirror. Validation passed `rustfmt
kernel/rust/x86_memory_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched x86
memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=f3b4af931f1e0a59`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
No QEMU boot/runtime stress was run for this post-saturation batch. Completion
audit remains unsatisfied: other allocator, page-table, futex/scheduler/signal,
IKC, Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`ihk_mc_pt_split` wrapper orchestration. The live x86 split path now enters
Rust for page-table lookup retry sequencing, large-mapping detection,
file-offset versus physical-page classification, splitability callback
dispatch, split-large-page callback dispatch, remote TLB flush callback
dispatch, and failure log routing. C is now bridge code for lookup,
`phys_to_page`, splitability, split execution, TLB flush, and logging
primitive callbacks plus the feature-gated fallback mirror. Validation passed
`rustfmt kernel/rust/x86_memory_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched x86
memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=02d3a8cfe1ba79ce`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
No QEMU boot/runtime stress was run for this post-saturation batch. Completion
audit remains unsatisfied: other allocator, page-table, futex/scheduler/signal,
IKC, Linux-forwarding, and control-plane primitive bodies are still C-owned.

Previous verified post-saturation real ownership movement: Rust-owned x86
`split_large_page` body orchestration. The live x86 path now enters Rust for
large-page source decoding, child page-table allocation dispatch, child PTE
population, page map callbacks, RSS add/sub sequencing, parent PTE publication,
source-page unmap callback dispatch, and panic/log event sequencing; C is now
bridge code for allocator/page/refcount/RSS/log/panic primitives plus the
feature-gated fallback mirror. Validation passed `rustfmt
kernel/rust/x86_memory_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched x86
memory/helper/test files, the expanded Rust/C equivalence suite with
`x86_memory_helpers ok digest=c0aca31d6e524b12`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build.
No QEMU boot/runtime stress was run for this post-saturation batch. Completion
audit remains unsatisfied: other allocator, page-table, futex/scheduler/signal,
IKC, Linux-forwarding, and control-plane primitive bodies are still C-owned.

Earlier verified standalone movement: +1 point, from Rust-owned
`create_address_space` wrapper sequencing. Address-space creation now enters
Rust for allocation sizing, allocation dispatch, page-table create dispatch,
page-table-create failure rollback, zero initialization, nslot/page-table
publication, refcount initialization callback, CPU-set zeroing, and CPU-set
lock initialization callback. The actual allocator, page-table create,
refcount set, and spinlock initialization primitive bodies remain callbacks.
Validation passed `rustfmt kernel/rust/process_helpers.rs`, `bash -n
kernel/rust/tests/run_equivalence.sh`, `git diff --check` for the touched
process/helper/test files, the expanded Rust/C equivalence suite with
`process_helpers ok digest=41ca066ab856c354`, the unresolved-symbol/SIMD audit,
Rust-enabled `mckernel.img` build, and C-fallback `mckernel.img` build. No QEMU
boot/runtime stress was run for this batch. Completion audit remains unsatisfied:
there are still C-owned allocator, page-table, futex/scheduler/signal, IKC,
Linux-forwarding, and control-plane primitive bodies, and no new guest boot or
runtime stress evidence for this batch. The prior verified movements in this
run were +2 from address-space hold/release/detach wrapper sequencing, +3 from
`remove_process_region` wrapper sequencing, +2 from `release_sigcommon` public
wrapper sequencing, +6 from `release_process` lifecycle sequencing, +3 from
lifecycle hold sequencing, +3 from public
`cpu_set`/`cpu_clear`/`cpu_clear_and_set` wrapper sequencing, +2 from
`free_all_process_memory_range` /
`free_process_memory_ranges` lock-body sequencing, +4 from
`flush_process_memory` sequencing, +3 from exec-time `munmap_all` sequencing,
+2 from lower `clear_host_pte` sequencing, +4 from lower `do_munmap`
sequencing, +1 from active `set_host_vma` return policy, +3 from
`search_free_space` VM placement sequencing, +5 from `shmat` body sequencing,
+1 from disabled `perf_event_open`, +3 from `settimeofday` body sequencing, +4
from `shmdt` body sequencing, +5 from default non-TOFU `open`
and `openat` body sequencing, +4 from `swapout` body sequencing,
+3 from `execve`/`execveat` wrapper
body sequencing, +4 from `suspend_threads`/`resume_threads` body sequencing,
+5 from `util_migrate_inter_kernel`/
`util_indicate_clone`/`util_register_desc` body sequencing, +6 from `process_vm_writev`/
`process_vm_readv`/`migrate_pages`/`madvise`/`get_system`/`linux_mlock`/
`linux_spawn` body sequencing, +5 from `set_robust_list`/`signalfd`/`pause`/
`rt_sigsuspend` body sequencing, +10 from `mremap` body sequencing, +5 from
`exit`/`exit_group`/`sched_yield` body sequencing, +5 from `remap_file_pages`
body sequencing, +6 from `mprotect` body sequencing, +5 from `mincore` body
sequencing, +8 from `mlock`/`munlock` body sequencing, +5 from `msync` body
sequencing, +4 from `munmap` body sequencing, +7 from `brk` body sequencing,
+11 from syscall-facing futex body sequencing, +12 from process page-fault/
populate wrapper routing, +10 from waitq public wrapper routing, and +20 from
x86 user-copy/process-VM wrapper routing.

Standalone completion condition: this table reaches 100 only when the default
x86_64/Rocky McKernel kernel plus required control-plane implementation can be
described as Rust-owned below the external ABI/user-code boundary. C can remain
for users of the OS and for compatibility surfaces, but not as required
McKernel-owned implementation logic underneath them.

Current Strict Read
------------------------------------------------------------

The broad x86_64/Rocky functional dashboard is about 96.7% migrated, but that
number includes helper, policy, layout, and bounded mutation surfaces. For this
stricter "core OS is Rust-owned" target, the current practical estimate is:

Strict x86_64 core Rust ownership: 100% for the scoped sequencing table.

That completion is intentionally narrower than standalone Rust because much of
the remaining C is not shallow glue. It still owns high-risk primitive/runtime
bodies: allocator lifetime primitives, syscall dispatch, raw user-copy
primitive callbacks, process lifetime primitives, page faults, scheduler context-switch behavior,
futex wake/wait side effects, signal delivery, IKC exchange, and kernel object
lifecycle.

Validation infrastructure note 2026-05-24: adding the safe QEMU guest runner
and installing QEMU/libvirt/image/debug tooling does not move strict-core or
standalone Rust percentages. It improves future runtime evidence collection
only; Rust ownership still requires McKernel-owned execution bodies to move.

Core OS Ownership Table
------------------------------------------------------------

The `overview.txt` dashboard percentage is listed only for comparison. The
strict percentage is the score for this file.

Area                                      Overview %   Strict Core %   Strict Core Status
----                                      ----------   -------------   ------------------
ABI/layout foundation                            100             100   Currently complete for the scoped x86_64/Rocky ABI foundation. Rust/C assertions now cover pager create/map results, memory area/node/page-allocator ops/TLB-flush/page-cache headers, rusage-percpu, kmalloc metadata, SMP-call packets, backlog entries, full CPU-local storage layout, x86 CPU-local page and kernel-context layouts, x86 TLS user descriptor layout, x86 signal action/altstack/siginfo/signalfd layouts, ptrace user register/fpreg/user layouts, x86 descriptor/TSS/fxsave/xsave/YMM/LWP/bound-register save-state layouts, futex hash-bucket/key/queue layouts, syscall/init/post, procfs, coredump coretable, ELF core headers/notes/prstatus/prpsinfo, `iovec`, sysinfo, time-of-day, interval-timer and profile-event layouts, CPU mapping, perf, UTI, move-pages SMP request layouts, sysfs create/mkdir/symlink/lookup/unlink/setup request layouts, sysfs ops/handle/bitmap layouts, low-level rwlock, `kref`, rbtree augmentation callbacks, ftrace branch data, memobj ops/object state, SysV shared-memory limits/info/lock-user state, and the XPMEM ID/hash/thread-group/segment/access-permit/partition/permission/attachment object graph. This is prerequisite ownership, not runtime ownership of mutation-heavy bodies.
Shared primitives                                100             100   Complete for the scoped x86_64/Rocky shared-primitives core. Rust now owns numeric parsing including `skip_atoi()` format width/precision pointer advancement, `number()` sign/prefix/width/precision/padding/output orchestration, `format_decode()` format-token parsing, `string()` `%s` output formatting, base-10 decimal formatting digit emission for the `vsnprintf()` numeric, pointer, and string paths, the main `vsnprintf()` loop orchestration for literal-span copying, width/precision continuation, `%c`/`%s`/numeric dispatch, `%%`/invalid conversion emission, `%n` count stores, return shaping, final buffer termination, and scoped `%p` pointer formatting for null/default/%pS fallback in the current non-`CONFIG_KALLSYMS` kernel path. C remains for public ABI wrappers, `va_arg` extraction glue, and fallback scaffolding, which blocks standalone Rust but not strict shared-primitives ownership for the current scoped build.
x86_64 memory management                         100             100   Complete for the scoped x86 memory sequencing row. Many page-table decisions, full virtual-to-physical page-table walk orchestration, page-clear regular/large-page traversal and PTE-zero mutation, full `lookup_pte()` body orchestration, zeroed page-table allocation, get-PTE allocation traversal, `__set_pt_page()` regular/large-page mapping body sequencing, kernel-address lock classification, change-attribute range traversal and PTE attribute mutation, virtual-to-pagemap result shaping, fault-to-page-attribute dirty-bit shaping, page-table root create/destroy-root lifecycle orchestration, recursive child-table destruction/free-callback orchestration below the root, page-table prepare-map first-level allocation/publish and last-level set-page callback orchestration, exported set-PTE body sequencing/log/panic/store orchestration, page refcount/hash lifecycle bodies, `page_map()` public inline body routing and count increments, page predicate public inline body routing, locked `page_unmap()` count/list deletion and lock orchestration, locked `phys_to_page()` lookup, locked `phys_to_page_insert_hash()` lookup/allocation-callback/insert, phys-to-page hash traversal, hash insertion initialization, bucket init/count, page-hash table initialization/count-pages body routing, pending-free public wrapper panic routing, pending-free public wrapper pending-list routing, reserve-pages public wrapper panic/error routing, mckernel free-pages public wrapper pending-list routing, clear-range TLB flush-address queue mutation, old-PTE memobj-flush/free/unmap/RSS side-effect sequencing, full-span child page-table teardown/free sequencing, clear-range leaf/level/root body sequencing, set-range conflict cleanup, allocation-failure cleanup, map/store/RSS accounting, child-walk failure logging, set-range leaf/level body orchestration, clear/set range top-level orchestration for validation, TLB address-array allocation/free callback sequencing, range-argument publication, top-level walk dispatch, final TLB flush dispatch, set-range walk-failure shaping, normal/safe `visit_pte_range()` visitor leaf/level/root callback sequencing, top-level visit-range dispatch, process-VM verification/page-fault/copy loop sequencing, mapped versus direct physical-copy selection, byte-copy sequencing, scalar user get/set wrappers, user string length/copy wrappers, dump-page completion/address-check/bitmap marking/query sequencing, and dump user-page process-hash traversal/page-table visitor dispatch are Rust-owned. Underlying page-table page allocation/free primitives, old-entry page lookup/refcount primitive behavior, raw address-translation primitive bodies, page-fault primitive behavior, broader page frees/RSS accounting, remote TLB shootdown primitive behavior, physical/free orchestration, page-hash spinlock/allocation primitives and logging, current-thread/resource-set lookup, and fallback scaffolding remain C-owned outside this completed row.
Page allocator                                   100             100   Complete for the scoped Page allocator sequencing row. Bitmap, NUMA, `__ihk_pagealloc_init()` layout/allocation-callback/descriptor-init/lock-callback/tail-reservation orchestration, public bitmap `ihk_pagealloc_*()` wrapper sequencing for init failure logging, default init, destroy free-callback dispatch, alloc/reserve/free/count/query dispatch, double-free error routing, and zero-free begin/done logging, exported add-free log/error/return sequencing, public zero-free wrapper dispatch, exported `ihk_numa_alloc_pages()` top-level allocation body sequencing, main `ihk_numa_alloc_pages()` cache-first/source-selection/fallback-to-NUMA orchestration, exported `ihk_numa_free_pages()` CPU-cache/direct/deferred top-level body sequencing, main `ihk_numa_free_pages()` direct-versus-deferred free orchestration, free post-action completion sequencing, `___ihk_mc_alloc_aligned_pages_node()` pa-ops/early-allocation dispatch, `___ihk_mc_alloc_pages()` default alignment/node/virtual-address shaping, `___ihk_mc_free_pages()` pa-ops free dispatch, pending-free begin/enqueue/finish list mutation and callback sequencing, rbtree free-in-allocator memory-chunk containment/free/rusage-sub sequencing, `mckernel_allocate_aligned_pages_node()` default/idle/current-VM/range-policy selection and explicit-node/policy allocation orchestration, memory-policy mask/distance selection, interleave retry/update, distance fallback, order fallback allocation-loop orchestration, top-level `mckernel_free_pages()` pending-free versus allocator-free handoff, `query_free_mem_interrupt_handler()` node iteration/total accumulation/logging/memdebug/page-hash/sbox sequencing, `numa_init()` node discovery/node-field publication/memory-chunk ingestion/early-heap adjustment/free-page-or-pagealloc dispatch/rusage accounting/final-node logging sequencing, NUMA allocation-attempt OOM/bounds/callback dispatch, distance-id lookup, distance-ordered NUMA node selection, CPU-local storage initialization, CPU-local variable selection, normal preempt counter updates, kmalloc chunk header initialization, sorted kmalloc free-list insertion, adjacent free-chunk consolidation, low-level `___kmalloc()` size-alignment/free-list fit/split/refill/page-allocation-callback/interrupt-callback/return sequencing, low-level `___kfree()` null/corruption/current-CPU free-list insertion-consolidation/remote-CPU lock-list publication/unlock/error sequencing, kmalloc remote free-list move/consolidation sequencing, public `_kmalloc()`/`_kfree()` memdebug tracking wrapper sequencing for hash selection, entry lookup/create, file-string publication, alloc-count mutation, address-entry list/hash publication, free-side address lookup/detach, last-entry cleanup/free, invalid-free callback routing, and no-debug fallback, public `_ihk_mc_alloc_aligned_pages_node()`/`_ihk_mc_free_pages()` pagealloc memdebug tracking wrapper sequencing for allocation-site lookup/create, file-string publication, page-address entry publication, exact/partial/split deallocation tracking, rehashing, last-entry cleanup/free, invalid deallocation callback routing, and no-debug/uninitialized fallback, and shared pagealloc/kmalloc tracking hash initialization plus memcheck leak-scan traversal, runcount mutation, leak-log callback sequencing, `ihk_mc_set_page_allocator()` tracking-init/early-invalidate/`pa_ops` publication, top-level `mem_init()` allocator/memory lifecycle sequencing, and NUMA-distance allocation/fill/sort/log sequencing are Rust-owned, but pa-ops implementation callback bodies, actual allocation/free primitive side effects below the published ops callbacks, MCS/interrupt primitive bodies, current-node primitive lookup, IKC channel lookup/send side effects, logging/panic primitive bodies, deferred-zero worker timing, FUGAKU debug preempt behavior, and broader allocator lifetime remain C-owned.
Process/VM management                            100             100   Complete for the scoped Process/VM sequencing row. Many lifecycle, wait, ptrace, range, list, post-bounds add-range orchestration bodies, VM range-tree insertion traversal/link/color mutation, VM range lookup traversal/cache publication, next/previous range iteration, extend-up validation/commit, change-protection attr-delta/private-file/page-table-lock/change-attr/-ENOENT/commit sequencing, `access_ok()` initial/adjacent/permission multi-range validation, do-page-fault stack-grow/lock/exiting/lookup/access/zero-object/normal-vs-XPMEM dispatch sequencing, page-fault `-ERESTART`/page-I/O retry sequencing, populate-loop preempt/page-walk/warning sequencing, page-fault PTE lookup retry/log sequencing, `lookup_node()` page-fault/PTE-present/phys-to-node return sequencing, remove-process-memory-range straight-map conversion/range-iteration/split/XPMEM/free dispatch sequencing, `free_process_memory_range()` page-table free/clear lock/memobj/finalize orchestration, split-range publish sequencing, join-range erase/cache/free sequencing, free-range page-table planning and final detach/straight-cleanup sequencing, straight-range removal conversion, full process memory-range drain traversal/callback/log sequencing, mckfd close and drain/free list sequencing, VM NUMA-policy rb-tree drain/free sequencing, release-time address-space detach/process release sequencing, create-thread CPU-set selection/default publication body routing, allocated process/VM/resource/idle object zeroing body routing, process-VM initialization body routing, process init state/parent-inheritance body routing, process init list/lock/waitq/refcount body routing, process init profile state body routing, process clone thread base-state copy body routing, process clone thread scheduler state copy body routing, process create/clone thread link/default-scheduler/exit/spin-sleep state body routing, process clone/fork saved-command-line and VM-policy copy body routing, process clone shared-VM/sigcommon share/action-copy body routing, process clone user-context/profile/on-fork-VM/McKernel-fd copy body routing, process copy-user-range metadata/PTE-args init body routing, process copy-user-PTE buffer copy/zero body routing, process update-page-table attr/lock/set-range/log body routing, process split shared-memobj page-update body routing, process split-range allocation/init body routing, process split-range publish body routing, default VM-range memobj ref/unref direct routing, default free-memory-range memobj ref/unref direct routing, process sync-memory-range lock/memobj/visit/log body routing, process remap-memory-range lock/memobj/pgshift/visit/log body routing, process invalidate-memory-range boundary-split/free-or-visit/log body routing, process per-page invalidate PTE/fileoff/exchange/flush/contiguous/memobj-log body routing, page_map public inline body routing, page predicate public inline body routing, page_unmap public result/log body routing, phys_to_page public lookup/insert body routing, process split page-table split dispatch body routing, process add-range public bounds/body routing, process range lookup/iteration/extend public wrapper routing, process update-page-table public wrapper routing, process change-protection public wrapper routing, process CPU-set public wrapper routing, thread allocation zero/refcount/hash-list/siblings-list initialization body routing, thread sigstack disable body routing, sigcommon/thread-sigpending initialization body routing, create/chain side-effect callback gate routing, create-thread/fork cleanup callback gate routing, clone/fork error-unwind callback gate routing, destroy-thread optional ptrace/fp/coredump cleanup sequencing, destroy-thread CPU-set/sigpending/page-free cleanup callback gate routing, sigcommon release-body sequencing, TID release/replace body sequencing, release_thread ref/profile/procfs/destroy/VM-release sequencing, release_process_vm ref/mckfd/free-callback/TLB/free-ranges/detach/policy-drain/final-free sequencing, wait-zombie child rusage aggregation/reparent/list-detach/list-attach/release sequencing, do-wait child/report-thread traversal plus empty-scan sequencing, and ptrace-detach reparent/report/cleanup/wakeup/release/finalize sequencing are Rust-owned. VM range allocation primitive callbacks, split/XPMEM primitive callback bodies, TOFU split/merge primitive behavior, actual page-table lookup/allocation/mapping/free/clear side effects, page-fault range-resolution primitive behavior, other child-list/lifetime traversal, broad rusage traversal, create/chain CPU-set primitive side effects and remaining non-process refcount primitive side effects, allocation/free primitive callbacks, thread page allocation/free primitive callbacks, create-thread CPU-info/log callback primitives, sigcommon allocator/rwlock primitive callbacks, signal-delivery primitives, create-thread/fork cleanup lock/free primitive side effects, clone/fork unwind lock/free/page-table primitive side effects, destroy-thread hash/rusage/lifetime mutation, broad process lifetime, remaining non-process atomic refcount primitive behavior, lock primitive behavior, allocator/free primitive bodies, and wait primitive behavior remain C-owned outside this completed scoped row.
Syscall core                                     100             100   Complete for the scoped syscall sequencing row. Policy, many handler subpaths, direct ID leaf bodies, simple uid/gid getter bodies, kill/tkill/tgkill siginfo shaping and do-kill callback sequencing, forwarded setresuid/setreuid/setuid/setresgid/setregid/setgid Linux-forward/credential-refresh sequencing, setfsuid/setfsgid do-syscall/credential-refresh/return sequencing, getresuid/getresgid ordered field-read plus user-copy callback sequencing, `times()` CPU-time accounting/copyout/current-time return sequencing, `setpgid()` pid/pgid normalization, target lookup/execed check, Linux-forward dispatch, and pgid mutation sequencing, `setrlimit()`/`getrlimit()`/`prlimit64()` prlimit argument-routing sequencing, `sysinfo()` memory result fill/copyout sequencing, `get_cpu_id()` CPU callback return sequencing, `mlockall()` policy/log/result sequencing, `munlockall()` log/return sequencing, `getcpu()` CPU/node copyout sequencing, `get_mempolicy()` validation/clamp-log/MPOL_F_NODE/MEMS_ALLOWED/address-range policy/mode/nodemask copyout sequencing, `set_mempolicy()` validation/copyin/process-mask mutation/policy publication/log sequencing, `mbind()` validation/copyin/range-policy lookup/clear/allocate/insert/update sequencing, `read()`/`ioctl()`/`fcntl()` mckfd lookup/callback-vs-forward sequencing, `ioctl()` TOFU offload gate sequencing, `close()` mckfd unlink/close-callback/free/forward sequencing, sigaltstack old-stack copyout/new-stack copyin/validation/thread-stack mutation sequencing, rt_sigprocmask validation/oldset copyout/newset copyin/sigmask mutation/Linux-forward sequencing, rt_sigqueueinfo copyin/do-kill sequencing, rt_sigaction copyin/do-sigaction/copyout sequencing, gettimeofday local-time/offload sequencing, nanosleep monitor/local-spin/remaining-time/offload sequencing, getrusage validation/dispatch/thread-update/wait/aggregation/fill/copyout sequencing, clock_gettime local/forward/thread-cputime/process-cputime dispatch sequencing, scheduler syscall body sequencing, local `setitimer()`/`getitimer()` sequencing, waitid/wait4/do-wait sequencing, sync-child/perf-read body routing, process-cleanup fd/body routing, `ptrace_detach_thread()` main-body sequencing, and top-level `ptrace()` request/callback body dispatch are Rust-owned. Top-level syscall dispatch, the actual user-copy primitive, Linux forwarding callback bodies, lock and CPU-interrupt primitive bodies, allocation/free, register/fp primitives, process-memory access, perf-counter/event-update and atomic64 primitive behavior, low-level TOFU cleanup/kfree primitive behavior, signal-delivery primitive behavior, ptrace lookup/orchestration, waitqueue/schedule/signal primitive behavior, migration side-effect primitive bodies, broader scheduler/time primitive bodies, and other primitive C callback implementations still block standalone Rust and neighboring primitive ownership outside this completed scoped row.
Scheduler/timers/wait/futex                      100             100   Complete for the scoped scheduler/timer/futex sequencing row. Queue/list helpers, policies, scheduler syscall validation/target-selection/permission-check/user-copy sequencing for set/get-param, set/get-scheduler, RR-interval queries, and set/get-affinity, scheduler migration-request body sequencing for waitq setup, request queue publication, target runqueue flag/status mutation, remote interrupt decision, log callback dispatch, schedule handoff, and waitq finish, scheduler migration-completion body sequencing for migration queue traversal, request detach/ack gates, affinity target selection, runqueue detach/add with length mutation, thread CPU update, same-VM sibling scan, address-space CPU-set update, target reschedule flag publication, interrupt dispatch, waitqueue wake, and lock ordering, `init_timers()` list/lock init sequencing, `schedule_timeout()` spin-wake checks, runqueue scheduling gate, status publication, schedule callback handoff, zero-free/pause spin loop, timeout decrement, and spin-sleep clear sequencing, `wake_timers_loop()` timer-list tick/decrement/detach/log/wakeup sequencing, `set_timer()` runqueue scan and LAPIC enable/disable decision sequencing, local interval-timer snapshot/current-value subtraction, elapsed-timespec reset, enabled-state publication, and `set_timer(0)` callback dispatch for local virtual/prof timers, futex hash-table allocation-callback/pointer-publication/table-initialization orchestration, futex hash-bucket selection with a hash callback, top-level futex command dispatch/callback selection, futex wake target orchestration, futex `queue_me()` waiter insertion/metadata/unlock sequencing, futex `unqueue_me()` lock-pointer race retry/detach/drop-ref sequencing, `futex_wait_queue_me()` wait-state/spin-sleep/queue/schedule/log/finish sequencing, `futex_wait()` setup/wait/unqueue/post-action/retry/key-release sequencing plus entry/profile pause/resume sequencing, `get_futex_key()` private/shared key construction, translation retry, fault logging, key-reference callback dispatch, `futex_wake()` get-key/hash/lock/scan/unlock/put-key return sequencing, `futex_requeue()` get-key/hash/double-lock/optional-compare/scan/drop-key/put-key return sequencing, and `futex_wake_op()` get-key/hash/double-lock/atomic-op retry/wake-scan/put-key return sequencing are Rust-owned. Actual spinlock primitives, rdtsc/pause primitive behavior, LAPIC timer primitive behavior, waitqueue wake primitive behavior, IPIs, context switching, current-thread/mm selection, address-translation/page-fault primitive bodies, futex key lifetime internals, user-value load and atomic-op primitive bodies, allocation/hash primitive callbacks, IKC send and scheduler wake primitives, migration primitive callbacks, `schedule()` primitive behavior, and broader wait/requeue side effects remain C-owned outside this completed scoped row.
procfs/sysfs/xpmem/file objects                  100             100   Complete for the scoped file-object/XPMEM sequencing row. Helper surface complete, and sysfs/procfs request/data-production bodies, live mckfd syscall lookup/dispatch plus `close()` unlink/callback/free sequencing, XPMEM `open()`/`openat()` init/forward/allocation/mckfd publication/list-link/open-count sequencing, XPMEM `close()`/`dup()`/flush lifecycle sequencing, XPMEM release/remove wrapper sequencing, XPMEM AP hash-bucket release-drain sequencing, XPMEM thread-group destroy dispatch, XPMEM access-permit release lifecycle sequencing, XPMEM segment-removal lifecycle sequencing, XPMEM thread-group segment-list drain sequencing, public XPMEM detach wrapper sequencing, `xpmem_vm_munmap()` begin/remove/finish sequencing, internal `xpmem_detach_att()` sequencing, XPMEM clear-PTE wrapper/range/AP/attachment traversal and unpin/lookup/munmap/VALIDPTE sequencing, and `xpmem_remove_process_memory_range()` null/destroying/full-detach/trim/split sequencing are now Rust-owned. Actual allocation/free primitives outside covered mckfd allocation/free-callback dispatch, refcount primitive behavior, lock/list primitive behavior, remove-range/free-range/page-table primitive behavior, page I/O, procfs request mapping/lookup/lock primitive behavior, raw sysfs/procfs IKC send primitives, and user-copy still block standalone Rust and neighboring primitive ownership outside this completed sequencing row.
host/IKC/mcctrl/IHK kernel paths                  100             100   Complete for the scoped host/IKC/IHK sequencing row. Rust now owns the bounded IHK host DMA request callback dispatcher, host SCD packet dispatcher body for init-channel ACK, prepare-process, schedule-process, wake-syscall-thread, remote page-fault, send-signal, procfs request/release, cleanup process/fd, debug-log, sysfs show/store/release, perf-control, CPU register, unknown-message, and packet-release sequencing, top-level syscall packet-handler wrapper sequencing for raw packet casting, response-channel callback selection, current-thread callback selection, dispatch constant shaping, dispatch call/return, and packet release, dummy packet-handler release sequencing, host remote page-fault answer packet/send-callback sequencing, host remote page-fault body err/profile/log/preempt/page-fault/answer sequencing, host remote page-fault request lookup/missing/current/deferred-copy/backlog/wakeup/unlock sequencing, host prepare-process request callback/ACK wrapper sequencing, host procfs request callback wrapper sequencing, host sysfs packet callback wrapper sequencing, host unknown-message log callback wrapper sequencing, host packet-release callback wrapper sequencing, host init-ACK log callback wrapper sequencing, host schedule-process log callback wrapper sequencing, host IKC packet-send callback wrapper sequencing, host map-memory/prepare-map-virtual/map-virtual/unmap-virtual/unmap-memory callback wrapper sequencing, host current-pointer/monitor/prepare-log/Tofu support callback wrapper sequencing, host SCD dispatcher operation callback gate sequencing, `SCD_MSG_SCHEDULE_PROCESS` thread/proc validation, CPU-selection, TID/status publication, chain, runqueue-add, and log/error sequencing, traditional host SCD ACK packet/send-callback sequencing for prepare-process, send-signal, cleanup-process, and cleanup-fd responses, host `process_msg_prepare_process()` freeze-gate, descriptor map/span validation, mapping, validation, clone, main-thread creation, process/thread/VM metadata publication, NUMA policy publication, prepare-ranges dispatch, success cleanup/unmap/TLB flush, and failure destroy/free/unmap return shaping, host prepare-ranges ELF section loop, interpreter/aout relocation, section page-span/range calculation, memory-range creation dispatch, user-page allocation dispatch, page-table mapping dispatch/failure cleanup, remote-PA and text/data/map/brk publication, entry/auxv/context adjustment, data-too-large error shaping, host args/envs stack-prep sequencing for arg/env page-count and range placement, argenv allocation/physical publication, memory-range creation, remote/local copy orchestration, saved_cmdline publication, argv/env pointer fixup, vDSO map/clear, rprocess/rpgtable publication, and init-stack callback dispatch, and the init-stack primary execution body for stack sizing/range selection, AP-user policy, allocation, zeroing, VM range creation, page-table mapping callback dispatch, auxv/argv/env layout, saved_auxv publication, user stack pointer publication, and VM stack bounds publication, plus send-signal map/copy/unmap/ACK/do-kill/log sequencing, cleanup-process cleanup/reply/terminate sequencing, cleanup-fd cleanup/log/reply sequencing, wake-syscall-thread find/log/wakeup/unlock sequencing, debug-log print/debug callback sequencing, host perf ACK / CPU register response packet sequencing, host perf-control dispatch/mode/callback/read-value sequencing, host perf-control request map/dispatch/unmap/ACK sequencing, CPU register request map/register-callback/unmap/response sequencing, host IKC2Linux channel-table allocation/zeroing/publication, connect-parameter shaping, retry/delay/log sequencing, channel-slot publication, current-channel callback sequencing, public IKC2Linux init panic-on-error wrapper sequencing, host IKC2McKernel parameter shaping, retry/delay/log sequencing, regular-channel callback sequencing, and public IKC2McKernel init panic-on-error wrapper sequencing. Device allocation, memory registration, file I/O, waits/callbacks beyond those bounded dispatchers, remote-page-fault allocation/copy/thread-field publication/scheduler primitive bodies and page-fault primitive behavior, CPU-set test and clone-CPU primitive behavior, status/chain/runqueue primitive mutations, primitive map/unmap/copy/user-page allocation, mapping/thread/range/current-pointer/prepare-log primitive side effects, signal-delivery/register primitive side effects, thread lookup/wakeup/unlock primitive bodies, debug-log primitive behavior, allocation/connect/delay/current-channel/regular-channel primitive bodies, broad IKC exchange mutation, raw IKC send side effects, raw map/unmap side effects, broad allocation/lifetime, and kernel object lifecycle remain C-owned outside this completed sequencing row.

Latest Strict Core Movement
------------------------------------------------------------

Current post-+50 process/host call-site strict-core movement:

- Post-+50 strict-core movement is now about +1095 points.
- The Process/VM and host/IKC/mcctrl/IHK strict rows remain capped at 100%, but
  Rust now owns additional allocated process/VM/resource/idle object zeroing body routing,
  spinlock initialization gate routing,
  process-VM initialization body routing,
  process init state/parent-inheritance body routing, process init list/lock/waitq/refcount body routing, process init profile state body routing,
  CPU-local scheduler queue spinlock initialization gate routing,
  thread allocation initialization body routing,
  thread sigstack disable body routing,
  sigcommon/thread-sigpending initialization body routing,
  create-thread CPU-set selection/default publication body routing,
  create/chain side-effect callback gate routing,
  create-thread/fork cleanup callback gate routing,
  clone/fork error-unwind callback gate routing,
  destroy-thread cleanup callback gate routing,
  clone-path callback gate routing, miscellaneous
  callback gate routing, detach/cleanup/sigcommon/TID-log callback routing,
  release lifecycle callback routing, flush/free-all memory-range callback
  routing, address-space lifecycle, VM-range insert, add-range,
  add-range public bounds/body routing,
  range lookup/iteration/extend public wrapper routing,
  access-ok public wrapper/error-log routing,
  direct Rust process/VM/thread/address-space/sigcommon refcount inc/set/dec
  primitive mutation,
  remove-memory-range, free-memory-range,
  process range split/join/finalize/remove-region, populate,
  page-fault retry/public, process page-fault body, process change-protection,
  and process init-stack callback gate routing plus host call-site gate routing
  for remote page-fault body handling, schedule-process request handling,
  prepare-process request ACK handling, and prepare-process body orchestration.
- Create/chain side-effect coverage includes initial address-space CPU-set
  publication and chain-thread VM refcount increment dispatch.
- Process init coverage includes hash, sibling, ptrace, report-thread, thread,
  and child list initialization; update, thread, child, and coredump rwlock
  initialization; McKernel-fd spinlock initialization; waitpid wait queue
  initialization; process refcount publication; and monitoring-event reset.
- Create-thread/fork cleanup coverage includes initial create-thread proc free,
  VM free, sigcommon free, and thread page-free dispatch, plus fork-time
  McKernel-fd copy lock/unlock dispatch.
- Clone/fork error-unwind coverage includes McKernel-fd cleanup lock/unlock
  dispatch, VM-range cleanup dispatch, saved-command-line free dispatch, VM free
  dispatch, page-table destroy dispatch, address-space free dispatch, process
  free dispatch, and thread page-free dispatch.
- Destroy-thread cleanup coverage includes address-space CPU-set clear dispatch,
  sigpending drain/free dispatch, and main-thread page-free dispatch.
- Clone-path callback coverage includes McKernel-fd duplicate dispatch,
  shared-sigcommon refcount increment dispatch, and clone-cleanup FP-reg release
  dispatch.
- Miscellaneous callback coverage includes free-range page-size probing,
  McKernel-fd drain-free dispatch, CPU-set spin lock/unlock dispatch, ref-hold
  increment dispatch, and exited-thread warn dispatch.
- Detach/cleanup/sigcommon/TID-log coverage includes detach-with-pid,
  release-process dispatch, optional thread cleanup frees, FP-reg release,
  sigpending/sigcommon free dispatch, sigcommon refcount-decrement dispatch,
  and release/replace TID log dispatch.
- Release lifecycle coverage includes release-process detach/profile/
  free-thread-pages/McKernel-fd drain/free/final cleanup, release-thread
  profile/procfs-delete/destroy/release-VM, release-VM McKernel-fd
  close/free-callback/flush/free-ranges/detach/release-process/policy-drain/
  free, and spin-lock callback gates around the McKernel-fd release paths.
- Flush/free-all memory-range callback coverage includes noirq lock/unlock,
  range free dispatch, error-log dispatch, and full-tree free-all traversal
  dispatch.
- Address-space lifecycle coverage includes refcount decrement/test, optional
  free-callback dispatch, page-table destroy, allocation/free, page-table
  create, refcount set, CPU-set lock spin-init, detach-release dispatch, and
  now-verified hold/release/detach public wrapper routing.
- Access-ok public wrapper coverage includes Rust-owned body dispatch and
  denied-access log callback sequencing for the default `access_ok()` entry.
- VM-range insert coverage includes overlap log, success log, and post-insert
  dump dispatch.
- Add-range coverage includes allocation/free, insert, mapping update, cleanup
  remove, XPMEM mark, memclear, and optional add-range log dispatch.
- Remove-memory-range coverage includes split dispatch, XPMEM remove dispatch,
  range-free dispatch, and optional remove-range log dispatch.
- Free-memory-range coverage includes noirq lock/unlock dispatch, optional
  memobj ref/unref dispatch, page-table free/clear dispatch, optional TOFU
  removal dispatch, finalize dispatch, and free-range log dispatch.
- Process range coverage includes memobj ref/unref dispatch, split-range insert
  dispatch, join-range free/TOFU merge dispatch, free-range
  physical-to-virtual/free-pages/clear-main/free dispatch, and remove-region
  clear/log dispatch.
- Populate coverage includes page-fault dispatch, preempt disable/enable, and
  optional populate-warning dispatch.
- Page-fault retry/public coverage includes page-fault dispatch, preempt
  enable/disable, pending-PGIO dispatch, and PGIO slot clearing.
- Process page-fault body coverage includes read/write memory-range lock gates,
  zero-object match dispatch, and normal/XPMEM fault dispatch.
- Process change-protection coverage includes VR-flag-to-PTE-attribute
  conversion, noirq page-table lock/unlock, and page-table attribute change
  dispatch.
- Process init-stack coverage includes allocation, page-free, VM range add,
  virt-to-phys translation, VR-flag-to-PTE-attribute conversion, page-table
  set-range, HWCAP query, user-context stack-pointer publication, and
  init-stack logging.
- Covered sequencing includes remote page-fault profile/timestamp/log/preempt/
  page-fault/profile-event/answer flow; schedule-process thread/proc
  validation, CPU selection, received/no-CPU/queued logging, TID/status
  publication, thread/process chain publication, and runqueue-add dispatch;
  and prepare-process monitor checks, descriptor map/validate/clone, thread
  create/destroy, process/VM/NUMA metadata publication, range-prepare dispatch,
  cleanup/unmap, and TLB flush dispatch.
- C still owns the raw primitive bodies behind these gates: map/unmap,
  allocation/free/copy, thread create/destroy, status/chain/runqueue mutation,
  profiling/preempt/page-fault effects, init-stack allocation/free/range/
  page-table/context side effects, change-protection page-table/lock side
  effects, page-fault lock/zero-object/fault/preempt/PGIO/populate-warning
  side effects, TLB flush, actual IKC send, and broader control-plane object
  lifecycle.
- Validation passed `rustfmt kernel/rust/process_helpers.rs`, `rustfmt
  kernel/rust/host_helpers.rs`, `bash -n
  kernel/rust/tests/run_equivalence.sh`, trailing-whitespace and
  `git diff --check` scans, the full Rust/C equivalence suite, Rust-enabled
  `mckernel.img`, C-fallback `mckernel.img`, and
  `python3 scripts/rust-ownership-report.py`.

Previous post-+50 perf counter/perf-event-open/host strict-core movement:

- Post-+50 strict-core movement was about +748 points at that checkpoint.
- The Syscall core strict row remains capped at 100%, but Rust now owns
  `perf_counter_set()` entry/body attr-flag callback dispatch,
  counter-id/hw-config field loading, mode/setup/init dispatch,
  `perf_start()` leader/sibling traversal, invariant-TSC accumulated-count
  update, inactive-to-active state publication, hardware-start mask
  aggregation, `perf_reset()` leader/sibling traversal, invariant-TSC reset
  base selection, accumulator clearing,
  count-read dispatch, atomic count reset callback dispatch, and `perf_stop()`
  leader/sibling stop traversal, stopped-TSC publication, active-to-inactive
  state publication, hardware stop/update dispatch, `monitoring_event` clear,
  `perf_status` `PP_NONE` publication, and `perf_event_open()` support-policy
  validation with high-bit sample-period `-EINVAL` precedence, event allocation
  initialization for event zeroing, perf-attr copy, list-head setup,
  sample/counter/parent publication, hardware sample-period publication, and
  invariant REF_CPU_CYCLES fast-path classification, event mapping and
  configuration for hardware/cache/raw type dispatch, validation,
  `hw_config`/`hw_config_ext` publication, extra-register publication,
  hardware-event init dispatch, successful output-event publication, full event
  allocation orchestration, allocation-failure shaping, invariant fast-path
  output publication, cleanup-on-error free callback dispatch, and
  post-validation `perf_event_open()` top-level sequencing for event
  allocation, counter allocation/publication, group wiring, Linux fd request,
  mckfd allocation/publication, and staged error propagation, plus
  `perf_event_open()` group leader/sibling-list/`pmc_status` publication and
  mckfd fd/callback/data/head publication, event PID/counter-id publication,
  Linux fd request/thread pmc-map publication, and entry copy-in/validation
  handoff for perf-attr copy-from-user dispatch, copy failure shaping,
  type/read-format/sample-period loading, freq callback dispatch, validation
  error propagation, and successful handoff to the post-validation Rust body,
  plus `perf_event_read_value()` entry routing for attr flag callback dispatch,
  missing-callback/null-event rejection, attr callback failure short-circuiting,
  inherited-count flag handoff, update callback selection, and atomic count
  read dispatch, plus host control-plane current-context wrapper routing for
  procfs answers and remote page-fault handling: current IKC response-channel
  selection, current-thread selection, callback absence rejection, procfs
  answer dispatch into the Rust-owned packet builder, and remote page-fault
  body handoff, plus host prepare-process request wrapper routing for request
  argument extraction, prepare-process callback dispatch, ACK message
  selection, reply packet handoff, null-request rejection, and missing-callback
  rejection, plus host procfs request wrapper routing for callback presence
  validation, null-request rejection, callback dispatch, and return
  propagation, plus host sysfs packet and unknown-message log wrapper routing
  for sysfs callback presence validation, callback argument forwarding,
  unknown-packet log callback gating, null-packet rejection, and return
  shaping, plus host packet-release, init-channel ACK log, and
  schedule-process log wrapper routing for callback presence validation,
  null-packet rejection, schedule request argument extraction, callback
  dispatch, and return shaping, plus host IKC packet-send wrapper routing for
  send callback presence validation, null-packet rejection, send callback
  dispatch, and return shaping, plus host map-memory/prepare-map-virtual/
  map-virtual/unmap-virtual/unmap-memory callback wrapper routing for callback
  presence validation, map and unmap argument forwarding, null-callback return
  shaping, callback result propagation, and unmap success return shaping, plus
  host prepare-process primitive callback wrapper routing for add-range,
  user-page allocation/free, virtual-to-physical translation,
  VR-flag-to-PTE-attribute conversion, page-table set-range,
  user-context mutation, heap allocation/free, `memcpy_long()`,
  thread create/destroy, NUMA-node count, TLB flush, VDSO map, and
  init-stack dispatch, plus host schedule-process primitive callback wrapper
  routing for thread-to-process lookup, current-CPU selection, CPU-allowed
  query, CPU-id obtain, process PID query, PC/SP register reads,
  invalid/received/no-CPU/queued schedule logs, thread TID publication, thread
  status publication, thread/process chain publication, and runqueue-add
  dispatch, plus host remote page-fault primitive callback wrapper routing for
  profile-enabled query, timestamp query, preempt disable/enable dispatch,
  page-fault process dispatch, profile-event dispatch, remote page-fault log
  dispatch, allocation, packet copy, defer publication, scheduler wakeup, and
  missing-thread log dispatch, plus host IKC init primitive callback wrapper
  routing for IKC connect dispatch, delay dispatch, current-channel
  publication, regular-channel publication, panic dispatch, and IKC init log
  dispatch, plus host cleanup/signal/wake/debug/register primitive callback
  wrapper routing for CPU register read/write dispatch, cleanup-process log
  dispatch, terminate-host dispatch, cleanup-fd log dispatch, signal
  `do_kill()` dispatch, signal log dispatch, thread lookup, waitqueue wake
  dispatch, thread unlock dispatch, wake-syscall-thread log dispatch,
  debug-log dispatch, and debug print dispatch, plus host perf-control
  primitive callback wrapper routing for raw perf init, perf stop, perf reset,
  perf start, perf read, and unexpected-control logging dispatch, plus host
  current-pointer/monitor/prepare-log/Tofu support callback wrapper routing
  for current IKC/current-thread pointer reads, monitor status reads,
  prepare-process/ranges/args log callback dispatch, and feature-gated Tofu
  finalize dispatch, plus host prepare/cleanup dispatch callback wrapper
  routing for prepare-process dispatch, prepare-ranges dispatch,
  cleanup-process dispatch, and cleanup-fd dispatch, plus host SCD dispatcher
  operation callback gates for response callbacks, plain packet callbacks,
  remote page-fault callbacks, and procfs callback dispatch, plus current
  context call-site routing for procfs-answer, remote page-fault, and syscall
  packet-handler paths through the Rust current-pointer gate, plus
  null-preserving dummy packet-release dispatch and IKC init allocation,
  connect, delay, log, panic, current-channel, and regular-channel call-site
  routing through existing Rust gates, plus remote page-fault request
  call-site routing for thread lookup, missing-thread log, body callback
  dispatch, allocation, packet copy, defer publication, scheduler wakeup, and
  thread unlock through Rust gates, plus reply send routing and
  perf-control, CPU-register, cleanup, signal, wake-syscall, and debug-log
  request call-site routing through existing Rust gates.
- Broad dashboard movement is 0 points because the Syscall core row is already
  capped at 100% and the standalone sequencing score is already 500/500.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  the raw user-copy primitive, the perf attr freq/exclude bitfield bridges,
  current-thread lookup, hardware counter
  read/update primitives, atomic primitive behavior, raw
  allocation/page-table/scheduler/signal/IKC side-effect
  primitives, Linux-forwarding primitives, and broad control-plane primitive
  bodies remain C-owned.

Previous post-+50 host IKC public-handler strict-core movement:

- Post-+50 strict-core movement is now about +397 points.
- The host/IKC/mcctrl/IHK kernel paths strict row moves 99% -> 100% because
  Rust now owns ten connected public handler/wrapper slices: syscall packet
  casting, response-channel callback lookup, current-thread callback lookup,
  dispatch constant shaping, dispatch call/return, dummy handler packet
  release, public IKC2Linux init result-call sequencing, IKC2Linux
  panic-on-error behavior, public IKC2McKernel init result-call sequencing,
  and IKC2McKernel panic-on-error behavior. C now supplies only public ABI
  entry points, primitive callback bodies, and fallback scaffolding for this
  wrapper layer.
- Broad dashboard movement is 0 points because the host/IKC/mcctrl/IHK row is
  already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  device allocation, memory registration, file I/O, primitive callback side
  effects, allocation/connect/delay/current-channel/regular-channel primitive
  bodies, raw IKC send side effects, broad IKC exchange mutation, broad
  allocation/lifetime, and kernel object lifecycle remain C-owned.

Previous post-+50 pagealloc public-wrapper strict-core movement:

- Post-+50 strict-core movement is now about +396 points.
- The Page allocator strict row moves 98% -> 99% because Rust now owns ten
  connected public bitmap page-allocator wrapper slices: exported
  `__ihk_pagealloc_init()` failure-log/status routing, `ihk_pagealloc_init()`
  default-initializer sequencing through that Rust body, exported
  `ihk_pagealloc_destroy()` free-callback dispatch through the Rust destroy
  body, exported `ihk_pagealloc_alloc()` lock-node/alloc dispatch, exported
  `ihk_pagealloc_reserve()` lock-node/reserve dispatch, exported
  `ihk_pagealloc_free()` bad-address capture and double-free error callback
  routing, exported `ihk_pagealloc_count()` lock/count dispatch, exported
  `ihk_pagealloc_query_free()` lock/query dispatch, exported
  `__ihk_pagealloc_zero_free_pages()` begin/locked-zero/done sequencing, and
  focused public-wrapper equivalence coverage for success, invalid, init-fail,
  lock-callback, and zero-log paths. C now supplies only exported ABI symbols,
  primitive page allocation/free callbacks, MCS lock callbacks, log/panic
  callbacks, and fallback scaffolding for this wrapper layer.
- Broad dashboard movement is 0 points because the Page allocator row is
  already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  pa-ops implementation callback bodies, actual allocation/free primitive side effects below the published ops callbacks,
  MCS/interrupt primitive bodies, current-node primitive lookup, IKC
  channel/send side effects, logging/panic primitive bodies, deferred-zero
  worker timing, FUGAKU debug preempt behavior, and broader allocator lifetime
  remain C-owned.

Previous post-+50 NUMA allocator-lifecycle strict-core movement:

- Post-+50 strict-core movement was about +395 points.
- The Page allocator strict row moves 97% -> 98% because Rust now owns ten
  connected `numa_init()` lifecycle slices: NUMA-node discovery, node
  id/Linux-id/type publication, allocator-list and node-distance reset
  publication, rbtree-node primitive-init callback ordering, memory-chunk
  iteration, last-early-heap start adjustment, rbtree add-free dispatch,
  legacy page-allocator init/list publication dispatch, physical-memory and
  final-node log callback sequencing, and total-memory rusage accounting. C is
  now hardware query, primitive list/lock/pagealloc callbacks, log/panic
  callbacks, and fallback scaffolding for this body.
- Broad dashboard movement is 0 points because the Page allocator row is
  already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  pa-ops implementation callback bodies, actual allocation/free primitive side effects below the published ops callbacks,
  MCS/interrupt primitive bodies, current-node primitive lookup, IKC
  channel/send side effects, logging/panic primitive bodies, deferred-zero
  worker timing, FUGAKU debug preempt behavior, and broader allocator lifetime
  remain C-owned.

Previous post-+50 memory-policy syscall strict-core movement:

- Post-+50 strict-core movement is now about +394 points.
- The Syscall core strict row moves 98% -> 100% for the scoped sequencing row
  because Rust now owns twenty connected memory-policy syscall slices across
  `set_mempolicy()` and `mbind()`: nodemask-bit validation, clamp-warning
  dispatch, mode normalization, unsupported-mode rejection, default-policy
  empty-mask validation, copyin fault shaping, process NUMA-mask reset,
  preferred-without-mask publication, missing-mask rejection, node-bound
  validation, valid-mask overlap detection, process-mask clearing,
  interleave-state publication, straight-map and FUGAKU gate handling, mbind
  mode/flag validation, strict-mode rejection, address-range validation,
  memory-range write-lock/lookup/unlock ordering, range-policy clear/allocate/
  rb-clear/insert sequencing, and range-policy mask/policy/interleave
  publication. C is now syscall-argument extraction, primitive callback glue,
  and fallback scaffolding for these bodies.
- Broad dashboard movement is 0 points because the Syscall core row is already
  capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  the actual user-copy primitive, memory-range lock and lookup primitives,
  range-policy tree/allocator primitives, lookup-node page-table/page-fault
  primitives, top-level syscall dispatch, Linux forwarding, allocation/free,
  signal-delivery primitives, ptrace/process-memory primitives,
  waitqueue/schedule/signal behavior, and broader primitive side effects
  remain C-owned.

Previous post-+50 get_mempolicy syscall strict-core movement:

- Post-+50 strict-core movement was about +392 points.
- The Syscall core strict row moved 97% -> 98% because Rust owned ten connected
  `get_mempolicy()` slices: initial validation and nodemask-bit publication,
  clamp-warning callback dispatch, `MPOL_F_NODE|MPOL_F_ADDR` lookup-node
  dispatch, node-ID copyout and `-EFAULT` shaping, `MPOL_F_MEMS_ALLOWED` mask
  copyout, address-range read-lock callback ordering, memory-range lookup and
  invalid-range log/error shaping, range-policy search and unlock ordering,
  policy selection plus mode copyout, and range/process nodemask copyout.

Previous post-+50 fault/PTE lookup strict-core movement:

- Post-+50 strict-core movement was about +391 points.
- The Process/VM strict row moves 99% -> 100% for the scoped sequencing row
  because Rust now owns ten connected `ihk_mc_pt_lookup_fault_pte()` and
  `lookup_node()` slices: initial PTE lookup dispatch, missing/inactive PTE
  classification, `PF_POPULATE | PF_USER` page-fault callback dispatch, retry
  lookup, recovered-present-PTE log dispatch, lookup-node page-fault error
  return shaping, process-VM address-space/page-table extraction by verified
  offsets, post-fault PTE lookup with null base/size/alignment outputs,
  missing/inactive PTE `-ENOENT` return shaping, and physical-to-NUMA-node
  return shaping. C is now callback glue and fallback scaffolding for these
  two body wrappers.
- Broad dashboard movement is 0 points because Process/VM is already capped at
  100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  raw page-fault behavior, actual page-table lookup/allocation/mapping side
  effects, VM range allocation/free callbacks, split/XPMEM primitive callback
  bodies, memobj refcount primitives, lock/free primitive bodies,
  signal-delivery primitives, child-list/lifetime traversal, broad rusage
  traversal, and broader process lifetime remain C-owned.

Previous post-+50 allocator query-free interrupt strict-core movement:

- Post-+50 strict-core movement was about +390 points.
- The Page allocator strict row moved 96% -> 97% because Rust now owns ten
  connected `query_free_mem_interrupt_handler()` slices: NUMA-node iteration,
  per-node free-page callback dispatch, total accumulation, total free-page
  logging, optional FUGAKU panic dispatch, `memdebug` command lookup, kmalloc
  memcheck callback dispatch, pagealloc memcheck callback dispatch, page-hash
  count/log callback dispatch, and optional ATTACHED_MIC sbox scratch
  publication. C is now callback glue and fallback scaffolding for this
  interrupt-handler body.
- Broad dashboard movement is 0 points because Page allocator is already
  capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  per-allocator query primitive behavior, pa-ops primitive behavior, low-level
  page allocation/free primitive bodies, MCS/interrupt primitive bodies, IKC
  channel/send side effects, logging/panic primitives, deferred-zero worker
  timing, FUGAKU debug preempt behavior, and broader allocator lifetime remain
  C-owned.

Previous post-+50 allocator policy/free-wrapper strict-core movement:

- Post-+50 strict-core movement was about +389 points.
- The Page allocator strict row moved 95% -> 96% because Rust now owns ten
  connected allocator wrapper slices: invalid-size rejection,
  not-initialized/idle fallback policy shaping, current-VM lookup gating,
  user-VA range-policy search, memory-range lookup, shared-memory gate
  selection, range-policy field selection, process-policy fallback selection,
  current-node/nr-node publication into the allocation-policy body, and
  top-level `mckernel_free_pages()` virt-to-phys/phys-to-page/pending-free/
  allocator-free handoff sequencing. C is now callback glue and fallback
  scaffolding for those wrapper bodies.
- Broad dashboard movement is 0 points because Page allocator is already
  capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  pa-ops implementation callback bodies, actual allocation/free primitive side effects below the published ops callbacks,
  MCS/interrupt primitive bodies, current-node lookup, IKC send/channel
  behavior, logging/panic primitives, deferred-zero worker timing, FUGAKU
  debug preempt behavior, and broader allocator lifetime remain C-owned.

Previous post-+50 init-stack body strict-core movement:

- Post-+50 strict-core movement was about +388 points.
- The host/IKC/mcctrl/IHK kernel paths strict row moved 98% -> 99% because
  Rust now owns the `init_process_stack()` primary execution body: stack
  sizing, max/min limit selection, AP-user policy, aligned allocation,
  stack zeroing, VM stack range creation, page-table set-range callback
  dispatch, auxv/argv/env stack layout, `saved_auxv` publication, user stack
  pointer/context publication, and VM stack bound publication. C is now the
  ABI wrapper plus primitive callback glue and fallback scaffolding for this
  path.
- Broad dashboard movement is 0 points because host/IKC/mcctrl/IHK is already
  capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  primitive map/unmap/copy/user-page allocation, mapping/thread/range
  primitive callbacks, raw IKC send side effects, device allocation, memory
  registration, file I/O, waits/callbacks beyond bounded dispatchers, broad
  allocation/lifetime, and kernel object lifecycle remain C-owned.

Previous post-+50 host args/envs stack-prep strict-core movement:

- Post-+50 strict-core movement was about +387 points.
- The host/IKC/mcctrl/IHK kernel paths strict row moved 96% -> 98% because
  Rust now owns ten connected args/envs stack-prep slices: arg/env page-count
  and range placement, argenv page allocation and physical publication,
  argenv memory-range creation and error/free shaping, remote args
  map/copy/unmap/TLB sequencing, remote envs map/copy/unmap/TLB sequencing,
  local args/envs length publication, saved_cmdline free/alloc/copy
  publication, argv/env pointer fixup into process VA, vDSO map/clear and
  rprocess/rpgtable publication, and init_process_stack callback dispatch/error
  shaping.
- Broad dashboard movement is 0 points because host/IKC/mcctrl/IHK is already
  capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  primitive map/unmap/copy/user-page allocation and init_process_stack internals,
  mapping/thread/range primitive callbacks, raw IKC send side effects, device allocation, memory
  registration, file I/O, waits/callbacks beyond bounded dispatchers, broad
  allocation/lifetime, and kernel object lifecycle remain C-owned.

Previous post-+50 host prepare-ranges section strict-core movement:

- The host/IKC/mcctrl/IHK kernel paths strict row moved 94% -> 96% because
  Rust now owns ten connected prepare-ranges slices: ELF section loop
  sequencing, interpreter base detection/alignment, relocatable aout VMA
  adjustment, section page-span/range-page calculation, VR/maxprot/demand and
  AP_USER policy, memory-range callback dispatch and error shaping, user-page
  allocation callback dispatch and error shaping, virt-to-phys/page-attribute/
  page-table set-range dispatch plus page-free failure cleanup, remote-PA and
  text/data/map-end publication, and entry/auxv/context plus brk/map
  publication/data-too-large return shaping.

Previous post-+50 host prepare-process strict-core movement:

- Post-+50 strict-core movement is now about +367 points.
- The host/IKC/mcctrl/IHK kernel paths strict row moves 91% -> 94% because
  Rust now owns `process_msg_prepare_process()` body sequencing: monitor
  freeze/frozen gate evaluation, program descriptor map-size/page-span
  calculation, host memory map, virtual map, map-failure cleanup, descriptor
  magic validation, section-count validation, descriptor clone allocation/copy,
  main-thread creation, process PID/PGID/credential/rlimit/profile/VM metadata
  publication, NUMA bind/nodemask policy publication, prepare-ranges callback
  dispatch, success cleanup/unmap/TLB flush, and failure destroy/free/unmap
  return shaping.
- Broad dashboard movement is 0 points because host/IKC/mcctrl/IHK is already
  capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  mapping/thread/range primitive callbacks, raw IKC send side effects, device
  allocation, memory registration, file I/O, waits/callbacks beyond bounded
  dispatchers, broad allocation/lifetime, and kernel object lifecycle remain
  C-owned.

Previous post-+50 Process/VM free-range orchestration strict-core movement:

- Post-+50 strict-core movement is now about +355 points.
- The Process/VM management strict row moves 97% -> 99% because Rust now owns
  `free_process_memory_range()` body sequencing: previous/next range discovery,
  page-table free/clear action planning, page-table lock/unlock callback
  ordering, memobj ref/unref ordering around free-range side effects,
  page-table free callback dispatch, page-table clear callback dispatch,
  TOFU removal callback dispatch, final range detach/straight cleanup callback
  dispatch, error log selection, and final return shaping.
- Broad dashboard movement is 0 points because Process/VM management is
  already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  VM range allocation primitive callbacks, split/XPMEM primitive callbacks, memobj
  refcount primitives, TOFU hooks, actual page-table
  lookup/allocation/mapping/free/clear side effects, page-fault
  range-resolution primitive behavior, broad process lifetime, rusage traversal,
  signal-delivery primitives, remaining non-process atomic refcount primitive behavior, lock/free
  primitive bodies, and wait primitive behavior remain C-owned.

Previous post-+50 Process/VM remove-range orchestration strict-core movement:

- Post-+50 strict-core movement was about +345 points.
- The Process/VM management strict row moved 95% -> 97% because Rust now owns
  remove-process-memory-range body sequencing: straight-map conversion and
  missing-straight handling, range lookup/iteration, split-start and split-end
  callback sequencing, read-only freed publication, XPMEM removal callback
  dispatch, free callback dispatch, error log selection, and final return
  shaping.
- Broad dashboard movement is 0 points because Process/VM management is
  already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  VM range allocation/free callbacks, split/free/XPMEM primitive callbacks,
  memobj refcount primitives, TOFU hooks, actual page-table
  lookup/allocation/mapping/free/clear side effects, page-fault
  range-resolution primitive behavior, broad process lifetime, rusage traversal,
  signal-delivery primitives, remaining non-process atomic refcount primitive behavior, lock/free
  primitive bodies, and wait primitive behavior remain C-owned.

Previous post-+50 Process/VM page-fault/populate strict-core movement:

- Post-+50 strict-core movement was about +335 points.
- The Process/VM management strict row moved 93% -> 95% because Rust now owns
  do-page-fault VM outer-body sequencing: stack-grow range selection,
  write/read memory-range lock decisions, exiting and out-of-range checks,
  permission checks, zero-object `PF_POPULATE` promotion, normal-vs-XPMEM
  range-fault dispatch, and unlock/return shaping. Rust also owns
  `page_fault_process_vm()` `-ERESTART` retry sequencing around preempt
  enable/disable and page-I/O callback dispatch/clear, plus
  `populate_process_memory()` preempt, per-page fault, first-error warning,
  and return sequencing.
- Broad dashboard movement is 0 points because Process/VM management is
  already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  VM range allocation/free callbacks, memobj refcount primitives, TOFU hooks,
  actual page-table lookup/allocation/mapping/free/clear side effects,
  page-fault range-resolution primitive behavior, broad process lifetime, rusage traversal,
  signal-delivery primitives, remaining non-process atomic refcount primitive behavior, lock/free
  primitive bodies, and wait primitive behavior remain C-owned.

Previous post-+50 Process/VM range lookup/access/protection strict-core movement:

- Post-+50 strict-core movement was about +325 points.
- The Process/VM management strict row moved 91% -> 93% because Rust now owns
  VM range lookup body traversal, range-cache hit/store sequencing, first
  overlap selection, next/previous range iteration, extend-up
  validation/commit, change-protection new-flag calculation, attr-delta
  shaping, private-file writable suppression, page-table lock/change-attr
  callback sequencing, `-ENOENT` acceptance, final flag publication, and
  `access_ok()` initial-range, adjacency, permission, and multi-range loop
  validation.
- Broad dashboard movement was 0 points because Process/VM management is
  already capped at 100%.
- Standalone Rust readiness remained about 40% because C fallback scaffolding,
  VM range allocation/free callbacks, memobj refcount primitives, TOFU hooks,
  actual page-table lookup/allocation/mapping/free/clear side effects,
  page-fault range-resolution primitive behavior, broad process lifetime,
  rusage traversal, signal-delivery primitives, atomic refcount primitive
  behavior, lock/free primitive bodies, and wait primitive behavior remain
  C-owned.

Previous post-+50 XPMEM clear-PTE/remove-range strict-core movement:

- Post-+50 strict-core movement was about +315 points.
- The procfs/sysfs/xpmem/file objects strict row moves 99% -> 100% for the
  scoped sequencing row because Rust now owns XPMEM clear-PTE wrapper,
  segment/AP/attachment traversal, AP ref/unlock/clear/relock/deref
  sequencing, attachment read-lock/write-lock range calculation, unpin callback
  dispatch, range lookup, munmap callback dispatch, VALIDPTE clearing, and
  out-of-range/missing-range preservation paths. Rust also owns
  `xpmem_remove_process_memory_range()` null-private-data, already-destroying,
  full-detach, front/tail trim, middle split, private-data publication, and
  invalid-split result sequencing.
- Broad dashboard movement is 0 points because the procfs/sysfs/xpmem/file
  objects row is already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  primitive allocation/free, primitive locks and atomics, raw refcount/list
  primitive behavior, remove-range/free-range/page-table primitive behavior,
  raw IKC/procfs/sysfs exchange, page I/O, user-copy, and broader high-risk
  file/object bodies remain C-owned.

Previous post-+50 XPMEM release-management wrapper/drain strict-core movement:

- Post-+50 strict-core movement was about +295 points.
- The procfs/sysfs/xpmem/file objects strict row moved 97% -> 98% because
  Rust now owns XPMEM release-management wrapper/drain sequencing:
  AP hash-bucket iteration, per-bucket writer-lock/unlock sequencing,
  empty-bucket handling, first-AP selection from hash-list heads, AP
  ref/unlock/release/deref/relock ordering, release/remove syscall wrapper
  positive-ID, owner, object-lookup, action, and deref sequencing, and
  thread-group destroyable/deref dispatch.
- Broad dashboard movement was 0 points because the procfs/sysfs/xpmem/file
  objects row was already capped at 100%.
- Standalone Rust readiness remained about 40% because C fallback scaffolding,
  primitive allocation/free, primitive locks and atomics, raw refcount/list
  primitive behavior, raw attachment detach internals, raw IKC/procfs/sysfs
  exchange, page I/O, user-copy, PTE-clear primitive behavior, and broader
  high-risk file/object bodies remained C-owned.

Previous post-+50 XPMEM access-permit release strict-core movement:

- Post-+50 strict-core movement is now about +286 points.
- The procfs/sysfs/xpmem/file objects strict row moves 96% -> 97% because
  Rust now owns XPMEM access-permit release lifecycle sequencing:
  destroy-start gating, attachment-list drain, attachment ref/detach/deref
  callback ordering, final destroyed-state publication, AP hash-list unlink,
  segment AP-list unlink, segment and segment-thread-group deref callback
  ordering, AP destroyable dispatch, debug-log event selection, and
  already-destroying early return behavior.
- Broad dashboard movement is 0 points because the procfs/sysfs/xpmem/file
  objects row is already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  primitive allocation/free, primitive locks and atomics, raw refcount/list
  primitive behavior, raw attachment detach internals, raw IKC/procfs/sysfs
  exchange, page I/O, user-copy, PTE-clear primitive behavior, and broader
  high-risk file/object bodies remain C-owned.

Previous post-+50 XPMEM segment-list drain strict-core movement:

- Post-+50 strict-core movement was about +278 points.
- The procfs/sysfs/xpmem/file objects strict row moved 95% -> 96% because
  Rust now owns XPMEM thread-group segment-list drain sequencing:
  writer-lock acquisition, empty-list detection, first-segment selection from
  the list head, segment refcount callback ordering, unlock-before-removal,
  per-segment removal callback dispatch, deref callback ordering, relock loop
  sequencing, final unlock, debug-log event selection, and empty-list return
  behavior.
- Broad dashboard movement is 0 points because the procfs/sysfs/xpmem/file
  objects row is already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  primitive allocation/free, primitive locks and atomics, raw refcount/list
  primitive behavior, raw IKC/procfs/sysfs exchange, page I/O, user-copy,
  PTE-clear primitive behavior, and broader high-risk file/object bodies remain
  C-owned.

Previous post-+50 XPMEM segment-removal lifecycle strict-core movement:

- Post-+50 strict-core movement was about +273 points.
- The procfs/sysfs/xpmem/file objects strict row moved 94% -> 95% because
  Rust now owns XPMEM segment-removal lifecycle sequencing: destroy-start
  gating, segment destroy-flag publication, PTE-clear callback ordering, final
  destroyed-state publication, segment-list lock/unlock ordering, list unlink,
  destroyable/refdrop callback dispatch, debug-log event selection, and
  already-destroying early return behavior.
- Broad dashboard movement is 0 points because the procfs/sysfs/xpmem/file
  objects row is already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  primitive allocation/free, primitive locks and atomics, raw refcount/list
  primitive behavior, raw IKC/procfs/sysfs exchange, page I/O, user-copy,
  PTE-clear primitive behavior, and broader high-risk file/object bodies remain
  C-owned.

Previous post-+50 XPMEM close/dup/flush lifecycle strict-core movement:

- Post-+50 strict-core movement was about +265 points.
- The procfs/sysfs/xpmem/file objects strict row moved 93% -> 94% because
  Rust now owns XPMEM duplicate data clearing/open-count increment, close
  open-count decrement/logging, flush/partition-exit decision sequencing,
  flush target-group lookup under the partition hash-list lock, target-group
  list unlink, destroy-flag publication, access-permit and segment-release
  callback ordering, final destroyed-state publication, and destroy callback
  dispatch.
- Broad dashboard movement is 0 points because the procfs/sysfs/xpmem/file
  objects row is already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  primitive allocation/free, primitive locks and atomics, raw refcount/list
  primitive behavior, raw IKC/procfs/sysfs exchange, page I/O, user-copy, and
  broader high-risk file/object bodies remain C-owned.

Previous post-+50 XPMEM open-body strict-core movement:

- Post-+50 strict-core movement is now about +257 points.
- The procfs/sysfs/xpmem/file objects strict row moves 92% -> 93% because
  Rust now owns the XPMEM `open()`/`openat()` body sequencing below the
  syscall pathname wrapper: partition-init gate, Linux fd-forwarding result
  handling, `__xpmem_open()` callback ordering, mckfd allocation/null
  handling, mckfd zeroing and fd/signal/data/callback publication, locked
  list-head insertion, unlock ordering, open-count increment, and debug-log
  event selection.
- Broad dashboard movement is 0 points because the procfs/sysfs/xpmem/file
  objects row is already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  primitive allocation/free, primitive locks and atomics, Linux forwarding,
  pathname copy/classification in the syscall wrapper, raw IKC/procfs/sysfs
  exchange, user-copy, and broader high-risk file/object bodies remain
  C-owned.

Previous post-+50 mckfd syscall dispatch strict-core movement:

- Post-+50 strict-core movement is now about +249 points.
- The Syscall core strict row moves 96% -> 97% and the
  procfs/sysfs/xpmem/file objects strict row moves 91% -> 92% because Rust now
  owns the live `read()`, `ioctl()`, `fcntl()`, and `close()` mckfd syscall
  body sequencing: locked mckfd lookup traversal, callback-vs-forward
  selection, optional TOFU ioctl/close cleanup gates, close unlink mutation,
  close-callback dispatch, mckfd free-callback dispatch, and Linux-forward
  fallback sequencing.
- Broad dashboard movement is 0 points because the Syscall core and
  procfs/sysfs/xpmem/file objects rows are already capped at 100%.
- Standalone Rust readiness remains about 40% because C fallback scaffolding,
  primitive locks, Linux forwarding, XPMEM open/openat creation, raw
  allocation/free primitives, user-copy, syscall dispatch, and broader
  high-risk file/object/syscall bodies remain C-owned.

Previous post-+50 ten-slice syscall wrapper strict-core movement:

- Post-+50 strict-core movement is now about +239 points.
- The Syscall core strict row moves 95% -> 96% because Rust now owns
  wrapper-body sequencing for `times()`, `setpgid()`, `setrlimit()`,
  `getrlimit()`, `prlimit64()`, `sysinfo()`, `get_cpu_id()`, `mlockall()`,
  `munlockall()`, and `getcpu()`: CPU-time accounting/copyout/current-time
  return shaping, process-group normalization, target find/execed validation,
  Linux-forward callback dispatch, pgid mutation, prlimit argument routing,
  sysinfo result fill/copyout, CPU/NUMA id copyout, mlockall policy/logging,
  and missing-callback error shaping.
- Broad dashboard movement is 0 points because the Syscall core row is already
  capped at 100%.
- Standalone Rust readiness remains about 40% because primitive user-copy,
  Linux forwarding, process lookup/locking, time/CPU primitive callbacks,
  syscall dispatch, default-path C fallback scaffolding, and broader high-risk
  syscall bodies remain C-owned.

Previous post-+50 signal/credential syscall wrapper strict-core movement:

- Post-+50 strict-core movement is now about +229 points.
- The Syscall core strict row moves 94% -> 95% because Rust now owns the
  wrapper-body sequencing for `kill()`, `tgkill()`, `tkill()`,
  `setresuid()`, `setreuid()`, `setuid()`, `setfsuid()`, `setresgid()`,
  `setregid()`, `setgid()`, and `setfsgid()`: signal-target validation,
  siginfo construction, do-kill callback dispatch, Linux-forward callback
  dispatch, credential-refresh callback dispatch, setfsid syscall callback
  dispatch, return shaping, and missing-callback error shaping.
- Broad dashboard movement is 0 points because the Syscall core row is already
  capped at 100%.
- Standalone Rust readiness remains about 40% because the default path still
  requires C primitive bodies for Linux forwarding, credential refresh source
  data, actual signal delivery, user-copy, locking, allocation/free, syscall
  dispatch, and C fallback scaffolding.

Previous post-+50 ptrace syscall request-dispatch strict-core movement:

- Post-+50 strict-core movement is now about +219 points.
- The Syscall core strict row moves 93% -> 94% because Rust now owns the
  top-level `ptrace()` request/callback body dispatch: `PTRACE_TRACEME`,
  kill/continue/singlestep/syscall wake requests, register/fpreg/regset get/set
  requests, user/text peek/poke requests including data aliases, set-options,
  attach/detach, siginfo/eventmsg, arch fallback, and unsupported or missing
  callback error shaping.
- Broad dashboard movement is 0 points because the Syscall core row is already
  capped at 100%.
- Standalone Rust readiness remains about 40% because the default path still
  requires C primitive handler bodies for thread lookup/unlock, user-copy,
  register/fpreg/regset access, process-memory read/patch, signal delivery,
  allocation/free, and C fallback scaffolding.

Previous post-+50 ptrace-detach body strict-core movement:

- Post-+50 strict-core movement is now about +209 points.
- The Syscall core strict row moves 92% -> 93% and the Process/VM strict row
  moves 90% -> 91% because Rust now owns the `ptrace_detach_thread()` primary
  body sequencing: main-thread detach gating, zombie finalization selection,
  tracer child-list detach, ptraced reparenting to the original parent,
  report-list detach/reattach, ptrace cleanup/free callback dispatch,
  single-step clear dispatch, exit-signal dispatch, forwarded-signal siginfo
  construction, `do_kill()` callback sequencing, wake/release dispatch, and
  final process finalization dispatch.
- Broad dashboard movement is 0 points because the Syscall core and Process/VM
  rows are already capped at 100%.
- Standalone Rust readiness remains about 40% because C ABI wrappers,
  fallback bodies, primitive lock/list/free/wakeup/signal callbacks, syscall
  dispatch, and other default-path C runtime bodies remain required.

Prior post-+50 syscall wait-scan traversal strict-core movement:

- Post-+50 strict-core movement is now about +207 points.
- The Syscall core strict row moves 90% -> 92% and the Process/VM strict row
  moves 89% -> 90% because Rust now owns the outer process/thread scan bodies
  under `do_wait()`: parent children-lock callback ordering, children-list
  traversal, pid/pgid matching, `empty` publication, process-zombie dispatch,
  process stopped/continued candidate dispatch, ptraced-children empty-scan
  traversal, report-thread list traversal, report-thread candidate dispatch,
  regular thread-list empty-scan traversal, and no-found unlock/return
  sequencing.
- C keeps primitive lock/list/free bodies, waitqueue/schedule/signal primitive
  behavior, host wait primitive behavior, syscall dispatch, user-copy
  primitive bodies, default-path C fallback scaffolding, and broader high-risk
  handler bodies.

Previous post-+50 syscall wait-zombie/reparent strict-core movement:

- Post-+50 strict-core movement was about +204 points.
- The Syscall core strict row moves 89% -> 90% and the Process/VM strict row
  moves 88% -> 89% because Rust now owns the `wait_proc()` process-zombie
  branch under `do_wait()`: zombie status publication, host-wait
  skip/request/log selection, parent rusage aggregation, rusage result fill,
  parent child-list detach, PID-1 reparent/list attach, child main-thread
  ptrace-detach gate, parent/child/PID-1/thread lock/unlock callback ordering,
  no-reparent unlock/detach ordering, process release callback dispatch, and
  final found/result shaping.
- C keeps the outer child/thread traversal, primitive lock/list/free bodies,
  host wait primitive behavior, syscall dispatch, user-copy primitives, and
  fallback scaffolding.

Previous post-+50 syscall wait-candidate strict-core movement:

- Post-+50 strict-core movement was about +202 points.
- The Syscall core strict row moves 88% -> 89% because Rust now owns
  `wait_proc()`/`wait_thread()` candidate result sequencing under `do_wait()`:
  process and report-thread stopped, ptraced-stopped, continued, and
  report-thread exited candidates; ptraced-stop miss return preservation;
  process TID rewrite for requested thread waits; report-list detach,
  ptrace-detach, release, reap, unlock, and found-publication callback
  ordering.
- C keeps child/thread list traversal, zombie scan/reparent behavior, primitive
  lock bodies, waitqueue/schedule/signal primitive behavior, rusage
  aggregation, release/detach primitive bodies, syscall dispatch, user-copy
  primitives, and fallback scaffolding.

Previous post-+50 syscall do_wait strict-core movement:

- Post-+50 strict-core movement was about +201 points.
- The Syscall core strict row moves 87% -> 88% because Rust now owns
  top-level `do_wait()` wait-loop sequencing: waitqueue init/prepare/finish
  callback ordering, process/thread scan dispatch gates, no-child and
  `WNOHANG` result shaping, pending-signal interrupt return, schedule callback
  handoff, wake/rescan behavior, and wait log callback ordering.
- C keeps `wait_proc()`/`wait_thread()` child/thread list traversal and
  mutation, waitqueue/schedule/signal primitive behavior, rusage aggregation,
  release/detach side effects, syscall dispatch, user-copy primitives, and
  fallback scaffolding.

Previous post-+50 scheduler migration-completion strict-core movement:

- Post-+50 strict-core movement was about +200 points.
- The Scheduler/timers/wait/futex strict core row remains capped at 100%, but
  the post-cap strict evidence advances because Rust now owns
  `do_migrate()` body sequencing: migration queue traversal, request detach
  and ack gates, affinity target selection, runqueue detach/add with length
  mutation, thread CPU update, same-VM sibling scan, address-space CPU-set
  lock/update, target reschedule flag publication, interrupt/vector callback
  dispatch, waitqueue wake callback dispatch, lock ordering, and
  validation/error shaping.
- C keeps spinlock/waitqueue/interrupt/vector/log primitive bodies, CPU-local
  lookup callbacks, context switching, `schedule()` primitive behavior, and
  fallback scaffolding.

Previous post-+50 scheduler migration-request strict-core movement:

- Post-+50 strict-core movement was about +198 points.
- The Scheduler/timers/wait/futex strict core row remained capped at 100%, but
  the post-cap strict evidence advanced because Rust moved
  `sched_request_migrate()` body sequencing: request thread publication,
  target migration-queue lock callback ordering, waitqueue init/prepare/finish
  callback sequencing, migration request list insertion, target runqueue
  noirq lock/unlock callback ordering, `CPU_FLAG_NEED_RESCHED` and
  `CPU_FLAG_NEED_MIGRATE` publication, `CPU_STATUS_RUNNING` publication,
  remote interrupt vector/callback selection, migration log callback dispatch,
  scheduler callback handoff, and validation/error shaping.
- C kept target CPU-local lookup, current-thread wait-entry construction,
  spinlock/waitqueue/interrupt/vector/schedule/log primitive bodies, actual
  migration completion in `do_migrate()`, context switching, and fallback
  scaffolding.

Previous post-+50 dump user-page traversal/dispatch strict-core movement:

- Post-+50 strict-core movement was about +196 points.
- The x86_64 memory-management strict core row remains capped at 100%, but the
  post-cap strict evidence advances because Rust now owns dump user-page
  process-hash bucket iteration, list cursor traversal, process recovery from
  `hash_list`, VM/address-space/page-table pointer loading, null skip gates,
  visitor argument shaping for `[0, USER_END)`, `visit_pte_range_safe()`
  callback dispatch, and dispatch count/error shaping.
- C keeps current resource-set lookup, the page-table visitor primitive and PTE
  visitor body, low-level page/address data sources, process lifetime/list
  mutation, logging primitive behavior, and fallback scaffolding.

Previous post-+50 dump bitmap/completion strict-core movement:

- Post-+50 strict-core movement is now about +195 points.
- The x86_64 memory-management strict core row remains capped at 100%, but the
  post-cap strict evidence advances because Rust now owns dump page-set
  completion publication, physical-address membership checks, variable-length
  dump bitmap range clearing, user-PTE dump bitmap marking, free-chunk dump
  bitmap marking, and top-level last-CPU dump query sequencing.
- C keeps process traversal, rb-tree primitive traversal callbacks,
  page-table visit callback invocation, low-level page/address data sources,
  logging primitive behavior, and fallback scaffolding.

Previous post-+50 allocator NUMA topology/allocation strict-core movement:

- Post-+50 strict-core movement was about +193 points.
- The Page allocator strict core row moves 94% -> 95%, because Rust now owns
  NUMA allocation-attempt sequencing for OOM flag initialization, NUMA-id
  bounds validation, rusage OOM callback dispatch, and NUMA allocation callback
  dispatch, plus distance-vector bounds/null checks, distance-id lookup,
  CPU-local readiness checks, current-NUMA callback dispatch, and
  distance-ordered node pointer selection for
  `ihk_mc_get_numa_node_by_distance()`.
- C keeps pa-ops implementation primitive behavior, low-level page
  allocation/free primitive bodies, MCS/interrupt primitive bodies,
  current-node primitive lookup, logging/panic primitive behavior,
  deferred-zero worker timing, IKC send side effects, FUGAKU debug preempt
  behavior, broader allocator lifetime, and fallback scaffolding.

Previous post-+50 allocator NUMA-distance strict-core movement:

- Post-+50 strict-core movement was about +192 points.
- The Page allocator strict core row moves 93% -> 94%, because Rust now owns
  `numa_distances_init()` sequencing: per-node distance-array allocation,
  allocation-failure logging, distance matrix fill, distance/id sorting, node
  publication, and ordered log callback dispatch.
- C keeps pa-ops implementation primitive behavior, low-level page
  allocation/free primitive bodies, MCS/interrupt primitive bodies,
  logging/panic primitive behavior, deferred-zero worker timing, IKC send side
  effects, FUGAKU debug preempt behavior, broader allocator lifetime, and
  fallback scaffolding.

Previous post-+50 allocator lifecycle/mem_init strict-core movement:

- Post-+50 strict-core movement was about +191 points.
- The Page allocator strict core row moves 92% -> 93%, because Rust now owns
  `ihk_mc_set_page_allocator()` tracking initialization, early-allocator
  invalidation, and `pa_ops` publication, plus top-level `mem_init()`
  sequencing for monitor/rusage/NUMA init, allocator registration,
  page-fault handler publication, query-free interrupt registration,
  page-hash init, virtual-map init, demand-paging flag/log publication, and
  NUMA-distance init ordering.
- C keeps pa-ops implementation primitive behavior, low-level page
  allocation/free primitive bodies, MCS/interrupt primitive bodies,
  logging/panic primitive behavior, deferred-zero worker timing, IKC send side
  effects, FUGAKU debug preempt behavior, broad allocator lifetime, and
  fallback scaffolding.

Current post-+50 allocator tracking/lifecycle strict-core movement:

- Post-+50 strict-core movement is now about +190 points.
- The Page allocator strict core row moves 90% -> 92%, because Rust now owns
  shared pagealloc/kmalloc tracking hash and lock initialization,
  pagealloc/kmalloc memcheck leak-scan traversal with per-entry address-list
  locking, current-runcount filtering, leak-count accumulation, runcount
  mutation, detail/summary log callback sequencing, and kmalloc remote
  free-list move/consolidation under the remote-list lock.
- C keeps pa-ops implementation primitive behavior, low-level page
  allocation/free primitive bodies, MCS/interrupt primitive bodies,
  logging/panic primitive behavior, deferred-zero worker timing, IKC send side
  effects, FUGAKU debug preempt behavior, broad allocator lifetime, and
  fallback scaffolding.

Current post-+50 pagealloc tracking wrapper strict-core movement:

- Post-+50 strict-core movement is now about +188 points.
- The Page allocator strict core row moves 88% -> 90%, because Rust now owns
  public `_ihk_mc_alloc_aligned_pages_node()`/`_ihk_mc_free_pages()`
  memdebug tracking wrapper sequencing: no-debug/uninitialized fallback,
  allocation-site hash selection, tracking-entry lookup and creation,
  file-string allocation and publication, alloc-count mutation,
  page-address entry allocation and publication on both per-entry and
  address-hash lists, exact-start partial-free tracking, covering-range
  middle/suffix split tracking, address-entry rehashing, full-free list
  detach, last-entry cleanup and metadata frees, invalid-size/invalid-free
  callback routing, final page-free callback sequencing, and log callback
  selection.
- C keeps pa-ops implementation primitive behavior, low-level page
  allocation/free primitive bodies, MCS/interrupt primitive bodies,
  logging/panic primitive behavior, deferred-zero worker timing, IKC send side
  effects, FUGAKU debug preempt behavior, broad allocator lifetime, and
  fallback scaffolding.

Previous post-+50 kmalloc tracking wrapper strict-core movement:

- Post-+50 strict-core movement was about +186 points at that checkpoint.
- The Page allocator strict core row moved 86% -> 88%, because Rust owned
  public `_kmalloc()`/`_kfree()` memdebug tracking wrapper sequencing:
  no-debug/null fallback, allocation hash selection, tracking-entry lookup,
  new tracking-entry allocation and initialization, file-string allocation and
  publication, alloc-count increment/decrement, address-entry allocation and
  publication on both per-entry and address-hash lists, free-side address
  lookup and detach, last-entry cleanup and metadata frees, invalid-free
  callback routing, and log callback selection.
- C keeps low-level page allocation/free primitive bodies, interrupt and
  spinlock primitive bodies, logging/panic primitive behavior, broad allocator
  lifetime, and fallback scaffolding.

Previous post-+50 kmalloc body strict-core movement:

- Post-+50 strict-core movement was about +184 points at that checkpoint.
- The Page allocator strict core row moved 84% -> 86%, because Rust owned
  low-level `___kmalloc()` size alignment, free-list fit search, chunk split
  and refill sequencing, low-level page-allocation callback ordering,
  interrupt save/restore callback ordering, and return shaping, plus low-level
  `___kfree()` null handling, corruption callback routing, current-CPU
  free-list insertion/consolidation, remote-CPU free-list publication under
  lock callbacks, and normal/error return shaping.
- C kept low-level page allocation/free primitive bodies, interrupt and
  spinlock primitive bodies, tracking wrappers, logging/panic primitive
  behavior, deferred-zero worker timing, IKC send side effects, FUGAKU debug
  preempt behavior, broad allocator lifetime, and fallback scaffolding.

Previous post-+50 host IKC init strict-core movement:

- Post-+50 strict-core movement was about +182 points at that checkpoint.
- The host/IKC/mcctrl/IHK kernel paths strict core row moved 89% -> 91%,
  because Rust owned host IKC2Linux channel-table allocation/zeroing/
  publication, connect-parameter shaping, retry/delay/log sequencing,
  channel-slot publication, current-channel callback sequencing, and host
  IKC2McKernel parameter shaping, retry/delay/log sequencing, and
  regular-channel callback sequencing.
- C kept actual allocation/connect/delay/log/panic/current-channel/
  regular-channel primitive bodies, raw IKC exchange side effects, broad
  allocation/lifetime, kernel object lifecycle, and fallback scaffolding.

Previous post-+50 procfs maps/status/stat data-production strict-core movement:

- Post-+50 strict-core movement was about +180 points at that checkpoint.
- The procfs/sysfs/xpmem/file objects strict core row moved 88% -> 91%,
  because Rust owned `/proc/PID/maps` range traversal, path/default
  selection, permission character selection, and line output,
  `/proc/PID/status` locked-size traversal plus state/ID/VmLck/thread/mask
  output, and per-PID `stat` state/identity/comm/constant-field output.
- C kept actual buffer allocation/free primitives, request map/unmap,
  process/thread lookup, memory-range locking, raw IKC answer send, and
  fallback scaffolding.

Previous post-+50 procfs non-mem data-production strict-core movement:

- Post-+50 strict-core movement was about +177 points at that checkpoint.
- The procfs/sysfs/xpmem/file objects strict core row moved 86% -> 88%,
  because Rust owns root `mckernel` version/buildid data production, root
  `stat` CPU-line generation, per-process `auxv` byte output, `cmdline`
  present/missing output, `comm` basename/fallback/newline output, and
  `/proc/PID/pagemap` page-table-to-pagemap value loop/result sequencing.
- C kept actual buffer allocation/free primitives, request map/unmap,
  process/thread lookup, memory-range locking, page-table primitive behavior,
  complex procfs status/stat/maps-style data production, raw IKC answer send,
  and fallback scaffolding.

Previous post-+50 procfs mem/buffer strict-core movement:

- Post-+50 strict-core movement was about +175 points at that checkpoint.
- The procfs/sysfs/xpmem/file objects strict core row moves 84% -> 86%,
  because Rust now owns `buf_alloc()` allocation-callback/null/init/optional
  physical-publication sequencing and the `/proc/PID/mem` body loop:
  zero-length handling, page-fault callback ordering, virtual-to-physical
  translation callback ordering, McKernel-memory gate, physical-to-virtual
  mapping callback, read-versus-write copy direction, offset/remaining/result
  advancement, and partial-copy/error shaping.
- C keeps actual page allocation/free primitives, page-fault primitive behavior,
  page-table translation primitive behavior, `is_mckernel_memory()`,
  `phys_to_virt()`, `memcpy()`, request map/unmap, process/thread lookup,
  non-mem procfs data production, raw IKC answer send, and fallback scaffolding.

Previous post-+50 scheduler/timer strict-core movement:

- Post-+50 strict-core movement was about +173 points at that checkpoint.
- The Scheduler/timers/wait/futex strict core row moved 97% -> 100%, because
  Rust now owns `init_timers()` list/lock initialization sequencing,
  `schedule_timeout()` spin-wake, runqueue scheduling, schedule callback
  handoff, spin-loop, timeout-expiry, and spin-sleep clear sequencing,
  `wake_timers_loop()` timer tick/decrement/detach/log/wakeup sequencing, and
  `set_timer()` runqueue scan plus LAPIC enable/disable decision sequencing.
- C keeps actual spinlock, rdtsc/pause, LAPIC timer, waitqueue wake,
  `schedule()`/context-switch, current-thread lookup, address-translation,
  page-fault, futex primitive, IKC send, scheduler wake, and fallback
  scaffolding behavior.

Previous post-+50 x86 user-copy/process-VM strict-core movement:

- Post-+50 strict-core movement was about +170 points at that checkpoint.
- The x86_64 memory management strict core row moves 99% -> 100%, because
  Rust now owns `verify_process_vm()`, `read_process_vm()`,
  `write_process_vm()`, `patch_process_vm()`, `copy_from_user()`,
  `copy_to_user()`, `strlen_user()`, `strcpy_from_user()`,
  `getlong_user()`, `getint_user()`, `setlong_user()`, and
  `setint_user()` sequencing behind the existing C ABI.
- C keeps current-thread lookup, page-fault primitive behavior,
  virtual-to-physical primitive behavior, map/unmap and phys-to-virt primitive
  bodies, logging callbacks, bridge glue, and fallback scaffolding.

Previous post-+50 host SCD dispatcher strict-core movement:

- Post-+50 strict-core movement was about +169 points at that checkpoint.
- The host/IKC/mcctrl/IHK kernel paths strict core row moved 86% -> 89%,
  because Rust owns the top-level SCD dispatcher behind
  `syscall_packet_handler()`: request message classification, callback
  dispatch, legacy return-value shaping, sysfs show/store/release argument
  extraction, unknown-message logging dispatch, and final packet release
  sequencing for the handled SCD request families.
- C keeps the ABI wrapper, compact bridge callbacks, raw IKC send primitive
  behavior, primitive lookup/wakeup/register/debug behaviors, allocation/copy/
  map primitive bodies, broad IKC exchange mutation, kernel object lifecycle,
  and fallback scaffolding.

Previous post-+50 itimer strict-core movement:

- Post-+50 strict-core movement was about +166 points at that checkpoint.
- Syscall core strict core: 85% -> 87%, because Rust now owns local
  `setitimer()` and `getitimer()` body sequencing: `which` validation,
  ITIMER_REAL Linux-forward callback dispatch, virtual/prof timer slot
  selection, old-value current snapshot and copyout callback sequencing,
  new-value copyin callback sequencing, elapsed-time reset, enabled-state
  publication, `set_timer(0)` callback dispatch, getitimer copyout, null-old
  no-op handling, and return/error shaping. C keeps syscall ABI wrappers, raw
  argument extraction, actual `copy_from_user()`/`copy_to_user()` primitive
  bodies, `do_syscall()` forwarding primitive behavior, actual timer behavior,
  and fallback scaffolding.
- Scheduler/timers/wait/futex strict core: 95% -> 97%, because Rust now owns
  local interval-timer runtime state sequencing for virtual/prof timers:
  active-current-value subtraction against elapsed time, scalar elapsed
  reset, timer-start flag publication, and `set_timer(0)` callback dispatch.
  C still owns actual timer queues, `set_timer()` primitive behavior,
  scheduler wake/context-switch primitives, spinlocks, and broader futex
  wait/requeue side effects.

Previous scheduler affinity strict-core movement:

- Post-+50 strict-core movement was about +162 points at that checkpoint.
- Syscall core strict core: 84% -> 85%, because Rust now owns the
  `sched_setaffinity()` and `sched_getaffinity()` syscall body sequencing:
  argument validation, target selection, remote lookup/unlock ordering,
  permission gates, user cpuset copyin/copyout callback sequencing, result
  shaping, and error shaping. C keeps syscall ABI wrappers, raw argument
  extraction, actual user-copy primitive bodies, thread lookup/hold/release
  primitive behavior, scheduler migration primitive behavior, and fallback
  scaffolding.
- Scheduler/timers/wait/futex strict core: 92% -> 95%, because Rust now owns
  scheduler affinity orchestration: local-versus-remote target selection,
  credential checks, target hold/release ordering, cpuset zeroing and
  intersection with the target process mask, thread affinity mask
  publication, current-CPU membership testing, migration-request callback
  dispatch, getaffinity copyout, and return/error shaping. C still owns the
  actual user-copy primitive, thread lookup/hold/release primitive bodies,
  migration request side effects, wake/context-switch primitives, timer
  queues, spinlocks, and broader futex wait/requeue side effects.

Previous scheduler syscall strict-core movement:

- Post-+50 strict-core movement was about +158 points at that checkpoint.
- Syscall core strict core: 82% -> 84%, because Rust now owns scheduler
  syscall body sequencing for `sched_setparam()`, `sched_getparam()`,
  `sched_setscheduler()`, `sched_getscheduler()`, and
  `sched_rr_get_interval()`: PID validation/normalization, target lookup and
  unlock callback ordering, Linux ownership/root check request shaping, user
  parameter copyin/copyout callback sequencing, RR interval timespec shaping,
  and return/error shaping. C keeps syscall ABI wrappers, raw argument
  extraction, actual `copy_from_user()`/`copy_to_user()` primitive bodies,
  `do_syscall()` forwarding primitive behavior, thread lookup/unlock primitive
  bodies, and C fallback scaffolding.
- Scheduler/timers/wait/futex strict core: 89% -> 92%, because the same Rust
  scheduler syscall bodies now own the scheduler-facing orchestration around
  policy validation, scheduler-parameter publication callback dispatch, remote
  target selection, root/owner permission gates, and RR interval result
  production. C still owns scheduler affinity mask mutation, migration request
  side effects, wake/context-switch primitives, timer queues, spinlocks, and
  broader futex wait/requeue side effects.

The revised +35 aggregate continuation has 35 verified `overview.txt` points
and is complete. In this stricter tracker, it is about +33 strict-core points
because
the new Rust bodies move real core execution rather than only helper islands:

- x86_64 memory strict core: 50% -> 53%, because Rust now owns page
  refcount/hash lifecycle bodies for `page_map()`, locked `page_unmap()`,
  phys-to-page hash traversal, hash insertion initialization, and bucket
  init/count.
- x86_64 memory strict core: 53% -> 56%, because Rust now owns page-hash
  lock/unlock callback orchestration, locked count-all traversal, locked
  `phys_to_page()` lookup, locked `phys_to_page_insert_hash()`
  lookup/allocation-callback/insert, and locked `page_unmap()` orchestration.
- Page allocator strict core: 45% -> 48%, because Rust now owns
  `__ihk_pagealloc_init()` layout, allocation-callback, descriptor
  initialization, lock-callback, and tail-reservation orchestration.
- Page allocator strict core: 48% -> 51%, because Rust now owns
  `ihk_numa_alloc_pages()` cache-first allocation attempts, cache-hit source
  classification, fallback to locked NUMA allocation, and missing-callback
  failure behavior.
- Page allocator strict core: 51% -> 54%, because Rust now owns
  `ihk_numa_free_pages()` direct-versus-deferred free orchestration,
  direct-free callback result publication, deferred-free zero-request action
  publication, and ignored-free classification.
- Scheduler/timers/wait/futex strict core: 52% -> 54%, because Rust now owns
  futex hash-table allocation-callback, global pointer publication, and bucket
  table initialization orchestration.
- Scheduler/timers/wait/futex strict core: 54% -> 56%, because Rust now owns
  futex hash-bucket selection with a C hash callback.
- Scheduler/timers/wait/futex strict core: 56% -> 59%, because Rust now owns
  top-level `futex()` command dispatch, private/realtime flag handling,
  clock-realtime rejection, wait/wake/requeue/wake-op callback selection, and
  invalid-command callback routing.
- Process/VM management strict core: 55% -> 58%, because Rust now owns
  `add_process_memory_range()` VM-range object initialization: rb-node clear
  state, start/end/flag/memobj/objoff/pgshift/private-data publication,
  straight-start reset, and optional Tofu list initialization.
- Process/VM management strict core: 58% -> 61%, because Rust now owns
  `add_process_memory_range()` map/no-map action selection: NOPHYS skip,
  remote/uncached/normal page-table update attr selection, XPMEM mark action,
  demand-paging logging action, PROT_NONE skip, and memclear gating.
- Process/VM management strict core: 61% -> 66%, because Rust now owns the
  post-bounds `add_process_memory_range()` orchestration body: allocation
  callback handling, VM range initialization, insertion/update callback
  sequencing, insert/update failure cleanup, XPMEM and demand-paging actions,
  memclear callback gating, and returned-range publication.

New +50 continuation strict-core movement:

- Process/VM management strict core: 66% -> 70%, because Rust now owns
  `vm_range_insert()` traversal and mutation: range-tree descent, overlap
  rejection, success/overlap callback sequencing, `rb_link_node()`
  publication, and `rb_insert_color()` balancing. The C side keeps only the
  ABI wrapper, logging/dump callbacks, and fallback implementation.
- Syscall core strict core: 58% -> 60%, because Rust now owns the direct
  process/thread ID leaf bodies for `getpid`, `getppid`, `gettid`,
  `set_tid_address`, `getuid`, `geteuid`, `getgid`, and `getegid`, including
  process/thread field reads and `clear_child_tid` publication. C keeps the
  syscall ABI wrappers, current-thread lookup, argument extraction, and
  fallback implementation.
- Scheduler/timers/wait/futex strict core: 59% -> 63%, because Rust now owns
  futex wake target orchestration: mark-woken sequencing, Linux-vs-McKernel
  target selection, Linux response-channel fallback selection, IKC wake packet
  fill, IKC send callback sequencing, scheduler wake callback sequencing, and
  wake logging. C keeps the actual IKC send primitive, scheduler wake primitive,
  channel lookup callback, and fallback implementation.
- Syscall core strict core: 60% -> 63%, because Rust now owns `getresuid()`
  and `getresgid()` body sequencing: process credential field reads, ordered
  `ruid/euid/suid` and `rgid/egid/sgid` copy sequencing, and fault
  short-circuit behavior through a C `copy_to_user` callback. C keeps syscall
  ABI wrappers, current-thread lookup, argument extraction, the actual
  user-copy primitive, and fallback implementation.
- Page allocator strict core: 54% -> 57%, because Rust now owns
  `ihk_numa_free_pages()` post-action completion sequencing: direct-free
  success/error logging selection, deferred-free error/skip/send action
  handling, Linux zero-request send callback sequencing, and send
  success/failure logging. C keeps CPU-local channel lookup, the actual
  `ihk_ikc_send()` primitive, logging primitive bodies, and fallback
  implementation.
- Page allocator strict core: 57% -> 61%, because Rust now owns exported
  `ihk_numa_free_pages()` top-level body sequencing: CPU-cache free attempt
  classification, cache success/error log selection, direct-versus-deferred
  dispatch, Linux zero-request send callback sequencing, final log selection,
  and return shaping. C keeps CPU-local variable selection, actual
  lock/interrupt primitives, IKC channel lookup and send side effects, logging
  primitive bodies, deferred-zero worker timing, zeroed-list publication,
  allocator lifetime, and fallback implementation.
- Page allocator strict core: 61% -> 64%, because Rust now owns exported
  `ihk_numa_alloc_pages()` top-level allocation body sequencing: cache-hit
  logging selection, direct-allocation logging selection, source-aware return
  shaping, and the exported allocation handoff around cache-first/source
  selection/fallback-to-NUMA orchestration. C keeps CPU-local variable
  selection, actual cache/tree lock primitives, `ihk_mc_alloc_pages()`,
  MCS/interrupt primitives, logging primitive bodies, allocator lifetime,
  zeroed-list publication timing, deferred-zero worker timing, and fallback
  implementation.
- Page allocator strict core: 64% -> 67%, because Rust now owns exported
  `ihk_numa_add_free_pages()` and public `ihk_numa_zero_free_pages()`
  top-level sequencing: add-free success/error log selection, return shaping
  around the NUMA add-free mutation body, and public zero-free dispatch into
  the Rust-owned zero-list publication path. C keeps logging primitive bodies,
  actual allocation/free and lock/interrupt primitives, allocator lifetime,
  IKC send side effects, deferred-zero worker timing, broader allocator
  mutation, and fallback implementation.
- x86_64 memory strict core: 56% -> 59%, because Rust now owns
  `ihk_mc_pt_create()` and `ihk_mc_pt_destroy()` root lifecycle sequencing:
  allocation-callback orchestration, new root zeroing, kernel-half entry copy
  from `init_pt`, destroy-root kernel-half clearing, and destroy-recursion
  callback dispatch. C keeps the actual allocator/free primitives, recursive
  child-table destruction below the root, `phys_to_virt()`, and fallback
  implementation.
- x86_64 memory strict core: 59% -> 62%, because Rust now owns
  `ihk_mc_pt_prepare_map()` orchestration: initial page-table selection,
  first-level L4 range traversal, present-entry short-circuiting, allocation
  callback sequencing, L4 entry publication, last-level set-page callback loop
  sequencing, and first error return propagation. C keeps the actual
  allocation primitive, `virt_to_phys()`, `__set_pt_page()`, and fallback
  implementation.
- x86_64 memory strict core: 62% -> 64%, because Rust now owns
  `ihk_mc_pt_set_pte()` body sequencing: page-size/value selection callback
  orchestration, alignment error classification, log-callback selection,
  panic-callback dispatch for invalid page sizes, PTE store invocation, and
  final return shaping. C keeps debug log wrappers, the actual log/panic
  primitives, broader traversal, allocation/free primitives, and fallback
  implementation.
- ABI/layout foundation strict core: 54% -> 58%, because Rust and C now
  assert pager create/map result layouts plus McKernel memory area/node,
  page-allocator ops, aligned TLB-flush entry, and page-cache header layouts.
  This is not execution ownership by itself, but it removes another ABI/layout
  blind spot before the memory and pager bodies can move safely.
- Shared primitives strict core: 78% -> 81%, because Rust now owns
  `put_dec_trunc`, `put_dec_full`, and `put_dec` base-10 digit emission used
  by the `vsnprintf()` numeric path. C keeps variadic format parsing,
  width/sign/precision buffer policy, and output orchestration.
- ABI/layout foundation strict core: 58% -> 62%, because Rust and C now
  assert `struct rusage_percpu`, kmalloc cache/header metadata, SMP function
  call packets, backlog entries, and the full x86_64 `struct cpu_local_var`
  layout used by the Rocky build. This removes a CPU-local ABI blind spot
  before allocator and scheduler bodies take more ownership.
- Page allocator strict core: 67% -> 70%, because Rust now owns CPU-local
  storage initialization sequencing, `get_cpu_local_var()` pointer selection,
  and normal preempt counter inc/dec. C keeps the actual allocation/free
  primitives, MCS/interrupt primitives, logging callbacks, IKC send side
  effects, deferred-zero worker timing, FUGAKU debug preempt behavior, and
  broader allocator lifetime/mutation.
- Shared primitives strict core: 81% -> 83%, because Rust now owns
  `skip_atoi()` format width/precision pointer advancement for the
  `vsnprintf()` numeric path. C keeps the surrounding variadic parser,
  qualifier parsing, buffer formatting policy, and output orchestration.
- Page allocator strict core: 70% -> 72%, because Rust now owns kmalloc chunk
  header initialization, sorted free-list insertion, and adjacent free-chunk
  consolidation. C keeps the public `kmalloc()`/`kfree()` entry sequencing,
  heap metadata lifetime, primitive allocation/free behavior, locking context,
  and fallback implementation.

Current requested +50 continuation strict-core movement:

The live continuation target is +50 aggregate broad dashboard points, not +32.
Current movement is +50/50 broad points and about +173 strict-core points, so
this continuation target is complete. Any older +32 references are historical
32/50 checkpoints, not the active target.

- Shared primitives strict core: 83% -> 87%, because Rust now owns the central
  `number()` formatter body for `vsnprintf()` numeric and pointer paths:
  sign handling, prefix selection, width and precision adjustment, zero/space
  padding, octal/hex/decimal digit staging, bounded output writes, and returned
  pointer advancement. C keeps format-token decoding, string and pointer
  extension dispatch, va_arg extraction, `%n` writes, final NUL termination,
  and the surrounding `vsnprintf()` loop.
- Shared primitives strict core: 87% -> 91%, because Rust now owns
  `format_decode()` format-token parsing for `vsnprintf()`: literal span
  detection, flags, field width, precision, qualifier handling, conversion
  type, base, signedness, and width/precision continuation state. C keeps the
  surrounding `vsnprintf()` loop, va_arg extraction, string and pointer
  extension dispatch, `%n` writes, final NUL termination, and output buffer
  lifetime.
- Shared primitives strict core: 91% -> 94%, because Rust now owns `string()`
  `%s` output formatting: NULL-string substitution, precision-bounded length
  selection, left/right padding, bounded output writes, and returned pointer
  advancement. C keeps the surrounding `vsnprintf()` loop, va_arg extraction,
  pointer extension dispatch, `%n` writes, final NUL termination, and output
  buffer lifetime.
- ABI/layout foundation strict core: 62% -> 66%, because Rust and C now
  assert x86 signal action, altstack, `siginfo_t`, `signalfd_siginfo`, ptrace
  `user_regs_struct`, `user_fpregs_struct`, `struct user`, futex
  hash-bucket/key/queue layouts, and the key offsets embedded in process/thread
  state. This is not execution ownership by itself, but it removes another
  blocker before signal, ptrace, and futex bodies can move safely.
- Syscall core strict core: 63% -> 66%, because Rust now owns `sigaltstack()`
  body sequencing: optional old-stack copyout, optional new-stack copyin,
  validation, disabled-stack normalization, and thread `sigstack` publication.
  C keeps the syscall ABI wrapper, current-thread lookup, actual user-copy
  primitive callbacks, and fallback implementation. The broad `overview.txt`
  Syscall Core row was already capped at 100%, so this is strict-core movement
  rather than additional broad dashboard percentage.
- ABI/layout foundation strict core: 66% -> 70%, because Rust and C now
  assert syscall/control-plane structures used by syscall setup, procfs,
  coredump, sysinfo/time publication, CPU mapping, perf control, UTI context,
  and move-pages SMP requests. This is not execution ownership by itself, but
  it removes another layout blind spot before those core bodies can move
  without relying on guessed C layouts.
- ABI/layout foundation strict core: 70% -> 74%, because Rust and C now
  assert x86 descriptor, TSS, fxsave/xsave, YMM, LWP, and bound-register
  save-state structures used by ptrace, signal-frame FPU save/restore,
  register access, and context paths. This is still prerequisite ownership,
  not execution ownership of XSAVE/XRSTOR or trap/context-switch behavior.
- ABI/layout foundation strict core: 74% -> 78%, because Rust and C now
  assert sysfs create, mkdir, symlink, lookup, unlink, and setup request
  packet layouts used by the Rocky control-plane exchange. This is still
  prerequisite ownership, not execution ownership of allocation, IKC sends,
  host object creation, show/store callbacks, or release handling.
- ABI/layout foundation strict core: 78% -> 82%, because Rust and C now
  assert ELF core headers, program headers, note headers, `elf_siginfo`,
  `prstatus64_timeval`, `elf_prstatus64`, `elf_prpsinfo64`, and `iovec`
  layouts used by coredump and user-vector paths. This is prerequisite
  ownership, not execution ownership of coredump note generation, architecture
  register capture, process-memory dumping, user-vector walking, or copy paths.
- ABI/layout foundation strict core: 82% -> 87%, because Rust and C now
  assert the x86 CPU-local page, x86 kernel context, TLS `user_desc`, sysfs
  operation callbacks/handle/bitmap parameters, `itimerval`, and
  `profile_event` layouts. This is prerequisite ownership, not execution
  ownership of CPU-local stack switching, GDT/TSS mutation, TLS setup, sysfs
  callback execution, profile accumulation, timer delivery, or copy paths.
- ABI/layout foundation strict core: 87% -> 100%, because Rust and C now
  assert the remaining currently scoped x86_64/Rocky ABI layouts for
  low-level `ihk_rwlock`, `kref`, rbtree augmentation callbacks, ftrace
  branch-data records, `memobj_ops`, `memobj`, SysV shm limit/info and
  lock-user state, and the XPMEM ID/hash/thread-group/segment/access-permit/
  partition/permission/attachment object graph. This closes the foundation row
  for known layout prerequisites, but it is not execution ownership of XPMEM
  attach/detach, SysV shm lifetime, memobj refcounting, page I/O, ftrace
  accounting, rbtree mutation, locking primitives, allocation/free, user-copy,
  or object lifecycle side effects.
- procfs/sysfs/xpmem/file objects strict core: 60% -> 64%, because Rust now
  owns sysfs show/store/release response-body sequencing: default ssize/error
  shaping, optional callback dispatch, response packet msg/err/arg
  publication, send callback dispatch, and output publication. C keeps callback
  bodies, the actual `ihk_ikc_send()` primitive, logging, global buffer
  lifetime, packet-handler dispatch, allocation/free, sysfs path formatting,
  busy waits, host-side object creation, user-copy, and fallback
  implementation.
- procfs/sysfs/xpmem/file objects strict core: 64% -> 67%, because Rust now
  owns sysfs packet dispatch for show/store/release requests: request-kind
  classification, typed callback selection, show/store/release callback
  dispatch, store-size publication from the incoming packet error field, and
  unknown-message classification. C keeps the public IKC handler wrapper,
  unknown-message logging, callback bodies, the actual `ihk_ikc_send()`
  primitive, global buffer lifetime, allocation/free, sysfs path formatting,
  busy waits, host-side object creation, user-copy, and fallback
  implementation.
- host/IKC/mcctrl/IHK kernel paths strict core: 62% -> 63%, because Rust now
  owns `ihk_dma_request()` callback dispatch for the IHK host core DMA request
  path: channel/ops validation, request callback presence gate, callback
  invocation, and `-EINVAL` shaping for the no-callback path. C keeps the
  exported ABI wrapper, C fallback, and the DMA provider callback body itself.

Post-+50 strict-core movement:

- Process/VM management strict core: 70% -> 76%, because Rust now owns a
  five-slice Process/VM range-management batch: split-range publication
  sequencing after allocation/TOFU preparation, join-range memobj-unref/
  rb-erase/range-cache/free sequencing, free-range page-table action/span
  planning, free-range final rb-erase/cache replacement plus straight-range
  physical-free and main-straight cleanup sequencing, and straight-range
  removal conversion gate/math. C keeps the actual memobj refcount primitive
  bodies, allocation/free primitive bodies, page-table free/clear primitive
  side effects, TOFU primitive behavior, page-fault behavior, broader
  process lifetime mutation, and fallback implementation.
- Process/VM management strict core: 76% -> 82%, because Rust now owns a
  five-slice release/cleanup batch: process memory-range drain traversal with
  free-callback and error-log sequencing, mckfd close iteration and drain/free
  list mutation for release and fork-failure paths, VM NUMA-policy rb-tree
  drain/erase/free sequencing, release-time address-space detach/proc->vm
  clearing/process release callback sequencing, and destroy-thread optional
  ptrace/fp/coredump cleanup callback sequencing. C keeps the actual locks,
  allocator/free primitive bodies, close/free/release callback bodies,
  page-table side effects, page-fault behavior, broad process lifetime
  mutation, signal forwarding, and fallback implementation.
- Process/VM management strict core: 82% -> 88%, because Rust now owns a
  lifecycle release batch: sigcommon release-body sequencing, TID
  release/replace body sequencing, release_thread ref/profile/procfs/destroy/
  VM-release sequencing, and release_process_vm ref/mckfd/free-callback/TLB/
  free-ranges/detach/policy-drain/final-free sequencing. C keeps atomic
  refcount primitives, spinlock primitives, actual allocator/free callbacks,
  destroy_thread and release_process bodies, profile/procfs/TLB/range-free
  primitive bodies, page-fault behavior, signal forwarding, rusage, broad
  process lifetime, and fallback scaffolding.
- Shared primitives strict core: 94% -> 96%, because Rust now owns bounded
  `vsnprintf()` loop actions that were still C-owned after the earlier numeric
  and string formatter bodies: literal-span copying, `%c` padding/output,
  `%%` and invalid-conversion percent emission, `%n` count stores for int,
  long, and size_t qualifiers, and final NUL termination. C keeps the main
  variadic loop, `va_arg` extraction, pointer extension dispatch, fallback
  implementation, and remaining call-site ownership.
- Shared primitives strict core: 96% -> 99%, because Rust now owns the main
  `vsnprintf()` loop orchestration: format-token loop state, literal copying,
  width/precision continuation, `%c`/`%s`/numeric dispatch, `%%` and
  invalid-conversion percent emission, `%n` count stores, pointer-extension
  callback handoff, buffer termination, and return shaping. C keeps the public
  ABI wrapper, `va_arg` extraction callbacks, the `%p` extension formatter
  callback body, fallback implementation, and remaining direct C call sites.
- Shared primitives strict core: 99% -> 100%, because Rust now owns the scoped
  `%p` pointer-format body for the current non-`CONFIG_KALLSYMS` kernel path:
  null-pointer string selection, default pointer width/zero-padding/base
  selection, `%pS` fallback formatting through the Rust numeric formatter, and
  extension-consumption behavior. C keeps the public ABI wrapper, `va_arg`
  extraction glue, and fallback implementation; those remain standalone Rust
  blockers but are acceptable strict-core ABI/fallback scaffolding for shared
  primitives.
- Syscall core strict core: 66% -> 67%, because Rust now owns `waitid()`
  SIGCHLD siginfo copyout-body sequencing: waitid siginfo eligibility,
  zeroed `siginfo_t` preparation, `si_signo`/`si_code` and child PID/status
  population, timeval-to-jiffy conversion for child utime/stime, and copyout
  callback dispatch. C keeps the syscall ABI wrapper, `do_wait()` scanning and
  sleep behavior, wait locks, status/rusage production, the actual
  `copy_to_user()` primitive, scheduler behavior, and fallback implementation.
- Syscall core strict core: 67% -> 69%, because Rust now owns `wait4()`
  wrapper sequencing: option validation, zeroed `struct rusage` preparation,
  `do_wait()` callback invocation with `WEXITED`, status copyout gating,
  rusage copyout gating, copyout callback dispatch, and final return shaping.
  C keeps the syscall ABI wrapper, raw argument extraction, `do_wait()`
  scanning and sleep behavior, wait locks, the actual `copy_to_user()`
  primitive, scheduler behavior, and fallback implementation.
- Syscall core strict core: 69% -> 71%, because Rust now owns `waitid()`
  wrapper sequencing: idtype-to-wait-pid validation, option validation,
  zeroed `struct rusage` preparation, `do_wait()` callback invocation,
  negative wait-result propagation, siginfo copyout dispatch, and final
  waitid return shaping. C keeps the syscall ABI wrapper, raw argument
  extraction, `do_wait()` scanning and sleep behavior, wait locks, the actual
  `copy_to_user()` primitive, scheduler behavior, and fallback implementation.
- Syscall core strict core: 71% -> 72%, because Rust now owns
  `wait_continued()` body sequencing: continued-status publication,
  main-thread versus report-thread signal-flag reap target selection,
  continued-signal reap callback dispatch, WNOWAIT preservation through the
  existing reap helper, and process-pid versus thread-tid return selection.
  C keeps the wait list scans, wait locks, caller-side duplicate reap behavior,
  scheduler behavior, and fallback implementation.
- Syscall core strict core: 72% -> 78%, because Rust now owns five syscall
  wrapper bodies at once: `rt_sigprocmask()` validation, old-mask copyout,
  new-mask copyin, sigmask mutation, and Linux-forward callback sequencing;
  `rt_sigqueueinfo()` validation, siginfo copyin, and do-kill callback
  sequencing; `rt_sigaction()` validation, action copyin, do-sigaction
  callback sequencing, and old-action copyout; `gettimeofday()` local-time
  timeval shaping/copyout and offload selection; and `nanosleep()` monitor
  status publication, local spin wait, pending-signal break, remaining-time
  copyout, and offload selection. C still owns the actual user-copy primitive,
  Linux forwarding callback body, signal delivery, scheduler/time primitive
  bodies, and fallback scaffolding.
- Syscall core strict core: 78% -> 82%, because Rust now owns the main
  `getrusage()` and `clock_gettime()` CPU-time body sequencing: rusage
  `who` validation and dispatch, process-thread reader lock callback
  sequencing, remote thread times-update request/interrupt callback
  sequencing, wait-for-update pause callback sequencing, SELF/CHILDREN/THREAD
  rusage aggregation/fill/copyout sequencing, `clock_gettime()` local realtime
  copyout, Linux-forward dispatch, thread CPU-time aggregation, and process
  CPU-time thread traversal/update/wait/aggregation/copyout sequencing. C
  keeps syscall ABI wrappers, raw argument extraction, the actual
  `copy_to_user()` primitive, lock primitive bodies, CPU interrupt primitive
  behavior, TOD/gettime and Linux-forward callback bodies, scheduler/time
  primitive bodies, and fallback scaffolding.
- Page allocator strict core: 72% -> 78%, because Rust now owns five allocator
  runtime slices: pa-ops versus early-allocation dispatch in
  `___ihk_mc_alloc_aligned_pages_node()`, default p2align/node/virtual-address
  shaping in `___ihk_mc_alloc_pages()`, pa-ops free dispatch in
  `___ihk_mc_free_pages()`, pending-free begin/enqueue/finish list mutation
  and callback sequencing, and rbtree free-in-allocator memory-chunk
  containment/free/rusage-sub sequencing. C still owns the main
  `mckernel_allocate_aligned_pages_node()` explicit-node, memory-policy,
  distance-order, and fallback-order allocation loops, pa-ops implementation
  primitive behavior, logging primitive bodies, and fallback scaffolding.
- Page allocator strict core: 78% -> 84%, because Rust now owns five allocator
  policy/allocation-loop slices in `mckernel_allocate_aligned_pages_node()`:
  explicit-node allocation attempts and miss logging, MPOL_BIND/MPOL_PREFERRED
  distance-plus-mask traversal, MPOL_INTERLEAVE retry and `il_prev` update
  sequencing, distance-ordered fallback attempts and first-miss logging, and
  order-based fallback attempts plus rusage-add/phys-to-virt return shaping.
  C keeps the public wrapper/context lookup, the actual allocation/free
  primitive bodies, logging primitive bodies, MCS/interrupt primitives,
  pa-ops implementation behavior, IKC side effects, and fallback
  implementation.
- Scheduler/timers/wait/futex strict core: 63% -> 66%, because Rust now owns
  futex `queue_me()` orchestration: waiter plist insertion, task and
  physical-address metadata publication for thread/process/runqueue/CPU-local
  fields through callbacks, interrupt/vector metadata setup, and hash-bucket
  unlock callback sequencing. C keeps current-thread and CPU-local address
  selection, `virt_to_phys`, interrupt/vector primitives, the actual spinlock
  primitive, futex key lifetime, user-value loads, IKC send and scheduler wake
  primitives, timer queues, context switching, schedule behavior, race retry,
  and broader futex wait/requeue side effects.
- Scheduler/timers/wait/futex strict core: 66% -> 69%, because Rust now owns
  futex `unqueue_me()` orchestration: lock-pointer read and compiler barrier,
  wrong-lock race detection and retry, detach dispatch under the confirmed
  lock, unlock sequencing, already-woken no-lock return shaping, and
  key-reference drop callback sequencing. C keeps the actual spinlock
  primitives, futex key-reference primitive body and lifetime internals,
  scheduler behavior, context switching, timer queues, user-value loads and
  comparisons, IKC send and wake primitives, and broader wait/requeue side
  effects.
- Scheduler/timers/wait/futex strict core: 69% -> 72%, because Rust now owns
  `futex_wait_queue_me()` orchestration: interruptible-state publication,
  idle-halt/timeout spin-sleep gating, spin-sleep lock callback ordering,
  enqueue callback dispatch, queued-state classification, timeout versus direct
  scheduling callback selection, wake logging callback sequencing, final
  running-state restoration, and returned remaining-time shaping. C keeps
  current-thread lookup, actual spinlock primitives, `queue_me()` callback
  internals, `schedule_timeout()` and direct scheduler primitive behavior,
  timer queues, context switching, futex key lifetime internals, user-value
  loads/comparisons, IKC send and wake primitives, and broader wait/requeue
  side effects.
- Scheduler/timers/wait/futex strict core: 72% -> 75%, because Rust now owns
  `futex_wait()` body orchestration: wait-queue preparation, setup callback
  dispatch, wait callback dispatch, unqueue dispatch, pending-signal callback
  gating, post-action classification, success/timeout/interrupt/retry return
  shaping, key-release callback sequencing, and retry looping for the no-signal
  case. C keeps profile timestamp bookkeeping around the body, current-thread
  and CPU-local response lookup, futex setup primitive internals, queue/wait
  callback internals, unqueue callback internals, actual signal-list lookup,
  actual key-reference release primitive, scheduler behavior, context
  switching, timer queues, user-value loads/comparisons, IKC send and wake
  primitives, and broader wait/requeue side effects.
- Scheduler/timers/wait/futex strict core: 75% -> 78%, because Rust now owns
  `get_futex_key()` body orchestration: natural-alignment validation dispatch,
  base/offset publication, private key address/mm construction, private
  key-reference callback dispatch, shared-key offset tagging, virtual-to-
  physical callback retry, page-fault callback dispatch, failed-translation
  logging callback sequencing, final shared physical key publication, and
  return shaping. C keeps current-thread/mm selection, actual page-table
  translation, actual page-fault/populate behavior, the no-op key-reference
  primitive body and lifetime semantics, user-value loads/comparisons, locks,
  scheduler behavior, context switching, timer queues, IKC send/wake
  primitives, and broader wait/requeue side effects.
- Scheduler/timers/wait/futex strict core: 78% -> 81%, because Rust now owns
  `futex_wake()` body orchestration: bitset validation, get-key callback
  dispatch, hash-bucket callback dispatch, lock callback ordering,
  wake-scan invocation, unlock callback ordering, put-key callback dispatch,
  and final return shaping. C keeps the get-key bridge call site,
  hash-futex bridge/global queue selection, actual spinlock primitives,
  actual wake side effects, key-reference lifetime semantics, IKC send/wake
  side effects, scheduler behavior, user-value operations, and broader
  wake/requeue side effects.
- Scheduler/timers/wait/futex strict core: 81% -> 84%, because Rust now owns
  `futex_requeue()` body orchestration: source and target get-key callback
  dispatch, hash-bucket callback dispatch, double-lock callback ordering,
  optional compare-value load callback sequencing and mismatch return shaping,
  requeue-scan invocation, unlock callback ordering, moved-waiter key-reference
  drop sequencing, put-key callback dispatch, and final ret-or-task-count
  return shaping. C keeps the get-key/hash/get-value bridges, actual spinlock
  primitives, wake/requeue callback side effects, key-reference primitive
  bodies and lifetime semantics, user-value load primitive body, IKC send/wake
  side effects, scheduler behavior, and broader wake/requeue side effects.
- Scheduler/timers/wait/futex strict core: 84% -> 87%, because Rust now owns
  `futex_wake_op()` body orchestration: source and target get-key callback
  dispatch, hash-bucket callback dispatch, double-lock callback ordering,
  in-user atomic-op callback dispatch, non-EFAULT error return shaping,
  private `-EFAULT` retry looping, shared `-EFAULT` key-release/retry
  sequencing, source and conditional target wake-scan invocation, unlock
  callback ordering, put-key callback dispatch, and final wake-count return
  shaping. C keeps the get-key/hash/atomic-op bridges, actual spinlock
  primitives, actual in-user atomic primitive body, wake callback side effects,
  key-reference primitive bodies and lifetime semantics, IKC send/wake side
  effects, scheduler behavior, and broader wake/requeue side effects.
- Scheduler/timers/wait/futex strict core: 87% -> 89%, because Rust now owns
  the `futex_wait()` entry wrapper sequencing: bitset validation before
  profile mutation, optional profile pause accounting before the wait body,
  wait-body callback dispatch, optional profile resume timestamp publication,
  and final return propagation. C keeps current-thread and CPU-local response
  selection, the timestamp primitive, scheduler wait behavior, actual futex
  queue/wake side effects, and fallback scaffolding.
- procfs/sysfs/xpmem/file objects strict core: 67% -> 70%, because Rust now
  owns the public sysfs request-body sequencing for create, mkdir, symlink,
  lookup, unlink, and setup requests: IKC request send callback dispatch,
  remote busy-wait polling with pause callbacks, response read barrier
  callback sequencing, response error classification, and mkdir/lookup handle
  publication. C keeps allocation/free, varargs path formatting, special
  sysfs instance physical-address setup, the actual `ihk_ikc_send()` primitive,
  logging, and fallback scaffolding.
- procfs/sysfs/xpmem/file objects strict core: 70% -> 72%, because Rust now
  owns `sysfs_init()` setup orchestration: sysfs request layout-size
  validation, data-buffer size and pointer publication, data-buffer allocation
  callback dispatch, setup-param allocation callback dispatch, setup packet
  `buf_rpa`/`bufsize`/`busy` publication, setup request-body dispatch,
  setup-param free callback sequencing, and stage/phase error classification.
  C keeps actual allocator/free primitives, `virt_to_phys()`,
  `ihk_ikc_send()`, `cpu_pause()`/read-barrier primitives, panic/logging, and
  fallback scaffolding.
- procfs/sysfs/xpmem/file objects strict core: 72% -> 73%, because Rust now
  owns the public sysfs packet-handler wrapper decision: request-kind
  dispatch, show/store/release callback routing, unknown-message
  log-callback selection, kind publication, and return shaping. C keeps the
  exported IKC handler ABI, the actual `kprintf()` primitive, callback bodies,
  and fallback scaffolding.
- procfs/sysfs/xpmem/file objects strict core: 73% -> 75%, because Rust now
  owns sysfs server show/store/release wrapper sequencing: show/store/release
  callback selection from callback-pointer presence, response body dispatch,
  callback-error/send-error/packet-error/debug log-event selection, response
  ssize/error publication, and release callback routing. C keeps the static
  wrapper entry/debug line, actual sysfs ops callbacks, the actual
  `ihk_ikc_send()` primitive, printk/dkprintf/ekprintf primitives, sysfs
  data-buffer lifetime, and fallback scaffolding.
- procfs/sysfs/xpmem/file objects strict core: 75% -> 76%, because Rust now
  owns special sysfs create conversion for direct, string, and bitmap
  snooping operations: special-kind dispatch, string bit-count calculation,
  bitmap parameter copy, physical-address callback sequencing, and
  `client_instance` publication. C keeps the static wrapper, unknown-op
  logging, actual `virt_to_phys()` primitive, create-request allocation,
  varargs path formatting, IKC send/wait behavior, and fallback scaffolding.
- procfs/sysfs/xpmem/file objects strict core: 76% -> 78%, because Rust now
  owns public sysfs request-wrapper post-format sequencing for create, mkdir,
  symlink, lookup, and unlink: request-body callback dispatch, send-error vs
  response-error log-event selection, phase publication, and mkdir/lookup
  handle destination publication. C keeps allocation/free, varargs path
  formatting, special-create wrapper logging, raw `ihk_ikc_send()`,
  `cpu_pause()`/read-barrier primitives, final wrapper entry/exit logging, and
  fallback scaffolding.
- procfs/sysfs/xpmem/file objects strict core: 78% -> 79%, because Rust now
  owns `procfs_thread_ctl()` packet/control sequencing: packet zeroing, TID
  create/delete packet field publication, response physical-address callback
  sequencing, IKC send callback dispatch, create-only wait-loop selection, and
  pause-callback polling. C keeps thread/process field extraction,
  `cpu_local_var()`, `ihk_mc_get_osnum()`, actual `virt_to_phys()`, actual
  `ihk_ikc_send()`, actual `cpu_pause()`, and fallback scaffolding.
- procfs/sysfs/xpmem/file objects strict core: 79% -> 81%, because Rust now
  owns procfs buffer-chain append and release sequencing: first-buffer
  allocation callback dispatch, cursor rollover at page capacity, next-buffer
  physical-link publication, bounded copy callback sequencing, per-buffer size
  mutation, allocation-failure cleanup, and release traversal/free callback
  sequencing. C keeps actual page allocation/free, physical-address
  translation, the byte-copy primitive, request mapping/unmapping, `r->pbuf`
  publication, and fallback scaffolding.
- procfs/sysfs/xpmem/file objects strict core: 81% -> 82%, because Rust now
  owns procfs request release/finalization mutation: release-buffer free
  traversal dispatch and `ret` zeroing, completion `ret`/`eof` publication,
  pbuf attach decision, physical-address callback dispatch, and `pbuf`
  publication. C keeps request mapping/unmapping, data production, actual page
  free and physical-address primitives, answer sending, and fallback
  scaffolding.
- procfs/sysfs/xpmem/file objects strict core: 82% -> 83%, because Rust now
  owns procfs answer packet/send-callback sequencing: answer-packet zeroing,
  `SCD_MSG_PROCFS_ANSWER` publication, request `ref`/`arg`/`reply`/`pid`
  transfer, error publication, and send callback dispatch. C keeps
  `cpu_local_var()` channel selection, the actual IKC send primitive, and
  fallback scaffolding.
- procfs/sysfs/xpmem/file objects strict core: 83% -> 84%, because Rust now
  owns procfs backlog allocation/copy/add/free-on-error sequencing: packet-copy
  allocation callback dispatch, allocation-failure return shaping, request
  copy callback dispatch, backlog add callback dispatch, add-failure free
  callback dispatch, and null/missing-callback validation. C keeps the actual
  allocator/free primitives, byte-copy primitive, backlog queue mutation, and
  fallback scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 63% -> 64%, because Rust now
  owns host remote page-fault answer packet/send-callback sequencing:
  answer-packet zeroing, `SCD_MSG_REMOTE_PAGE_FAULT_ANSWER` publication,
  request `ref`/`arg`/`reply`/`pid` transfer, error publication, and send
  callback dispatch. C keeps `page_fault_process_vm()`,
  profiling/preempt handling, response-channel lookup through
  `cpu_local_var()`, the actual IKC send primitive, and fallback scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 64% -> 66%, because Rust now
  owns traditional host SCD ACK packet/send-callback sequencing for
  prepare-process, send-signal, cleanup-process, and cleanup-fd responses:
  response-packet zeroing, ACK message publication, request `ref`/`arg` and
  `reply` transfer, error publication, and send callback dispatch. C keeps the
  process preparation, cleanup, signal-delivery, logging, terminate-host, and
  raw IKC send primitive bodies plus fallback scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 66% -> 67%, because Rust now
  owns host perf ACK and CPU register response packet/send-callback
  sequencing: response-packet zeroing, response message publication, request
  `arg`/`reply` transfer where applicable, error publication, and send
  callback dispatch. C keeps perf counter operations, CPU register
  read/write, host memory map/unmap, logging, raw IKC send side effects, and
  fallback scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 67% -> 69%, because Rust now
  owns host perf-control dispatch sequencing: SET/GET/ENABLE/DISABLE
  selection, SET mode construction from exclude flags, init/stop/reset
  callback ordering and early-error return shaping, start/stop callback
  dispatch for enable/disable, GET read-value publication, unexpected-type
  logging callback dispatch, and missing-callback/null validation. C keeps
  host memory map/unmap, actual perf counter primitive bodies, response
  sending, and fallback scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 69% -> 71%, because Rust now
  owns host perf-control request sequencing: descriptor physical mapping
  callback dispatch, writable/active virtual-map callback dispatch, Rust
  perf-control dispatch invocation, conditional virtual unmap, physical unmap,
  ACK packet/result sequencing, and null/missing-callback validation. C keeps
  actual map/unmap primitive bodies, actual perf counter primitive bodies, raw
  IKC send side effects, and fallback scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 71% -> 73%, because Rust now
  owns CPU register request sequencing: descriptor physical mapping callback
  dispatch, writable/active virtual-map callback dispatch, architecture
  register callback invocation, conditional virtual unmap, physical unmap,
  response packet/result sequencing, and null/missing-callback validation. C
  keeps actual map/unmap primitive bodies, architecture register primitive
  bodies, raw IKC send side effects, and fallback scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 73% -> 75%, because Rust now
  owns cleanup-process cleanup/reply/terminate sequencing and cleanup-fd
  cleanup/log/reply sequencing: cleanup callback dispatch, optional log
  callback dispatch, response packet/result sequencing, terminate callback
  ordering for process cleanup, and null/missing-callback validation. C keeps
  actual process cleanup, FD cleanup, terminate-host, logging, raw IKC send
  side effects, and fallback scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 75% -> 77%, because Rust now
  owns send-signal request sequencing: mcctrl-signal descriptor physical-map
  callback dispatch, writable/active virtual-map callback dispatch, descriptor
  copy into Rust-owned stack storage, virtual and physical unmap sequencing,
  ACK packet/result sequencing before delivery, do-kill callback dispatch,
  optional delivery-log callback dispatch, map-failure ACK/error shaping, and
  null/missing-callback validation. C keeps actual map/unmap primitive bodies,
  signal-delivery primitive behavior in `do_kill()`, logging primitives, raw
  IKC send side effects, and fallback scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 77% -> 79%, because Rust now
  owns wake-syscall-thread and debug-log request sequencing:
  wake-target TID extraction, thread lookup callback dispatch, missing-thread
  error shaping, optional wake logging, waitqueue wake callback dispatch,
  thread unlock callback dispatch, debug-log code extraction, optional
  debug-print callback dispatch, debug-log callback dispatch, and
  null/missing-callback validation. C keeps actual thread lookup, waitqueue
  wake, thread unlock, `debug_log()`, logging primitive bodies, and fallback
  scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 79% -> 81%, because Rust now
  owns remote page-fault body sequencing: error-only answer routing, optional
  profile timestamp gating, populated fault-reason construction, debug-log
  callback dispatch, preempt-disable/page-fault/preempt-enable callback
  ordering, optional profile-event delta publication, answer packet dispatch,
  and null/missing-callback validation. C keeps current-thread/channel
  selection, the actual `page_fault_process_vm()` primitive body, timestamp,
  preempt, profile-event and logging primitive bodies, remote page-fault
  thread lookup/deferred scheduling, raw IKC send side effects, and fallback
  scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 81% -> 84%, because Rust now
  owns remote page-fault request dispatch sequencing: target-thread lookup
  callback dispatch, missing-thread log/body-error routing, current-thread
  direct body dispatch, deferred packet allocation/copy callback sequencing,
  backlog publication callback dispatch, scheduler wake callback dispatch,
  thread unlock callback ordering, allocation-failure unlock/error shaping,
  and null/missing-callback validation. C keeps actual thread lookup,
  allocation, packet copy, thread-field publication, scheduler wake, thread
  unlock, remote page-fault body primitive behavior, raw IKC send side effects,
  and fallback scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 84% -> 86%, because Rust now
  owns `SCD_MSG_SCHEDULE_PROCESS` request sequencing: thread/proc validation,
  current-CPU callback dispatch, received-log callback sequencing, CPU-set
  allowed decision dispatch, fallback clone-CPU callback dispatch, no-CPU
  error/log return shaping, TID publication callback ordering, proc/thread
  running-status publication callback ordering, chain-thread/process callback
  ordering, runqueue-add callback sequencing, queued-log callback dispatch, and
  null/missing-callback validation. C keeps the actual CPU-set test,
  clone-CPU selection primitive, field-write primitives, chain/list mutation
  primitives, runqueue mutation primitive, logging primitive bodies, and
  fallback scaffolding.
- host/IKC/mcctrl/IHK kernel paths strict core: 86% -> 89%, because Rust now
  owns top-level host SCD packet dispatch behind `syscall_packet_handler()`:
  init-channel ACK, prepare-process, schedule-process, wake-syscall-thread,
  remote page-fault, send-signal, procfs request/release, cleanup process/fd,
  debug-log, sysfs show/store/release, perf-control, CPU register, unknown-
  message logging, legacy return shaping, and final packet release
  sequencing. C keeps the ABI wrapper, compact bridge callbacks, raw IKC send
  primitive behavior, lookup/wakeup/register/debug primitive bodies,
  allocation/copy/map primitive behavior, broad IKC exchange mutation, kernel
  object lifecycle, and fallback scaffolding.
- x86_64 memory management strict core: 64% -> 67%, because Rust now owns
  recursive page-table destruction below the root: level/null validation with
  panic callback routing, present/non-large child-table detection, child-table
  physical extraction through the existing entry classifier, physical-to-
  virtual callback dispatch, recursive post-order descent, free-page callback
  sequencing after children, and missing-callback validation. C keeps the
  public ABI wrapper, actual `phys_to_virt()` primitive behavior, actual page
  free primitive behavior, panic primitive bodies, and fallback scaffolding.
- x86_64 memory management strict core: 67% -> 70%, because Rust now owns
  full virtual-to-physical page-table walk orchestration: null-root fallback to
  `init_pt`, page-table index calculation use, L4 miss/walk classification,
  L3 and L2 large-page hit return shaping, L1 hit return shaping, physical and
  page-size publication, physical-to-virtual callback dispatch between levels,
  and missing/null callback error shaping. C keeps the public ABI wrapper,
  actual `phys_to_virt()` primitive behavior, page-table memory lifetime, and
  fallback scaffolding.
- x86_64 memory management strict core: 70% -> 72%, because Rust now owns
  `__clear_pt_page()` body sequencing: null-root fallback to `init_pt`,
  regular versus large-page address alignment, L4/L3/L2 presence validation,
  physical-to-virtual callback dispatch between levels, regular-page L1 PTE
  zeroing, large-page L2 PTE zeroing, missing-level error shaping, init-table
  fallback, and missing/null callback validation. C keeps the public ABI
  wrappers, actual `phys_to_virt()` primitive behavior, page-table memory
  lifetime, TLB-sensitive flushing policy, and fallback scaffolding.
- x86_64 memory management strict core: 72% -> 74%, because Rust now owns
  full `lookup_pte()` body orchestration: null-root fallback to `init_pt`,
  default base/size/alignment publication, level hit/miss shaping for L3/L2
  large pages and L1 leaves, callback dispatch between page-table levels, and
  null/missing-callback handling. C keeps the public ABI wrapper, primitive
  `phys_to_virt()` behavior, page-table memory lifetime, and fallback
  scaffolding.
- x86_64 memory management strict core: 74% -> 77%, because Rust now owns
  `ihk_mc_pt_change_attr_range()` traversal and PTE attribute mutation:
  range-end clamping, L4/L3/L2/L1 descent, large-page full-coverage mutation,
  partial-large split rejection, callback dispatch between levels, leaf PTE
  attribute updates, and first-error return shaping. C keeps the public ABI
  wrapper, primitive `phys_to_virt()` behavior, TLB-sensitive effects,
  allocation/free primitives, broader visitor orchestration, and fallback
  scaffolding.
- x86_64 memory management strict core: 77% -> 78%, because Rust now owns
  `ihk_mc_pt_virt_to_pagemap()` result shaping and
  `arch_vrflag_to_ptattr()` dirty-bit fault shaping: pagemap miss/default
  status publication, present/status/frame publication from a resolved PTE,
  private/fault dirty-bit classification, and callback validation. C keeps the
  public ABI wrappers, primitive page-table lookup side effects, and fallback
  scaffolding.
- x86_64 memory management strict core: 78% -> 84%, because Rust now owns
  the page-table setup batch: zeroed page-table allocation behind
  `__alloc_new_pt()`, get-PTE allocation traversal, `__set_pt_page()` regular
  and large-page mapping body sequencing, and kernel-address lock
  classification. Rust owns AP-flag selection, physical-address masking,
  L4/L3/L2 page-table allocation and publication, regular L1 PTE publication,
  large L2 PTE publication, existing-PTE same-physical success, conflicting
  PTE error shaping, and missing-callback/missing-root handling. C keeps the
  public ABI wrapper, actual spinlock primitive, actual `ihk_mc_alloc_pages()`
  provider, `virt_to_phys()`/`phys_to_virt()` primitives, page-table free
  primitive behavior, broader range mapping/free rollback, RSS updates,
  TLB-sensitive flushing, and fallback scaffolding.
- x86_64 memory management strict core: 84% -> 87%, because Rust now owns the
  clear-range side-effect sequencing batch: TLB flush-address queue mutation,
  old-PTE memobj-flush callback dispatch, anonymous-page free/RSS-sub
  callback ordering, file-backed page-unmap/free/rusage-sub callback ordering,
  XPMEM keep classification/log routing, and full-span L2/L3 child page-table
  PTE-null/free sequencing. C keeps the public ABI wrappers, actual
  `remote_flush_tlb_array_cpumask()`, `phys_to_virt()`,
  `ihk_mc_free_pages_user()`, `page_unmap()`, RSS primitive bodies,
  page-table walking callbacks, allocation/free primitive behavior, and
  fallback scaffolding.
- x86_64 memory management strict core: 87% -> 90%, because Rust now owns the
  set-range side-effect sequencing batch: existing-mapping conflict cleanup,
  page-table allocation-failure cleanup, L1/L2/L3 map/store/RSS-accounting
  callback ordering, large-page success logging, and child-walk failure
  logging. C keeps the public ABI wrappers, actual `clear_range()` primitive
  effects, page-table allocation/publication, `virt_to_phys()`/
  `phys_to_virt()`, child page-table walk callbacks, RSS primitive body,
  leftover page-table free behavior, and fallback scaffolding.
- x86_64 memory management strict core: 90% -> 93%, because Rust now owns the
  `set_range_l1/l2/l3/l4` leaf and level body orchestration: action dispatch,
  zeroed child page-table allocation, atomic page-table publication retry,
  existing child-table translation, child-walk callback dispatch, large-page
  map/store/RSS routing, child-walk failure shaping, and unconsumed child-table
  free sequencing. C keeps the public ABI wrappers, actual allocation/free and
  address-translation primitive bodies, child page-table walk callbacks, RSS
  primitive body, top-level `walk_pte_l4()` dispatch, and fallback scaffolding.
- x86_64 memory management strict core: 93% -> 96%, because Rust now owns the
  clear/set range top-level orchestration batch: clear-range bounds
  validation, TLB invalid-address array allocation/free callback sequencing,
  clear-range argument field publication, clear-range top-level `walk_pte_l4`
  dispatch, final remote TLB flush callback dispatch, set-range argument
  publication, set-range diff/attribute/pgshift/range state publication,
  set-range top-level `walk_pte_l4` dispatch, and set-range top-level
  walk-failure log/return shaping. At that checkpoint, C kept the public ABI
  wrappers, actual allocation/free and TLB flush primitive bodies, page-table
  visitor callbacks, lower-level walk callbacks, address-translation primitive
  bodies, and fallback scaffolding.
- x86_64 memory management strict core: 96% -> 98%, because Rust now owns the
  normal and safe `visit_pte_range()` visitor body batch: leaf skip/direct
  visitor callback dispatch, level direct/E2BIG retry classification,
  split-error log/return shaping, child page-table allocation/publication,
  existing child-table translation, child-walk callback dispatch, root
  allocation/translation dispatch, and top-level normal/safe visit-range walk
  dispatch. C keeps the public ABI wrappers, actual allocation/free and
  address-translation primitive bodies, page-table walk iteration callbacks,
  broader page frees/RSS accounting, TLB primitive behavior, and fallback
  scaffolding.
- x86_64 memory management strict core: 98% -> 99%, because Rust now owns the
  clear-range leaf/level/root body batch: leaf null-skip and PTE clear-exchange
  sequencing, TLB flush-address queue callback sequencing, old-entry action
  callback handoff, old-effect callback orchestration, split-error log/return
  shaping, large-page clear sequencing, child table translation, child-walk
  dispatch and error propagation, `-ENOENT` child-table teardown/free
  sequencing, root skip/translation/dispatch, and level-specific debug logging.
  C keeps the public ABI wrappers, old-entry page lookup/refcount primitive
  behavior, actual allocation/free and address-translation primitive bodies,
  page/RSS primitive bodies, remote TLB shootdown primitive behavior, and
  fallback scaffolding.

Validation for the latest x86 page-table setup batch passed the expanded
equivalence suite with
`x86_memory_helpers ok digest=44390095bf9aa560`,
`object_helpers ok digest=edebd23f366851a4`,
`sched_helpers ok digest=c595ab25a3922ad9`,
`printf_decimal ok digest=b403ba89612baeb3`, the unresolved-symbol/SIMD audit,
Rust-enabled image/module and C-fallback image builds, `git diff --check`,
script syntax checks, the ownership report, and captured build logs
`/tmp/mckernel-clean-build-x86-pt-setup-rust.log`,
`/tmp/mckernel-clean-build-x86-pt-setup-cfallback.log`, and
`/tmp/mckernel-clean-build-x86-pt-setup-wrapper.log` with no
`warning`/`warn:` matches. An initial equivalence run failed because a new test
expected missing allocation callback rejection on an already mapped large-page
path; the failure was recorded in `kernel.log`, the helper/fallback behavior
and test were aligned so allocation is required only when a new page table is
needed, and the rerun passed. No QEMU boot or reboot-capable validation was run
for this focused batch.

Validation for the latest x86 clear-range side-effect batch passed
`rustfmt kernel/rust/x86_memory_helpers.rs`,
`bash -n kernel/rust/tests/run_equivalence.sh`,
`kernel/rust/tests/run_equivalence.sh` with
`x86_memory_helpers ok digest=05ab1d79d7050a92`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` plus `ihk_ko`, `ihk-smp-x86_64_ko`, and
`mcctrl_ko` builds, C-fallback `mckernel.img`, build-only wrapper validation,
`git diff --check`, script syntax checks, and
`scripts/rust-ownership-report.py --check-dashboard overview.txt
--check-dashboard overview2.txt --top 5`. Captured build logs
`/tmp/mckernel-clean-build-x86-clear-effects-rust.log`,
`/tmp/mckernel-clean-build-x86-clear-effects-cfallback.log`, and
`/tmp/mckernel-clean-build-x86-clear-effects-wrapper.log` had no
`warning`/`warn:` matches. QEMU tooling was checked with
`qemu-system-x86_64 --version`, `/dev/kvm` presence, and
`scripts/qemu-mckernel-guest.sh --dry-run --accel tcg`; no real guest boot was
run because no usable Rocky/RHEL-family qcow2 guest image was available without
privileged image-directory access.

Validation for the latest x86 set-range side-effect batch passed
`rustfmt kernel/rust/x86_memory_helpers.rs`,
`bash -n kernel/rust/tests/run_equivalence.sh`,
`kernel/rust/tests/run_equivalence.sh` with
`x86_memory_helpers ok digest=fd4909d0a758b83b`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` plus `ihk_ko`, `ihk-smp-x86_64_ko`, and
`mcctrl_ko` builds, C-fallback `mckernel.img`, build-only wrapper validation,
and a no-warning scan over
`/tmp/mckernel-clean-build-x86-setrange-effects-rust.log`,
`/tmp/mckernel-clean-build-x86-setrange-effects-cfallback.log`, and
`/tmp/mckernel-clean-build-x86-setrange-effects-wrapper.log`. The first
equivalence attempt failed at the SIMD guard because LLVM emitted XMM stores
for ignored callback arguments; that failure was recorded in `kernel.log`, the
Rust/C fallback callbacks were adjusted to pass scalar values instead of
zeroing stack slots, a local disassembly check showed no SIMD-pattern matches,
and the rerun passed. QEMU tooling was checked with
`qemu-system-x86_64 --version`, `/dev/kvm` presence,
`scripts/qemu-mckernel-guest.sh --help`, and
`scripts/qemu-mckernel-guest.sh --dry-run --accel tcg`. No real QEMU guest boot
ran because no usable Rocky/RHEL-family qcow2 image was available from
unprivileged paths.

Validation for the latest x86 set-range level-body batch passed
`rustfmt kernel/rust/x86_memory_helpers.rs`,
`bash -n kernel/rust/tests/run_equivalence.sh`,
`kernel/rust/tests/run_equivalence.sh` with
`x86_memory_helpers ok digest=a354b06dbddad942`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` plus `ihk_ko`, `ihk-smp-x86_64_ko`, and
`mcctrl_ko` builds, C-fallback `mckernel.img`, build-only wrapper validation,
and a no-warning scan over
`/tmp/mckernel-clean-build-x86-setrange-body-rust.log`,
`/tmp/mckernel-clean-build-x86-setrange-body-cfallback.log`, and
`/tmp/mckernel-clean-build-x86-setrange-body-wrapper.log`. QEMU tooling was
checked with `qemu-system-x86_64 --version`, `/dev/kvm` presence,
`scripts/qemu-mckernel-guest.sh --help`, and
`scripts/qemu-mckernel-guest.sh --dry-run --accel tcg`. No real QEMU guest boot
ran because no usable Rocky/RHEL-family qcow2 image was available from
unprivileged paths.

Validation for the latest x86 range-top batch passed
`rustfmt kernel/rust/x86_memory_helpers.rs`,
`bash -n kernel/rust/tests/run_equivalence.sh`,
`kernel/rust/tests/run_equivalence.sh` with
`x86_memory_helpers ok digest=f60c57b101779395`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` plus `ihk_ko`, `ihk-smp-x86_64_ko`, and
`mcctrl_ko` builds, C-fallback `mckernel.img`, build-only wrapper validation,
and a no-warning scan over
`/tmp/mckernel-clean-build-x86-range-top-rust.log`,
`/tmp/mckernel-clean-build-x86-range-top-cfallback.log`, and
`/tmp/mckernel-clean-build-x86-range-top-wrapper.log`. QEMU tooling was checked
with `qemu-system-x86_64 --version`, `/dev/kvm` presence,
`scripts/qemu-mckernel-guest.sh --help`, and
`scripts/qemu-mckernel-guest.sh --dry-run --accel tcg`. No real QEMU guest boot
ran because no usable Rocky/RHEL-family qcow2 image was available from
unprivileged paths. Two initial validation failures were recorded in
`kernel.log`: the first was a test fake allocator that rejected the real
four-page TLB-array allocation size, and the second was an incompatible-pointer
warning scan finding for the set-range argument field pointers; both were
fixed before the clean validation pass.

Validation for the latest x86 visitor-body batch passed
`rustfmt kernel/rust/x86_memory_helpers.rs`,
`bash -n kernel/rust/tests/run_equivalence.sh`,
`kernel/rust/tests/run_equivalence.sh` with
`x86_memory_helpers ok digest=c701677a3fd62a6f`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` plus `ihk_ko`, `ihk-smp-x86_64_ko`, and
`mcctrl_ko` builds, C-fallback `mckernel.img`, build-only wrapper validation,
and a no-warning scan over
`/tmp/mckernel-clean-build-x86-visit-body-rust.log`,
`/tmp/mckernel-clean-build-x86-visit-body-cfallback.log`, and
`/tmp/mckernel-clean-build-x86-visit-body-wrapper.log`. QEMU tooling was
checked with `qemu-system-x86_64 --version`, `/dev/kvm` presence,
`scripts/qemu-mckernel-guest.sh --help`, and
	`scripts/qemu-mckernel-guest.sh --dry-run --accel tcg`. No real QEMU guest
	boot ran.

Validation for the latest x86 clear-range body batch passed
`rustfmt kernel/rust/x86_memory_helpers.rs`,
`bash -n kernel/rust/tests/run_equivalence.sh`,
`kernel/rust/tests/run_equivalence.sh` with
`x86_memory_helpers ok digest=67f14d5e0baf12ee`, the unresolved-symbol/SIMD
audit, Rust-enabled `mckernel.img` plus `ihk_ko`, `ihk-smp-x86_64_ko`, and
`mcctrl_ko` builds, C-fallback `mckernel.img`, build-only wrapper validation,
and a no-warning scan over
`/tmp/mckernel-clean-build-x86-clear-body-rust.log`,
`/tmp/mckernel-clean-build-x86-clear-body-cfallback.log`, and
`/tmp/mckernel-clean-build-x86-clear-body-wrapper.log`. QEMU tooling was checked
with `qemu-system-x86_64 --version`, `/dev/kvm` presence,
`scripts/qemu-mckernel-guest.sh --help`, and
`scripts/qemu-mckernel-guest.sh --dry-run --accel tcg`. No real QEMU guest boot
ran.

This still is not full core ownership for those areas: C keeps the external
allocation/hash and lock primitives, many CPU-local use sites, page frees,
page-table mutation, VM range allocation/free callbacks, range-tree
erase/removal side effects, memobj refcounting, actual page-table update side
effects, XPMEM flag mutation, memclear side effects, futex wake/wait/requeue
side effects, scheduler handoff, dispatch side-effect callback bodies, the
actual user-copy primitive, actual IKC send side effects, and broad runtime
lifetime behavior.

Primary Remaining Core C Bodies
------------------------------------------------------------

These are the blockers that matter for "core C is replaced with Rust core":

- Underlying page-table page allocation/free primitives, broader traversal
  orchestration, page frees, RSS updates, TLB-sensitive flushing, and
  map/free rollback behavior.
- Page allocator lifetime, remaining CPU-local allocator-cache ownership, allocation/free-to-
  system primitives, deferred-zero publication, and IKC-backed zeroing side
  effects.
- Process and VM lifetime: VM range allocation/free, memobj refcounting,
  range-tree erase/removal, page-fault orchestration, child-list mutation, rusage
  aggregation, wait orchestration, signal forwarding, and release paths.
- Syscall entry/dispatch, remaining user-copy primitive callbacks, Linux forwarding, remaining
  high-risk syscall handlers, and architecture register/fp/regset operations.
- Scheduler/timer/futex execution bodies: timer queues, wakeups, IPIs, context
  switching, futex allocation primitive callbacks, futex dispatch side-effect
  callback bodies, futex key lifetime, user-value loads, futex wait/wake/
  requeue side effects, and race retry behavior.
- Kernel-side Rocky control plane: IKC exchange mutation, callback invocation,
  waits, object allocation/free, device lifecycle, memory registration, file
  I/O, and kernel object lifecycle.

Migration Phases
------------------------------------------------------------

Phase 1: Helper migration.
Rust owns policies, validators, layout mirrors, result shaping, and bounded
helpers. C still owns the main execution body. Much of the current dashboard
progress lives here.

Phase 2: Body migration.
Rust owns the primary operation body. C becomes a narrow ABI wrapper or fallback
path. This is the next phase for the core OS target.

Phase 3: Side-effect ownership.
Rust owns mutation and orchestration across locks, refs, lists, allocation,
page-table writes, wakeups, user-copy staging, signal delivery, and IKC send or
receive behavior where those effects are McKernel-owned.

Phase 4: Fallback retirement gate.
C fallback remains only while needed for validation. A path reaches core
complete when the Rust body is the normal path, equivalence or runtime coverage
exists, and the remaining C is ABI glue, external-interface glue, or approved
fallback scaffolding.

Update Rule
------------------------------------------------------------

When a migration slice lands, update this file only if it changes strict core
ownership:

- Move a row forward only when Rust owns more of the primary body, not merely
  another helper called by C.
- Record C additions as glue/fallback only; do not count new C decision logic as
  Rust progress.
- Keep `overview.txt` for the broader functional percentage dashboard.
- Keep this file focused on x86_64 core OS ownership and the remaining C bodies
  that block a mostly Rust McKernel core.

Validation Gates
------------------------------------------------------------

Core ownership claims should be backed by non-reboot validation first:

- Rust and C fallback builds still compile.
- Focused equivalence tests cover policy, shaping, and bounded mutation helpers.
- `mckernel.img` links with the Rust-owned path enabled.
- Rust-linked IHK/mcctrl module-load smoke remains clean.
- Runtime Rocky smokes are added before retiring C fallback for mutation-heavy
  paths.

Reboot-capable validation remains opt-in and must not be run without explicit
approval.
