McKernel Whole-Tree Rust Completion Overview
Updated: 2026-05-28

Scope
------------------------------------------------------------

This file tracks the literal full McKernel Rust port target on Rocky Linux.
It is separate from overview.txt because overview.txt currently tracks the
McKernel core helper/decision surface, where some areas are much farther ahead
than the full source tree.

Target definition:

- Rocky Linux remains the paired Linux host target for the active x86_64 port.
- All McKernel-owned non-arm64 functional code is in scope for Rust ownership.
- IHK host modules are now explicitly in scope for full completion: ihk,
  ihk-smp-x86_64, and mcctrl. Preserve Linux-facing ABI and module behavior
  while moving owned helper, policy, and mutation bodies into Rust.
- User tools are in scope where they are McKernel-owned.
- Public C headers, ioctl numbers, syscall numbers, exported ABI symbols, and
  Linux-facing compatibility surfaces may remain as C ABI shims while logic
  moves to Rust.
- Architecture assembly, trampolines, context-switch assembly, and Linux
  kernel internals are not counted as Rust-port debt unless the project later
  chooses to replace them.
- arm64 is deferred and excluded from the active completion denominator until
  the x86_64/Rocky target is stable.

Whole-Tree Completion Estimate
------------------------------------------------------------

Functional dashboard average, arm64 excluded: 96.7%
McKernel-owned core row average, excluding build/host/user/Rocky/arm64: 100.0%
Mechanical LOC audit share: 32.2% Rust implementation, not a progress score
Active continuation target: finish the capped 500/500 tracked sequencing score
after the additional +100 request. The tracker is already at 500/500, so new
verified work records post-saturation strict-core ownership movement instead of
increasing the capped broad dashboard score. Older +32/+50 references are
historical checkpoints, not the active target.
Post-+50 strict-core movement: +1095 strict-core points landed after the broad
dashboard reached its current cap. The latest wait/scheduler/syscall/allocator/host/process
movement covers allocated process/VM/resource/idle object zeroing body routing
through Rust for create-thread, fork, new resource-set, process/thread hash,
pid1, idle-thread, idle-VM, and idle-process initialization; process spinlock
initialization gate routing through Rust for process McKernel-fd, VM page-table,
and create/clone spin-sleep locks; process-VM initialization body routing
through Rust for memory-range lock initialization dispatch, page-table spinlock
initialization dispatch, VM refcount publication, VM rb-root/address-space/proc
state publication, NUMA-mask/default-policy initialization, range-cache reset,
and TOFU STAG lock/hash initialization when enabled; process init
state/parent-inheritance body routing through Rust for pid/status publication,
parent/ppid-parent publication, credential inheritance, memory-policy and
THP-disable inheritance, rlimit and CPU-set copying, and UTI enable
inheritance; process init list/lock/waitq/refcount body routing through Rust
for hash/sibling/ptrace/report/thread/child list initialization,
update/threads/children/coredump rwlock initialization, McKernel-fd spinlock
initialization, waitpid wait queue initialization, process refcount
publication, and monitoring-event reset; process init profile state body
routing through Rust for profile lock initialization dispatch and profile-event
list reset under `PROFILE_ENABLE`; clone-thread base/scheduler state copy body
routing through Rust for CPU-set, in-kernel state, scheduler policy, and
scheduler priority copy;
clone user-context copy plus stack-pointer/program-counter mutation, fork
profile inheritance, on-fork VM publication/clear, and McKernel-fd struct copy
body routing through Rust;
copy-user-range VM-range metadata/refcount setup and PTE-copy argument
initialization through Rust;
copy-user-PTE page-buffer copy/zero body routing through Rust;
process update-page-table attr/lock/set-range/log body routing through Rust;
split shared-memobj page-update body routing through Rust;
split-range allocation/init body routing through Rust;
split-range publish body routing through Rust;
default copy/split/join VM-range memobj ref/unref direct routing and free-memory-range memobj ref/unref direct routing through Rust; process sync-memory-range lock/memobj/visit/log body routing, process remap-memory-range lock/memobj/pgshift/visit/log body routing, process invalidate-memory-range boundary-split/free-or-visit/log body routing, process per-page invalidate PTE/fileoff/exchange/flush/contiguous/memobj-log body routing, page_map public inline body routing, page predicate public inline body routing, page_unmap public result/log body routing, phys_to_page public lookup/insert body routing, page-hash table init/count-pages body routing, NUMA-distance public node-count routing, NUMA-distance init public node-count routing, free-path NUMA-node lookup bounds routing, try-alloc public node-count routing, distance-id public node-count routing, allocator public body node-count routing, dump free-page public node-count routing, query-free interrupt public node-count/body dispatch routing, pending-free public wrapper panic routing, pending-free public wrapper pending-list routing, reserve-pages public wrapper panic/error routing, mckernel free-pages public wrapper pending-list routing through Rust;
allocator public wrapper dispatch for NUMA-distance node-count lookup,
NUMA-distance init node-count dispatch, free-path NUMA-node bounds selection,
allocation-attempt node-count dispatch, distance-ID node-count dispatch,
top allocator public-body node-count dispatch, dump free-page query node-count
dispatch, and query-free interrupt node-count/body dispatch through Rust;
process split page-table split dispatch body routing through Rust;
process add-range public bounds/body routing through Rust;
process range lookup/iteration/extend public wrapper routing through Rust;
process update-page-table public wrapper routing through Rust;
process change-protection public wrapper routing through Rust;
process CPU-set public wrapper routing through Rust;
process access-ok public wrapper/error-log routing through Rust;
address-space hold/release/detach public wrapper routing now verified through Rust;
direct Rust process/VM/thread/address-space/sigcommon refcount inc/set/dec
primitive mutation for the default lifecycle paths;
CPU-local scheduler queue
spinlock initialization gate routing through Rust for idle-time run queue and
migration queue locks; process thread allocation
initialization body routing through Rust for create/clone thread zeroing,
refcount publication, and hash/siblings list initialization; process thread
sigstack disable body routing through Rust for initial create-thread and
shared-VM clone altstack reset; process
sigcommon/thread-sigpending initialization body routing
through Rust for create/clone sigcommon allocation, zeroing, refcount
publication, rwlock/list initialization, and per-thread sigpending rwlock/list
initialization; process create-thread CPU-set selection/default publication body
routing through Rust for requested CPU validation/logging, default all-CPU
fallback selection, and thread/process CPU-set bit publication; process
create/chain side-effect callback gate routing through Rust for initial
address-space CPU-set publication and chain-thread VM refcount increment
dispatch; process create-thread/fork cleanup callback gate routing
through Rust for initial create-thread proc free, VM free, sigcommon free, and
thread page-free dispatch, plus fork-time McKernel-fd copy lock/unlock dispatch;
process clone/fork error-unwind callback gate routing through
Rust for McKernel-fd cleanup lock/unlock dispatch, VM-range cleanup dispatch,
saved-command-line free dispatch, VM free dispatch, page-table destroy dispatch,
address-space free dispatch, process free dispatch, and thread page-free
dispatch;
process destroy-thread cleanup callback gate routing through
Rust for address-space CPU-set clear dispatch, sigpending drain/free dispatch,
and main-thread page-free dispatch;
process clone-path callback gate routing through Rust for
McKernel-fd duplicate dispatch, shared-sigcommon refcount increment dispatch,
and clone-cleanup FP-reg release dispatch;
miscellaneous process callback gate routing through Rust for
free-range page-size probing, McKernel-fd drain-free dispatch, CPU-set spin
lock/unlock dispatch, ref-hold increment dispatch, and exited-thread warn
dispatch;
process detach/cleanup/sigcommon/TID-log callback gate routing
through Rust for detach-with-pid, release-process dispatch, optional thread
cleanup frees, FP-reg release, sigpending/sigcommon free dispatch, sigcommon
refcount-decrement dispatch, and release/replace TID log dispatch;
process release lifecycle callback gate routing through Rust
for release-process detach/profile/free-thread-pages/McKernel-fd drain/free/
final-cleanup dispatch, release-thread profile/procfs-delete/destroy/
release-VM dispatch, release-VM McKernel-fd close/free-callback/flush/
free-ranges/detach/release-process/policy-drain/free dispatch, and spin-lock
callback gates around the release McKernel-fd paths;
flush/free-all memory-range callback gate routing through Rust
for noirq lock/unlock, range free dispatch, error-log dispatch, and full-tree
free-all traversal dispatch;
address-space lifecycle callback gate routing through Rust
for refcount decrement/test, optional free-callback dispatch, page-table
destroy, allocation/free, page-table create, refcount set, CPU-set lock
spin-init, and detach-release dispatch;
VM-range insert log/dump callback gate routing through Rust
for overlap logging, success logging, and post-insert dump dispatch;
add-range callback gate routing through Rust for VM range
allocation/free, tree insertion, mapping update, cleanup remove, XPMEM
marking, physical memclear, and optional add-range log dispatch;
remove-memory-range callback gate routing through Rust for
split dispatch, XPMEM remove dispatch, range-free dispatch, and optional
remove-range log dispatch;
free-memory-range body callback gate routing through Rust for
noirq lock/unlock dispatch, optional memobj ref/unref dispatch, page-table
free/clear dispatch, optional TOFU removal dispatch, finalize dispatch, and
free-range log dispatch;
process range split/join/finalize/remove-region callback gate
routing through Rust for memobj ref/unref dispatch, split-range insert
dispatch, join-range free/TOFU merge dispatch, free-range
physical-to-virtual/free-pages/clear-main/free dispatch, and remove-region
clear/log dispatch;
populate callback gate routing through Rust for page-fault
dispatch, preempt disable/enable, and optional populate-warning dispatch;
page-fault retry/public callback gate routing through Rust for
page-fault dispatch, preempt enable/disable, pending-PGIO dispatch, and PGIO
slot clearing; process page-fault body callback gate routing through Rust for
read/write memory-range locks, zero-object match dispatch, and normal/XPMEM
fault dispatch; process change-protection callback gate routing through Rust
for VR-flag-to-PTE-attribute conversion, noirq page-table lock/unlock, and
page-table attribute change dispatch; process init-stack callback gate routing through Rust for
aligned allocation, page-free, VM range add, virt-to-phys translation,
VR-flag-to-PTE-attribute conversion, page-table set-range dispatch, HWCAP
query, user-context stack-pointer publication, and init-stack logging;
`perf_fcntl()` body routing through
`perf_fcntl_body_result()` for `F_SETSIG` `sig_no` publication,
`F_SETOWN_EX`/default no-op classification, Linux-forward callback dispatch,
forward return propagation, and missing-callback/null-fd validation;
`perf_mmap()` body routing through `perf_mmap_body_result()` for
`MAP_ANONYMOUS`/`PROT_WRITE` forcing, `do_mmap()` callback dispatch, perf mmap
page `data_head` publication, `cap_user_rdpmc` capability-bit publication,
missing-callback validation, and return propagation;
`perf_counter_set()` entry/body routing and `perf_start()` body routing through
Rust-owned result helpers for perf attr-flag callback dispatch,
counter-id/hw-config field loading, perf counter mode construction from
exclude flags, extra-register setup dispatch, raw-counter init dispatch,
leader/sibling start traversal, invariant-TSC accumulation and base-TSC
publication, inactive-to-active state publication, set-period/counter-set
callback dispatch, hardware-start mask aggregation, and process perf-status
publication;
`perf_event_read_value()` entry routing through
`perf_event_read_value_entry_body_result()` for attr flag callback dispatch,
missing-callback/null-event rejection, attr callback failure short-circuiting,
inherited-count flag handoff, update callback selection, and atomic count read
dispatch;
host control-plane current-context wrapper routing for procfs answers and
remote page-fault handling, covering current IKC response-channel selection,
current-thread selection, callback absence rejection, procfs answer dispatch
into the Rust-owned packet builder, and remote page-fault body handoff;
host prepare-process request wrapper routing through
`host_prepare_process_request_result()` for request argument extraction,
prepare-process callback dispatch, ACK message selection, reply packet handoff,
null-request rejection, and missing-callback rejection;
host procfs request wrapper routing through `host_procfs_request_result()` for
callback presence validation, null-request rejection, callback dispatch, and
return propagation;
host sysfs packet and unknown-message log wrapper routing through
`host_sysfs_packet_result()` and `host_unknown_packet_log_result()` for sysfs
callback presence validation, callback argument forwarding, unknown-packet log
callback gating, null-packet rejection, and return shaping;
host packet-release, init-channel ACK log, and schedule-process log wrapper
routing through `host_release_packet_result()`, `host_init_ack_log_result()`,
and `host_schedule_process_log_result()` for callback presence validation,
null-packet rejection, schedule request argument extraction, callback dispatch,
and return shaping;
host IKC packet-send wrapper routing through `host_ikc_packet_send_result()`
for send callback presence validation, null-packet rejection, send callback
dispatch, and return shaping;
host map-memory, prepare-map-virtual, map-virtual, unmap-virtual, and
unmap-memory callback wrapper routing through `host_map_memory_result()`,
`host_prepare_map_virtual_result()`, `host_map_virtual_result()`,
`host_unmap_virtual_result()`, and `host_unmap_memory_result()` for callback
presence validation, map and unmap argument forwarding, null-callback return
shaping, callback result propagation, and unmap success return shaping;
host prepare-process primitive callback wrapper routing through Rust for
add-range, user-page allocation/free, virtual-to-physical translation,
VR-flag-to-PTE-attribute conversion, page-table set-range,
user-context mutation, heap allocation/free, `memcpy_long()`, thread
create/destroy, NUMA-node count, TLB flush, VDSO map, and init-stack dispatch;
host schedule-process primitive callback wrapper routing through Rust for
thread-to-process lookup, current-CPU selection, CPU-allowed query, CPU-id
obtain, process PID query, PC/SP register reads,
invalid/received/no-CPU/queued schedule logs, thread TID/status publication,
thread/process chain publication, and runqueue-add dispatch;
host remote page-fault primitive callback wrapper routing through Rust for
profile-enabled query, timestamp query, preempt disable/enable dispatch,
page-fault process dispatch, profile-event dispatch, remote page-fault log
dispatch, allocation, packet copy, defer publication, scheduler wakeup, and
missing-thread log dispatch;
host remote page-fault body call-site routing through Rust gates for
profile-enabled checks, timestamp reads, optional page-fault logging, preempt
disable/enable dispatch, page-fault processing, profile-event accounting, and
answer dispatch while preserving the error-reply fast path;
host schedule-process request call-site routing through Rust gates for
thread-to-process lookup, current CPU selection, PID/register reads,
CPU-allowed and fallback CPU selection, invalid/received/no-CPU/queued logs,
TID/status publication, thread/process chain publication, and runqueue-add
dispatch;
host prepare-process body/request call-site routing through Rust gates for
monitor checks, descriptor map/validate/clone, allocation/free/copy,
main-thread create/destroy, process/VM/NUMA metadata publication,
prepare-ranges dispatch, failure cleanup, success unmap, TLB flush, and
request ACK preparation;
process init-stack callback gate routing through Rust for aligned allocation,
page-free, VM range add, virt-to-phys translation, VR-flag-to-PTE-attribute
conversion, page-table set-range dispatch, HWCAP query, user-context
stack-pointer publication, and init-stack logging;
process change-protection callback gate routing through Rust for
VR-flag-to-PTE-attribute conversion, noirq page-table lock/unlock, and
page-table attribute change dispatch;
process page-fault body callback gate routing through Rust for read/write
memory-range locks, zero-object match dispatch, and normal/XPMEM fault
dispatch;
page-fault retry/public callback gate routing through Rust for page-fault
dispatch, preempt enable/disable, pending-PGIO dispatch, and PGIO slot
clearing;
VM-range insert log/dump callback gate routing through Rust for overlap
logging, success logging, and post-insert dump dispatch;
add-range callback gate routing through Rust for VM range allocation/free, tree
insertion, mapping update, cleanup remove, XPMEM marking, physical memclear,
and optional add-range log dispatch;
remove-memory-range callback gate routing through Rust for split dispatch,
XPMEM remove dispatch, range-free dispatch, and optional remove-range log
dispatch;
populate callback gate routing through Rust for page-fault dispatch, preempt
disable/enable, and optional populate-warning dispatch;
free-memory-range body callback gate routing through Rust for noirq lock/unlock
dispatch, optional memobj ref/unref dispatch, page-table free/clear dispatch,
optional TOFU removal dispatch, finalize dispatch, and free-range log dispatch;
process range split/join/finalize/remove-region callback gate routing through
Rust for memobj ref/unref dispatch, split-range insert dispatch, join-range
free/TOFU merge dispatch, free-range physical-to-virtual/free-pages/clear-main/
free dispatch, and remove-region clear/log dispatch;
host IKC init primitive callback wrapper routing through Rust for IKC connect
dispatch, delay dispatch, current-channel publication, regular-channel
publication, panic dispatch, and IKC init log dispatch;
host cleanup, signal, wake, debug, and register primitive callback wrapper
routing through Rust for CPU register read/write dispatch, cleanup-process log
dispatch, terminate-host dispatch, cleanup-fd log dispatch, signal `do_kill()`
dispatch, signal log dispatch, thread lookup, waitqueue wake dispatch, thread
unlock dispatch, wake-syscall-thread log dispatch, debug-log dispatch, and
debug print dispatch;
host perf-control primitive callback wrapper routing through Rust for raw perf
init, perf stop, perf reset, perf start, perf read, and unexpected-control
logging dispatch;
host current-pointer, monitor-status, prepare-log, and Tofu support callback
wrapper routing through Rust for current IKC/current-thread pointer reads,
monitor status reads, prepare-process/ranges/args log dispatch, and
feature-gated Tofu finalize dispatch;
host prepare/cleanup dispatch callback wrapper routing through Rust for
prepare-process dispatch, prepare-ranges dispatch, cleanup-process dispatch,
and cleanup-fd dispatch;
host SCD dispatcher operation callback gate routing through Rust for response
callbacks, plain packet callbacks, remote page-fault callbacks, and procfs
callback dispatch;
current-context call-site routing through the Rust current-pointer gate for
procfs-answer, remote page-fault, and syscall packet-handler paths;
null-preserving dummy packet-release dispatch plus IKC init allocation,
connect, retry delay, log, panic, current-channel, and regular-channel
call-site routing through Rust-owned gates;
remote page-fault request call-site routing through Rust-owned gates for
thread lookup, missing-thread log, body callback dispatch, allocation, packet
copy, defer publication, scheduler wakeup, and thread unlock;
reply send routing and host request call-site routing through Rust-owned gates
for perf-control, CPU-register, cleanup, signal, wake-syscall, and debug-log
requests;
`perf_reset()` body routing through `perf_reset_body_result()` for
leader/sibling reset traversal, counter-mask validation callback dispatch,
invariant-TSC reset base selection, accumulator clearing, hardware-backed count
read callback dispatch, atomic count reset callback dispatch, and
missing-callback/null-event validation;
`perf_stop()` body routing through `perf_stop_body_result()` for
leader/sibling stop traversal, counter-mask validation callback dispatch,
invariant stopped-TSC publication, active-to-inactive state publication,
hardware stop-mask aggregation, hardware stop callback dispatch, post-stop
update callback dispatch, `monitoring_event` clearing, and `perf_status`
`PP_NONE` publication;
`perf_event_open()` support-policy validation through
`perf_event_open_validate_body_result()` for CPU, flags, event type,
read-format, freq, and sample-period support classification, including
high-bit sample-period `-EINVAL` precedence before unsupported-feature
`-ENOENT` shaping;
`perf_event_open()` event allocation initialization through
`perf_event_alloc_init_body_result()` for event zeroing, perf-attr copy
publication, group/sibling list-head initialization,
sample-frequency/count/child-total/parent publication, hardware
sample-period/last-period/period-left publication, and invariant
REF_CPU_CYCLES fast-path classification;
`perf_event_open()` event allocation mapping/configuration through
`perf_event_alloc_map_body_result()` for hardware/cache/raw type dispatch,
event-map callback selection, extra-config callback dispatch for cache events,
validation result shaping, `hw_config`/`hw_config_ext` publication,
extra-register id/MSR/index callback sequencing and publication,
`hw_perf_event_init()` callback dispatch, and successful `*out` publication;
full `mc_perf_event_alloc()` wrapper orchestration through
`perf_event_alloc_body_result()` for attr presence validation, event allocation
callback dispatch, allocation-failure `-ENOMEM` shaping, initialization helper
dispatch, invariant fast-path output publication, mapping/configuration helper
dispatch, and cleanup-on-error free callback dispatch;
post-validation `perf_event_open()` top-level body routing through
`perf_event_open_body_result()` for event-allocation dispatch, event
PID/counter sequencing, group wiring sequencing, Linux fd request sequencing,
mckfd allocation callback dispatch, mckfd publication dispatch, and
return/error propagation across counter, group, Linux-fd, allocation, and
publish failures;
`perf_event_open()` entry copy-in, support validation, and post-validation
handoff through `perf_event_open_entry_body_result()` for perf-attr
copy-from-user callback dispatch, copy failure `-EFAULT` shaping,
type/read-format/sample-period loading, freq bitfield callback dispatch,
validation callback sequencing, validation error propagation, and successful
handoff to the Rust-owned post-validation body;
`perf_event_open()` group wiring through
`perf_event_open_group_body_result()` for standalone-vs-grouped leader
selection, sibling-list tail insertion, sibling count publication, and leader
`pmc_status` bit publication from the allocated counter index;
`perf_event_open()` mckfd publication through
`perf_event_open_mckfd_publish_body_result()` for mckfd fd, signal number,
event data pointer, read/ioctl/mmap/close/fcntl callback publication, process
mckfd-lock callback sequencing, and process mckfd head insertion;
`perf_event_open()` counter and Linux fd sequencing through
`perf_event_open_counter_body_result()` and
`perf_event_open_linux_fd_body_result()` for event PID publication,
counter-allocation callback dispatch, counter-id publication, Linux
`perf_event_open` request-number/arg0 publication, `do_syscall()` callback
dispatch, negative-fd return shaping, and thread `pmc_alloc_map` publication;
`perf_close()` body routing through
`perf_close_body_result()` for counter allocation bitmap clearing, optional
extra-register bitmap clearing, invalid-bit no-op shaping, and free callback
dispatch; `perf_ioctl()` command dispatch through
`perf_ioctl_body_result()` for enable/disable/reset/refresh selection, local
monitoring-event publication, target-process lookup callback dispatch, target
monitoring-event/perf-status publication for newly monitored processes,
process-unlock callback ordering, inherited-event refresh rejection,
unsupported-command shaping, and missing-callback/null-event validation;
top-level `perf_read()` group-vs-single dispatch through
`perf_read_body_result()` for group-format testing, read-group vs read-one
callback selection, missing-callback/null-event validation, and callback return
propagation; `perf_event_read_one()` and `perf_event_read_group()` copyout
sequencing through Rust-owned body helpers for one-event count read callback
dispatch, grouped leader/sibling list traversal, group count publication,
per-sibling value copyout ordering, copy-failure `-EFAULT` shaping,
missing-callback/null-leader validation, and byte-count return shaping;
`perf_event_read_value()` body routing through
`perf_event_read_value_body_result()` for stopped-vs-live TSC selection,
invariant-TSC user/kernel count arithmetic, event-update callback dispatch for
non-invariant leader events, atomic count-read callback dispatch, inherited
child-total addition, null input handling, and return shaping;
`sync_child_event()` body routing through
`sync_child_event_body_result()` for event eligibility gating, leader and
sibling action selection, sibling-list traversal, counter-read callback
dispatch, child-total accumulation, atomic count-set callback dispatch, and
missing-callback/null-leader error shaping; `process_cleanup_fd()` and
`process_cleanup_before_terminate()` body routing through Rust-owned cleanup
body helpers for process lookup callback dispatch, normal lookup-miss
return/log handling, cleanup callback dispatch, optional TOFU fd sweep
selection and iteration, and process-unlock callback ordering;
`terminate_host()` body routing through `terminate_host_body_result()` for
process lookup callback dispatch, lookup-miss detached-thread release
sequencing, refcount-set callback dispatch, release-thread/release-process
callback ordering, nohost kill-forward classification, nohost publication,
process-unlock callback dispatch, and `SIGKILL` do-kill callback dispatch;
`terminate_mcexec()` body routing through
`terminate_mcexec_body_result()` for group-exit-status claim checking,
exit-status construction, compare-exchange callback dispatch and failure
classification, nohost forwarding gate, exit-group request field publication,
nohost publication, and Linux-forward callback dispatch; top-level
`finalize_process()` body routing through
`finalize_process_body_result()` for update-lock callback sequencing,
`PS_ZOMBIE` status publication, pid1-vs-parent-notify branch selection, pid1
release callback dispatch, wakeup-log callback dispatch, and handoff to the
Rust-owned parent-notification body; `finalize_process()` non-pid1 parent
notification through
`finalize_process_parent_notify_body_result()` for parent presence validation,
termsig-to-notify gating, SIGCHLD siginfo construction, process utime/stime
jiffy callback sequencing, `do_kill` callback dispatch, optional log callback
dispatch, waitpid wake callback dispatch, and return shaping; thread-exit
signal reporting through
`thread_exit_signal_body_result()` for report-parent gating, signal selection,
SIGCHLD siginfo construction, TSC-to-jiffy callback sequencing, `do_kill`
callback dispatch, log callback dispatch, and waitpid wake callback dispatch;
stopped-child wait body routing through
`wait_stopped_body_result()` for stopped source selection, status publication,
thread/process/main-thread exit-status reap callback dispatch, and PID/TID
return shaping; scheduler priority syscall wrapper body routing for
`sched_get_priority_max()` and `sched_get_priority_min()` through Rust-owned
body-result helpers, plus
`move_pages()` SMP request field publication through Rust for count, user
address/status/node vectors, kernel scratch vectors, PTE vector, node-ready
reset, destination physical vector, process pointer, phase counter reset, and
phase return reset, plus a page-allocator fallback cleanup that routes
Linux-zero request packet publication through the existing Rust-owned
zero-request helper. The simple `SYSCALL_DECLARE()` wrapper audit now finds no
wrappers without `_result(` in their textual body; this is only a narrow routing
check, not proof that top-level syscall dispatch, scheduler internals, or C
fallback bodies are retired. The latest allocator movement also covers
low-level Page allocator registration and dispatch state for `pa_ops` storage,
tracking-init and early-invalidate callback ordering, allocator ops
publication, aligned/default allocation dispatch, free dispatch,
early-allocation fallback selection, and exported `___ihk_mc_*()` body
publication. Recent host/IKC movement covers top-level syscall packet-handler
wrapper sequencing, dummy packet-handler release sequencing, and public
IKC2Linux/IKC2McKernel init panic-on-error wrapper sequencing. Recent allocator
movement covers the public bitmap
`ihk_pagealloc_*()` wrapper layer for exported init failure logging, default
init, destroy free-callback dispatch,
alloc/reserve/free/count/query dispatch, double-free error routing, and
zero-free begin/done logging. Recent allocator movement covers `numa_init()`
lifecycle sequencing for NUMA-node discovery, node-field publication,
memory-chunk ingestion, early-heap adjustment, free-page/pagealloc dispatch,
rusage accounting, and final-node logging. Recent memory-policy movement covers
`get_mempolicy()`, `set_mempolicy()`, and `mbind()` body sequencing for
validation, nodemask copyin/copyout, process-mask
mutation, range-policy lookup/update, and policy/interleave publication. The previous
Process/VM movement covers `ihk_mc_pt_lookup_fault_pte()` and `lookup_node()`
sequencing for initial PTE lookup, missing-PTE fault dispatch, retry lookup,
recovered-PTE logging, lookup-node fault dispatch/error return,
address-space/page-table extraction, post-fault PTE lookup, absent-PTE
`-ENOENT` shaping, PTE physical-address extraction, and physical-to-NUMA-node
return shaping. The earlier allocator
movement covers `query_free_mem_interrupt_handler()` NUMA-node iteration, per-node free-page
callback dispatch, total accumulation/logging, optional FUGAKU panic dispatch,
`memdebug` lookup, kmalloc/pagealloc memcheck dispatch, page-hash count/log
callback dispatch, and optional ATTACHED_MIC sbox scratch publication. Earlier
allocator movement covers wrapper selection and free handoff in
`mckernel_allocate_aligned_pages_node()` and `mckernel_free_pages()`:
invalid-size/default-idle/current-VM/range-policy/process-policy selection,
current-node publication, pending-free enqueue, and allocator-free fallback
sequencing. The previous movement covers the
`init_process_stack()` primary execution body: stack sizing, max/min limit
selection, AP-user policy, aligned allocation, stack zeroing, VM range creation,
page-table set-range callback dispatch, auxv/argv/env layout, `saved_auxv`
publication, user stack pointer/context publication, and VM stack bounds
publication. Earlier movement covers host args/envs stack-prep sequencing:
arg/env page-count and range placement, argenv page allocation and physical
publication, argenv memory-range creation, remote/local copy orchestration,
saved_cmdline publication, argv/env pointer fixup, vDSO map/clear,
rprocess/rpgtable publication, and init-stack callback dispatch. The previous
movement covers host prepare-ranges ELF section-loop sequencing,
interpreter/aout relocation, section page-span/range calculation,
memory-range creation, user-page allocation, page-table mapping
dispatch/failure cleanup, remote-PA and text/data/map/brk publication,
entry/auxv/context adjustment, and data-too-large return shaping. The previous
movement covers host
`process_msg_prepare_process()` freeze gates, program-descriptor map/span
validation, descriptor clone allocation/copy, main-thread creation,
process/thread/VM metadata publication, NUMA policy publication,
prepare-ranges dispatch, success cleanup, unmap, TLB flush, destroy, and error
shaping. The previous movement covers Process/VM
`free_process_memory_range()` page-table free/clear lock and memobj
ref/unref ordering, TOFU removal callback dispatch, final range
detach/straight cleanup, error-log selection, and return shaping. The earlier
movement covers Process/VM
remove-process-memory-range straight-map conversion, range iteration,
split-start and split-end callback sequencing, read-only freed publication,
XPMEM removal callback dispatch, free callback dispatch, error-log selection,
and final return shaping. Earlier movement covers Process/VM
do-page-fault outer-body sequencing, page-fault retry/page-I/O dispatch, and
populate-loop preempt/page-walk/warning sequencing. Earlier movement covers
Process/VM range lookup, range-cache publication, next/previous iteration,
extend-up, change-protection, and `access_ok()` body sequencing.
Earlier movement covers XPMEM clear-PTE and process-memory-range removal sequencing:
segment/AP/attachment traversal, AP ref/unlock/clear/relock/deref ordering,
attachment range calculation, unpin callback dispatch, range lookup, munmap
callback dispatch, VALIDPTE clearing, out-of-range/missing-range preservation,
and `xpmem_remove_process_memory_range()` null/destroying/full-detach/trim/split
result sequencing. Earlier movement covers XPMEM detach/munmap sequencing: public detach lookup/private-data
validation, attachment ref/lock/destroy-start sequencing, owner gating and
rollback, unpin callback dispatch, range private-data clearing,
`xpmem_vm_munmap()` begin/remove/finish sequencing, VALIDPTE clearing, AP list
detach, destroyable/deref dispatch, and internal `xpmem_detach_att()`
already-destroying, missing-range, and success paths. Earlier movement
covers XPMEM release-management wrapper/drain sequencing: AP hash-bucket
iteration, per-bucket writer-lock/unlock sequencing, empty-bucket handling,
first-AP selection from hash-list heads, AP ref/unlock/release/deref/relock
ordering, release/remove syscall wrapper positive-ID, owner, object-lookup,
action, and deref sequencing, and thread-group destroyable/deref dispatch. The
previous movement covers XPMEM access-permit release lifecycle sequencing:
destroy-start gating, attachment-list drain, attachment ref/detach/deref
callback ordering, final destroyed-state publication, AP hash-list unlink,
segment AP-list unlink, segment and segment-thread-group deref callback
ordering, AP destroyable dispatch, and already-destroying early return
behavior. The earlier movement covers XPMEM thread-group segment-list drain sequencing: writer-lock
acquisition, empty-list detection, first-segment selection from the list head,
segment refcount callback ordering, unlock-before-removal, per-segment removal
callback dispatch, deref callback ordering, relock loop sequencing, final
unlock, and empty-list return behavior. The earlier movement covers XPMEM
segment-removal lifecycle sequencing: destroy-start gating, segment
destroy-flag publication, PTE-clear callback ordering, final destroyed-state
publication, segment-list lock/unlock ordering, list unlink,
destroyable/refdrop callback dispatch, and already-destroying early return
behavior. The still earlier movement covers XPMEM `close()`/`dup()`/flush lifecycle
sequencing: duplicate data clearing and open-count increment, close open-count
decrement/logging, flush/partition-exit decisions, target-group lookup under
the partition hash-list lock, hash-list unlink, destroy-flag publication,
access-permit/segment release callback ordering, final destroyed-state
publication, and destroy callback dispatch. The still earlier movement covers
XPMEM `open()`/`openat()` body sequencing below
the syscall pathname wrapper: partition-init gate, Linux fd-forward result
handling, `__xpmem_open()` callback ordering, mckfd allocation/null handling,
mckfd zeroing and field publication, locked list-head insertion, unlock
ordering, open-count increment, and debug-log event selection. The still earlier
movement covers live mckfd syscall dispatch for `read()`, `ioctl()`,
`fcntl()`, and `close()`: locked mckfd lookup traversal, callback-vs-forward
selection, optional TOFU ioctl/close cleanup gates, close unlink mutation,
close-callback dispatch, mckfd free-callback dispatch, and Linux-forward
fallback sequencing. The still earlier movement covers ten syscall wrapper
bodies for `times()`, `setpgid()`,
`setrlimit()`, `getrlimit()`, `prlimit64()`, `sysinfo()`, `get_cpu_id()`,
`mlockall()`, `munlockall()`, and `getcpu()`: CPU-time accounting/copyout/
current-time return shaping, process-group normalization/find/forward/mutation,
prlimit argument routing, sysinfo result fill/copyout, CPU/NUMA id copyout,
mlockall policy/logging, and missing-callback error shaping. The previous
movement covers signal and
credential syscall wrapper bodies for `kill()`, `tgkill()`, `tkill()`,
`setresuid()`, `setreuid()`, `setuid()`, `setfsuid()`, `setresgid()`,
`setregid()`, `setgid()`, and `setfsgid()`: validation, siginfo shaping,
do-kill dispatch, Linux-forward dispatch, credential-refresh dispatch, setfsid
syscall dispatch, and return shaping. The previous movement covers top-level
`ptrace()` request/callback body dispatch for traceme, wake/resume,
register/fpreg/regset get/set, user/text peek/poke including data aliases,
set-options, attach/detach, siginfo/eventmsg, arch fallback, and
unsupported/missing-callback error shaping. The previous movement covers
`ptrace_detach_thread()` main-body sequencing, including main-thread detach
gating, zombie finalization selection, tracer child-list detach, ptraced
reparenting, report-list detach/reattach, ptrace cleanup/free dispatch,
single-step clear, exit-signal dispatch, forwarded-signal siginfo
construction, `do_kill()` callback sequencing, wake/release dispatch, and
final process finalization dispatch, plus outer `do_wait()` process/thread
scan traversal, pid/pgid matching, `empty` publication, process-zombie/
candidate dispatch, ptraced-children empty scans, report-thread candidate
dispatch, and regular thread empty scans, plus
`do_wait()` process-zombie host-wait/reparent/ptrace-detach/release
sequencing, top-level `do_wait()` waitqueue/scan-dispatch/nohang/
pending-signal/schedule/rescan sequencing, `do_wait()` process/report-thread
stopped/continued/exited candidate sequencing, shared `vsnprintf()` loop/
pointer formatting, syscall
wait wrapper/body sequencing,
local `setitimer()`/`getitimer()` validation, copyin/copyout, forwarding, and
timer callback dispatch sequencing, futex `queue_me()` waiter
enqueue/metadata/unlock orchestration, and futex
`unqueue_me()` lock-pointer race retry/detach/drop-ref sequencing, and futex
`futex_wait_queue_me()` state/spin-sleep/queue/schedule/log/finish sequencing,
and futex `futex_wait()` setup/wait/unqueue/post-action/retry/key-release
sequencing, and futex `get_futex_key()` private/shared key construction,
translation retry, fault logging, and key-reference callback dispatch, and
futex `futex_wake()` get-key/hash/lock/scan/unlock/put-key return sequencing,
and futex `futex_requeue()` get-key/hash/double-lock/optional-compare/
scan/drop-key/put-key return sequencing, and futex `futex_wake_op()`
get-key/hash/double-lock/atomic-op retry/wake-scan/put-key return sequencing,
and futex `futex_wait()` entry/profile pause/resume sequencing, public sysfs
create/mkdir/symlink/lookup/unlink/setup request send/wait/result sequencing,
sysfs init setup-buffer/setup-param orchestration, and public sysfs
packet-handler dispatch/log-callback selection, plus sysfs server
show/store/release request-wrapper response/result/log-error sequencing, plus
special sysfs create direct/string/bitmap conversion, plus public sysfs
request-wrapper send/response log selection and mkdir/lookup handle
publication, plus procfs thread-control packet/send/wait sequencing, plus
procfs buffer allocation/init/optional physical publication sequencing, plus
`/proc/PID/mem` zero-length/page-fault/translation/memory-gate/phys-map/
read-vs-write copy-loop/error-shaping sequencing, plus
root `mckernel` version/buildid output, root `stat` CPU-line output,
per-process `auxv`, `cmdline`, and `comm` output, `/proc/PID/pagemap`
page-table-to-pagemap value-loop/result sequencing, `/proc/PID/maps`
range/path/perms/line output, `/proc/PID/status` locked-size/state/ID/VmLck/
thread/mask output, and per-PID `stat` state/identity/comm/constant-field
output, plus host IKC2Linux channel-table allocation/zeroing/publication,
connect-parameter shaping, retry/delay/log sequencing, channel-slot
publication, current-channel callback sequencing, and host IKC2McKernel
parameter shaping, retry/delay/log sequencing, and regular-channel callback
sequencing, plus
low-level `___kmalloc()` size alignment, free-list fit search, chunk
split/refill, page-allocation callback ordering, interrupt save/restore
callback ordering, and return shaping, plus low-level `___kfree()` null
handling, corruption callback routing, current-CPU free-list insertion/
consolidation, remote-CPU free-list publication under lock callbacks, and
normal/error return shaping, plus
public `_kmalloc()`/`_kfree()` memdebug tracking wrapper sequencing for
no-debug/null fallback, allocation hash selection, tracking-entry lookup and
creation, file-string publication, alloc-count mutation, address-entry
list/hash publication, free-side address lookup/detach, last-entry
cleanup/free, invalid-free callback routing, and log callback selection, plus
public `_ihk_mc_alloc_aligned_pages_node()`/`_ihk_mc_free_pages()` pagealloc
memdebug tracking wrapper sequencing for no-debug/uninitialized fallback,
allocation-site lookup/create, file-string publication, page-address entry
publication, exact/partial/split deallocation tracking, address-entry
rehashing, last-entry cleanup/free, invalid deallocation callback routing,
final page-free callback sequencing, and log callback selection, plus
shared pagealloc/kmalloc tracking hash and lock initialization,
pagealloc/kmalloc memcheck leak-scan traversal with per-entry address-list
locking, runcount mutation, detail/summary log callback sequencing, and
kmalloc remote free-list move/consolidation under the remote-list lock, plus
procfs buffer-chain append/release sequencing, plus procfs request
release/finalization mutation, plus procfs answer packet/send-callback
sequencing, plus procfs backlog allocation/copy/add/free-on-error sequencing,
plus host remote page-fault answer packet/send-callback
sequencing, plus traditional host SCD ACK packet/send-callback sequencing for
prepare-process, send-signal, cleanup-process, and cleanup-fd responses, plus
host perf ACK and CPU register response packet/send-callback sequencing, plus
host perf-control dispatch/mode/callback/read-value sequencing, plus host
perf-control request map/dispatch/unmap/ACK sequencing, plus CPU register
request map/register-callback/unmap/response sequencing, plus host
cleanup-process cleanup/reply/terminate and cleanup-fd cleanup/log/reply
sequencing, host send-signal map/copy/unmap/ACK/do-kill/log sequencing, host
wake-syscall-thread find/log/wakeup/unlock sequencing, host debug-log
print/debug callback sequencing, host current-pointer/monitor/prepare-log/Tofu
support callback wrapper sequencing, and host remote page-fault body
err/profile/log/preempt/page-fault/answer sequencing, and host remote
page-fault request lookup/missing/current/deferred-copy/backlog/wakeup/unlock
sequencing, and host schedule-process thread/proc validation, CPU-selection,
TID/status publication, chain, runqueue-add, and log/error sequencing, and
top-level host SCD packet dispatch behind `syscall_packet_handler()` for
init-channel ACK, prepare-process, schedule-process, wake-syscall-thread,
remote page-fault, send-signal, procfs request/release, cleanup process/fd,
debug-log, sysfs show/store/release, perf-control, CPU register, unknown-
message logging, legacy return shaping, and final packet release sequencing,
and x86 process-VM verification/page-fault/copy loop sequencing, mapped versus
direct physical-copy selection, byte-copy sequencing, scalar user get/set
wrappers, user string length/copy wrappers, and
x86 recursive page-table destroy/free-callback orchestration below the root,
and x86 virtual-to-physical page-table walk orchestration, plus the x86
five-slice memory batch for page clearing, `lookup_pte()`, change-attribute
range mutation, pagemap shaping, and fault-to-attribute dirty-bit shaping, plus
the latest Process/VM five-slice range-management batch for split publication,
join erase/cache/free sequencing, free-range plan/finalize sequencing, and
straight-removal conversion, plus the latest Process/VM release/cleanup batch
for memory-range drain traversal, mckfd close/drain list sequencing, VM
NUMA-policy drain/free sequencing, release-time address-space detach/process
release sequencing, allocated process/VM/resource/idle object zeroing gate routing,
spinlock initialization gate routing, thread allocation initialization gate routing, thread sigstack disable gate routing,
sigcommon/thread-sigpending initialization gate routing,
create-thread CPU-set selection/default publication gate routing,
create/chain side-effect gate routing,
create-thread/fork cleanup gate routing,
clone/fork error-unwind cleanup gate routing,
destroy-thread optional cleanup sequencing, and
destroy-thread CPU-set/sigpending/page-free cleanup gate routing, plus the
latest page-allocator runtime batch for
pa-ops/early-allocation dispatch, default allocation wrapper shaping, pa-ops
free dispatch, pending-free list mutation, and rbtree free-in-allocator
memory-chunk/free/rusage sequencing, plus the latest page-allocator policy
batch for explicit-node allocation, MPOL_BIND/MPOL_PREFERRED mask-distance
selection, MPOL_INTERLEAVE retry/update behavior, distance fallback attempts,
and order fallback attempts in `mckernel_allocate_aligned_pages_node()`, plus
the latest page-allocator query-free interrupt batch for NUMA-node iteration,
per-node free-page callback dispatch, total accumulation/logging, optional
panic/memdebug/memcheck dispatch, page-hash count/log dispatch, and optional
ATTACHED_MIC sbox scratch publication, plus the latest Process/VM fault/PTE
lookup batch for fault retry, PTE-present classification, recovered-PTE
logging, lookup-node fault dispatch, `-ENOENT` shaping, and physical-to-node
return shaping, plus
x86 clear-range TLB flush-address queue mutation, old-PTE
memobj-flush/free/unmap/RSS side-effect sequencing, and full-span child
page-table teardown/free sequencing, plus x86 set-range conflict cleanup,
allocation-failure cleanup, map/store/RSS accounting, large-page success
logging, and child-walk failure logging, plus x86 set-range leaf/level body
orchestration for child page-table allocation, atomic publication retry,
existing child-table translation, child-walk dispatch, large-page map routing,
failure shaping, and leftover table free sequencing, plus x86 clear/set range
top-level orchestration for validation, TLB address-array allocation/free,
argument publication, top-level walk dispatch, final TLB flush dispatch, and
set-range walk-failure shaping, plus x86 clear-range leaf/level/root body
sequencing for PTE clear-exchange, TLB address queueing, old-entry effect
orchestration, split-error handling, child-walk dispatch/error propagation,
child-table teardown, and root dispatch, plus Process/VM lifecycle release
sequencing for sigcommon release, TID release/replace, release_thread, and
release_process_vm, plus the latest syscall rusage/CPU-time batch for
`getrusage()` SELF/CHILDREN/THREAD aggregation/copyout sequencing and
`clock_gettime()` local/forward/thread/process CPU-time sequencing, plus the
latest scheduler syscall batch for `sched_setparam()`, `sched_getparam()`,
`sched_setscheduler()`, `sched_getscheduler()`, and
`sched_rr_get_interval()` validation, target lookup, Linux permission-check
request shaping, user-copy callback sequencing, RR interval result shaping,
and return/error sequencing, plus the latest scheduler migration-completion
batch for migration queue traversal, request detach/ack gates, affinity target
selection, runqueue detach/add with length mutation, thread CPU update,
same-VM sibling scan, address-space CPU-set update, target reschedule flag
publication, interrupt/vector callback dispatch, waitqueue wake callback
dispatch, and lock ordering, plus the previous scheduler migration-request batch
for request publication, waitqueue init/prepare/finish callback sequencing,
target runqueue flag/status mutation, remote interrupt vector/callback
selection, log callback dispatch, and scheduler callback handoff, plus the
latest scheduler affinity batch for
`sched_setaffinity()` and `sched_getaffinity()` validation, target selection,
permission gates, cpuset copyin/copyout, affinity mask publication, migration
request dispatch, and return/error sequencing, plus the latest scheduler/timer
runtime batch for `init_timers()`, `schedule_timeout()`,
`wake_timers_loop()`, and `set_timer()` sequencing, plus the latest
allocator lifecycle/mem_init batch for `ihk_mc_set_page_allocator()`
tracking-init/early-invalidate/`pa_ops` publication and top-level
`mem_init()` monitor/rusage/NUMA/page-fault/interrupt/page-hash/vmap/
demand-paging/NUMA-distance sequencing, plus the allocator NUMA-distance batch
for per-node distance allocation, matrix fill, distance/id sorting, node
publication, allocation-failure logging, and ordered log callback sequencing,
plus the allocator NUMA topology/allocation batch for allocation-attempt
OOM/bounds/callback dispatch, distance-id lookup, and distance-ordered node
selection, plus the dump user-page traversal/dispatch batch for
process-hash traversal, VM/address-space/page-table pointer loading, null skip
gates, visitor argument shaping, page-table visitor callback dispatch, and
dispatch-count/error shaping, plus the prior dump bitmap/completion batch for
dump page-set completion publication, physical-address membership checks,
variable-length dump bitmap range clearing, user-PTE dump bitmap marking,
free-chunk dump bitmap marking, and top-level last-CPU dump query sequencing.
Current total distance to 100: 40 aggregate functional points across the
non-arm64 dashboard rows, or 0 points across the McKernel-owned core rows
excluding build/host/user/Rocky/arm64.
Standalone Rust target: tracked in `migration.txt` as a stricter end-state than
strict core ownership. Current standalone readiness is about 40%; it reaches
100 only when the default x86_64/Rocky kernel and required control-plane path no
longer depend on McKernel-owned C implementation or fallback bodies below the
external/user-code boundary.
QEMU boot-path preflight: `qemu-system-x86_64` is available through the Rocky
`qemu-kvm` binary, libvirt/image/debug tooling is present, the system libvirt
connection responds, and host `/dev/kvm` is present when commands run outside
the default sandbox. The default sandbox does not expose `/dev/kvm`, and a
non-system `virsh` connection failed there because `/run/user/1000/libvirt` is
read-only. The repository has `scripts/qemu-mckernel-guest.sh` as a disposable
guest boundary for an existing Rocky/RHEL-family qcow2 image, but no real QEMU
McKernel boot has been proven yet. QEMU boot validation should use
`--accel auto` or `--accel tcg` inside the sandbox, and can use KVM only when
the command is run in a context where `/dev/kvm` is exposed; a 2026-05-25
non-boot probe showed unprivileged sandboxed QEMU still fails KVM init, while
an escalated/root QEMU process can initialize KVM and then be killed by timeout
without booting a guest. The latest 2026-05-26 pass confirmed
`qemu-system-x86_64 --version`, observed `/dev/kvm` in the current environment,
validated `scripts/qemu-mckernel-guest.sh --help`, and validated
`scripts/qemu-mckernel-guest.sh --image
/tmp/mckernel-qemu-dryrun.Vz7ukE/base.qcow2 --accel auto --dry-run --log-dir
/tmp/mckernel-qemu-dryrun.Vz7ukE/log`; local qcow2 discovery only found
disposable dry-run images, so no real guest boot ran. The kmalloc-body pass
also validated a dry-run using `/tmp/mckernel-qemu-kmalloc.xEKd3q/base.qcow2`
and ran a bounded TCG boot-process probe against that disposable blank image;
it reached SeaBIOS/iPXE and timed out cleanly without booting McKernel or
touching host reboot paths.
The kmalloc-tracking pass repeated those safe checks with
`/tmp/mckernel-qemu-track.mX1nMd/base.qcow2`; a raw bounded probe exited 124
because `timeout` terminated the blank-image boot as expected, and the wrapped
rerun treated that timeout as a passing QEMU boot-boundary probe.
The pagealloc-tracking pass repeated the safe dry-run and bounded TCG probe
with `/tmp/mckernel-qemu-pagealloc.O8Ur9D/base.qcow2`; the blank image reached
firmware/iPXE and timed out cleanly under the wrapper without booting
McKernel or touching host reboot paths.
The allocator lifecycle/mem_init pass repeated the safe dry-run and bounded
TCG probe with `/tmp/mckernel-qemu-lifecycle2.tl97yy/base.qcow2`; the blank
image again reached firmware/iPXE and timed out cleanly under the wrapper
without booting McKernel or touching host reboot paths.
The allocator NUMA-distance pass repeated the safe dry-run and bounded TCG
probe with `/tmp/mckernel-qemu-numadist.AnnC0b/base.qcow2`; the blank image
again reached firmware/iPXE and timed out cleanly under the wrapper without
booting McKernel or touching host reboot paths.
The allocator NUMA topology/allocation pass repeated the safe dry-run and
bounded TCG probe with `/tmp/mckernel-qemu-numatopo.4QuyLW/base.qcow2`; the
blank image again reached firmware/iPXE and timed out cleanly under the wrapper
without booting McKernel or touching host reboot paths.
The dump bitmap/completion pass repeated the safe dry-run and bounded TCG
probe with `/tmp/mckernel-qemu-dumpmap.CnQDUA/base.qcow2`; the blank image
again reached firmware/iPXE and timed out cleanly under the wrapper without
booting McKernel or touching host reboot paths.
The dump user-page traversal/dispatch pass repeated the safe dry-run and
bounded TCG probe with `/tmp/mckernel-qemu-userdump.qUVhow/base.qcow2`; the
blank image again reached firmware/iPXE and timed out cleanly under the
wrapper without booting McKernel or touching host reboot paths.
The scheduler migration-request pass repeated the safe dry-run and bounded TCG
probe with `/tmp/mckernel-qemu-schedmig.qiFJJ6/base.qcow2`; the blank image
again timed out cleanly under the wrapper without booting McKernel or touching
host reboot paths.
The scheduler migration-completion pass repeated the safe dry-run and bounded
TCG probe with `/tmp/mckernel-qemu-domigrate.ZtJIgE/base.qcow2`; the blank
image again timed out cleanly under the wrapper without booting McKernel or
touching host reboot paths.
The `do_wait()` syscall pass repeated the safe dry-run and bounded TCG probe
with `/tmp/mckernel-qemu-dowait.tQCG5z/base.qcow2`; sandboxed QEMU could not
bind the host-forward port, the escalated retry started QEMU, and the blank
image timed out waiting for SSH as expected without booting McKernel or
touching host reboot paths.
The wait-candidate syscall pass repeated the safe dry-run and bounded TCG
probe with `/tmp/mckernel-qemu-waitcand.yNsShP/base.qcow2`; sandboxed QEMU
could not bind the host-forward port, the escalated retry started QEMU, and
the blank image timed out waiting for SSH as expected without booting McKernel
or touching host reboot paths.
The wait-zombie/reparent syscall pass repeated the safe dry-run and bounded
TCG probe with `/tmp/mckernel-qemu-waitzombie.vDIyU2/base.qcow2`; the blank
image timed out waiting for SSH as expected without booting McKernel or
touching host reboot paths.
The wait-scan traversal syscall pass repeated the safe dry-run and bounded
TCG probe with `/tmp/mckernel-qemu-waitscan.ogpUBC/base.qcow2`; the blank
image timed out waiting for SSH as expected without booting McKernel or
touching host reboot paths, and no `qemu-system-x86_64` process remained.
The ptrace-detach body syscall pass repeated the safe dry-run and bounded TCG
probe with `/tmp/mckernel-qemu-ptracedetach.kFpjUx/base.qcow2`; the blank
image timed out waiting for SSH as expected without booting McKernel or
touching host reboot paths, and no `qemu-system-x86_64` process remained.
The ptrace request-dispatch syscall pass repeated the safe dry-run and bounded
TCG probe with `/tmp/mckernel-qemu-ptracesyscall.aLKK34/base.qcow2`; the blank
image timed out waiting for SSH as expected without booting McKernel or
touching host reboot paths, and no `qemu-system-x86_64` process remained.

Update 2026-05-26 Page allocator low-level dispatch strict-core batch
------------------------------------------------------------

Latest strict-core-only Page allocator batch moved low-level allocator
registration and dispatch state into Rust:

- Rust-owned `pa_ops` storage for the normal Rust-enabled path.
- Tracking-init callback ordering in `ihk_mc_set_page_allocator()`.
- Early-allocator invalidation callback ordering.
- Allocator ops publication after the init/invalidate callbacks.
- Aligned allocation pa-ops versus early-allocation selection.
- Missing allocation-callback/null result handling.
- Default page-allocation alignment, node, and virtual-address shaping.
- Free-side pa-ops/null/free-callback selection.
- Exported `___ihk_mc_alloc_aligned_pages_node()`,
  `___ihk_mc_alloc_pages()`, and `___ihk_mc_free_pages()` Rust body
  publication.
- Focused Rust fixture coverage for the direct exported dispatch path.

The Page allocator strict ownership row in `migration.txt` moves 99% -> 100%,
and post-+50 strict-core movement is now about +398 points. The broad
dashboard row for Page allocator is already capped at 100%, so this adds 0
broad dashboard points.

Standalone readiness remains about 40% because C fallback scaffolding, pa-ops
implementation callback bodies, actual allocation/free primitive side effects
below the published ops callbacks, MCS/interrupt primitive bodies,
current-node primitive lookup, IKC channel/send side effects, logging/panic
primitive bodies, deferred-zero worker timing, FUGAKU debug preempt behavior,
and broader allocator lifetime remain C-owned.

Update 2026-05-26 host IKC public-handler strict-core batch
------------------------------------------------------------

Previous strict-core-only host/IKC batch moved public handler and wrapper
sequencing into Rust:

- Syscall packet handler raw packet casting.
- Response-channel callback lookup.
- Current-thread callback lookup.
- Host SCD dispatch constant shaping.
- Dispatch call/return sequencing.
- Dummy packet-handler release-callback sequencing.
- Public IKC2Linux init result-call sequencing.
- Public IKC2Linux panic-on-error wrapper behavior.
- Public IKC2McKernel init result-call sequencing.
- Public IKC2McKernel panic-on-error wrapper behavior.

The host/IKC/mcctrl/IHK strict ownership row in `migration.txt` moves
99% -> 100%, and post-+50 strict-core movement is now about +397 points. The
broad host dashboard row is already capped at 100%, so this adds 0 broad
dashboard points.

Standalone readiness remains about 40% because C fallback scaffolding, device
allocation, memory registration, file I/O, primitive callback side effects,
allocation/connect/delay/current-channel/regular-channel primitive bodies, raw
IKC send side effects, broad IKC exchange mutation, broad allocation/lifetime,
and kernel object lifecycle still block a default standalone Rust OS path.

Update 2026-05-26 pagealloc public-wrapper strict-core batch
------------------------------------------------------------

Previous strict-core-only Page allocator batch moved the public bitmap
`ihk_pagealloc_*()` wrapper layer into Rust:

- Exported `__ihk_pagealloc_init()` status and allocation-failure log routing.
- `ihk_pagealloc_init()` default-initializer sequencing through the Rust init
  wrapper.
- Exported `ihk_pagealloc_destroy()` free-callback dispatch through the Rust
  destroy body.
- Exported `ihk_pagealloc_alloc()` lock-node and allocation dispatch.
- Exported `ihk_pagealloc_reserve()` lock-node and reserve dispatch.
- Exported `ihk_pagealloc_free()` bad-address capture and double-free error
  callback routing.
- Exported `ihk_pagealloc_count()` lock/count dispatch.
- Exported `ihk_pagealloc_query_free()` lock/query dispatch.
- Exported `__ihk_pagealloc_zero_free_pages()` begin/locked-zero/done
  sequencing.
- Focused public-wrapper equivalence coverage for success, invalid,
  init-fail, lock-callback, and zero-log paths.

The allocator strict ownership row in `migration.txt` moves 98% -> 99%, and
post-+50 strict-core movement is now about +396 points. The broad dashboard
row for Page allocator is already capped at 100%, so this adds 0 broad
dashboard points.

Standalone readiness remains about 40% because C fallback scaffolding, pa-ops
primitive behavior, low-level page allocation/free primitive bodies,
MCS/interrupt primitive bodies, current-node primitive lookup, IKC
channel/send side effects, logging/panic primitive bodies, deferred-zero worker
timing, FUGAKU debug preempt behavior, and broader allocator lifetime remain
C-owned.

Update 2026-05-26 NUMA allocator-lifecycle strict-core batch
------------------------------------------------------------

Previous strict-core-only Page allocator batch moved `numa_init()` lifecycle
sequencing into Rust:

- NUMA-node discovery and get-node error/panic callback ordering.
- Node id, Linux NUMA id, and type publication.
- Allocator-list initialization and node-distance reset publication.
- Rbtree-node primitive initialization callback ordering.
- Memory-chunk iteration and invalid-node skip behavior.
- Last-early-heap physical start adjustment.
- Rbtree add-free callback dispatch.
- Legacy page-allocator init, list publication, and count sequencing.
- Physical-memory and final-node log callback sequencing.
- Total-memory rusage accounting.

The allocator strict ownership row in `migration.txt` moved 97% -> 98%, and
post-+50 strict-core movement was about +395 points. The broad dashboard
row for Page allocator is already capped at 100%, so this adds 0 broad
dashboard points.

Standalone readiness remains about 40% because C fallback scaffolding, pa-ops
primitive behavior, low-level page allocation/free primitive bodies,
MCS/interrupt primitive bodies, current-node primitive lookup, IKC
channel/send side effects, logging/panic primitive bodies, deferred-zero worker
timing, FUGAKU debug preempt behavior, and broader allocator lifetime remain
C-owned.

Previous 2026-05-26 memory-policy syscall strict-core batch
------------------------------------------------------------

Latest strict-core-only Syscall core batch moved `set_mempolicy()` and
`mbind()` body sequencing into Rust. Rust now owns the connected
memory-policy mutation slices there:

- `set_mempolicy()` nodemask validation/copyin and clamp-warning dispatch.
- Mode normalization, unsupported-mode rejection, and default empty-mask checks.
- Process NUMA-mask reset, requested-mask intersection, policy publication, and
  interleave-state publication.
- `mbind()` straight-map/FUGAKU gates, address-range validation, mode/flag
  validation, strict-mode rejection, and nodemask copyin.
- Memory-range write-lock/lookup/unlock ordering.
- Range-policy search, clear-range, allocation, rb-clear, insert, mask update,
  policy publication, and interleave-state publication.

The Syscall core strict ownership moves 98% -> 100% for the scoped sequencing
row in `migration.txt`, and post-+50 strict-core movement is now about +394
points. The broad dashboard row for Syscall core is already capped at 100%, so
this adds 0 broad dashboard points.

Standalone readiness remains about 40% because C fallback scaffolding, the
actual user-copy primitive, memory-range lock/lookup primitives, lookup-node
page-table/page-fault primitives, range-policy tree/allocation primitives,
top-level syscall dispatch, Linux forwarding, allocation/free,
signal-delivery primitives, ptrace/process-memory primitives,
waitqueue/schedule/signal behavior, and broader primitive side effects remain
C-owned.

Previous 2026-05-26 fault/PTE lookup strict-core batch
------------------------------------------------------------

Latest strict-core-only Process/VM and x86-memory boundary batch moved
`ihk_mc_pt_lookup_fault_pte()` and `lookup_node()` body sequencing into Rust.
Rust now owns ten connected slices there:

- Initial page-table lookup dispatch for `ihk_mc_pt_lookup_fault_pte()`.
- Missing or inactive PTE classification.
- `PF_POPULATE | PF_USER` page-fault callback dispatch.
- Retry PTE lookup after the fault callback.
- Recovered-present-PTE log callback dispatch.
- `lookup_node()` page-fault callback dispatch and error return shaping.
- Process-VM address-space and page-table extraction by verified offsets.
- Post-fault PTE lookup with null base/size/alignment outputs.
- Missing or inactive PTE `-ENOENT` return shaping.
- PTE physical-address extraction and physical-to-NUMA-node return shaping.

The Process/VM strict ownership moved 99% -> 100% for the scoped sequencing
row, and post-+50 strict-core movement was about +391 points. The broad
dashboard row for Process/VM is already capped at 100%, so this adds 0 broad
dashboard points.

Standalone readiness remains about 40% because C fallback scaffolding, raw
page-fault behavior, actual page-table lookup/allocation/mapping side effects,
VM range allocation/free callbacks, split/XPMEM primitive callbacks, memobj
refcount primitives, lock/free primitive bodies, signal-delivery primitives,
child-list/lifetime traversal, broad rusage traversal, and broader process
lifetime remain C-owned.

Validation: rustfmt `kernel/rust/mem_helpers.rs`; bash -n
`kernel/rust/tests/run_equivalence.sh`; git diff --check for `kernel/mem.c`,
`kernel/rust/mem_helpers.rs`, and `kernel/rust/tests/run_equivalence.sh`;
`kernel/rust/tests/run_equivalence.sh` (`mem_init_helpers` digest
c687f899a576d46a and unresolved/SIMD checks ok); Rust configured build
`cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
ihk-smp-x86_64_ko mcctrl_ko -j2` (mckernel.img SHA-1
4a988e2fdb862c3a30d56a9f12c008e80295c319); C fallback build
`cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
(mckernel.img SHA-1 49fa7fff3f82c5384135243b43781e00ce15d1e3);
`scripts/rust-ownership-report.py --check-dashboard overview.txt
--check-dashboard overview2.txt --top 10` (mechanical LOC audit 27.9%).
QEMU-safe checks: `/dev/kvm` present, qemu harness help passed, dry-run passed
against `/tmp/mckernel-qemu-dowait.tQCG5z/base.qcow2`, bounded TCG probe
timed out waiting for SSH from the disposable blank qcow2 as expected, and
`pgrep` found no qemu-system/qemu-kvm process left over. No host reboot or
reboot-capable script was run.

Update 2026-05-26 allocator query-free interrupt strict-core batch
------------------------------------------------------------

Previous strict-core-only allocator query-free batch moved
`query_free_mem_interrupt_handler()` execution-body sequencing into Rust. Rust
now owns ten connected slices there:

- NUMA-node iteration and negative-node-count rejection.
- Per-node free-page callback dispatch.
- Total free-page accumulation.
- Total free-page logging callback dispatch.
- Optional FUGAKU panic dispatch.
- `memdebug` command lookup.
- `kmalloc_memcheck()` callback dispatch.
- `pagealloc_memcheck()` callback dispatch.
- Page-hash count and log callback dispatch.
- Optional ATTACHED_MIC sbox scratch publication.

The Page allocator strict ownership moved 96% -> 97%, and post-+50 strict-core
movement was about +390 points. The broad Page allocator dashboard row is
already capped at 100%, so this adds 0 broad dashboard points.

Standalone readiness remains about 40% because C fallback scaffolding,
per-allocator query primitive behavior, pa-ops primitive behavior, low-level
page allocation/free primitive bodies, MCS/interrupt primitive bodies, IKC
channel/send side effects, logging/panic primitives, deferred-zero worker
timing, FUGAKU debug preempt behavior, and broader allocator lifetime remain
C-owned.

Validation: rustfmt --check `kernel/rust/mem_helpers.rs`; bash -n
`kernel/rust/tests/run_equivalence.sh` `scripts/qemu-mckernel-guest.sh`
`scripts/rocky-rust-validation.sh`; git diff --check for `kernel/mem.c`,
`kernel/rust/mem_helpers.rs`, and `kernel/rust/tests/run_equivalence.sh`;
`kernel/rust/tests/run_equivalence.sh` (`mem_init_helpers` digest
54fd8dc7def03950 and unresolved/SIMD checks ok); Rust configured build
`cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
ihk-smp-x86_64_ko mcctrl_ko -j2` (mckernel.img SHA-1
88f9244dddaeccddfcf0f4a9811a4594d4fd7a2b); C fallback build
`cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
(mckernel.img SHA-1 23076f9721ac7b1408179c210db06d13685268c2);
`scripts/rust-ownership-report.py --check-dashboard overview.txt
--check-dashboard overview2.txt --top 10` (mechanical LOC audit 27.9%).
QEMU-safe checks: `/dev/kvm` present, qemu harness help passed, dry-run passed
against `/tmp/mckernel-qemu-dowait.tQCG5z/base.qcow2`, bounded TCG probe
timed out waiting for SSH from the disposable blank qcow2 as expected, and
`pgrep` found no qemu-system/qemu-kvm process left over. No host reboot or
reboot-capable script was run.

Update 2026-05-26 allocator policy/free-wrapper strict-core batch
------------------------------------------------------------

Previous strict-core-only allocator wrapper batch moved the allocation-policy
selection wrapper and the public free handoff into Rust. Rust now owns ten
connected slices there:

- `mckernel_allocate_aligned_pages_node()` invalid-size rejection.
- Not-initialized/idle-process default policy fallback.
- Current-VM lookup gating.
- User-address range-policy search.
- Process memory-range lookup.
- Shared-memory gate selection.
- Range-policy field selection.
- Process-policy fallback selection.
- Current-node and NUMA-node-count publication into the allocation-policy body.
- `mckernel_free_pages()` virt-to-phys/phys-to-page, pending-free enqueue, and
  allocator-free fallback dispatch.

The Page allocator strict ownership moved 95% -> 96%, and post-+50 strict-core
movement was about +389 points. The broad Page allocator dashboard row is
already capped at 100%, so this adds 0 broad dashboard points.

Standalone readiness remains about 40% because C fallback scaffolding, pa-ops
primitive behavior, low-level page allocation/free primitive bodies,
MCS/interrupt primitive bodies, current-node lookup, IKC send/channel behavior,
logging/panic primitives, deferred-zero worker timing, FUGAKU debug preempt
behavior, and broader allocator lifetime remain C-owned.

Validation: rustfmt --check `kernel/rust/mem_helpers.rs`; bash -n
`kernel/rust/tests/run_equivalence.sh` `scripts/qemu-mckernel-guest.sh`
`scripts/rocky-rust-validation.sh`; git diff --check for `kernel/mem.c`,
`kernel/rust/mem_helpers.rs`, and `kernel/rust/tests/run_equivalence.sh`;
`kernel/rust/tests/run_equivalence.sh` (`mem_init_helpers` digest
ba01ad7151b581b9 and unresolved/SIMD checks ok); Rust configured build
`cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
ihk-smp-x86_64_ko mcctrl_ko -j2` (mckernel.img SHA-1
5fa4004d1adfeb4a1ff0755f08283a259992c3a5); C fallback build
`cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
(mckernel.img SHA-1 509fde59007323d3b95f88e2b6442cacae44f82e);
`scripts/rust-ownership-report.py --check-dashboard overview.txt
--check-dashboard overview2.txt --top 10` (mechanical LOC audit 27.9%).
QEMU-safe checks: `/dev/kvm` present, qemu harness help passed, dry-run passed
against `/tmp/mckernel-qemu-dowait.tQCG5z/base.qcow2`, bounded TCG probe
against the same blank 193K qcow2 exited waiting for SSH as expected, and
`pgrep` found no qemu-system/qemu-kvm process left over. No host reboot or
reboot-capable script was run.

Update 2026-05-26 init-stack body strict-core batch
------------------------------------------------------------

Latest strict-core-only process/host boundary batch moved the
`init_process_stack()` primary execution body into Rust. Rust now owns ten
connected slices there:

- Stack premap, max-stack, and VM map-bound size selection.
- AP-user allocation policy.
- Aligned stack allocation, zeroing, and allocation-failure logging.
- VM stack range creation and add-range error shaping.
- Page-table set-range callback dispatch and cleanup on failure.
- Auxv construction, including vDSO, random, HWCAP, IDs, PHDR, base, and entry
  fields.
- `saved_auxv` publication.
- Env/argv/argc stack layout and alignment checks.
- User stack pointer/context publication.
- VM stack-end/start bound publication.

host/IKC/mcctrl/IHK strict ownership moved 98% -> 99%, and post-+50
strict-core movement was about +388 points. The broad host dashboard row is
already capped at 100%, so this adds 0 broad dashboard points.

Standalone readiness remains about 40% because C fallback scaffolding,
primitive map/unmap/copy/user-page allocation, mapping/thread/range primitive
callbacks, raw IKC send side effects, device allocation, memory registration,
file I/O, waits/callbacks beyond bounded dispatchers, broad allocation/lifetime,
and kernel object lifecycle remain C-owned.

Validation: rustfmt --check `kernel/rust/process_helpers.rs`; bash -n
`kernel/rust/tests/run_equivalence.sh` `scripts/qemu-mckernel-guest.sh`
`scripts/rocky-rust-validation.sh`; git diff --check for the touched
process-helper files and trackers; `kernel/rust/tests/run_equivalence.sh`
(`process_helpers` digest e482fa401b9e582f, expected Rust undefined-symbol
list, and unresolved/SIMD checks ok); Rust configured build
`cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
ihk-smp-x86_64_ko mcctrl_ko -j2` (mckernel.img SHA-1
9f27302af504ef14ba2e5936155a46e053db3e7e); C fallback build
`cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
(mckernel.img SHA-1 8507d641be412f24a2720062f0b26d387b0e1796);
`scripts/rust-ownership-report.py --check-dashboard overview.txt
--check-dashboard overview2.txt --top 10` (mechanical LOC audit 27.8%).
QEMU-safe checks: `/dev/kvm` present, QEMU harness help passed, dry-run passed
against `/tmp/mckernel-qemu-dowait.tQCG5z/base.qcow2` with log dir
`/tmp/mckernel-qemu-initstack.dryrun`, bounded TCG probe exited waiting for SSH
from the 193K blank qcow2 as expected with log dir
`/tmp/mckernel-qemu-initstack.probe`, and no qemu-system/qemu-kvm process
remained. No host reboot or reboot-capable script was run.

Previous update 2026-05-26 host args/envs stack-prep strict-core batch
------------------------------------------------------------

Previous strict-core-only host batch moved the args/envs stack-prep half of
`prepare_process_ranges_args_envs()` into Rust. Rust now owns ten connected
slices there:

- Arg/env page-count and range placement.
- Argenv page allocation and physical publication.
- Argenv memory-range creation and error/free shaping.
- Remote args map/copy/unmap/TLB sequencing.
- Remote envs map/copy/unmap/TLB sequencing.
- Local args/envs length publication.
- Saved_cmdline free/alloc/copy publication.
- Argv/env pointer fixup into process VA.
- vDSO map/clear and rprocess/rpgtable publication.
- init_process_stack callback dispatch and error shaping.

host/IKC/mcctrl/IHK strict ownership moved 96% -> 98%, and post-+50
strict-core movement was about +387 points. The broad host dashboard row is
already capped at 100%, so this adds 0 broad dashboard points.

Standalone readiness remains about 40% because C fallback scaffolding,
primitive map/unmap/copy/user-page allocation and init-stack internals,
mapping/thread/range primitive callbacks, raw IKC send side effects, device
allocation, memory registration, file I/O, waits/callbacks beyond bounded
dispatchers, broad allocation/lifetime, and kernel object lifecycle remain
C-owned.

Validation: rustfmt --check `kernel/rust/host_helpers.rs`; bash -n
`kernel/rust/tests/run_equivalence.sh` `scripts/qemu-mckernel-guest.sh`
`scripts/rocky-rust-validation.sh`; git diff --check for the touched
host-helper files and trackers; `kernel/rust/tests/run_equivalence.sh`
(`object_helpers` digest ebc565196e710168, expected Rust undefined-symbol list,
and unresolved/SIMD checks ok); Rust configured build
`cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
ihk-smp-x86_64_ko mcctrl_ko -j2` (mckernel.img SHA-1
2ff25957e7e9670344b61ac2247a34d50cf24a7e); C fallback build
`cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
(mckernel.img SHA-1 9bb3f1955b0c0d771a3c2a293c61a7995cfad3b1);
`scripts/rust-ownership-report.py --check-dashboard overview.txt
--check-dashboard overview2.txt --top 10` (mechanical LOC audit 27.7%).
QEMU-safe checks: `/dev/kvm` present, QEMU harness help passed, dry-run passed
against `/tmp/mckernel-qemu-dowait.tQCG5z/base.qcow2` with log dir
`/tmp/mckernel-qemu-argsenv.dryrun`, bounded TCG probe exited waiting for SSH
from the 193K blank qcow2 as expected with log dir
`/tmp/mckernel-qemu-argsenv.probe`, and no qemu-system/qemu-kvm process
remained. No host reboot or reboot-capable script was run.

Previous update 2026-05-26 host prepare-ranges section strict-core batch
------------------------------------------------------------

Previous strict-core-only host batch moved the ELF-section/range/page-table
half of `prepare_process_ranges_args_envs()` into Rust: section loop
sequencing, interpreter base detection/alignment, relocatable aout VMA
adjustment, section page-span/range-page calculation, VR/maxprot/demand and
AP_USER policy, memory-range callback dispatch and error shaping, user-page
allocation callback dispatch and error shaping, virt-to-phys/page-attribute/
page-table set-range dispatch plus page-free failure cleanup, remote-PA and
text/data/map-end publication, and entry/auxv/context plus brk/map
publication/data-too-large return shaping.

host/IKC/mcctrl/IHK strict ownership moved 94% -> 96%, and post-+50
strict-core movement reached about +377 points.

Previous update 2026-05-26 host prepare-process strict-core batch
------------------------------------------------------------

Previous strict-core-only host batch moved `process_msg_prepare_process()` body
sequencing through Rust while preserving the C ABI wrapper,
mapping/thread/range primitive callbacks, scalar no-SIMD Rust code generation,
and C fallback scaffolding:

- monitor freeze/frozen gate evaluation across processors.
- program descriptor map-size/page-span calculation.
- host memory map, virtual map, and map-failure cleanup sequencing.
- descriptor magic validation and broken-desc cleanup/logging.
- section-count validation, including legacy invalid-section no-unmap
  behavior.
- descriptor clone allocation/copy and alloc-failure cleanup.
- main-thread creation and thread-create failure cleanup.
- process PID/PGID/credential/rlimit/profile/VM metadata publication.
- NUMA bind/nodemask policy publication and invalid-node error behavior.
- prepare-ranges callback dispatch, success cleanup/unmap/TLB flush, and
  failure destroy/free/unmap return shaping.
- Equivalence coverage for freeze, bad magic, invalid section count, success
  bind-mask path, alloc failure, create failure, prepare-ranges failure,
  nodemask policy, invalid NUMA node, and no-SIMD/unresolved-symbol checks.

host/IKC/mcctrl/IHK strict ownership moves 91% -> 94%, and post-+50
strict-core movement is now about +367 points. The broad host dashboard row is
already capped at 100%, so this does not add broad dashboard percentage.
Standalone readiness remains about 40% because C fallback scaffolding,
mapping/thread/range primitive callbacks, raw IKC send side effects, device
allocation, memory registration, file I/O, waits/callbacks beyond bounded
dispatchers, broad allocation/lifetime, and kernel object lifecycle remain
C-owned.

Update 2026-05-26 Process/VM free-range orchestration strict-core batch
------------------------------------------------------------

Latest strict-core-only Process/VM batch moved `free_process_memory_range()`
body sequencing through Rust while preserving the C ABI wrapper, primitive
lock/page-table/memobj/TOFU callbacks, scalar no-SIMD Rust code generation,
and C fallback scaffolding:

- previous/next range discovery for free-bound planning.
- page-table free/clear action planning and plan-error logging.
- page-table lock/unlock callback ordering.
- memobj ref/unref ordering around free-range side effects.
- page-table free callback dispatch and failure log selection.
- page-table clear callback dispatch and failure log selection.
- post-free memobj unref ordering.
- TOFU removal callback dispatch and removed-entry logging.
- final range detach/straight cleanup callback dispatch.
- final error/done log selection and return shaping.
- Equivalence coverage for PT-free with memobj refs, PT-clear failure
  continuation, TOFU removal logging, finalize failure, callback count/order
  checks, and no-SIMD/unresolved-symbol checks.

Process/VM strict ownership moves 97% -> 99%, and post-+50 strict-core
movement is now about +355 points. The broad Process/VM dashboard row is
already capped at 100%, so this does not add broad dashboard percentage.
Standalone readiness remains about 40% because C fallback scaffolding, VM
range allocation callbacks, split/XPMEM primitive callbacks, memobj refcount
primitives, TOFU hooks, actual page-table lookup/allocation/mapping/free/clear
side effects, page-fault range-resolution primitive behavior, broad process
lifetime, rusage traversal, signal-delivery primitives, atomic refcount
primitive behavior, lock/free primitive bodies, and wait primitive behavior
remain C-owned.

Update 2026-05-26 Process/VM remove-range orchestration strict-core batch
------------------------------------------------------------

Previous strict-core-only Process/VM batch moved remove-process-memory-range
body sequencing through Rust while preserving the C ABI wrapper, primitive
split/free/XPMEM callbacks, scalar no-SIMD Rust code generation, and C
fallback scaffolding:

- straight-map conversion and missing-straight early return/logging.
- converted address publication before range lookup.
- range lookup and next-range iteration.
- split-start callback sequencing and split-failure return shaping.
- split-end callback sequencing and split-failure return shaping.
- read-only freed result publication.
- XPMEM removal callback dispatch.
- free callback dispatch against the replacement range.
- split/free error log selection.
- final done-log and return shaping.
- Equivalence coverage for normal remove, straight-map conversion,
  missing-straight early return, split failure, callback count/order checks,
  and no-SIMD/unresolved-symbol checks.

Process/VM strict ownership moved 95% -> 97%, and post-+50 strict-core
movement was about +345 points. The broad Process/VM dashboard row is
already capped at 100%, so this does not add broad dashboard percentage.
Standalone readiness remains about 40% because C fallback scaffolding, VM
range allocation/free callbacks, split/free/XPMEM primitive callbacks, memobj
refcount primitives, TOFU hooks, actual page-table
lookup/allocation/mapping/free/clear side effects, page-fault range-resolution
primitive behavior, broad process lifetime, rusage traversal,
signal-delivery primitives, remaining non-process atomic refcount primitive behavior, lock/free
primitive bodies, and wait primitive behavior remain C-owned.

Update 2026-05-26 Process/VM page-fault/populate strict-core batch
------------------------------------------------------------

Previous strict-core-only Process/VM batch moved do-page-fault VM outer-body,
page-fault retry/page-I/O, and populate-loop sequencing through Rust while
preserving the C ABI wrappers, primitive page-table/fault callbacks, primitive
locks, scalar no-SIMD Rust code generation, and C fallback scaffolding:

- do-page-fault stack-grow range selection and range-start commit.
- write/read memory-range lock decisions, including already-held lock
  handling.
- exiting, missing-range, and permission failure result shaping.
- zero-object `PF_POPULATE` promotion.
- normal-vs-XPMEM range-fault dispatch and unlock/return sequencing.
- `page_fault_process_vm()` `-ERESTART` retry sequencing around preempt
  enable/disable, page-I/O callback dispatch, and `pgio_fp` clearing.
- `populate_process_memory()` preempt, per-page fault, first-error warning,
  and return sequencing.
- Equivalence coverage for stack-grow, skip-lock, zero-object populate, XPMEM
  dispatch, write-denied, exiting, retry/page-I/O, populate success/failure,
  callback count/order checks, and no-SIMD/unresolved-symbol checks.

Process/VM strict ownership moved 93% -> 95%, and post-+50 strict-core
movement was about +335 points. The broad Process/VM dashboard row is
already capped at 100%, so this does not add broad dashboard percentage.
Standalone readiness remains about 40% because C fallback scaffolding, VM
range allocation/free callbacks, memobj refcount primitives, TOFU hooks,
actual page-table lookup/allocation/mapping/free/clear side effects,
page-fault range-resolution primitive behavior, broad process lifetime,
rusage traversal, signal-delivery primitives, atomic refcount primitive
behavior, lock/free primitive bodies, and wait primitive behavior remain
C-owned.

Update 2026-05-26 mckfd syscall dispatch strict-core batch
------------------------------------------------------------

Previous strict-core-only syscall/file-object batch moved live mckfd syscall
dispatch through Rust while preserving the C ABI wrappers, primitive lock,
TOFU cleanup, free, Linux-forward callbacks, scalar no-SIMD Rust code
generation, and C fallback scaffolding:

- `read()`, `ioctl()`, and `fcntl()` locked mckfd traversal, callback presence
  checks, callback dispatch, and Linux-forward fallback sequencing.
- `ioctl()` optional TOFU gate sequencing before mckfd callback/fallback
  dispatch.
- `close()` optional TOFU cleanup gate, locked mckfd traversal, head/middle
  unlink mutation, callback dispatch, free-callback dispatch, and Linux-forward
  fallback sequencing.
- Equivalence coverage for mckfd hit, miss, null-callback fallback, TOFU
  handled/not-handled branches, close unlink/free ordering, and no-SIMD/
  unresolved-symbol checks.

The Syscall core strict ownership row moves 96% -> 97% in `migration.txt`, the
file-object strict row moves 91% -> 92%, and post-+50
strict-core movement is now about +249 points. The broad dashboard rows are
already capped at 100%, so this does not add broad dashboard percentage.
Standalone readiness remains about 40% because C fallback scaffolding,
primitive locks, Linux forwarding, XPMEM open/openat creation, raw allocation/
free primitives, user-copy, syscall dispatch, and broader high-risk
file/object/syscall bodies remain C-owned.

Previous update 2026-05-26 ten-slice syscall wrapper strict-core batch
------------------------------------------------------------

Previous strict-core-only syscall batch moved ten wrapper bodies through Rust
while preserving the C ABI wrapper, primitive callbacks, scalar no-SIMD Rust
code generation, and C fallback scaffolding:

- `times()` CPU-time accounting, child CPU-time aggregation, copyout, and
  current-time return shaping.
- `setpgid()` pid/pgid normalization, target process lookup, execed rejection,
  Linux-forward callback dispatch, and post-success pgid mutation.
- `setrlimit()`, `getrlimit()`, and `prlimit64()` argument routing into the
  existing prlimit implementation callback.
- `sysinfo()` zero/fill/copyout sequencing, `get_cpu_id()` CPU callback return
  shaping, `mlockall()` policy/log/result sequencing, `munlockall()` log/return
  sequencing, and `getcpu()` CPU/node copyout sequencing.
- Equivalence coverage for success paths, copyout failures, missing callback
  gates, process lookup failures, execed rejection, prlimit argument routing,
  mlockall policy branches, and getcpu partial-copy behavior.

The Syscall core strict ownership row moves 95% -> 96% in `migration.txt`, and
post-+50 strict-core movement is now about +239 points. The broad Syscall core
dashboard row is already capped at 100%, so this does not add broad dashboard
percentage. Standalone readiness remains about 40% because primitive user-copy,
Linux forwarding, process lookup/locking, time/CPU primitive callbacks,
syscall dispatch, default-path C fallback scaffolding, and broader high-risk
syscall bodies remain C-owned.

Previous update 2026-05-26 signal/credential syscall wrapper strict-core batch
------------------------------------------------------------

Previous strict-core-only syscall batch moved signal and credential wrapper-body
sequencing through Rust while preserving the C ABI wrapper, primitive
callbacks, and C fallback scaffolding:

- `kill()`, `tgkill()`, and `tkill()` target validation, siginfo construction,
  do-kill callback dispatch, return shaping, and missing-callback error
  shaping.
- `setresuid()`, `setreuid()`, `setuid()`, `setresgid()`, `setregid()`, and
  `setgid()` Linux-forward callback dispatch plus credential-refresh callback
  sequencing.
- `setfsuid()` and `setfsgid()` do-syscall callback dispatch,
  credential-refresh callback sequencing, and return shaping.
- Equivalence coverage for success, validation failure, negative forwarding
  returns, missing refresh callback, and uid/gid setfsid dispatch cases.

The Syscall core strict ownership row moves 94% -> 95% in `migration.txt`, and
post-+50 strict-core movement is now about +229 points. The broad Syscall core
dashboard row is already capped at 100%, so this does not add broad dashboard
percentage. Standalone readiness remains about 40% because Linux forwarding,
credential refresh source data, actual signal delivery, user-copy, locks,
allocation/free, syscall dispatch, default-path primitive bodies, and fallback
scaffolding remain C-owned.

Previous update 2026-05-26 ptrace syscall request-dispatch strict-core batch
------------------------------------------------------------

The previous strict-core-only syscall batch moved top-level `ptrace()`
request/callback body dispatch through Rust while preserving the C ABI wrapper,
primitive handler callbacks, and C fallback scaffolding:

- `PTRACE_TRACEME`, kill/continue/singlestep/syscall wake requests, and
  register/fpreg/regset get/set request routing.
- User/text peek/poke request routing, including PEEKDATA/POKEDATA aliases.
- Set-options, attach/detach, siginfo/eventmsg, arch fallback, and
  unsupported/missing-callback error shaping.
- Equivalence coverage for every dispatch case, alias routing, null ops, and
  missing getregs/arch callbacks.

The Syscall core strict ownership row moved 93% -> 94% in `migration.txt`, and
post-+50 strict-core movement reached about +219 points. The broad Syscall core
dashboard row was already capped at 100%, so this did not add broad dashboard
percentage. Standalone readiness remains about 40% because thread lookup,
user-copy, register/fpreg/regset primitives, process-memory access, signal
delivery, allocation/free, default-path primitive bodies, and fallback
scaffolding remained C-owned.

Previous update 2026-05-26 ptrace-detach body strict-core batch
------------------------------------------------------------

Latest strict-core-only syscall/process batch moved
`ptrace_detach_thread()` main-body sequencing through Rust while preserving the
C ABI wrapper, primitive callbacks, and C fallback scaffolding:

- Main-thread detach gating, zombie finalization selection, tracer child-list
  detach, and ptraced reparenting to the original parent.
- Report-list detach/reattach, ptrace cleanup/free callback dispatch,
  single-step clear dispatch, and exit-signal dispatch.
- Forwarded-signal siginfo construction, `do_kill()` callback sequencing,
  wake/release dispatch, and final process finalization dispatch.
- Equivalence coverage for a main-thread zombie detach with forwarded signal,
  a non-main no-report/no-forward detach, and invalid null-input rejection.

The Syscall core strict ownership row moves 92% -> 93% in `migration.txt`, the
Process/VM strict ownership row moves 90% -> 91%, and post-+50 strict-core
movement is now about +209 points. The broad Syscall core and Process/VM
dashboard rows were already capped at 100%, so this does not add broad
dashboard percentage. C still owns primitive lock/list/free/wakeup/signal
callback bodies, syscall dispatch, user-copy primitive bodies, ptrace
lookup/orchestration, default-path C fallback scaffolding, and broader
high-risk handler bodies.

Update 2026-05-26 wait-scan traversal strict-core batch
------------------------------------------------------------

Latest strict-core-only syscall batch moved the outer `do_wait()` process and
thread scan bodies through Rust while preserving the C ABI wrapper, primitive
callbacks, and offset-based C fallback scaffolding:

- Parent children-lock callback ordering, children-list traversal, pid/pgid
  matching, `empty` publication, and no-found unlock/return sequencing.
- Process-zombie dispatch, process stopped/continued candidate dispatch, and
  ptraced-children empty-scan traversal.
- Report-thread list traversal, report-thread candidate dispatch, and regular
  thread-list empty-scan traversal.
- Equivalence coverage for process zombie dispatch, process candidate dispatch,
  ptraced-child empty scans, invalid empty pointers, report-thread release
  dispatch, and regular thread empty scans.

The Syscall core strict ownership row moves 90% -> 92% in `migration.txt`, the
Process/VM strict ownership row moves 89% -> 90%, and post-+50 strict-core
movement is now about +207 points. The broad Syscall core and Process/VM
dashboard rows were already capped at 100%, so this does not add broad
dashboard percentage. C still owns primitive lock/list/free bodies,
waitqueue/schedule/signal primitive behavior, host wait primitive behavior,
syscall dispatch, user-copy primitive bodies, default-path C fallback
scaffolding, and broader high-risk handler bodies.

Update 2026-05-26 wait-zombie/reparent strict-core batch
------------------------------------------------------------

Latest strict-core-only syscall batch moved the `do_wait()` process-zombie
branch through Rust while preserving the C ABI wrapper, typed primitive
callbacks, and fallback scaffolding:

- Zombie status publication and host wait4 skip/request/warning/log result
  selection.
- Parent rusage aggregation, rusage result fill, parent child-list detach, and
  PID-1 reparent/list attach sequencing.
- Child main-thread ptrace-detach gating, parent/child/PID-1/thread lock and
  unlock callback ordering, no-reparent unlock/detach ordering, process
  release callback dispatch, and final found/result shaping.
- Equivalence coverage for reparent, no-reparent, `WNOWAIT`, host wait
  failure, rusage copy, callback ordering, and invalid thread cases.

The Syscall core strict ownership row moved 89% -> 90% in `migration.txt`, the
Process/VM strict ownership row moved 88% -> 89%, and post-+50 strict-core
movement was about +204 points. The broad Syscall core and Process/VM
dashboard rows were already capped at 100%, so this does not add broad
dashboard percentage. C still owned outer child/thread traversal, primitive
lock/list/free bodies, host wait primitive behavior, syscall dispatch,
user-copy primitives, and fallback scaffolding.

Previous update 2026-05-26 wait-candidate strict-core batch
------------------------------------------------------------

Previous strict-core-only syscall batch moved the `do_wait()` process and
report-thread candidate bodies through Rust while preserving the C ABI wrapper,
typed primitive callbacks, and fallback scaffolding:

- Process and report-thread stopped, ptraced-stopped, continued, and
  report-thread exited candidate result sequencing.
- Ptraced-stop miss return preservation and process TID rewrite for requested
  thread waits.
- Report-list detach, ptrace-detach, release, reap, unlock, and found
  publication callback ordering.
- Equivalence coverage for process stopped/continued hits and misses,
  report-thread exit no-wait/detach/release actions, report-thread
  stopped/continued hits and misses, and invalid callback/null cases.

The Syscall core strict ownership row moved 88% -> 89% in `migration.txt`, and
post-+50 strict-core movement was about +202 points. The broad Syscall core
dashboard row was already capped at 100%, so this does not add broad dashboard
percentage. C still owns child/thread list traversal, zombie scan/reparent
behavior, primitive lock bodies, waitqueue/schedule/signal primitive behavior,
rusage aggregation, release/detach primitive bodies, syscall dispatch,
user-copy primitives, and fallback scaffolding.

Previous update 2026-05-26 do_wait strict-core batch
------------------------------------------------------------

Previous strict-core-only syscall batch moved the top-level `do_wait()` wait
loop through Rust while preserving the C ABI wrapper, typed primitive
callbacks, and fallback scaffolding:

- Waitqueue entry initialization, prepare, and finish callback ordering.
- Existing `wait_proc()` and `wait_thread()` scan callback dispatch gates.
- No-child, `WNOHANG`, pending-signal interrupt, schedule handoff,
  wake/rescan, and wait-log sequencing.
- Equivalence coverage for process hit, clone/thread scan, empty child set,
  `WNOHANG`, pending signal, sleep/rescan/found, and invalid callback/null
  cases.

The Syscall core strict ownership row moved 87% -> 88% in `migration.txt`, and
post-+50 strict-core movement was about +201 points. The broad Syscall core
dashboard row was already capped at 100%, so this does not add broad dashboard
percentage. C still owns child/thread list traversal and mutation inside
`wait_proc()`/`wait_thread()`, waitqueue/schedule/signal primitive behavior,
rusage aggregation, release/detach side effects, syscall dispatch, user-copy
primitives, and fallback scaffolding.

Validation for this batch:

- `rustfmt kernel/rust/syscall_policy.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`; final output included
  `syscall_policy_helpers ok digest=bf93a8fed1d7d7bf` and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- QEMU version, `/dev/kvm`, image file-type checks, runner `--help`, runner
  dry-run, and bounded TCG probe against a disposable blank qcow2.

Previous scheduler migration-completion strict-core batch
------------------------------------------------------------

Previous strict-core-only scheduler batch moved the main `do_migrate()`
migration-completion sequencing through Rust while preserving the C ABI wrapper,
typed primitive callbacks, and fallback scaffolding:

- Migration queue traversal with safe next-pointer capture.
- Request list detach plus already-migrated, current-CPU-allowed, empty
  affinity, and missing-target ack gates.
- Affinity target selection and CPU-local callback dispatch.
- Current/target runqueue lock ordering, runqueue detach/add, runqueue length
  mutation, and thread CPU publication.
- Same-VM sibling scan and address-space CPU-set lock/update sequencing.
- Target `CPU_FLAG_NEED_RESCHED` publication, interrupt/vector callback
  dispatch, migration log callback dispatch, waitqueue wake callback dispatch,
  and validation/error shaping.

The Scheduler/timers/wait/futex strict ownership row remains capped at 100% in
`migration.txt`, but this added about +2 post-cap strict-core points. Post-+50
strict-core movement was about +200 points. The broad Scheduler/timers/
wait/futex dashboard row was already capped at 100%, so this does not add
broad dashboard percentage. Standalone readiness improves only slightly
because spinlock/waitqueue/interrupt/vector/log primitive bodies, CPU-local
lookup callbacks, context switching, `schedule()` primitive behavior,
default-path C primitive bodies, and fallback scaffolding remain C-owned.

Validation for this batch:

- `rustfmt kernel/rust/sched_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`; final output included
  `sched_helpers ok digest=297c3f5b0b7c220c` and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- Safe QEMU checks: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  image file-type checks, `scripts/qemu-mckernel-guest.sh --help`, runner
  dry-run with `/tmp/mckernel-qemu-domigrate.ZtJIgE/base.qcow2`, and a
  bounded TCG boot-process probe whose wrapped run timed out cleanly as
  expected on the disposable blank image. No real McKernel guest boot or
  reboot-capable validation ran.

Previous update 2026-05-26 scheduler migration-request strict-core batch
------------------------------------------------------------

Previous strict-core-only scheduler batch moved the main
`sched_request_migrate()` request-body sequencing through Rust while preserving
the C ABI wrapper, typed primitive callbacks, and fallback scaffolding:

- Request thread publication into the migration request object.
- Target migration-queue lock callback ordering.
- Waitqueue init/prepare/finish callback sequencing.
- Migration request list insertion into the target CPU queue.
- Target runqueue noirq lock/unlock callback ordering.
- `CPU_FLAG_NEED_RESCHED`, `CPU_FLAG_NEED_MIGRATE`, and
  `CPU_STATUS_RUNNING` publication.
- Remote interrupt vector/callback selection.
- Migration log callback dispatch, scheduler callback handoff, and
  validation/error shaping.

The Scheduler/timers/wait/futex strict ownership row remains capped at 100% in
`migration.txt`, but this added about +2 post-cap strict-core points. Post-+50
strict-core movement was about +198 points. The broad Scheduler/timers/
wait/futex dashboard row was already capped at 100%, so this does not add
broad dashboard percentage. Standalone readiness improves only slightly
because target CPU-local lookup, current-thread wait-entry construction,
spinlock/waitqueue/interrupt/vector/schedule/log primitive bodies, actual
migration completion in `do_migrate()`, context switching, default-path C
primitive bodies, and fallback scaffolding remain C-owned.

Validation for this batch:

- `rustfmt kernel/rust/sched_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`; final output included
  `sched_helpers ok digest=55c920613e2381d5` and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- Safe QEMU checks: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  image file-type checks, `scripts/qemu-mckernel-guest.sh --help`, runner
  dry-run with `/tmp/mckernel-qemu-schedmig.qiFJJ6/base.qcow2`, and a
  bounded TCG boot-process probe whose wrapped run timed out cleanly as
  expected on the disposable blank image. No real McKernel guest boot or
  reboot-capable validation ran.

Previous update 2026-05-26 dump user-page traversal/dispatch strict-core batch
------------------------------------------------------------

Latest strict-core-only dump user-page batch moved the main
`ihk_mc_query_mem_user_page()` process traversal and page-table visitor
dispatch body through Rust while preserving the C ABI wrapper, page-table
visitor primitive, PTE visitor body, and fallback scaffolding:

- Process-hash bucket iteration and list cursor traversal.
- Process recovery from the embedded `hash_list` member.
- VM, address-space, and page-table pointer loading.
- Null skip gates for VM/address-space/page-table holes.
- Visitor argument shaping for `[0, USER_END)`.
- `visit_pte_range_safe()` callback dispatch and dispatch count/error shaping.

The x86_64 memory-management strict ownership row remains capped at 100% in
`migration.txt`, but this added about +1 post-cap strict-core point. Post-+50
strict-core movement was about +196 points. The broad x86_64 memory
dashboard row was already capped at 100%, so this does not add broad dashboard
percentage. Standalone readiness improves only slightly because current
resource-set lookup, the page-table visitor primitive and PTE visitor body,
low-level page/address data sources, process lifetime/list mutation, logging
primitive behavior, default-path C primitive bodies, and fallback scaffolding
remain C-owned.

Validation for this batch:

- `rustfmt kernel/rust/mem_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`; final output included
  `mem_init_helpers ok digest=f2832d4ec8ab78e8` and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- Safe QEMU checks: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  image file-type checks, `scripts/qemu-mckernel-guest.sh --help`, runner
  dry-run with `/tmp/mckernel-qemu-userdump.qUVhow/base.qcow2`, and a bounded
  TCG boot-process probe whose wrapped run timed out cleanly as expected on the
  disposable blank image. No real McKernel guest boot or reboot-capable
  validation ran.
- `git diff --check`
- `bash -n kernel/rust/tests/run_equivalence.sh scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt --check-dashboard overview2.txt --top 10`

Previous update 2026-05-26 dump bitmap/completion strict-core batch
------------------------------------------------------------

Latest strict-core-only dump bitmap batch moved memory-dump page-set and
bitmap data-production sequencing through Rust while preserving C ABI wrappers,
typed primitive callbacks, volatile completion publication, and C fallback
scaffolding:

- Dump page-set completion clear/set sequencing.
- Physical-address membership checks over memory chunks.
- Variable-length dump bitmap range clearing across dump-page descriptors.
- User PTE physical extraction, address validation, and bitmap marking.
- Free-chunk traversal callback sequencing and bitmap marking.
- Top-level last-CPU dump query dispatch for user/free-page collection.

The x86_64 memory-management strict ownership row remains capped at 100% in
`migration.txt`, but this adds about +2 post-cap strict-core points. Post-+50
strict-core movement is now about +195 points. The broad x86_64 memory
dashboard row was already capped at 100%, so this does not add broad dashboard
percentage. Standalone readiness improves only slightly because process
traversal, rb-tree primitive traversal callbacks, page-table visit callback
invocation, low-level page/address data sources, logging primitive behavior,
default-path C primitive bodies, and fallback scaffolding remain C-owned.

Validation for this batch:

- `rustfmt kernel/rust/mem_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `kernel/rust/tests/run_equivalence.sh`; final output included
  `mem_init_helpers ok digest=3b17b81ac8d56553` and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- Safe QEMU checks: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  image file-type checks, `scripts/qemu-mckernel-guest.sh --help`, runner
  dry-run with `/tmp/mckernel-qemu-dumpmap.CnQDUA/base.qcow2`, and a bounded
  TCG boot-process probe whose wrapped run timed out cleanly as expected on the
  disposable blank image. No real McKernel guest boot or reboot-capable
  validation ran.

Update 2026-05-26 allocator NUMA topology/allocation strict-core batch
------------------------------------------------------------

Previous strict-core-only page-allocator topology batch moved NUMA
allocation-attempt and distance-ordered lookup sequencing through Rust while
preserving C ABI wrappers, typed primitive callbacks, and C fallback
scaffolding:

- Allocation-attempt OOM flag initialization and NUMA-id bounds validation.
- Rusage OOM callback dispatch and NUMA allocation callback dispatch.
- Distance-vector bounds/null checks and distance-id lookup.
- CPU-local readiness checks, current-NUMA callback dispatch, and
  distance-ordered node pointer selection for
  `ihk_mc_get_numa_node_by_distance()`.

The Page allocator strict ownership row was recorded as 94% -> 95% in
`migration.txt`. Post-+50 strict-core movement was about +193 points. The
broad Page allocator dashboard row was already capped at 100%, so this does
not add broad dashboard percentage. Standalone readiness improves only slightly
because low-level page allocation/free primitive bodies, interrupt and spinlock
primitive bodies, current-node primitive lookup, logging/panic primitive
behavior, deferred-zero timing, IKC send side effects, default-path C primitive
bodies, and fallback scaffolding remain C-owned.

Validation for this batch:

- `rustfmt kernel/rust/mem_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`; final output included
  `mem_init_helpers ok digest=c6e5831ac84d68d8` and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- Safe QEMU checks: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  image file-type checks, `scripts/qemu-mckernel-guest.sh --help`, runner
  dry-run with `/tmp/mckernel-qemu-numatopo.4QuyLW/base.qcow2`, and a bounded
  TCG boot-process probe whose wrapped run timed out cleanly as expected on the
  disposable blank image. No real McKernel guest boot or reboot-capable
  validation ran.

Update 2026-05-26 allocator NUMA-distance strict-core batch
------------------------------------------------------------

Latest strict-core-only page-allocator lifecycle batch moved
`numa_distances_init()` sequencing through Rust while preserving C ABI wrappers,
typed bridge callbacks, and C fallback scaffolding:

- Per-node `nodes_by_distance` allocation and publication.
- Allocation-failure log callback routing while continuing other nodes.
- NUMA distance matrix fill, distance/id stable ordering, and ordered distance
  log callback sequencing.

The Page allocator strict ownership row is now recorded as 93% -> 94% in
`migration.txt`. Post-+50 strict-core movement was about +192 points. The
broad Page allocator dashboard row was already capped at 100%, so this does
not add broad dashboard percentage. Standalone readiness improves only
slightly because low-level page allocation/free primitive bodies, interrupt
and spinlock primitive bodies, logging/panic primitive behavior,
deferred-zero timing, IKC send side effects, default-path C primitive bodies,
and fallback scaffolding remain C-owned.

Validation for this batch:

- `rustfmt kernel/rust/mem_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`; final output included
  `mem_init_helpers ok digest=dcabaa1a141d5ef3` and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- Safe QEMU checks: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  image file-type checks, `scripts/qemu-mckernel-guest.sh --help`, runner
  dry-run with `/tmp/mckernel-qemu-numadist.AnnC0b/base.qcow2`, and a bounded
  TCG boot-process probe whose wrapped run timed out cleanly as expected on the
  disposable blank image. No real McKernel guest boot or reboot-capable
  validation ran.

Update 2026-05-26 allocator lifecycle/mem_init strict-core batch
------------------------------------------------------------

Latest strict-core-only page-allocator lifecycle batch moved
`ihk_mc_set_page_allocator()` and top-level `mem_init()` sequencing through Rust
while preserving C ABI wrappers, typed bridge callbacks, and C fallback
scaffolding:

- Pagealloc tracking initialization, early-allocator invalidation, and `pa_ops`
  publication in `ihk_mc_set_page_allocator()`.
- Top-level `mem_init()` ordering for monitor init, rusage init, NUMA init,
  allocator registration, page-fault handler publication, query-free interrupt
  registration, page-hash init, virtual-map init, demand-paging flag/log
  publication, and NUMA-distance init.

The Page allocator strict ownership row is now recorded as 92% -> 93% in
`migration.txt`. Post-+50 strict-core movement was about +191 points. The
broad Page allocator dashboard row was already capped at 100%, so this does
not add broad dashboard percentage. Standalone readiness improves only
slightly because low-level page allocation/free primitive bodies, interrupt
and spinlock primitive bodies, logging/panic primitive behavior,
deferred-zero timing, IKC send side effects, default-path C primitive bodies,
and fallback scaffolding remain C-owned.

Validation for this batch:

- `rustfmt kernel/rust/mem_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`; final output included
  `mem_init_helpers ok digest=8d38e73464524412` and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- Safe QEMU checks: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  image file-type checks, `scripts/qemu-mckernel-guest.sh --help`, runner
  dry-run with `/tmp/mckernel-qemu-lifecycle2.tl97yy/base.qcow2`, and a
  bounded TCG boot-process probe whose wrapped run timed out cleanly as
  expected on the disposable blank image. No real McKernel guest boot or
  reboot-capable validation ran.

Update 2026-05-26 allocator tracking/lifecycle strict-core batch
------------------------------------------------------------

Latest strict-core-only page-allocator batch moved allocator tracking and
lifecycle maintenance through Rust while preserving C ABI wrappers, typed
bridge callbacks, and C fallback scaffolding:

- Shared pagealloc/kmalloc tracking hash and lock initialization.
- Pagealloc/kmalloc memcheck bucket traversal, per-entry address-list locking,
  current-runcount filtering, leak-count accumulation, runcount mutation, and
  detail/summary leak-log callback sequencing.
- Kmalloc remote free-list move into the CPU free list and consolidation under
  the remote-list lock.

The Page allocator strict ownership row is now recorded as 90% -> 92% in
`migration.txt`. Post-+50 strict-core movement is now about +190 points. The
broad Page allocator dashboard row was already capped at 100%, so this does
not add broad dashboard percentage. Standalone readiness improves only
slightly because low-level page allocation/free primitive bodies, interrupt
and spinlock primitive bodies, logging/panic primitive behavior,
deferred-zero timing, IKC send side effects, default-path C primitive bodies,
and fallback scaffolding remain C-owned.

Validation for this batch:

- `rustfmt kernel/rust/mem_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`; final output included
  `kmalloc_helpers ok digest=31fdc2c4adc7d006`,
  `pagealloc_track ok digest=504fd0be6a7cfb0b`, and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- Safe QEMU checks: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  image file-type checks, `scripts/qemu-mckernel-guest.sh --help`, runner
  dry-run with `/tmp/mckernel-qemu-lifecycle.mAxKdJ/base.qcow2`, and a
  bounded TCG boot-process probe whose wrapped run timed out cleanly as
  expected on the disposable blank image. No real McKernel guest boot or
  reboot-capable validation ran.

Update 2026-05-26 pagealloc tracking wrapper sequencing
------------------------------------------------------------

Latest strict-core-only page-allocator batch moved public pagealloc memdebug
tracking wrappers through Rust while preserving C ABI wrappers, typed bridge
callbacks, and C fallback scaffolding:

- `_ihk_mc_alloc_aligned_pages_node()` no-debug/uninitialized fallback and
  allocation-site hash selection.
- Tracking-entry lookup, new entry allocation/init, file-string allocation and
  publication, and alloc-count mutation.
- Page-address entry allocation/init, per-entry list insertion, address-hash
  insertion, and log callback selection.
- `_ihk_mc_free_pages()` no-debug/uninitialized fallback, exact-start
  partial-free tracking, covering-range middle/suffix split tracking,
  address-entry rehashing, full-free list detach, count decrement, last-entry
  metadata cleanup/free, invalid-size/invalid-free callback routing, and final
  page-free callback sequencing.

The Page allocator strict ownership row is now recorded as 88% -> 90% in
`migration.txt`. Post-+50 strict-core movement is now about +188 points. The
broad Page allocator dashboard row was already capped at 100%, so this does
not add broad dashboard percentage. Standalone readiness improves only
slightly because low-level page allocation/free primitive bodies, interrupt
and spinlock primitive bodies, logging/panic primitive behavior,
deferred-zero timing, IKC send side effects, default-path C primitive bodies,
and fallback scaffolding remain C-owned.

Validation for this batch:

- `rustfmt kernel/rust/mem_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`; final output included
  `pagealloc_track ok digest=1c3f131498b15b77` and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- Safe QEMU checks: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  image file-type checks, `scripts/qemu-mckernel-guest.sh --help`, runner
  dry-run with `/tmp/mckernel-qemu-pagealloc.O8Ur9D/base.qcow2`, and a
  bounded TCG boot-process probe whose wrapped run timed out cleanly as
  expected on the disposable blank image. No real McKernel guest boot or
  reboot-capable validation ran.

Previous update 2026-05-26 kmalloc tracking wrapper sequencing
------------------------------------------------------------

Previous strict-core-only page-allocator batch moved the public memdebug
tracking wrappers through Rust while preserving C ABI wrappers, typed bridge
callbacks, and C fallback scaffolding:

- `_kmalloc()` no-debug/null fallback and allocation hash selection.
- Tracking-entry lookup, new entry allocation/init, file-string allocation and
  publication, and alloc-count increment.
- Address-entry allocation/init, per-entry list insertion, address-hash
  insertion, and log callback selection.
- `_kfree()` no-debug/null fallback, address-hash lookup, address-entry detach,
  per-entry list detach, count decrement, last-entry metadata cleanup/free, and
  invalid-free callback routing.

The Page allocator strict ownership row was recorded as 86% -> 88% in
`migration.txt`. Post-+50 strict-core movement was about +186 points. The
broad Page allocator dashboard row was already capped at 100%, so this does
not add broad dashboard percentage. Standalone readiness improves only slightly
because low-level page allocation/free primitive bodies, interrupt and spinlock
primitive bodies, logging/panic primitive behavior, deferred-zero timing, IKC
send side effects, default-path C primitive bodies, and fallback scaffolding
remain C-owned.

Validation for this batch:

- `rustfmt kernel/rust/mem_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`; final output included
  `kmalloc_helpers ok digest=4dba05f7d7e30671` and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `bash -n kernel/rust/tests/run_equivalence.sh
  scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 10`
- Safe QEMU checks: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  image file-type checks, `scripts/qemu-mckernel-guest.sh --help`, runner
  dry-run with `/tmp/mckernel-qemu-track.mX1nMd/base.qcow2`, and a bounded TCG
  boot-process probe whose wrapped run timed out cleanly as expected on the
  disposable blank image. No real McKernel guest boot or reboot-capable
  validation ran.

Previous update 2026-05-26 kmalloc body sequencing
------------------------------------------------------------

Previous strict-core-only page-allocator batch moved low-level kmalloc/free body
sequencing through Rust while preserving C ABI wrappers, typed bridge
callbacks, and C fallback scaffolding:

- `___kmalloc()` size alignment, free-list fit search, exact-fit removal, chunk
  split, empty-list page-refill callback ordering, interrupt save/restore
  callback ordering, and return shaping.
- `___kfree()` null handling, corruption callback routing, current-CPU
  free-list insertion/consolidation, remote-CPU free-list publication under
  lock callbacks, and normal/error return shaping.

The Page allocator strict ownership row was recorded as 84% -> 86% in
`migration.txt`. Post-+50 strict-core movement was about +184 points. The
broad Page allocator dashboard row was already capped at 100%, so this does
not add broad dashboard percentage. Standalone readiness improved only slightly
because low-level page allocation/free primitive bodies, interrupt and spinlock
primitive bodies, logging/panic primitive behavior, deferred-zero timing, IKC
send side effects, default-path C primitive bodies, and fallback scaffolding
remain C-owned.

Validation for this batch:

- `rustfmt kernel/rust/mem_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh` passed after fixing the logged
  test-only `stddef.h` typedef conflict; final output included
  `kmalloc_helpers ok digest=e8d759c35653d57c` and
  `rust object unresolved symbols and SIMD checks ok`.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `bash -n kernel/rust/tests/run_equivalence.sh
  scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 10`
- Safe QEMU checks: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  image file-type checks, `scripts/qemu-mckernel-guest.sh --help`, runner
  dry-run with `/tmp/mckernel-qemu-kmalloc.xEKd3q/base.qcow2`, and a bounded
  TCG boot-process probe that reached SeaBIOS/iPXE and timed out cleanly on the
  disposable blank image. No real McKernel guest boot or reboot-capable
  validation ran.

Previous update 2026-05-26 host IKC init sequencing
------------------------------------------------------------

Previous strict-core-only host batch moved IKC initialization sequencing through
Rust while preserving C ABI wrappers, typed bridge callbacks, and C fallback
scaffolding:

- IKC2Linux channel-table allocation, zeroing, and pointer publication.
- Linux connect-parameter shaping, connect retry/delay/log sequencing,
  channel-slot publication, and current-channel callback sequencing.
- IKC2McKernel connect-parameter shaping, retry/delay/log sequencing, and
  regular-channel callback sequencing.

The host/IKC/mcctrl/IHK strict ownership row was recorded as 89% -> 91% for
this batch. Post-+50 strict-core movement was about +182 points. The broad
host/IKC/mcctrl/IHK dashboard row was already capped at 100%, so this does not
add broad dashboard percentage. C still owns actual allocation/connect/delay/
log/panic/current-channel/regular-channel primitive bodies, raw IKC exchange
side effects, broad allocation/lifetime, and fallback scaffolding.

Validation for this batch:

- `rustfmt kernel/rust/host_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `bash -n kernel/rust/tests/run_equivalence.sh
  scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 10`
- Safe QEMU checks only: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  `scripts/qemu-mckernel-guest.sh --help`, and runner dry-run with
  `/tmp/mckernel-qemu-dryrun.Vz7ukE/base.qcow2`.

Previous update 2026-05-26 procfs maps/status/stat data-production sequencing
------------------------------------------------------------

Previous strict-core-only procfs batch moved maps/status/stat data-production
sequencing through Rust while preserving C ABI wrappers, typed bridge
callbacks, and C fallback scaffolding:

- `/proc/PID/maps` memory-range traversal, path/default selection, permission
  character selection, and line output.
- `/proc/PID/status` locked-size traversal plus state, PID, UID/GID, VmLck,
  thread count, CPU mask/list, and NUMA mask/list output.
- Per-PID `stat` state, identity, command-name, thread-count, CPU, and
  constant-field output.

The procfs/sysfs/xpmem/file-object strict ownership row was recorded as
88% -> 91% for this batch. Post-+50 strict-core movement was about
+180 points. The broad procfs/sysfs/xpmem/file-object dashboard row was already
capped at 100%, so this did not add broad dashboard percentage. C still owned
allocation/free primitives, request map/unmap, process/thread lookup,
memory-range locking, raw IKC answer send, and fallback scaffolding.

Validation for this batch:

- `rustfmt kernel/rust/object_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `bash -n kernel/rust/tests/run_equivalence.sh
  scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 10`
- Safe QEMU checks only: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  `scripts/qemu-mckernel-guest.sh --help`, and runner dry-run with
  `/tmp/mckernel-qemu-dryrun.ETxpZd/base.qcow2`.

Update 2026-05-26 procfs non-mem data-production sequencing
------------------------------------------------------------

Previous strict-core-only procfs batch moved non-mem procfs data-production
sequencing through Rust while preserving C ABI wrappers, typed bridge
callbacks, and C fallback scaffolding:

- Root `mckernel` version/buildid output.
- Root `stat` CPU-line generation.
- Per-process `auxv` byte output.
- Per-process `cmdline` present/missing output.
- Per-process `comm` basename/fallback/newline output.
- `/proc/PID/pagemap` page-table-to-pagemap value-loop/result sequencing.

The procfs/sysfs/xpmem/file-object strict ownership row was recorded as
86% -> 88% for this batch. Post-+50 strict-core movement was about
+177 points. The broad procfs/sysfs/xpmem/file-object dashboard row was already
capped at 100%, so this did not add broad dashboard percentage. C still owned
allocation/free primitives, request map/unmap, process/thread lookup,
memory-range locking, page-table primitive behavior, complex procfs
status/stat/maps-style data production, raw IKC answer send, and fallback
scaffolding at that checkpoint.

Validation for this batch:

- `rustfmt kernel/rust/object_helpers.rs`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `bash -n kernel/rust/tests/run_equivalence.sh
  scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 10`
- Safe QEMU checks only: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  `scripts/qemu-mckernel-guest.sh --help`, and runner dry-run with
  `/tmp/mckernel-qemu-dryrun.u0vvx7/base.qcow2`.

Update 2026-05-26 procfs mem/buffer sequencing
------------------------------------------------------------

Previous strict-core-only procfs batch moved buffer allocation and `/proc/PID/mem`
data-copy sequencing through Rust while preserving C ABI wrappers, typed bridge
callbacks, and C fallback scaffolding:

- `buf_alloc()` allocation-callback/null-result handling, buffer metadata
  initialization, and optional physical-address publication.
- `/proc/PID/mem` zero-length handling, page-fault callback ordering,
  virtual-to-physical translation callback ordering, McKernel-memory gate,
  physical-to-virtual mapping callback, read-versus-write copy direction,
  offset/remaining/result advancement, and partial-copy/error shaping.

The procfs/sysfs/xpmem/file-object strict ownership row was recorded as
84% -> 86% for this batch. Post-+50 strict-core movement was about
+175 points. The broad procfs/sysfs/xpmem/file-object dashboard row was already
capped at 100%, so this did not add broad dashboard percentage. C still owned
page allocation/free primitives, page-fault primitive behavior, page-table
translation primitive behavior, `is_mckernel_memory()`, `phys_to_virt()`,
`memcpy()`, request map/unmap, process/thread lookup, non-mem procfs data
production, raw IKC answer send, and fallback scaffolding at that checkpoint.

Validation for this batch:

- `rustfmt kernel/rust/object_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `bash -n scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 10`
- Safe QEMU checks only: `qemu-system-x86_64 --version`, `/dev/kvm` presence,
  `scripts/qemu-mckernel-guest.sh --help`, and harness dry-run with a temporary
  qcow2 backing image.

Update 2026-05-26 scheduler/timer sequencing
------------------------------------------------------------

Latest strict-core-only scheduler/timer batch moved runtime timer sequencing
through Rust while preserving C ABI wrappers, typed bridge callbacks, and C
fallback scaffolding:

- `init_timers()` list-head and timer-lock initialization.
- `schedule_timeout()` spin-wake checks, runqueue scheduling gate, status
  publication, schedule callback handoff, zero-free/pause spin loop, timeout
  decrement, and spin-sleep clear sequencing.
- `wake_timers_loop()` timer-list tick, timeout decrement, expired timer
  detach, debug-log callback, and waitqueue wake callback sequencing.
- `set_timer()` runqueue scan plus LAPIC timer enable/disable decision
  sequencing.

The Scheduler/timers/wait/futex strict ownership row is now recorded as
97% -> 100% for this batch. Post-+50 strict-core movement is now about
+173 points. The broad Scheduler/timers/wait/futex dashboard row was already
capped at 100%, so this does not add broad dashboard percentage. C still owns
actual spinlock primitives, rdtsc/pause primitive behavior, LAPIC timer
primitive behavior, waitqueue wake primitive behavior, `schedule()`/
context-switch behavior, current-thread lookup, futex primitive bodies, and
fallback scaffolds.

Validation for this batch:

- `rustfmt kernel/rust/sched_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `bash -n kernel/rust/tests/run_equivalence.sh
  scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --format json`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 5`
- QEMU version, `/dev/kvm`, runner `--help`, and runner dry-run checks passed;
  no real guest boot ran.

Update 2026-05-26 x86 user-copy/process-VM sequencing
------------------------------------------------------------

Latest strict-core-only x86 memory batch moved user-copy and process-VM copy
loop sequencing through Rust while preserving C ABI wrappers, typed bridge
callbacks, and C fallback scaffolding:

- Process-VM range validation and page-fault retry dispatch.
- Read/write/patch process-VM per-page chunk copying with direct versus mapped
  physical-copy selection.
- `copy_from_user()` and `copy_to_user()` wrapper dispatch.
- `strlen_user()` and `strcpy_from_user()` user string scanning/copying.
- `getlong_user()`, `getint_user()`, `setlong_user()`, and `setint_user()`
  scalar wrapper sequencing.

The x86 memory strict ownership row is now recorded as 99% -> 100% for this
batch. Post-+50 strict-core movement was about +170 points at that checkpoint. The broad
x86_64 memory row was already capped at 100%, so this does not add broad
dashboard percentage. C still owns current-thread lookup, page-fault primitive
behavior, virtual-to-physical primitive behavior, map/unmap and phys-to-virt
primitive bodies, logging callbacks, bridge glue, and fallback scaffolds.

Validation for this batch:

- `rustfmt kernel/rust/x86_memory_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `bash -n kernel/rust/tests/run_equivalence.sh
  scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --format json`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 5`
- QEMU version, `/dev/kvm`, runner `--help`, and runner dry-run checks passed;
  no real guest boot ran.

Update 2026-05-26 host SCD dispatcher sequencing
------------------------------------------------------------

Previous strict-core-only host control-plane batch moved the top-level SCD
packet dispatcher through Rust while preserving C ABI wrappers, compact bridge
callbacks, and C fallback scaffolding:

- Init-channel ACK logging dispatch, prepare-process reply dispatch,
  schedule-process dispatch, wake-syscall-thread dispatch, remote page-fault
  dispatch, send-signal dispatch, procfs request/release dispatch, cleanup
  process/fd dispatch, debug-log dispatch, sysfs show/store/release dispatch,
  perf-control dispatch, CPU register dispatch, unknown-message logging
  dispatch, legacy return-value shaping, and final packet release sequencing.

The host control-plane strict ownership row was recorded as 86% -> 89% for
this batch. Post-+50 strict-core movement was about +169 points at that
checkpoint. The broad
host/IKC/mcctrl/IHK row was already capped at 100%, so this does not add broad
dashboard percentage. C still owns raw IKC send effects, primitive lookup/
wakeup/register/debug behaviors, allocation/copy/map primitive bodies, broad
IKC exchange mutation, kernel object lifecycle, and fallback scaffolds.

Validation for this batch:

- `rustfmt kernel/rust/host_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `bash -n kernel/rust/tests/run_equivalence.sh
  scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --format json`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 5`
- QEMU version, `/dev/kvm`, runner `--help`, and runner dry-run checks passed;
  no real guest boot ran.

Update 2026-05-26 itimer sequencing
------------------------------------------------------------

Latest strict-core-only interval-timer syscall batch moved set/get-itimer
sequencing through Rust while preserving C ABI wrappers and C fallback
scaffolding:

- `setitimer()` `which` validation, ITIMER_REAL Linux-forward callback
  dispatch, virtual/prof timer slot selection, old-value current snapshot and
  copyout callback sequencing, new-value copyin callback sequencing,
  elapsed-time reset, enabled-state publication, `set_timer(0)` callback
  dispatch, and return/error shaping.
- `getitimer()` `which` validation, ITIMER_REAL Linux-forward callback
  dispatch, virtual/prof current snapshot/copyout callback sequencing,
  null-old no-op handling, and return/error shaping.

Syscall strict ownership is now recorded as 85% -> 87% for this batch, and
the scheduler/timers/wait/futex strict row is now recorded as 95% -> 97%.
Post-+50 strict-core movement was about +166 points at that checkpoint. The broad Syscall Core
and Scheduler/timers/wait/futex rows were already capped at 100%, so this does
not add broad dashboard percentage. C still owns raw syscall argument
extraction, actual user-copy primitives, `do_syscall()` forwarding primitive
behavior, actual `set_timer()` behavior, timer queues, wake/context-switch
primitives, spinlocks, broader futex wait/requeue side effects, and fallback
scaffolds.

Validation for this batch:

- `rustfmt kernel/rust/syscall_policy.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `bash -n kernel/rust/tests/run_equivalence.sh
  scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --format json`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 5`
- QEMU version, `/dev/kvm`, runner `--help`, and runner dry-run checks passed;
  no real guest boot ran.

Update 2026-05-26 scheduler affinity sequencing
------------------------------------------------------------

Previous strict-core-only scheduler affinity batch moved set/get-affinity
sequencing through Rust while preserving C ABI wrappers and C fallback
scaffolding:

- `sched_setaffinity()` argument validation, local/remote target selection,
  remote lookup/unlock ordering, credential checks, cpuset copyin, cpuset
  zeroing, target-process mask intersection, thread affinity mask publication,
  current-CPU membership testing, migration callback dispatch, and
  return/error shaping.
- `sched_getaffinity()` size validation, local/remote target selection,
  remote lookup/unlock ordering, cpuset copyout callback sequencing, and
  return/error shaping.

Syscall strict ownership is now recorded as 84% -> 85% for this batch, and
the scheduler/timers/wait/futex strict row is now recorded as 92% -> 95%.
Post-+50 strict-core movement was about +162 points at that checkpoint. The broad Syscall Core
and Scheduler/timers/wait/futex rows were already capped at 100%, so this does
not add broad dashboard percentage. C still owns raw syscall argument
extraction, actual user-copy primitives, thread lookup/hold/release primitive
bodies, migration request side effects, wake/context-switch primitives, timer
queues, spinlocks, broader futex wait/requeue side effects, and fallback
scaffolds.

Validation for this batch:

- `rustfmt kernel/rust/sched_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `bash -n kernel/rust/tests/run_equivalence.sh
  scripts/qemu-mckernel-guest.sh scripts/rocky-rust-validation.sh`
- `scripts/rust-ownership-report.py --format json`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 5`
- QEMU version, `/dev/kvm`, runner `--help`, and runner dry-run checks passed;
  no real guest boot ran.

Update 2026-05-26 scheduler syscall sequencing
------------------------------------------------------------

Previous strict-core-only scheduler syscall batch moved set/get-param,
set/get-scheduler, and RR-interval query sequencing through Rust while
preserving C ABI wrappers and C fallback scaffolding:

- `sched_setparam()` PID validation/normalization, remote-target lookup and
  unlock ordering, Linux same-owner request shaping, user-parameter copyin,
  remote re-lookup, scheduler-parameter callback dispatch, and return/error
  shaping.
- `sched_setscheduler()` policy validation, root-check request shaping,
  user-parameter copyin, remote target lookup, Linux same-owner request
  shaping, scheduler mutation callback dispatch, and return/error shaping.
- `sched_getparam()`, `sched_getscheduler()`, and
  `sched_rr_get_interval()` target selection, copyout callback sequencing,
  policy/RR-interval result shaping, and fault/error handling.

Syscall strict ownership is now recorded as 82% -> 84% for this batch, and
The scheduler/timers/wait/futex strict row is now recorded as 89% -> 92%.
Post-+50 strict-core movement is now about +158 points. The broad Syscall Core
and Scheduler/timers/wait/futex rows were already capped at 100%, so this does
not add broad dashboard percentage. C still owns raw syscall argument
extraction, actual user-copy primitives, Linux forwarding primitive behavior,
thread lookup/unlock primitive bodies, scheduler affinity/migration side
effects, and fallback scaffolds.

Validation for this batch:

- `rustfmt kernel/rust/sched_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `git diff --check`
- `scripts/rust-ownership-report.py --format json`
- QEMU version, `/dev/kvm`, runner `--help`, and runner dry-run checks passed;
  no real guest boot ran.

Update 2026-05-26 rusage/CPU-time syscall sequencing
------------------------------------------------------------

Latest strict-core-only syscall batch moved `getrusage()` and
`clock_gettime()` CPU-time sequencing through Rust while preserving C ABI
wrappers and C fallback scaffolding:

- `getrusage()` `who` validation, dispatch, zeroed `struct rusage`
  preparation, SELF thread-list lock/update/wait/TSC aggregation, CHILDREN
  and THREAD result filling, copyout callback sequencing, and error shaping.
- Remote running user-thread times-update request and CPU interrupt callback
  sequencing, with pause-callback wait loops for completion.
- `clock_gettime()` no-output fast return, local realtime copyout, Linux
  forward dispatch, thread CPU-time TSC aggregation, and process CPU-time
  thread traversal/update/wait/aggregation/copyout sequencing.

Syscall strict ownership was recorded as 78% -> 82% for this batch.
Post-+50 strict-core movement was +153 points at that checkpoint. The broad Syscall Core
dashboard row was already capped at 100%, so this does not add broad dashboard
percentage. C still owns syscall ABI wrappers, raw argument extraction, the
actual `copy_to_user()` primitive, lock primitive bodies, CPU interrupt
primitive behavior, TOD/gettime and Linux-forward callback bodies,
scheduler/time primitive bodies, and fallback scaffolding. Standalone
readiness improves only slightly because those default-path C primitive bodies
and fallback scaffolds remain required.

Validation for this batch:

- `rustfmt kernel/rust/syscall_policy.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 2`
- Warning scan over `/tmp/mckernel-clean-build-rusage-cputime-rust.log`,
  `/tmp/mckernel-clean-build-rusage-cputime-cfallback.log`, and
  `/tmp/mckernel-clean-build-rusage-cputime-wrapper.log` found no
  `warning`/`warn:` matches.
- `git diff --check`
- `bash -n scripts/rocky-rust-validation.sh
  scripts/qemu-mckernel-guest.sh kernel/rust/tests/run_equivalence.sh`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 5`
- QEMU version, `/dev/kvm`, runner `--help`, and runner dry-run checks passed;
  no real guest boot ran.

Update 2026-05-26 Process/VM lifecycle release sequencing
------------------------------------------------------------

Latest strict-core-only Process/VM batch moved lifecycle release sequencing
through Rust while preserving C ABI wrappers and C fallback scaffolding:

- `release_sigcommon()` refcount gate, pending-signal list drain, pending
  siginfo free callbacks, and final sigcommon free callback.
- `__release_tid()` and `__find_and_replace_tid()` TID-slot scan, slot
  clear/replace, thread TID update, and debug-log callback sequencing.
- `release_thread()` refcount gate, profile callback, procfs delete callback,
  destroy-thread callback, and VM-release callback sequencing.
- `release_process_vm()` refcount gate, mckfd close/drain, optional free
  callback, TLB flush callback, range-free callback, address-space
  detach/process release, policy drain/free, and final VM free callback.

Process/VM strict ownership is now recorded as 82% -> 88% for this batch.
Post-+50 strict-core movement was +149 points at that checkpoint. The broad Process/VM
dashboard row was already capped at 100%, so this does not add broad dashboard
percentage. C still owns atomic refcount primitives, spinlock primitives,
allocator/free callback bodies, destroy_thread and release_process bodies,
profile/procfs/TLB/range-free primitive bodies, page-fault behavior, signal
forwarding, rusage aggregation, broad process lifetime, and fallback
scaffolding. Standalone readiness improves only slightly because default-path C
primitive bodies and fallback scaffolding still remain required.

Validation for this batch:

- `rustfmt kernel/rust/process_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- `cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j2`
- `scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 2`
- Warning scan over `/tmp/mckernel-clean-build-process-lifecycle-rust.log`,
  `/tmp/mckernel-clean-build-process-lifecycle-cfallback.log`, and
  `/tmp/mckernel-clean-build-process-lifecycle-wrapper.log` found no
  `warning`/`warn:` matches.
- QEMU version, `/dev/kvm`, runner `--help`, and runner dry-run checks passed.
  No real guest boot ran.
- The final equivalence run included
  `process_helpers ok digest=b1b68056ccefbaac`.

Update 2026-05-26 x86 clear-range body sequencing
------------------------------------------------------------

Latest strict-core-only x86 batch moved clear-range leaf/level/root body
sequencing through Rust while preserving C ABI wrappers and C fallback
scaffolding:

- Leaf null-skip and PTE clear-exchange sequencing.
- TLB flush-address queue callback sequencing.
- Old-entry action callback handoff and old-effect callback orchestration.
- Split-error log/return shaping and large-page clear sequencing.
- Child table translation, child-walk dispatch, and error propagation.
- `-ENOENT` child-table teardown/free sequencing.
- Root skip/translation/dispatch and level-specific debug logging.

Strict x86_64 memory ownership is now recorded as 98% -> 99% for this batch.
Post-+50 strict-core movement was +143 points at that checkpoint. The broad x86_64 memory
dashboard row was already capped at 100%, so this does not add broad dashboard
percentage. C still owns the public ABI wrappers, old-entry page lookup/refcount
primitive behavior, actual allocation/free and address-translation primitive
bodies, page/RSS primitive bodies, remote TLB shootdown primitive behavior, and
fallback scaffolding. Standalone readiness improves only slightly because
default-path C primitive bodies and fallback scaffolding still remain required.

Validation for this batch:

- `rustfmt kernel/rust/x86_memory_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- Logged Rust build:
  `/tmp/mckernel-clean-build-x86-clear-body-rust.log`
- Logged C fallback build:
  `/tmp/mckernel-clean-build-x86-clear-body-cfallback.log`
- Logged wrapper build-only validation:
  `/tmp/mckernel-clean-build-x86-clear-body-wrapper.log`
- Warning scan over those three logs found no `warning`/`warn:` matches.
- QEMU version, `/dev/kvm`, runner `--help`, and runner dry-run checks passed.
  No real guest boot ran.

Update 2026-05-26 x86 visitor-body sequencing
------------------------------------------------------------

Previous strict-core-only x86 batch moved normal/safe `visit_pte_range()`
visitor body sequencing through Rust while preserving C ABI wrappers and C
fallback scaffolding:

- Leaf skip/null and visitor callback dispatch.
- Level direct visitor dispatch and E2BIG retry classification.
- Split-error log/return shaping.
- Child page-table allocation/publication and existing child-table translation.
- Child-walk callback dispatch for normal and safe traversal paths.
- Root allocation/translation dispatch.
- Top-level normal/safe visit-range walk dispatch.

At that checkpoint, strict x86_64 memory ownership was recorded as 96% -> 98%
for this batch. Post-+50 strict-core movement was +142 points. The broad x86_64 memory
dashboard row was already capped at 100%, so this does not add broad dashboard
percentage. C still owns the public ABI wrappers, actual allocation/free and
address-translation primitive bodies, page-table walk iteration callbacks,
broader page frees/RSS accounting, TLB primitive behavior, and fallback
scaffolding. Standalone readiness improves only slightly because default-path C
primitive bodies and fallback scaffolding still remain required.

Validation for this batch:

- `rustfmt kernel/rust/x86_memory_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- Logged Rust build:
  `/tmp/mckernel-clean-build-x86-visit-body-rust.log`
- Logged C fallback build:
  `/tmp/mckernel-clean-build-x86-visit-body-cfallback.log`
- Logged wrapper build-only validation:
  `/tmp/mckernel-clean-build-x86-visit-body-wrapper.log`
- Warning scan over those three logs found no `warning`/`warn:` matches.
- QEMU version, `/dev/kvm`, runner `--help`, and runner dry-run checks passed.
  No real guest boot ran.

Update 2026-05-26 x86 range-top sequencing
------------------------------------------------------------

Latest strict-core-only x86 batch moved clear/set range top-level
orchestration through Rust while preserving C ABI wrappers and C fallback
scaffolding:

- Clear-range bounds validation.
- TLB invalid-address array allocation/free callback sequencing.
- Clear-range argument field publication for address array, counters,
  effective free policy, memobj, and VM.
- Clear-range top-level `walk_pte_l4` dispatch.
- Final remote TLB flush callback dispatch.
- Set-range argument publication for page table, VM, physical base, attr,
  diff, pgshift, and VM range.
- Set-range top-level `walk_pte_l4` dispatch and walk-failure log/return
  shaping.

Strict x86_64 memory ownership was recorded as 93% -> 96% for this batch.
Post-+50 strict-core movement was +140 points at that checkpoint. The broad
x86_64 memory dashboard row was already capped at 100%, so this did not add
broad dashboard percentage. At that checkpoint, C still owned the public ABI
wrappers, actual allocation/free and TLB flush primitive bodies, page-table
visitor callbacks, lower-level walk callbacks, address-translation primitive
bodies, and fallback scaffolding. Standalone readiness improved only slightly:
default-path C primitive bodies and fallback scaffolding still remained
required.

Validation for this batch:

- `rustfmt kernel/rust/x86_memory_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- Logged Rust build:
  `/tmp/mckernel-clean-build-x86-range-top-rust.log`
- Logged C fallback build:
  `/tmp/mckernel-clean-build-x86-range-top-cfallback.log`
- Logged wrapper build-only validation:
  `/tmp/mckernel-clean-build-x86-range-top-wrapper.log`
- Warning scan over those three logs found no `warning`/`warn:` matches after
  fixing the recorded pointer-type warning.
- QEMU-safe checks: `qemu-system-x86_64 --version`, `ls -l /dev/kvm`,
  `scripts/qemu-mckernel-guest.sh --help`, and dry-run command construction.
- No real QEMU guest boot ran without a usable guest image.

Update 2026-05-25 x86 set-range level-body sequencing
------------------------------------------------------------

Latest strict-core-only x86 batch moved `set_range_l1/l2/l3/l4` leaf and
level body orchestration through Rust while preserving C ABI wrappers and C
fallback scaffolding:

- Leaf action dispatch for mapped/busy/error paths.
- Zeroed child page-table allocation and atomic page-table publication retry.
- Existing child-table translation through the physical-to-virtual callback.
- Child-walk callback dispatch and failure shaping.
- Large-page map/store/RSS routing.
- Unconsumed child page-table free sequencing.

Strict x86_64 memory ownership is now recorded as 90% -> 93% for this batch.
Post-+50 strict-core movement is now +137 points. The broad x86_64 memory
dashboard row was already capped at 100%, so this does not add broad dashboard
percentage. C still owns the public ABI wrappers, actual allocation/free and
address-translation primitive bodies, child page-table walk callbacks, RSS
primitive body, top-level `walk_pte_l4()` dispatch, and fallback scaffolding.
Standalone readiness is unchanged.

Validation for this batch:

- `rustfmt kernel/rust/x86_memory_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- Logged Rust build:
  `/tmp/mckernel-clean-build-x86-setrange-body-rust.log`
- Logged C fallback build:
  `/tmp/mckernel-clean-build-x86-setrange-body-cfallback.log`
- Logged wrapper build-only validation:
  `/tmp/mckernel-clean-build-x86-setrange-body-wrapper.log`
- Warning scan over those three logs found no `warning`/`warn:` matches.
- QEMU-safe checks: `qemu-system-x86_64 --version`, `ls -l /dev/kvm`,
  `scripts/qemu-mckernel-guest.sh --help`, and dry-run command construction.
- No real QEMU guest boot ran without a usable guest image.

Update 2026-05-25 x86 set-range side-effect sequencing
------------------------------------------------------------

Latest strict-core-only x86 batch moved set-range side-effect sequencing
through Rust while preserving C ABI wrappers and C fallback scaffolding:

- Existing-mapping conflict cleanup.
- Page-table allocation-failure cleanup.
- L1/L2/L3 map/store/RSS-accounting callback ordering.
- L2/L3 large-page success logging.
- Child-walk failure logging.

Strict x86_64 memory ownership is now recorded as 87% -> 90% for this batch.
Post-+50 strict-core movement is now +134 points. The broad x86_64 memory
dashboard row was already capped at 100%, so this does not add broad dashboard
percentage. C still owns the public ABI wrappers, actual `clear_range()`
primitive effects, page-table allocation/publication, `virt_to_phys()`/
`phys_to_virt()`, child page-table walk callbacks, RSS primitive body, leftover
page-table free behavior, and fallback scaffolding. Standalone readiness is
unchanged.

Validation for this batch:

- `rustfmt kernel/rust/x86_memory_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- Local `objdump` SIMD-pattern check after fixing the first failed run.
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- Logged Rust build:
  `/tmp/mckernel-clean-build-x86-setrange-effects-rust.log`
- Logged C fallback build:
  `/tmp/mckernel-clean-build-x86-setrange-effects-cfallback.log`
- Logged wrapper build-only validation:
  `/tmp/mckernel-clean-build-x86-setrange-effects-wrapper.log`
- Warning scan over those three logs found no `warning`/`warn:` matches.
- QEMU-safe checks: `qemu-system-x86_64 --version`, `ls -l /dev/kvm`,
  `scripts/qemu-mckernel-guest.sh --help`, and dry-run command construction.
- No real QEMU guest boot ran without a usable guest image.

Update 2026-05-25 x86 clear-range side-effect sequencing
------------------------------------------------------------

Latest strict-core-only x86 batch moved clear-range side-effect sequencing
through Rust while preserving C ABI wrappers and C fallback scaffolding:

- TLB flush-address queue mutation for clear-range paths.
- Old-PTE memobj-flush callback dispatch.
- Anonymous-page free/RSS-sub callback ordering.
- File-backed page-unmap/free/rusage-sub callback ordering.
- XPMEM keep classification and log routing.
- Full-span L2/L3 child page-table PTE-null/free sequencing.

Strict x86_64 memory ownership is now recorded as 84% -> 87% for this batch.
Post-+50 strict-core movement is now +131 points. The broad x86_64 memory
dashboard row was already capped at 100%, so this does not add broad dashboard
percentage. C still owns the public ABI wrappers, actual
`remote_flush_tlb_array_cpumask()`, `phys_to_virt()`,
`ihk_mc_free_pages_user()`, `page_unmap()`, RSS primitive bodies, page-table
walking callbacks, allocation/free primitive behavior, and fallback
scaffolding. Standalone readiness is unchanged.

Validation for this batch:

- `rustfmt kernel/rust/x86_memory_helpers.rs`
- `bash -n kernel/rust/tests/run_equivalence.sh`
- `kernel/rust/tests/run_equivalence.sh`
- `cmake --build /tmp/mckernel-rocky-rust --target mckernel.img ihk_ko
  ihk-smp-x86_64_ko mcctrl_ko -j2`
- Logged Rust build:
  `/tmp/mckernel-clean-build-x86-clear-effects-rust.log`
- Logged C fallback build:
  `/tmp/mckernel-clean-build-x86-clear-effects-cfallback.log`
- Logged wrapper build-only validation:
  `/tmp/mckernel-clean-build-x86-clear-effects-wrapper.log`
- Warning scan over those three logs found no `warning`/`warn:` matches.
- `git diff --check`
- `bash -n scripts/rocky-rust-validation.sh scripts/qemu-mckernel-guest.sh
  kernel/rust/tests/run_equivalence.sh`
- `scripts/rust-ownership-report.py --check-dashboard overview.txt
  --check-dashboard overview2.txt --top 5`
- QEMU tooling check: `qemu-system-x86_64 --version`, `/dev/kvm` presence, and
  `scripts/qemu-mckernel-guest.sh --dry-run --accel tcg`; no real guest boot
  ran because no usable Rocky/RHEL-family qcow2 guest image was available from
  unprivileged paths.

This is an area-weighted engineering estimate, not a line-count, and it is
deliberately lower than earlier helper-surface estimates. Full ownership still
includes mutation-heavy kernel bodies, x86_64 page-table code, process
lifetime code, syscall dispatch, scheduler/futex internals, IHK Linux host
modules, and user tools. The estimate moved this round because allocator
allocation/free orchestration, futex command dispatch, Process/VM add-range
orchestration, VM range-tree insertion, syscall ID/getresid bodies,
page-allocator add/zero/allocation/free-finish sequencing, futex wake orchestration, process/VM
decision logic, x86_64
page-attribute/page-size decisions, `mcstat` leaf behavior, `mcexec`
option/env-list, policy, dirent layout, and proc-task path
helpers, `mcexec` extra-environment allocation/copy/free and OMPI
memory-policy classification, saved stack-limit parsing/application, FLIB
affinity CPU-list shifting, LD_PRELOAD string construction, overlay fd path
preparation, and execveat path construction, `ihklib` CPU/memory/IKC parser,
cpuset.mems allocation, request-formatting helpers, packed-env parser,
create-os env parser, first-match env lookup, reserve-memory config parsing,
environment action classification, parsed-env cleanup, device path formatting,
IHK command integer/hex/status helpers, mcinspect status/action/path/option
helpers, eclair dump-section/path/GDB packet-format/remote-reply helpers,
crash extension label/path/command/address-translation helpers, IHK
Linux-module Rust object links for `ihk`, `ihk-smp-x86_64`, and `mcctrl`,
mcctrl control/IKC policy helpers, the larger IHK core/SMP module policy batch
for kmsg, monitor, freeze/thaw, ioctl permission, boot sizing, request
validation, dump sizing, panic-dump exclusion, x86 status mapping, IHK
page-allocator descriptor/order/mask/address helpers, extra SMP CPU-bound
checks, host-driver map/unmap range math, SMP kernel vtop/bootstrap math,
load-file/load-memory arithmetic, memory assignment policy, free-list
insertion/merge decisions, wait/debug decode, reserve-memory stop/leftover
policy, request granularity checks, partial-release policy, x86_64 trampoline
and identity-page-table sizing decisions, dump query/NMI/topology/cache
decisions, host-driver ioctl/debug/aux-call, device lifecycle validation,
read/write completion, mmap calculation helpers, IHK user command dispatch,
resource/get/set target selection, release-all handling, ihkosctl size and
interrupt parsing, and ihkmond facility/action/path/slot decisions now have
Rust ownership. Recent passes also moved eclair GDB remote command
classification, Hg/T/qThreadExtraInfo thread-id parsing, memory-read packet
parsing, and mcexec overlay/device/proc/sys path construction into Rust.
This pass moved more crash-extension command-output decisions into Rust,
including thread command-name basename selection, memory-range filtering,
PTE physical-address derivation, x86 sign extension, PTE flag formatting,
PTE coalescing, and lookup range skip/break decisions.
This pass added Rust ownership for crash PID/thread context parsing, PID
hash-bucket selection, boot-refresh comparison, PTE type classification,
page-table level shift selection, mcvtop address parsing, and kmsg wrap checks.
This pass added Rust ownership for x86_64 page-table root lifecycle:
`ihk_mc_pt_create()` allocation-callback orchestration, root zeroing,
kernel-half entry copy from `init_pt`, and `ihk_mc_pt_destroy()` root
kernel-half clearing plus destroy-recursion callback dispatch now route through
Rust with C fallbacks. C still owns the actual allocator/free primitives,
recursive child-table destruction below the root, page frees, TLB-sensitive
behavior, and user-copy.
This pass added Rust ownership for x86_64 page-table prepare-map
orchestration: `ihk_mc_pt_prepare_map()` initial page-table selection,
first-level L4 range traversal and present-entry short-circuiting, allocation
callback sequencing, L4 entry publication, last-level set-page loop sequencing,
and first error return propagation now route through Rust with C fallbacks. C
still owns the actual allocation primitive, `virt_to_phys()`, `__set_pt_page()`,
recursive child-table destruction, page frees, RSS/TLB-sensitive behavior,
rollback orchestration, and user-copy.
This pass added Rust ownership for exported x86_64 set-PTE body sequencing:
`ihk_mc_pt_set_pte()` page-size/value selection, alignment error
classification, log-callback selection, panic-callback dispatch for invalid
page sizes, PTE store invocation, and return shaping now route through Rust
with C fallbacks. C still owns the actual log/panic primitives, broader
page-table traversal, allocation/free primitives, recursive child-table
destruction, page frees, RSS/TLB-sensitive behavior, rollback orchestration,
and user-copy.
This pass also closed the currently scoped x86_64/Rocky ABI/layout foundation:
low-level rwlock, `kref`, rbtree augmentation callback, ftrace branch-data,
memobj ops/object state, SysV shared-memory limit/info/lock-user state, and
the XPMEM ID/hash/thread-group/segment/access-permit/partition/permission/
attachment object graph now have Rust/C compile-time layout mirrors. This is
prerequisite ownership, not full runtime ownership of XPMEM, SysV shm, memobj,
ftrace, rbtree, locking, allocation/free, user-copy, or object lifecycle
side effects.
This pass completed the active +50 continuation by moving the IHK host core
DMA request callback dispatcher into Rust: `ihk_dma_request()` now enters a
Rust-owned body for channel/ops validation, callback presence handling,
callback invocation, and no-callback `-EINVAL` shaping, while C keeps the
exported ABI wrapper and fallback.
This pass also added strict-core syscall ownership for `waitid()` SIGCHLD
siginfo copyout-body sequencing: Rust now constructs the zeroed `siginfo_t`,
fills `si_signo`, `si_code`, child PID/status and utime/stime jiffy fields,
and dispatches the copyout callback. C still owns `do_wait()` scanning,
wait/scheduler behavior, rusage production, the actual `copy_to_user()`
primitive, and fallback implementation.
This pass also added strict-core syscall ownership for `wait4()` wrapper
sequencing: Rust now validates options, zeroes `struct rusage`, invokes the
`do_wait()` callback with `WEXITED`, gates/copies status and rusage through the
copyout callback, and shapes the return. C still owns the syscall ABI wrapper,
argument extraction, `do_wait()` scanning/sleeping, wait locks, the actual
`copy_to_user()` primitive, scheduler behavior, and fallback implementation.
This pass also added strict-core syscall ownership for `waitid()` wrapper
sequencing: Rust now validates idtype-to-wait-pid and options, zeroes
`struct rusage`, invokes the `do_wait()` callback, propagates negative wait
results, dispatches siginfo copyout, and shapes the final return. C still owns
the syscall ABI wrapper, raw argument extraction, `do_wait()` scanning/sleeping,
wait locks, the actual `copy_to_user()` primitive, scheduler behavior, and
fallback implementation.
This pass also added strict-core syscall ownership for `wait_continued()` body
sequencing: Rust now publishes continued status, selects main-thread versus
report-thread signal-flag reap targets, dispatches the continued-signal reap
callback, preserves WNOWAIT through the reap helper, and returns process pid or
thread tid. C still owns the wait list scans, wait locks, caller-side duplicate
reap behavior, scheduler behavior, and fallback implementation.
This pass added Rust ownership for exported page-allocator allocation
sequencing: `ihk_numa_alloc_pages()` cache-hit logging selection,
direct-allocation logging selection, source-aware return shaping, and the
exported allocation handoff now route through Rust with C fallbacks. C still
owns CPU-local variable selection, actual cache/tree lock primitives,
`ihk_mc_alloc_pages()`, MCS/interrupt primitives, logging primitive bodies,
allocator lifetime, zeroed-list publication, deferred-zero worker timing, and
broader allocator mutation.
This pass added Rust ownership for exported page-allocator add/zero entry
sequencing: `ihk_numa_add_free_pages()` success/error log selection and return
shaping around the NUMA add-free mutation body, plus public
`ihk_numa_zero_free_pages()` dispatch into the Rust-owned zero-list publication
path, now route through Rust with C fallbacks. C still owns logging primitive
bodies, actual allocation/free and lock/interrupt primitives, allocator
lifetime, IKC send side effects, deferred-zero worker timing, and broader
allocator mutation.
This pass added Rust ownership for mcinspect DWARF size/offset/address form
classification, DWARF expression validation, structure/member/global tag-name
matching, thread marker/command-name selection, CPU-local address math,
sched-list thread derivation, and vtop process-selection helpers. It also
expanded mcstat Rust ownership to CLI delay/count parsing, mcos device-path
formatting, NUMA memory total/current/max aggregation, and corrected the Rust
monitor-status constants to match the IHK header.
This pass added Rust ownership for process VM-range split/join mutation
preflight: split high-half field shaping, low-half end commit, join adjacency
and object-offset validation, and surviving-range end commit. C still owns the
allocation/free, page-table split/clear/free calls, memobj refcounting, TOFU
hooks, rb-tree/cache mutation, and broader process lifetime behavior.
This pass added Rust ownership for the bitmap page allocator's public
locked-wrapper orchestration around allocation, reserve, free, count,
query-free, and zero-free operations. C still owns the actual MCS lock
primitive, free error panic/log policy, descriptor allocation, actual free
primitive, and broader allocator lifetime.
This pass added Rust ownership for descriptor destroy/free-callback
orchestration in the bitmap page allocator. C still owns descriptor
allocation, the actual `ihk_mc_free_pages()` implementation, and broader
allocator lifetime.
This pass added Rust ownership for process VM stack-growth range-start
alignment and commit mutation in the page-fault path. C still owns page-fault
handling, access checks, range lookup, locks, page allocation, page-table lookup
and mapping, retry behavior, and broader VM/process lifetime behavior.
This pass expanded eclair Rust ownership across static GDB response
construction, qSupported/qC/qAttached/target.xml/qTStatus/memory-map/thread-list
formatting, packet checksum parsing/summing, Ctrl-C command construction, and
register/memory byte hex formatting while keeping socket I/O, GDB process
orchestration, dump reads, and NMI side effects in C.
This pass moved the crash extension's `mcps` row formatting and `mcmem`
mapping-line formatting into Rust while keeping crash API reads, list/tree
walking, command orchestration, and fallback output in C.
This pass expanded crash Rust output ownership again: `mcps` and `mcmem`
headers, `mcmem` process lines, `mcvtop` headers, full PTE table rows, raw PTE
lines, lookup-failure text, and memory-range section markers are now built in
Rust with C fallbacks.
This pass expanded eclair Rust ownership again: simple GDB remote replies,
packet frame construction, banner and usage text, `/dev/mcosN` open-error text,
invalid physical-memory diagnostics, and symbol lookup-failure diagnostics are
now built in Rust with C fallbacks.
This pass expanded IHK SMP-module Rust ownership: full CPU/NUMA mapping
lookups, CPU-list request serialization, and IKC-map request serialization are
now Rust-owned in `ihk-smp-x86_64`, with the C side reduced to compatibility
wrappers and error logging.
This pass added another IHK SMP-module Rust slice: CPU request-array bounds
preflight for OS assign/release and device reserve/release paths, plus IKC
source/destination pair bounds preflight, now run through Rust before C performs
cpumask construction, CPU hotplug, IKC map mutation, and state changes.
This stabilization pass fixed the SMP CPU assign/release/reserve stack-frame
problem by moving the request logging string buffer out of those C compatibility
wrappers' stack frames and into heap allocation with shared cleanup. No new
Rust ownership percentage was counted for this fix; it preserves the validated
Rust SMP preflight path without pretending CPU hotplug/cpumask mutation has
already moved to Rust.
This pass expanded IHK SMP Rust ownership again: CPU-state transition
predicates, OS CPU ownership checks, IKC source/destination state checks,
reserve/release rollback-state filters, available-CPU query filters, CPU
mapping copy, and release-time CPU mapping compaction now live in Rust while
C still owns reset calls, hotplug sysfs writes, status mutation, locks,
copy_to_user/copy_from_user, and module lifecycle side effects.
This pass expanded IHK SMP Rust ownership through sysfs/topology helpers:
CPU-online sysfs path construction, CPU topology prefix construction, cache
topology prefix construction, sysfs string newline trimming, and sysfs long
parsing now live in Rust. C still owns file open/read/write, allocation,
topology list mutation, bitmap parsing, and hotplug side effects.
This pass expanded IHK module Rust ownership in `ihk` and `mcctrl`: the IHK
core page-allocator bitmap reserve, small-allocation scan, large-allocation
scan, and free-bit mutation are now Rust-owned while the Linux spinlock,
descriptor allocation, exported allocator ABI, and fallback wrappers remain in
C; mcctrl sysfs CPU/NUMA mapping lookups and local CPU-online bitset
construction are now Rust-owned while sysfs object creation, topology list
walking, cpumask translation, and kernel object lifetime remain C. A focused
IHK module helper smoke was added to `kernel/rust/tests/run_equivalence.sh`.
This pass added another small mcctrl sysfs Rust slice: read-buffer completion,
decimal long parsing, and PCI realpath prefix classification now live in Rust
helpers with C fallbacks; file open/readlink, sysfs creation, PCI directory
walking, and node-topology list selection remain C.
This pass wired the IHK module-load smoke into the Rocky validation wrapper and
validated Rust-linked load/unload for `ihk`, `ihk-smp-x86_64`, and `mcctrl`.
It also moved the mcctrl Tofu file-to-pidfd device-name buffer fill into Rust,
with C still owning allocation, hash/list locking, insertion/removal, and
lifetime.
This pass moved more IHK host-core traversal into Rust: reverse kmsg-buffer
lookup by OS index now runs through a Rust helper for OS boot and device kmsg
lookup, generic forward list-membership traversal now covers OS notifier
registration/deregistration, and generic next-entry cursor traversal now covers
OS notifier boot/shutdown callbacks, eventfd signaling traversal, and aux-call
handler-list traversal. C still owns spinlocks/semaphores, refcount increments,
returned-pointer assignment, callback invocation, eventfd signaling, user-copy,
error handling, and lifetime.
This pass also moved the bounded kmsg container atomic count operations into
Rust: set/read/inc/dec/dec-return now route through Rust atomics with C
fallbacks. At that point OS/device open refcount `cmpxchg` still remained
C-owned; the later open-refcount follow-up moved that boundary, while
container/page allocation, freeing, callbacks, user-copy, and broad lifetime
remain C-owned.
This pass added Rust compile-time layout mirrors for the IHK kmsg buffer,
kmsg buffer container, event entry, OS notifier ops/entry, aux-call handler,
and aux-call list structs touched by the new helpers. These assertions compile
into the IHK helper object and are part of ABI/layout foundation, not a claim
that runtime allocation or callback ownership moved.
This pass also moved the page allocator NUMA zeroing-worker atomic increment
into Rust. C still owns the zero-request decision context, IKC packet
construction/send, locks, interrupt masking, allocator lifetime, and broad
zeroing side effects.
This pass expanded `mcexec` Rust ownership across executable/interpreter
path-resolution policy: absolute-path and single-component classification,
filename length-limit checks, bounded path copy, root-prefix joins,
PATH/COKERNEL_PATH candidate joins, and altroot interpreter candidate
construction now live in Rust. C still owns environment lookup, PATH token
iteration, filesystem access/stat calls, diagnostics, and path lifetime.
This pass also made ownership reporting stricter and easier to gate: the report
now emits dashboard rows in functional-dashboard order, checks overview files
for duplicated dashboard rows, exports dashboard data in JSON, and supports
per-area `--fail-on-c-area` gates. A small follow-up `mcexec` path-formatting
slice moved `/proc/self/exe` fallback copies, relative `exec_path` joining,
RPATH `objdump` command construction, and RPATH libdir bounded copy through
Rust helpers. C still owns `readlink()`, `popen()`, `getline()`, ELF parsing,
and process execution.
This pass completed the requested Batch 2 ABI/layout checkpoint and then
expanded it into a larger lifecycle/helper batch. Rust mirrors and compile-time
assertions now cover process/thread lifetime structs, private `resource_set`,
`sig_common`, `sig_pending`, `mckfd`, `mcexec_tid`, wait/futex support structs,
public IHK monitor/rusage/register/resource descriptors, and private host Linux
device/OS/file objects compiled into the `ihk` module. The new code also moves
address-space release, clone VM/sighand branch decisions, initial CPU-set
fallback, mckfd duplicate/close decisions, process/thread refcount cleanup
gates, and IHK open/release/close/init/exit/minor-registration policies through
Rust helpers. The follow-up lifecycle batch moved process TID-table scans,
sigpending cleanup gates, optional ptrace/fp cleanup gates, IHK register-device
cleanup decisions, destroy-all-OS candidate/restore decisions, stray-kmsg trim
counts, event-list cleanup gates, destroy callback policy, and notifier
registration/deregistration policy into Rust helpers. The ABI/layout report
gate remains green with zero C implementation debt in that area.
The next McKernel-owned Process/VM follow-up moved process/thread list add-tail
and detach helpers through Rust for `chain_process()`, `chain_thread()`,
`ptrace_traceme()`, `release_process()`, `destroy_thread()`, and
initialization-only process list setup. Scheduler runqueue and migration queue
mutation remain C-owned for a scheduler-specific pass. The long-running target
was revised upward to a verified 65 percentage point aggregate gain across
McKernel-owned core sections during this session and has now landed at +68
points.
Subsequent McKernel-owned syscall follow-ups moved bounded wait/ptrace/process
list rewiring, pending-signal delivery and offloaded-interrupt classification,
ptrace wakeup/siginfo/eventmsg classification, ptrace request dispatch
classification, wait/reap result-shape classification, getrusage
dispatch/update classification, process-exit status/siginfo classification,
and terminate cleanup/reparent action classification through Rust helpers. A
contained mutation-heavy Process/VM follow-up also moved terminate child cleanup
delete-init and reparent/list-move mutation behind Rust helpers while C keeps
locks, release calls, ptrace detach, signal delivery, and memory cleanup. The
latest syscall follow-up moved clone parent/child TID gates, TLS source
selection, last-CPU placement, remote-spawn, and `CLONE_PARENT` pid1 fallback
result shaping through Rust helpers. The latest Process/VM mutation follow-up
moved clone report-thread, `ptrace_traceme()`, and `ptrace_attach_thread()`
report-list attach mutation through a Rust helper while C keeps lock and
lifetime ownership. A follow-up moved report-list detach/retarget and
main-thread ptrace-detach reparenting through Rust helpers as well. The next
cleanup pass reused those helpers for wait-thread reap and terminate cleanup,
and moved release-time `termsig` clearing through Rust. The following pass
moved ptrace detach state cleanup through Rust while C still frees the returned
debug-register pointer. The wait signal-flag reap mutation pass then moved
stopped/continued signal-flag clearing through Rust helpers, and the next pass
moved wait stopped exit-status reap mutation through Rust helpers. The
next pass moved ptrace wakeup saved-context clearing and syscall-trace flag
mutation through Rust helpers. Ptrace resume pending-signal take/clear now
routes through Rust helpers too, and ptrace detach signal-forward gates now
route through Rust syscall policy helpers. The final pass moved ptrace attach
main-thread reparent/list-add mutation through Rust while C keeps the existing
lock split. The long-running verified gain is now +68 points, exceeding the
65-point aggregate target; top-level syscall dispatch, ptrace handler bodies,
user-copy, locks, allocation, register/memory access, signal delivery, broader
child-list orchestration, rusage aggregation, and Linux forwarding remain
C-owned.
The latest +60 McKernel-owned lift moved eight more bounded surfaces through
Rust helpers: waitqueue entry initialization and wake scheduling predicates,
x86 early allocator alignment/exhaustion/next-pointer arithmetic,
page-allocator init end/count and destroy-page count helpers, fork-time
process VM/thread metadata copy, getpid/getppid/gettid/set_tid_address return
shaping, procfs cmdline/comm helpers, and timer ABI layout assertions. The
current honest distance to 100 is still substantial in mutation-heavy areas:
the page allocator row is 17 points away, Process/VM is now complete for the
current functional helper/body dashboard,
x86_64 memory is 9 points away, scheduler/timer/futex/wait internals are 4
points away, ABI/layout is 46 points away, and syscall core is 5 points away.
The latest verified slices toward the requested +135 campaign added 135
aggregate points: `timeval`/`rusage` layout assertions, futex key preparation,
syscall-offload scheduling decisions, x86 page-table index/walk/split-large
arithmetic, syscall requester/preempt and `mprotect` split/write-change
decisions, VM range-cache/lookup relation decisions, and page-allocator Linux
zero-request action selection, plus futex wake/requeue decision policy, x86
page-clear target selection, process VM remove-range preflight, and x86
page-table visit/free-range decision policy, plus getrusage TSC/timeval/rusage
result shaping, page-allocator CPU-cache action classification, and x86
change-attribute, set-range, and lookup-pte action classification, plus IHK
host core OS load-file dispatch/read-loop policy, shutdown status policy, kmsg
buffer initialization/clear mutation, kmsg container lifecycle field/pointer
mutation, locked list-add publication, locked list-del mutation, reverse
kmsg-buffer lookup traversal by OS index, generic list-membership traversal for
OS notifier registration, and generic next-entry cursor traversal for notifier,
eventfd, and aux-call paths, plus kmsg container atomic count set/read/inc/dec
and dec-return, plus IHK kmsg/event/notifier/aux-call layout assertions. The
latest page allocator follow-up moved the NUMA zeroing-worker atomic increment
through Rust, the scheduler/futex wake-mark follow-up moved futex wake-list
detach plus the `lock_ptr` clear through Rust, and the latest scheduler/futex
follow-up moved futex cross-bucket requeue list move plus `lock_ptr`
publication through Rust, queue-time futex waiter metadata publication through
Rust, and the latest scheduler/futex follow-up moved self-unqueue
priority-list detach through Rust. An earlier x86 memory follow-up moved
move-PTE fileoff rejection, destination calculation, and PTE phys/attr splitting
through Rust while C keeps the exchange, mapping, and TLB-sensitive work. The
latest x86 clear/free-range follow-up moved old-PTE classification and
flush/free/XPMEM/try-unmap action selection through Rust while C keeps
`xchg()`, page lookup, `page_unmap()`, actual frees, RSS updates, and remote
TLB flushes. The latest x86 set-range follow-up moved mapped physical-address
and final PTE value shaping through Rust for 4 KiB, 2 MiB, and 1 GiB mappings
while C keeps page-table allocation, publication, direct PTE writes, rollback,
RSS updates, and runtime map orchestration. The
latest x86 split-large-page follow-up moved source classification, child-map
physical derivation, page-table publish entry shaping, and source-unmap gate
selection through Rust while C keeps allocation, child entry writes,
`page_map()`, `page_unmap()`, RSS updates, PTE publication, remote TLB
flushing, and runtime split orchestration. The requested +135 campaign is now
met with verified code, but the full Rust port remains incomplete. The latest
post-campaign page allocator follow-up moved the NUMA zero-free dispatcher and
explicit-node/all-node traversal policy through Rust while C keeps caller
timing, locks, logging, CPU-local rb-tree mutation, interrupt masking,
allocator lifetime, IKC packet construction/send, and zero-request side
effects. The latest page allocator follow-ups moved deferred-zero IKC packet
field shaping, CPU-local cache rb-tree allocation/free helper entry points,
CPU-cache fast-path orchestration, NUMA allocation/direct-free lock-callback
orchestration, and deferred-free enqueue/zero-request orchestration through
Rust while C keeps send/channel selection/current-thread lookup, request
timing, interrupt masking, logging, actual MCS/interrupt primitives, allocator
lifetime, zeroed-list publication, and Linux zeroing side effects. An earlier x86 memory follow-up
moved direct PTE stores, atomic child page-table publication, PTE clear
exchange, and attribute-apply mutation through Rust helpers while C keeps
page-table allocation/free, walk orchestration, `page_map()`/`page_unmap()`,
physical frees, RSS updates, TLB flushing, user-copy, and broader runtime
mapping orchestration.
An earlier x86 memory follow-up moved normal and safe page-table walk
iteration, callback result folding, bounds-derived entry selection, and
optional physical-address skip policy through Rust helpers while C keeps
visitor callback execution, table allocation/free, `phys_to_virt()` traversal,
splitting, page/RSS accounting, page free side effects, mapping/free
orchestration, TLB-sensitive mutation, and user-copy.
The latest x86 virtual-to-physical follow-up moved per-level miss/walk/hit
decisions plus physical-address and page-size result shaping for 4 KiB, 2 MiB,
and 1 GiB entries through Rust helpers while C keeps page-table pointer
traversal, `phys_to_virt()`, fault handling, and all mapping side effects.
The latest IHK host-core follow-up moved OS/device exclusive-open refcount
compare-exchange mutation through Rust helpers while C keeps callback
invocation, file operation orchestration, device allocation, memory
registration, file I/O, waits/callbacks, IKC exchange mutation, and broad
object lifecycle.
The latest scheduler/futex follow-up moved futex wait entry/exit state
mutation and post-wait success/timeout/interrupt/retry classification through
Rust helpers while C keeps hash-bucket locking, queue orchestration,
`schedule()`/`schedule_timeout()`, signal detection, wakeups, timer queues,
context switching, key lifetime, race retry, and broader futex queue/list
mutation.
The latest scheduler/futex wake follow-up moved futex wake target
classification, Linux response-channel fallback selection, and
`SCD_MSG_FUTEX_WAKE` IKC packet field publication through Rust helpers while C
keeps queued-node detach, `ihk_ikc_send()`, `sched_wakeup_thread()`, channel
array access, wakeup side effects, IPIs, timer queues, context switching, key
lifetime, race retry, and broader futex queue/list mutation.
The latest mcctrl follow-up moved the Linux-side deferred-zero worker's
lockless `to_zero_list` pop, chunk payload clear, `zeroed_list` publish, and
zeroing-worker/page-count atomic updates through Rust helpers while C keeps the
debug wrapper, the non-Rust fallback, broader IKC exchange, allocation
lifetime, and module lifecycle side effects.
The latest scheduler/futex follow-up moved futex wait-queue bitset/requeue/UTI
initialization, key-region zeroing before `get_futex_key()`, and hash-bucket
lock-pointer publication through Rust helpers while C keeps hash-bucket
locking, user-value comparison, key reference lifetime, timeout and wake
orchestration, scheduler handoff, IKC sends, and address translation. The
latest x86 follow-up moved page-table teardown descend/skip policy and child
page-table physical-address extraction through Rust helpers while C keeps
destroy recursion, `phys_to_virt()`, frees, and broader page-table lifetime.
The latest page allocator follow-up moved deferred-free Linux zero-request
preparation into Rust while C keeps CPU-local lookup, channel selection,
IKC send side effects, and allocator lifetime.
The latest scheduler/futex follow-up moved futex double hash-bucket
lock/unlock ordering into Rust while C keeps the actual spinlock primitives,
protected list mutation, wakeups, IKC sends, scheduling, key lifetime, and
broader futex wake/requeue orchestration.
The earlier scheduler/futex wake-body follow-up moved `futex_wake()` bitset
validation, get-key callback dispatch, hash callback dispatch, lock callback
ordering, wake-scan invocation, unlock callback ordering, put-key callback
dispatch, and return shaping into Rust while C keeps the get-key bridge call
site, hash-futex bridge/global queue selection, actual spinlock primitives,
actual wake side effects, key-reference lifetime semantics, IKC sends,
scheduler handoff, user-value operations, and broader wake/requeue
orchestration.
The earlier scheduler/futex requeue-body follow-up moved `futex_requeue()`
source/target get-key dispatch, hash dispatch, double-lock ordering, optional
compare-value load sequencing, mismatch return shaping, scan invocation,
unlock ordering, moved-waiter key-reference drops, put-key dispatch, and
ret-or-task-count return shaping into Rust while C keeps the get-key/hash/
get-value bridges, actual spinlock primitives, wake/requeue side effects,
key-reference primitive bodies and lifetime semantics, user-value load
primitive body, IKC sends, scheduler handoff, and broader wake/requeue
orchestration.
The earlier scheduler/futex wake-op follow-up moved `futex_wake_op()` source/
target get-key dispatch, hash dispatch, double-lock ordering, in-user atomic
callback dispatch, non-EFAULT error shaping, private `-EFAULT` retry looping,
shared `-EFAULT` key-release/retry sequencing, wake-scan invocation, unlock
ordering, put-key dispatch, and wake-count return shaping into Rust while C
keeps the get-key/hash/atomic-op bridges, actual spinlock primitives, actual
in-user atomic primitive body, wake side effects, key-reference primitive
bodies and lifetime semantics, IKC sends, scheduler handoff, and broader
wake/requeue orchestration.
The latest scheduler/futex wait-entry follow-up moved `futex_wait()` entry
wrapper sequencing into Rust: bitset validation before profile mutation,
optional profile pause accounting before the wait body, wait-body callback
dispatch, optional profile resume timestamp publication, and final return
propagation. C keeps current-thread and CPU-local response selection, the
timestamp primitive, scheduler wait behavior, actual futex queue/wake side
effects, and fallback scaffolding.
The previous sysfs request-body follow-up moved public sysfs create, mkdir,
symlink, lookup, unlink, and setup request-body sequencing into Rust: IKC
request send callback dispatch, remote busy-wait polling with pause callbacks,
response read barrier callback sequencing, response error classification, and
mkdir/lookup handle publication. C keeps allocation/free, varargs path
formatting, special sysfs instance physical-address setup, the actual
`ihk_ikc_send()` primitive, logging, and fallback scaffolding.
The latest sysfs init-body follow-up moved `sysfs_init()` setup orchestration
into Rust: sysfs request layout-size validation, data-buffer size and pointer
publication, data-buffer allocation callback dispatch, setup-param allocation
callback dispatch, setup packet `buf_rpa`/`bufsize`/`busy` publication, setup
request-body dispatch, setup-param free callback sequencing, and stage/phase
error classification. C keeps the actual allocator/free primitives,
`virt_to_phys()`, `ihk_ikc_send()`, `cpu_pause()`/read-barrier primitives,
panic/logging, and fallback scaffolding.
The latest sysfs request-wrapper follow-up moved sysfs server show/store/release
wrapper sequencing into Rust: callback-pointer selection, response body
dispatch, callback-error/send-error/packet-error/debug log-event selection,
response ssize/error publication, and release callback routing. C keeps the
static wrapper entry/debug line, actual sysfs ops callbacks, actual
`ihk_ikc_send()`, printk/dkprintf/ekprintf primitives, sysfs data-buffer
lifetime, and fallback scaffolding.
The previous sysfs packet-handler wrapper follow-up moved public packet-handler
dispatch and unknown-message log-callback selection into Rust: request-kind
dispatch, show/store/release callback routing, kind publication, unknown log
callback selection, and return shaping. C keeps the exported IKC handler ABI,
the actual `kprintf()` primitive, callback bodies, and fallback scaffolding.
The earlier scheduler/futex wake-scan follow-up moved futex hash-bucket
wake-list scanning, key matching, optional bitset filtering, wake-limit
handling, and wake callback ordering through Rust helpers for `futex_wake()`
and `futex_wake_op()` while C keeps key lookup, locks, wake side effects,
IKC sends, scheduler handoff, user-value operations, and broader wake/requeue
orchestration.
The latest scheduler/futex requeue-loop follow-up moved futex source-list
scanning, key matching, wake-vs-requeue selection, task/drop count accounting,
and wake/requeue callback ordering through Rust helpers for `futex_requeue()`
while C keeps comparison-user-value loading, locks, wake side effects,
key-reference/key-copy mutation, IKC sends, scheduler handoff, user-value
operations, and broader wake/requeue orchestration.
The latest scheduler/futex get-key follow-up moved private/shared key
construction, translation retry, page-fault callback dispatch, failed-translation
logging, and key-reference callback dispatch into Rust while C keeps
current-thread/mm selection, address-translation and page-fault primitive
bodies, user-value operations, locks, scheduler handoff, and broader
wake/requeue orchestration.
The earlier scheduler/futex wait-setup follow-up moved key initialization,
get-key/queue-lock/user-value-load callback sequencing, mismatch/error cleanup,
key release, and hash-bucket publication through Rust helpers for
`futex_wait_setup()` while C keeps address translation, page fault behavior,
lock primitives, user-value load, key-reference lifetime,
timeout/schedule behavior, and broader futex wait/retry orchestration.
The latest x86 memory follow-up moved clear-range leaf/level/root body
sequencing into Rust helpers while C keeps table allocation/free,
old-entry page lookup/refcount primitive behavior,
`phys_to_virt()`/`virt_to_phys()` primitive behavior, TLB-sensitive mutation,
RSS/page accounting, and page free side effects.
The latest page allocator follow-up moved descriptor destroy/free-callback
orchestration into Rust while C keeps descriptor allocation, the actual
`ihk_mc_free_pages()` behavior, logging, CPU-local selection, IKC sends, and
broader allocator lifetime.

Completion Dashboard
------------------------------------------------------------

Area                                      Rust %   Whole-tree status
----                                      ------   -----------------
Rust build/link foundation                   95   Rust object builds and links into mckernel.img on the Rocky x86_64 path; user tools link Rust helper objects; IHK Linux-module Kbuild can now link prebuilt Rust objects for `ihk`, `ihk-smp-x86_64`, and `mcctrl`; C fallback remains available
ABI/layout foundation                       100   Loader, IKC, primitive, page, memobj, memobj ops, SysV shm objects/limits/lock-user state, resource_set, address-space, process-hash, VM range, VM region, process_vm, process/thread lifetime, signal/mckfd/mcexec_tid, wait/futex support, timer, x86 signal action/altstack/siginfo/signalfd layouts, x86 CPU-local page and kernel-context layouts, x86 TLS user descriptor layout, ptrace user register/fpreg/user layouts, x86 descriptor/TSS/fxsave/xsave/YMM/LWP/bound-register save-state layouts, futex hash-bucket/key/queue layouts, syscall/init/post, procfs, coredump coretable, ELF core headers/notes/prstatus/prpsinfo, `iovec`, sysinfo, time-of-day, interval-timer and profile-event layouts, CPU mapping, perf, UTI, move-pages SMP request layouts, sysfs create/mkdir/symlink/lookup/unlink/setup request layouts, sysfs ops/handle/bitmap layouts, low-level rwlock, `kref`, rbtree augment callback, ftrace branch-data, timeval/rusage, pager create/map result packets, McKernel memory area/node/page-allocator ops/TLB-flush/page-cache headers, CPU-local/kmalloc/backlog/SMP-call layouts, XPMEM ID/hash/thread-group/segment/access-permit/partition/permission/attachment object graph, IHK monitor/register/resource, host Linux device/OS/file, and IHK kmsg/event/notifier/aux-call layouts are checked for the currently scoped x86_64/Rocky ABI foundation; mutation-heavy runtime bodies still need Rust ownership
Shared primitives                           100   rbtree, llist, plist, waitq init/entry/list core, wake scheduling predicate, string/memory leaves, bitops, bitmap, numeric parsing including `skip_atoi()` format width/precision pointer advancement, `number()` sign/prefix/width/precision/padding/output orchestration for `vsnprintf()` numeric and pointer paths, `format_decode()` parsing of flags, width, precision, qualifiers, and conversion types, `string()` output formatting for `%s`, base-10 decimal formatting digit emission for `vsnprintf()`, the main `vsnprintf()` loop orchestration through Rust callbacks, scoped `%p` pointer formatting for null/default/%pS fallback in the current non-`CONFIG_KALLSYMS` path, and region helpers are Rust-owned
x86_64 memory management                    100   Policy/query helpers, common VR-flag to page-attribute conversion, x86 page-attribute translation, PTE value shaping, page-size PTE validation/value selection, page-table index calculation, walk-bound calculation, normal/safe page-table walk iteration, callback result folding, normal/safe visit-PTE leaf/level/root body sequencing and top-level visit-range dispatch, optional physical-address skip policy, virtual-to-physical per-level miss/walk/hit decisions plus full walk orchestration and physical/size result shaping, split-large-page preparation/entry arithmetic, split-large-page source classification, child-map physical derivation, page-table publish entry shaping, source-unmap gate selection, PTE direct-store helpers, atomic child-table publication helpers, PTE clear-exchange helpers, attribute-apply mutation helpers, page-clear target selection plus regular/large-page traversal and PTE-zero mutation, page-table visit/direct-walk decision policy, clear/free-range validation and large-entry action selection, clear-range old-PTE phys/fileoff/dirty classification plus flush/free/unmap action selection, clear-range leaf/level/root body sequencing, clear/set range top-level validation/allocation/free/walk/flush orchestration, change-attribute leaf/large/walk action selection plus change-attribute range traversal and PTE attribute mutation, set-range leaf/direct-large/allocate/busy/walk action selection, set-range mapped physical/PTE value shaping for 4 KiB/2 MiB/1 GiB entries, set-range conflict cleanup, allocation-failure cleanup, map/store/RSS accounting, child-walk failure shaping, and leaf/level body orchestration, lookup-pte default size/level hit/walk/miss/shape decisions plus full `lookup_pte()` body orchestration, zeroed page-table allocation helper, get-PTE allocation traversal, `__set_pt_page()` regular/large-page mapping body sequencing, kernel-address lock classification, virtual-to-pagemap result shaping, fault-to-page-attribute dirty-bit shaping, move-PTE fileoff preflight/destination shaping/PTE phys-attr splitting, page-table destroy descend/skip policy, destroy child-table physical extraction, recursive page-table destroy/free-callback orchestration below the root, page-table root create/destroy-root lifecycle orchestration, page-table prepare-map first-level allocation/publish and last-level set-page callback orchestration, exported set-PTE body sequencing/log/panic/store orchestration, page refcount/hash lifecycle bodies, `page_map()` public inline body routing and count increments, page predicate public inline body routing, locked `page_unmap()` count/list deletion and lock orchestration, locked `phys_to_page()` lookup orchestration, locked `phys_to_page_insert_hash()` lookup/allocation-callback/insert orchestration, locked page-hash count-all traversal, phys-to-page hash traversal, hash insertion initialization, page-hash bucket init/count, page-hash table initialization/count-pages body routing, pending-free public wrapper panic routing, pending-free public wrapper pending-list routing, reserve-pages public wrapper panic/error routing, mckernel free-pages public wrapper pending-list routing, smaller-page-size selection, early allocator arithmetic/exhaustion checks, process-VM verification/page-fault/copy loop sequencing, mapped versus direct physical-copy selection, byte-copy sequencing, scalar user get/set wrappers, user string length/copy wrappers, dump-page completion/address-check/bitmap marking/query sequencing, and dump user-page process-hash traversal/page-table visitor dispatch are Rust-owned; underlying page-table page allocation/free primitives, old-entry page lookup/refcount primitive behavior, `phys_to_virt()`/`virt_to_phys()` primitive behavior, page-fault primitive behavior, map/unmap primitive bodies, callback execution, actual page frees, RSS updates, mapping/free rollback orchestration, page-hash spinlock/allocation primitives and logging, TLB-sensitive primitive behavior, current-thread/resource-set lookup, and fallback scaffolding are still C
Page allocator                              100   Several allocator helper bodies are Rust-owned, including `__ihk_pagealloc_init()` layout/allocation-callback/descriptor-init/lock-callback/tail-reservation orchestration, init layout/end/count calculation, descriptor zeroing and descriptor field initialization, tail-map reservation, destroy-page count and descriptor destroy/free-callback orchestration, public bitmap `ihk_pagealloc_*()` wrapper sequencing for init failure logging, default init, destroy free-callback dispatch, alloc/reserve/free/count/query dispatch, double-free error routing, and zero-free begin/done logging, bitmap public locked-wrapper orchestration for alloc/reserve/free/count/query/zero-free, exported add-free log/error/return sequencing, NUMA add-free, public zero-free wrapper dispatch, NUMA zero-free dispatcher/all-node traversal, zeroing-worker atomic increment, exported `ihk_numa_alloc_pages()` top-level allocation body sequencing, main `ihk_numa_alloc_pages()` cache-first/source-selection/fallback-to-NUMA orchestration, exported `ihk_numa_free_pages()` CPU-cache/direct/deferred top-level body sequencing, main `ihk_numa_free_pages()` direct-versus-deferred free orchestration and post-action completion sequencing, `___ihk_mc_alloc_aligned_pages_node()` pa-ops/early-allocation dispatch, `___ihk_mc_alloc_pages()` default alignment/node/virtual-address shaping, `___ihk_mc_free_pages()` pa-ops free dispatch, pending-free begin/enqueue/finish list mutation and callback sequencing, rbtree free-in-allocator memory-chunk containment/free/rusage-sub sequencing, `mckernel_allocate_aligned_pages_node()` explicit-node allocation, memory-policy mask/distance selection, interleave retry/update, distance fallback, order fallback allocation-loop orchestration, `numa_init()` node discovery/node-field publication/memory-chunk ingestion/early-heap adjustment/free-page-or-pagealloc dispatch/rusage accounting/final-node logging sequencing, NUMA allocation-attempt OOM/bounds/callback dispatch, distance-id lookup, distance-ordered NUMA node selection, main NUMA allocation lock-callback orchestration, direct free-to-tree lock-callback orchestration, deferred-free enqueue/zero-request orchestration, CPU-local cache action classification, CPU-local cache rb-tree allocation/free helper entry points, CPU-local cache alloc/free fast-path orchestration with interrupt callback ordering and result classification, CPU-local storage initialization, CPU-local variable selection, normal preempt counter updates, kmalloc chunk header initialization, sorted kmalloc free-list insertion, adjacent free-chunk consolidation, low-level `___kmalloc()` size-alignment/free-list fit/split/refill/page-allocation-callback/interrupt-callback/return sequencing, low-level `___kfree()` null/corruption/current-CPU free-list insertion-consolidation/remote-CPU lock-list publication/unlock/error sequencing, kmalloc remote free-list move/consolidation sequencing, public `_kmalloc()`/`_kfree()` memdebug tracking wrapper sequencing, public `_ihk_mc_alloc_aligned_pages_node()`/`_ihk_mc_free_pages()` pagealloc memdebug tracking wrapper sequencing, pagealloc/kmalloc tracking hash initialization, pagealloc/kmalloc memcheck leak-scan traversal, runcount mutation, leak-log callback sequencing, `ihk_mc_set_page_allocator()` tracking-init/early-invalidate/`pa_ops` publication, top-level `mem_init()` allocator/memory lifecycle sequencing, NUMA-distance allocation/fill/sort/log sequencing, Linux zero-request action selection, deferred-zero IKC packet field shaping, Linux zero-request preparation current/idle/nohost/worker/pid checks plus packet fill and worker increment, and deferred-zero send/log callback sequencing; low-level page allocation/free primitive bodies, MCS/interrupt primitives, IKC channel lookup/current-thread lookup, current-node primitive lookup, actual IKC send side effects, logging/panic callbacks, deferred-zero worker timing, FUGAKU debug preempt path, and broader allocator lifetime remain C
Process/VM management                       100   Wait/clone/ptrace policy helpers plus clone VM/sighand sharing branches, fork-time VM/thread metadata copy, VM range bounds, split-alignment, extend-up, protection-delta, access-check decisions, range-cache lookup relation plus range-cache replace/store mutation for join, free, and lookup paths, full VM range lookup traversal/cache publication, next/previous range iteration bodies, post-bounds `add_process_memory_range()` orchestration including VM range object initialization, mapping-action selection, insert/update failure cleanup, returned-range publication, and VM range-tree insertion traversal/link/color mutation, VM range end/flag commit mutation for extend-up and protection-change paths, stack-growth range-start alignment/commit mutation for the page-fault path, page-fault PTE lookup retry/log sequencing, `lookup_node()` page-fault/PTE-present/phys-to-node return sequencing, remove-range split/free preflight, remove-process-memory-range straight-map conversion/range-iteration/split/XPMEM/free dispatch sequencing, `free_process_memory_range()` page-table free/clear lock/memobj/finalize orchestration, split-range high-half field shaping plus low-half commit and publish sequencing, join-range adjacency/object-offset validation plus rb-erase/cache/free sequencing, free-range page-table action/span planning plus final detach/straight-cleanup sequencing, range split/join/finalize/remove-region callback gate routing, straight-range removal conversion, process memory-range drain traversal/callback/log sequencing, mckfd close iteration and drain/free list sequencing, VM NUMA-policy rb-tree drain/free sequencing, release-time address-space detach/proc->vm clear/process release sequencing, create-thread CPU-set selection/default publication body routing, allocated process/VM/resource/idle object zeroing body routing, process-VM initialization body routing, process init state/parent-inheritance body routing, process init list/lock/waitq/refcount body routing, process init profile state body routing, process clone thread base-state copy body routing, process clone thread scheduler state copy body routing, process create/clone thread link/default-scheduler/exit/spin-sleep state body routing, process clone/fork saved-command-line and VM-policy copy body routing, process clone shared-VM/sigcommon share/action-copy body routing, process clone user-context/profile/on-fork-VM/McKernel-fd copy body routing, process copy-user-range metadata/PTE-args init body routing, process copy-user-PTE buffer copy/zero body routing, process update-page-table attr/lock/set-range/log body routing, process split shared-memobj page-update body routing, process split-range allocation/init body routing, process split-range publish body routing, default VM-range memobj ref/unref direct routing, default free-memory-range memobj ref/unref direct routing, process sync-memory-range lock/memobj/visit/log body routing, process remap-memory-range lock/memobj/pgshift/visit/log body routing, process invalidate-memory-range boundary-split/free-or-visit/log body routing, process per-page invalidate PTE/fileoff/exchange/flush/contiguous/memobj-log body routing, page_map public inline body routing, page predicate public inline body routing, page_unmap public result/log body routing, phys_to_page public lookup/insert body routing, process split page-table split dispatch body routing, process add-range public bounds/body routing, process range lookup/iteration/extend public wrapper routing, process update-page-table public wrapper routing, process change-protection public wrapper routing, process CPU-set public wrapper routing, thread allocation zero/refcount/hash-list/siblings-list initialization body routing, thread sigstack disable body routing, sigcommon/thread-sigpending initialization body routing, create/chain side-effect callback gate routing, create-thread/fork cleanup callback gate routing, clone/fork error-unwind callback gate routing, destroy-thread optional ptrace/fp/coredump cleanup sequencing, destroy-thread CPU-set/sigpending/page-free cleanup callback gate routing, sigcommon release-body sequencing, TID release/replace body sequencing, release_thread ref/profile/procfs/destroy/VM-release sequencing, release_process_vm ref/mckfd/free-callback/TLB/free-ranges/detach/policy-drain/final-free sequencing, address-space release, address-space PID detach mutation, CPU-set fallback, mckfd duplicate/close decisions, mckfd push/pop-head mutation, TID-table scan/index decisions, TID slot release/replace writes, sigpending list pop/unlink, process/thread list add/detach helpers, terminate child cleanup list unlink/reparent mutation helpers, thread report-list attach/detach mutation helpers, ptrace main-thread attach/detach reparent helpers, ptrace detach/wakeup state and pending-signal cleanup helpers, wait signal-flag and exit-status reap mutation helpers, terminate/wait report-thread release cleanup helpers, optional ptrace/fp cleanup gates, wait-zombie child rusage aggregation/reparent/list-detach/list-attach/release sequencing, do-wait child/report-thread traversal plus empty-scan sequencing, ptrace-detach reparent/report/cleanup/wakeup/release/finalize sequencing, and lifecycle/refcount/free-callback/TID-release policy predicates are Rust-owned; broader process lifetime mutation, allocation/free primitive callbacks, thread page allocation/free primitive callbacks, create-thread CPU-info/log callback primitives, sigcommon allocator/rwlock primitive callbacks, signal-delivery primitives, create/chain CPU-set primitive side effects and remaining non-process refcount primitive side effects, create-thread/fork cleanup lock/free primitive side effects, clone/fork unwind lock/free/page-table primitive side effects, destroy-thread hash/rusage/lifetime mutation, other child-list/lifetime traversal, VM range allocation primitive callbacks, split/XPMEM primitive callbacks, TOFU split/merge primitive behavior, page-fault range-resolution primitive behavior, page-table lookup/allocation/mapping/free/clear side effects, page-table attribute mutation/free/clear, remaining wait primitive behavior, broad rusage traversal, remaining non-process atomic refcount primitive bodies, and lock/free primitive bodies remain C
Syscall core                                100   Many syscall validation/policy helpers, credential-refresh forwarding gates, direct ID and uid/gid leaf bodies, kill/tkill/tgkill siginfo shaping and do-kill callback sequencing, forwarded uid/gid setter Linux-forward/credential-refresh sequencing, getresuid/getresgid ordered field-read and user-copy callback sequencing, mckfd lookup/dispatch and close sequencing, memory-policy syscall bodies including `get_mempolicy()`, `set_mempolicy()`, and `mbind()`, `mprotect` split/write-change decisions, signal/time body sequencing, scheduler syscall body sequencing, top-level `do_wait()` traversal/result sequencing, bounded wait/ptrace/termination/signal list rewiring, ptrace request/event/register/regset sequencing, wait/reap result-shape classification, process-exit status/siginfo classification, terminate cleanup/reparent action classification, sync-child/perf-read body routing, process cleanup fd/body routing, clone spawn/TID/TLS/reparent result shaping, ptrace detach signal-forward gates, `ptrace_detach_thread()` main-body sequencing, and top-level `ptrace()` request/callback body dispatch are Rust-owned; top-level dispatch, argument extraction, actual user-copy primitive, Linux forwarding callback bodies, lock and CPU-interrupt primitive bodies, allocation/free primitives, architecture fp/register/regset primitives, actual process-memory read/patch primitives, perf-counter/event-update and atomic64 primitive behavior, low-level TOFU cleanup/kfree primitive behavior, signal-delivery primitive behavior, waitqueue/schedule/signal primitive behavior, scheduler migration side-effect primitive bodies, broader scheduler/time primitive bodies, ptrace lookup/orchestration, and other primitive C callback implementations remain C
Scheduler/timers/wait/futex                 100   Scheduler/futex/time policy helpers, scheduler set/get-param, set/get-scheduler, RR-interval, set-affinity, and get-affinity syscall sequencing, scheduler migration-request body sequencing for waitq setup, request queue publication, target runqueue flag/status mutation, remote interrupt decision, log callback dispatch, schedule handoff, and waitq finish, scheduler migration-completion body sequencing for migration queue traversal, request detach/ack gates, affinity target selection, runqueue detach/add with length mutation, thread CPU update, same-VM sibling scan, address-space CPU-set update, target reschedule flag publication, interrupt dispatch, waitqueue wake, and lock ordering, timer spin-sleep/runqueue/remaining-time arithmetic, `init_timers()` list/lock initialization sequencing, `schedule_timeout()` spin-wake/runqueue/schedule-callback/spin-loop/timeout-expiry sequencing, `wake_timers_loop()` timer tick/decrement/detach/log/wakeup sequencing, `set_timer()` runqueue scan and LAPIC enable/disable decision sequencing, local interval-timer current-value subtraction, elapsed reset, enabled-state publication, and set-timer callback dispatch, futex hash-table allocation-callback/pointer-publication/table-initialization orchestration, futex hash-bucket selection with hash callback, top-level futex command dispatch/private/realtime decode, clock-realtime rejection, wait/wake/requeue/wake-op callback selection, invalid-command callback routing, futex hash-bucket table lock/list initialization, futex key matching/key preparation plus wake/requeue decision policy, futex `get_futex_key()` private/shared key construction, translation retry, fault logging, and key-reference callback dispatch, futex `futex_wake()` get-key/hash/lock/scan/unlock/put-key return orchestration, futex `futex_requeue()` get-key/hash/double-lock/optional-compare/scan/drop-key/put-key return orchestration, futex `futex_wake_op()` get-key/hash/double-lock/atomic-op retry/wake-scan/put-key return orchestration, futex `futex_wait()` entry/profile pause/resume sequencing, futex double hash-bucket lock/unlock ordering, futex wake-list scan/key-match/bitset/limit orchestration, futex wake target orchestration, futex requeue source-list scan/key-match/wake-vs-requeue/drop-count orchestration, futex requeue key-reference callback and key-copy publication, futex wait-setup key-init/get-key/queue-lock/get-value/mismatch cleanup orchestration, futex wake-list detach and lock-pointer clear, futex requeue list move and lock-pointer publication, futex waiter plist initialization/insertion, futex wait-queue bitset/requeue/UTI initialization, futex key-region zeroing, futex hash-bucket lock-pointer publication, futex waiter metadata publication, futex `queue_me()` waiter enqueue/metadata/unlock orchestration, futex `unqueue_me()` lock-pointer race retry/detach/drop-ref sequencing, futex `futex_wait_queue_me()` state/spin-sleep/queue/schedule/log/finish sequencing, futex `futex_wait()` setup/wait/unqueue/post-action/retry/key-release orchestration, futex self-unqueue list detach, futex wait-side bitset validation, futex wait scheduling action classification, futex wait-state status/spin-sleep mutation, futex post-wait success/timeout/interrupt/retry classification, futex wake target classification, Linux response-channel fallback selection, futex wake IKC packet field publication, syscall-offload scheduling decisions, waitq init/entry/list core, wake scheduling predicate, bounded scheduler runqueue/migration list rewiring, and paired runqueue length updates are Rust-owned; futex allocation/hash primitive callbacks, rdtsc/pause primitive behavior, LAPIC timer primitive behavior, waitqueue wake primitive behavior, remaining futex queue/requeue/wait side effects, IKC send and scheduler wake primitives, IPIs, context switching, current-thread/mm selection, address-translation/page-fault callback bodies, key-reference implementation/lifetime, user-value load/comparison/atomic-op primitive bodies, migration primitive callbacks, `schedule()` primitive behavior, and actual lock primitives remain C
procfs/sysfs/xpmem/file objects             100   Decision-helper surface is Rust-owned, including XPMEM `open()`/`openat()` init/forward/allocation/mckfd publication/list-link/open-count sequencing, XPMEM `close()`/`dup()`/flush lifecycle sequencing, XPMEM release/remove wrapper sequencing, XPMEM AP hash-bucket release-drain sequencing, XPMEM thread-group destroy dispatch, XPMEM access-permit release lifecycle sequencing, XPMEM segment-removal lifecycle sequencing, XPMEM thread-group segment-list drain sequencing, public XPMEM detach wrapper sequencing, `xpmem_vm_munmap()` begin/remove/finish sequencing, internal `xpmem_detach_att()` sequencing, XPMEM clear-PTE wrapper/range/AP/attachment traversal and unpin/lookup/munmap/VALIDPTE sequencing, `xpmem_remove_process_memory_range()` null/destroying/full-detach/trim/split sequencing, live mckfd lookup/dispatch and close unlink/callback/free sequencing, procfs/sysfs request/data-production sequencing, and per-PID procfs output sequencing; actual allocation/free primitives outside covered mckfd allocation/free-callback dispatch, reference mutation, the raw IKC send primitive, page I/O, remove-range/free-range/page-table primitive behavior, procfs request mapping/lookup/lock primitive behavior, remaining sysfs buffer mutation, remap, and page-table mutation remain C
host/IKC/mcctrl/IHK modules                 100   Rust Linux-module build/link integration is active on Rocky x86_64 for all three host/module targets and Rust-linked module-load smoke now passes: `ihk` links `rust/core_helpers.o` for page-allocator bitmap math, page-allocator descriptor sizing, descriptor-order calculation, allocation classification, allocator mask/address/block math, allocator padding reservation, small/large bitmap allocation scan, allocator free-bit mutation, free-path address conversion, host-driver map/unmap range math, open-path validation predicates, OS/device exclusive-open refcount compare-exchange mutation, monitor/rusage page rounding, reserve-CPU count validation, kmsg ring length/shift decisions, kmsg buffer initialization/clear mutation, kmsg container lifecycle field/pointer mutation, kmsg container atomic count set/read/inc/dec/dec-return, locked list-add/delete mutation for kmsg buffers/events/aux-call handlers/notifiers, reverse kmsg-buffer lookup traversal by OS index, generic forward list-membership traversal for OS notifier registration, generic next-entry cursor traversal for notifier callback, eventfd, aux-call paths, DMA request callback dispatch, stray-kmsg trim count, release precheck/delete policy, open/refcount/callback policy, minor-slot registration policy, register-device allocation/cdev/device-create cleanup policy, release/close/init/exit/destroy callback presence/failure policy, destroy-all-OS candidate/restore decisions, event-list cleanup gate, notifier match/add/remove policy, OS load-file dispatch/read-loop policy, OS shutdown status policy, hung-monitor status classification, freeze/thaw policy, read-kaddr mode selection, OS ioctl permission/debug/aux-call classification, device destroy/detect-hungup validation, destroy pointer/refcount policy, read/write copy completion math, and mmap VMA/refcount/physical-offset decisions; kernel host Rust helpers now own remote page-fault answer packet/send-callback sequencing, remote page-fault body err/profile/log/preempt/page-fault/answer sequencing, remote page-fault request lookup/missing/current/deferred-copy/backlog/wakeup/unlock sequencing, host schedule-process thread/proc validation, CPU-selection, TID/status publication, chain, runqueue-add, and log/error sequencing, traditional host SCD ACK packet/send-callback sequencing for prepare-process, send-signal, cleanup-process, and cleanup-fd responses, host prepare-process freeze-gate, descriptor map/span validation, mapping, validation, clone, main-thread creation, process/thread/VM metadata publication, NUMA policy publication, prepare-ranges dispatch, success cleanup/unmap/TLB flush, and failure destroy/free/unmap return shaping, host perf ACK / CPU register response packet sequencing, host perf-control dispatch/mode/callback/read-value sequencing, host perf-control request map/dispatch/unmap/ACK sequencing, CPU register request map/register-callback/unmap/response sequencing, host send-signal map/copy/unmap/ACK/do-kill/log sequencing, host wake-syscall-thread find/log/wakeup/unlock sequencing, host debug-log print/debug callback sequencing, host cleanup-process cleanup/reply/terminate plus cleanup-fd cleanup/log/reply sequencing, host IKC2Linux/IKC2McKernel initialization body sequencing, top-level syscall packet-handler wrapper sequencing, dummy packet-handler release sequencing, and public host IKC init panic-on-error wrapper sequencing; `ihk-smp-x86_64` links `rust/smp_driver_helpers.o` for dump bitmap math, request validation, LWK-to-Linux index bounds, full CPU/NUMA mapping lookups, CPU-list request serialization, IKC-map request serialization, CPU request-array bounds preflight, IKC source/destination pair bounds preflight, CPU-state transition predicates, CPU mapping copy/release compaction, CPU sysfs path construction, CPU/cache topology prefix construction, sysfs string newline trimming, sysfs long parsing, reserve/release CPU bounds, boot busy/status gates, boot/dump page sizing, CPU-count validation, panic dump-exclusion policy, x86 built-in status mapping, monitor panic classification, freeze/frozen fold decisions, kernel vtop/bootstrap math, load-file/load-memory arithmetic, memory assignment policy, free-list insertion/merge decisions, wait/debug decode, reserve-memory stop/leftover/granularity decisions, partial-release policy, trampoline page usability, identity-page-table sizing/order/mapping decisions, dump query memory-area sizing/chunk fitting/map math, dump NMI command policy, interrupt CPU validation, topology snprintf overflow checks, and cache index/size decisions; `mcctrl` links `rust/mcctrl_helpers.o` for binfmt/path whitelist decisions, pager path/fs/device decisions, procfs mcos/pid/tid name formatting, futex command/private/realtime decode plus labels, control permission/partition/release arithmetic, IKC timeout/free/release/cpu/init validation policy, sysfs CPU/NUMA mapping lookup helpers, local CPU-online bitset construction, sysfs read-buffer completion, sysfs decimal long parsing, PCI realpath prefix classification, Tofu file-to-pidfd device-name buffer mutation, and deferred-zero worker list pop, payload clear, zeroed-list publish, and atomic counter updates; public Linux-module and private host Linux object layout assertions now compile into `ihk`; device allocation, memory registration, file I/O, waits/callbacks beyond the bounded DMA dispatcher, remote-page-fault allocation/copy/thread-field publication/scheduler primitive bodies and page-fault primitive behavior, CPU-set test and clone-CPU primitive behavior, status/chain/runqueue primitive mutations, prepare-process mapping/thread/range primitive side effects, cleanup/signal-delivery/perf/register/thread-wakeup/debug primitive side effects, allocation/connect/delay/current-channel/regular-channel primitive bodies, raw IKC send side effects, broader IKC exchange mutation, broad allocation/lifetime ownership, and kernel object lifecycle mutation remain C-owned/pending
User tools                                   83   mcstat, mcexec, ihklib, ihkconfig, ihkosctl, ihkmond, mcinspect, eclair, and the crash extension now link Rust helper objects for monitor labels, OS-status labels, mcstat CLI delay/count parsing, mcstat mcos device-path formatting, mcstat NUMA memory total/current/max aggregation, mcexec option integer/flag/mcos-id parsing, extra-env list count/search/allocation/copy/free, byte-size parsing, stack/heap/thread policy, saved stack-limit env parsing/application, memory-policy flags, OMPI memory-policy classification, FLIB affinity CPU-list shifting, LD_PRELOAD string construction, overlay fd path preparation, execveat path construction, getdents dirent field layout, proc-task path classification, mcos device and proc/self-fd path formatting, relative fd/cwd path joining, /dev/xpmem overlay, UTI library redirection, proc/sys overlay path construction, overlay path normalization, proc-task blacklist path construction, CPU-list parsing, memory-list parsing, IKC-map parsing, cpuset.mems parsing/allocation, request-to-string formatting, meminfo path formatting, packed-env parsing, create-os env parsing, first-match env lookup, reserve-memory config parsing, environment action classification, parsed-env cleanup, generic command integer/hex parsing, device path formatting, IHK OS-status labels, ihkconfig/ihkosctl command dispatch, get/resource/set target classification, release-all detection, ihkosctl allocation-size parsing and interrupt argument packing, ihkmond facility/action classification, ihkmond mcos/tmp/kmsg path formatting, file-buffer rotation, consumer-slot, and create-wait timeout decisions, mcinspect status labels, pid/vtop parsing, mcinspect action validation, mcinspect DWARF size/offset/address form classification, DWARF expression validation, structure/member/global tag-name matching, thread active marker and command basename selection, CPU-local address math, sched-list thread derivation, vtop process-selection helpers, eclair physmem section-name formatting, eclair GDB remote hex/binary packet formatting, eclair option integer parsing, eclair GDB remote command classification, Hg/T/qThreadExtraInfo thread-id parsing, memory-read packet parsing, static GDB reply formatting, qSupported/qC/qAttached/target.xml/qTStatus/memory-map/thread-list response construction, packet checksum parsing/summing, Ctrl-C command construction, register-byte formatting, memory-byte formatting, simple GDB reply buffering, packet-frame construction, banner/usage/open-error formatting, invalid physical-memory and lookup-failure diagnostics, eclair remote reply selection, eclair GDB target-command formatting, eclair thread-extra-info formatting, crash extension status/permission/path/page-size labels, crash GDB command formatting, x86 crash address translation decisions, kmsg ring split math, thread command-name basename selection, mcps header/row formatting, memory-range filtering, mcmem process/header/mapping-line formatting, mcvtop header and memory-range section formatting, PTE physical-address derivation, x86 sign extension, PTE flag-string formatting, full PTE output-line formatting, raw PTE and lookup-failure line formatting, PTE coalescing decisions, lookup range skip/break decisions, PID/thread context parsing, PID hash-bucket selection, boot-refresh comparison, PTE type classification, page-table level shift selection, mcvtop address parsing, and kmsg wrap checks; device I/O, ioctl handling, DWARF/BFD traversal, crash command orchestration, eclair GDB process/socket orchestration, eclair dump NMI side effects, register/memory reads, ihkmond daemon/thread/event-loop mutation, and most IHK command mutation remain C
Rocky runtime integration                    82   Rust McKernel image boots and focused Rocky smoke passes; Rust-linked IHK module-load smoke passes; wider runtime/performance coverage is pending

Latest Rocky/IHK strict-readiness closeout
------------------------------------------------------------

The stricter standalone tracker in `migration.txt` now closes the
`Rocky/IHK control-plane implementation` row from 85% to 100%. The verified
slice routes master IKC init/finalize sequencing, host master-channel bring-up,
Linux IKC work scheduling/extraction, send-interrupt dispatch, OS boot
kmsg/notifier/master-init rollback sequencing, and OS shutdown
status/wait/thaw/NMI/notifier/finalize/shutdown/kmsg-release sequencing through
Rust-owned helpers while preserving public C ABI wrappers and explicit C
fallback builds.

This does not claim runtime reboot coverage. The closeout was validated with
non-reboot gates only: Rust/C equivalence including IHK module helper smoke,
Rust-enabled kernel/module builds, C fallback kernel/module builds, shell
syntax checks, whitespace checks, and dashboard consistency checks.

Mechanical LOC Debt Inventory
------------------------------------------------------------

The area estimates above are the functional progress score for the Rust-port
goal. A conservative LOC denominator is still available only as a C-debt
inventory:

`scripts/rust-ownership-report.py --top 25 --check-dashboard overview.txt --check-dashboard overview2.txt`

Current x86_64 active-scope debt inventory from that script:

- Rust implementation LOC: 59,451.
- C implementation-debt LOC: 125,686.
- C/header boundary LOC: 31,923.
- Required assembly LOC: 872.
- Tests/smokes LOC: 118,366.
- External/generated LOC: 230,989.
- Mechanical LOC audit share: 32.2% Rust implementation.

This mechanical number is audit-only, not the port progress score. It treats
mixed C wrapper files as C implementation debt until the file is reduced to an
explicitly allowed boundary. External/vendor code is reported for visibility but
excluded from the Rust ownership denominator.

What Counts As 100%
------------------------------------------------------------

An area is not complete until the Rust path is the primary implementation and
the matching C path is either removed, retained only as a compatibility shim,
or documented as a deliberate external boundary.

The full-tree target reaches 100% only when all non-arm64 areas above are
Rust-owned and these gates pass:

- Rust-enabled full McKernel image builds on Rocky.
- C fallback image still builds until the fallback is intentionally retired.
- IHK Linux host modules build and load with Rust-owned logic behind their
  Linux-facing entry points.
- User tools build from Rust or are reduced to thin compatibility wrappers.
- McKernel boots on Rocky with McKernel cores.
- Non-root mcexec smoke passes.
- procfs/sysfs smoke passes.
- SysV shared memory smoke passes.
- XPMEM runtime smoke passes when /dev/xpmem is present.
- Memory-management smoke covers mmap, brk, mprotect, munmap, mincore,
  page-fault, file-backed mmap, and hugetlbfs-backed mappings.
- Scheduler/futex smoke covers wait, wake, timeout, requeue, affinity, and
  timer behavior.
- Regression tests include targeted equivalence tests for every moved helper
  plus runtime probes for every moved mutation-heavy path.

Current Blockers To 100%
------------------------------------------------------------

- Rust build/link and module-load smoke now pass for `ihk`, `ihk-smp-x86_64`,
  and `mcctrl`; a focused IHK module helper smoke covers page-allocator bitmap
  mutation, mcctrl sysfs mapping helpers, and Tofu device-name buffer fill, but
  refcount mutation, lock/list mutation, broad allocation/lifetime ownership,
  memory registration, and IKC exchange remain pending.
- Most user tools still use C orchestration, even though `mcstat`, `mcexec`,
  `ihklib`, `ihkconfig`, `ihkosctl`, `ihkmond`, `mcinspect`, `eclair`, and the
  crash extension now link Rust helper objects for substantial parsing,
  formatting, policy, and output-decision slices.
- x86_64 page-table and TLB-sensitive mutation are still C because direct
  runtime probes must exist before moving them.
- Process lifetime and most VM range mutation are still C because allocation,
  free, rb-tree/cache mutation, child-list, reparenting, rusage, locking, and
  teardown paths need staged runtime tests.
- Syscall dispatch and user-copy-heavy handlers are still C.
- Remaining futex queue/requeue side effects, timer mutation, and scheduler
  callbacks are still C.
- Direct XPMEM runtime remains host-dependent; use /dev/xpmem when present and
  keep helper-only conversions until runtime coverage exists.
- The latest IHK SMP memory-assignment follow-up moved free-chunk scan
  decisions, no-chunk/all/fake-chunk action selection, and used-chunk insertion
  ordering through Rust helpers, but host/IHK remains at 99% because memory
  registration, IKC exchange mutation, and module lifecycle ownership still
  need direct runtime coverage.
- The latest page allocator follow-up moved descriptor destroy/free-callback
  orchestration into Rust. Page allocator functional ownership is now 74%;
  actual `ihk_mc_free_pages()` behavior, CPU-local current/channel selection,
  IKC send side effects, deferred-zero worker timing, zeroed-list publication,
  and broader lifetime ownership remain C.
- The latest process/VM follow-up moved range-cache retarget, clear, and store
  mutation into Rust for join, free, and lookup paths. Process/VM functional
  ownership is now 81%; rb-tree insert/erase, VM range allocation/free,
  page-table free/clear, memobj refcounting, TOFU list moves, rusage
  aggregation, and broader lifetime ownership remain C.
- The latest process/VM follow-up moved VM range end/flag commit mutation into
  Rust for extend-up and protection-change paths. Process/VM functional
  ownership is now 84%; page-table attribute mutation/free/clear,
  allocation/free, rb-tree mutation, TOFU hooks, rusage aggregation, and
  broader lifetime ownership remain C.
- The latest process/VM follow-up moved stack-growth range-start
  alignment/commit mutation into Rust for the page-fault path. Process/VM
  functional ownership is now 87%; page-fault handling, access checks, range
  lookup, locks, page allocation, page-table lookup/mapping, retry behavior,
  rb-tree mutation, TOFU hooks, rusage aggregation, and broader lifetime
  ownership remain C.
- The latest x86/page/futex follow-up toward the revised +35 continuation
  moved page refcount/hash lifecycle bodies, `__ihk_pagealloc_init()`
  orchestration, and futex table allocation/publication/initialization
  orchestration into Rust. x86_64 memory management is now 88%, page allocator
  is now 77%, and scheduler/timers/wait/futex is now 91%; C still owns the
  external allocation and lock primitives, page frees, page-table mutation,
  futex wake/wait/requeue side effects, scheduler handoff, user-copy, and
  broader runtime lifetime behavior.
- The latest page-hash/futex-hash follow-up toward the revised +35
  continuation moved page-hash lock/allocation orchestration and futex
  hash-bucket selection into Rust with C callbacks for the primitive lock,
  allocation, and key-hash operations. x86_64 memory management is now 91% and
  scheduler/timers/wait/futex is now 93%; C still owns page-hash debug logging,
  actual spinlock/allocation/hash primitives, page frees, page-table mutation,
  futex wake/wait/requeue side effects, scheduler handoff, user-copy, and broad
  runtime lifetime behavior.
- The latest page allocator follow-up toward the revised +35 continuation
  moved `ihk_numa_alloc_pages()` cache-first allocation attempts, source
  classification, fallback to locked NUMA allocation, and missing-callback
  failure behavior plus `ihk_numa_free_pages()` direct-versus-deferred free
  orchestration into Rust. Page allocator functional ownership is now 83%; C
  still owns CPU-local variable selection, logging, actual interrupt and MCS
  lock primitives, actual allocation/free primitives, IKC send side effects,
  zeroed-list publication, deferred-zero worker timing, and broader allocator
  lifetime behavior.
- The latest scheduler/futex dispatch follow-up toward the revised +35
  continuation moved top-level `futex()` command decode, private/realtime flag
  handling, clock-realtime rejection, wait/wake/requeue/wake-op callback
  selection, and invalid-command callback routing into Rust. Scheduler/timers/
  wait/futex functional ownership is now 96%; C still owns the side-effect
  callback bodies, lock/hash/allocation primitives, futex key lifetime,
  user-value loads, wake/requeue/wait side effects, timer queues, scheduler
  handoff, and context switching.
- The latest Process/VM range-initialization follow-up toward the revised +35
  continuation moved `add_process_memory_range()` VM-range object
  initialization into Rust: rb-node clear state, range fields, object offset,
  page shift, private data, straight-start reset, and optional Tofu list
  initialization. Process/VM functional ownership moved to 90%; at that point C still owned
  range allocation/free, rb-tree insertion, memobj refcounting, page-table
  updates, XPMEM side effects, rollback, user-copy, and broader VM lifetime.
- The latest Process/VM range mapping-action follow-up toward the revised +35
  continuation moved `add_process_memory_range()` map/no-map action selection
  into Rust: NOPHYS skip, remote/uncached/normal page-table update attr
  selection, XPMEM mark action, demand-paging logging action, PROT_NONE skip,
  and memclear gating. Process/VM functional ownership is now 93%; C still
  owned range allocation/free, rb-tree insertion, memobj refcounting, actual
  page-table updates, XPMEM flag mutation, memclear, rollback, user-copy, and
  broader VM lifetime.
- The latest Process/VM add-range orchestration follow-up completes the revised
  +35 continuation by moving the post-bounds `add_process_memory_range()` body
  into Rust: allocation callback handling, VM range initialization,
  insertion/update callback sequencing, insert/update failure cleanup, XPMEM
  and demand-paging actions, memclear callback gating, and returned-range
  publication. Process/VM functional ownership moved to 98%; at that point C still owned
  allocation/free callbacks, rb-tree insertion/removal side effects, memobj refcounting,
  actual page-table updates, XPMEM flag mutation, memclear side effects,
  rollback side effects, user-copy, and broader VM lifetime.
- The latest Process/VM range-tree insertion follow-up starts the completed
  prior +50 continuation by moving `vm_range_insert()` traversal, overlap rejection,
  success/overlap callback sequencing, `rb_link_node()` publication, and
  `rb_insert_color()` balancing into Rust. Process/VM functional ownership is
  now 100%; C still owns allocation/free callbacks, memobj refcounting, TOFU
  split/merge hooks, range-tree erase/removal side effects, actual page-table
  updates, XPMEM flag mutation, memclear, rollback side effects, user-copy, and
  broader VM lifetime.
- The latest syscall ID leaf-body follow-up brings the completed prior +50
  continuation to +4 broad points by moving `getpid`, `getppid`, `gettid`,
  `set_tid_address`, `getuid`, `geteuid`, `getgid`, and `getegid`
  process/thread field reads plus `clear_child_tid` publication into Rust.
  Syscall functional ownership is now 97%; C still owns syscall ABI wrappers,
  current-thread lookup, argument extraction, top-level dispatch, user-copy,
  Linux forwarding, locks, allocation/free, signal delivery, ptrace
  orchestration, architecture register primitives, and high-risk handler
  bodies.
- The latest futex wake orchestration follow-up brings the completed prior +50
  continuation to +8 broad points by moving futex wake target orchestration
  into Rust: mark-woken sequencing, Linux-vs-McKernel target selection,
  Linux response-channel fallback selection, IKC wake packet fill, send
  callback sequencing, scheduler wake callback sequencing, and wake logging.
  Scheduler/timers/wait/futex functional ownership is now 100%; C still owns
  the actual IKC send primitive, scheduler wake primitive, channel lookup
  callback, futex key lifetime, user-value loads/comparisons, futex wait and
  requeue side effects, timer queues, IPIs, context switching, race retry, and
  schedule/schedule_timeout behavior.
- The latest syscall getresid body follow-up brings the completed prior +50
  continuation to +11 broad points by moving `getresuid()` and `getresgid()`
  sequencing into Rust. Rust owns process credential field reads, ordered
  `ruid/euid/suid` and `rgid/egid/sgid` copy sequencing, and fault
  short-circuit behavior through a C `copy_to_user` callback. Syscall
  functional ownership is now 100%; C still owns syscall ABI wrappers,
  current-thread lookup, argument extraction, the actual user-copy primitive,
  top-level dispatch, Linux forwarding, locks, allocation/free, ptrace
  orchestration, architecture register primitives, and high-risk handler
  bodies.
- The latest page-allocator free-finish follow-up brings the completed prior +50
  continuation to +14 broad points by moving `ihk_numa_free_pages()`
  post-action completion sequencing into Rust. Rust owns direct-free
  success/error logging selection, deferred-free error/skip/send action
  handling, Linux zero-request send callback sequencing, and send
  success/failure logging. Page allocator functional ownership is now 86%; C
  still owns CPU-local channel lookup, the actual `ihk_ikc_send()` primitive,
  logging primitive bodies, allocator lifetime, MCS/interrupt primitives,
  zeroed-list publication timing, and broader allocator mutation.
- The latest x86 page-table root-lifecycle follow-up brings the completed prior
  +50 continuation to +17 broad points by moving `ihk_mc_pt_create()` and
  `ihk_mc_pt_destroy()` root lifecycle sequencing into Rust. Rust owns
  allocation-callback orchestration, new root zeroing, kernel-half entry copy
  from `init_pt`, destroy-root kernel-half clearing, and destroy-recursion
  callback dispatch. x86_64 memory management functional ownership was 94% at
  that checkpoint; C still owned the actual allocator/free primitives,
  recursive child-table destruction below the root, `phys_to_virt()`,
  page-table traversal, page frees, RSS/TLB-sensitive behavior, and user-copy.
- The latest x86 page-table prepare-map follow-up brings the completed prior
  +50 continuation to +20 broad points by moving `ihk_mc_pt_prepare_map()`
  orchestration into Rust. Rust owns initial page-table selection, first-level
  L4 range traversal, present-entry short-circuiting, allocation callback
  sequencing, L4 entry publication, last-level set-page callback loop
  sequencing, and first error return propagation. x86_64 memory management
  functional ownership was 97% at that checkpoint; C still owned the actual
  allocation primitive, `virt_to_phys()`, `__set_pt_page()`, recursive
  child-table destruction, page frees, RSS/TLB-sensitive behavior, rollback
  orchestration, and user-copy.
- The latest x86 set-PTE body follow-up brings the completed prior +50
  continuation to +23 broad points by moving `ihk_mc_pt_set_pte()` body
  sequencing into Rust.
  Rust owns page-size/value selection callback orchestration, alignment error
  classification, log-callback selection, panic-callback dispatch for invalid
  page sizes, PTE store invocation, and final return shaping. x86_64 memory
  management functional ownership is now 100%; this is not strict completion,
  because C still owns allocation/free primitive bodies, broader traversal,
  actual log/panic primitive bodies, RSS/TLB-sensitive behavior, rollback
  orchestration, and user-copy.
- The latest page-allocator top-level free follow-up brings the completed prior
  +50 continuation to +26 broad points by moving exported `ihk_numa_free_pages()`
  sequencing into Rust. Rust owns CPU-cache free attempt classification, cache
  success/error log selection, direct-versus-deferred dispatch, Linux
  zero-request send callback sequencing, final log selection, and return
  shaping. Page allocator functional ownership is now 89%; C still owns
  CPU-local variable selection, actual lock/interrupt primitives, IKC channel
  lookup and send side effects, logging primitive bodies, deferred-zero worker
  timing, zeroed-list publication, allocator lifetime, and broad allocator
  mutation.
- The latest page-allocator top-level allocation follow-up brings the completed
  prior +50 continuation to +29 broad points by moving exported
  `ihk_numa_alloc_pages()` sequencing into Rust. Rust owns cache-hit logging
  selection, direct-allocation logging selection, source-aware return shaping,
  and the exported allocation handoff around the existing cache-first/source
  selection/fallback-to-NUMA orchestration. Page allocator functional
  ownership is now 92%; C still owns CPU-local variable selection, actual
  cache/tree lock primitives, `ihk_mc_alloc_pages()`, MCS/interrupt
  primitives, logging primitive bodies, allocator lifetime, zeroed-list
  publication, deferred-zero worker timing, and broader allocator mutation.
- The latest page-allocator add/zero top-level follow-up brought the completed
  prior +50 continuation to a historical 32/50 broad-point checkpoint by moving exported
  `ihk_numa_add_free_pages()` and public `ihk_numa_zero_free_pages()`
  sequencing into Rust. Rust owns add-free success/error log selection, return
  shaping around the NUMA add-free mutation body, and public zero-free dispatch
  into the Rust-owned zero-list publication path. Page allocator functional
  ownership is now 95%; C still owns logging primitive bodies, actual
  allocation/free and lock/interrupt primitives, allocator lifetime, IKC send
  side effects, deferred-zero worker timing, and broader allocator mutation.
- The latest ABI/shared-primitives follow-up brings the completed prior +50
  continuation to +39 broad points by adding Rust/C compile-time layout mirrors
  for pager create/map result packets plus McKernel memory area/node,
  page-allocator ops, aligned TLB-flush entry, and page-cache header structs,
  and by routing `vsnprintf()` base-10 digit emission through Rust-owned
  `put_dec_*` helpers. ABI/layout functional ownership is now 58%, shared
  primitives functional ownership is now 87%; C still owns variadic printf
  parsing, width/sign/precision buffer policy, output orchestration, and
  broader private lifecycle layout coverage.
- The latest CPU-local lifecycle follow-up brings the completed prior +50
  continuation to +46 broad points by adding Rust/C compile-time layout mirrors for
  `rusage_percpu`, kmalloc metadata, SMP-call packets, backlog entries, and
  the full x86_64 `cpu_local_var`, and by routing CPU-local storage
  initialization, `get_cpu_local_var()` selection, and normal preempt counter
  updates through Rust. ABI/layout functional ownership is now 62%, page
  allocator functional ownership is now 98%; C still owns the underlying
  allocation/free primitives, MCS/interrupt primitives, logging callbacks, IKC
  send side effects, deferred-zero worker timing, FUGAKU debug preempt path,
  and broader allocator lifetime/mutation.
- The latest kmalloc/shared-parser follow-up completes that prior +50
  continuation by moving kmalloc chunk header initialization, sorted free-list
  insertion, adjacent free-chunk consolidation, and `skip_atoi()` format
  width/precision pointer advancement into Rust with C fallbacks. Shared
  primitives functional ownership is now 89% and page allocator functional
  ownership is now 100%; this is not strict allocator completion because C
  still owns allocator lifetime, primitive allocation/free operations,
  MCS/interrupt primitives, logging callbacks, IKC send side effects,
  deferred-zero worker timing, FUGAKU debug preempt behavior, and broader
  runtime orchestration.
- The latest shared-formatting follow-up starts the requested +50 continuation
  with +4 broad points by moving the central `number()` integer/pointer
  formatter body into Rust: sign handling, prefix selection, width and
  precision adjustment, zero/space padding, octal/hex/decimal digit staging,
  bounded output writes, and returned pointer advancement. Shared primitives
  functional ownership is now 93%; C still owns format-token decoding, string
  and pointer extension dispatch, va_arg extraction, `%n` writes, final NUL
  termination, and the surrounding `vsnprintf()` loop.
- The latest shared-format-decoder follow-up brings the requested +50
  continuation to +8 broad points by moving `format_decode()` format-token
  parsing into Rust: literal spans, flags, field width, precision, qualifiers,
  conversion type, base, signedness, and width/precision continuation state.
  Shared primitives functional ownership is now 97%; C still owns the
  surrounding `vsnprintf()` loop, va_arg extraction, string and pointer
  extension dispatch, `%n` writes, final NUL termination, and output buffer
  lifetime.
- The latest shared-string-format follow-up brings the requested +50
  continuation to +11 broad points and completes the broad shared-primitives
  row by moving `string()` `%s` output formatting into Rust: NULL-string
  substitution, precision-bounded length selection, left/right padding, bounded
  output writes, and returned pointer advancement. C still owns the surrounding
  `vsnprintf()` loop, va_arg extraction, pointer extension dispatch, `%n`
  writes, final NUL termination, and output buffer lifetime.
- The previous signal/futex/register ABI-layout follow-up brings the requested
  +50 continuation to +15 broad points by adding Rust/C compile-time layout
  mirrors for x86 signal action, altstack, `siginfo_t`, `signalfd_siginfo`,
  ptrace `user_regs_struct`, `user_fpregs_struct`, `struct user`, futex
  hash-bucket/key/queue layouts, and the key offsets embedded in process/thread
  state. ABI/layout functional ownership is now 66%; C still owns the actual
  signal delivery, ptrace register access, futex wait/wake side effects,
  user-copy, scheduler handoff, lock primitives, and IKC send behavior.
- The previous syscall/control-plane ABI-layout follow-up brings the requested
  +50 continuation to +19 broad points by adding Rust/C compile-time layout
  mirrors for `ikc_scd_init_param`, `syscall_post`, coredump `coretable`,
  procfs request/file descriptors, `sysinfo`, `tod_data_s`, CPU mapping
  requests, perf-control descriptors, UTI attributes/context, and
  `move_pages_smp_req`. ABI/layout functional ownership is now 70%; C still
  owns procfs buffer mutation, perf-control side effects, UTI scheduling, time
  publication, move-pages page-table mutation, CPU mapping exchange, user-copy,
  locks, IKC exchange, callback bodies, and fallback implementation.
- The previous x86 register/FPU save-state ABI-layout follow-up brings the
  requested +50 continuation to +23 broad points by adding Rust/C compile-time
  layout mirrors for x86 descriptor pointers, `tss64`, `i387_fxsave_struct`,
  YMM high halves, LWP save areas, bound-register state, `xsave_hdr_struct`,
  and packed/aligned `xsave_struct`. ABI/layout functional ownership is now
  74%; C still owns context switching, ptrace register access, signal-frame
  FPU save/restore, XSAVE/XRSTOR execution, architecture trap behavior, and
  fallback implementation.
- The latest sysfs control-plane ABI-layout follow-up brings the requested
  +50 continuation to +27 broad points by adding Rust/C compile-time layout
  mirrors for sysfs create, mkdir, symlink, lookup, unlink, and setup request
  packets. ABI/layout functional ownership is now 78%; C still owns sysfs
  allocation, path formatting, IKC sends, busy waits, host-side object
  creation, show/store callbacks, release handling, and fallback
  implementation.
- The latest sigaltstack follow-up is strict-core progress, not broad
  dashboard movement because Syscall Core is already 100%. It routes
  `sigaltstack()` body sequencing through Rust with C fallback: old-stack
  copyout, new-stack copyin, validation, disabled-stack normalization, and
  thread `sigstack` mutation. `migration.txt` Syscall Core strict ownership is
  now 66%; C still owns the syscall ABI wrapper, actual user-copy primitive,
  top-level dispatch, Linux forwarding, signal delivery, and fallback
  implementation.
- The latest sysfs response-body follow-up is strict-core progress, not broad
  dashboard movement because procfs/sysfs/xpmem/file objects is already 100%.
  It routes show/store/release response sequencing through Rust with C
  fallback: default ssize/error shaping, optional callback dispatch, response
  packet msg/err/arg publication, send callback dispatch, and output
  publication. `migration.txt` procfs/sysfs/xpmem/file object strict ownership
  is now 64%; C still owns callback bodies, the actual `ihk_ikc_send()`
  primitive, logging, global buffer lifetime, packet-handler dispatch,
  allocation/free, sysfs path formatting, busy waits, host-side object
  creation, user-copy, and fallback implementation.
- The latest coredump/uio ABI-layout follow-up brings the requested +50
  continuation to +31 broad points by adding Rust/C compile-time layout
  mirrors for ELF core headers, program headers, note headers, `elf_siginfo`,
  `prstatus64_timeval`, `elf_prstatus64`, `elf_prpsinfo64`, and `iovec`.
  ABI/layout functional ownership is now 82%; C still owns coredump note
  generation, architecture register capture, process-memory dumping,
  user-vector walking, copy paths, and fallback implementation.
- The previous sysfs packet-dispatch follow-up is strict-core progress, not
  broad dashboard movement because procfs/sysfs/xpmem/file objects is already
  100%. It routes show/store/release request dispatch through Rust with C
  fallback: request-kind classification, typed callback selection,
  show/store/release callback dispatch, store-size publication from the
  incoming packet error field, and unknown-message classification.
  `migration.txt` procfs/sysfs/xpmem/file object strict ownership was then
  67%;
  C still owns the public IKC handler wrapper, unknown-message logging,
  callback bodies, raw IKC send behavior, global buffer lifetime,
  allocation/free, sysfs path formatting, busy waits, host-side object
  creation, user-copy, and fallback implementation.
- The previous sysfs request-body follow-up is strict-core progress, not broad
  dashboard movement because procfs/sysfs/xpmem/file objects is already 100%.
  It routes create/mkdir/symlink/lookup/unlink/setup request send/wait/result
  sequencing through Rust with C fallback: IKC request send callback dispatch,
  remote busy-wait polling, response read barrier callback sequencing,
  response error classification, and mkdir/lookup handle publication.
  `migration.txt` procfs/sysfs/xpmem/file object strict ownership is now 70%;
  C still owns allocation/free, varargs path formatting, special sysfs
  instance physical-address setup, the actual `ihk_ikc_send()` primitive,
  logging, public IKC handler wrapping, user-copy, and fallback implementation.
- The previous sysfs init-body follow-up is strict-core progress, not broad
  dashboard movement because procfs/sysfs/xpmem/file objects is already 100%.
  It routes `sysfs_init()` setup orchestration through Rust with C fallback:
  sysfs request layout-size validation, data-buffer size/pointer publication,
  data-buffer and setup-param allocation callback dispatch, setup packet
  `buf_rpa`/`bufsize`/`busy` publication, setup request-body dispatch,
  setup-param free callback sequencing, and stage/phase error classification.
  `migration.txt` procfs/sysfs/xpmem/file object strict ownership is now 72%;
  C still owns actual allocator/free primitives, `virt_to_phys()`,
  `ihk_ikc_send()`, `cpu_pause()`/read-barrier primitives, panic/logging, and
  fallback implementation.
- The previous x86 range-top follow-up is strict-core progress, not broad
  dashboard movement because x86_64 memory management is already 100%. It
  routes clear/set range top-level orchestration through Rust with C fallback:
  clear-range bounds validation, TLB invalid-address array allocation/free
  callback sequencing, argument field publication, top-level `walk_pte_l4`
  dispatch, final remote TLB flush callback dispatch, set-range argument
  publication, and set-range top-level walk-failure log/return shaping.
  at that checkpoint, `migration.txt` x86_64 memory strict ownership was 96%;
  C still owns
  public ABI wrappers, actual allocation/free and TLB flush primitive bodies,
  page-table visitor callbacks, lower-level walk callbacks, address-
  translation primitive bodies, and fallback scaffolding.
- The latest x86 clear-range body follow-up is strict-core progress, not broad
  dashboard movement because x86_64 memory management is already 100%. It
  routes clear-range leaf/level/root body sequencing through Rust with C
  fallback: leaf null-skip and PTE clear-exchange sequencing, TLB flush-address
  queue callback sequencing, old-entry action callback handoff, old-effect
  callback orchestration, split-error log/return shaping, large-page clear
  sequencing, child table translation, child-walk dispatch/error propagation,
  `-ENOENT` child-table teardown/free sequencing, root skip/translation/
  dispatch, and level-specific debug logging. `migration.txt` x86_64 memory
  strict ownership is now 99%; C still owns public ABI wrappers, old-entry page
  lookup/refcount primitive behavior, actual allocation/free and
  address-translation primitive bodies, page/RSS primitive bodies, remote TLB
  shootdown primitive behavior, and fallback scaffolding.
- The previous x86 visitor-body follow-up is strict-core progress, not broad
  dashboard movement because x86_64 memory management is already 100%. It
  routes normal/safe `visit_pte_range()` visitor body sequencing through Rust
  with C fallback: leaf skip/direct visitor callback dispatch, level
  direct/E2BIG retry classification, split-error log/return shaping, child
  page-table allocation/publication, existing child-table translation,
  child-walk callback dispatch, root allocation/translation dispatch, and
  normal/safe top-level visit dispatch. At that checkpoint, `migration.txt`
  x86_64 memory strict ownership was 98%; C still owns public ABI wrappers, actual
  allocation/free and address-translation primitive bodies, page-table walk
  iteration callbacks, broader page frees/RSS accounting, TLB primitive
  behavior, and fallback scaffolding.
- The previous x86 set-range level-body follow-up is strict-core progress, not
  broad dashboard movement because x86_64 memory management is already 100%.
  It routes `set_range_l1/l2/l3/l4` leaf and level body orchestration through
  Rust with C fallback: action dispatch, zeroed child page-table allocation,
  atomic publication retry, existing child-table translation, child-walk
  callback dispatch, large-page map/store/RSS routing, child-walk failure
  shaping, and unconsumed child-table free sequencing. `migration.txt`
  x86_64 memory strict ownership is now 93%; C still owns public ABI wrappers,
  actual allocation/free and address-translation primitive bodies, child
  page-table walk callbacks, RSS primitive body, top-level `walk_pte_l4()`
  dispatch, and fallback scaffolding.
- The previous x86 set-range side-effect follow-up is strict-core progress,
  not broad dashboard movement because x86_64 memory management is already
  100%. It routes set-range existing-mapping conflict cleanup, page-table
  allocation-failure cleanup, map/store/RSS-accounting callback ordering,
  large-page success logging, and child-walk failure logging through Rust with
  C fallback. `migration.txt` x86_64 memory strict ownership is now 90%; C
  still owns public ABI wrappers, actual `clear_range()` primitive effects,
  page-table allocation/publication, address-translation callbacks, child
  page-table walk callbacks, RSS primitive body, leftover table free behavior,
  and fallback scaffolding.
- The previous x86 page-table setup follow-up is strict-core progress, not
  broad dashboard movement because x86_64 memory management is already 100%.
  It routes zeroed page-table allocation, get-PTE allocation traversal,
  `__set_pt_page()` regular/large-page mapping body sequencing, and
  kernel-address lock classification through Rust with C fallback.
  `migration.txt` x86_64 memory strict ownership is now 84%; C still owns
  public ABI wrappers, actual spinlock and allocator primitives,
  `virt_to_phys()`/`phys_to_virt()` behavior, page-table free primitive
  behavior, broader range mapping/free rollback, RSS updates, TLB-sensitive
  flushing, and fallback scaffolding.
- The previous x86 five-slice memory follow-up is strict-core progress, not
  broad dashboard movement because x86_64 memory management is already 100%.
  It routes `__clear_pt_page()`, `lookup_pte()`,
  `ihk_mc_pt_change_attr_range()`, `ihk_mc_pt_virt_to_pagemap()`, and
  `arch_vrflag_to_ptattr()` body/result sequencing through Rust with C
  fallback. `migration.txt` x86_64 memory strict ownership is now 78%; C still
  owns public ABI wrappers, primitive `phys_to_virt()` behavior, page-table
  lifetime, TLB-sensitive flushing, broader visitor/mutation traversal,
  allocation/free primitives, and fallback scaffolding.
- The latest x86 virtual-to-physical walk follow-up is strict-core progress,
  not broad dashboard movement because x86_64 memory management is already
  100%. It routes `ihk_mc_pt_virt_to_phys_size()` walk orchestration through
  Rust with C fallback: null-root fallback to `init_pt`, page-table index
  calculation use, L4 miss/walk classification, L3 and L2 large-page hit return
  shaping, L1 hit return shaping, physical and page-size publication,
  physical-to-virtual callback dispatch between levels, and missing/null
  callback error shaping. `migration.txt` x86_64 memory strict ownership is
  now 70%; C still owns the public ABI wrapper, actual `phys_to_virt()`
  primitive behavior, page-table memory lifetime, and fallback scaffolding.
- The previous x86 page-table destroy follow-up is strict-core progress, not
  broad dashboard movement because x86_64 memory management is already 100%.
  It routes recursive page-table destruction below the root through Rust with
  C fallback: level/null validation with panic callback routing, present/non-
  large child-table detection, child-table physical extraction, physical-to-
  virtual callback dispatch, recursive post-order descent, free-page callback
  sequencing after children, and missing-callback validation. `migration.txt`
  x86_64 memory strict ownership is now 67%; C still owns the public ABI
  wrapper, actual `phys_to_virt()` primitive behavior, actual page-free
  primitive behavior, panic primitive bodies, and fallback scaffolding.
- The previous host schedule-process follow-up is strict-core progress, not broad
  dashboard movement because host/IKC/mcctrl/IHK modules is already 100%. It
  routes `SCD_MSG_SCHEDULE_PROCESS` request sequencing through Rust with C
  fallback: thread/proc validation, current-CPU callback dispatch, received-log
  callback sequencing, CPU-set allowed decision dispatch, fallback clone-CPU
  callback dispatch, no-CPU error/log return shaping, TID publication callback
  ordering, proc/thread running-status publication callback ordering,
  chain-thread/process callback ordering, runqueue-add callback sequencing,
  queued-log callback dispatch, and null/missing-callback validation.
  `migration.txt` host/IKC/mcctrl/IHK kernel path strict ownership is now 86%;
  C still owns the actual CPU-set test, clone-CPU selection primitive,
  field-write primitives, chain/list mutation primitives, runqueue mutation
  primitive, logging primitive bodies, and fallback scaffolding.
- The previous procfs backlog follow-up is strict-core progress, not broad
  dashboard movement because procfs/sysfs/xpmem/file objects is already 100%.
  It routes procfs backlog allocation/copy/add/free-on-error sequencing
  through Rust with C fallback: packet-copy allocation callback dispatch,
  allocation-failure return shaping, request copy callback dispatch, backlog
  add callback dispatch, add-failure free callback dispatch, and
  null/missing-callback validation. `migration.txt`
  procfs/sysfs/xpmem/file object strict ownership is now 84%; C still owns
  the actual allocator/free primitives, byte-copy primitive, backlog queue
  mutation, and fallback scaffolding.
- The previous host remote page-fault request follow-up is strict-core progress,
  not broad dashboard movement because host/IKC/mcctrl/IHK modules is already
  100%. It routes remote page-fault request dispatch sequencing through Rust
  with C fallback: target-thread lookup callback dispatch, missing-thread
  log/body-error routing, current-thread direct body dispatch, deferred packet
  allocation/copy callback sequencing, backlog publication callback dispatch,
  scheduler wake callback dispatch, thread unlock callback ordering,
  allocation-failure unlock/error shaping, and null/missing-callback
  validation. `migration.txt` host/IKC/mcctrl/IHK kernel path strict ownership
  is now 84%; C still owns actual thread lookup, allocation, packet copy,
  thread-field publication, scheduler wake, thread unlock, remote page-fault
  body primitive behavior, raw IKC send side effects, and fallback
  scaffolding.
- The previous host remote page-fault body follow-up was strict-core progress, not
  broad dashboard movement because host/IKC/mcctrl/IHK modules is already
  100%. It routes remote page-fault body sequencing through Rust with C
  fallback: error-only answer routing, optional profile timestamp gating,
  populated fault-reason construction, debug-log callback dispatch,
  preempt-disable/page-fault/preempt-enable callback ordering, optional
  profile-event delta publication, answer packet dispatch, and
  null/missing-callback validation. `migration.txt` host/IKC/mcctrl/IHK kernel
  path strict ownership was then 81%; C still owns current-thread/channel
  selection, the actual `page_fault_process_vm()` primitive body, timestamp,
  preempt, profile-event and logging primitive bodies, remote page-fault
  thread lookup/deferred scheduling, raw IKC send side effects, and fallback
  scaffolding.
- The previous host wake/debug request follow-up was strict-core progress, not
  broad dashboard movement because host/IKC/mcctrl/IHK modules is already
  100%. It routes `SCD_MSG_WAKE_UP_SYSCALL_THREAD` and `SCD_MSG_DEBUG_LOG`
  request sequencing through Rust with C fallback: wake-target TID extraction,
  thread lookup callback dispatch, missing-thread error shaping, optional wake
  logging, waitqueue wake callback dispatch, thread unlock callback dispatch,
  debug-log code extraction, optional debug-print callback dispatch,
  debug-log callback dispatch, and null/missing-callback validation.
  `migration.txt` host/IKC/mcctrl/IHK kernel path strict ownership was then 79%;
  C still owns actual thread lookup, waitqueue wake, thread unlock,
  `debug_log()`, logging primitive bodies, and fallback scaffolding.
- The previous host send-signal request follow-up was strict-core progress, not
  broad dashboard movement because host/IKC/mcctrl/IHK modules is already
  100%. It routes `SCD_MSG_SEND_SIGNAL` request sequencing through Rust with
  C fallback: mcctrl-signal descriptor physical-map callback dispatch,
  writable/active virtual-map callback dispatch, descriptor copy into
  Rust-owned stack storage, virtual and physical unmap sequencing, ACK
  packet/result sequencing before delivery, do-kill callback dispatch,
  optional delivery-log callback dispatch, map-failure ACK/error shaping, and
  null/missing-callback validation. `migration.txt` host/IKC/mcctrl/IHK kernel
  path strict ownership was then 77%; C still owns actual map/unmap primitive
  bodies, `do_kill()` signal-delivery primitive behavior, logging primitives,
  raw IKC send side effects, and fallback scaffolding.
- The previous host cleanup request follow-up was strict-core progress, not
  broad dashboard movement because host/IKC/mcctrl/IHK modules is already
  100%. It routed cleanup-process and cleanup-fd request sequencing through
  Rust with C fallback: process cleanup callback dispatch, cleanup-process
  response sequencing, terminate callback ordering, fd cleanup callback
  dispatch, optional cleanup logging callback dispatch, cleanup-fd response
  sequencing, and null/missing-callback validation. `migration.txt`
  host/IKC/mcctrl/IHK kernel path strict ownership was 75% after that slice;
  C still owns actual process cleanup, FD cleanup, terminate-host, logging,
  raw IKC send side effects, and fallback scaffolding.
- The previous host CPU register request follow-up is strict-core progress, not
  broad dashboard movement because host/IKC/mcctrl/IHK modules is already
  100%. It routes `SCD_MSG_CPU_RW_REG` request-wrapper sequencing through
  Rust with C fallback: descriptor physical-map callback dispatch,
  writable/active virtual-map callback dispatch, architecture register
  callback invocation, conditional virtual unmap, physical unmap, response
  packet/result sequencing, and null/missing-callback validation.
  `migration.txt` host/IKC/mcctrl/IHK kernel path strict ownership was 73% after that slice;
  C still owns actual map/unmap primitive bodies, architecture register
  primitive bodies, raw IKC send side effects, and fallback scaffolding.
- The previous host perf-control request follow-up is strict-core progress, not
  broad dashboard movement because host/IKC/mcctrl/IHK modules is already
  100%. It routes `SCD_MSG_PERF_CTRL` request-wrapper sequencing through Rust
  with C fallback: descriptor physical-map callback dispatch, writable/active
  virtual-map callback dispatch, Rust perf-control dispatch invocation,
  conditional virtual unmap, physical unmap, ACK packet/result sequencing, and
  null/missing-callback validation. `migration.txt` host/IKC/mcctrl/IHK
  kernel path strict ownership was 71% after that slice; C still owns actual map/unmap
  primitive bodies, actual perf counter primitive bodies, raw IKC send side
  effects, and fallback scaffolding.
- The previous host perf-control dispatch follow-up is strict-core progress, not
  broad dashboard movement because host/IKC/mcctrl/IHK modules is already
  100%. It routes host perf-control dispatch sequencing through Rust with C
  fallback: SET/GET/ENABLE/DISABLE selection, SET mode construction from
  exclude flags, init/stop/reset callback ordering and early-error return
  shaping, start/stop callback dispatch for enable/disable, GET read-value
  publication, unexpected-type logging callback dispatch, and
  missing-callback/null validation. `migration.txt` host/IKC/mcctrl/IHK
  kernel path strict ownership was 69% after that slice; C still owns host memory map/unmap,
  actual perf counter primitive bodies, response sending, raw IKC send side
  effects, and fallback scaffolding.
- The previous host perf/register reply follow-up is strict-core progress, not
  broad dashboard movement because host/IKC/mcctrl/IHK modules is already
  100%. It routes host perf ACK and CPU register response packet/send-callback
  sequencing through Rust with C fallback: response-packet zeroing, response
  message publication, request `arg`/`reply` transfer where applicable, error
  publication, and send callback dispatch. `migration.txt`
  host/IKC/mcctrl/IHK kernel path strict ownership was 67% after that slice; C still owns
  perf counter operations, CPU register read/write, host memory map/unmap,
  logging, raw IKC send side effects, and fallback scaffolding.
- The previous host traditional-reply follow-up is strict-core progress, not
  broad dashboard movement because host/IKC/mcctrl/IHK modules is already
  100%. It routes traditional host SCD ACK packet/send-callback sequencing
  through Rust with C fallback for prepare-process, send-signal,
  cleanup-process, and cleanup-fd responses: response-packet zeroing, ACK
  message publication, request `ref`/`arg` and `reply` transfer, error
  publication, and send callback dispatch. `migration.txt`
  host/IKC/mcctrl/IHK kernel path strict ownership is now 66%; C still owns
  process preparation, cleanup, signal-delivery, logging, terminate-host, and
  raw IKC send primitive bodies plus fallback scaffolding.
- The previous host remote page-fault answer follow-up is strict-core progress,
  not broad dashboard movement because host/IKC/mcctrl/IHK modules is already
  100%. It routes host remote page-fault answer packet/send-callback
  sequencing through Rust with C fallback: answer-packet zeroing,
  `SCD_MSG_REMOTE_PAGE_FAULT_ANSWER` publication, request
  `ref`/`arg`/`reply`/`pid` transfer, error publication, and send callback
  dispatch. `migration.txt` host/IKC/mcctrl/IHK kernel path strict ownership
  is now 64%; C still owns `page_fault_process_vm()`, profiling/preempt
  handling, response-channel lookup through `cpu_local_var()`, the actual IKC
  send primitive, and fallback scaffolding.
- The previous procfs answer follow-up is strict-core progress, not broad
  dashboard movement because procfs/sysfs/xpmem/file objects is already 100%.
  It routes procfs answer packet/send-callback sequencing through Rust with C
  fallback: answer-packet zeroing, `SCD_MSG_PROCFS_ANSWER` publication,
  request `ref`/`arg`/`reply`/`pid` transfer, error publication, and send
  callback dispatch. `migration.txt` procfs/sysfs/xpmem/file object strict
  ownership is now 83%; C still owns `cpu_local_var()` channel selection, the
  actual IKC send primitive, request mapping/data production, and fallback
  scaffolding.
- The previous procfs request-finalization follow-up is strict-core progress, not
  broad dashboard movement because procfs/sysfs/xpmem/file objects is already
  100%. It routes procfs request release/finalization mutation through Rust
  with C fallback: release-buffer free traversal dispatch and `ret` zeroing,
  completion `ret`/`eof` publication, pbuf attach decision, physical-address
  callback dispatch, and `pbuf` publication. `migration.txt`
  procfs/sysfs/xpmem/file object strict ownership is now 82%; C still owns
  request mapping/unmapping, procfs data production, actual page free and
  physical-address primitives, answer sending, and fallback scaffolding.
- The previous procfs buffer-chain follow-up is strict-core progress, not broad
  dashboard movement because procfs/sysfs/xpmem/file objects is already 100%.
  It routes procfs buffer-chain append/release sequencing through Rust with C
  fallback: first-buffer allocation callback dispatch, cursor rollover at page
  capacity, next-buffer physical-link publication, bounded copy callback
  sequencing, per-buffer size mutation, allocation-failure cleanup, and release
  traversal/free callback sequencing. `migration.txt`
  procfs/sysfs/xpmem/file object strict ownership is now 81%; C still owns
  actual page allocation/free, physical-address translation, the byte-copy
  primitive, request mapping/unmapping, `r->pbuf` publication, and fallback
  scaffolding.
- The previous procfs thread-control follow-up is strict-core progress, not
  broad dashboard movement because procfs/sysfs/xpmem/file objects is already
  100%. It routes `procfs_thread_ctl()` packet/control sequencing through Rust
  with C fallback: packet zeroing, TID create/delete packet field publication,
  response physical-address callback sequencing, IKC send callback dispatch,
  create-only wait-loop selection, and pause-callback polling.
  `migration.txt` procfs/sysfs/xpmem/file object strict ownership was then 79%;
  C still owns thread/process field extraction, `cpu_local_var()`,
  `ihk_mc_get_osnum()`, actual `virt_to_phys()`, actual `ihk_ikc_send()`,
  actual `cpu_pause()`, and fallback scaffolding.
- The previous sysfs public request-wrapper follow-up is strict-core progress,
  not broad dashboard movement because procfs/sysfs/xpmem/file objects is
  already 100%. It routes post-format request sequencing for create, mkdir,
  symlink, lookup, and unlink through Rust with C fallback: request-body
  callback dispatch, send-error vs response-error log-event selection, phase
  publication, and mkdir/lookup handle destination publication.
  `migration.txt` procfs/sysfs/xpmem/file object strict ownership was then 78%;
  C still owns allocation/free, varargs path formatting, special-create
  wrapper logging, raw `ihk_ikc_send()`, `cpu_pause()`/read-barrier
  primitives, final wrapper entry/exit logging, and fallback scaffolding.
- The previous sysfs special-create follow-up is strict-core progress, not broad
  dashboard movement because procfs/sysfs/xpmem/file objects is already 100%.
  It routes special sysfs create conversion through Rust with C fallback:
  direct/string/bitmap snooping-operation dispatch, string bit-count
  calculation, bitmap parameter copy, physical-address callback sequencing,
  and `client_instance` publication. `migration.txt`
  procfs/sysfs/xpmem/file object strict ownership was then 76%; C still owns the
  static wrapper, unknown-op logging, actual `virt_to_phys()` primitive,
  create-request allocation, varargs path formatting, IKC send/wait behavior,
  and fallback scaffolding.
- The previous sysfs request-wrapper follow-up is strict-core progress, not
  broad dashboard movement because procfs/sysfs/xpmem/file objects is already
  100%. It routes sysfs server show/store/release wrapper sequencing through
  Rust with C fallback: callback-pointer selection, response body dispatch,
  callback-error/send-error/packet-error/debug log-event selection, response
  ssize/error publication, and release callback routing. `migration.txt`
  procfs/sysfs/xpmem/file object strict ownership was then 75%; C still owns the
  static wrapper entry/debug line, actual sysfs ops callbacks, actual
  `ihk_ikc_send()`, printk/dkprintf/ekprintf primitives, sysfs data-buffer
  lifetime, and fallback scaffolding.
- The previous sysfs packet-handler wrapper follow-up is strict-core progress,
  not broad dashboard movement because procfs/sysfs/xpmem/file objects is
  already 100%. It routes public packet-handler dispatch through Rust with C
  fallback: request-kind dispatch, show/store/release callback routing,
  unknown-message log-callback selection, kind publication, and return
  shaping. `migration.txt` procfs/sysfs/xpmem/file object strict ownership was
  then 73%; C still owns the exported IKC handler ABI, actual `kprintf()`
  primitive, callback bodies, and fallback implementation.
- The latest x86 CPU-local/sysfs/profile ABI-layout follow-up brings the
  requested +50 continuation to +36 broad points by adding Rust/C compile-time
  layout mirrors for the x86 CPU-local page, x86 kernel context, TLS
  `user_desc`, sysfs operation callbacks/handle/bitmap parameters,
  `itimerval`, and `profile_event`. ABI/layout functional ownership is now
  87%; C still owns CPU-local stack switching, GDT/TSS mutation, TLS setup,
  sysfs callback execution, profile accumulation, timer delivery, copy paths,
  and fallback implementation.

Next Milestones
------------------------------------------------------------

1. Use the passing module-load smoke for Rust-linked `ihk`, `ihk-smp-x86_64`,
   and `mcctrl` as the gate for deeper module policy and carefully staged
   mutation-heavy body moves.
2. Expand remaining private process/thread lifecycle and IHK module object
   layouts, especially refcount, lock/list, allocation, and lifecycle structs.
3. Convert more process/VM and x86_64 memory policy helpers while adding
   runtime probes before moving mutation-heavy bodies.
4. Expand user-tool Rust crate scaffolding and port command parsing/output
   logic beyond the current mcstat, mcexec, ihklib, mcinspect, eclair, and
   crash helper objects, especially IHK command orchestration and dump paths.
5. Keep `scripts/rust-ownership-report.py` current and drive its C
   implementation-debt list down until every remaining C/assembly file has an
   allowed-boundary reason.
