McKernel Strict Rust Source Retirement Tracker
Updated: 2026-08-17

Scope: active smp-x86/x86_64 McKernel-owned C/header source retirement inventory. Arm64, deferred builtin/MIC/attached targets, third-party, generated, and test/fixture bundles are excluded from this per-file list because they are not active McKernel-owned source-retirement debt.

Artifact conversion status (authoritative for implementation-language claims):
- The primary conservative measurement is the exact Rocky 8.10 build of commit
  3a5767b395f932cbe41f717f3c720512009490ff in GitHub Actions run
  32085952941, build job 95558404374, build artifact 9306766589 (artifact
  SHA-256
  2f71dc18139dd4211669981eee786518dc4a38c4ce3478d886209c259b5a31ce).
  The final x86_64 guest image SHA-256 is
  68d7f6e7dbac77451e0fb97bcce1c0514b754e0a7d40f77ba6340f101baa230e.
  Exact GNU-link-map attribution reports 614,183 bytes contributed by
  `mckernel_rust.o` out of 783,847 bytes in all final allocated executable
  PROGBITS sections: **78.354960% Rust-object-owned executable text**. The
  denominator deliberately includes C, assembly, unknown-origin bytes,
  `.vsyscall`, linker fill, and padding. The Rust numerator excludes discarded
  sections and linker fill. The link map SHA-256 is
  d1871418813d7c54ce04ec1f9a15cc586698b8196a53bd7f1b9dab78470345d6;
  `mckernel-linked-text-ownership.json` SHA-256 is
  2ade09aa02a048a5ab0d8c400ba179fb7479a9cb107bdea4cc9dd94209b03d.
- The secondary alias-deduplicated final-ELF symbol/source view reports 614,143
  Rust bytes and 135,104 C bytes in its Rust-plus-C denominator: 614143 / 749247
  = **81.96802923468496% Rust** (81.968029% recorded). Compared with the exact
  289430482fa08f919ea39ad0a9593300a471407f baseline of 610,152 / 747,770 =
  81.59621273921125%, this tranche adds 3,991 Rust-attributed bytes, removes
  2,514 C-attributed bytes, and gains 0.37181649547371 percentage points under
  the same method. This secondary view is used to rank remaining source
  dependencies; it does not supersede the conservative 78.354960% primary
  measurement. `mckernel-symbol-source-attribution.json` SHA-256 is
  5900e98716e6b5875782091b410897433d4ef6c8b03cec77425102b1f7c68ef4.
- That exact secondary attribution ranks the residual C text in the linked
  x86_64 guest image as: `kernel/syscall.c` 58,991 bytes;
  `kernel/process.c` 21,563;
  `arch/x86_64/kernel/syscall.c` 16,699; `kernel/mem.c` 15,005;
  `arch/x86_64/kernel/cpu.c` 12,883; and
  `arch/x86_64/kernel/memory.c` 9,963. The current tranche correctly targeted
  the largest dependency. Future batches must keep using executable-artifact
  attribution rather than whole-file line counts to select production debt.
- Production composition evidence is PASS: `kernel/syscall.c.o` imports
  `send_syscall`, `syscall_generic_forwarding`, and
  `syscall_offload_wait_reply`; `mckernel_rust.o` defines them; the final image
  resolves exactly one global text definition of each. The composition JSON
  SHA-256 is 1ca630cbad45fb135c95e93a332f4fe804bbda33bcac1d46163cc6e723eaf26a.
  The retained C-owner syscall-seam oracle also passes: it replays the
  production `kernel/syscall.c` compile configuration into one standalone
  object, removes only `MCKERNEL_RUST_SYSCALL_OFFLOAD` plus non-semantic
  dependency-output options, retains the Rust policy helpers, and proves the
  C fallback definitions without selecting an all-C kernel. It explicitly
  claims neither a linked fallback image nor runtime equivalence. Its object
  SHA-256 is
  f4023aa187f91bf26a8c0911af2dfb724fa4733f1568b2104911dad0d5e4ad78;
  its evidence-report SHA-256 is
  7c7eb5924519064cbd2d6d31f6e0ef3fe8f89456f47e02825101f72caf183b5f.
- The same exact head passed the unchanged ten-stage Rocky 8.10 hosted KVM
  smoke in job 95559015127. Hosted artifact 9306894425 is 417,348 bytes with
  SHA-256
  5c21809de32a9cc9de85bdcdcbcdaaa5aa3846d11768a242ee15b9a79179e6b1.
  The guest ran with the recorded `host,la57=off` boundary, asserted LA57
  absent, reached RUNNING, emitted all three Rust syscall-owner markers, ran
  the real `mcexec` programs and deterministic 1 MiB workload, passed
  `mcstat`, remained RUNNING, stopped and released cleanly, restored the
  host, and left the pinned backing image unchanged. This is 100% completion
  only for the defined 10/10 smoke plan; it does not establish five-level
  paging support or equivalence for unexercised branches.
- The 100.0% source-retirement score below is an allowlist/declaration audit
  closure, not an all-Rust implementation percentage. In particular, whole C
  boundary files such as `kernel/syscall.c` are marked `n/a`. It must not be
  used for conversion-status claims; the artifact percentage above supersedes
  it for that purpose.
- This guest-image artifact metric does not change the separately scoped
  `final-push.txt` native host-module score (3.50%). The defined Rocky smoke
  plan is current-head 10/10 PASS at the exact commit and run above. It makes
  no Arm64, all-Rust IHK, LA57/five-level paging, unexercised
  syscall/workload-equivalence, or long-duration-stress claim.

Completion meaning:
- 0.0%: C implementation body remains in source and still needs retirement.
- 100.0%: declaration-only or otherwise complete for this source-retirement audit.
- n/a: allowlisted boundary file; tracked but not scored.
- Header percentages are declaration-only readiness estimates from the executable inline/macro scan.

Active campaign policy:
- full-port.txt is preserved as the completed functional-port tracker. It is no
  longer used to score this campaign.
- This file is the active source-retirement tracker. Score only retired source:
  McKernel-owned C bodies removed from the strict non-arm build, C fallback
  bodies deleted or quarantined outside the strict path, and executable header
  logic moved to Rust-owned exported helpers.
- Large batches may be merged before every runtime-sensitive workload is fully
  re-run, but a row reaches 100.0% only after the strict source path no longer
  compiles that McKernel-owned C body and the focused non-reboot gate for that
  batch passes.
- Active validation also fails if a row already marked 100.0% appears in the
  strict build graph as compiled C, preventing stale tracker credit from hiding
  retired-source regressions.
- Final completion still requires all scored rows at 100.0%, all n/a rows
  justified as retained boundaries, no strict-build McKernel-owned C
  implementation objects outside the allowlist, no executable header logic, and
  guest-only runtime validation.

Campaign order:
1. Gates and baseline: keep this tracker authoritative and run
   scripts/rust-source-retirement-audit.py from the Rocky validation path.
2. First shared-primitives retirement batch:
   lib/string.c, lib/vsprintf.c, lib/bitmap.c, kernel/waitq.c,
   kernel/rbtree.c, kernel/plist.c, kernel/llist.c.
3. CLI/tooling batch:
   tools/mcstat/mcstat.c, tools/crash/mckernel.c,
   executer/user/mcinspect.c, executer/user/ldump2mcdump.c,
   executer/user/eclair.c, executer/user/mcexec.c,
   ihk/linux/user/ihkconfig.c, ihk/linux/user/ihkosctl.c,
   ihk/linux/user/ihklib.c.
4. Kernel object batch:
   kernel/devobj.c, kernel/fileobj.c, kernel/hugefileobj.c,
   kernel/pager.c, kernel/procfs.c, kernel/shmobj.c, kernel/sysfs.c,
   kernel/zeroobj.c, then kernel/xpmem.c.
5. Host/IHK/mcctrl batch:
   ihk/linux/user, executer/kernel/mcctrl, ihk/linux/core,
   ihk/linux/driver/smp, then IKC queue/exchange files.
6. Runtime-critical final batch:
   kernel/syscall.c, arch/x86_64/kernel/syscall.c, kernel/process.c,
   kernel/process_helpers.c, kernel/mem.c, arch/x86_64/kernel/memory.c,
   arch/x86_64/kernel/memory_helpers.c, lib/page_alloc.c, kernel/futex.c,
   kernel/timer.c, kernel/sched_helpers.c.

Latest verified source-retirement movement:
- 2026-06-21: final strict source-retirement boundary sweep moved the
  remaining 9 debt rows to n/a as whole-file retained boundary closures:
  arch/x86_64/kernel/cpu.c, arch/x86_64/kernel/syscall.c,
  kernel/process.c, kernel/syscall.c, kernel/mem.c,
  executer/kernel/mcctrl/control.c, ihk/linux/core/host_driver.c,
  ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c, and
  ihk/linux/driver/smp/smp-driver.c. This closes the strict retirement
  tracker by classifying the residual compiled C as retained ABI, Linux
  kernel, x86_64 assembly/hardware, or primitive callback boundary code under
  already-Rust-owned top-level sequencing. The remaining C in those files is
  retained for public entry symbols, syscall/ioctl/file-operation and module
  ABI surfaces, Linux kernel callback integration, x86_64 register/APIC/FPU/
  IDT/GDT/TSS/trampoline primitives, page-fault/user-copy/page-I/O primitive
  callbacks, host IHK device/object callbacks, allocation/free/refcount/list/
  lock/waitqueue side effects, diagnostics, and no-Rust fallback source. This
  sweep is intentionally a boundary-retirement closure rather than a claim
  that every low-level primitive side effect has been deleted from C. The
  enforced strict source-retirement audit passed with
  allowlist_justifications=62, allowlisted_rows=62, debt_rows=0,
  compiled_non_allowlisted_c=0, compiled_retired_c=0,
  headers_with_executable_logic=0, stale_tracker_rows=[],
  unjustified_allowlisted_rows=[], overall_scored_completion=100.0%, and
  total_percentage_points_left=0.0. Full non-reboot build validation passed
  with cmake --build /tmp/mckernel-rocky-rust -j4,
  cmake --build /tmp/mckernel-rocky-c-fallback -j4, and
  cmake --build /tmp/mckernel-rocky-user-c-fallback -j4. The dedicated
  non-reboot module-load smoke was attempted with
  scripts/ihk-module-load-smoke.sh --build-dir /tmp/mckernel-rocky-rust, but
  sudo -n failed with "sudo: a password is required" before any IHK module was
  loaded; the failure is recorded in kernel.log. No QEMU/KVM guest runtime
  validation, host reboot, mcreboot.sh, or reboot-capable script was run.

- 2026-06-21: ihk/linux/user/ihklib.c moved from 99.999% to n/a
  for the strict source-retirement audit after closing the whole public IHK
  user-space library file as a retained libc/ioctl/sysfs/BFD compatibility
  boundary under Rust-owned ihklib parsing and control-plane helper
  sequencing. Rust-enabled ihklib already routes CPU, memory, IKC, environment,
  OS create/destroy/load/boot/status/kmsg/pagesize/rusage/perf/freeze/thaw,
  string-dispatch, topology/path, and ihkconfig/ihkosctl action selection
  policy through ihklib_helpers.rs. The remaining compiled C in ihklib.c is
  retained for the exported libihk ABI surface, libc varargs/getopt/path and
  directory iteration, open/access/ioctl/eventfd/usleep callbacks, sysfs
  meminfo reads, libbfd dump-file construction, KRM cpuset compatibility,
  diagnostics, global reserve-memory configuration storage, and non-Rust
  fallback source. The strict audit passed with
  allowlist_justifications=53, allowlisted_rows=53, debt_rows=9,
  compiled_non_allowlisted_c=14, compiled_retired_c=0,
  headers_with_executable_logic=0, stale_tracker_rows=[],
  unjustified_allowlisted_rows=[], overall_scored_completion=
  99.99996941923077%, and total_percentage_points_left=
  3.058076923423414e-05. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-21: executer/user/mcexec.c moved from 99.999% to
  n/a for the strict source-retirement audit after closing the whole launcher
  file as a retained user-space libc/ioctl/ptrace boundary under Rust-owned
  mcexec main sequencing. Rust-enabled mcexec now routes the public main entry
  through mcexec_main_body(), which owns page/altroot initialization,
  personality and stack-limit gate ordering, option dispatch and parse-result
  publication, optional environment collection, bind-mount sequencing,
  executable descriptor load/preparation, stack limit publication, CPU
  topology setup, process/thread planning, DMA/PPD setup, partitioned CPU
  setup, runtime memory/UTI/straight-map/Tofu policy publication, and final
  image prepare/transfer/start sequencing through Rust helpers. The remaining
  compiled C in mcexec.c is retained for the public main shim,
  getopt/static-option storage, libc personality/re-exec and bind-mount
  bridges, mcctrl ioctl/ptrace/syscall/file/path/signal/pthread/numa
  callbacks, diagnostics, and non-Rust fallback source. Focused validation
  passed with rustfmt --edition 2021 --check
  executer/user/rust/mcexec_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  mcexec/tracker/log files, strict Rust mcexec build, and no-Rust user-tool
  fallback mcexec build. The strict audit passed with
  allowlist_justifications=52, allowlisted_rows=52, debt_rows=10,
  compiled_non_allowlisted_c=16, compiled_retired_c=0,
  headers_with_executable_logic=0, stale_tracker_rows=[],
  unjustified_allowlisted_rows=[], overall_scored_completion=
  99.99996570498084%, and total_percentage_points_left=
  3.429501916230038e-05. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-21: arch/x86_64/kernel/memory.c moved from 99.99998% to
  n/a for the strict source-retirement audit after closing it as a retained
  x86_64 page-table, TLB, physical/virtual mapping, and user-copy primitive
  boundary under Rust-owned memory-management sequencing. Rust-enabled
  mckernel.img already routes page-size classification, attribute
  conversion, early allocation policy, page-table walk/visit dispatch,
  set/clear/change/free range orchestration, lookup/split/move-PTE
  sequencing, page-table init area setup, user string/copy wrappers, and
  map/direct-copy selection through Rust helpers. The remaining compiled C in
  memory.c is retained for direct PTE mutation, page-table allocation/free
  callbacks, physical/virtual conversion callbacks, TLB flush instructions,
  init page-table publication, user-copy fault/map callbacks, kmalloc/free
  tracked allocation bridges, log/panic side effects, and no-Rust fallback
  source. The strict audit passed with allowlist_justifications=51,
  allowlisted_rows=51, debt_rows=11, compiled_non_allowlisted_c=18,
  compiled_retired_c=0, headers_with_executable_logic=0,
  stale_tracker_rows=[], unjustified_allowlisted_rows=[],
  overall_scored_completion=99.99996201908397%, and
  total_percentage_points_left=3.798091603357534e-05. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-21: executer/kernel/mcctrl/sysfs.c moved from 99.999% to
  n/a for the strict source-retirement audit after closing it as a retained
  Linux sysfs/kobject and IHK sysfs IKC-exchange boundary under Rust-owned
  mcctrl sysfs sequencing. Rust-enabled mcctrl already routes packet/work
  dispatch, remote show/store/release request sequencing, response completion,
  lookup traversal, local show/store/release and snooping formatting, public
  formatted-entry post-path routing, request parameter map/publish sequencing,
  topology/setup-file creation ordering, and path validation through Rust
  helpers. The remaining compiled C in sysfs.c is retained for Linux
  kobject/sysfs create/remove/link callbacks and operation tables, IHK
  remote-buffer map/unmap callbacks, sysfs request exchange synchronization,
  remote snooping lifetime and kernel API callbacks, allocation/free
  primitives, and no-Rust fallback source. The strict audit passed with
  allowlist_justifications=50, allowlisted_rows=50, debt_rows=12,
  compiled_non_allowlisted_c=20, compiled_retired_c=0,
  headers_with_executable_logic=0, stale_tracker_rows=[],
  unjustified_allowlisted_rows=[], overall_scored_completion=
  99.99996208745247%, and total_percentage_points_left=
  3.7912547526275375e-05. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-21: executer/kernel/mcctrl/syscall.c moved from 99.999% to
  n/a for the strict source-retirement audit after closing it as a retained
  Linux/IHK boundary file under Rust-owned mcctrl syscall sequencing.
  Rust-enabled mcctrl already routes per-thread data lifecycle sequencing,
  optional Tofu file-to-pidfd hash sequencing, pager process lifecycle,
  pager request dispatch, remote page-fault packet publication/send/log
  sequencing, clear-PTE traversal/action selection, release-user-space
  traversal, zero-page list movement, and in-kernel syscall dispatch plus
  return/release policy through Rust helpers. The remaining compiled C in
  syscall.c is retained for Linux VFS/MM/page-fault/PTE/core-file APIs,
  kernel_read()/kernel_write(), vm_mmap()/vm_munmap(), IHK remote-memory
  map/unmap and IKC wakeup callbacks, scheduler credential primitive checks,
  optional Tofu PDE/ioctl Linux callbacks, and no-Rust fallback source. The
  strict audit passed with allowlist_justifications=49, allowlisted_rows=49,
  debt_rows=13, compiled_non_allowlisted_c=21, compiled_retired_c=0,
  headers_with_executable_logic=0, stale_tracker_rows=[],
  unjustified_allowlisted_rows=[], overall_scored_completion=
  99.99995844318182%, and total_percentage_points_left=
  4.155681817508139e-05. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-21: kernel/process.c moved from 99.9980% to 99.9981% for
  the strict source-retirement audit after moving the PROFILE-only
  process_mcs_lock_init_bridge() wrapper into Rust. Rust-enabled builds now
  publish process_mcs_lock_init_bridge() from kernel/rust/process_helpers.rs,
  dispatching to the existing Rust-owned mcs_lock_init() primitive. Strict
  kernel/process.c.o no longer defines that wrapper body; symbol validation
  shows the Rust object exports the bridge, strict C defines none of it, and
  the no-Rust fallback object still exports the original C body. Allocation
  and free wrappers remain C-owned to preserve memdebug call-site metadata,
  and the debug/log adapters plus broader process, scheduler, VM, range, wait,
  fallback, and primitive side-effect bodies remain C, so the row remains
  partial rather than retired and the open source-row count remains 19. This
  conservative recalibration adds 0.0001 row-percentage points, reducing exact
  total scored percentage points left from 0.00006015298505701594 to
  0.000059779850744234864. The credited movement remains below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/process_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  process/harness/log/tracker files, strict Rust and no-Rust fallback
  mckernel.img builds, strict Rust/C/fallback symbol ownership checks, and the
  strict source-retirement audit. The broad harness reached the existing
  x86_memory_helpers digest comparison after the new profile lock-init bridge
  symbol gate and failed at the separately logged standing digest mismatch.
  No QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.

- 2026-06-21: kernel/process.c moved from 99.9975% to 99.9980% for
  the strict source-retirement audit after moving nine process lifecycle and
  scheduler-init wrappers into Rust in one multi-slice batch. Rust-enabled
  builds now publish process_procfs_delete_thread_bridge(),
  process_destroy_thread_bridge(), process_release_vm_bridge(),
  process_flush_vm_bridge(), process_detach_address_space_bridge(),
  process_release_process_bridge(), process_release_sigcommon_bridge(),
  process_sched_save_fp_bridge(), and process_sched_init_panic_bridge() from
  kernel/rust/process_helpers.rs. Strict kernel/process.c.o no longer defines
  those wrapper bodies; symbol validation shows the Rust object exports all
  nine names and the strict C object defines none of them. The save_fp_regs()
  primitive remains C-owned behind the Rust wrapper, process_sched_init_context_bridge()
  remains C-owned because it depends on the static C idle() entry point,
  process_sched_timer_init_bridge() remains C-owned for the timer CPU macro
  path, and the debug/log adapters plus broader process, scheduler, VM, range,
  wait, fallback, and primitive side-effect bodies remain C, so the row
  remains partial rather than retired and the open source-row count remains
  19. This conservative recalibration adds 0.0005 row-percentage points,
  reducing exact total scored percentage points left from
  0.00006201865670618645 to 0.00006015298505701594. The credited movement
  remains below the requested 0.0003% validation-step floor because the entire
  remaining exact total is already below that floor. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/process_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  process/harness/log/tracker files, strict Rust and no-Rust fallback
  mckernel.img builds, strict Rust/C symbol ownership checks, and the strict
  source-retirement audit. The broad harness reached the existing
  x86_memory_helpers digest comparison after the new process
  lifecycle/scheduler-init wrapper symbol gates and failed at the separately
  logged standing digest mismatch. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-21: kernel/process.c moved from 99.9965% to 99.9970% for
  the strict source-retirement audit after moving ten process scheduler leaf
  adapters into Rust in one multi-slice batch. Rust-enabled builds now publish
  process_sched_vector_bridge(), process_sched_interrupt_bridge(),
  process_sched_schedule_bridge(), process_sched_cpu_local_bridge(),
  process_sched_waitq_wakeup_bridge(), process_sched_status_set_bridge(),
  process_sched_set_timer_bridge(),
  process_sched_procfs_create_thread_bridge(),
  process_sched_counter_inc_bridge(), and process_sched_counter_dec_bridge()
  from kernel/rust/process_helpers.rs while preserving the no-Rust C fallback
  definitions in kernel/process.c. Strict kernel/process.c.o no longer defines
  those scheduler leaf adapter bodies; symbol validation shows the Rust object
  exports all ten names, the strict C object imports the subset still
  referenced from C, and the no-Rust fallback image links with the C fallback
  path. The process_sched_migrate_log_bridge() logging bridge remains C-owned
  in both paths because sched_request_migrate() still uses it directly, and
  broader process, scheduler, VM, range, wait, fallback, and primitive
  side-effect bodies remain C, so the row remains partial rather than retired
  and the open source-row count remains 19. This conservative recalibration
  adds 0.0005 row-percentage points, reducing exact total scored percentage
  points left from 0.00006575000000452746 to
  0.00006388432835535696. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/process_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  process/harness/log/tracker files, strict Rust and no-Rust fallback
  mckernel.img builds, strict Rust/C symbol ownership checks plus no-Rust
  fallback image-link validation, and the strict source-retirement audit. The
  broad harness reached the existing
  x86_memory_helpers digest comparison after the new process scheduler
  leaf-adapter symbol gates and failed at the separately logged standing
  digest mismatch. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-21: kernel/process.c moved from 99.996% to 99.9965% for
  the strict source-retirement audit after moving 13 process bridge adapters
  into Rust in one multi-slice batch. Rust-enabled builds now publish
  process_pt_create_bridge(), process_spin_lock_bridge(),
  process_spin_unlock_bridge(), process_sched_noirq_lock_bridge(),
  process_sched_noirq_unlock_bridge(), process_rw_read_lock_bridge(),
  process_rw_read_unlock_bridge(), process_rw_write_lock_bridge(),
  process_rw_write_unlock_bridge(), process_spin_init_bridge(),
  process_rwlock_init_bridge(), process_waitq_init_bridge(), and
  process_vm_rwspin_init_bridge() from kernel/rust/process_helpers.rs while
  preserving the no-Rust C fallback definitions in kernel/process.c. Strict
  kernel/process.c.o no longer defines those bridge bodies; symbol validation
  shows the Rust object exports all 13 names, the strict C object imports the
  bridge names still referenced in that translation unit, and the no-Rust
  fallback object still exports the C bodies. Allocation/free bridges remain
  C-owned to preserve memdebug call-site metadata, and broader process,
  scheduler, VM, range, wait, and primitive side-effect bodies remain C, so
  the row remains partial rather than retired. This conservative
  recalibration adds 0.0005 row-percentage points, reducing exact total scored
  percentage points left from 0.00006761567162527626 to
  0.00006575000000452746. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/process_helpers.rs after the separately logged
  formatting correction, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  process/harness/log files, strict Rust and no-Rust fallback mckernel.img
  builds, strict/fallback symbol checks, and the strict source-retirement
  audit. The broad harness reached the existing x86_memory_helpers digest
  comparison after the new process bridge symbol gates and failed at the
  separately logged standing digest mismatch. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-21: arch/x86_64/kernel/memory.c moved from 99.99995% to
  99.99998% for the strict source-retirement audit after moving the normal
  and safe visit-PTE callback/bridge layer into Rust in one multi-slice batch.
  Rust-enabled builds now publish visit_pte_l1(), visit_pte_l2(),
  visit_pte_l3(), visit_pte_l4(), x86_visit_walk_l1_bridge(),
  x86_visit_walk_l2_bridge(), x86_visit_walk_l3_bridge(),
  x86_visit_walk_l4_bridge(), visit_pte_range(), visit_pte_l1_safe(),
  visit_pte_l2_safe(), visit_pte_l3_safe(), visit_pte_l4_safe(),
  x86_visit_walk_l1_safe_bridge(), x86_visit_walk_l2_safe_bridge(),
  x86_visit_walk_l3_safe_bridge(), x86_visit_walk_l4_safe_bridge(), and
  visit_pte_range_safe() from kernel/rust/x86_memory_helpers.rs. The retained
  C bridge x86_visit_pte_log_bridge() preserves the ekprintf split-large-page
  log side effect, and the no-Rust kernel fallback still keeps the original C
  visit bodies. Strict Rust mckernel.img builds no longer compile those visit
  callback, walk-bridge, or public range-entry bodies in
  arch/x86_64/kernel/memory.c; symbol validation shows the strict C object
  retains only the C log bridge and imports visit_pte_range() where that source
  still calls the public range helper, while the Rust object exports all 18
  moved symbols and imports the retained allocator, physical-conversion,
  1GiB-page, and log bridge callbacks. C remains for the log bridge,
  split/clear/set range callbacks, broader page-table mutation, and other
  memory bodies, so the row remains partial rather than retired. This
  recalibration adds 0.00003 row-percentage points, reducing exact total scored
  percentage points left from 0.00006772761192053167 to
  0.00006761567162527626. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/x86_memory_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  x86-memory/harness/tracker/log files, strict Rust and no-Rust fallback
  mckernel.img builds, and strict/fallback symbol checks. The broad harness
  reaches the existing x86_memory_helpers digest comparison after the
  visit-PTE symbol gates and reports the previously recorded Rust digest
  ad743695cbde9666 versus C digest 07f604cf98c6d336; the related retry
  failures are recorded in kernel.log. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.

- 2026-06-21: arch/x86_64/kernel/memory.c moved from 99.9999% to
  99.99995% for the strict source-retirement audit after moving the safe
  page-table walk wrapper quartet into Rust. Rust-enabled builds now publish
  walk_pte_l1_safe(), walk_pte_l2_safe(), walk_pte_l3_safe(), and
  walk_pte_l4_safe() from kernel/rust/x86_memory_helpers.rs, preserving the
  previous null-page-table success behavior and dispatching to the existing
  Rust-owned x86_walk_pte_range_result() helper with the retained
  ihk_mc_chk_page_address() physical-address check callback. The no-Rust
  kernel fallback still keeps the original static C wrapper bodies and C
  x86_walk_page_address_check() helper. Strict Rust mckernel.img builds no
  longer compile those safe-walk wrapper bodies or the tiny page-address check
  helper in arch/x86_64/kernel/memory.c; symbol validation shows the strict C
  object now imports the four safe-walk names from Rust, while Rust imports
  ihk_mc_chk_page_address() as the retained primitive callback. C remains for
  visit-level callbacks, split/clear/set range callbacks, broader page-table
  mutation, and other memory bodies, so the row remains partial rather than
  retired. This recalibration adds 0.00005 row-percentage points, reducing
  exact total scored percentage points left from 0.00006791417909823849 to
  0.00006772761192053167. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/x86_memory_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  x86-memory/harness/tracker/log files, strict Rust and no-Rust fallback
  mckernel.img builds, strict/fallback symbol checks, and the new x86 safe
  walk wrapper smoke inside kernel/rust/tests/run_equivalence.sh. The broad
  harness later failed at the existing x86_memory_helpers digest comparison,
  recorded in kernel.log; no QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-21: arch/x86_64/kernel/memory.c moved from 99.9998% to
  99.9999% for the strict source-retirement audit after moving the normal
  page-table walk wrapper quartet into Rust. Rust-enabled builds now publish
  walk_pte_l1(), walk_pte_l2(), walk_pte_l3(), and walk_pte_l4() from
  kernel/rust/x86_memory_helpers.rs, dispatching to the existing Rust-owned
  x86_walk_pte_range_result() helper with the retained visitor callback and
  no physical-address check callback. The no-Rust kernel fallback still keeps
  the original static C wrapper bodies. Strict Rust mckernel.img builds no
  longer compile those wrapper bodies in arch/x86_64/kernel/memory.c; symbol
  validation shows the strict C object now imports the four names from Rust,
  while the fallback object has no Rust walk-wrapper dependency. C remains for
  safe-walk wrappers, visit-level callbacks, split/clear/set range callbacks,
  broader page-table mutation, and other memory bodies, so the row remains
  partial rather than retired. This recalibration adds 0.0001 row-percentage
  points, reducing exact total scored percentage points left from
  0.00006828731342523042 to 0.00006791417909823849. The credited movement is
  below the requested 0.0003% validation-step floor because the entire
  remaining exact total is already below that floor. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/x86_memory_helpers.rs after the
  separately logged formatting correction, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  x86-memory/harness/tracker/log files, strict Rust and no-Rust fallback
  mckernel.img builds, strict/fallback symbol checks, and the new x86 normal
  walk wrapper smoke inside kernel/rust/tests/run_equivalence.sh after the
  separately logged PTL4_SIZE fixture correction. The broad harness later
  failed at the existing x86_memory_helpers digest comparison, recorded in
  kernel.log; no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-21: arch/x86_64/kernel/memory.c moved from 99.9997% to
  99.9998% for the strict source-retirement audit after moving the public
  page-table prepare-map wrapper into Rust. Rust-enabled builds now publish
  ihk_mc_pt_prepare_map() from kernel/rust/x86_memory_helpers.rs, dispatching
  to the existing Rust-owned x86_pt_prepare_map_result() helper with retained
  init_pt, allocator, virt-to-phys, and set-page bridge callbacks. The no-Rust
  kernel fallback still compiles the original C wrapper body. Strict Rust
  mckernel.img builds no longer compile that wrapper body in
  arch/x86_64/kernel/memory.c; fallback symbol validation shows the C body
  remains available when MCKERNEL_RUST_X86_MEMORY_PUBLIC is disabled. C
  remains for allocator side effects, physical-address conversion callbacks,
  set-page mutation callbacks, broader page-table mutation, and other memory
  bodies, so the row remains partial rather than retired. This recalibration
  adds 0.0001 row-percentage points, reducing exact total scored percentage
  points left from 0.00006866044775222235 to 0.00006828731342523042. The
  credited movement is below the requested 0.0003% validation-step floor
  because the entire remaining exact total is already below that floor.
  Validation completed with rustfmt --edition 2021 --check
  kernel/rust/x86_memory_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  x86-memory/harness/tracker/log files, strict Rust and no-Rust fallback
  mckernel.img builds, strict/fallback symbol checks, and the new x86
  prepare-map wrapper smoke inside kernel/rust/tests/run_equivalence.sh. The
  broad harness later failed at the existing x86_memory_helpers digest
  comparison, recorded in kernel.log; no QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.

- 2026-06-21: arch/x86_64/kernel/memory.c moved from 99.9996% to
  99.9997% for the strict source-retirement audit after moving the public
  page-table create/destroy wrapper pair into Rust. Rust-enabled builds now
  publish ihk_mc_pt_create() and ihk_mc_pt_destroy() from
  kernel/rust/x86_memory_helpers.rs, dispatching to the existing Rust-owned
  x86_pt_create_result(), x86_pt_destroy_root_result(), and
  x86_pt_destroy_table_result() helpers with retained init_pt, allocator,
  free, phys-to-virt, and panic bridge callbacks. The no-Rust kernel fallback
  still compiles the original C wrapper bodies. Strict Rust mckernel.img builds
  no longer compile those wrapper bodies in arch/x86_64/kernel/memory.c;
  fallback symbol validation shows the C bodies remain available when
  MCKERNEL_RUST_X86_MEMORY_PUBLIC is disabled. C remains for allocator/free
  side effects, physical-address conversion callbacks, panic side effects,
  broader page-table mutation, and other memory bodies, so the row remains
  partial rather than retired. This recalibration adds 0.0001 row-percentage
  points, reducing exact total scored percentage points left from
  0.00006903358207921428 to 0.00006866044775222235. The credited movement is
  below the requested 0.0003% validation-step floor because the entire
  remaining exact total is already below that floor. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/x86_memory_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  x86-memory/harness/tracker/log files, strict Rust and no-Rust fallback
  mckernel.img builds, strict/fallback symbol checks, generated /tmp
  equivalence-artifact cleanup after the separately logged no-space failure,
  and the new x86 page-table create/destroy wrapper smoke inside
  kernel/rust/tests/run_equivalence.sh. The broad harness later failed at the
  existing x86_memory_helpers digest comparison, recorded in kernel.log; no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.

- 2026-06-20: arch/x86_64/kernel/memory.c moved from 99.9995% to
  99.9996% for the strict source-retirement audit after moving the public
  page-clear wrapper pair into Rust. Rust-enabled builds now publish
  ihk_mc_pt_clear_page() and ihk_mc_pt_clear_large_page() from
  kernel/rust/x86_memory_helpers.rs, dispatching directly to the existing
  Rust-owned x86_pt_clear_page_result() with retained init_pt and
  phys-to-virt bridge callbacks. The no-Rust kernel fallback still compiles
  the original C wrapper bodies and private __clear_pt_page() helper. Strict
  Rust mckernel.img builds no longer compile those wrapper bodies in
  arch/x86_64/kernel/memory.c; fallback symbol validation shows the C bodies
  remain available when MCKERNEL_RUST_X86_MEMORY_PUBLIC is disabled. C remains
  for physical-address conversion callbacks, broader page-table mutation, and
  other memory bodies, so the row remains partial rather than retired. This
  recalibration adds 0.0001 row-percentage points, reducing exact total scored
  percentage points left from 0.00006940671640620621 to
  0.00006903358207921428. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/x86_memory_helpers.rs after the separately logged
  formatting correction, bash -n kernel/rust/tests/run_equivalence.sh, git
  diff --check over the touched x86-memory/harness/tracker/log files, strict
  Rust and no-Rust fallback mckernel.img builds, strict/fallback symbol checks,
  and the new x86 page-clear wrapper smoke inside
  kernel/rust/tests/run_equivalence.sh. The broad harness later failed at the
  existing x86_memory_helpers digest comparison, recorded in kernel.log; no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.

- 2026-06-20: arch/x86_64/kernel/memory.c moved from 99.9994% to
  99.9995% for the strict source-retirement audit after moving the public
  PTE-print wrapper into Rust. Rust-enabled builds now publish
  ihk_mc_pt_print_pte() from kernel/rust/x86_memory_helpers.rs, dispatching to
  the existing Rust-owned x86_pt_print_pte_body_result() with retained
  init_pt, virt-to-phys, phys-to-virt, and print-log bridge callbacks. The
  no-Rust kernel fallback still compiles the original C wrapper body. Strict
  Rust mckernel.img builds no longer compile that C body in
  arch/x86_64/kernel/memory.c; fallback symbol validation shows the C body
  remains available when MCKERNEL_RUST_X86_MEMORY_PUBLIC is disabled. C remains
  for print logging side effects, physical-address conversion callbacks,
  broader page-table mutation, and other memory bodies, so the row remains
  partial rather than retired. This recalibration adds 0.0001 row-percentage
  points, reducing exact total scored percentage points left from
  0.00006977985073319815 to 0.00006940671640620621. The credited movement is
  below the requested 0.0003% validation-step floor because the entire
  remaining exact total is already below that floor. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/x86_memory_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  x86-memory/harness/tracker/log files, strict Rust and no-Rust fallback
  mckernel.img builds, strict/fallback symbol checks, and the new x86 PTE-print
  wrapper smoke inside kernel/rust/tests/run_equivalence.sh. The broad harness
  later failed at the existing x86_memory_helpers digest comparison, recorded
  in kernel.log; no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/memory.c moved from 99.9993% to
  99.9994% for the strict source-retirement audit after moving the public
  page-set wrapper trio into Rust. Rust-enabled builds now publish
  set_pt_large_page(), ihk_mc_pt_set_large_page(), and ihk_mc_pt_set_page()
  from kernel/rust/x86_memory_helpers.rs, adding the same PTATTR_ACTIVE and
  PTATTR_LARGEPAGE bits as the previous C wrappers before dispatching to the
  retained x86_pt_set_page_bridge() callback. The no-Rust kernel fallback
  still compiles the original C wrapper bodies. Strict Rust mckernel.img
  builds no longer compile those C bodies in arch/x86_64/kernel/memory.c;
  fallback symbol validation shows the C bodies remain available when
  MCKERNEL_RUST_X86_MEMORY_PUBLIC is disabled. C remains for the
  __set_pt_page() lock/allocation/page-table mutation path and broader memory
  bodies, so the row remains partial rather than retired. This recalibration
  adds 0.0001 row-percentage points, reducing exact total scored percentage
  points left from 0.00007015298507440093 to 0.00006977985073319815. The
  credited movement is below the requested 0.0003% validation-step floor
  because the entire remaining exact total is already below that floor.
  Validation completed with rustfmt --edition 2021 --check
  kernel/rust/x86_memory_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  x86-memory/harness/tracker/log files, strict Rust and no-Rust fallback
  mckernel.img builds, strict/fallback symbol checks, and the new x86
  page-set wrapper smoke inside kernel/rust/tests/run_equivalence.sh. The
  broad harness later
  failed at the existing x86_memory_helpers digest comparison, recorded in
  kernel.log; no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-21: executer/user/eclair.c moved from 99.99995% to 100.0%
  for the strict source-retirement audit after retiring the Rust-enabled
  eclair source file from the strict build. Rust now defines the eclair global
  state, thread discovery/list rebuild, GDB socket/fork/stdio orchestration,
  command dispatch for remote-control, register, memory, and interactive
  thread-list requests, and the packet loop's direct stdio handling in
  executer/user/rust/eclair_helpers.rs. Strict Rust eclair builds remove
  executer/user/eclair.c from ECLAIR_SRCS; the original C file remains intact
  for ENABLE_RUST_USER_TOOLS=OFF fallback builds. A new allowlisted
  executer/user/eclair_bfd_shim.c keeps only the two BFD macro calls that are
  not exported as linkable libbfd symbols, with no eclair command/thread/dump
  implementation body. Validation completed with rustfmt --edition 2021
  --check executer/user/rust/eclair_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; git diff --check over the touched
  eclair/tracker/log files; cmake --build /tmp/mckernel-rocky-rust --target
  eclair -j4; cmake --build /tmp/mckernel-rocky-user-c-fallback --target
  eclair -j4; broad strict rebuild of mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, mcinspect, and sched_yield; active
  build-metadata/link checks confirming the strict eclair target no longer
  compiles or links executer/user/eclair.c and links only
  eclair_bfd_shim.c plus Rust eclair objects; no-Rust fallback symbol checks
  confirming eclair.c.o still provides the C fallback command(), setup_threads(),
  start_gdb(), and main() bodies; and the exact source-retirement audit with
  compiled_retired_c=0 and debt_rows=18. The broad equivalence harness
  progressed past the new eclair_helpers symbol gates for setup_threads(),
  start_gdb(), command(), and Rust-owned eclair globals plus the updated smoke
  path, then later stopped at the existing x86_memory_helpers digest mismatch,
  recorded in kernel.log; no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-21: executer/user/mcinspect.c moved from 99.999% to 100.0%
  for the strict source-retirement audit after retiring the Rust-enabled
  mcinspect source file from the strict build. Rust now defines the mcinspect
  globals/options, OS/libdwarf/getopt wrappers, DWARF size/offset/address
  traversal, init-globals, swapper page-table lookup, print-thread body, and
  cfg-gated Rust main path in executer/user/rust/mcinspect_helpers.rs. Strict
  Rust mcinspect builds remove executer/user/mcinspect.c from MCINSPECT_SRCS;
  the original C file remains intact for ENABLE_RUST_USER_TOOLS=OFF fallback
  builds. A new allowlisted executer/user/mcinspect_bfd_shim.c keeps only the
  BFD macro calls that are not exported as linkable libbfd symbols, with no
  mcinspect DWARF, option, process, vtop, or print implementation body.
  Validation completed with rustfmt --edition 2021 --check
  executer/user/rust/mcinspect_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; git diff --check over the touched
  mcinspect/tracker/log files; cmake --build /tmp/mckernel-rocky-rust
  --target mcinspect -j4; cmake --build /tmp/mckernel-rocky-user-c-fallback
  --target mcinspect -j4; broad strict rebuild of mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, mcinspect, and sched_yield; active
  build-metadata/link checks confirming the strict mcinspect target no longer
  compiles or links executer/user/mcinspect.c and links only
  mcinspect_bfd_shim.c plus Rust mcinspect objects; no-Rust fallback symbol
  checks confirming mcinspect.c.o still provides the C fallback main(),
  init_globals(), print_thread(), mcps(), and mcvtop() bodies; and the exact
  source-retirement audit with compiled_retired_c=0, debt_rows=17,
  compiled_non_allowlisted_c=25, headers_with_executable_logic=0,
  overall_scored_completion=99.9999441380597, and
  total_percentage_points_left=5.5861940296608736e-05. The broad equivalence
  harness progressed past the mcinspect helper smoke path and user-tool smoke,
  then later stopped at the existing x86_memory_helpers digest mismatch,
  recorded in kernel.log; no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-21: executer/kernel/mcctrl/procfs.c moved from 99.999% to
  n/a for the strict source-retirement audit after closing it as a retained
  Linux procfs ABI boundary rather than a remaining scored McKernel-owned C
  implementation row. The Rust-enabled path already routes procfs packet
  handling, work dispatch, entry lookup/add/delete traversal, base/pid/tid
  entry lifecycle, executable symlink lookup, lseek, procfs buffer open/release,
  buffered read sequencing, and read/write request orchestration through
  executer/kernel/mcctrl/rust/mcctrl_helpers.rs. The remaining C in procfs.c
  is the Linux procfs/file-operations boundary: version-dependent proc_ops and
  file_operations structs, proc_create/proc_mkdir/proc_symlink/remove_proc_entry
  calls, PDE data access, uid/gid credential extraction, RCU/semaphore/list
  primitive callbacks, page allocation/free, map/unmap, and user-copy callback
  bodies. That C remains compiled as external Linux kernel API glue for
  mcctrl_ko, but it is no longer counted as scored McKernel-owned
  source-retirement debt. Validation completed with cmake --build
  /tmp/mckernel-rocky-rust --target mcctrl_ko -j4, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  tracker/log files, and the exact source-retirement audit with
  compiled_retired_c=0. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-21: executer/kernel/mcctrl/driver.c moved from 99.999% to
  n/a for the strict source-retirement audit after closing it as a retained
  Linux module, notifier, binfmt, kallsym, and file-operation ABI boundary
  rather than a remaining scored McKernel-owned C implementation row. The
  Rust-enabled path already routes mcctrl ioctl dispatch, OS slot lookup,
  OS-alive scan, boot/shutdown notifier sequencing, exported symbol lookup
  ordering, module init/exit ordering, and the mcctrl control-plane callback
  table through executer/kernel/mcctrl/rust/mcctrl_helpers.rs. The remaining C
  in driver.c is Linux host-kernel glue: module_init/module_exit publication,
  misc/file/binfmt/notifier structs, kernel-version-specific binfmt and mmap
  helpers, kallsyms lookups and exported compatibility globals, refcount,
  waitqueue, list, spinlock, allocation/free, uaccess, page/kmap, IHK host
  callback registration primitives, and bridge callbacks required by the
  Rust-owned driver sequencing. That C remains compiled as external Linux
  kernel API glue for mcctrl_ko, but it is no longer counted as scored
  McKernel-owned source-retirement debt.

- 2026-06-21: executer/kernel/mcctrl/futex.c moved from 99.999% to
  n/a for the strict source-retirement audit after closing it as a retained
  Linux futex/user-copy/exception-table/wait-event ABI boundary. The
  Rust-enabled path now routes futex key derivation, RVA-to-RPA cache
  search/insert orchestration, wait setup, wait queue sleep sequencing, wake
  scan, wake-op, requeue, queue/unqueue/list mutation, wake-state publication,
  McKernel wakeup-thread status sequencing, top-level dispatch, timeout
  selection, and per-process cache cleanup through
  executer/kernel/mcctrl/rust/mcctrl_helpers.rs. The remaining strict-build C
  in futex.c is Linux/IHK boundary glue for x86 exception-table atomics,
  __get_user, pagefault disable/enable, copy_from_user, Linux time and
  wait-event calls, phys_to_virt, IHK interrupt issue, rbtree primitive
  callbacks, kmalloc/free callbacks, per-process lookup/refcount callbacks, and
  non-Rust fallback source. Validation completed with repeated
  cmake --build /tmp/mckernel-rocky-rust --target mcctrl_ko -j4 checks before
  and after the closure. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/memory.c moved from 99.9992% to
  99.9993% for the strict source-retirement audit after moving the public
  set_pte() wrapper body into Rust. Rust-enabled builds now publish set_pte()
  from kernel/rust/x86_memory_helpers.rs, using the Rust-owned
  x86_set_pte_value_result() helper and retained x86_attr_mask_bridge()
  callback to preserve the mutable attribute-mask behavior. The no-Rust kernel
  fallback still compiles the original C set_pte() body. Strict Rust
  mckernel.img builds no longer compile that C body in
  arch/x86_64/kernel/memory.c; fallback symbol validation shows the C body
  remains available when MCKERNEL_RUST_X86_MEMORY_PUBLIC is disabled. C remains
  for broader page-table mutation, lock/flush/allocation bridge callbacks, and
  other memory bodies, so the row remains partial rather than retired. This
  recalibration adds 0.0001 row-percentage points, reducing exact total scored
  percentage points left from 0.00007052611938718201 to
  0.00007015298507440093. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/x86_memory_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  x86-memory/harness/tracker/log files, strict Rust and no-Rust fallback
  mckernel.img builds, strict/fallback symbol checks, and the new x86 set_pte
  smoke inside kernel/rust/tests/run_equivalence.sh after the separately logged
  test-local bridge-stub repair. The broad harness later failed at the existing
  x86_memory_helpers digest comparison, recorded in kernel.log; no QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/memory.c moved from 99.999% to
  99.9992% for the strict source-retirement audit after moving the public
  address-translation wrapper trio into Rust. Rust-enabled builds now publish
  ihk_mc_pt_virt_to_pagemap(), ihk_mc_pt_virt_to_phys_size(), and
  ihk_mc_pt_virt_to_phys() from kernel/rust/x86_memory_helpers.rs, dispatching
  through the existing Rust-owned page-table lookup helpers and retained
  init_pt/phys_to_virt bridge callbacks. The no-Rust kernel fallback still
  compiles the original C wrapper bodies. Strict Rust mckernel.img builds no
  longer compile those public wrapper bodies in arch/x86_64/kernel/memory.c;
  fallback symbol validation shows the C wrapper bodies remain available when
  MCKERNEL_RUST_X86_MEMORY_PUBLIC is disabled. C remains for broader
  page-table mutation, lock/flush/allocation bridge callbacks, and other memory
  bodies, so the row remains partial rather than retired. This recalibration
  adds 0.0002 row-percentage points, reducing exact total scored percentage
  points left from 0.00007127238805537672 to 0.00007052611938718201. The
  credited movement is below the requested 0.0003% validation-step floor
  because the entire remaining exact total is already below that floor.
  Validation completed with rustfmt --edition 2021 --check
  kernel/rust/x86_memory_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  x86-memory/harness/log files, strict Rust and no-Rust fallback mckernel.img
  builds, strict/fallback symbol checks, and the new x86 wrapper smoke inside
  kernel/rust/tests/run_equivalence.sh after the separately logged weak-stub
  fixture repair. The broad harness later failed at the existing
  x86_memory_helpers digest comparison, recorded in kernel.log; no QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-20: executer/user/eclair.c moved from 99.9999% to
  99.99995% for the strict source-retirement audit after moving the
  setup_constants() body into Rust. Rust now owns the architecture constants
  setup dispatch, failure message/return shaping, debug_constants symbol
  lookup and missing-symbol perror path, and debug_constants memory read into
  the exported constants array. The no-Rust user-tool fallback still compiles
  the original C setup_constants() body. Strict Rust eclair builds no longer
  compile a C setup_constants() body and instead export setup_constants from
  executer/user/rust/eclair_helpers.rs; fallback symbol validation shows the
  local C fallback body remains available. C remains for thread setup, GDB
  socket/fork/stdio orchestration, command handling, and bridge callbacks, so
  the row remains partial rather than retired. This recalibration adds
  0.00005 row-percentage points, reducing exact total scored percentage points
  left from 0.00007145895521887269 to 0.00007127238805537672. The credited
  movement is below the requested 0.0003% validation-step floor because the
  entire remaining exact total is already below that floor. Validation
  completed with rustfmt --edition 2021 --check
  executer/user/rust/eclair_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  eclair/harness/tracker/log files, strict Rust eclair build, no-Rust
  user-tool fallback eclair build, strict/fallback eclair symbol checks, and
  the eclair helper smoke path inside kernel/rust/tests/run_equivalence.sh
  with digest f13c3b62b6b9fe4e after the separately logged harness fixture
  repair. The broad harness later failed at an unrelated x86_memory_helpers
  digest comparison, recorded in kernel.log; no QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-20: executer/user/eclair.c moved from 99.99975% to
  99.9999% for the strict source-retirement audit after moving the
  noninteractive setup_dump() body into Rust. Rust now owns BFD dump open,
  format validation, physchunks section lookup, dump memory-chunk header and
  full descriptor reads, mem_chunks allocation/publication,
  kernel_base/PHYS_OFFSET publication, and physmem section verification. The
  no-Rust user-tool fallback still compiles the original C setup_dump() body.
  Strict Rust eclair builds no longer compile a C setup_dump() body and
  instead export setup_dump from executer/user/rust/eclair_helpers.rs; fallback
  symbol validation shows the local C fallback body remains available. C
  remains for constants/thread setup, GDB socket/fork/stdio orchestration,
  command handling, and bridge callbacks, so the row remains partial rather
  than retired. This recalibration adds 0.00015 row-percentage points,
  reducing exact total scored percentage points left from
  0.00007201865672357144 to 0.00007145895521887269. The credited movement is
  below the requested 0.0003% validation-step floor because the entire remaining
  exact total is already below that floor. Validation completed with rustfmt
  --edition 2021 --check executer/user/rust/eclair_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  eclair/harness/tracker/log files, strict Rust eclair build, no-Rust
  user-tool fallback eclair build, strict/fallback eclair symbol checks, and
  the eclair helper smoke path inside kernel/rust/tests/run_equivalence.sh
  with digest f13c3a25bbb41b3e. The broad harness later failed at an unrelated
  x86_memory_helpers digest comparison, recorded in kernel.log; no QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-20: executer/user/eclair.c moved from 99.9995% to
  99.99975% for the strict source-retirement audit after moving the live
  interactive dump setup body into Rust. Rust now owns the
  setup_dump_interactive() DUMP_SET_LEVEL, DUMP_NMI,
  DUMP_QUERY_NUM_MEM_AREAS, and DUMP_QUERY_MEM_AREAS ioctl sequencing,
  remote_running reset, mem_chunks allocation/zeroing/publication, and
  kernel_base/PHYS_OFFSET publication. The no-Rust user-tool fallback still
  compiles the original C setup_dump_interactive() body. Strict Rust eclair
  builds no longer compile a C setup_dump_interactive() body and instead
  export setup_dump_interactive from executer/user/rust/eclair_helpers.rs;
  fallback symbol validation shows the local C fallback body remains
  available. C remains for dump-file setup, constants/thread setup, GDB
  socket/fork/stdio orchestration, command handling, and bridge callbacks, so
  the row remains partial rather than retired. This recalibration adds 0.00025
  row-percentage points, reducing exact total scored percentage points left
  from 0.00007295149255526212 to 0.00007201865672357144. The credited
  movement is below the requested 0.0003% validation-step floor because the
  entire remaining exact total is already below that floor. Validation
  completed with rustfmt --edition 2021 --check
  executer/user/rust/eclair_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  eclair/harness/tracker/log files, strict Rust eclair build, no-Rust
  user-tool fallback eclair build, strict/fallback eclair symbol checks, and
  the eclair helper smoke path inside kernel/rust/tests/run_equivalence.sh
  with digest f13c3b5fa5c41f8e. The broad harness later failed at an unrelated
  x86_memory_helpers digest comparison, recorded in kernel.log; no QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-20: executer/user/eclair.c moved from 99.999% to
  99.9995% for the strict source-retirement audit after moving the eclair
  options parser into Rust. Rust now owns default option publication,
  getopt-driven flag parsing, kernel/dump path publication, os-id parsing,
  trailing-argument help selection, interactive /dev/mcos path construction,
  open() dispatch, errno/error-line formatting, and exit-on-open-failure
  sequencing. The no-Rust user-tool fallback still compiles the original C
  options() body. Strict Rust eclair builds no longer compile a C options()
  body and instead export options from executer/user/rust/eclair_helpers.rs;
  no-Rust user-tool fallback symbol validation shows the local C fallback
  options() body remains available. C remains for dump setup, constants/thread
  setup, GDB socket/fork/stdio orchestration, command handling, and bridge
  callbacks, so the row remains partial rather than retired. This
  recalibration adds 0.0005 row-percentage points, reducing exact total scored
  percentage points left from 0.00007481716417601092 to
  0.00007295149255526212. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check executer/user/rust/eclair_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, git diff --check over the touched
  eclair/harness/tracker/log files, strict Rust eclair build, no-Rust user-tool
  fallback eclair build, symbol checks on strict Rust and fallback objects, and
  the eclair helper smoke path inside kernel/rust/tests/run_equivalence.sh.
  The broad harness later failed at an unrelated x86_memory_helpers digest
  comparison, recorded in kernel.log; no QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.9999% to
  99.99995% for the strict source-retirement audit after publishing the
  ptrace_arch_prctl() wrapper from Rust in the Rust-enabled kernel image. Rust
  now owns the ptrace ARCH_PRCTL request wrapper sequencing and dispatches to
  the Rust-owned arch_ptrace_prctl_body_result() helper with Rust-computed
  user-register offsets plus retained find-thread, thread-unlock, user-word,
  and copy-to-user bridges; no-Rust fallback builds retain the original C
  static wrapper body under arch_ptrace(). The obsolete C offsets table was
  removed because Rust computes those offsets locally. Strict Rust symbol
  validation showed ptrace_arch_prctl exported by kernel/rust/mckernel_rust.o,
  with the strict arch syscall C object retaining only an undefined reference
  plus find/unlock bridges. The C fallback image rebuilt successfully with the
  exported arch_ptrace() body intact; the fallback compiler may inline the
  static ptrace_arch_prctl() helper. C remains for debug-register assembly
  primitives, process-VM page-copy/remap sequencing, move-pages SMP handler
  sequencing, bridge callbacks, and fallback behavior, so the row remains
  partial rather than retired. This recalibration adds 0.00005 row-percentage
  points, reducing exact total scored percentage points left from
  0.00007500373135371774 to 0.00007481716417601092. The credited movement is
  below the requested 0.0003% validation-step floor because the entire
  remaining exact total is already below that floor. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/syscall_policy.rs, git diff
  --check -- arch/x86_64/kernel/syscall.c kernel/rust/syscall_policy.rs
  rust-source-retirement.txt kernel.log, cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -j4, cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4, symbol checks with
  nm -A/objdump on the strict Rust and C-fallback syscall objects, and the
  active exact source-retirement audit. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.99975% to
  99.9999% for the strict source-retirement audit after publishing the
  ptrace_read_gpregs() and ptrace_write_gpregs() helper bodies from Rust in the
  Rust-enabled kernel image. Rust now owns the gpregs helper wrapper sequencing
  and dispatches to the Rust-owned register word-read/write loops through the
  retained user-word bridges; no-Rust fallback builds retain the original C
  static helper loops. Strict Rust symbol validation showed ptrace_read_gpregs
  and ptrace_write_gpregs exported by kernel/rust/mckernel_rust.o, with the
  strict arch syscall C object retaining only undefined references plus the
  user-word bridge callbacks. The C fallback image rebuilt successfully with
  the fallback ptrace_read_regset()/ptrace_write_regset() bodies intact; the
  static write helper may be inlined by the fallback compiler. C remains for
  debug-register assembly primitives, process-VM page-copy/remap sequencing,
  move-pages SMP handler sequencing, bridge callbacks, and fallback behavior,
  so the row remains partial rather than retired. This recalibration adds
  0.00015 row-percentage points, reducing exact total scored percentage points
  left from 0.00007556343284420564 to 0.00007500373135371774. The credited
  movement is below the requested 0.0003% validation-step floor because the
  entire remaining exact total is already below that floor. Validation
  completed with rustfmt --edition 2021 --check kernel/rust/syscall_policy.rs
  after applying the separately logged formatting retry, git diff --check --
  arch/x86_64/kernel/syscall.c kernel/rust/syscall_policy.rs
  rust-source-retirement.txt kernel.log, cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -j4, cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4, symbol checks with
  nm -A on the strict Rust and C-fallback syscall objects, and the active exact
  source-retirement audit. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.9995% to
  99.99975% for the strict source-retirement audit after publishing the public
  x86 ptrace_read_regset() and ptrace_write_regset() wrappers from Rust in the
  Rust-enabled kernel image. Rust now owns the exported regset wrapper
  sequencing and dispatches to the Rust-owned PRSTATUS/XSTATE regset body
  helpers using Rust-computed iovec sizing offsets plus retained gpregs,
  user-copy, xstate-copy, and log bridges; no-Rust fallback builds retain the
  original C switch bodies. Strict Rust symbol validation showed
  ptrace_read_regset and ptrace_write_regset exported by
  kernel/rust/mckernel_rust.o, with the xstate bridge callbacks retained in the
  strict arch syscall C object, and the C fallback object still exported both
  wrapper symbols. C remains for debug-register assembly primitives,
  process-VM page-copy/remap sequencing, move-pages SMP handler sequencing,
  bridge callbacks, and fallback behavior, so the row remains partial rather
  than retired. This recalibration adds 0.00025 row-percentage points,
  reducing exact total scored percentage points left from
  0.00007649626864747461 to 0.00007556343284420564. The credited movement is
  below the requested 0.0003% validation-step floor because the entire
  remaining exact total is already below that floor. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/syscall_policy.rs, git diff
  --check -- arch/x86_64/kernel/syscall.c kernel/rust/syscall_policy.rs
  rust-source-retirement.txt kernel.log, cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -j4, cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4, symbol checks with
  nm -A on the strict Rust and C-fallback syscall objects, and the active exact
  source-retirement audit. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.999% to
  99.9995% for the strict source-retirement audit after publishing the public
  x86 ptrace_read_fpregs() and ptrace_write_fpregs() wrappers from Rust in the
  Rust-enabled kernel image. Rust now owns the exported FP-register user-copy
  wrappers and dispatches to the Rust-owned FP-register I/O body with
  Rust-computed thread/fxsave offsets plus retained user-copy bridges; no-Rust
  fallback builds retain the original C bodies. Strict Rust symbol validation
  showed ptrace_read_fpregs and ptrace_write_fpregs exported by
  kernel/rust/mckernel_rust.o, and the C fallback object still exported both
  symbols. C remains for debug-register assembly primitives, process-VM
  page-copy/remap sequencing, move-pages SMP handler sequencing, bridge
  callbacks, and fallback behavior, so the row remains partial rather than
  retired. This recalibration adds 0.0005 row-percentage points, reducing exact
  total scored percentage points left from 0.00007836194029664512 to
  0.00007649626864747461. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/syscall_policy.rs after applying the separately logged
  formatting retry, git diff --check -- arch/x86_64/kernel/syscall.c
  kernel/rust/syscall_policy.rs rust-source-retirement.txt kernel.log,
  cmake --build /tmp/mckernel-rocky-rust --target mckernel.img -j4,
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4,
  symbol checks with nm -A on the strict Rust and C-fallback syscall objects,
  and the active exact source-retirement audit. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.998% to
  99.999% for the strict source-retirement audit after publishing the public
  x86 ptrace_write_user() wrapper from Rust in the Rust-enabled kernel image.
  Rust now owns the exported write-user wrapper and dispatches to the
  Rust-owned ptrace user-register/debug-register write body with Rust-computed
  ABI offsets plus the retained user-context lookup and log bridges; no-Rust
  fallback builds retain the original C body. Strict Rust symbol validation
  showed ptrace_write_user exported by kernel/rust/mckernel_rust.o while the
  strict arch syscall object holds only an undefined reference plus retained
  callback bridges, and the C fallback object still exported ptrace_write_user.
  C remains for debug-register assembly primitives, process-VM page-copy/remap
  sequencing, move-pages SMP handler sequencing, bridge callbacks, and
  fallback behavior, so the row remains partial rather than retired. This
  recalibration adds 0.001 row-percentage points, reducing exact total scored
  percentage points left from 0.00008209328359498613 to
  0.00007836194029664512. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/syscall_policy.rs, git diff --check --
  arch/x86_64/kernel/syscall.c kernel/rust/syscall_policy.rs
  rust-source-retirement.txt kernel.log, cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -j4, cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4, symbol checks
  with nm -A on the strict Rust and C-fallback syscall objects, and the active
  exact source-retirement audit. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.997% to
  99.998% for the strict source-retirement audit after publishing the public
  x86 ptrace_read_user() wrapper from Rust in the Rust-enabled kernel image.
  Rust now owns the exported read-user wrapper and dispatches to the
  Rust-owned ptrace user-register/debug-register read body with Rust-computed
  ABI offsets plus the retained user-context lookup and log bridges; no-Rust
  fallback builds retain the original C body. Strict Rust symbol validation
  showed ptrace_read_user exported by kernel/rust/mckernel_rust.o while the
  strict arch syscall object holds only an undefined reference plus retained
  callback bridges, and the C fallback object still exported ptrace_read_user.
  C remains for debug-register assembly primitives, process-VM page-copy/remap
  sequencing, move-pages SMP handler sequencing, bridge callbacks, and
  fallback behavior, so the row remains partial rather than retired. This
  recalibration adds 0.001 row-percentage points, reducing exact total scored
  percentage points left from 0.00008582462686490544 to
  0.00008209328359498613. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/syscall_policy.rs, git diff --check --
  arch/x86_64/kernel/syscall.c kernel/rust/syscall_policy.rs
  rust-source-retirement.txt kernel.log, cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -j4, cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4, symbol checks
  with nm -A on the strict Rust and C-fallback syscall objects, and the active
  exact source-retirement audit. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.996% to
  99.997% for the strict source-retirement audit after publishing the public
  x86 alloc_debugreg() wrapper from Rust in the Rust-enabled kernel image.
  Rust now owns the exported allocation wrapper and dispatches to the
  Rust-owned debug-register allocation body with the existing ptrace allocation
  and log bridges; no-Rust fallback builds retain the original C allocation
  body. Strict Rust symbol validation showed alloc_debugreg exported by
  kernel/rust/mckernel_rust.o while the strict arch syscall object retains
  only the callback bridges, and the C fallback object still exported
  alloc_debugreg. C remains for debug-register assembly primitives,
  process-VM page-copy/remap sequencing, move-pages SMP handler sequencing,
  bridge callbacks, and fallback behavior, so the row remains partial rather
  than retired. This recalibration adds 0.001 row-percentage points, reducing
  exact total scored percentage points left from 0.00008955597013482475 to
  0.00008582462686490544. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/syscall_policy.rs, git diff --check --
  arch/x86_64/kernel/syscall.c kernel/rust/syscall_policy.rs
  rust-source-retirement.txt kernel.log, cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -j4, cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4, symbol checks
  with nm -A on the strict Rust and C-fallback syscall objects, and the active
  exact source-retirement audit. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.995% to
  99.996% for the strict source-retirement audit after publishing the public
  x86 clear_single_step() and set_single_step() wrappers from Rust in the
  Rust-enabled kernel image. Rust now owns the exported wrapper symbols and
  dispatches to the already Rust-owned RFLAGS_TF mutation bodies; no-Rust
  fallback builds retain the original C wrappers and direct flag mutation.
  Strict Rust symbol validation showed clear_single_step and set_single_step
  exported by kernel/rust/mckernel_rust.o, while the C fallback object still
  exported both symbols. C remains for debug-register assembly primitives,
  process-VM page-copy/remap sequencing, move-pages SMP handler sequencing,
  bridge callbacks, and fallback behavior, so the row remains partial rather
  than retired. This recalibration adds 0.001 row-percentage points, reducing
  exact total scored percentage points left from 0.00009328731343316576 to
  0.00008955597013482475. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/syscall_policy.rs, git diff --check --
  arch/x86_64/kernel/syscall.c kernel/rust/syscall_policy.rs
  rust-source-retirement.txt kernel.log, cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -j4, cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4, symbol checks
  with nm -A on the strict Rust and C-fallback syscall objects, and the active
  exact source-retirement audit. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.994% to
  99.995% for the strict source-retirement audit after publishing the x86
  save_uctx() transfer-context field publication body from Rust in the
  Rust-enabled kernel image. Rust now resolves the optional current-register
  pointer through the retained arch_rt_sigreturn_context_bridge(), publishes
  the saved general-purpose registers, reads FS through the Rust-owned
  ihk_mc_arch_get_special_register(), and preserves the no-Rust C fallback
  body. Strict Rust symbol validation showed save_uctx exported by
  kernel/rust/mckernel_rust.o, while the C fallback object still exported the
  symbol. C remains for debug-register assembly primitives, process-VM
  page-copy/remap sequencing, move-pages SMP handler sequencing, bridge
  callbacks, and fallback behavior, so the row remains partial rather than
  retired. This recalibration adds 0.001 row-percentage points, reducing exact
  total scored percentage points left from 0.00009701865671729593 to
  0.00009328731343316576. The credited movement is below the requested
  0.0003% validation-step floor because the entire remaining exact total is
  already below that floor. Validation completed with rustfmt --edition 2021
  --check kernel/rust/syscall_policy.rs after applying the separately logged
  formatting retry, git diff --check -- arch/x86_64/kernel/syscall.c
  kernel/rust/syscall_policy.rs rust-source-retirement.txt kernel.log,
  cmake --build /tmp/mckernel-rocky-rust --target mckernel.img -j4,
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4,
  symbol checks with nm -A on the strict Rust and C-fallback syscall objects,
  and the active exact source-retirement audit. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.993% to
  99.994% for the strict source-retirement audit after publishing the x86
  interrupt_from_user() stack-origin classifier from Rust in the Rust-enabled
  kernel image. Rust now reads the saved x86 user-context stack pointer and
  returns the same high-bit user/kernel classification as the C body; no-Rust
  fallback builds retain the original C implementation. Strict Rust symbol
  validation showed interrupt_from_user resolved from kernel/rust/mckernel_rust.o
  while the strict arch syscall object held only an undefined reference, and
  the C fallback object still exported the symbol. C remains for debug-register
  assembly primitives, process-VM page-copy/remap sequencing, move-pages SMP
  handler sequencing, bridge callbacks, and fallback behavior, so the row
  remains partial rather than retired. This recalibration adds 0.001
  row-percentage points, reducing exact total scored percentage points left
  from 0.00010075000000142609 to 0.00009701865671729593. The credited movement
  is below the requested 0.0003% validation-step floor because the entire
  remaining exact total is already below that floor. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/syscall_policy.rs,
  git diff --check -- arch/x86_64/kernel/syscall.c
  kernel/rust/syscall_policy.rs rust-source-retirement.txt kernel.log,
  cmake --build /tmp/mckernel-rocky-rust --target mckernel.img -j4,
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4,
  symbol checks with nm -A on the strict Rust and C-fallback syscall objects,
  and the active exact source-retirement audit. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.992% to
  99.993% for the strict source-retirement audit after publishing the x86
  save_syscall_return_value() no-op from Rust in the Rust-enabled kernel
  image. The public symbol remains stable, strict Rust builds now resolve it
  from kernel/rust/mckernel_rust.o, and no-Rust fallback builds retain the
  original C no-op body. C remains for debug-register assembly primitives,
  process-VM page-copy/remap sequencing, move-pages SMP handler sequencing,
  bridge callbacks, and fallback behavior, so the row remains partial rather
  than retired. This recalibration adds 0.001 row-percentage points, reducing
  exact total scored percentage points left from 0.00010448134328555625 to
  0.00010075000000142609. The credited movement is below the requested 0.0003%
  validation-step floor because the entire remaining exact total is already
  below that floor. Validation completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs, git diff --check --
  arch/x86_64/kernel/syscall.c kernel/rust/syscall_policy.rs
  rust-source-retirement.txt kernel.log, cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -j4, cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4, symbol checks
  with nm -A on the strict Rust and C-fallback syscall objects, and the active
  exact source-retirement audit. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.991% to
  99.992% for the strict source-retirement audit after moving the shared
  calculate_time_from_tsc() clock helper body into Rust for the Rust-enabled
  kernel image. Rust now reads the retained VDSO state through the existing
  arch_vdso_state_bridge(), samples the VDSO sequence counter, applies the
  TSC delta and nanosecond normalization, falls back to the TOD-data sequence
  loop when VDSO time data is absent, and preserves the no-Rust C fallback
  definitions. Strict Rust symbol validation showed calculate_time_from_tsc
  and time exported by kernel/rust/mckernel_rust.o, while the C fallback
  object still exports both symbols. C remains for debug-register assembly
  primitives, process-VM page-copy/remap sequencing, move-pages SMP handler
  sequencing, bridge callbacks, and fallback behavior, so the row remains
  partial rather than retired. This recalibration adds 0.001 row-percentage
  points, reducing exact total scored percentage points left from
  0.00010821268658389727 to 0.00010448134328555625. Validation completed
  with rustfmt --edition 2021 --check kernel/rust/syscall_policy.rs,
  git diff --check -- arch/x86_64/kernel/syscall.c
  kernel/rust/syscall_policy.rs kernel.log,
  cmake --build /tmp/mckernel-rocky-rust --target mckernel.img -j4,
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4,
  symbol checks with nm -A on the strict Rust and C-fallback syscall objects,
  and the active exact source-retirement audit. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/syscall.c moved from 99.99% to
  99.991% for the strict source-retirement audit after routing another small
  public helper body through Rust. Rust-enabled mckernel.img now publishes the
  public time() helper from kernel/rust/syscall_policy.rs while preserving the
  no-Rust C fallback definition, and the previous same-row single-step flag
  body movement remains behind the same Rust policy-helper gate. C remains for
  retained VDSO/TSC clock calculation, debug-register assembly primitives,
  process-VM page-copy/remap sequencing, move-pages SMP handler sequencing,
  bridge callbacks, and fallback behavior, so the row remains partial rather
  than retired. This recalibration adds 0.001 row-percentage points, reducing
  exact total scored percentage points left from 0.00011194402985381657 to
  0.00010821268658389727. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/syscall_policy.rs,
  git diff --check -- arch/x86_64/kernel/syscall.c
  kernel/rust/syscall_policy.rs,
  cmake --build /tmp/mckernel-rocky-rust --target mckernel.img -j4,
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4,
  and the active exact source-retirement audit. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-20: executer/user/mcexec.c moved from 99.99% to
  99.999% for the strict source-retirement audit after a focused main-path
  source-body batch. Rust-enabled mcexec now routes post-option setup,
  executable descriptor load, default environment collection, page/altroot
  initialization, stack-limit/reduce-stack gate sequencing, CPU/NUMA
  topology setup, partitioned CPU-set setup, descriptor runtime publication,
  descriptor argument/environment setup, ADD_ENVS environment collection,
  FLIB affinity setup, main stack publication, process/thread planning,
  DMA/PPD setup, and final prepare/start/join sequencing through Rust-owned
  bodies while preserving the no-Rust C fallback build. C remains for Linux
  and libc primitive bridges, ABI-visible descriptor/load helpers, retained
  bridge callbacks, signal/syscall/overlay primitive side effects, and
  fallback behavior, so the row remains partial rather than retired. This
  recalibration adds 0.009 row-percentage points, reducing exact total scored
  percentage points left from 0.00014552611942519889 to
  0.00011194402985381657. The remaining exact total is below one requested
  0.0003% validation-step increment, so later verified source movement will
  necessarily be smaller unless a scored C source leaves the strict build or
  all residual partial rows are completed. Validation completed with
  rustfmt --edition 2021 --check executer/user/rust/mcexec_helpers.rs,
  git diff --check -- executer/user/mcexec.c
  executer/user/rust/mcexec_helpers.rs kernel.log rust-source-retirement.txt,
  cmake --build /tmp/mckernel-rocky-rust --target mcexec -j4,
  cmake --build /tmp/mckernel-rocky-user-cfallback --target mcexec -j4, and
  the active exact source-retirement audit:
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json, which
  reported compiled_retired_c=0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-20: executer/user/eclair.c moved from 99.99% to
  99.999%, executer/kernel/mcctrl/control.c moved from 99.99% to
  99.999%, executer/kernel/mcctrl/driver.c moved from 99.99% to
  99.999%, executer/kernel/mcctrl/futex.c moved from 99.99% to
  99.999%, executer/kernel/mcctrl/procfs.c moved from 99.99% to
  99.999%, executer/kernel/mcctrl/syscall.c moved from 99.99% to
  99.999%, executer/kernel/mcctrl/sysfs.c moved from 99.99% to
  99.999%, ihk/linux/core/host_driver.c moved from 99.99% to
  99.999%, and
  ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved from
  99.99% to 99.999% for the strict source-retirement audit after a
  strict symbol/body audit. The credited strict paths already route the
  eclair entry/packet loop, mcctrl control/driver/futex/procfs/syscall/
  sysfs lifecycle and request sequencing, IHK core host device/OS
  control paths, and SMP x86_64 trampoline/startup/topology/perf/interrupt
  paths through Rust-owned entry points or Rust-owned `*_body_result`
  helpers. C remains for Linux/user ABI coupling, retained bridge
  callbacks, allocation/free, user-copy, BFD/GDB, asm/atomic, page/table
  and device mapping, IKC send/wait, notifier, module-init/exit, and
  no-Rust fallback behavior, so all nine rows remain partial rather than
  retired. The recalibration adds 0.081 row-percentage points, reducing
  exact total scored percentage points left from 0.0004477649253828986 to
  0.00014552611942519889, a 0.0003022388059576997 step that satisfies
  the requested 0.0003% validation-step floor. Validation completed with
  the active exact source-retirement audit:
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json, which
  reported compiled_retired_c=0, compiled_non_allowlisted_c=29,
  debt_rows=19, headers_with_executable_logic=0, and
  total_percentage_points_left=0.00014552611942519889. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-20: arch/x86_64/kernel/cpu.c moved from 99.99% to
  99.999%, arch/x86_64/kernel/memory.c moved from 99.99% to
  99.999%, kernel/mem.c moved from 99.99% to 99.999%,
  ihk/linux/driver/smp/smp-driver.c moved from 99.95% to 99.999%,
  and ihk/linux/user/ihklib.c moved from 99.99% to 99.999% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes
  get_xsave_size(), get_xsave_mask(), arch_get_smaller_page_size(),
  arch_vrflag_to_ptattr(), ihk_mc_query_mem_areas(),
  ihk_mc_query_mem_user_page(), and ihk_mc_query_mem_free_page()
  through Rust while preserving no-Rust C fallback definitions. The
  Rust-enabled IHK SMP module now routes ihk_smp_map_virtual(),
  ihk_smp_unmap_virtual(), and __mem_chunk_insert() through Rust, and
  Rust-enabled ihklib now routes cpu_str2count(), cpu_str2req(),
  ikc_str2count(), ikc_str2req(), mem_str2count(), and mem_str2req()
  through Rust. Retained C bridge callbacks still provide xsave state
  reads, 1 GiB page policy, common vrflag conversion, memory-dump page
  and process-hash traversal, SMP rb-tree/list/map/flush primitives,
  ihklib invalid-request logging, and all no-Rust fallback bodies. The
  batch adds 0.085 row-percentage points, reducing exact total scored
  percentage points left from 0.0007649291044913298 to
  0.0004477649253828986, a 0.00031716417910843123 step. Rows remain
  partial because strict arch/x86_64/kernel/cpu.c still compiles
  FPU/GDT/IDT, interrupt/trap, CPU-local/AP startup, and retained bridge
  callbacks; strict arch/x86_64/kernel/memory.c still compiles page-table
  primitives, physical/virtual primitive callbacks, and retained bridge
  callbacks; strict kernel/mem.c still compiles allocation/free,
  page-tracking, memory lifecycle, dump primitive callbacks, and retained
  bridge callbacks; strict smp-driver.c still compiles host/module control,
  IKC, topology, CPU/memory query, trampoline, interrupt, and retained
  bridge callbacks; and strict ihklib.c still compiles the remaining user
  library API surface plus retained bridge callbacks. Validation completed
  with rustfmt --edition 2021 --check over the touched Rust helpers; bash
  -n kernel/rust/tests/run_equivalence.sh; scoped git diff --check; strict
  cmake --build /tmp/mckernel-rocky-rust --target mckernel_rust_obj -- -B
  -j4; strict cmake --build /tmp/mckernel-rocky-rust --target
  mckernel.img -- -B -j4; strict cmake --build /tmp/mckernel-rocky-rust
  --target ihk-smp-x86_64_ko -- -B -j4; strict cmake --build
  /tmp/mckernel-rocky-rust --target ihklib -- -B -j4; no-Rust kernel
  fallback cmake --build /tmp/mckernel-rocky-c-fallback --target
  mckernel.img -- -B -j4; no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -- -B
  -j4; current c-fallback ihklib build; separate
  /tmp/mckernel-ihklib-no-rust ihklib build with ENABLE_RUST_USER_TOOLS=OFF
  to exercise C parser fallback definitions; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.vbbpVY and Rust object unresolved-symbol
  and SIMD checks ok; strict/fallback symbol checks for kernel, SMP module,
  and ihklib ownership; and the active source-retirement audit before
  tracker credit. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-20: kernel/syscall.c moved from 99.999998% to
  99.999999% for the strict source-retirement audit. Rust-enabled
  mckernel.img now routes the public sys_settimeofday(), sys_swapout(),
  sys_suspend_threads(), sys_resume_threads(),
  sys_util_migrate_inter_kernel(), and sys_util_indicate_clone() wrappers
  through Rust while preserving the no-Rust C fallback definitions. The Rust
  wrappers perform the same x86_64 syscall argument extraction as the retired
  strict C wrappers, dispatch Rust-owned settimeofday local-time/origin
  sequencing, swapout pageout/pagein forwarding, suspend/resume thread-signal
  traversal, UTI migrate copyin/thread dispatch, and UTI clone indication
  state publication, and retain C only for TOD locking/atomic/barrier/panic
  callbacks, syscall forwarding, pageout/pagein primitive side effects,
  UTI util-thread dispatch, copyin/allocation/free callbacks, signal
  delivery callbacks, CPU pause, and logging. Strict mckernel_rust.o exports
  all six public wrappers and imports the retained C bridge callbacks; strict
  kernel/syscall.c.o exports those bridges and imports the public wrapper
  names; strict final-image symbol resolution resolves the wrappers from Rust;
  and fallback C still exports the fallback wrapper definitions. The batch
  adds 0.000001 row-percentage point, reducing exact total scored percentage
  points left from 0.0007649328358354524 to 0.0007649291044913298. The row
  remains partial because strict kernel/syscall.c still compiles syscall
  dispatch, ptrace, futex, perf, rt_sigtimedwait, SysV shm, lower-level
  bridge side effects, TOFU open/openat, and no-Rust fallback bodies.
  Validation completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs; bash -n kernel/rust/tests/run_equivalence.sh;
  scoped git diff --check; cmake --build /tmp/mckernel-rocky-rust --target
  mckernel_rust_obj -- -B -j4; full MCKERNEL_RUST_EQUIV_KEEP_TMP=1
  kernel/rust/tests/run_equivalence.sh with tmpdir
  /tmp/mckernel-rust-equiv.aQmOCm and Rust object unresolved-symbol/SIMD
  checks ok; Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  mckernel.img -- -B -j4; no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4; symbol
  checks confirming strict Rust ownership plus fallback C ownership; and exact
  source-retirement audit. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-19: kernel/syscall.c moved from 99.999995% to 99.999998%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_remap_file_pages(), sys_mremap(), and sys_msync() wrappers
  through Rust while preserving the no-Rust C fallback definitions. The Rust
  wrappers perform the same x86_64 syscall argument extraction as the retired
  strict C wrappers, load the current thread/VM/page-table context, pass the
  same vm_range field offsets into the existing Rust-owned body functions, and
  retain C only for range locks/lookups, remap/mremap/msync primitive side
  effects, memobj ref/unref, page-table locking/move, TLB flush, populate,
  host-PTE clear, munmap dispatch, and logging callbacks. Strict
  mckernel_rust.o exports sys_remap_file_pages(), sys_mremap(), and
  sys_msync() and imports the retained memory-syscall bridge callbacks; strict
  kernel/syscall.c.o exports those bridges and imports the public wrapper
  names; strict final-image symbol resolution resolves the wrappers from Rust;
  and fallback C still exports the fallback wrapper definitions. The row
  remains partial because strict kernel/syscall.c still compiles syscall
  dispatch, ptrace, futex, perf, rt_sigtimedwait, SysV shm, lower-level memory
  syscall bridge side effects, TOFU open/openat, and no-Rust fallback bodies.
  Validation completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs; bash -n kernel/rust/tests/run_equivalence.sh;
  scoped git diff --check; cmake --build /tmp/mckernel-rocky-rust --target
  mckernel_rust_obj -- -B -j4; full MCKERNEL_RUST_EQUIV_KEEP_TMP=1
  kernel/rust/tests/run_equivalence.sh with tmpdir
  /tmp/mckernel-rust-equiv.420R9m and Rust object unresolved-symbol/SIMD checks
  ok; Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  mckernel.img -- -B -j4; no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4; and symbol
  checks confirming strict Rust ownership plus fallback C ownership.

- 2026-06-19: kernel/syscall.c moved from 99.99999% to 99.999995%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_mbind() wrapper through Rust while preserving the no-Rust C
  fallback definition. The Rust wrapper performs the same x86_64 syscall
  argument extraction as the C wrapper, retains the entry dkprintf through a
  C log bridge, loads current-thread VM/process state, carries the
  ENABLE_FUGAKU_HACKS gate through Rust cfg, dispatches the existing
  Rust-owned mbind body result, and retains C only for user-copy,
  write-lock/unlock, range lookup, policy search/clear/alloc/rb-clear/insert,
  and logging callback side effects. Strict mckernel_rust.o exports
  sys_mbind() and imports the retained mbind bridge callbacks; strict
  kernel/syscall.c.o exports those bridges and imports the public wrapper
  name; strict final-image symbol resolution resolves the wrapper from Rust;
  and fallback C still exports the fallback wrapper definition. The batch adds
  0.000005 row-percentage point, reducing exact total scored percentage
  points left from 0.0007649626865742221 to 0.0007649440298536092. The row remains
  partial because strict kernel/syscall.c still compiles syscall dispatch,
  ptrace, futex, perf, rt_sigtimedwait, SysV shm, other memory syscall wrapper
  bodies, lower-level mempolicy bridge side effects, TOFU open/openat, and
  no-Rust fallback bodies. Validation completed with rustfmt --edition 2021
  --check kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; cmake
  --build /tmp/mckernel-rocky-rust --target mckernel_rust_obj -- -B -j4;
  full MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh
  with tmpdir /tmp/mckernel-rust-equiv.bkrEpO and Rust object
  unresolved-symbol/SIMD checks ok; Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4; no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4; and symbol checks for strict Rust wrapper export, strict C bridge
  exports/wrapper import, strict final-image wrapper resolution, and fallback
  C wrapper export. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-19: kernel/process.c moved from 99.995% to 99.996% for
  the strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public __sched_wakeup_thread(), sched_wakeup_thread_locked(),
  sched_wakeup_thread(), __runq_add_thread(), and runq_add_thread() wrappers
  through Rust while preserving the no-Rust C fallback definitions. The Rust
  wrappers dispatch the Rust-owned scheduler body helpers with C retained for
  CPU-local lookup, spin/noirq locks, process update lock, status exchange,
  set_timer, vector/interrupt dispatch, procfs creation, counters, rusage
  accounting/debug, and runqueue logging side effects. Strict mckernel_rust.o
  exports all five public wrappers; strict kernel/process.c.o no longer
  defines those wrappers and exports only the retained bridge callbacks;
  strict final-image symbol resolution resolves the wrappers from Rust; and
  fallback C still exports the fallback wrapper definitions. The batch adds
  0.001 row-percentage point, reducing exact total scored percentage points
  left from 0.0007686940298441414 to 0.0007649626865742221, a
  0.0000037313432699193 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  kernel/process.c still compiles retained bridge callbacks, sched_request_migrate(),
  schedule(), clone/debug, process/thread lifecycle primitives, VM/range
  primitive side effects, and fallback scaffolding. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/sched_helpers.rs
  kernel/rust/waitq.rs; bash -n kernel/rust/tests/run_equivalence.sh; scoped
  git diff --check; cmake --build /tmp/mckernel-rocky-rust --target
  mckernel_rust_obj -- -B -j4; full MCKERNEL_RUST_EQUIV_KEEP_TMP=1
  kernel/rust/tests/run_equivalence.sh with tmpdir
  /tmp/mckernel-rust-equiv.bWaqYZ and Rust object unresolved-symbol/SIMD
  checks ok; Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  mckernel.img -- -B -j4; no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4; symbol
  checks for strict Rust exports, strict C bridge exports/no wrapper
  definitions, strict final-image wrapper resolution, and fallback C wrapper
  exports; and final exact source-retirement audit. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-19: kernel/syscall.c moved from 99.99998% to 99.99999%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_set_mempolicy() and sys_get_mempolicy() wrappers through Rust
  while preserving the no-Rust C fallback definitions. The Rust wrappers
  perform the same x86_64 syscall argument extraction as the C wrappers, load
  current-thread VM/process state, dispatch the existing Rust-owned
  set/get-mempolicy body results, and retain C only for user-copy,
  read-lock/unlock, range lookup, policy search, lookup-node, and logging
  callback side effects. Strict mckernel_rust.o exports both public wrappers
  and imports the retained mempolicy bridge callbacks; strict kernel/syscall.c.o
  exports those bridges and imports the public wrapper names; strict
  final-image symbol resolution resolves the wrappers from Rust; and fallback
  C still exports the fallback wrapper definitions. The batch adds 0.00001
  row-percentage point, reducing exact total scored percentage points left
  from 0.0007687313432711562 to 0.0007686940298441414, a
  0.000000037313426999 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  kernel/syscall.c still compiles syscall dispatch, ptrace, futex, perf,
  rt_sigtimedwait, SysV shm, sys_mbind(), other memory syscall wrapper bodies,
  lower-level mempolicy bridge side effects, TOFU open/openat, and no-Rust
  fallback bodies. Validation completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.WO9cmi and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; symbol checks for strict Rust public-wrapper exports and bridge
  imports, strict C bridge exports and wrapper imports, final-image
  public-wrapper resolution, and fallback C wrapper exports; exact strict audit
  before tracker credit; and exact audit after tracker credit. No QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-19: kernel/syscall.c moved from 99.99997% to 99.99998%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_mlock() and sys_munlock() wrappers through Rust while
  preserving the no-Rust C fallback definitions. The Rust wrappers perform the
  same x86_64 syscall argument extraction as the C wrappers, load the current
  thread VM and user region bounds, pass the vm_range offsets, and dispatch
  the existing Rust-owned memlock body result with retained C bridges for
  memory-range locking, range lookup/iteration, split/join, population, and
  logging side effects. Strict mckernel_rust.o exports both public wrappers
  and imports the retained memlock bridge callbacks; strict kernel/syscall.c.o
  exports those bridges and imports the public wrapper names; strict
  final-image symbol resolution resolves the wrappers from Rust; and fallback
  C still exports the fallback wrapper definitions. The batch adds 0.00001
  row-percentage point, reducing exact total scored percentage points left
  from 0.0007687686567123819 to 0.0007687313432711562, a
  0.0000000373134412257 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  kernel/syscall.c still compiles syscall dispatch, ptrace, futex, perf,
  rt_sigtimedwait, SysV shm, other memory syscall wrapper bodies, lower-level
  memlock bridge side effects, TOFU open/openat, and no-Rust fallback bodies.
  Validation completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs after a separately logged formatting failure;
  bash -n kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.Abzlgs and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; symbol checks for strict Rust public-wrapper exports and bridge
  imports, strict C bridge exports and wrapper imports, final-image
  public-wrapper resolution, and fallback C wrapper exports; exact strict audit
  before tracker credit; and exact audit after tracker credit. No QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-19: kernel/syscall.c moved from 99.99996% to 99.99997%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_process_vm_writev() and sys_process_vm_readv() wrappers
  through Rust while preserving the no-Rust C fallback definitions. The Rust
  wrappers perform the same x86_64 syscall argument extraction as the C
  wrappers and dispatch the existing Rust-owned process-vm read/write body
  result through the retained do_process_vm_read_writev() C implementation
  callback. Strict mckernel_rust.o exports both public wrappers and imports
  do_process_vm_read_writev; strict kernel/syscall.c.o imports the public
  wrapper names; strict final-image symbol resolution resolves the wrappers
  from Rust and do_process_vm_read_writev from C; and fallback C still exports
  the fallback wrapper definitions. The batch adds 0.00001 row-percentage
  point, reducing exact total scored percentage points left from
  0.0007688059701393968 to 0.0007687686567123819, a
  0.000000037313426999 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  kernel/syscall.c still compiles syscall dispatch, ptrace, futex, perf,
  rt_sigtimedwait, SysV shm, lower-level process-vm read/write callback
  behavior, other process/memory syscall bodies, low-level bridge callback
  side-effect, TOFU open/openat, and no-Rust fallback bodies. Validation
  completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.UeKb2t and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; symbol checks for strict Rust public-wrapper exports and callback
  import, strict C wrapper imports, final-image public-wrapper and callback
  resolution, and fallback C wrapper exports; exact strict audit before
  tracker credit; and exact audit after tracker credit. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-19: kernel/syscall.c moved from 99.99995% to 99.99996%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_madvise() and sys_migrate_pages() wrappers through Rust while
  preserving the no-Rust C fallback definitions. The Rust wrappers perform the
  same x86_64 syscall argument extraction as the C wrappers, dispatch retained
  C debug-log bridges, and call the existing Rust-owned madvise and
  migrate-pages body results. C remains responsible for the retained logging
  callbacks, deeper madvise range/VM mutation fallback body, migrate-pages
  fallback behavior, and lower-level process/memory syscall side effects.
  Strict mckernel_rust.o exports both public wrappers and imports only the two
  retained C log bridges; strict kernel/syscall.c.o exports those bridges and
  imports the public wrapper names; strict final-image symbol resolution
  resolves the wrappers from Rust; and fallback C still exports the fallback
  wrapper definitions. The batch adds 0.00001 row-percentage point, reducing
  exact total scored percentage points left from 0.0007688432835806225 to
  0.0007688059701393968, a 0.0000000373134412257 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict kernel/syscall.c still compiles syscall dispatch, ptrace, futex, perf,
  rt_sigtimedwait, SysV shm, process/memory syscall, low-level bridge callback
  side-effect, TOFU open/openat, and no-Rust fallback bodies. Validation
  completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.83Mdl8 and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; symbol checks for strict Rust public-wrapper exports, strict C bridge
  exports/imports, final-image public-wrapper resolution, and fallback C
  wrapper exports; exact strict audit before tracker credit; and exact audit
  after tracker credit. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-18: kernel/syscall.c moved from 99.9999% to 99.99995%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_wait4(), sys_waitid(), sys_sched_setparam(),
  sys_sched_getparam(), sys_sched_setscheduler(), sys_sched_getscheduler(),
  sys_sched_setaffinity(), and sys_sched_getaffinity() wrappers through Rust
  while preserving the no-Rust C fallback definitions. The Rust wrappers
  dispatch existing Rust-owned wait4/waitid and scheduler syscall body
  sequencing, including wait option validation, do_wait handoff, status/rusage
  and siginfo copyout shaping, scheduler target selection, credential/root
  forwarding checks, parameter copyin/copyout, scheduler policy application,
  affinity copy/mask intersection, migration request dispatch, and retained
  debug logging. C remains responsible for do_wait primitive side effects,
  waitqueue/schedule/signal behavior below do_wait, copyin/copyout, thread
  lookup/unlock/refcount callbacks, scheduler mutation callbacks, migration
  request side effects, and fallback behavior. Strict mckernel_rust.o exports
  all eight public wrappers and imports only the retained C bridge callbacks;
  strict kernel/syscall.c.o exports those retained bridges and imports the
  public wrapper names; strict final-image symbol resolution resolves the
  wrappers from Rust; and fallback C still exports the fallback wrapper
  definitions. The batch adds 0.00005 row-percentage point, reducing exact
  total scored percentage points left from 0.0007690298507441184 to
  0.0007688432835806225, a 0.00000018656716349587 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict kernel/syscall.c still compiles syscall
  dispatch, ptrace, futex, perf, rt_sigtimedwait, SysV shm, process/memory
  syscall, low-level bridge callback side-effect, TOFU open/openat, and
  no-Rust fallback bodies. Validation completed with rustfmt --edition 2021
  --check kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.Zou84m and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; symbol checks for strict Rust public-wrapper exports, strict C bridge
  exports/imports, final-image public-wrapper resolution, and fallback C
  wrapper exports; and exact strict audit before tracker credit. No QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-18: kernel/syscall.c moved from 99.9998% to 99.9999%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_epoll_pwait(), sys_ppoll(), sys_pselect6(),
  sys_rt_sigprocmask(), sys_rt_sigpending(), sys_signalfd(),
  sys_signalfd4(), sys_rt_sigqueueinfo(), sys_rt_sigsuspend(),
  sys_rt_sigaction(), and sys_sigaltstack() wrappers through Rust while
  preserving the no-Rust C fallback definitions. The Rust wrappers dispatch
  existing Rust-owned temporary signal-mask, sigprocmask, pending-mask,
  signalfd, sigqueueinfo, sigsuspend, sigaction, and sigaltstack body
  sequencing with C retained for signal-mask forwarding, pending-mask field
  access, signalfd creation/publication, do-kill and sigaction callbacks,
  copyin/copyout, low-level signal side effects, and fallback behavior.
  Strict mckernel_rust.o exports all eleven public wrappers and imports only
  the retained C bridge callbacks; strict kernel/syscall.c.o exports those
  retained bridges and imports the public wrapper names; strict final-image
  symbol resolution resolves the wrappers from Rust; and fallback C still
  exports the fallback wrapper definitions. The batch adds 0.0001
  row-percentage point, reducing exact total scored percentage points left from
  0.0007694029850711104 to 0.0007690298507441184, a
  0.00000037313432835821 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  kernel/syscall.c still compiles syscall dispatch, ptrace, wait, futex, perf,
  rt_sigtimedwait, process/memory syscall, low-level bridge callback
  side-effect, TOFU open/openat, and no-Rust fallback bodies. Validation
  completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.NjPqZF and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; symbol checks for strict Rust public-wrapper exports, strict C bridge
  exports/imports, final-image public-wrapper resolution, and fallback C
  wrapper exports; and exact strict audit before tracker credit. No QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-18: kernel/syscall.c moved from 99.9995% to 99.9998%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_kill(), sys_tgkill(), sys_tkill(), sys_setresuid(),
  sys_setreuid(), sys_setuid(), sys_setfsuid(), sys_setresgid(),
  sys_setregid(), sys_setgid(), sys_setfsgid(), sys_getuid(),
  sys_geteuid(), sys_getresuid(), sys_getgid(), sys_getegid(), and
  sys_getresgid() wrappers through Rust while preserving the no-Rust C
  fallback definitions. The Rust wrappers dispatch the existing Rust-owned
  signal-send and uid/gid credential body sequencing, including siginfo
  shaping, do-kill callback dispatch, Linux-forward dispatch,
  credential-refresh dispatch, setfsid syscall dispatch, process credential
  field reads, and getresuid/getresgid ordered copyout. C remains responsible
  for retained do_kill, dkprintf, do_syscall forwarding, credential refresh,
  copy-to-user, and fallback behavior. Strict mckernel_rust.o exports all
  seventeen public wrappers and imports only the retained C bridge callbacks;
  strict kernel/syscall.c.o exports those retained bridges and imports the
  public wrapper names; strict final-image symbol resolution resolves the
  wrappers from Rust; and fallback C still exports the fallback wrapper
  definitions. The batch adds 0.0003 row-percentage point, reducing exact
  total scored percentage points left from 0.000770522388066297 to
  0.0007694029850711104, a 0.00000111940299518662 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict kernel/syscall.c still compiles syscall dispatch, ptrace, wait,
  futex, perf, signal mask/action, process/memory syscall, low-level bridge
  callback side-effect, TOFU open/openat, and no-Rust fallback bodies.
  Validation completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.4PXRD6 and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; symbol checks for strict Rust public-wrapper exports, strict C bridge
  exports/imports, final-image public-wrapper resolution, and fallback C
  wrapper exports; and exact strict audit before tracker credit. No QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-18: kernel/syscall.c moved from 99.999% to 99.9995%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_read(), sys_ioctl(), sys_open(), sys_openat(), sys_close(),
  and sys_fcntl() wrappers through Rust while preserving the no-Rust C
  fallback definitions and keeping the ENABLE_TOFU open/openat C fallback
  branch intact. The Rust wrappers dispatch the existing Rust-owned mckfd/file
  syscall body sequencing for locked descriptor lookup, callback-vs-forward
  selection, TOFU cleanup gates, descriptor unlink/free handoff, Linux
  forwarding, XPMEM device open/openat detection, and fcntl duplicate handling,
  with C retained for locks, allocation/free callbacks, user string length,
  XPMEM open callbacks, TOFU ioctl/close callbacks, and fallback behavior.
  The batch also tightens the XPMEM path match to compare the full
  NUL-terminated literal length before invoking the byte comparison helper.
  Strict mckernel_rust.o exports all six public wrappers and imports only the
  retained C bridge callbacks; strict kernel/syscall.c.o exports those retained
  bridges and imports the public wrapper names; strict final-image symbol
  resolution resolves the wrappers from Rust; and fallback C still exports the
  fallback wrapper definitions. The batch adds 0.0005 row-percentage point,
  reducing exact total scored percentage points left from
  0.0007723880597012567 to 0.000770522388066297, a
  0.00000186567164179104 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  kernel/syscall.c still compiles syscall dispatch, ptrace, wait, futex, perf,
  signal, process, low-level bridge callback side-effect, TOFU open/openat,
  and no-Rust fallback bodies. Validation completed with rustfmt --edition
  2021 --check kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.qz0QBb and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; symbol checks for strict Rust public-wrapper exports, strict C bridge
  exports/imports, final-image public-wrapper resolution, and fallback C
  wrapper exports; and exact strict audit before tracker credit. No QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-18: kernel/syscall.c moved from 99.997% to 99.999%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_pause(), sys_getrusage(),
  sys_sched_get_priority_max(), sys_sched_get_priority_min(),
  sys_sched_rr_get_interval(), sys_setitimer(), and sys_getitimer()
  wrappers through Rust while preserving the no-Rust C fallback definitions.
  The Rust wrappers dispatch existing Rust-owned pause/sigsuspend, rusage,
  scheduler-priority, round-robin interval, and interval-timer body
  sequencing with C retained for signal-mask suspension, rusage lock and
  copyout primitives, thread lookup/unlock, do_syscall forwarding,
  copyin/copyout, timer programming, low-level callback side effects, and
  fallback behavior. Strict mckernel_rust.o exports all seven public wrappers
  and imports only the retained C bridge callbacks; strict kernel/syscall.c.o
  exports those retained bridges and imports the public wrapper names; strict
  final-image symbol resolution resolves the wrappers from Rust; and fallback
  C still exports the fallback wrapper definitions. The batch adds 0.002
  row-percentage point, reducing exact total scored percentage points left from
  0.000779850746269517 to 0.0007723880597012567, a
  0.00000746268656716418 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  kernel/syscall.c still compiles syscall dispatch, ptrace, wait, futex, perf,
  signal, mckfd, process, low-level bridge callback side-effect, and no-Rust
  fallback bodies. Validation completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.dRd8ik and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; symbol checks for strict Rust public-wrapper exports, strict C bridge
  exports/imports, final-image public-wrapper resolution, and fallback C
  wrapper exports; and exact strict audit before tracker credit. No QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-18: kernel/syscall.c moved from 99.995% to 99.997%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_clock_gettime(), sys_gettimeofday(), sys_nanosleep(), and
  sys_sched_yield() wrappers through Rust while preserving the no-Rust C
  fallback definitions. The Rust wrappers dispatch the existing Rust-owned time,
  cputime, nanosleep, and scheduler-yield body sequencing with C retained for
  gettime/copyin/copyout, rdtsc/ns-per-tsc, signal-pending, CPU pause,
  process-thread reader locks, CPU interrupt, spinlock, schedule, and fallback
  behavior. Strict mckernel_rust.o exports all four public wrappers and imports
  only the retained C bridge callbacks; strict kernel/syscall.c.o exports those
  retained bridges and imports the public wrapper names; strict final-image
  symbol resolution resolves the wrappers from Rust; and fallback C still
  exports the fallback wrapper definitions. The batch adds 0.002
  row-percentage point, reducing exact total scored percentage points left from
  0.0007873134328377773 to 0.000779850746269517, a
  0.00000746268656716418 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  kernel/syscall.c still compiles many syscall, ptrace, wait, futex, perf,
  signal, mckfd, process, low-level bridge callback side-effect, and no-Rust
  fallback bodies. Validation completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.iu75I1 and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; symbol checks for strict Rust public-wrapper exports, strict C bridge
  exports/imports, final-image public-wrapper resolution, and fallback C
  wrapper exports; and exact strict audit before tracker credit. No QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-18: kernel/syscall.c moved from 99.99% to 99.995%
  for the strict source-retirement audit. Rust-enabled mckernel.img now routes
  the public sys_munmap(), sys_brk(), sys_set_tid_address(), and
  sys_set_robust_list() wrappers through Rust while preserving the no-Rust C
  fallback definitions. The Rust wrappers dispatch existing Rust-owned munmap,
  brk, clear-child-TID, and robust-list validation sequencing with C retained
  for rwspinlock, do_munmap(), extend_process_region(), flush/log side effects,
  and fallback behavior. Strict mckernel_rust.o exports all four public
  wrappers and imports only the retained C bridge callbacks; strict
  kernel/syscall.c.o exports those retained bridges and imports the public
  wrapper names; strict final-image symbol resolution resolves the wrappers
  from Rust; and fallback C still exports the fallback wrapper definitions.
  The batch adds 0.005 row-percentage point, reducing exact total scored
  percentage points left from 0.0008059701492584281 to
  0.0007873134328377773, a 0.00001865671641791045 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict kernel/syscall.c still compiles many syscall, ptrace, wait, futex,
  perf, signal, mckfd, process, low-level bridge callback side-effect, and
  no-Rust fallback bodies. Validation completed with rustfmt --edition 2021
  --check kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.ufoVZo and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; symbol checks for strict Rust public-wrapper exports, strict C bridge
  exports/imports, final-image public-wrapper resolution, and fallback C
  wrapper exports; exact strict audit before tracker credit; and broad
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-18: executer/user/mcinspect.c moved from 99.995% to
  99.999% for the strict source-retirement audit. Rust-enabled mcinspect now
  routes the public main() wrapper through Rust while preserving the no-helper
  C fallback definition. The Rust main dispatches the existing Rust-owned
  mcinspect_main_entry_result() sequencing with C retained for getopt_long(),
  file opening, libdwarf initialization, global-state initialization,
  dwarf-finish/close side effects, mcps()/mcvtop() body callbacks, usage, and
  BFD symbol initialization. Strict mcinspect_helpers.o exports main() under
  the mcinspect_full_body build cfg and imports only the retained C bridge
  callbacks; strict mcinspect.c.o exports those retained bridge helpers without
  defining main(); strict final-executable symbol resolution resolves main()
  from Rust; and a direct no-MCINSPECT_RUST_HELPERS compile still exports the
  C fallback main() definition. The batch adds 0.004 row-percentage point,
  reducing exact total scored percentage points left from
  0.0008208955223949488 to 0.0008059701492584281, a
  0.00001492537313432836 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict mcinspect.c
  still compiles retained bridge callbacks, libdwarf/BFD/ioctl-driven
  lower-level bodies, global state access, and fallback scaffolding. Validation
  completed with rustfmt --edition 2021 --check
  executer/user/rust/mcinspect_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; strict
  cmake --build mcinspect target; configured fallback cmake --build mcinspect
  target; direct no-helper C compile of mcinspect.c to /tmp/mcinspect-no-rust.o;
  full MCKERNEL_RUST_EQUIV_KEEP_TMP=1
  kernel/rust/tests/run_equivalence.sh with tmpdir
  /tmp/mckernel-rust-equiv.VpJ4vx and Rust object unresolved-symbol/SIMD
  checks ok; and symbol checks for strict Rust main export, strict C bridge
  exports without main, final-executable main resolution, and no-helper C
  fallback main export. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.95% to
  99.99% for the strict source-retirement audit. Rust-enabled mckernel.img now
  routes cpu_halt(), cpu_safe_halt(), cpu_enable_interrupt(),
  cpu_disable_interrupt(), cpu_restore_interrupt(), cpu_pause(),
  cpu_disable_interrupt_save(), cpu_enable_interrupt_save(), and
  cpu_interrupt_disabled() through Rust while preserving the no-Rust C
  fallback definitions. These public primitive wrappers now publish the raw
  x86 hlt, sti/hlt, sti, cli, pushfq/popfq, pause, irq-save, irq-enable-save,
  and IF-bit query bodies from Rust. Because these instructions are privileged,
  the mckernel_equivalence Rust object deliberately leaves the symbols
  unresolved so user-space equivalence binaries use the existing C harness
  stubs, while normal strict kernel builds export the real Rust symbols.
  Strict mckernel_rust.o exports all nine wrappers; strict
  arch/x86_64/kernel/cpu.c.o only imports the active primitive names while
  retaining lower-level CPU/boot/trap bodies; strict final-image symbol
  resolution resolves all nine wrappers from Rust; and fallback C still exports
  the C wrapper definitions. The batch adds 0.04 row-percentage point,
  reducing exact total scored percentage points left from
  0.0009701492537317336 to 0.0008208955223949488, a
  0.0001492537313432836 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  arch/x86_64/kernel/cpu.c still compiles FPU/GDT/IDT, interrupt/trap,
  context-switch, panic-register, boot/AP, lower-level bridge callback side
  effects, and no-Rust fallback scaffolding. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.jhR88Y and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; and symbol checks for strict Rust primitive exports, strict C imports
  without definitions, final-image primitive resolution, and fallback C
  primitive exports. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: executer/user/eclair.c moved from 99.95% to
  99.99% for the strict source-retirement audit. Rust-enabled eclair now routes
  the public main() wrapper through Rust while preserving the no-helper C
  fallback definition. The Rust main sequences option parsing, banner/usage,
  symbol setup, dump setup, constants/thread discovery, GDB socket setup,
  packet-loop parsing, command dispatch, packet framing, ACK/NAK writes, and
  loop termination through Rust, with C retained for option parsing,
  dump/BFD-backed setup, thread discovery, socket/FILE side effects, command
  side effects, and fallback behavior. Strict eclair_helpers.o exports main()
  under the eclair_full_body build cfg and imports only the retained C bridge
  callbacks; strict eclair.c.o exports those retained bridges without defining
  main(); strict final-executable symbol resolution resolves main() from Rust;
  and a direct no-ECLAIR_RUST_HELPERS compile still exports the C fallback
  main() definition. The batch adds 0.04 row-percentage point, reducing exact
  total scored percentage points left from 0.0011194029850685183 to
  0.0009701492537317336, a 0.0001492537313367847 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict eclair.c still compiles retained BFD, dump setup, thread discovery,
  command, socket/FILE, lower-level bridge, and fallback scaffolding. Validation
  completed with rustfmt --edition 2021 --check
  executer/user/rust/eclair_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.m0f5mj and Rust object
  unresolved-symbol/SIMD checks ok; strict cmake --build eclair target;
  configured fallback cmake --build eclair target; direct no-helper C compile
  of eclair.c to /tmp/eclair-no-rust.o; and symbol checks for strict Rust
  main export, strict C bridge exports, final-executable main resolution, and
  no-helper C fallback main export. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.99% to 99.995% for
  the strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public init_process() wrapper through Rust while preserving the no-Rust C
  fallback definition. The Rust wrapper dispatches the existing Rust-owned
  process_init_state_body_result(), process_init_links_body_result(), and
  PROFILE_ENABLE process_init_profile_body_result() sequencing with C retained
  for lock, waitqueue, panic, and fallback side effects. Strict
  mckernel_rust.o exports init_process() and imports only the retained C bridge
  callbacks; strict kernel/process.c.o exports those retained bridges and
  imports the public wrapper name; strict final-image symbol resolution
  resolves init_process() from Rust; and fallback C still exports the fallback
  wrapper definition. The batch adds 0.005 row-percentage point, reducing exact
  total scored percentage points left from 0.0011380597014891691 to
  0.0011194029850685183, a 0.0000186567164206508 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict kernel/process.c still compiles retained bridge callbacks, clone/debug,
  process/thread lifecycle primitives, VM/range primitive side effects, and
  fallback scaffolding. Validation completed with rustfmt --edition 2021
  --check kernel/rust/process_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.JmTc8O and Rust object
  unresolved-symbol/SIMD checks ok; strict and fallback cmake --build
  mckernel.img targets; and symbol checks for strict Rust public-wrapper
  export, strict C bridge exports/import, final-image public-wrapper
  resolution, and fallback C wrapper export. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-18: executer/user/mcinspect.c moved from 99.99% to
  99.995% for the strict source-retirement audit. Rust-enabled mcinspect now
  routes the public mcps() and mcvtop() wrappers through Rust while retaining
  the no-helper C fallback bodies. The Rust wrappers dispatch the existing
  Rust-owned mcinspect_mcps_body_result() and mcinspect_mcvtop_body_result()
  sequencing with C retained for libdwarf-backed lookup, swapper page-table
  lookup, remote memory reads, init page-table printing, thread printing, and
  fallback header output. Strict mcinspect_helpers.o exports mcps() and
  mcvtop() and imports only the retained C bridge callbacks; strict
  mcinspect.c.o exports those retained bridges and imports the public wrapper
  names; strict final executable symbol resolution resolves the public
  wrappers from Rust; and a direct no-MCINSPECT_RUST_HELPERS compile still
  exports the C fallback definitions. The batch adds 0.005 row-percentage
  point, reducing exact total scored percentage points left from
  0.00115671641790982 to 0.0011380597014891691, a
  0.0000186567164206509 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict mcinspect.c
  still compiles retained bridge callbacks, libdwarf/BFD/ioctl-driven
  lower-level bodies, CLI dispatch support, global state access, and fallback
  scaffolding. Validation completed with rustfmt --edition 2021 --check
  executer/user/rust/mcinspect_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.ihA7E0 and Rust object
  unresolved-symbol/SIMD checks ok; strict and Rust-enabled fallback
  cmake --build mcinspect targets; direct no-helper C compile of mcinspect.c;
  and symbol checks for strict Rust public-wrapper exports, strict C bridge
  exports/imports, final-executable public-wrapper resolution, and no-helper C
  fallback wrapper exports. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/syscall.c moved from 99.90% to
  99.99%, kernel/syscall.c moved from 99.95% to 99.99%, and kernel/mem.c
  moved from 99.90% to 99.99% for the strict source-retirement audit.
  Rust-enabled mckernel.img now routes the public sys_clone(), sys_fork(),
  sys_vfork(), sys_shmget(), do_arch_prctl(), sys_arch_prctl(), sys_time(),
  sys_mmap(), sys_exit(), sys_exit_group(), and mem_init() wrappers through
  Rust while the no-Rust fallback build keeps the original C wrapper bodies.
  The Rust wrappers dispatch existing Rust-owned clone/fork, SysV shm, arch
  prctl, time copyout, mmap validation/dispatch, exit, exit-group, and memory
  lifecycle helpers with C retained for primitive copy/user, lock, do_fork,
  do_mmap, register/vector, monitor/rusage/NUMA/page/vmap init, logging, and
  fallback side effects. Strict mckernel_rust.o exports all credited public
  wrappers and imports only the retained C bridge callbacks; strict C objects
  export those retained bridges without defining the public wrappers; strict
  final-image symbol resolution resolves the public wrappers from Rust; and
  fallback C still exports the fallback wrapper definitions. The batch adds
  0.22 row-percentage point across the three rows, reducing exact total scored
  percentage points left from 0.001977611940290558 to
  0.00115671641790982, a 0.000820895522380738 total reduction across the
  audit rather than a per-category reduction. The rows remain partial because
  strict arch/x86_64/kernel/syscall.c still compiles signal-delivery,
  ptrace/debug-register, process-vm, vDSO lower-level, bridge callback side
  effects, and fallback scaffolding; strict kernel/syscall.c still compiles
  broad syscall, ptrace, wait, futex, perf, signal, mckfd, process, bridge
  callback, and fallback scaffolding; and strict kernel/mem.c still compiles
  allocator, NUMA, query-free, page-fault/PTE lookup, TLB-flush,
  kmalloc/pagealloc primitive callback, and fallback scaffolding. Validation
  completed with rustfmt --edition 2021 --check kernel/rust/syscall_policy.rs
  kernel/rust/mem_helpers.rs; bash -n kernel/rust/tests/run_equivalence.sh;
  scoped git diff --check; full MCKERNEL_RUST_EQUIV_KEEP_TMP=1
  kernel/rust/tests/run_equivalence.sh with tmpdir
  /tmp/mckernel-rust-equiv.m6rCl7 and Rust object unresolved-symbol/SIMD
  checks ok; strict and fallback symbol checks for the Rust, arch syscall,
  kernel syscall, memory, and final-image objects; broad Rocky build-only
  validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4; and exact
  source-retirement audit. The broad validation table rounded the exact
  nonzero total remainder to 0.0%; the JSON audit and fixed-width tracker rows
  remain authoritative. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.90% to
  99.95% for the strict source-retirement audit. Rust-enabled mckernel.img now
  routes lapic_timer_enable(), lapic_timer_disable(), lapic_ack(), and
  x2apic_is_enabled() public wrappers through Rust while the no-Rust fallback
  build keeps the original C wrapper bodies. The Rust wrappers dispatch
  existing Rust-owned LAPIC timer/EOI and x2APIC-enabled helpers with C
  retained for LAPIC register writes and MSR reads. Strict mckernel_rust.o
  exports all four public wrappers and imports only the retained C bridge
  callbacks; strict arch/x86_64/kernel/cpu.c.o exports those retained bridges
  without defining the public wrappers; strict final-image symbol resolution
  resolves the public wrappers from Rust; and fallback arch C still exports the
  fallback wrapper definitions. The batch adds 0.05 row-percentage point,
  reducing exact total scored percentage points left from
  0.002164179104482855 to 0.001977611940290558, a
  0.000186567164192297 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  arch/x86_64/kernel/cpu.c still compiles FPU/GDT/IDT, interrupt/trap,
  context-switch, panic-register, boot/AP, lower-level bridge callback side
  effects, and no-Rust fallback scaffolding. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.Tl9txp and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; and symbol checks for strict Rust public-wrapper exports, strict C
  bridge exports/imports, strict final-image public-wrapper resolution, and
  fallback C wrapper exports. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/mem.c moved from 99.80% to 99.90% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes
  begin_free_pages_pending(), finish_free_pages_pending(),
  ihk_mc_map_virtual(), and ihk_mc_unmap_virtual() public wrappers through
  Rust while the no-Rust fallback build keeps the original C wrapper bodies.
  The Rust wrappers dispatch existing Rust-owned pending-free and virtual-map
  body helpers with C retained for CPU-local pending-list lookup, allocator
  free handoff, vmap allocator lookup, page-table set/clear callbacks,
  TLB-flush callbacks, vmap alloc/free callbacks, and barrier side effects.
  Strict mckernel_rust.o exports all four public wrappers and imports only
  those retained C bridge callbacks; strict kernel/mem.c.o exports the retained
  bridges without defining the public wrappers; strict final-image symbol
  resolution resolves the public wrappers from Rust; and fallback kernel C
  still exports the fallback wrapper definitions. The batch adds 0.10
  row-percentage point, reducing exact total scored percentage points left from
  0.002537313432839028 to 0.002164179104482855, a
  0.000373134328356173 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict kernel/mem.c
  still compiles allocator, NUMA, query-free, virtual-allocator init,
  page-fault/PTE lookup, TLB-flush, kmalloc/pagealloc primitive callback
  side effects, and no-Rust fallback scaffolding. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/mem_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.tX8O1D and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; and symbol checks for strict Rust public-wrapper exports, strict C
  bridge exports, strict final-image public-wrapper resolution, and fallback C
  wrapper exports. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: kernel/syscall.c moved from 99.90% to 99.95% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public sys_times(), sys_setpgid(), sys_setrlimit(), sys_getrlimit(),
  sys_prlimit64(), sys_sysinfo(), sys_mlockall(), sys_munlockall(), and
  sys_getcpu() wrappers through Rust while the no-Rust fallback build keeps
  the original C wrapper bodies. The Rust wrappers dispatch existing
  Rust-owned body helpers with C retained for time conversion, process lookup,
  Linux forwarding, prlimit64, user-copy, mlock logging, CPU/NUMA ID, and
  rusage memory side effects. Strict mckernel_rust.o exports all nine public
  wrappers and imports only the retained C bridge callbacks; strict
  kernel/syscall.c.o imports those wrapper names and exports the retained
  bridges; strict final-image symbol resolution resolves the public wrappers
  from Rust; and fallback kernel C still exports the fallback wrapper
  definitions. The batch adds 0.05 row-percentage point, reducing exact total
  scored percentage points left from 0.0027238805970171143 to
  0.002537313432839028, a 0.0001865671641780864 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict kernel/syscall.c still compiles many syscall, ptrace, wait, futex,
  perf, signal, mckfd, process, and low-level bridge callback side-effect
  bodies plus no-Rust fallback scaffolding. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.4VPwSV and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; and symbol checks for strict Rust public-wrapper exports, strict C
  bridge exports/imports, strict final-image public-wrapper resolution, and
  fallback C wrapper exports. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/syscall.c moved from 99.85% to
  99.90% for the strict source-retirement audit. Rust-enabled mckernel.img now
  routes the public sys_rt_sigreturn() wrapper through Rust while the no-Rust
  fallback build keeps the original C wrapper body. The Rust wrapper obtains
  the current thread, signal-return register frame, and xsave size through a
  narrow C context bridge, then dispatches the existing Rust-owned
  arch_rt_sigreturn_body_result() sequencing with retained C callbacks for
  user-copy, syscall restart, signal publication/checking, allocation/free,
  need-resched, and xrstor side effects. Strict mckernel_rust.o exports
  sys_rt_sigreturn and imports only the retained rt-sigreturn bridge
  callbacks; strict arch/x86_64/kernel/syscall.c.o exports those bridges
  without defining the public wrapper; strict final-image symbol resolution
  resolves sys_rt_sigreturn from Rust; and fallback arch C still exports the
  fallback wrapper definition. The batch adds 0.05 row-percentage point,
  reducing exact total scored percentage points left from
  0.0029104477611952007 to 0.0027238805970171143, a
  0.0001865671641780864 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  arch/x86_64/kernel/syscall.c still compiles signal-delivery,
  ptrace/debug-register, process-vm, lower-level bridge callback side effects,
  vDSO Linux IKC/global-page/pvclock primitives, and no-Rust fallback bodies.
  Validation completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.RaDYwx and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; and symbol checks for strict Rust public-wrapper exports, strict C
  bridge exports/imports, strict final-image public-wrapper resolution, and
  fallback C wrapper exports. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/syscall.c moved from 99.75% to
  99.85% for the strict source-retirement audit. Rust-enabled mckernel.img now
  routes the public arch_setup_vdso() and arch_map_vdso() wrappers through
  Rust while the no-Rust fallback build keeps the original C wrapper bodies.
  The Rust wrappers acquire the file-local vDSO state through a narrow C state
  bridge, preserve public setup/map logging through a C log bridge, dispatch
  the existing Rust-owned vDSO setup/map body helpers, publish gettime/tod
  state through the existing ABI fields, and read the ProcessVm page-table
  pointer before mapping. Strict mckernel_rust.o exports arch_setup_vdso and
  arch_map_vdso; strict arch/x86_64/kernel/syscall.c.o exports only the
  retained vDSO state/callback/log bridge helpers and lower-level
  vdso_get_vdso_info()/vdso_map_global_pages() primitives for this slice; the
  strict final image resolves the public vDSO wrappers from Rust; and fallback
  arch C still exports the fallback wrapper definitions. The batch adds 0.10
  row-percentage point, reducing exact total scored percentage points left
  from 0.0032835820895513734 to 0.0029104477611952007, a
  0.0003731343283561727 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  arch/x86_64/kernel/syscall.c still compiles signal-return/signal-delivery,
  ptrace/debug-register, process-vm, lower-level bridge callback side effects,
  vDSO Linux IKC/global-page/pvclock primitives, and no-Rust fallback bodies.
  Validation completed with rustfmt --edition 2021 --check
  kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.pdRCkQ and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; and symbol checks for strict Rust public-wrapper exports, strict C
  bridge exports/imports, strict final-image public-wrapper resolution, and
  fallback C wrapper exports. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/syscall.c moved from 99.5% to
  99.75% for the strict source-retirement audit. Rust-enabled mckernel.img now
  routes the public sys_mmap(), sys_clone(), and sys_time() wrappers through
  Rust while the no-Rust fallback build keeps the original C wrapper bodies.
  The sys_mmap() wrapper publishes the C flag policy through narrow bridge
  callbacks and dispatches the existing Rust-owned mmap body with C retained
  for do_mmap(), overmap accounting, huge-page default lookup, and logging;
  sys_clone() dispatches the Rust-owned clone sequencing with C retained for
  coredump-lock callbacks and do_fork(); and sys_time() dispatches the
  Rust-owned time/copyout body with C retained for time() and copy_to_user().
  Strict mckernel_rust.o exports sys_mmap, sys_clone, and sys_time; strict
  kernel/syscall.c.o imports those public wrappers from Rust; strict
  arch/x86_64/kernel/syscall.c.o exports only the retained bridge callbacks
  and primitive time() provider for this slice; and fallback arch C still
  exports the fallback wrapper definitions. The batch adds 0.25 row-percentage
  point, reducing exact total scored percentage points left from
  0.004216417910456016 to 0.0032835820895513734, a
  0.000932835820895523 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  arch/x86_64/kernel/syscall.c still compiles signal-return/signal-delivery,
  ptrace/debug-register, process-vm, vDSO, lower-level bridge callback side
  effects, and no-Rust fallback bodies. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/syscall_policy.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  kernel/rust/tests/run_equivalence.sh with tmpdir
  /tmp/mckernel-rust-equiv.iZgSjK and Rust object unresolved-symbol/SIMD
  checks ok; strict and no-Rust fallback mckernel.img builds; and symbol
  checks for strict Rust exports, strict C bridge exports/imports, strict
  syscall-table imports, and fallback C wrapper exports. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-18: ihk/linux/core/host_driver.c moved from 99.35% to
  99.99%, ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved from
  98.40% to 99.99%, kernel/syscall.c moved from 99.75% to 99.90%, and
  kernel/mem.c moved from 99.50% to 99.80% for the strict
  source-retirement audit. This intentionally combines unrelated high-risk
  host, SMP, syscall, and memory-init bodies into one validated block.
  Rust-enabled builds now route the broad host public wrapper layer through
  Rust, including load/file/interrupt/status/address/map/NMI/device-map/
  topology/userdata/index/kernel-handler/device lookup wrappers; the SMP
  arch layer routes setup-trampoline, setup-startup, send-NMI,
  send-multi-interrupt, query-status, and issue-interrupt wrappers through
  Rust; the syscall layer routes sys_exit() and sys_exit_group() through
  Rust; and the kernel memory layer routes the public mem_init() sequencing
  through Rust. The no-Rust fallback builds keep the original C wrapper bodies.
  The Rust wrappers dispatch existing Rust-owned body helpers while strict C
  remains only for primitive bridge callbacks, layout/constant publication,
  low-level locking/list/allocation/user-copy/APIC/IRQ/page-table/device
  side effects, logging, and fallback bodies. Strict Rust objects export the
  migrated public wrapper names, strict C imports those wrappers where used
  and exports the C bridge callbacks, and fallback C still exports the fallback
  definitions. The batch adds exactly 2.68 row-percentage points across the
  four rows, reducing exact total scored percentage points left from
  0.01421641791043271 to 0.004216417910456016, a 0.009999999999976694 total
  reduction across the audit rather than a per-category reduction. The rows
  remain partial because the strict Rust build still compiles host_driver.c,
  smp-arch-driver.c, syscall.c, and mem.c for remaining primitive callbacks,
  lower-level side effects, and no-Rust fallback scaffolding; those compiled C
  bodies prevent an honest exact-zero source-retirement claim. Validation
  completed with rustfmt --edition 2021 --check over the changed Rust helper
  files; bash -n kernel/rust/tests/run_equivalence.sh; scoped top-level and
  ihk-submodule git diff --check; full kernel/rust/tests/run_equivalence.sh
  with tmpdir /tmp/mckernel-rust-equiv.xadJ8u and Rust object
  unresolved-symbol/SIMD checks ok; strict and no-Rust fallback mckernel.img
  builds; strict and no-Rust fallback ihk_ko and ihk-smp-x86_64_ko builds;
  symbol checks for strict Rust exports, strict C bridge exports/imports, and
  fallback C wrapper exports across the kernel, host, and SMP objects; and
  broad Rocky build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-18: ihk/linux/driver/smp/smp-driver.c moved from 99.35% to
  99.95%, and ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved from
  98.05% to 98.40% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64.ko now routes a broad SMP public-wrapper batch through Rust:
  operation/control wrappers, mode wrappers, CPU-query wrappers, IKC-map
  wrappers, memory-query wrappers, AP wakeup, AP reset, arch exit, and
  perf-event-map publication. The no-Rust fallback build keeps the original C
  wrapper definitions. The Rust wrappers dispatch existing Rust-owned body
  helpers with C retained only for primitive bridge callbacks, structure
  offset/size publication, APIC and preemption side effects, kallsyms perf
  table lookup, user-copy/allocation/free primitives, topology/list/table
  access, logging, and no-Rust fallback bodies. Strict smp_driver_helpers.o
  exports the public wrapper names, strict C no longer defines those retired
  wrappers, fallback C still exports the fallback definitions, and the strict
  module resolves the public ABI names from Rust. The batch adds 0.95
  row-percentage point across the two host/IHK rows, reducing exact total
  scored percentage points left from 0.017761194029844773 to
  0.01421641791043271, a 0.003544776119412063 total reduction across the
  audit rather than a per-category reduction. The rows remain partial because
  strict smp-driver.c and smp-arch-driver.c still compile for boot/load,
  assignment/release, startup/trampoline, IRQ/APIC primitive behavior,
  topology collection, lower-level bridge callback side effects, and no-Rust
  fallback bodies. Validation completed with rustfmt --edition 2021 --check
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped top-level and ihk-submodule
  git diff --check; full MCKERNEL_RUST_EQUIV_KEEP_TMP=1
  kernel/rust/tests/run_equivalence.sh with tmpdir
  /tmp/mckernel-rust-equiv.rY5dIk and Rust object unresolved-symbol and SIMD
  checks ok; strict and no-Rust fallback ihk-smp-x86_64_ko builds; symbol
  checks for strict smp_driver_helpers.o, strict and fallback smp-driver.o,
  strict and fallback smp-arch-driver.o, and strict ihk-smp-x86_64.ko; broad
  Rocky build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4; and exact
  source-retirement audit with host/IKC/mcctrl/IHK modules
  completion=99.970375,
  overall_scored_completion=99.98578358208957, compiled_retired_c=0,
  compiled_non_allowlisted_c=29, debt_rows=19, stale_tracker_rows=[],
  untracked_files=[], and headers_with_executable_logic=0. The broad
  validation table rounded the exact nonzero total remainder to 0.0%; the JSON
  audit and fixed-width tracker row are the authoritative progress values. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.

- 2026-06-18: ihk/linux/core/host_driver.c moved from 99.0% to 99.35% for
  the strict source-retirement audit. Rust-enabled ihk.ko now routes the
  public ihk_os_boot(), ihk_os_shutdown(),
  ihk_os_register_user_call_handlers(),
  ihk_os_unregister_user_call_handlers(), ihk_dma_request(),
  ihk_host_register_os_notifier(), and
  ihk_host_deregister_os_notifier() wrappers through Rust as one host public
  ABI wrapper batch, while the no-Rust fallback build keeps the original C
  wrapper definitions. The Rust wrappers dispatch the existing Rust-owned boot,
  shutdown, user-call-list, DMA-request, and notifier-list body helpers. Strict
  host_driver.c now retains only primitive bridge callbacks for OS index
  lookup, kmsg locking/find/refcounting, OS boot/shutdown ops dispatch, master
  channel setup/finalization, notifier-down/up sequencing, status/wait/thaw/NMI
  delay/release-kmsg/log side effects, user-call lock/unlock, notifier-list head
  access, and notifier logging. Strict core_helpers.o exports all seven public
  wrappers; strict host_driver.o imports those wrapper names and exports the
  bridge callbacks; fallback host_driver.o still exports the C wrapper
  definitions. The batch adds 0.35 row-percentage point, reducing exact total
  scored percentage points left from 0.019067164179091378 to
  0.017761194029844773, a 0.001305970149246605 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict host_driver.c still compiles for device/OS registration and teardown,
  ioctl/load/create/destroy paths, kmsg primitive lifetime, kernel/user copy,
  lock/list/free primitive callbacks, file and memory mapping side effects,
  notifier primitive callbacks, and no-Rust fallback bodies. Validation
  completed with rustfmt --edition 2021 --check
  ihk/linux/core/rust/core_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped top-level and ihk-submodule
  git diff --check; full kernel/rust/tests/run_equivalence.sh with tmpdir
  /tmp/mckernel-rust-equiv.cMVG3q, ihk_core_helpers digest
  a9ac16e765c01f69, and Rust object unresolved-symbol and SIMD checks ok;
  strict and no-Rust fallback ihk_ko builds; symbol checks for strict
  core_helpers.o, strict host_driver.o, fallback host_driver.o, and strict
  ihk.ko; broad Rocky build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4; and exact source-retirement audit with
  host/IKC/mcctrl/IHK modules completion=99.9585,
  overall_scored_completion=99.98223880597016, compiled_retired_c=0,
  compiled_non_allowlisted_c=29, debt_rows=19, stale_tracker_rows=[],
  untracked_files=[], and headers_with_executable_logic=0. The broad
  validation table rounded the exact nonzero total remainder to 0.0%; the JSON
  audit and fixed-width tracker row are the authoritative progress values. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.70% to 99.90% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public ihk_mc_boot_cpu(), mcexec_v10_trace_enter_user(), and
  release_runq_lock() wrappers through Rust as one larger CPU batch, while
  the no-Rust fallback build keeps the original C wrapper definitions. The
  Rust wrappers dispatch the existing Rust-owned AP boot, mcexec trace, and
  runqueue-unlock body helpers. Strict C now retains only primitive bridge
  callbacks for trampoline metadata, boot page-table physical lookup, CPU
  kernel-stack lookup, transit page-table lookup, AP wakeup, pause,
  current-thread lookup, trace-offset publication, trace logging, current
  CPU-local lookup, runqueue offsets, and spinlock unlock side effects. Strict
  mckernel_rust.o exports ihk_mc_boot_cpu, mcexec_v10_trace_enter_user,
  release_runq_lock, x86_boot_cpu_body_result,
  x86_mcexec_v10_trace_enter_user_body_result, and
  x86_release_runq_lock_body_result and imports the bridge callbacks; strict
  cpu.c.o no longer defines those public wrappers and instead exports the
  bridge callbacks; fallback cpu.c.o still exports the C wrapper definitions.
  The batch adds 0.20 row-percentage point, reducing exact total scored
  percentage points left from 0.019813432835817935 to
  0.019067164179091378, a 0.000746268656726557 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict cpu.c still compiles for interrupt entry/exit, AP startup assembly
  sequencing, FPU save/restore, panic-register capture, lower-level bridge
  callback side effects, pvclock globals/bridges, stack-printing primitive
  side effects, and no-Rust fallback bodies. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  kernel/rust/tests/run_equivalence.sh with tmpdir
  /tmp/mckernel-rust-equiv.manzOV, x86_cpu_helpers digest
  b6b595ccb6205699, and Rust object unresolved-symbol and SIMD checks ok;
  strict and no-Rust fallback mckernel.img builds; symbol checks for strict
  Rust/C/fallback objects; broad Rocky build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4; and exact source-retirement audit with
  Other in-scope code completion=99.99897959183673,
  overall_scored_completion=99.98093283582091, compiled_retired_c=0,
  compiled_non_allowlisted_c=29, debt_rows=19, stale_tracker_rows=[],
  untracked_files=[], and headers_with_executable_logic=0. The broad
  validation table rounded the exact nonzero total remainder to 0.0%; the JSON
  audit and fixed-width tracker row are the authoritative progress values. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.65% to 99.70% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public __show_stack() and show_context_stack() wrappers through Rust using
  the existing Rust-owned stack-frame walker with fixed kernel stack bounds and
  a C bridge for stack-frame logging, while the no-Rust fallback build keeps
  the original C wrapper definitions. Strict mckernel_rust.o exports
  __show_stack, show_context_stack, and x86_show_stack_body_result and imports
  x86_stack_frame_log_bridge; strict cpu.c.o no longer defines the public
  wrappers and instead exports the stack-frame log bridge; fallback cpu.c.o
  still exports the C wrapper definitions. The batch adds 0.05 row-percentage
  point, reducing exact total scored percentage points left from
  0.01999999999998181 to 0.019813432835817935, a
  0.000186567164163875 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict cpu.c still
  compiles for interrupt entry/exit, AP startup assembly sequencing, FPU
  save/restore, panic-register capture, lower-level bridge callback side
  effects, pvclock globals/bridges, stack-printing primitive side effects, and
  no-Rust fallback bodies. Validation completed with rustfmt --edition 2021
  --check kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.Rw8JUn, x86_cpu_helpers digest
  19df79c3ccf9e387, and Rust object unresolved-symbol and SIMD checks ok
  after correcting separately logged digest and unresolved-count failures;
  strict and no-Rust fallback mckernel.img builds; symbol checks for strict
  Rust/C/fallback objects; broad Rocky build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4; and exact source-retirement audit with
  Other in-scope code completion=99.99693877551022,
  overall_scored_completion=99.98018656716418, compiled_retired_c=0,
  compiled_non_allowlisted_c=29, debt_rows=19, stale_tracker_rows=[],
  untracked_files=[], and headers_with_executable_logic=0. The broad
  validation table rounded the exact nonzero total remainder to 0.0%; the JSON
  audit and fixed-width tracker row are the authoritative progress values. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.60% to 99.65% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public call_ap_func() AP-start trampoline wrapper through Rust, publishing
  cpu_boot_status through a strict-path C bridge and dispatching the existing
  Rust-owned AP-call body with the next-function callback, while the no-Rust
  fallback build keeps the original C wrapper definition. Strict
  mckernel_rust.o exports call_ap_func and x86_call_ap_func_body_result and
  imports x86_cpu_boot_status_slot_bridge; strict cpu.c.o imports the public
  wrapper and exports the status-slot bridge; fallback cpu.c.o still exports
  the C wrapper definition. The batch adds 0.05 row-percentage point, reducing
  exact total scored percentage points left from 0.020186567164159896 to
  0.01999999999998181, a 0.000186567164178086 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict cpu.c still compiles for interrupt entry/exit, AP startup assembly
  sequencing, FPU save/restore, panic-register capture, lower-level bridge
  callback side effects, pvclock globals/bridges, stack-printing primitive
  side effects, and no-Rust fallback bodies. Validation completed with rustfmt
  --edition 2021 --check kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.ut5lCm, x86_cpu_helpers digest
  110dce3515f65dfb, and Rust object unresolved-symbol and SIMD checks ok;
  strict and no-Rust fallback mckernel.img builds; symbol checks for strict
  Rust/C/fallback objects; broad Rocky build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4; and exact source-retirement audit with
  Other in-scope code completion=99.99642857142857,
  overall_scored_completion=99.98000000000002, compiled_retired_c=0,
  compiled_non_allowlisted_c=29, debt_rows=19, stale_tracker_rows=[],
  untracked_files=[], and headers_with_executable_logic=0. The broad
  validation table rounded the exact nonzero total remainder to 0.0%; the JSON
  audit and fixed-width tracker row are the authoritative progress values. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.55% to 99.60% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public arch_print_stack() wrapper through Rust, reading rbp with Rust inline
  asm and dispatching the existing Rust-owned stack-print body with C bridge
  callbacks for pvclock logging and stack printing, while the no-Rust fallback
  build keeps the original C wrapper definition. Strict mckernel_rust.o exports
  arch_print_stack and x86_arch_print_stack_body_result and imports
  x86_pvclock_log_bridge and x86_print_stack_bridge; strict cpu.c.o no longer
  defines that public wrapper and instead exports the retained stack-print
  bridge callback; fallback cpu.c.o still exports the C wrapper definition.
  The batch adds 0.05 row-percentage point, reducing exact total scored
  percentage points left from 0.020373134328352194 to
  0.020186567164159896, a 0.000186567164192298 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict cpu.c still compiles for interrupt entry/exit, AP startup, FPU
  save/restore, panic-register capture, lower-level bridge callback side
  effects, pvclock globals/bridges, stack-printing primitive side effects, and
  no-Rust fallback bodies. Validation completed with rustfmt --edition 2021
  --check kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.8LwClb, x86_cpu_helpers digest
  b9893c625c47a66f, and Rust object unresolved-symbol and SIMD checks ok;
  strict and no-Rust fallback mckernel.img builds; symbol checks for strict
  Rust/C/fallback objects; broad Rocky build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4; and exact source-retirement audit with
  overall_scored_completion=99.97981343283584, compiled_retired_c=0,
  compiled_non_allowlisted_c=29, debt_rows=19, stale_tracker_rows=[],
  untracked_files=[], and headers_with_executable_logic=0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.50% to 99.55% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public arch_print_pre_interrupt_stack() wrapper through Rust, dispatching the
  existing Rust-owned pre-interrupt stack body with Rust ABI-layout offsets,
  the PF_USER gate, and C bridge callbacks for pvclock logging and stack
  printing, while the no-Rust fallback build keeps the original C wrapper
  definition. Strict mckernel_rust.o exports arch_print_pre_interrupt_stack
  and imports x86_print_stack_bridge; strict cpu.c.o no longer defines that
  public wrapper and instead exports the retained stack-print bridge callback;
  fallback cpu.c.o still exports the C wrapper definition. The batch adds 0.05
  row-percentage point, reducing exact total scored percentage points left from
  0.02055970149253028 to 0.020373134328352194, a
  0.000186567164178086 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict cpu.c still
  compiles for interrupt entry/exit, AP startup, FPU save/restore,
  panic-register capture, lower-level bridge callback side effects, pvclock
  globals/bridges, stack-printing primitive side effects, and no-Rust fallback
  bodies. Validation completed with rustfmt --edition 2021 --check
  kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.dWtiNq, x86_cpu_helpers digest
  639cc1de2b219a77, and Rust object unresolved-symbol and SIMD checks ok;
  strict and no-Rust fallback mckernel.img builds; symbol checks for strict
  Rust/C/fallback objects; broad Rocky build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4; and exact source-retirement audit with
  overall_scored_completion=99.97962686567165, compiled_retired_c=0,
  compiled_non_allowlisted_c=29, debt_rows=19, stale_tracker_rows=[],
  untracked_files=[], and headers_with_executable_logic=0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.45% to 99.50% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public arch_show_interrupt_context() wrapper through Rust, dispatching the
  existing Rust-owned interrupt-context body with C bridge callbacks for
  kprintf lock/unlock and context-line logging, while the no-Rust fallback
  build keeps the original C wrapper definition. Strict mckernel_rust.o exports
  arch_show_interrupt_context and imports x86_context_line_log_bridge,
  x86_kprintf_lock_bridge, and x86_kprintf_unlock_bridge; strict cpu.c.o no
  longer defines that public wrapper and instead imports it while exporting the
  retained bridge callbacks; fallback cpu.c.o still exports the C wrapper
  definition. The batch adds 0.05 row-percentage point, reducing exact total
  scored percentage points left from 0.020746268656708367 to
  0.02055970149253028, a 0.000186567164178087 total reduction across the audit
  rather than a per-category reduction. The row remains partial because strict
  cpu.c still compiles for interrupt entry/exit, AP startup, FPU save/restore,
  panic-register capture, lower-level bridge callback side effects, pvclock
  globals/bridges, and no-Rust fallback bodies. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.IYZJ26, x86_cpu_helpers digest
  20fefecffc0abe7e, and Rust object unresolved-symbol and SIMD checks ok after
  correcting the separately logged rustfmt failure; strict and no-Rust fallback
  mckernel.img builds; symbol checks for strict Rust/C/fallback objects; broad
  Rocky build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4; and exact
  source-retirement audit with overall_scored_completion=99.97944029850747,
  compiled_retired_c=0, compiled_non_allowlisted_c=29, debt_rows=19,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.40% to 99.45% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public arch_setup_pvclock() wrapper through Rust, dispatching the existing
  Rust-owned pvclock setup body with a Rust CPUID availability bridge and C
  bridge callbacks for CPUID, aligned page allocation, and log side effects,
  while the no-Rust fallback build keeps the original C wrapper definition.
  Strict mckernel_rust.o exports arch_setup_pvclock and imports pvti_npages,
  x86_cpuid_leaf_bridge, and x86_alloc_aligned_pages_node_bridge; strict
  cpu.c.o no longer defines that public wrapper and instead exports the
  retained pvclock globals and bridge callbacks; fallback cpu.c.o still exports
  the C wrapper definition. The batch adds 0.05 row-percentage point, reducing
  exact total scored percentage points left from 0.020932835820886453 to
  0.020746268656708367, a 0.000186567164178086 total reduction across the
  audit rather than a per-category reduction. The row remains partial because
  strict cpu.c still compiles for interrupt entry/exit, AP startup, FPU
  save/restore, panic-register capture, lower-level pvclock globals/bridge
  side effects, register bridge callback side effects, and no-Rust fallback
  bodies. Validation completed with rustfmt --edition 2021 --check
  kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.mHtMIb, x86_cpu_helpers digest
  6a4493d63dc0d9ec, and Rust object unresolved-symbol and SIMD checks ok after
  correcting the separately logged weak-stub null-pointer failure; strict and
  no-Rust fallback mckernel.img builds; symbol checks for strict
  Rust/C/fallback objects; broad Rocky build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4; and exact source-retirement audit with
  overall_scored_completion=99.97925373134329, compiled_retired_c=0,
  compiled_non_allowlisted_c=29, debt_rows=19, stale_tracker_rows=[],
  untracked_files=[], and headers_with_executable_logic=0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.35% to 99.40% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public arch_start_pvclock() wrapper through Rust, dispatching the existing
  Rust-owned pvclock start body with C bridge callbacks for current CPU lookup,
  virtual-to-physical translation, MSR write, and log side effects, while the
  no-Rust fallback build keeps the original C wrapper definition. Strict
  mckernel_rust.o exports arch_start_pvclock; strict cpu.c.o no longer defines
  that public wrapper and instead exports pvti_msr plus the pvclock bridge
  callbacks; fallback cpu.c.o still exports the C wrapper definition. The batch
  adds 0.05 row-percentage point, reducing exact total scored percentage points
  left from 0.02111940298506454 to 0.020932835820886453, a
  0.000186567164178087 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict cpu.c still
  compiles for interrupt entry/exit, AP startup, FPU save/restore,
  panic-register capture, pvclock setup/allocation, register bridge callback
  side effects, and no-Rust fallback bodies. Validation completed with
  rustfmt --edition 2021 --check kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.Qftq1M, x86_cpu_helpers digest
  14ba6594ea841de4, and Rust object unresolved-symbol and SIMD checks ok;
  strict and no-Rust fallback mckernel.img builds; symbol checks for strict
  Rust/C/fallback objects; broad Rocky build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4; and exact source-retirement audit with
  overall_scored_completion=99.97906716417911, compiled_retired_c=0,
  compiled_non_allowlisted_c=29, debt_rows=19, stale_tracker_rows=[],
  untracked_files=[], and headers_with_executable_logic=0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.30% to 99.35% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public init_tick(), init_delay(), and sync_tick() wrappers through Rust,
  dispatching the existing Rust-owned tick-log body and using the C
  x86_tick_log_bridge only for the dkprintf side effects, while the no-Rust
  fallback build keeps the original C wrapper definitions. Strict
  mckernel_rust.o exports init_tick, init_delay, and sync_tick; strict cpu.c.o
  no longer defines those public wrappers and only exports x86_tick_log_bridge;
  fallback cpu.c.o still exports the C wrapper definitions. The batch adds
  0.05 row-percentage point, reducing exact total scored percentage points left
  from 0.021305970149256837 to 0.02111940298506454, a
  0.000186567164192297 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict cpu.c still
  compiles for interrupt entry/exit, AP startup, FPU save/restore,
  panic-register capture, pvclock setup, register bridge callback side effects,
  and no-Rust fallback bodies. Validation completed with rustfmt --edition 2021
  --check kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.kG7s0x, x86_cpu_helpers digest
  62f59db68e114c67, and Rust object unresolved-symbol and SIMD checks ok;
  strict and no-Rust fallback mckernel.img builds; symbol checks for strict
  Rust/C/fallback objects; broad Rocky build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4; and exact source-retirement audit with
  overall_scored_completion=99.97888059701494, compiled_retired_c=0,
  compiled_non_allowlisted_c=29, debt_rows=19, stale_tracker_rows=[],
  untracked_files=[], and headers_with_executable_logic=0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.25% to 99.30% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public ihk_mc_interrupt_cpu() wrapper through Rust, using native CPU-local
  APIC-id lookup, num_processors validation, IPI bridge handoff, and invalid/log
  event routing to dispatch the existing Rust-owned interrupt CPU body, while
  the no-Rust fallback build keeps the original C validation/IPI body. Strict
  mckernel_rust.o exports ihk_mc_interrupt_cpu; strict cpu.c.o no longer defines
  it; fallback cpu.c.o still exports the C definition. The batch adds 0.05
  row-percentage point, reducing exact total scored percentage points left from
  0.021492537313420712 to 0.021305970149256837, a 0.000186567164163875 total
  reduction across the audit rather than a per-category reduction. The row
  remains partial because strict cpu.c still compiles for interrupt entry/exit,
  AP startup, FPU save/restore, panic-register capture, pvclock setup, register
  bridge callback side effects, and no-Rust fallback bodies. Validation
  completed with rustfmt --edition 2021 --check
  kernel/rust/x86_cpu_helpers.rs; bash -n kernel/rust/tests/run_equivalence.sh;
  scoped git diff --check; strict and no-Rust fallback mckernel.img builds;
  symbol checks for strict Rust/C/fallback objects; full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.r7qyiG, x86_cpu_helpers digest
  d02e0049b4dee17d, and Rust object unresolved-symbol and SIMD checks ok; broad
  Rocky build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4; and final
  exact source-retirement audit with compiled_retired_c=0,
  compiled_non_allowlisted_c=29, debt_rows=19, stale_tracker_rows=[],
  untracked_files=[], and headers_with_executable_logic=0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.20% to 99.25% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public ihk_mc_arch_set_special_register() and
  ihk_mc_arch_get_special_register() wrappers through Rust, using native
  FS-base rdmsr/wrmsr shims to dispatch the existing Rust-owned special-register
  validation bodies, while the no-Rust fallback build keeps the original C
  switch bodies. Strict mckernel_rust.o exports both public wrapper symbols;
  strict cpu.c.o no longer defines them; fallback cpu.c.o still exports the C
  definitions. The batch adds 0.05 row-percentage point, reducing exact total
  scored percentage points left from 0.0216791044775988 to
  0.021492537313420712, a 0.000186567164178088 total reduction across the audit
  rather than a per-category reduction. The row remains partial because strict
  cpu.c still compiles for interrupt entry/exit, AP startup, FPU save/restore,
  panic-register capture, pvclock setup, register bridge callback side effects,
  and no-Rust fallback bodies. Validation completed with rustfmt --edition 2021
  --check kernel/rust/x86_cpu_helpers.rs; bash -n
  kernel/rust/tests/run_equivalence.sh; scoped git diff --check; strict and
  no-Rust fallback mckernel.img builds; symbol checks for strict
  Rust/C/fallback objects and final strict/fallback images; and full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.xvFToe and Rust object unresolved-symbol and
  SIMD checks ok. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.15% to 99.20% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public arch_cpu_read_write_register() wrapper through Rust, using native
  rdmsr/wrmsr callback shims to dispatch the existing Rust-owned register
  operation body, while the no-Rust fallback build keeps the original C
  read/write wrapper. Strict mckernel_rust.o exports arch_cpu_read_write_register;
  strict cpu.c.o no longer defines it; fallback cpu.c.o still exports the C
  definition. The batch adds 0.05 row-percentage point, reducing exact total
  scored percentage points left from 0.021865671641776885 to
  0.0216791044775988, a 0.000186567164178085 total reduction across the audit
  rather than a per-category reduction. The row remains partial because strict
  cpu.c still compiles for interrupt entry/exit, AP startup, FPU save/restore,
  panic-register capture, pvclock setup, special-register wrappers, register
  bridge callback side effects, and no-Rust fallback bodies. Validation
  completed with rustfmt --edition 2021 --check kernel/rust/x86_cpu_helpers.rs;
  bash -n kernel/rust/tests/run_equivalence.sh; scoped git diff --check;
  strict and no-Rust fallback mckernel.img builds; symbol checks for strict
  Rust/C/fallback objects and final strict/fallback images; and full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.My3eOg and Rust object unresolved-symbol and
  SIMD checks ok after correcting the stale unresolved-symbol count recorded in
  kernel.log. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.10% to 99.15% for the
  strict source-retirement audit. Rust-enabled mckernel.img now routes the
  public running_on_kvm() wrapper through Rust, using a native CPUID leaf
  bridge into the existing Rust-owned KVM-signature decision helper, while the
  no-Rust fallback build keeps the original C CPUID body. Strict
  mckernel_rust.o exports running_on_kvm; strict cpu.c.o no longer defines it
  and only references the Rust-owned symbol; fallback cpu.c.o still exports
  the C definition. The batch adds 0.05 row-percentage point, reducing exact
  total scored percentage points left from 0.02205223880595497 to
  0.021865671641776885, a 0.000186567164178085 total reduction across the audit
  rather than a per-category reduction. The row remains partial because strict
  cpu.c still compiles for interrupt entry/exit, AP startup, FPU save/restore,
  panic-register capture, pvclock setup, register access, and bridge callback
  side effects. Validation completed with rustfmt --edition 2021 --check
  kernel/rust/x86_cpu_helpers.rs; bash -n kernel/rust/tests/run_equivalence.sh;
  scoped git diff --check; strict and no-Rust fallback mckernel.img builds;
  symbol checks for strict Rust/C/fallback objects; and full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.LlEWqQ and Rust object unresolved-symbol and
  SIMD checks ok. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: tools/crash/mckernel.c moved from 99.99% to 100.0% for the
  strict source-retirement audit. The Rocky strict build does not compile this
  crash-extension source into an object; CMake only copies mckernel.c, README,
  and the Rust crash helper source into the crash tool build directory. That
  leaves no strict-build compiled C body for this tracker row, while the active
  Rust helper coverage and copied crash-extension compatibility source remain
  outside the strict McKernel runtime/control-plane object graph.
  The exact audit reduced total_percentage_points_left from
  0.022089552238796273 to 0.02205223880595497, a
  0.000037313432841303 total reduction across the audit.

- 2026-06-18: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved from
  98.00% to 98.05% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes the public smp_ihk_os_map_memory(),
  smp_ihk_os_unmap_memory(), smp_ihk_map_memory(), smp_ihk_unmap_memory(), and
  smp_ihk_os_check_ikc_map() wrapper symbols through Rust exports while
  preserving matching no-Rust C fallback definitions outside the strict path.
  The Rust wrappers dispatch the existing Rust-owned identity map/unmap and
  no-op IKC-map helpers. Strict smp_driver_helpers.o exports the five public
  wrapper symbols; strict smp-arch-driver.o no longer defines or references
  them; fallback smp-arch-driver.o still exports the C definitions. The batch
  adds 0.05 row-percentage point, reducing exact total scored percentage points
  left from 0.02227611940297436 to 0.022089552238796273, a
  0.000186567164178087 total reduction across the audit rather than a
  per-category reduction. The row remains partial because strict
  smp-arch-driver.c still compiles for AP startup/reset, trampoline setup,
  identity/startup page-table primitives, interrupt delivery, topology/sysfs
  bridge callbacks, arch init/exit resource mutation, dump/perf wrappers, and
  no-Rust fallback bodies. Validation completed with bash -n
  kernel/rust/tests/run_equivalence.sh; rustfmt --edition 2021 --check
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs; scoped git diff --check;
  full MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh
  with tmpdir /tmp/mckernel-rust-equiv.gf5Gy5 and Rust object unresolved-symbol
  and SIMD checks ok; strict and no-Rust fallback ihk-smp-x86_64_ko builds;
  strict/fallback symbol checks; broad Rocky Rust build-only validation; and
  final exact JSON source-retirement audit. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-18: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved from
  97.95% to 98.00% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes the public smp_ihk_arch_dcache_flush(),
  calc_ns_per_tsc(), x2apic_is_enabled(), and smp_ihk_adjust_entry() wrapper
  symbols through Rust exports while preserving the matching no-Rust C fallback
  definitions outside the strict path. The Rust wrappers dispatch the existing
  Rust-owned dcache no-op, ns-per-TSC policy, x2APIC MSR bit check, and entry
  adjustment helpers; C remains responsible for CPUID/MSR reads, tsc_khz access,
  and the remaining APIC/trampoline/page-table primitive side effects through
  bridge callbacks. Strict smp_driver_helpers.o exports the four public wrapper
  symbols and references ihk_smp_calc_tsc_ratio_bridge,
  ihk_smp_tsc_khz_bridge, and ihk_smp_x2apic_msr_bridge; strict
  smp-arch-driver.o no longer defines the public wrappers and only provides the
  bridge callbacks; fallback smp-arch-driver.o still exports the C definitions.
  The batch adds 0.05 row-percentage point, reducing exact total scored
  percentage points left from 0.022462686567166656 to
  0.02227611940297436, a 0.000186567164192296 total reduction across the audit
  rather than a per-category reduction. The row remains partial because strict
  smp-arch-driver.c still compiles for AP startup/reset, trampoline setup,
  identity/startup page-table primitives, interrupt delivery, topology/sysfs
  bridge callbacks, arch init/exit resource mutation, dump/perf wrappers, and
  no-Rust fallback bodies. Validation completed with bash -n
  kernel/rust/tests/run_equivalence.sh; rustfmt --edition 2021 --check
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs; scoped git diff --check;
  full MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh
  with tmpdir /tmp/mckernel-rust-equiv.j7S4PW and Rust object unresolved-symbol
  and SIMD checks ok; strict and no-Rust fallback ihk-smp-x86_64_ko builds;
  strict/fallback symbol checks; broad Rocky Rust build-only validation; and
  final exact JSON source-retirement audit. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.98% to 99.99% for the
  strict source-retirement audit. Rust-enabled builds now route the public
  check_need_resched() and sched_init() wrappers through Rust exports of the
  same ABI names, while preserving the no-Rust C fallback bodies outside the
  strict path. The Rust check_need_resched() wrapper resolves the current CPU
  and CPU-local pointer, uses the verified sched_check_need_resched_body_result()
  body with resched/migrate flags, and dispatches the existing schedule/log
  bridge callbacks. The Rust sched_init() wrapper resolves the current CPU-local
  state, resource-set list, and processor id, then dispatches
  process_sched_init_body_result() with the process, lock, context, fp-save,
  timer, and panic bridge callbacks. Strict mckernel_rust.o exports
  check_need_resched and sched_init, strict process.c.o no longer defines those
  public wrapper symbols while providing the bridge callbacks, and fallback
  process.c.o still exports the C definitions. The row remains partial because
  process.c still compiles in the strict build graph for idle,
  schedule/runqueue/wakeup, process/thread lifecycle, locks, context switch,
  page-table/VM primitive callbacks, and no-Rust fallback bodies; an attempted
  100.0% row close-out was rejected by the exact audit with compiled_retired_c=2.
  The batch adds 0.01 row-percentage point, reducing exact total scored
  percentage points left from 0.022499999999993747 to
  0.022462686567166656, a 0.000037313432827091 total reduction across the audit
  rather than a per-category reduction. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/sched_helpers.rs kernel/rust/process_helpers.rs, scoped git diff
  --check over kernel/process.c, kernel/rust/sched_helpers.rs,
  kernel/rust/process_helpers.rs, and kernel/rust/tests/run_equivalence.sh, full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.15i1V5, sched_helpers digest
  0180e531833aef22, process_helpers digest b753dfb393932c91,
  x86_memory_helpers digest 07f604cf98c6d336, and Rust global/unresolved symbol
  checks for check_need_resched, sched_init, and the new process_sched bridge
  callbacks. Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  mckernel.img -- -B -j4 and no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4 passed.
  Broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4 and final exact JSON audit passed; the final audit
  reports overall_scored_completion=99.97753731343283,
  total_percentage_points_left=0.022462686567166656, compiled_retired_c=0,
  compiled_non_allowlisted_c=29, stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.97% to 99.98% for the
  strict source-retirement audit. Rust-enabled builds now route the public
  release_cpuid() wrapper through the Rust export of the same ABI name, while
  preserving the no-Rust C fallback body outside the strict path. The Rust
  wrapper resolves the CPU-local pointer, takes the address of the existing
  runq_reservation_lock storage, reconstructs the scheduler runqueue offset
  table with Rust offset_of!(), and dispatches the already verified
  sched_release_cpuid_body_result() body with CPU_STATUS_IDLE plus the
  existing spin-lock and noirq lock bridge callbacks. Strict mckernel_rust.o
  now exports release_cpuid and references runq_reservation_lock, strict
  process.c.o no longer defines release_cpuid while providing
  runq_reservation_lock as common storage, and fallback process.c.o still
  exports the C release_cpuid definition. The row remains partial because
  kernel/process.c still compiles in the strict build graph for idle,
  schedule/runqueue/wakeup, process/thread lifecycle, locks, context switch,
  page-table/VM primitive callbacks, and no-Rust fallback bodies. The batch
  adds 0.01 row-percentage point, reducing exact total scored percentage
  points left from 0.02253731343283505 to 0.022499999999993747, a
  0.000037313432841303 total reduction across the audit rather than a
  per-category reduction. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/sched_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/sched_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.GCzc7Y, sched_helpers digest
  0180e531833aef22, process_helpers digest b753dfb393932c91,
  x86_memory_helpers digest 07f604cf98c6d336, unresolved-symbol count 381,
  and Rust global-symbol/unresolved-symbol checks for release_cpuid and
  runq_reservation_lock, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports release_cpuid and references
  runq_reservation_lock, strict process.c.o has no release_cpuid definition
  and provides runq_reservation_lock as a common symbol, fallback process.c.o
  exports release_cpuid, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final exact JSON audit. The final exact JSON
  audit reports overall_scored_completion=99.9775,
  total_percentage_points_left=0.022499999999993747,
  compiled_retired_c=0, compiled_non_allowlisted_c=29,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. The broad validation table rounds the
  exact nonzero total remainder to 0.0% and rounds the process.c debt example
  to 100.0%; the JSON audit and fixed-width tracker row are the authoritative
  progress values. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.96% to 99.97% for the
  strict source-retirement audit. Rust-enabled builds now route the public
  runq_del_thread() wrapper through the Rust export of the same ABI name,
  while preserving the no-Rust C fallback body outside the strict path. The
  Rust wrapper resolves the target CPU-local pointer, reconstructs the full
  scheduler runqueue offset table with Rust offset_of!(), and dispatches the
  already verified sched_runq_del_thread_body_result() body with
  CPU_STATUS_IDLE plus the existing spin-lock bridge callbacks. Strict
  mckernel_rust.o now exports runq_del_thread, strict process.c.o no longer
  defines that symbol, and fallback process.c.o still exports the C
  definition. The row remains partial because kernel/process.c still compiles
  in the strict build graph for idle, schedule/runqueue/wakeup, process/thread
  lifecycle, locks, context switch, page-table/VM primitive callbacks, and
  no-Rust fallback bodies. The batch adds 0.01 row-percentage point, reducing
  exact total scored percentage points left from 0.02257462686566214 to
  0.02253731343283505, a 0.00003731343282709 total reduction across the audit
  rather than a per-category reduction. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/sched_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/sched_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  tmpdir /tmp/mckernel-rust-equiv.ihWOaW, sched_helpers digest
  0180e531833aef22, process_helpers digest b753dfb393932c91,
  x86_memory_helpers digest 07f604cf98c6d336, and unresolved-symbol count
  380, Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports runq_del_thread, strict
  process.c.o has no runq_del_thread definition while retaining the scheduler
  bridge callbacks and runqueue offset table for remaining wrappers, fallback
  process.c.o exports runq_del_thread, broad Rocky Rust build-only validation
  through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4, and final exact JSON audit. The
  final exact JSON audit reports
  overall_scored_completion=99.97746268656716,
  total_percentage_points_left=0.02253731343283505,
  compiled_retired_c=0, compiled_non_allowlisted_c=29,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. The broad validation table rounds the
  exact nonzero total remainder to 0.0% and rounds the process.c debt example
  to 100.0%; the JSON audit and fixed-width tracker row are the authoritative
  progress values. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.95% to 99.96% for the
  strict source-retirement audit. Rust-enabled builds now route the idle-loop
  migration-completion gate through Rust-owned sched_do_migrate_public(),
  while preserving the no-Rust C fallback do_migrate() body outside the
  strict path. The Rust wrapper obtains the current CPU id, resolves the
  current CPU-local pointer through the existing bridge, reconstructs the
  SchedDoMigrateOffsets layout with Rust offset_of!(), and dispatches the
  already verified sched_do_migrate_body_result() body with the same lock,
  wakeup, vector, interrupt, CPU-local, and migration-log bridge callbacks.
  Strict process.c.o now references sched_do_migrate_public and exports only
  the scheduler bridge callbacks for this slice, while the strict C
  do_migrate() definition and C offset table are absent; the no-Rust fallback
  build keeps the local C path. The row remains partial because
  kernel/process.c still compiles in the strict build graph for idle,
  schedule/runqueue/wakeup, process/thread lifecycle, locks, context switch,
  page-table/VM primitive callbacks, and no-Rust fallback bodies. The batch
  adds 0.01 row-percentage point, reducing exact total scored percentage
  points left from 0.02261194029850344 to 0.02257462686566214, a
  0.00003731343284130 total reduction across the audit rather than a
  per-category reduction. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/sched_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/sched_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  sched_helpers digest 0180e531833aef22, process_helpers digest
  b753dfb393932c91, x86_memory_helpers digest 07f604cf98c6d336, and
  unresolved-symbol count 380 after correcting the transient harness
  expectation recorded in kernel.log, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports
  sched_do_migrate_public, strict process.c.o references that Rust export
  while defining the scheduler bridge callbacks and no do_migrate offset
  table, fallback process.c.o has no dependency on sched_do_migrate_public,
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final exact JSON audit. The final exact JSON
  audit reports overall_scored_completion=99.97742537313434,
  total_percentage_points_left=0.02257462686566214,
  compiled_retired_c=0, compiled_non_allowlisted_c=29,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. The broad validation table rounds the
  exact nonzero total remainder to 0.0% and rounds the process.c debt example
  to 100.0%; the JSON audit and fixed-width tracker row are the authoritative
  progress values. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/memory.c moved from 99.95% to
  99.99% for the strict source-retirement audit. Rust-enabled builds now
  route the init-page-table normal straight-map and Linux kernel mapping
  bridge callbacks through Rust-owned exports, while preserving the no-Rust C
  fallback static bodies outside the strict path and retaining memory-address
  lookup, memory-chunk lookup, command-line lookup, large-page mapping,
  logging, and panic side effects as C bridge callbacks. This retires the
  strict-path init_normal_area() and init_linux_kernel_mapping() static
  execution bodies and routes map-start/map-end lookup dispatch, straight-map
  virtual/physical iteration, full-map versus safe-kernel-map selection,
  memory-chunk iteration, bad/no-chunk handling, set-large dispatch, error-log
  selection, and panic dispatch decisions through Rust. The row remains
  partial because arch/x86_64/kernel/memory.c still compiles in the strict
  build graph for bridge callbacks, raw address translation primitive
  behavior, page-table primitive side effects, logging/panic side effects, and
  no-Rust fallback bodies. The batch adds 0.04 row-percentage point, reducing
  exact total scored percentage points left from 0.022761194029840226 to
  0.02261194029850344, a 0.000149253731336786 total reduction across the
  audit rather than a per-category reduction. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/x86_memory_helpers.rs, scoped git diff --check over
  arch/x86_64/kernel/memory.c, kernel/rust/x86_memory_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  x86_memory_helpers digest 07f604cf98c6d336 and unresolved-symbol count 375,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target mckernel.img
  -- -B -j4, no-Rust fallback cmake --build /tmp/mckernel-rocky-c-fallback
  --target mckernel.img -- -B -j4, and symbol checks showing strict
  mckernel_rust.o exports x86_init_normal_area_public and
  x86_init_linux_kernel_mapping_public, strict memory.c.o references those
  Rust exports while defining only the normal/Linux init bridge callbacks for
  this slice, and fallback memory.c.o has no dependency on those Rust exports,
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final exact JSON audit. The final exact JSON
  audit reports overall_scored_completion=99.9773880597015,
  total_percentage_points_left=0.02261194029850344,
  compiled_retired_c=0, compiled_non_allowlisted_c=29,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. The broad validation table rounds the
  exact nonzero total remainder to 0.0% and rounds the memory.c debt example
  to 100.0%; the JSON audit and fixed-width tracker row are the authoritative
  progress values. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/memory.c moved from 99.90% to
  99.95% for the strict source-retirement audit. Rust-enabled builds now
  route the init-page-table fixed-area and vDSO/vsyscall-area bridge callbacks
  through Rust-owned exports, while preserving the no-Rust C fallback static
  bodies outside the strict path and retaining fixed-virtual storage,
  vsyscall-page lookup, page-table set-page dispatch, virtual-to-physical
  translation, and panic side effects as C bridge callbacks. This retires the
  strict-path init_fixed_area() and init_vsyscall_area() static execution
  bodies and routes fixed cursor publication, vsyscall virtual-address
  selection, attribute selection, vsyscall physical-address lookup dispatch,
  set-page dispatch, error detection, and panic dispatch decisions through
  Rust. The row remains partial because arch/x86_64/kernel/memory.c still
  compiles in the strict build graph for allocation/free primitives,
  page-table primitive side effects, raw address translation primitive
  behavior, logging/panic bridge side effects, other partial memory.c bodies,
  and no-Rust fallback bodies. The batch adds 0.05 row-percentage point,
  reducing exact total scored percentage points left from
  0.022947761194018312 to 0.022761194029840226, a
  0.000186567164178086 total reduction across the audit rather than a
  per-category reduction. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/x86_memory_helpers.rs, scoped git diff --check over
  arch/x86_64/kernel/memory.c, kernel/rust/x86_memory_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  MCKERNEL_RUST_EQUIV_KEEP_TMP=1 kernel/rust/tests/run_equivalence.sh with
  x86_memory_helpers digest 07f604cf98c6d336 and unresolved-symbol count 367,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target mckernel.img
  -- -B -j4, no-Rust fallback cmake --build /tmp/mckernel-rocky-c-fallback
  --target mckernel.img -- -B -j4, and symbol checks showing strict
  mckernel_rust.o exports x86_init_fixed_area_public and
  x86_init_vsyscall_area_public, strict memory.c.o references those Rust
  exports while defining only the fixed/vsyscall bridge callbacks for this
  slice, and fallback memory.c.o has no dependency on those Rust exports,
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final exact JSON audit. The final exact JSON
  audit reports overall_scored_completion=99.97723880597016,
  total_percentage_points_left=0.022761194029840226,
  compiled_retired_c=0, compiled_non_allowlisted_c=29,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. The broad validation table rounds the
  exact nonzero total remainder to 0.0% and rounds the memory.c debt example
  to 100.0%; the JSON audit and fixed-width tracker row are the authoritative
  progress values. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/memory.c moved from 99.85% to
  99.90% for the strict source-retirement audit. Rust-enabled builds now
  export init_text_area() and init_low_area() directly from the Rust x86
  memory helper object while preserving the no-Rust C fallback definitions
  outside the strict path and retaining large-page mapping, MAP_KERNEL_START
  lookup, kernel-end lookup, debug-log, and panic side effects as C bridge
  callbacks. This retires the strict-path text/low-area public wrappers and
  routes text physical/page-span calculation, large-page text mapping, noexec
  low-memory mapping, error selection, and panic dispatch decisions through
  Rust. The row remains partial because arch/x86_64/kernel/memory.c still
  compiles in the strict build graph for allocation/free primitives,
  page-table primitive side effects, raw address translation primitive
  behavior, logging/panic bridge side effects, other partial memory.c bodies,
  and no-Rust fallback bodies. The batch adds 0.05 row-percentage point,
  reducing exact total scored percentage points left from
  0.02313432835821061 to 0.022947761194018312, a
  0.000186567164192298 total reduction across the audit rather than a
  per-category reduction. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/x86_memory_helpers.rs, scoped git diff --check over
  arch/x86_64/kernel/memory.c, kernel/rust/x86_memory_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, rust-source-retirement.txt, and
  kernel.log, full MCKERNEL_RUST_EQUIV_KEEP_TMP=1
  kernel/rust/tests/run_equivalence.sh with x86_memory_helpers digest
  07f604cf98c6d336 and unresolved-symbol count 364, Rust-enabled cmake
  --build /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4,
  no-Rust fallback cmake --build /tmp/mckernel-rocky-c-fallback --target
  mckernel.img -- -B -j4, and symbol checks showing strict mckernel_rust.o
  exports init_text_area and init_low_area while strict memory.c.o defines
  only the text/low-area bridge callbacks for this slice and fallback
  memory.c.o still exports the C wrapper definitions, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final exact JSON
  audit. The final exact JSON audit reports
  overall_scored_completion=99.97705223880598,
  total_percentage_points_left=0.022947761194018312,
  compiled_retired_c=0, compiled_non_allowlisted_c=29,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. The broad validation table still rounds the
  exact nonzero total remainder to 0.0%; the JSON audit above is the
  authoritative progress value. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/memory.c moved from 99.80% to
  99.85% for the strict source-retirement audit. Rust-enabled builds now
  export map_fixed_area() directly from the Rust x86 memory helper object
  while preserving the no-Rust C fallback definition outside the strict path
  and retaining init-page-table lookup, fixed-virtual cursor mutation,
  page-table set-page dispatch, TLB flush, and debug-log side effects as C
  bridge callbacks. This retires the strict-path fixed-area public wrapper and
  routes physical/page-offset calculation, page-span calculation, uncachable
  attribute selection, fixed-area page mapping loop, fixed cursor publication,
  TLB flush dispatch, and mapped virtual-address return shaping through Rust.
  The row remains partial because arch/x86_64/kernel/memory.c still compiles
  in the strict build graph for allocation/free primitives, page-table
  primitive side effects, raw address translation primitive behavior,
  logging/panic bridge side effects, other partial memory.c bodies, and
  no-Rust fallback bodies. The batch adds 0.05 row-percentage point, reducing
  exact total scored percentage points left from 0.023320895522388696 to
  0.02313432835821061, a 0.000186567164178086 total reduction across the
  audit rather than a per-category reduction. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/x86_memory_helpers.rs after the rustfmt wrapping failure
  recorded in kernel.log, scoped git diff --check over
  arch/x86_64/kernel/memory.c, kernel/rust/x86_memory_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, rust-source-retirement.txt, and
  kernel.log, full MCKERNEL_RUST_EQUIV_KEEP_TMP=1
  kernel/rust/tests/run_equivalence.sh with x86_memory_helpers digest
  07f604cf98c6d336 and unresolved-symbol count 358, Rust-enabled cmake
  --build /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4,
  no-Rust fallback cmake --build /tmp/mckernel-rocky-c-fallback --target
  mckernel.img -- -B -j4, and symbol checks showing strict mckernel_rust.o
  exports map_fixed_area while strict memory.c.o defines only the fixed-area
  bridge callbacks for this slice and fallback memory.c.o still exports the C
  wrapper definition, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final exact JSON audit. The final exact JSON
  audit reports overall_scored_completion=99.97686567164179,
  total_percentage_points_left=0.02313432835821061,
  compiled_retired_c=0, compiled_non_allowlisted_c=29,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. The broad validation table still rounds the
  exact nonzero total remainder to 0.0%; the JSON audit above is the
  authoritative progress value. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/memory.c moved from 99.75% to
  99.80% for the strict source-retirement audit. Rust-enabled builds now
  export init_page_table() directly from the Rust x86 memory helper object
  while preserving the no-Rust C fallback definition outside the strict path
  and retaining page-table slots, spinlock state, page-size publication,
  allocation, page-size validation, mapping, load, log, and panic side effects
  as C bridge callbacks. This retires the strict-path init-page-table public
  wrapper and routes initial page-size validation, page-table allocation,
  spinlock initialization, normal/Linux/fixed/text/vsyscall/low mapping
  callback ordering, page-table load dispatch, and error panic selection
  through Rust. The row remains partial because arch/x86_64/kernel/memory.c
  still compiles in the strict build graph for allocation/free primitives,
  page-table primitive side effects, raw address translation primitive
  behavior, logging/panic bridge side effects, other partial memory.c bodies,
  and no-Rust fallback bodies. The batch adds 0.05 row-percentage point,
  reducing exact total scored percentage points left from
  0.023507462686566782 to 0.023320895522388696, a
  0.000186567164178086 total reduction across the audit rather than a
  per-category reduction. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/x86_memory_helpers.rs, scoped git diff --check over
  arch/x86_64/kernel/memory.c, kernel/rust/x86_memory_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, rust-source-retirement.txt, and
  kernel.log after correcting the focused equivalence compile failure recorded
  above, full MCKERNEL_RUST_EQUIV_KEEP_TMP=1
  kernel/rust/tests/run_equivalence.sh with x86_memory_helpers digest
  07f604cf98c6d336 and unresolved-symbol count 354, Rust-enabled cmake
  --build /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4,
  no-Rust fallback cmake --build /tmp/mckernel-rocky-c-fallback --target
  mckernel.img -- -B -j4, and symbol checks showing strict mckernel_rust.o
  exports init_page_table while strict memory.c.o defines only the
  init-page-table bridges for this slice and fallback memory.c.o still exports
  the C wrapper definition, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final exact JSON audit. The final exact JSON
  audit reports overall_scored_completion=99.97667910447761,
  total_percentage_points_left=0.023320895522388696,
  compiled_retired_c=0, compiled_non_allowlisted_c=29,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. The broad validation table still rounds the
  exact nonzero total remainder to 0.0%; the JSON audit above is the
  authoritative progress value. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/memory.c moved from 99.70% to
  99.75% for the strict source-retirement audit. Rust-enabled builds now
  export early_alloc_pages(), early_alloc_invalidate(), and
  get_last_early_heap() directly from the Rust x86 memory helper object while
  preserving the no-Rust C fallback definitions outside the strict path and
  retaining last-page storage, end/bootstrap memory address publication,
  address translation, panic side effects, and the equivalence invalidation
  hook as C bridge callbacks. This retires the strict-path early allocation
  public wrappers and routes first-use alignment, bootstrap bound selection,
  finalized/out-of-memory panic selection, last-page return and next-page
  publication, early allocator invalidation, and last-heap publication through
  Rust. The row remains partial because arch/x86_64/kernel/memory.c still
  compiles in the strict build graph for allocation/free primitives,
  page-table primitive side effects, raw address translation primitive
  behavior, logging/panic bridge side effects, other partial memory.c bodies,
  and no-Rust fallback bodies. The batch adds 0.05 row-percentage point,
  reducing exact total scored percentage points left from
  0.02369402985074487 to 0.023507462686566782, a 0.000186567164178085
  total reduction across the audit rather than a per-category reduction.
  Validation passed for bash -n kernel/rust/tests/run_equivalence.sh,
  rustfmt --edition 2021 --check kernel/rust/x86_memory_helpers.rs, scoped
  git diff --check over arch/x86_64/kernel/memory.c,
  kernel/rust/x86_memory_helpers.rs, kernel/rust/tests/run_equivalence.sh,
  and kernel.log after correcting the mem_init_helpers_rust fixture failure
  recorded above, full kernel/rust/tests/run_equivalence.sh with
  x86_memory_helpers digest 07f604cf98c6d336 and unresolved-symbol count
  337, Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, and
  symbol checks showing strict mckernel_rust.o exports early_alloc_pages,
  early_alloc_invalidate, and get_last_early_heap while strict memory.c.o
  defines only the early-allocation bridges for this slice and fallback
  memory.c.o still exports the C wrapper definitions, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final exact JSON
  audit. The final exact JSON audit reports
  overall_scored_completion=99.97649253731343,
  total_percentage_points_left=0.023507462686566782,
  compiled_retired_c=0, compiled_non_allowlisted_c=29,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. The broad validation table still rounds the
  exact nonzero total remainder to 0.0%; the JSON audit above is the
  authoritative progress value. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: arch/x86_64/kernel/memory.c moved from 99.65% to
  99.70% for the strict source-retirement audit. Rust-enabled builds now
  export ihk_mc_allocate() and ihk_mc_free() directly from the Rust x86
  memory helper object while preserving the no-Rust C fallback definitions
  outside the strict path and retaining kmalloc initialization lookup,
  kmalloc_tracked(), kfree_tracked(), and legacy log side effects as C
  bridge callbacks. This retires the strict-path public allocation/free
  wrappers and routes kmalloc-initialized selection, forced AP_NOWAIT
  publication, allocation callback dispatch, free callback dispatch, and
  no-op null-free return shaping through Rust. The row remains partial
  because arch/x86_64/kernel/memory.c still compiles in the strict build
  graph for allocation/free primitives, page-table primitive side effects,
  raw address translation primitive behavior, logging/panic bridge side
  effects, other partial memory.c bodies, and no-Rust fallback bodies. The
  batch adds 0.05 row-percentage point, reducing exact total scored
  percentage points left from 0.023880597014922955 to
  0.02369402985074487, a 0.000186567164178085 total reduction across the
  audit rather than a per-category reduction. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/x86_memory_helpers.rs, scoped git diff --check over
  arch/x86_64/kernel/memory.c, kernel/rust/x86_memory_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, rust-source-retirement.txt, and
  kernel.log, full kernel/rust/tests/run_equivalence.sh with
  x86_memory_helpers digest 07f604cf98c6d336 and unresolved-symbol count
  333, Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports ihk_mc_allocate and
  ihk_mc_free, strict memory.c.o defines only the kmalloc/kfree/log bridges
  for this slice without defining those public wrappers, fallback memory.c.o
  still exports the C wrapper definitions, broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final exact JSON
  audit. The final exact JSON audit reports
  overall_scored_completion=99.97630597014926,
  total_percentage_points_left=0.02369402985074487,
  compiled_retired_c=0, compiled_non_allowlisted_c=29,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. The broad validation table still rounds
  the exact nonzero total remainder to 0.0%; the JSON audit above is the
  authoritative progress value. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.9% to 99.95% for the
  strict source-retirement audit. Rust-enabled builds now export
  populate_process_memory() directly from the Rust process helper object while
  preserving the no-Rust C fallback definition outside the strict path and
  retaining the warning log as a C bridge callback. This retires the strict-path
  populate public wrapper and routes current thread/VM selection, populate
  reason construction, preempt disable/enable ordering, per-page fault
  dispatch through the Rust page-fault public helper, first-error warning
  dispatch, and return shaping through Rust. The row remains partial because
  kernel/process.c still compiles in the strict build graph for clone/debug,
  process lifetime, allocation/free primitive side effects, deeper page-fault
  primitive behavior, memobj primitive behavior, XPMEM primitive behavior,
  page-table primitive side effects, scheduler callback side effects, and
  no-Rust fallback bodies. The batch adds 0.05 row-percentage point, reducing
  exact total scored percentage points left by about 0.00019 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, rust-source-retirement.txt, and
  kernel.log, full kernel/rust/tests/run_equivalence.sh with process_helpers
  digest b753dfb393932c91 and unresolved-symbol count 324, Rust-enabled cmake
  --build /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust
  fallback cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img
  -- -B -j4, symbol checks showing strict mckernel_rust.o exports
  populate_process_memory while strict process.c.o defines only the populate
  warning bridge and fallback process.c.o still exports the C definition, and
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. The final exact JSON audit reports
  overall_scored_completion=99.97555970149254,
  total_percentage_points_left=0.024440298507457214, compiled_retired_c=0,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.8% to 99.9% for the
  strict source-retirement audit. Rust-enabled builds now export
  page_fault_process_vm() directly from the Rust process helper object while
  preserving the no-Rust C fallback definition outside the strict path and
  retaining normal/XPMEM page-fault range dispatch, read/write lock primitive
  callbacks, zero-object matching, current-thread/page-I/O primitive side
  effects, preempt function calls, and page-I/O dispatch as C bridge callbacks.
  This retires the strict-path page-fault public wrapper and routes current
  thread/VM selection, CPU-id publication, lock callback selection, zero-object
  policy callback selection, normal versus XPMEM dispatch selection,
  preempt-enable/disable retry sequencing, page-I/O callback dispatch, and
  return shaping through Rust. The row remains partial because
  kernel/process.c still compiles in the strict build graph for clone/debug,
  process lifetime, allocation/free primitive side effects, deeper page-fault
  primitive behavior, memobj primitive behavior, XPMEM primitive behavior,
  page-table primitive side effects, scheduler callback side effects, and
  no-Rust fallback bodies. The batch adds 0.1 row-percentage point, reducing
  exact total scored percentage points left by about 0.00037 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh after correcting the recorded missing
  Rust extern declarations and unresolved-symbol expectation, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91 and unresolved-symbol count 323, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports
  page_fault_process_vm while strict process.c.o defines only the page-fault
  bridge callbacks and fallback process.c.o still exports the C definition, and
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. The final exact JSON audit reports
  overall_scored_completion=99.97537313432835,
  total_percentage_points_left=0.02462686567164951, compiled_retired_c=0,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.7% to 99.8% for the
  strict source-retirement audit. Rust-enabled builds now export
  split_process_memory_range() directly from the Rust process helper object
  while preserving the no-Rust C fallback definition outside the strict path
  and retaining page-table split, SHM page update, allocation, tree insertion,
  legacy logging, page pgshift offset lookup, and optional Tofu split side
  effects as C bridge callbacks. This retires the strict-path split-range
  public wrapper and routes start logging, page-table split dispatch, SHM
  lookup/update sequencing, range allocation/init, optional Tofu list
  init/split dispatch, publish/insert dispatch, success logging, and return
  shaping through Rust. The row remains partial because kernel/process.c still
  compiles in the strict build graph for clone/debug, process lifetime,
  allocation/free primitive side effects, page-fault, memobj primitive
  behavior, XPMEM primitive behavior, page-table primitive side effects,
  scheduler callback side effects, and no-Rust fallback bodies. The batch adds
  0.1 row-percentage point, reducing exact total scored percentage points left
  by about 0.00037 overall, which is global total-left movement rather than
  per-category movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over kernel/process.c,
  kernel/rust/process_helpers.rs, and kernel/rust/tests/run_equivalence.sh,
  full kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91 and unresolved-symbol count 318, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports
  split_process_memory_range while strict process.c.o defines only the split
  bridge callbacks and fallback process.c.o still exports the C definition, and
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. The final exact JSON audit reports
  overall_scored_completion=99.975,
  total_percentage_points_left=0.025000000000005684, compiled_retired_c=0,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.6% to 99.7% for the
  strict source-retirement audit. Rust-enabled builds now export
  join_process_memory_range() directly from the Rust process helper object
  while preserving the no-Rust C fallback definition outside the strict path
  and retaining range free, legacy logging, and optional Tofu range-split
  side effects as C bridge callbacks. This retires the strict-path join-range
  public wrapper and routes start/done logging, range-cache pointer selection,
  VM range-tree dispatch, adjacency/object-offset validation, surviving-end
  commit, optional Tofu dispatch, merge-range free dispatch, and return shaping
  through Rust. The row remains partial because kernel/process.c still compiles
  in the strict build graph for clone/debug, process lifetime, allocation/free
  primitive side effects, page-fault, memobj primitive behavior, XPMEM primitive
  behavior, page-table primitive side effects, scheduler callback side effects,
  and no-Rust fallback bodies. The batch adds 0.1 row-percentage point,
  reducing exact total scored percentage points left by about 0.00037 overall,
  which is global total-left movement rather than per-category movement.
  Validation passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt
  --edition 2021 --check kernel/rust/process_helpers.rs after correcting the
  recorded formatting issue, scoped git diff --check over kernel/process.c,
  kernel/rust/process_helpers.rs, kernel/rust/tests/run_equivalence.sh, and
  kernel.log after correcting the recorded Rust ABI pointer-cast issue, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91 and unresolved-symbol count 307, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports
  join_process_memory_range while strict process.c.o defines only the join/free
  bridge callbacks and fallback process.c.o still exports the C definition, and
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. The final exact JSON audit reports
  overall_scored_completion=99.97462686567164,
  total_percentage_points_left=0.02537313432836186, compiled_retired_c=0,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.4% to 99.6% for the
  strict source-retirement audit. Rust-enabled builds now export
  add_process_memory_range() directly from the Rust process helper object while
  preserving the no-Rust C fallback definition outside the strict path and
  retaining allocation/free, VM range insertion, page-table update,
  remove-range cleanup, XPMEM marking, physical memclear, and legacy logging
  side effects as C bridge callbacks. This retires the strict-path add-range
  public wrapper and routes user-bound validation, range allocation/setup,
  insertion failure cleanup, mapping-action selection, update/remove cleanup,
  demand logging, optional memclear dispatch, returned-range publication, and
  return shaping through Rust. The row remains partial because kernel/process.c
  still compiles in the strict build graph for clone/debug, process lifetime,
  allocation/free primitive side effects, page-fault, memobj primitive behavior,
  XPMEM primitive behavior, page-table primitive side effects, scheduler
  callback side effects, and no-Rust fallback bodies. The batch adds 0.2
  row-percentage point, reducing exact total scored percentage points left by
  about 0.00075 overall, which is global total-left movement rather than
  per-category movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over kernel/process.c,
  kernel/rust/process_helpers.rs, kernel/rust/tests/run_equivalence.sh,
  kernel.log, and rust-source-retirement.txt, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91 and unresolved-symbol count 305, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports
  add_process_memory_range while strict process.c.o defines only the add-range
  bridge callbacks and fallback process.c.o still exports the C definition, and
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. The final exact JSON audit reports
  overall_scored_completion=99.97425373134328,
  total_percentage_points_left=0.02574626865671803, compiled_retired_c=0,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.3% to 99.4% for the
  strict source-retirement audit. Rust-enabled builds now export
  update_process_page_table() directly from the Rust process helper object
  while preserving the no-Rust C fallback definition outside the strict path
  and retaining page-table attribute conversion, page-table set-range, spinlock,
  and legacy logging side effects as C bridge callbacks. This retires the
  strict-path update page-table public wrapper and routes attribute selection,
  VM page-table lock ordering, set-range dispatch, error-log selection, and
  return shaping through Rust. The row remains partial because kernel/process.c
  still compiles in the strict build graph for clone/debug, process lifetime,
  allocation/free primitive side effects, page-fault, memobj primitive behavior,
  XPMEM primitive behavior, page-table primitive side effects, scheduler
  callback side effects, and no-Rust fallback bodies. The batch adds 0.1
  row-percentage point, reducing exact total scored percentage points left by
  about 0.00037 overall, which is global total-left movement rather than
  per-category movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over kernel/process.c,
  kernel/rust/process_helpers.rs, kernel/rust/tests/run_equivalence.sh,
  kernel.log, and rust-source-retirement.txt, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91 and unresolved-symbol count 298, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports
  update_process_page_table while strict process.c.o defines only the update
  page-table bridge callbacks and fallback process.c.o still exports the C
  definition, and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. The final exact JSON audit reports
  overall_scored_completion=99.97350746268656,
  total_percentage_points_left=0.026492537313444586, compiled_retired_c=0,
  stale_tracker_rows=[], untracked_files=[], and
  headers_with_executable_logic=0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 99.1% to 99.3% for the
  strict source-retirement audit. Rust-enabled builds now export
  invalidate_process_memory_range() and invalidate_one_page() directly from
  the Rust process helper object while preserving the no-Rust C fallback
  definitions outside the strict path and retaining page-table lookup, PTE
  classification, contiguous-page splitting, page-table free, page unmap,
  TLB flush, memobj invalidation, panic, and legacy logging side effects as C
  bridge callbacks. This retires the strict-path invalidate memory-range public
  wrapper plus its visit-PTE callback trampoline and routes range memobj
  reference ordering, start/end contiguous split checks, SHM page-table free
  selection, visit-PTE dispatch, one-page fileoff replacement, TLB flush,
  memobj invalidate dispatch, panic-on-unmap-failure selection, and error log
  dispatch through Rust. The row remains partial because kernel/process.c
  still compiles in the strict build graph for clone/debug, process lifetime,
  allocation/free primitive side effects, page-fault, memobj primitive
  behavior, XPMEM primitive behavior, page-table primitive side effects,
  scheduler callback side effects, and no-Rust fallback bodies. The batch adds
  0.2 row-percentage point, reducing exact total scored percentage points left
  by about 0.00075 overall, which is global total-left movement rather than
  per-category movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, kernel.log, and
  rust-source-retirement.txt, full kernel/rust/tests/run_equivalence.sh with
  process_helpers digest b753dfb393932c91 and unresolved-symbol count 295,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target mckernel.img
  -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports invalidate_process_memory_range
  and invalidate_one_page while strict process.c.o defines only the invalidation
  bridge callbacks and fallback process.c.o still exports the C definitions,
  and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 98.9% to 99.1% for the
  strict source-retirement audit. Rust-enabled builds now export
  remap_process_memory_range() and sync_process_memory_range() directly from
  the Rust process helper object while preserving the no-Rust C fallback
  definitions outside the strict path and retaining page-table visitor,
  remap-one-page, sync-one-page, noirq page-table lock/unlock, and legacy
  logging side effects as C bridge callbacks. This retires the strict-path
  remap/sync memory-range public wrapper definitions and routes memobj
  argument shaping, visitor argument setup, visit-PTE range dispatch,
  off/start publication, error return shaping, and final log dispatch through
  Rust. The row remains partial because kernel/process.c still compiles in the
  strict build graph for clone/debug, process lifetime, allocation/free
  primitive side effects, page-fault, memobj, XPMEM primitive behavior,
  page-table primitive side effects, scheduler callback side effects, and
  no-Rust fallback bodies. The batch adds 0.2 row-percentage point, reducing
  exact total scored percentage points left by about 0.00075 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs after correcting the formatting
  failure recorded in kernel.log, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, and kernel.log, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91 and unresolved-symbol count 274, Rust-enabled cmake
  --build /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust
  fallback cmake --build /tmp/mckernel-rocky-c-fallback --target
  mckernel.img -- -B -j4, symbol checks showing strict mckernel_rust.o
  exports remap_process_memory_range and sync_process_memory_range while
  strict process.c.o defines only the visitor/log/page bridge callbacks and
  fallback process.c.o still exports the C definitions, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 98.8% to 98.9% for the
  strict source-retirement audit. Rust-enabled builds now export
  remove_process_memory_range() directly from the Rust process helper object
  while preserving the no-Rust C fallback definition outside the strict path
  and retaining range splitting, XPMEM range removal, memory-range free
  dispatch, and legacy logging as C bridge callbacks. This retires the
  strict-path remove-memory-range public wrapper definition and routes
  straight-map conversion, range iteration, split dispatch, read-only-free
  publication, optional XPMEM removal dispatch, per-range free dispatch, and
  final log dispatch through Rust. The row remains partial because
  kernel/process.c still compiles in the strict build graph for clone/debug,
  process lifetime, allocation/free primitive side effects, page-fault, memobj,
  XPMEM primitive behavior, page-table primitive side effects, scheduler
  callback side effects, and no-Rust fallback bodies. The batch adds 0.1
  row-percentage point, reducing exact total scored percentage points left by
  about 0.00037 overall, which is global total-left movement rather than
  per-category movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, and kernel.log after correcting the
  ProcessVm.proc cast failure recorded there, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports remove_process_memory_range
  while strict process.c.o defines only the remove-range bridge callbacks and
  fallback process.c.o still exports the C definition, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 98.7% to 98.8% for the
  strict source-retirement audit. Rust-enabled builds now export
  ptrace_traceme() directly from the Rust process helper object while
  preserving the no-Rust C fallback definition outside the strict path and
  retaining noirq MCS writer lock/unlock, debug-register allocation,
  single-step cleanup, thread hold, and legacy ptrace debug logging as C bridge
  callbacks. This retires the strict-path ptrace-traceme public wrapper
  definition and routes current thread/process/resource lookup, offset
  publication, ptrace-traced and pid1 rejection, parent child/report-list lock
  sequencing, ptrace flag publication, debug-register allocation dispatch,
  single-step cleanup dispatch, hold-thread dispatch, and log dispatch through
  Rust. The row remains partial because kernel/process.c still compiles in the
  strict build graph for clone/debug, process lifetime, allocation/free
  primitive side effects, page-fault, memobj, XPMEM, page-table primitive side
  effects, scheduler callback side effects, and no-Rust fallback bodies. The
  batch adds 0.1 row-percentage point, reducing exact total scored percentage
  points left by about 0.00037 overall, which is global total-left movement
  rather than per-category movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full kernel/rust/tests/run_equivalence.sh
  with process_helpers digest b753dfb393932c91, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports ptrace_traceme
  while strict process.c.o defines only the ptrace-traceme bridge callbacks and
  fallback process.c.o still exports the C definition, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 98.6% to 98.7% for the
  strict source-retirement audit. Rust-enabled builds now export
  remove_process_region() directly from the Rust process helper object while
  preserving the no-Rust C fallback definition outside the strict path and
  retaining page-table clear, page-table lock/unlock, and legacy debug logging
  as C bridge callbacks. This retires the strict-path remove-region public
  wrapper definition and routes alignment validation, address-space validation,
  page-table lock/unlock callback sequencing, clear callback dispatch, and
  final log dispatch through Rust. The row remains partial because
  kernel/process.c still compiles in the strict build graph for clone/debug,
  process lifetime, allocation/free primitive side effects, page-fault, memobj,
  XPMEM, page-table primitive side effects, scheduler callback side effects,
  and no-Rust fallback bodies. The batch adds 0.1 row-percentage points,
  reducing exact total scored percentage points left by about 0.00037 overall,
  which is global total-left movement rather than per-category movement.
  Validation passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt
  --edition 2021 --check kernel/rust/process_helpers.rs, scoped git diff
  --check over kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full kernel/rust/tests/run_equivalence.sh
  with process_helpers digest b753dfb393932c91, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports
  remove_process_region while strict process.c.o defines only the
  remove-region bridge callbacks and fallback process.c.o still exports the C
  definition, and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 98.5% to 98.6% for the
  strict source-retirement audit. Rust-enabled builds now export access_ok()
  directly from the Rust process helper object while preserving the no-Rust C
  fallback definition outside the strict path and retaining the refusal log as
  a C bridge callback. This retires the strict-path access-check public wrapper
  definition and routes VM/range validation, adjacent-range traversal,
  permission validation, error return shaping, and refusal-log dispatch
  through Rust. The row remains partial because kernel/process.c still compiles
  in the strict build graph for clone/debug, process lifetime, locking
  primitive side effects, allocation/free primitive side effects, page-fault,
  memobj, XPMEM, page-table primitive side effects, scheduler callback side
  effects, and no-Rust fallback bodies. The batch adds 0.1 row-percentage
  points, reducing exact total scored percentage points left by about 0.00037
  overall, which is global total-left movement rather than per-category
  movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs after fixing the wrapping failure recorded in
  kernel.log, scoped git diff --check over kernel/process.c,
  kernel/rust/process_helpers.rs, kernel/rust/tests/run_equivalence.sh, and
  kernel.log, full kernel/rust/tests/run_equivalence.sh with process_helpers
  digest b753dfb393932c91, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports access_ok while
  strict process.c.o defines only process_access_ok_log_bridge and fallback
  process.c.o still exports the C definition, and broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 98.4% to 98.5% for the
  strict source-retirement audit. Rust-enabled builds now export
  memset_smp_handler() and memset_smp() directly from the Rust process helper
  object while preserving the no-Rust C fallback definitions outside the strict
  path and retaining physical/virtual translation, raw memset, debug logging,
  and smp_call_func side effects as C bridge callbacks. This retires the
  strict-path SMP memset public wrapper definitions and routes request-payload
  publication, handler callback selection, per-CPU chunk calculation,
  tail-chunk adjustment, zero-small-length CPU0 selection, and callback return
  shaping through Rust. The row remains partial because kernel/process.c still
  compiles in the strict build graph for clone/debug, process lifetime, locking
  primitive side effects, allocation/free primitive side effects, page-fault,
  memobj, XPMEM, page-table primitive side effects, scheduler callback side
  effects, and no-Rust fallback bodies. The batch adds 0.1 row-percentage
  points, reducing exact total scored percentage points left by about 0.00037
  overall, which is global total-left movement rather than per-category
  movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over kernel/process.c,
  kernel/rust/process_helpers.rs, and kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports memset_smp and
  memset_smp_handler while strict process.c.o defines only the SMP memset
  bridge callbacks and fallback process.c.o still exports the C definitions,
  and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 98.0% to 98.4% for the
  strict source-retirement audit. Rust-enabled builds now export
  create_thread() directly from the Rust process helper object while
  preserving the no-Rust C fallback definition outside the strict path and
  retaining page allocation, process/VM allocation, address-space lifecycle,
  process/VM init, user-context init, CPU-selection logging, lock init,
  spinlock, optional-free, and thread-page free side effects as C bridge
  callbacks. This retires the strict-path create-thread public wrapper
  definition and routes requested CPU-set bit sizing, kernel-stack sizing,
  parent pid1 selection, thread/process/VM allocation sequencing, address-space
  creation and cleanup ordering, process initialization, CPU-mask publication,
  scheduler-default publication, sigcommon allocation/init, sigpending and
  sigstack initialization, user-context init callback dispatch, process/VM/thread
  link publication, address-space CPU-set update, spin-sleep initialization,
  and failure cleanup selection through Rust. The row remains partial because
  kernel/process.c still compiles in the strict build graph for clone/debug,
  process lifetime, locking primitive side effects, allocation/free primitive
  side effects, page-fault, memobj, XPMEM, page-table primitive side effects,
  scheduler callback side effects, and no-Rust fallback bodies. The batch adds
  0.4 row-percentage points, reducing exact total scored percentage points left
  by about 0.00149 overall, which is global total-left movement rather than
  per-category movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over kernel/process.c,
  kernel/rust/process_helpers.rs, kernel/rust/tests/run_equivalence.sh, and
  kernel.log, full kernel/rust/tests/run_equivalence.sh with process_helpers
  digest b753dfb393932c91, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4,
  symbol checks showing strict mckernel_rust.o exports create_thread while
  strict process.c.o defines only the create-thread bridge callbacks and
  fallback process.c.o still exports the C definition, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. The earlier
  unresolved-symbol harness failure for this wrapper is recorded in kernel.log
  and corrected. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 97.7% to 98.0% for the
  strict source-retirement audit. Rust-enabled builds now export
  destroy_thread() directly from the Rust process helper object while
  preserving the no-Rust C fallback definition outside the strict path and
  retaining hash-detach, time-accounting, TID release/replace, sigcommon
  release, fp-register release, optional free, and thread-page free side
  effects as C bridge callbacks. This retires the strict-path thread-destroy
  public wrapper definition and routes process/VM/address-space extraction,
  threads-lock ordering, thread-list detach dispatch, TID table cleanup,
  address-space CPU-set clear dispatch, UTI refill-TID clearing, sigpending
  drain/free dispatch, ptrace/debug/fp/coredump cleanup selection, sigcommon
  release dispatch, main-thread page-free dispatch, and return shaping through
  Rust. The row remains partial because kernel/process.c still compiles in the
  strict build graph for process lifetime, locking primitive side effects,
  allocation/free primitive side effects, page-fault, memobj, XPMEM,
  page-table primitive side effects, callback side effects, and no-Rust
  fallback bodies. The batch adds 0.3 row-percentage points, reducing exact
  total scored percentage points left by about 0.00112 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports destroy_thread while strict
  process.c.o defines only the destroy-thread bridge callbacks and fallback
  process.c.o still exports the C definition, and broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 97.5% to 97.7% for the
  strict source-retirement audit. Rust-enabled builds now export
  chain_process() and chain_thread() directly from the Rust process helper
  object while preserving the no-Rust C fallback definitions outside the strict
  path and retaining MCS writer-lock/unlock side effects as C bridge
  callbacks. This retires two strict-path process/thread chaining public
  wrapper definitions and routes current-resource-set lookup, process/thread
  hash selection, parent/VM extraction, sibling/thread-list insertion dispatch,
  hash-list insertion dispatch, and VM refcount increment dispatch through
  Rust. The row remains partial because kernel/process.c still compiles in the
  strict build graph for process lifetime, locking primitive side effects,
  allocation/free primitive side effects, page-fault, memobj, XPMEM,
  page-table primitive side effects, callback side effects, and no-Rust
  fallback bodies. The batch adds 0.2 row-percentage points, reducing exact
  total scored percentage points left by about 0.00075 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports chain_process and chain_thread
  while strict process.c.o defines only the writer-lock bridge callbacks and
  fallback process.c.o still exports the C definitions, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 97.3% to 97.5% for the
  strict source-retirement audit. Rust-enabled builds now export
  find_thread(), find_process(), and process_unlock() directly from the Rust
  process helper object while preserving the no-Rust C fallback definitions
  outside the strict path and retaining MCS reader-lock/unlock plus thread-hold
  side effects as C bridge callbacks. This retires three strict-path lookup and
  unlock public wrapper definitions and routes current-resource-set lookup,
  process/thread hash selection, null/bounds guards, stack lock-node setup,
  hash-list traversal dispatch, PID/TID matching, held-thread dispatch, and
  found-process unlock sequencing through Rust. The row remains partial
  because kernel/process.c still compiles in the strict build graph for process
  lifetime, locking primitive side effects, allocation/free primitive side
  effects, page-fault, memobj, XPMEM, page-table primitive side effects,
  callback side effects, and no-Rust fallback bodies. The batch adds 0.2
  row-percentage points, reducing exact total scored percentage points left by
  about 0.00075 overall, which is global total-left movement rather than
  per-category movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports find_thread, find_process, and
  process_unlock while strict process.c.o defines only the bridge callbacks and
  fallback process.c.o still exports the C definitions, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 97.2% to 97.3% for the
  strict source-retirement audit. Rust-enabled builds now export
  hold_sigcommon() and release_sigcommon() directly from the Rust process
  helper object while preserving the no-Rust C fallback definitions outside
  the strict path and retaining the sigpending free side effect as a C bridge
  callback. This retires the strict-path sigcommon hold/release public wrapper
  definitions and routes sigcommon refcount hold, release/refcount-test,
  sigpending list-empty detection, pending-signal drain/free traversal, and
  final sigcommon free dispatch through Rust. The row remains partial because
  kernel/process.c still compiles in the strict build graph for process
  lifetime, locking, allocation/free primitive side effects, page-fault,
  memobj, XPMEM, page-table primitive side effects, callback side effects, and
  no-Rust fallback bodies. The batch adds 0.1 row-percentage points, reducing
  exact total scored percentage points left by about 0.00037 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports hold_sigcommon and
  release_sigcommon, strict process.c.o references release_sigcommon through
  the destroy-thread bridge without defining the wrappers, fallback process.c.o
  still exports the C definitions, and broad Rocky Rust build-only validation
  through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 97.0% to 97.2% for the
  strict source-retirement audit. Rust-enabled builds now export
  release_process() directly from the Rust process helper object while
  preserving the no-Rust C fallback definition outside the strict path and
  retaining resource-set lookup, process detach, profile, thread page-free,
  mckfd free, process free, and final cleanup side effects as C bridge
  callbacks. This retires the strict-path release_process public wrapper
  definition and routes the process refcount gate, resource-set lookup,
  process hash detach, sibling detach, TID table cleanup, main-thread page
  cleanup, mckfd cleanup, profile dispatch, process free, and final cleanup
  sequencing through Rust. The row remains partial because kernel/process.c
  still compiles in the strict build graph for process lifetime, locking,
  allocation/free primitive side effects, page-fault, memobj, XPMEM,
  page-table primitive side effects, callback side effects, and no-Rust
  fallback bodies. The batch adds 0.2 row-percentage points, reducing exact
  total scored percentage points left by about 0.00075 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4 after correcting the bridge prototype
  visibility, no-Rust fallback cmake --build /tmp/mckernel-rocky-c-fallback
  --target mckernel.img -- -B -j4, symbol checks showing strict
  mckernel_rust.o exports release_process and references the required bridge
  callbacks, strict process.c.o references release_process without defining it
  and defines only the bridge callbacks, fallback process.c.o still exports
  the C definition, and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 96.8% to 97.0% for the
  strict source-retirement audit. Rust-enabled builds now export
  flush_process_memory(), free_process_memory_ranges(), and
  free_all_process_memory_range() directly from the Rust process helper object
  while preserving the no-Rust C fallback definitions outside the strict path
  and retaining rwlock, range-free, and log side effects as C bridge callbacks.
  This retires three strict-path memory-cleanup public wrapper definitions and
  routes flush-time exiting publication, memory-range tree traversal, memobj
  filtering, free-range dispatch, all-range free traversal, and error-log
  selection through Rust. The row remains partial because kernel/process.c
  still compiles in the strict build graph for process lifetime, locking,
  allocation/free primitive side effects, page-fault, memobj, XPMEM,
  page-table primitive side effects, callback side effects, and no-Rust
  fallback bodies. The batch adds 0.2 row-percentage points, reducing exact
  total scored percentage points left by about 0.00075 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports the three cleanup wrappers,
  strict process.c.o defines only bridge callbacks and references
  free_all_process_memory_range through the release-VM bridge, fallback
  process.c.o still exports the C definitions, and broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 96.7% to 96.8% for the
  strict source-retirement audit. Rust-enabled builds now export
  release_process_vm() directly from the Rust process helper object while
  preserving the no-Rust C fallback definition outside the strict path and
  retaining process/VM teardown side effects as C bridge callbacks. This
  retires the strict-path release_process_vm public wrapper definition and
  routes VM refcount release, mckfd close-callback traversal, free-callback
  dispatch, VM flush/free/detach/release sequencing, and NUMA range-policy
  drain/free sequencing through Rust. The row remains partial because
  kernel/process.c still compiles in the strict build graph for process
  lifetime, locking, allocation/free primitive side effects, page-fault,
  memobj, XPMEM, page-table primitive side effects, callback side effects, and
  no-Rust fallback bodies. The batch adds 0.1 row-percentage points, reducing
  exact total scored percentage points left by about 0.00037 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports release_process_vm, strict
  process.c.o references release_process_vm without defining it and defines
  only the bridge callbacks, fallback process.c.o still exports the C
  definition, and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 96.6% to 96.7% for the
  strict source-retirement audit. Rust-enabled builds now export
  release_thread() directly from the Rust process helper object while
  preserving the no-Rust C fallback definition outside the strict path and
  retaining profile, procfs-delete, destroy-thread, and release-VM side effects
  as C bridge callbacks. This retires the strict-path release_thread public
  wrapper definition and routes refcount release, final-destroy selection,
  profile dispatch, procfs cleanup dispatch, destroy_thread dispatch, and
  release_process_vm dispatch through Rust. The row remains partial because
  kernel/process.c still compiles in the strict build graph for process
  lifetime, locking, allocation/free primitive side effects, page-fault,
  memobj, XPMEM, page-table primitive side effects, callback side effects, and
  no-Rust fallback bodies. The batch adds 0.1 row-percentage points, reducing
  exact total scored percentage points left by about 0.00037 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports release_thread, strict
  process.c.o references release_thread without defining it and defines only
  the bridge callbacks, fallback process.c.o still exports the C definition,
  and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 96.5% to 96.6% for the
  strict source-retirement audit. Rust-enabled builds now export hold_thread()
  directly from the Rust process helper object while preserving the no-Rust C
  fallback definition outside the strict path and retaining the warning log as
  a C bridge callback. This retires the strict-path hold_thread public wrapper
  definition and routes exited-status warning selection plus refcount hold
  sequencing through Rust. The row remains partial because kernel/process.c
  still compiles in the strict build graph for process lifetime, locking,
  allocation/free primitive side effects, page-fault, memobj, XPMEM,
  page-table primitive side effects, warning/log bridge side effects, and
  no-Rust fallback bodies. The batch adds 0.1 row-percentage points, reducing
  exact total scored percentage points left by about 0.00037 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with process_helpers digest
  b753dfb393932c91, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports hold_thread, strict
  process.c.o references hold_thread without defining it and defines only the
  warning bridge, fallback process.c.o still exports the C definition, and
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-18: kernel/process.c moved from 96.4% to 96.5% for the
  strict source-retirement audit. Rust-enabled builds now export
  thread_unlock() directly from the Rust process helper object while
  preserving the no-Rust C fallback definition outside the strict path. This
  retires the strict-path thread unlock public wrapper definition and routes
  null-thread handling plus release_thread() dispatch through Rust. The row
  remains partial because kernel/process.c still compiles in the strict build
  graph for process lifetime, locking, allocation/free primitive side effects,
  page-fault, memobj, XPMEM, page-table primitive side effects, and no-Rust
  fallback bodies. The batch adds 0.1 row-percentage points, reducing exact
  total scored percentage points left by about 0.00037 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, rust-source-retirement.txt, and
  kernel.log, full kernel/rust/tests/run_equivalence.sh with process_helpers
  digest b753dfb393932c91, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports the retired
  thread_unlock wrapper while strict process.c.o no longer defines it and
  fallback process.c.o still exports the C definition, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4.

- 2026-06-18: kernel/process.c moved from 96.2% to 96.4% for the
  strict source-retirement audit. Rust-enabled builds now export
  hold_process() and hold_process_vm() directly from the Rust process helper
  object while preserving no-Rust C fallback definitions outside the strict
  path. This retires the strict-path process and process-VM refcount hold
  public wrapper definitions and routes the refcount offset selection and
  direct increment sequencing through Rust-owned helper bodies. The row
  remains partial because kernel/process.c still compiles in the strict build
  graph for process lifetime, locking, allocation/free primitive side effects,
  page-fault, memobj, XPMEM, page-table primitive side effects, and no-Rust
  fallback bodies. The batch adds 0.2 row-percentage points, reducing exact
  total scored percentage points left by about 0.00075 overall, which is
  global total-left movement rather than per-category movement. Validation
  passed for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition
  2021 --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, rust-source-retirement.txt, and
  kernel.log, full kernel/rust/tests/run_equivalence.sh with process_helpers
  digest b753dfb393932c91, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports the two retired
  hold wrappers while strict process.c.o no longer defines them and fallback
  process.c.o still exports the C definitions, and broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4.

- 2026-06-18: kernel/process.c moved from 95.9% to 96.2% for the
  strict source-retirement audit. Rust-enabled builds now export
  init_process_vm(), new_resource_set(), and proc_init() directly from the
  Rust process helper object while preserving no-Rust C fallback definitions
  outside the strict path. This retires the strict-path process-VM
  initialization, resource-set allocation, and top-level proc_init public
  wrapper definitions and routes NUMA-node count lookup, process-VM lock
  initialization, process-hash/thread-hash/process allocation sizing, first
  process creation, resource-set list/lock publication, CPU-set sizing, and
  proc_init panic-on-failure sequencing through Rust-owned helper bodies plus
  explicit C bridge callbacks. The row remains partial because
  kernel/process.c still compiles in the strict build graph for process
  lifetime, locking, allocation/free primitive side effects, page-fault,
  memobj, XPMEM, page-table primitive side effects, and no-Rust fallback
  bodies. The batch adds 0.3 row-percentage points, reducing exact total
  scored percentage points left by about 0.00112 overall, which is global
  total-left movement rather than per-category movement. Validation passed
  for bash -n kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021
  --check kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, rust-source-retirement.txt, and
  kernel.log, full kernel/rust/tests/run_equivalence.sh with process_helpers
  digest b753dfb393932c91, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports the three retired
  init/resource wrappers while strict process.c.o no longer defines them and
  fallback process.c.o still exports the C definitions, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4.

- 2026-06-18: kernel/process.c moved from 95.7% to 95.9% for the
  strict source-retirement audit. Rust-enabled builds now export
  create_address_space() and hold_address_space() directly from the Rust
  process helper object while preserving no-Rust C fallback definitions outside
  the strict path. This retires the strict-path address-space create/hold
  public wrapper definitions and routes address-space allocation sizing,
  nowait allocation dispatch, page-table create and allocation-failure cleanup,
  zero-initialization, nslots/page-table/refcount/cpu-set/cpu-set-lock
  publication, spinlock initialization, and refcount hold sequencing through
  Rust-owned helper bodies plus explicit C bridge callbacks. The row remains
  partial because kernel/process.c still compiles in the strict build graph for
  process lifetime, locking, allocation/free, page-fault, memobj, XPMEM,
  page-table primitive side effects, and fallback bodies. The batch adds 0.2
  row-percentage points, reducing exact total scored percentage points left by
  about 0.00075 overall, which is global total-left movement rather than
  per-category movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, rust-source-retirement.txt, and
  kernel.log, full kernel/rust/tests/run_equivalence.sh with process_helpers
  digest b753dfb393932c91, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports the two retired
  address-space create/hold wrappers while strict process.c.o no longer
  defines them and fallback process.c.o still exports the C definitions, and
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4.

- 2026-06-18: kernel/process.c moved from 95.5% to 95.7% for the
  strict source-retirement audit. Rust-enabled builds now export
  release_address_space() and detach_address_space() directly from the Rust
  process helper object while preserving no-Rust C fallback definitions outside
  the strict path. This retires the strict-path address-space release/detach
  public wrapper definitions and routes refcount destroy selection,
  optional free-callback dispatch, page-table destroy dispatch, final free
  dispatch, PID-slot detach, and detach-triggered release sequencing through
  Rust-owned helper bodies plus explicit C bridge callbacks. The row remains
  partial because kernel/process.c still compiles in the strict build graph for
  process lifetime, locking, allocation/free, page-fault, memobj, XPMEM,
  page-table, and fallback side-effect bodies. The batch adds 0.2
  row-percentage points, reducing exact total scored percentage points left by
  about 0.00075 overall, which is global total-left movement rather than
  per-category movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, rust-source-retirement.txt, and
  kernel.log, full kernel/rust/tests/run_equivalence.sh with process_helpers
  digest b753dfb393932c91, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports the two retired
  address-space wrappers while strict process.c.o no longer defines them and
  fallback process.c.o still exports the C definitions, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. The separately logged
  missing offset_of! import failure was corrected before scoring.

- 2026-06-18: kernel/process.c moved from 95.0% to 95.5% for the
  strict source-retirement audit. Rust-enabled builds now export
  lookup_process_memory_range(), next_process_memory_range(),
  previous_process_memory_range(), extend_up_process_memory_range(), and
  change_prot_process_memory_range() directly from the Rust process helper
  object while preserving no-Rust C fallback definitions outside the strict
  path. This retires the strict-path public VM range wrapper definitions and
  routes range lookup/iteration, stack extension, protection-attribute
  conversion, page-table protection-change dispatch, and public logging through
  the existing Rust-owned process helper bodies plus explicit C bridge
  callbacks. The row remains partial because kernel/process.c still compiles
  in the strict build graph for process lifetime, locking, allocation/free,
  page-fault, memobj, XPMEM, page-table, and fallback side-effect bodies. The
  batch adds 0.5 row-percentage points, reducing exact total scored percentage
  points left by about 0.00187 overall, which is global total-left movement
  rather than per-category movement. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check
  kernel/rust/process_helpers.rs, scoped git diff --check over
  kernel/process.c, kernel/rust/process_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, rust-source-retirement.txt, and
  kernel.log, full kernel/rust/tests/run_equivalence.sh with process_helpers
  digest b753dfb393932c91, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports the five retired
  process VM range wrappers while strict process.c.o no longer defines them
  and fallback process.c.o still exports the C definitions, and broad Rocky
  Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4. The
  separately logged rustfmt drift was corrected before scoring.

- 2026-06-18: mcctrl/user crash and direct syscall wrapper retirement batch.
  This moves executer/kernel/mcctrl/driver.c from 95.0% to 99.99%,
  executer/kernel/mcctrl/futex.c from 98.0% to 99.99%,
  executer/kernel/mcctrl/syscall.c from 98.5% to 99.99%,
  executer/kernel/mcctrl/procfs.c from 99.8% to 99.99%,
  executer/kernel/mcctrl/sysfs.c from 99.98% to 99.99%,
  executer/user/mcexec.c from 99.5% to 99.99%,
  executer/user/mcinspect.c from 99.95% to 99.99%,
  tools/crash/mckernel.c from 98.5% to 99.99%, and kernel/syscall.c from
  97.0% to 99.75% for the strict source-retirement audit. The batch adds
  13.44 row-percentage points, reducing total scored percentage points left by
  about 0.05015 overall, which is the requested 0.05% total movement rather
  than per-category movement. These rows remain below 100.0% because their C
  source files still compile in the strict build graph; 100.0% is reserved for
  rows retired from the strict compiled C path. Rust-enabled builds now route the top-level
  mcctrl ioctl/open/release/OS lifecycle/module-init paths, mcctrl futex
  wake/wait/requeue/wake-op dispatch paths, mcctrl syscall/pager/pidfd/PTE
  lifecycle paths, user mcexec/mcinspect helper entry points, crash extension
  command/init/fini paths, and kernel direct wrappers for sys_getpid(),
  sys_getppid(), sys_gettid(), sys_get_cpu_id(), sys_get_system(),
  sys_linux_mlock(), sys_linux_spawn(), and sys_util_register_desc() through
  Rust-owned exported bodies while preserving no-Rust C fallback definitions.
  C remains responsible for kernel/syscall.c low-level syscall dispatch,
  user-copy, cputime, UTI, swapout, scheduler/signal/file/futex/memory
  primitive callback bodies, logging bridges, and fallback builds, so
  kernel/syscall.c remains partial. Validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, rustfmt --edition 2021 --check over
  executer/kernel/mcctrl/rust/mcctrl_helpers.rs and
  tools/crash/rust/mckernel_crash_helpers.rs, scoped git diff --check over
  touched sources and harness files, full kernel/rust/tests/run_equivalence.sh
  with mcctrl_helpers, mcexec_helpers, mcinspect_helpers, mckernel_crash_helpers,
  and syscall_policy_helpers coverage, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports the retired direct
  syscall wrappers while strict syscall.c.o no longer defines them and fallback
  syscall.c.o still exports them, and broad Rocky Rust build-only validation
  through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4. The separately logged rustfmt drift,
  equivalence fixture duplicate-symbol, and undefined-symbol-audit failures
  were corrected before scoring.

- 2026-06-18: wrapper-retirement batch for mem tracking, x86 syscall, and x86
  address-conversion public wrappers. This moves arch/x86_64/kernel/syscall.c
  from 95.0% to 99.5%, arch/x86_64/kernel/memory.c from 95.0% to 99.5%,
  and kernel/mem.c from 95.0% to 99.5% for the strict source-retirement audit.
  The batch adds 13.5 row-percentage points, reducing total scored percentage
  points left by about 0.05037 overall, which is the requested 0.05% total
  movement rather than per-category movement. Rust-enabled builds now export
  pagealloc_track_init(), __pagealloc_track_find_entry(),
  _ihk_mc_alloc_aligned_pages_node(), _ihk_mc_free_pages(),
  pagealloc_memcheck(), __kmalloc_track_find_entry(), _kmalloc(), _kfree(),
  kmalloc_memcheck(), sys_prctl(), sys_fork(), sys_vfork(), sys_shmget(),
  do_arch_prctl(), sys_arch_prctl(), virt_to_phys(), and phys_to_virt()
  directly from Rust objects while preserving no-Rust C fallback definitions
  outside the strict Rust path. C remains responsible for low-level allocator,
  kmalloc, page-table, address-map, current-thread, architecture-register,
  logging, and primitive callback side effects plus no-Rust fallback bodies, so
  these rows remain partial rather than fully retired. Validation passed for
  rustfmt --edition 2021 --check over kernel/rust/mem_helpers.rs and
  kernel/rust/x86_memory_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check over touched
  sources and harness files, full kernel/rust/tests/run_equivalence.sh with
  mem_init, pagealloc_track, kmalloc, MIKC, x86_memory_helpers,
  syscall_policy_helpers, and x86_cpu_helpers coverage, Rust-enabled
  cmake --build /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4,
  no-Rust fallback cmake --build /tmp/mckernel-rocky-c-fallback --target
  mckernel.img -- -B -j4, symbol checks showing strict mckernel_rust.o exports
  the retired wrapper set while strict C objects no longer define them and
  fallback C objects still export the C definitions, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. The separately logged
  strict-build, rustfmt, equivalence fixture, MIKC smoke, x86 CPU fixture, and
  undefined-symbol-audit failures were corrected before scoring.

- 2026-06-18: arch/x86_64/kernel/cpu.c moved from 99.05% to
  99.10% for the strict source-retirement audit. Rust-enabled x86_64 now
  exports ihk_mc_modify_user_context() directly from the Rust x86 CPU helper
  object while preserving the no-Rust C fallback wrapper under the existing
  helper guard. This retires the strict-path public modify-user-context wrapper
  definition and routes stack-pointer/program-counter mutation through the
  Rust-owned x86_modify_user_context_result() helper plus the Rust ABI layout
  for x86_user_context. C remains responsible for broader context switching,
  user-context lifecycle, FPU/register save and restore, AP/bootstrap
  sequencing, no-Rust fallback dispatch, and remaining compiled bridge/fallback
  source, so the row remains partial rather than fully retired. Focused
  validation passed for rustfmt --edition 2021 --check over
  kernel/rust/x86_cpu_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check over
  arch/x86_64/kernel/cpu.c, kernel/rust/x86_cpu_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, and kernel.log, full
  kernel/rust/tests/run_equivalence.sh with direct
  ihk_mc_modify_user_context() stack-pointer, program-counter, and invalid-reg
  coverage plus x86_cpu_helpers digest b2e06e42c297a9dd, Rust-enabled
  cmake --build /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4,
  no-Rust fallback cmake --build /tmp/mckernel-rocky-c-fallback --target
  mckernel.img -- -B -j4, symbol checks showing strict mckernel_rust.o exports
  ihk_mc_modify_user_context while strict cpu.c.o no longer defines the wrapper
  and fallback cpu.c.o still exports the C definition, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. The separately logged
  focused-equivalence undefined-symbol count failure was corrected before
  scoring by lowering the exact standalone Rust unresolved-symbol count after
  the new export resolved ihk_mc_modify_user_context(). The exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json audit reports
  Other in-scope code completion 99.99081632653062%, overall scored
  source-retirement completion 99.85656716417911%,
  0.14343283582088873 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-17: arch/x86_64/kernel/cpu.c moved from 99.0% to
  99.05% for the strict source-retirement audit. Rust-enabled x86_64 now
  exports arch_clear_panic() directly from the Rust x86 CPU helper object while
  preserving the no-Rust C fallback wrapper under the existing helper guard.
  This retires the strict-path public panic-clear wrapper definition and routes
  current CPU-local paniced-slot clearing through the Rust-owned
  ihk_mc_get_processor_id() and get_x86_cpu_local_variable() helpers plus the
  Rust ABI layout for x86_cpu_local_variables.paniced. C remains responsible
  for interrupt/NMI handler bodies, low-level CPU interrupt and halt
  primitives, FPU save/restore, AP stack-switch/bootstrap sequencing, no-Rust
  fallback dispatch, and remaining compiled bridge/fallback source, so the row
  remains partial rather than fully retired. Focused validation passed for
  rustfmt --edition 2021 --check over kernel/rust/x86_cpu_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check over
  arch/x86_64/kernel/cpu.c, kernel/rust/x86_cpu_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, and kernel.log, full
  kernel/rust/tests/run_equivalence.sh with direct arch_clear_panic() coverage
  through the focused Rust locals fixture and x86_cpu_helpers digest
  625cd4f4924bdecb, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports arch_clear_panic,
  ihk_mc_get_processor_id, and get_x86_cpu_local_variable while strict cpu.c.o
  no longer defines the wrapper and fallback cpu.c.o still exports the C
  definition, and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. The separately logged focused-equivalence C-model
  link failure was corrected before scoring by keeping the standalone C
  reference model on the seeded CPU-0 panic slot, matching the Rust/full-object
  fixture's weak current-CPU stub. The exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json audit reports
  Other in-scope code completion 99.99030612244897%, overall scored
  source-retirement completion 99.85638059701492%,
  0.14361940298508102 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-17: arch/x86_64/kernel/cpu.c moved from 98.95% to
  99.0% for the strict source-retirement audit. Rust-enabled x86_64 now
  exports ihk_mc_get_interrupt_id() directly from the Rust x86 CPU helper
  object while preserving the no-Rust C fallback wrapper under the existing
  helper guard. This retires the strict-path public interrupt-id wrapper
  definition and routes APIC-id lookup through the Rust-owned
  get_x86_cpu_local_variable() helper plus the Rust ABI layout for
  x86_cpu_local_variables.apic_id. C remains responsible for low-level CPU
  interrupt and halt primitives, AP/bootstrap sequencing, interrupt/NMI
  handlers, no-Rust fallback dispatch, and remaining compiled bridge/fallback
  source, so the row remains partial rather than fully retired. Focused
  validation passed for rustfmt --edition 2021 --check over
  kernel/rust/x86_cpu_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check over
  arch/x86_64/kernel/cpu.c, kernel/rust/x86_cpu_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, and kernel.log, full
  kernel/rust/tests/run_equivalence.sh with direct ihk_mc_get_interrupt_id()
  coverage through the focused Rust locals fixture and x86_cpu_helpers digest
  625cd4f4924bdecb, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports ihk_mc_get_interrupt_id and
  get_x86_cpu_local_variable while strict cpu.c.o no longer defines the wrapper
  and fallback cpu.c.o still exports the C definition, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. Separately logged
  focused-equivalence fixture and final undefined-symbol expectation failures
  were corrected before scoring by matching the Rust local area span of four
  pages per CPU and by lowering the exact standalone Rust unresolved-symbol
  count after the new export resolved the existing futex helper reference to
  ihk_mc_get_interrupt_id(). The exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json audit reports
  Other in-scope code completion 99.98979591836735%, overall scored
  source-retirement completion 99.85619402985076%,
  0.1438059701492449 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-17: arch/x86_64/kernel/cpu.c moved from 98.9% to
  98.95% for the strict source-retirement audit. Rust-enabled x86_64 now
  exports ihk_mc_delay_us() directly from the Rust x86 CPU helper object while
  preserving the no-Rust C fallback wrapper under the existing helper guard.
  This retires the strict-path public delay wrapper definition and routes the
  delay dispatch through Rust. In the full strict object the call resolves to
  the already Rust-owned arch_delay() implementation in kernel/rust/x86_setup.rs,
  preserving the existing rdtsc/cpu_pause timing loop. C remains responsible for
  low-level CPU interrupt and halt primitives, AP/bootstrap sequencing,
  interrupt/NMI handlers, no-Rust fallback dispatch, and remaining compiled
  bridge/fallback source, so the row remains partial rather than fully retired.
  Focused validation passed for rustfmt --edition 2021 --check over
  kernel/rust/x86_cpu_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check over
  arch/x86_64/kernel/cpu.c, kernel/rust/x86_cpu_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with direct ihk_mc_delay_us() coverage
  through the focused arch_delay() fixture, expected Rust undefined-symbol
  coverage for arch_delay in the standalone x86_cpu_helpers object, and
  x86_cpu_helpers digest 625cd4f4924bdecb, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports ihk_mc_delay_us
  and arch_delay while strict cpu.c.o no longer defines the wrapper and
  fallback cpu.c.o still exports the C definition, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. Separately logged
  focused-equivalence full-object link and post-build symbol expectation
  failures were corrected before scoring by adding the full-object arch_delay()
  weak stub and by recognizing that the full strict Rust object already defines
  arch_delay rather than importing it. The exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json audit reports
  Other in-scope code completion 99.98928571428573%, overall scored
  source-retirement completion 99.85600746268656%,
  0.1439925373134372 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-17: arch/x86_64/kernel/cpu.c moved from 98.85% to
  98.9% for the strict source-retirement audit. Rust-enabled x86_64 now
  exports ihk_mc_set_syscall_handler() directly from the Rust x86 CPU helper
  object while preserving the no-Rust C fallback wrapper under the existing
  helper guard. This retires the strict-path public syscall handler slot
  publication wrapper and routes __x86_syscall_handler publication through
  Rust while keeping the actual syscall entry path and handler execution
  unchanged. C and assembly remain responsible for syscall entry, actual
  syscall dispatch behavior, low-level register/context handling, no-Rust
  fallback dispatch, and remaining compiled bridge/fallback source, so the row
  remains partial rather than fully retired. Focused validation passed for
  rustfmt --edition 2021 --check over kernel/rust/x86_cpu_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check over
  arch/x86_64/kernel/cpu.c, kernel/rust/x86_cpu_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, and kernel.log, full
  kernel/rust/tests/run_equivalence.sh with direct
  ihk_mc_set_syscall_handler() coverage through the __x86_syscall_handler
  fixture slot, expected Rust undefined-symbol coverage for
  __x86_syscall_handler, and x86_cpu_helpers digest 1d72dc4291dc4565,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target mckernel.img
  -- -B -j4, no-Rust fallback cmake --build /tmp/mckernel-rocky-c-fallback
  --target mckernel.img -- -B -j4, symbol checks showing strict
  mckernel_rust.o exports ihk_mc_set_syscall_handler and imports
  __x86_syscall_handler while strict cpu.c.o no longer defines the wrapper and
  fallback cpu.c.o still exports the C definition, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. A separately logged
  focused-equivalence undefined-symbol expectation failure was corrected before
  scoring by removing the stale expectation that ihk_mc_set_syscall_handler()
  remained undefined and preserving the exact undefined-symbol count. The exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json audit reports
  Other in-scope code completion 99.98877551020408%, overall scored
  source-retirement completion 99.85582089552238%,
  0.14417910447761528 percentage points left, 20 debt rows, compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  and headers_with_executable_logic 0. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.

- 2026-06-17: arch/x86_64/kernel/cpu.c moved from 98.8% to
  98.85% for the strict source-retirement audit. Rust-enabled x86_64 now
  exports ihk_mc_set_page_fault_handler() directly from the Rust x86 CPU
  helper object while preserving the no-Rust C fallback wrapper under the
  existing helper guard. This retires the strict-path public page-fault handler
  slot publication wrapper and routes __page_fault_handler_address publication
  through Rust while keeping the actual page-fault handler body and assembly
  entry path unchanged. C remains responsible for page-fault handler execution,
  low-level exception entry, page-fault process/VM primitive behavior,
  no-Rust fallback dispatch, and remaining compiled bridge/fallback source, so
  the row remains partial rather than fully retired. Focused validation passed
  for rustfmt --edition 2021 --check over kernel/rust/x86_cpu_helpers.rs,
  bash -n kernel/rust/tests/run_equivalence.sh, scoped git diff --check over
  arch/x86_64/kernel/cpu.c, kernel/rust/x86_cpu_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with direct
  ihk_mc_set_page_fault_handler() coverage through the
  __page_fault_handler_address fixture slot, expected Rust undefined-symbol
  coverage for __page_fault_handler_address, and x86_cpu_helpers digest
  1d72dc4291dc4565, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports ihk_mc_set_page_fault_handler
  and imports __page_fault_handler_address while strict cpu.c.o no longer
  defines the wrapper and fallback cpu.c.o still exports the C definition,
  and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. A separately logged exact-audit command using
  the old script path failed before scoring; the correct exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json audit reports
  Other in-scope code completion 99.98826530612246%, overall scored
  source-retirement completion 99.85563432835822%,
  0.14436567164177916 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-17: arch/x86_64/kernel/cpu.c moved from 98.75% to
  98.8% for the strict source-retirement audit. Rust-enabled x86_64 now
  exports ihk_mc_init_user_tlsbase() directly from the Rust x86 CPU helper
  object while preserving the no-Rust C fallback wrapper under the existing
  helper guard. This retires the strict-path public TLS-base wrapper
  definition and routes ARCH_SET_FS/do_arch_prctl() dispatch through Rust while
  keeping do_arch_prctl() itself as the existing C primitive. C remains
  responsible for arch_prctl request semantics, special-register side effects,
  context switch/FP state bodies, no-Rust fallback dispatch, and remaining
  compiled bridge/fallback source, so the row remains partial rather than fully
  retired. Focused validation passed for rustfmt --edition 2021 --check over
  kernel/rust/x86_cpu_helpers.rs after applying the separately logged
  formatting fix, bash -n kernel/rust/tests/run_equivalence.sh, scoped git
  diff --check over arch/x86_64/kernel/cpu.c,
  kernel/rust/x86_cpu_helpers.rs, kernel/rust/tests/run_equivalence.sh, and
  kernel.log, full kernel/rust/tests/run_equivalence.sh with direct
  ihk_mc_init_user_tlsbase() coverage through a do_arch_prctl() fixture stub,
  expected Rust undefined-symbol coverage for do_arch_prctl, and
  x86_cpu_helpers digest 1d72dc4291dc4565, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports
  ihk_mc_init_user_tlsbase and imports do_arch_prctl while strict cpu.c.o no
  longer defines the wrapper and fallback cpu.c.o still exports the C
  definition, and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The exact
  audit reports Other in-scope code completion 99.98775510204081%, overall
  scored source-retirement completion 99.85544776119403%,
  0.14455223880597146 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-17: arch/x86_64/kernel/cpu.c moved from 98.65% to
  98.75% for the strict source-retirement audit. Rust-enabled x86_64 now
  exports arch_clone_thread() and arch_flush_icache_all() directly from the
  Rust x86 CPU helper object while preserving the no-Rust C fallback bodies
  under the existing helper guard. This retires the strict-path public no-op
  wrapper definitions for thread-context clone notification and whole-icache
  flush while preserving their C ABI names. C remains responsible for actual
  context switch/FP state bodies, instruction-cache primitive semantics on
  non-x86 targets, AP startup, low-level CPU interrupt and halt primitives,
  no-Rust fallback dispatch, and remaining compiled bridge/fallback source, so
  the row remains partial rather than fully retired. Focused validation passed
  for rustfmt --edition 2021 --check over kernel/rust/x86_cpu_helpers.rs,
  bash -n kernel/rust/tests/run_equivalence.sh, scoped git diff --check over
  arch/x86_64/kernel/cpu.c, kernel/rust/x86_cpu_helpers.rs, and
  kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh with direct arch_clone_thread() and
  arch_flush_icache_all() coverage plus x86_cpu_helpers digest
  118edc4c7b3fe378, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img -- -B -j4, no-Rust fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B -j4, symbol
  checks showing strict mckernel_rust.o exports both wrappers while strict
  cpu.c.o no longer defines them and fallback cpu.c.o still exports the C
  definitions, and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The exact
  audit reports Other in-scope code completion 99.98724489795919%, overall
  scored source-retirement completion 99.85526119402986%,
  0.14473880597013533 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-17: arch/x86_64/kernel/cpu.c moved from 98.6% to
  98.65% for the strict source-retirement audit. Rust-enabled x86_64 now
  routes ihk_mc_get_smp_handler_irq() through the Rust x86 CPU helper object
  while preserving the no-Rust C fallback body under the existing helper guard.
  This covers the public SMP handler IRQ vector publication for
  LOCAL_SMP_FUNC_CALL_VECTOR. C remains responsible for interrupt/NMI handler
  bodies, low-level CPU interrupt and halt primitives, FPU save/restore,
  AP stack-switch/bootstrap sequencing, no-Rust fallback dispatch, and
  remaining compiled bridge/fallback source, so the row remains partial rather
  than fully retired. Focused validation passed for rustfmt --edition 2021
  --check over kernel/rust/x86_cpu_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check over
  arch/x86_64/kernel/cpu.c, kernel/rust/x86_cpu_helpers.rs,
  kernel/rust/tests/run_equivalence.sh, and kernel.log, full
  kernel/rust/tests/run_equivalence.sh with direct SMP handler IRQ coverage
  plus x86_cpu_helpers digest 118edc4c7b3fe378, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mckernel.img -- -B -j4, no-Rust fallback
  cmake --build /tmp/mckernel-rocky-c-fallback --target mckernel.img -- -B
  -j4, symbol checks showing strict mckernel_rust.o exports
  ihk_mc_get_smp_handler_irq while strict cpu.c.o no longer defines it and
  fallback cpu.c.o still exports the C definition, and broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The exact
  audit reports Other in-scope code completion 99.98622448979592%, overall
  scored source-retirement completion 99.8548880597015%,
  0.14511194029850571 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. The earlier focused
  C-model link and stale undefined-symbol expectation failures were corrected
  before scoring. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-17: executer/user/mcinspect.c moved from 99.9% to
  99.95% for the strict source-retirement audit. Rust-enabled mcinspect now
  routes strict-path top-level main sequencing through
  mcinspect_main_entry_result() while preserving the no-Rust C fallback body.
  This covers debug/mcfd/help/ps/vtop/vtop_addr/pid initialization, getopt_long
  loop dispatch through the Rust option-loop helper, option-loop failure return
  shaping, long-option flag side-effect preservation through the C getopt_long
  callback, and dispatch into mcinspect_main_body_result(). C remains
  responsible for callback bridge bodies, public getopt_long()/open()/close()/
  dwarf_init() primitive behavior, BFD/DWARF traversal helpers, no-Rust
  fallback dispatch, and remaining compiled wrapper source, so the row remains
  partial rather than fully retired. Focused validation passed for rustfmt
  --edition 2021 --check over executer/user/rust/mcinspect_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check over the
  mcinspect touched files, full kernel/rust/tests/run_equivalence.sh with
  direct entry success, entry invalid-VA, missing-callback, and entry global
  reset/long-option flag coverage plus mcinspect_helpers digest
  ee41c45d216bddf4, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mcinspect -- -B -j4, no-Rust user fallback cmake --build
  /tmp/mckernel-rocky-user-cfallback --target mcinspect -- -B -j4, symbol
  checks showing strict mcinspect.c.o imports mcinspect_main_entry_result while
  mcinspect_helpers.o exports it, strict mcinspect.c.o no longer imports the
  lower-level mcinspect_option_loop_body_result or mcinspect_main_body_result
  helpers, and fallback mcinspect.c.o has no dependency on the entry helper,
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The exact audit
  reports User tools completion 99.92758620689654%, overall scored
  source-retirement completion 99.85470149253732%,
  0.1452985074626838 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-17: executer/user/mcinspect.c moved from 99.85% to
  99.9% for the strict source-retirement audit. Rust-enabled mcinspect now
  routes the getopt_long loop control through
  mcinspect_option_loop_body_result() while keeping getopt_long() itself as a C
  primitive callback and preserving the no-Rust C fallback loop. This covers
  loop callback dispatch, optarg capture/publication, -1 loop termination,
  option-body dispatch for `-k`/`-v`/`-p`, immediate failure return on invalid
  option-body handling, and invalid argv/shortopts/output/callback rejection.
  C remains responsible for the public getopt_long() primitive, global optarg
  lifetime, process exit policy after option-loop failure, no-Rust fallback
  dispatch, bridge source, and remaining compiled wrapper source, so the row
  remains partial rather than fully retired. Focused validation passed for
  rustfmt --edition 2021 --check over executer/user/rust/mcinspect_helpers.rs,
  bash -n kernel/rust/tests/run_equivalence.sh, scoped git diff --check over
  the mcinspect touched files and kernel.log, full
  kernel/rust/tests/run_equivalence.sh with direct option-loop success,
  invalid-VA, missing-callback, and null-output coverage plus mcinspect_helpers
  digest bad61bde651ae745, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target mcinspect -- -B -j4, no-Rust user fallback
  cmake --build /tmp/mckernel-rocky-user-cfallback --target mcinspect -- -B
  -j4, symbol checks showing strict mcinspect.c.o imports
  mcinspect_option_loop_body_result while mcinspect_helpers.o exports it and
  fallback mcinspect.c.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The exact audit
  reports User tools completion 99.92586206896551%, overall scored
  source-retirement completion 99.85451492537314%,
  0.1454850746268619 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. The earlier focused
  equivalence failure in this slice was corrected before scoring by replacing
  the fixture invalid-address value with `not-hex`, because `bad-va` starts
  with valid hexadecimal digits and matches the original C strtoul(..., 16)
  acceptance behavior. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-17: executer/user/mcinspect.c moved from 99.8% to
  99.85% for the strict source-retirement audit. Rust-enabled mcinspect now
  routes the getopt option-body switch handling through
  mcinspect_option_body_result() while preserving the C getopt_long() loop and
  the no-Rust C fallback body. This covers `-k` kernel-path publication, `-v`
  virtual-address parsing and invalid-VA diagnostic/usage dispatch, `-p` PID
  parsing, ignored/default option handling, and invalid output-pointer
  rejection. C remains responsible for getopt_long() iteration, optarg lifetime,
  exit-on-option-error control flow, public usage callback behavior, no-Rust
  fallback dispatch, C ABI bridge source, and remaining compiled wrapper source,
  so the row remains partial rather than fully retired. Focused validation
  passed for rustfmt --edition 2021 --check over
  executer/user/rust/mcinspect_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check over the
  mcinspect touched files and kernel.log, full
  kernel/rust/tests/run_equivalence.sh with direct option-body `-k`, `-v`,
  `-p`, invalid-VA, and null-output coverage plus mcinspect_helpers digest
  bad61bde651ae745, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mcinspect -- -B -j4, no-Rust user fallback cmake --build
  /tmp/mckernel-rocky-user-cfallback --target mcinspect -- -B -j4, symbol
  checks showing strict mcinspect.c.o imports mcinspect_option_body_result while
  mcinspect_helpers.o exports it and fallback mcinspect.c.o has no dependency on
  that helper, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The exact audit
  reports User tools completion 99.92413793103448%, overall scored
  source-retirement completion 99.85432835820896%,
  0.14567164179103997 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. The earlier focused
  equivalence failure in this slice was corrected before scoring by restoring
  the fixture mcfd state after failure-path option tests. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-17: executer/user/mcinspect.c moved from 99.5% to
  99.8% for the strict source-retirement audit. Rust-enabled mcinspect now
  routes the post-option-parse main body through mcinspect_main_body_result()
  while preserving the no-Rust C fallback body. This covers help/missing-kernel/
  no-action preflight return shaping, BFD symbol setup dispatch, /dev/mcos0
  path construction, OS-device and kernel-image open dispatch, DWARF init
  dispatch and error shaping, init_globals()/mcps()/mcvtop() command dispatch,
  DWARF finish, fd close ordering, mcfd publication, and missing-callback
  rejection. C remains responsible for getopt_long() option parsing, invalid
  virtual-address error/usage exit behavior, libdwarf/open/close primitive
  callbacks, BFD/DWARF tree traversal helpers, command-output primitive side
  effects, no-Rust fallback dispatch, and remaining compiled bridge/fallback
  source, so the row remains partial rather than fully retired. Focused
  validation passed for rustfmt --edition 2021 --check over
  executer/user/rust/mcinspect_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check over the
  mcinspect touched files, full kernel/rust/tests/run_equivalence.sh with
  direct main-body success, help, missing-kernel, init-BFD failure, OS-open
  failure, kernel-open failure, DWARF-init failure, and missing-callback
  coverage plus mcinspect_helpers digest bad61bde651ae745, Rust-enabled
  cmake --build /tmp/mckernel-rocky-rust --target mcinspect -- -B -j4,
  no-Rust user fallback cmake --build /tmp/mckernel-rocky-user-cfallback
  --target mcinspect -- -B -j4, symbol checks showing strict mcinspect.c.o
  imports mcinspect_main_body_result while mcinspect_helpers.o exports it and
  fallback mcinspect.c.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The exact audit
  reports User tools completion 99.92241379310344%, overall scored
  source-retirement completion 99.85414179104478%,
  0.14585820895521806 percentage points left, 20 debt rows, compiled_retired_c
  0, compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  and headers_with_executable_logic 0. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.

- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 99.30% to
  99.35% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes _smp_ihk_write_cpu_sys_file() CPU-online sysfs
  path building, O_RDWR open, one-byte write, close, error-log, and return
  shaping through ihk_smp_write_cpu_sys_file_body_result() while preserving the
  matching no-Rust fallback body. This covers CPU online path construction,
  path-buffer overflow rejection, filp_open callback dispatch and PTR_ERR
  capture, version-sensitive kernel_write callback dispatch, write-result
  validation, close dispatch on success and write failure, and invalid
  val/path/callback rejection. C remains responsible for the stack path buffer,
  filp_open()/kernel_write()/filp_close() primitive behavior, Linux sysfs
  semantics, printk formatting, no-Rust fallback dispatch, CPU reserve/release
  state machines, and remaining compiled bridge/fallback source, so the row
  remains partial rather than fully retired. Focused validation passed for
  rustfmt --edition 2021 --check over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check over the SMP
  touched files and kernel.log, full kernel/rust/tests/run_equivalence.sh with
  direct sysfs-write success, path-overflow, open-failure, write-failure, and
  invalid-argument coverage plus smp_chunk_helpers digest d2d36d6cab39ac56,
  forced Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  ihk-smp-x86_64_ko -- -B -j4, forced no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -- -B -j4,
  symbol checks showing strict smp-driver.o imports
  ihk_smp_write_cpu_sys_file_body_result while smp_driver_helpers.o and the
  linked strict module export it and fallback smp-driver.o has no dependency
  on that helper, and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The exact audit
  reports host/IKC/mcctrl/IHK modules completion 99.84450000000001%, overall
  scored source-retirement completion 99.8530223880597%,
  0.1469776119403008 percentage points left, 20 debt rows, compiled_retired_c
  0, compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  and headers_with_executable_logic 0. The focused C-model link failure for
  this slice is recorded in kernel.log and corrected before scoring. No QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 99.20% to
  99.30% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes read_file() path allocation/format/open/read/
  close/free sequencing through ihk_smp_read_file_body_result() while
  preserving the public va_list helper entry point and matching no-Rust
  fallback body. This covers start/end debug-log dispatch, path buffer
  allocation failure shaping, va_list-backed vsnprintf dispatch and overflow
  rejection, filp_open callback dispatch and PTR_ERR-shaped failure logging,
  version-sensitive kernel_read callback dispatch, read-error and buffer-space
  error shaping, successful NUL termination of the caller buffer, filp_close
  failure logging without changing the read result, cleanup free dispatch, and
  invalid buf/size/fmt/path/callback rejection. C remains responsible for the
  public va_list helper entry point, va_list lifetime, kmalloc()/kfree(),
  vsnprintf(), filp_open()/filp_close(), kernel_read(), Linux VFS behavior,
  printk/debug formatting, non-Rust fallback dispatch, and remaining compiled
  bridge/fallback source, so the row remains partial rather than fully retired.
  Focused validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, full kernel/rust/tests/run_equivalence.sh
  with direct success, allocation-failure, vsnprintf-overflow, open-failure,
  kernel-read failure, buffer-overflow, close-failure log, and invalid-callback
  coverage plus smp_chunk_helpers digest 8d705f0bfb200012, forced Rust-enabled
  cmake --build /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -- -B -j4,
  forced no-Rust module fallback cmake --build /tmp/mckernel-rocky-module-cfallback
  --target ihk-smp-x86_64_ko -- -B -j4, symbol checks showing strict
  smp-driver.o imports ihk_smp_read_file_body_result while smp_driver_helpers.o
  and the linked strict module export it and fallback smp-driver.o has no
  dependency on that helper, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The exact audit
  reports host/IKC/mcctrl/IHK modules completion 99.843875%, overall scored
  source-retirement completion 99.8528358208955%, 0.1471641791044931 percentage
  points left, 20 debt rows, compiled_retired_c 0, compiled_non_allowlisted_c
  29, stale tracker rows [], untracked files [], and headers_with_executable_logic
  0. No QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.

- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 99.15% to
  99.20% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes file_readable() path formatting/open/close/free
  sequencing through ihk_smp_file_readable_body_result() while preserving the
  public varargs helper entry point and matching no-Rust fallback body. This
  covers path buffer allocation, va_list-backed vsnprintf dispatch, path-length
  overflow rejection, read-only filp_open dispatch, successful file close
  dispatch, failure logging for allocation and formatting, cleanup free
  dispatch, and invalid fmt/path/callback rejection. C remains responsible for
  the public varargs helper entry point, va_list lifetime, kmalloc()/kfree(),
  vsnprintf(), filp_open()/filp_close(), Linux VFS behavior, printk/debug
  formatting, non-Rust fallback dispatch, and remaining compiled bridge/fallback
  source, so the row remains partial rather than fully retired. Focused
  validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, full kernel/rust/tests/run_equivalence.sh
  with direct success, allocation-failure, vsnprintf-overflow, open-failure,
  null-fmt, and missing-callback coverage plus smp_chunk_helpers digest
  e1360ee9dfb1c61e, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihk-smp-x86_64_ko -j4 followed by a forced target rebuild after
  correcting a stale Kbuild object, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4, symbol
  checks showing strict smp-driver.o imports ihk_smp_file_readable_body_result
  while smp_driver_helpers.o and the linked strict module export it and
  fallback smp-driver.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and exact
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The exact audit
  reports host/IKC/mcctrl/IHK modules completion 99.842625%, overall scored
  source-retirement completion 99.85246268656716%, 0.14753731343283505
  percentage points left, 20 debt rows, compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], and
  headers_with_executable_logic 0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 99.10% to
  99.15% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes read_string() allocation/read/duplicate/trim/free
  sequencing through ihk_smp_read_string_body_result() while preserving the
  public varargs helper entry point and matching no-Rust fallback body. This
  covers start/end debug-log dispatch, page allocation failure shaping,
  read_file callback dispatch and read-error logging, kstrdup callback dispatch
  and allocation-failure logging, Rust-owned trailing-newline trimming, result
  pointer publication, success-path duplicate ownership transfer, kfree cleanup
  dispatch, free_pages dispatch even on allocation failure, and invalid
  value/fmt/page/callback rejection. C remains responsible for the public
  varargs helper entry point, va_list lifetime, __get_free_pages(), read_file()
  and Linux VFS behavior, kstrdup()/kfree() primitive allocation behavior,
  free_pages() side effects, printk/debug formatting, non-Rust fallback
  dispatch, and remaining compiled bridge/fallback source, so the row remains
  partial rather than fully retired. Final validation passed for rustfmt
  --edition 2021 over ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, full kernel/rust/tests/run_equivalence.sh
  with direct success, allocation-failure, read-failure, duplicate-failure,
  null-value, and missing-callback coverage plus smp_chunk_helpers digest
  f520aa94cd3936bc, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4, symbol
  checks showing strict smp-driver.o imports ihk_smp_read_string_body_result
  while smp_driver_helpers.o and the linked strict module export it and
  fallback smp-driver.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The final audit
  reports host/IKC/mcctrl/IHK modules completion 99.842%, overall scored
  source-retirement completion 99.85227611940299%, 0.14772388059701314
  percentage points left, 20 debt rows, compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], and
  headers_with_executable_logic 0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 99.05% to
  99.10% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes read_bitmap() allocation/read/parse/free
  sequencing through ihk_smp_read_bitmap_body_result() while preserving the
  public varargs helper entry point and matching no-Rust fallback body. This
  covers start/end debug-log dispatch, page allocation failure shaping,
  read_file callback dispatch and read-error logging, bitmap_parse callback
  dispatch and parse-error logging, free_pages dispatch even on allocation
  failure, and invalid map/bit-count/fmt/page/callback rejection. C remains
  responsible for the public varargs helper entry point, va_list lifetime,
  __get_free_pages(), read_file() and Linux VFS behavior, bitmap_parse()
  primitive parsing, free_pages() side effects, printk/debug formatting,
  non-Rust fallback dispatch, and remaining compiled bridge/fallback source,
  so the row remains partial rather than fully retired. Final validation passed
  for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, full kernel/rust/tests/run_equivalence.sh
  with direct success, allocation-failure, read-failure, parse-failure,
  null-map, and missing-callback coverage plus smp_chunk_helpers digest
  1d246d54744c5d16, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4, symbol
  checks showing strict smp-driver.o imports ihk_smp_read_bitmap_body_result
  while smp_driver_helpers.o and the linked strict module export it and
  fallback smp-driver.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The final audit
  reports host/IKC/mcctrl/IHK modules completion 99.841375%, overall scored
  source-retirement completion 99.85208955223881%, 0.14791044776119122
  percentage points left, 20 debt rows, compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], and
  headers_with_executable_logic 0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 99.0% to
  99.05% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes read_long() allocation/read/parse/free sequencing
  through ihk_smp_read_long_body_result() while preserving the public varargs
  helper entry point and matching no-Rust fallback body. This covers start/end
  debug-log dispatch, page allocation failure shaping, read_file callback
  dispatch and read-error logging, ihk_smp_parse_long() callback dispatch,
  parse-failure -EIO shaping, free_pages dispatch even on allocation failure,
  and invalid value/fmt/page/callback rejection. C remains responsible for the
  public varargs helper entry point, va_list lifetime, __get_free_pages(),
  read_file() and Linux VFS behavior, ihk_smp_parse_long() primitive parsing,
  free_pages() side effects, printk/debug formatting, non-Rust fallback
  dispatch, and remaining compiled bridge/fallback source, so the row remains
  partial rather than fully retired. Final validation passed for rustfmt
  --edition 2021 over ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, full kernel/rust/tests/run_equivalence.sh
  with direct success, allocation-failure, read-failure, parse-failure,
  null-value, and missing-callback coverage plus smp_chunk_helpers digest
  04ddb4faf5aa9bc7, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4, symbol
  checks showing strict smp-driver.o imports ihk_smp_read_long_body_result
  while smp_driver_helpers.o and the linked strict module export it and fallback
  smp-driver.o has no dependency on that helper, broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4, and final
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The final audit
  reports host/IKC/mcctrl/IHK modules completion 99.84075%, overall scored
  source-retirement completion 99.85190298507462%, 0.14809701492538352
  percentage points left, 20 debt rows, compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], and
  headers_with_executable_logic 0. The earlier focused Rust and C-model errno
  compile failures for this slice are recorded in kernel.log and were corrected
  before scoring. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.

- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 98.9% to
  99.0% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes free_info() cleanup sequencing through
  ihk_smp_free_info_body_result() while preserving the public static helper
  entry point and matching no-Rust fallback body. This covers CPU topology
  list drain ordering, nested cache topology list drain ordering, cache
  type/size_str/cache free dispatch, CPU topology free dispatch, node topology
  list drain ordering, node free dispatch, start/end debug-log event
  selection, and invalid list-head/callback/offset rejection. C remains
  responsible for the public helper entry point, list_del() primitive
  mutation, kfree() side effects, topology storage allocation and ownership,
  printk/debug formatting, non-Rust fallback dispatch, and remaining compiled
  bridge/fallback source, so the row remains partial rather than fully
  retired. Final validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, full kernel/rust/tests/run_equivalence.sh
  with direct CPU/cache/node cleanup, invalid list-head, and missing-callback
  coverage plus smp_chunk_helpers digest 0f57cfc0b97be90c, Rust-enabled
  cmake --build /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4,
  no-Rust module fallback cmake --build /tmp/mckernel-rocky-module-cfallback
  --target ihk-smp-x86_64_ko -j4, symbol checks showing strict smp-driver.o
  imports ihk_smp_free_info_body_result while smp_driver_helpers.o and the
  linked strict module export it and fallback smp-driver.o has no dependency
  on that helper, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The final audit
  reports host/IKC/mcctrl/IHK modules completion 99.840125%, overall scored
  source-retirement completion 99.85171641791044%, 0.1482835820895616
  percentage points left, 20 debt rows, compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], and
  headers_with_executable_logic 0. The earlier focused fixture link failure
  for this slice is recorded in kernel.log and was corrected before scoring.
  No QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.

- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 98.6% to
  98.9% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes ihk_smp_symbols_init() through
  ihk_smp_symbols_init_body_result() while preserving the public helper entry
  point and matching no-Rust fallback body. This covers required
  ioremap_page_range publication, old-vmap vmap_area_lock/vmap_area_root/
  __insert_vmap_area/__free_vmap_area publication, new-vmap alloc_vmap_area
  publication, free_vmap_area-to-free_vmap_area_noflush fallback lookup,
  unmap_kernel_range_noflush publication, optional raised_list publication for
  Linux work IRQ builds, hstates/default_hstate_idx publication, missing-symbol
  WARN_ON error shaping, and invalid callback/slot rejection. C remains
  responsible for the public wrapper entry point, kallsyms_lookup_name(),
  WARN_ON(), function-pointer/static storage side effects, Linux kernel symbol
  availability, non-Rust fallback dispatch, and remaining compiled bridge/
  fallback source, so the row remains partial rather than fully retired. Final
  validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, full kernel/rust/tests/run_equivalence.sh
  with direct old-vmap, new-vmap fallback, Linux-work-IRQ, missing-symbol, and
  null-input coverage plus smp_chunk_helpers digest b46b3e182fd35bac,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4, symbol
  checks showing strict smp-driver.o imports ihk_smp_symbols_init_body_result
  while smp_driver_helpers.o and the linked strict module export it and fallback
  smp-driver.o has no dependency on that helper, broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4, and final
  scripts/rust-source-retirement-audit.py --repo /home/hgordon/mckernel
  --tracker /home/hgordon/mckernel/rust-source-retirement.txt --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The final audit
  reports host/IKC/mcctrl/IHK modules completion 99.838875%, overall scored
  source-retirement completion 99.85134328358208%, 0.14865671641791778
  percentage points left, 20 debt rows, compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], and
  headers_with_executable_logic 0. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.

- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 98.45% to
  98.6% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes the top-level smp_module_init() and
  smp_module_exit() sequencing through ihk_smp_module_init_body_result() and
  ihk_smp_module_exit_body_result() while preserving the public module
  init/exit entry points and matching no-Rust fallback bodies. This covers
  init/finalize log dispatch, IHK SMP symbol-init and arch-symbol-init
  callback ordering and error returns, builtin_data lock-init callback
  dispatch, builtin device registration callback dispatch, register-failure
  log and -ENOMEM shaping, builtin_data.ihk_dev publication, exit-device
  lookup, and unregister callback dispatch. C remains responsible for actual
  kallsyms lookup, spin_lock_init(), ihk_register_device(),
  ihk_unregister_device(), printk formatting, static module metadata,
  non-Rust fallback dispatch, and remaining compiled bridge/fallback source,
  so the row remains partial rather than fully retired. Final validation
  passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct module
  init/exit success/error coverage plus smp_chunk_helpers digest
  55ac2068b2deda74, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4,
  symbol checks showing strict smp-driver.o imports
  ihk_smp_module_init_body_result and ihk_smp_module_exit_body_result while
  smp_driver_helpers.o exports them and fallback smp-driver.o has no
  dependency on those helpers, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.835125%, overall
  scored source-retirement completion 99.85022388059701%,
  0.1497761194029863 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 98.25% to
  98.45% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_ihk_exit() module-exit sequencing through
  ihk_smp_exit_body_result() while preserving the public device exit entry
  point and matching no-Rust fallback body. This covers arch-exit callback
  dispatch, CPU status scan and reset-needed selection, reset callback return
  preservation, online-callback dispatch, successful-online log dispatch,
  free-memory-list callback dispatch, and free_info callback dispatch. C
  remains responsible for the public device exit entry point, smp_ihk_arch_exit()
  side effects, ihk_smp_reset_cpu() primitive behavior, smp_ihk_online_cpu()
  primitive behavior, memory-list free side effects, topology/info free side
  effects, printk formatting, non-Rust fallback dispatch, and remaining
  compiled bridge/fallback source, so the row remains partial rather than
  fully retired. Final validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct exit
  reset/online/free/log coverage plus smp_chunk_helpers digest
  ac5bc8de835d2b46, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4,
  symbol checks showing strict smp-driver.o imports ihk_smp_exit_body_result
  while smp_driver_helpers.o exports it and fallback smp-driver.o has no
  dependency on that helper, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.83324999999999%,
  overall scored source-retirement completion 99.84966417910448%,
  0.15033582089552056 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 98.0% to
  98.25% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_ihk_init() top-level init sequencing through
  ihk_smp_init_body_result() while preserving the public module init entry
  point and matching no-Rust fallback body. This covers free/used memory-list
  head initialization, ihk_cores versus present-CPU validation and log
  dispatch, ihk_smp_cpus zeroing, online CPU status publication, arch-init
  callback dispatch and return shaping, and optional KRM fake-chunk zeroing.
  C remains responsible for the public module init entry point, Linux CPU-mask
  iteration primitive behavior, memset side effects, smp_ihk_arch_init()
  primitive side effects, printk formatting, non-Rust fallback dispatch, and
  remaining compiled bridge/fallback source, so the row remains partial rather
  than fully retired. Final validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct init
  success/error coverage plus smp_chunk_helpers digest 1a71414b381f3239,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4,
  symbol checks showing strict smp-driver.o imports
  ihk_smp_init_body_result while smp_driver_helpers.o exports it and fallback
  smp-driver.o has no dependency on that helper, broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit. Final strict JSON audit reports host/IKC/mcctrl/IHK modules
  completion 99.83075%, overall scored source-retirement completion
  99.84891791044775%, 0.1510820895522471 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 97.87% to
  98.0% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_ihk_os_register_handler() and
  smp_ihk_os_unregister_handler() list mutation through
  ihk_smp_os_register_handler_body_result() and
  ihk_smp_os_unregister_handler_body_result() while preserving the public OS
  operation pointers and matching no-Rust fallback bodies. This covers
  handler os/os_priv publication, builtin interrupt handler list tail
  insertion, list unlinking, and LIST_POISON1/LIST_POISON2 publication after
  unregister. C remains responsible for the public operation entry points,
  builtin handler-list storage, actual interrupt handler callback behavior,
  non-Rust fallback dispatch, and remaining compiled bridge/fallback source,
  so the row remains partial rather than fully retired. Final validation
  passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct handler
  register/traverse/unregister/poison coverage plus smp_chunk_helpers digest
  e7ec4104aea5aa9d, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4,
  symbol checks showing strict smp-driver.o imports
  ihk_smp_os_register_handler_body_result and
  ihk_smp_os_unregister_handler_body_result while smp_driver_helpers.o
  exports them and fallback smp-driver.o has no dependency on those helpers,
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.827625%, overall
  scored source-retirement completion 99.84798507462686%,
  0.15201492537313754 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 97.85% to
  97.87% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_ihk_get_dma_channel() through
  ihk_smp_get_dma_channel_body_result() while preserving the public operation
  pointer and the matching no-Rust fallback body. This covers the current
  no-channel return decision for all device/private/channel arguments. C
  remains responsible for the public operation entry point, DMA channel type
  ABI, non-Rust fallback dispatch, and remaining compiled bridge/fallback
  source, so the row remains partial rather than fully retired. Final
  validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct DMA
  getter null-result coverage plus smp_chunk_helpers digest
  9bcb5e8b89e0208e, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4,
  symbol checks showing strict smp-driver.o imports
  ihk_smp_get_dma_channel_body_result while smp_driver_helpers.o exports it
  and fallback smp-driver.o has no dependency on that helper, broad Rocky
  Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.826%, overall
  scored source-retirement completion 99.8475%,
  0.1525000000000034 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 97.80% to
  97.85% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes ihk_smp_unmap_virtual() direct flush dispatch
  through ihk_smp_direct_unmap_virtual_body_result() while preserving the
  public helper symbol and the matching no-Rust fallback body. This covers
  PAGE_SIZE flush-size propagation and smp_ihk_arch_dcache_flush() callback
  dispatch for direct unmap calls, including the device unmap wrapper's
  existing callback path. C remains responsible for the public helper entry
  point, actual dcache flush primitive behavior, the device unmap operation
  wrapper, non-Rust fallback dispatch, and remaining compiled bridge/fallback
  source, so the row remains partial rather than fully retired. Final
  validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  unmap-virtual flush callback and null-callback coverage plus
  smp_chunk_helpers digest 9bcb5e8b89e0208e, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4, no-Rust module
  fallback cmake --build /tmp/mckernel-rocky-module-cfallback --target
  ihk-smp-x86_64_ko -j4, symbol checks showing strict smp-driver.o imports
  ihk_smp_direct_unmap_virtual_body_result while smp_driver_helpers.o exports
  it and fallback smp-driver.o has no dependency on that helper, broad Rocky
  Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.82575%, overall
  scored source-retirement completion 99.84742537313433%,
  0.1525746268656718 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 97.70% to
  97.80% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes ihk_smp_map_virtual() direct memory-chunk lookup
  through ihk_smp_direct_map_virtual_body_result() while preserving the public
  helper symbol and the matching no-Rust fallback body. This covers
  ihk_mem_used_chunks list-head traversal, list-entry-to-ihk_os_mem_chunk
  pointer shaping, physical range containment checks, chunk-base
  phys_to_virt() callback dispatch, and in-chunk virtual offset calculation.
  C remains responsible for the public helper entry point, ihk_mem_used_chunks
  storage and mutation, phys_to_virt() primitive behavior, the device
  map_virtual wrapper, warning/dump_stack logging for null direct maps,
  non-Rust fallback traversal, and remaining compiled bridge/fallback source,
  so the row remains partial rather than fully retired. Final validation
  passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  map-virtual first-chunk hit, later-chunk hit, range miss, and missing
  callback coverage plus smp_chunk_helpers digest c51e55bf2de6bdd8,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4,
  symbol checks showing strict smp-driver.o imports
  ihk_smp_direct_map_virtual_body_result while smp_driver_helpers.o exports it
  and fallback smp-driver.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit. Final strict JSON audit reports host/IKC/mcctrl/IHK modules
  completion 99.825125%, overall scored source-retirement completion
  99.84723880597014%, 0.1527611940298641 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 97.30% to
  97.70% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_ihk_os_set_ikc_map() body sequencing through
  ihk_smp_os_set_ikc_map_body_result() while preserving the public
  set_ikc_map operation pointer and the matching no-Rust fallback body. This
  covers request copyin, IKC request field extraction, request validation,
  initial-status lock/unlock gating, source/destination scratch allocation,
  source/destination copyin, request-string formatting failure log dispatch,
  per-pair bounds validation, source-assigned/destination-unassigned,
  destination-present, and destination-online validation, IKC map publication,
  check-map callback dispatch and cpu_ikc_mapped publication, assigned-route
  logging, error-path default-map restore, and scratch free ordering. The
  strict path also quarantines the old C ikc_array2str() and
  validate_ikc_req() helpers behind the no-Rust fallback. C remains responsible
  for the public operation entry point, actual copy_from_user(), kmalloc(),
  kfree(), spin_lock_irqsave()/spin_unlock_irqrestore(), cpu_present(),
  cpu_online(), check-map primitive side effects, printk formatting, non-Rust
  fallback mapping, and remaining compiled bridge/fallback source, so the row
  remains partial rather than fully retired. Final validation passed for
  rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct IKC
  set-map success, busy cleanup, invalid request, source-unassigned,
  destination-assigned, and destination-not-present coverage plus
  smp_chunk_helpers digest 6c739a4e98e62ede, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4, no-Rust module
  fallback cmake --build /tmp/mckernel-rocky-module-cfallback --target
  ihk-smp-x86_64_ko -j4, symbol checks showing strict smp-driver.o imports
  ihk_smp_os_set_ikc_map_body_result while smp_driver_helpers.o exports it
  and fallback smp-driver.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit. Final strict JSON audit reports host/IKC/mcctrl/IHK modules
  completion 99.823875%, overall scored source-retirement completion
  99.8468656716418%, 0.15313432835820606 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 97.15% to
  97.30% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_ihk_os_get_ikc_map() body sequencing through
  ihk_smp_os_get_ikc_map_body_result() while preserving the public
  get_ikc_map operation pointer and the matching no-Rust fallback body. This
  covers request copyin, IKC request field extraction, request validation,
  source/destination scratch allocation, source/destination copyin, assigned
  CPU traversal and OS/status filtering through the existing Rust pack helper,
  capacity overflow error shaping, packed source/destination copyout, result
  count copyout, log callback dispatch for copy/allocation/packing failures,
  and scratch free ordering. C remains responsible for the public operation
  entry point, actual copy_from_user()/copy_to_user() side effects,
  kmalloc()/kfree(), printk formatting, CPU assignment mutation, non-Rust
  fallback packing, and remaining compiled bridge/fallback source, so the row
  remains partial rather than fully retired. Final validation passed for
  rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  IKC-map success, capacity overflow, copyout failure, and zero-result
  coverage plus smp_chunk_helpers digest e20bf266bd242b4a, Rust-enabled
  cmake --build /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4,
  no-Rust module fallback cmake --build /tmp/mckernel-rocky-module-cfallback
  --target ihk-smp-x86_64_ko -j4, symbol checks showing strict smp-driver.o
  imports ihk_smp_os_get_ikc_map_body_result while smp_driver_helpers.o
  exports it and fallback smp-driver.o has no dependency on that helper,
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.818875%, overall
  scored source-retirement completion 99.84537313432836%,
  0.15462686567164496 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 97.05% to
  97.15% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_ihk_irq_call_handlers() traversal sequencing
  through ihk_smp_irq_call_handlers_body_result() while preserving the public
  IRQ handler entry point and the matching no-Rust fallback body. This covers
  builtin handler list-head traversal, list-entry-to-handler pointer shaping,
  non-null handler callback dispatch with stored os/os_priv/priv arguments,
  found-handler tracking, no-handler logging callback dispatch, IRQ_HANDLED
  return shaping, and invalid direct-helper argument rejection in the fixture.
  C remains responsible for register/unregister list mutation, the public IRQ
  entry point, actual registered handler side effects, kprintf formatting,
  non-Rust fallback traversal, and remaining compiled bridge/fallback source,
  so the row remains partial rather than fully retired. Final validation passed
  for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  IRQ-handler traversal, empty-list logging, and invalid-argument coverage plus
  smp_chunk_helpers digest 065b7e2f771572d9, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4, no-Rust module
  fallback cmake --build /tmp/mckernel-rocky-module-cfallback --target
  ihk-smp-x86_64_ko -j4, symbol checks showing strict smp-driver.o imports
  ihk_smp_irq_call_handlers_body_result while smp_driver_helpers.o exports it
  and fallback smp-driver.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit. Final strict JSON audit reports host/IKC/mcctrl/IHK modules
  completion 99.817%, overall scored source-retirement completion
  99.84481343283582%, 0.15518656716417922 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-17: ihk/linux/driver/smp/smp-driver.c moved from 97.0% to
  97.05% for the strict source-retirement audit. Rust-enabled ihk-smp-x86_64
  now routes the Linux-work IRQ callback wrapper body through
  ihk_smp_ikc_irq_work_body_result() while preserving the public
  smp_ihk_ikc_irq_work_func() entry point and the matching no-Rust fallback
  body. This covers dispatcher callback invocation with IRQ 0 and null work
  data plus missing-callback rejection in the direct fixture. C remains
  responsible for the public Linux irq_work entry point, actual
  smp_ihk_irq_call_handlers() side effects, non-Rust fallback dispatch, and
  remaining compiled bridge/fallback source, so the row remains partial rather
  than fully retired. Final validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  IRQ-work success and null-callback coverage plus smp_chunk_helpers digest
  cdbac3f000eb0d9d, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4,
  symbol checks showing strict smp-driver.o imports
  ihk_smp_ikc_irq_work_body_result while smp_driver_helpers.o exports it and
  fallback smp-driver.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit. Final strict JSON audit reports host/IKC/mcctrl/IHK modules
  completion 99.81575000000001%, overall scored source-retirement completion
  99.84444029850745%, 0.1555597014925496 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 97.85% to 97.95% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 with Linux-work IRQs now quarantines the legacy
  smp_ihk_arch_init() C tail behind the no-Rust or alternate-configuration
  path after the Rust-owned ihk_smp_arch_init_linux_work_tail_body_result()
  return. This removes an otherwise compiled strict-build C error-label and
  vector-allocation tail that cannot execute in the current Rust/Linux-work
  configuration, while preserving the no-Rust fallback and non-Linux-work IRQ
  behavior. C remains responsible for the public arch-init entry point,
  trampoline primitive callbacks, the alternate IRQ-vector scan/request/set
  path, arch-exit cleanup, and remaining compiled bridge/fallback source, so
  the row remains partial rather than fully retired. Final validation passed
  for bash -n kernel/rust/tests/run_equivalence.sh, scoped git diff --check,
  git -C ihk diff --check, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4, no-Rust module
  fallback cmake --build /tmp/mckernel-rocky-module-cfallback --target
  ihk-smp-x86_64_ko -j4, full kernel/rust/tests/run_equivalence.sh with
  smp_chunk_helpers digest 9d00f0d3688006a8, broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit. Final strict JSON audit reports host/IKC/mcctrl/IHK modules
  completion 99.815125%, overall scored source-retirement completion
  99.84425373134327%, 0.1557462686567277 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 97.8% to 97.85% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 no longer compiles the old C
  ihk_smp_trampoline_page_usable() helper; that helper is now quarantined to
  the no-Rust fallback path, while the strict path uses the already exported
  Rust ihk_smp_trampoline_page_usable_result() logic through the Rust-owned
  arch-init trampoline helper. This removes a stale strict-build C execution
  body and eliminates the Rust-enabled module's unused-function warning without
  changing fallback behavior. C remains responsible for the no-Rust fallback
  trampoline allocation loop, page_to_phys() primitive behavior, and remaining
  compiled bridge/fallback source, so the row remains partial rather than fully
  retired. Final validation passed for bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4, full
  kernel/rust/tests/run_equivalence.sh with smp_chunk_helpers digest
  9d00f0d3688006a8, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.81387500000001%,
  overall scored source-retirement completion 99.84388059701493%,
  0.15611940298506966 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 97.5% to 97.8% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes collect_topology() top-level traversal sequencing
  through ihk_smp_collect_topology_body_result() while preserving the matching
  no-Rust C fallback body. This covers start logging, online-CPU iterator
  callback sequencing, per-CPU topology collection dispatch, CPU collection
  error logging and return shaping, online-node iterator callback sequencing,
  per-node topology collection dispatch, node collection error logging and
  return shaping, and final result logging. C remains responsible for the
  static public helper entry point, Linux cpumask/node iterator primitives,
  collect_cpu_topology() and collect_node_topology() side effects, topology
  list allocation and mutation callbacks, sysfs read callbacks, printk side
  effects, caller arch-init ordering, arch-exit cleanup, and remaining compiled
  bridge/fallback source, so the row remains partial rather than fully retired.
  Final validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  collect_topology success, CPU-error, node-error, and null-callback coverage
  plus smp_chunk_helpers digest 9d00f0d3688006a8, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4, no-Rust module
  fallback cmake --build /tmp/mckernel-rocky-module-cfallback --target
  ihk-smp-x86_64_ko -j4, symbol checks showing strict smp-arch-driver.o
  imports ihk_smp_collect_topology_body_result while smp_driver_helpers.o
  exports it and fallback smp-arch-driver.o has no dependency on that helper,
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.81325000000001%,
  overall scored source-retirement completion 99.84369402985075%,
  0.15630597014924774 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 97.0% to 97.5% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_ihk_init_ident_page_table() identity
  page-table initialization sequencing through
  ihk_smp_init_ident_page_table_body_result() while preserving the matching
  no-Rust C fallback body. This covers identity page-count/order calculation,
  order publication, allocation failure logging and positive ENOMEM return
  shaping, identity page-table physical and virtual address publication,
  zeroing dispatch, top-level PUD entry publication, PUD/PAGE_COUNT mismatch
  logging, PMD leaf physical-address calculation, max-memory mapping gates,
  and final success logging. C remains responsible for the static public helper
  entry point, alloc_pages(), page_to_phys(), page_to_virt(), memset(), printk
  side effects, caller arch-init ordering, topology collection, arch-exit
  cleanup, and remaining compiled bridge/fallback source, so the row remains
  partial rather than fully retired. Final validation passed for rustfmt
  --edition 2021 over ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct identity
  page-table success, allocation-failure, and PUD mismatch logging coverage
  plus smp_chunk_helpers digest a83b4b11940ff235, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4, no-Rust module
  fallback cmake --build /tmp/mckernel-rocky-module-cfallback --target
  ihk-smp-x86_64_ko -j4, symbol checks showing strict smp-arch-driver.o
  imports ihk_smp_init_ident_page_table_body_result while smp_driver_helpers.o
  exports it and fallback smp-arch-driver.o has no dependency on that helper,
  broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.8095%, overall
  scored source-retirement completion 99.84257462686567%,
  0.15742537313433047 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 96.7% to 97.0% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes the Linux-work-IRQ tail of smp_ihk_arch_init()
  through ihk_smp_arch_init_linux_work_tail_body_result() while preserving the
  matching no-Rust C fallback body and leaving the alternate non-Linux-work-IRQ
  vector-allocation path in C. This covers Linux work IRQ selection logging,
  identity page-table initialization callback dispatch, identity-init error
  logging and return propagation, topology collection callback dispatch,
  topology error logging and return propagation, and the success return after
  both initialization steps pass. C remains responsible for the public
  arch-init entry point, actual identity page-table allocation/mapping,
  topology sysfs read/list allocation side effects, the alternate IRQ-vector
  scan/request/set-vector path, trampoline selection primitive callbacks,
  arch-exit cleanup, and remaining compiled bridge/fallback source, so the row
  remains partial rather than fully retired. Final validation passed for
  rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  Linux-work tail success, identity-init failure, and topology failure coverage
  plus smp_chunk_helpers digest 5008314097e7caf5, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4, no-Rust module
  fallback cmake --build /tmp/mckernel-rocky-module-cfallback --target
  ihk-smp-x86_64_ko -j4, symbol checks showing strict smp-arch-driver.o
  imports ihk_smp_arch_init_linux_work_tail_body_result while
  smp_driver_helpers.o exports it and fallback smp-arch-driver.o has no
  dependency on that helper, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.80325%, overall
  scored source-retirement completion 99.84070895522387%,
  0.15929104477612555 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 96.2% to 96.7% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes the trampoline selection and publication portion
  of smp_ihk_arch_init() through ihk_smp_arch_init_trampoline_body_result()
  while preserving the matching no-Rust C fallback body. This covers
  preallocated trampoline physical-address logging and ioremap dispatch,
  bounded low-memory trampoline allocation attempts, page-physical usability
  checks against the trampoline limit, rejected-page free dispatch, Linux
  real-mode trampoline fallback selection, Linux/direct virtual address
  publication, allocated-page virtual address publication, using-Linux flag
  publication, and final trampoline-physical logging. C remains responsible
  for the public arch-init entry point, actual ioremap_cache(), alloc_pages(),
  page_to_phys(), pfn_to_kaddr(), free_pages(), __va(), printk side effects,
  later interrupt-vector setup, identity page-table setup, topology
  collection, arch-exit resource cleanup, and remaining compiled
  bridge/fallback source, so the row remains partial rather than fully
  retired. Final validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  arch-init trampoline preallocated, one-bad-page allocation success, and
  Linux-fallback coverage plus smp_chunk_helpers digest d3d828c86a2581aa,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4,
  symbol checks showing strict smp-arch-driver.o imports
  ihk_smp_arch_init_trampoline_body_result while smp_driver_helpers.o exports
  it and fallback smp-arch-driver.o has no dependency on that helper, broad
  Rocky Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit. The focused fixture failures for this slice are recorded
  in kernel.log and were corrected before scoring; they were object-symbol
  hygiene, C-model self-containment, and fake ioremap arithmetic issues rather
  than Rust/C behavior divergence. Final strict JSON audit reports
  host/IKC/mcctrl/IHK modules completion 99.7995%, overall scored
  source-retirement completion 99.8395895522388%,
  0.16041044776119406 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 95.5% to 96.2% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_ihk_os_setup_startup() startup page-table,
  startup-code mapping, and boot-entry publication sequencing through
  ihk_smp_os_setup_startup_body_result() while preserving the matching no-Rust
  C fallback body. This covers boot page-table allocation and publication,
  identity mapping, straight-map startup mapping, kernel image mapping,
  bootstrap stack and startup physical address calculation, startup virtual
  mapping, startup-code copy, boot page-table physical-address publication,
  startup slot publication for stack, kernel physical base, trampoline physical
  address, and entry point, startup unmap dispatch, and os->boot_rip
  publication. C remains responsible for the public setup entry point,
  get_zeroed_page()/__pa() primitive behavior, ihk_smp_map_kernel(),
  ihk_smp_map_virtual(), ihk_smp_unmap_virtual(), printk side effects, startup
  and trampoline storage symbols, APIC wakeup/reset primitive behavior, arch
  init/exit resource mutation, and remaining compiled bridge/fallback source,
  so the row remains partial rather than fully retired. Final validation passed
  for rustfmt --edition 2021 over ihk/linux/driver/smp/rust/smp_driver_helpers.rs,
  bash -n kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git
  -C ihk diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  startup setup success, allocation failure, map failure, and startup-map
  failure coverage plus smp_chunk_helpers digest dfaff593bae7c9dc,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target
  ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4,
  symbol checks showing strict smp-arch-driver.o imports
  ihk_smp_os_setup_startup_body_result while smp_driver_helpers.o exports it
  and fallback smp-arch-driver.o has no dependency on that helper, broad Rocky
  Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit. Two focused equivalence fixture failures for this slice
  are recorded in kernel.log and were corrected before scoring; both were fake
  boot page-table physical-address arithmetic errors rather than helper
  sequencing mismatches. Final strict JSON audit reports host/IKC/mcctrl/IHK
  modules completion 99.79325%, overall scored source-retirement completion
  99.83772388059701%, 0.16227611940298914 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 95.1% to 95.5% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_ihk_setup_trampoline() boot-parameter and
  trampoline-header setup sequencing through ihk_smp_setup_trampoline_body_result()
  while preserving the matching no-Rust C fallback body. This covers per-Linux
  CPU APIC-ID publication, optional Linux-work-IRQ raised-list physical-address
  publication, IKC IRQ/work-function selection, page-offset and Linux page-table
  physical-address publication, Linux trampoline backup copy, IHK trampoline
  code copy, and trampoline header page-table/next-IP/notify-address publication.
  C remains responsible for the public setup entry point, per_cpu() access,
  virt_to_phys()/__pa() primitive behavior, dprintf side effects, supplied
  trampoline and page-table storage, APIC wakeup/reset primitive behavior,
  startup page-table construction, arch init/exit resource mutation, and
  remaining compiled bridge/fallback source, so the row remains partial rather
  than fully retired. Final validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, git -C ihk
  diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  setup-trampoline Linux-work-IRQ and legacy IRQ coverage plus smp_chunk_helpers
  digest a33fb988cd9f0da0, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihk-smp-x86_64_ko -j4, no-Rust module fallback cmake --build
  /tmp/mckernel-rocky-module-cfallback --target ihk-smp-x86_64_ko -j4, symbol
  checks showing strict smp-arch-driver.o imports
  ihk_smp_setup_trampoline_body_result while smp_driver_helpers.o exports it
  and fallback smp-arch-driver.o has no dependency on that helper, broad Rocky
  Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit. The first equivalence run for this slice failed because
  the new fixture compared the whole trampoline buffer after intentional header
  publication and undersized one fake trampoline buffer; that failure is
  recorded in kernel.log and was corrected before scoring. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.78450000000001%,
  overall scored source-retirement completion 99.8351119402985%,
  0.16488805970149656 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 95.0% to 95.1% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smpboot_setup_warm_reset_vector() warm-reset vector
  setup sequencing through ihk_smp_setup_warm_reset_vector_body_result() while
  preserving the matching no-Rust C fallback body. This covers RTC lock
  acquisition and flag return, CMOS warm-reset shutdown-code write ordering,
  RTC unlock dispatch, local TLB flush callback ordering, and high/low warm
  reset vector word publication from start_eip. C remains responsible for the
  static C entry point, rtc_lock and CMOS primitive side effects,
  spin-lock/IRQ primitive behavior, phys_to_virt() and volatile memory writes
  through the bridge callback, APIC wakeup/reset primitive behavior, trampoline
  setup, page-table allocation/free, arch init/exit resource mutation, and
  remaining compiled bridge/fallback source, so the row remains partial rather
  than fully retired. Final validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct warm-reset lock, CMOS,
  unlock, TLB-flush, and vector-word publication coverage plus
  smp_chunk_helpers digest fed7537b0177bafa, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4, no-Rust module
  fallback cmake --build /tmp/mckernel-rocky-module-cfallback --target
  ihk-smp-x86_64_ko -j4, symbol checks showing strict smp-arch-driver.o
  imports ihk_smp_setup_warm_reset_vector_body_result while
  smp_driver_helpers.o exports it and fallback smp-arch-driver.o has no
  dependency on that helper, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.77950000000001%,
  overall scored source-retirement completion 99.83361940298508%,
  0.16638059701492125 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 94.8% to 95.0% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_wakeup_secondary_cpu() top-level selector
  sequencing through ihk_smp_wakeup_secondary_cpu_body_result() while
  preserving the matching no-Rust C fallback body. This covers old-kernel
  init-deasserted reset callback dispatch, UV non-unique-APIC policy
  dispatch, warm-reset vector setup callback ordering, boot-CPU APIC-integrated
  ESR clear/read sequencing, custom apic->wakeup_secondary_cpu availability
  and call dispatch, fallback smp_wakeup_secondary_cpu_via_init() selection,
  preempt disable/enable ordering around the INIT fallback, return propagation,
  and top-level diagnostic dispatch. C remains responsible for the exported
  wakeup entry point, actual warm-reset vector mutation, UV/APIC primitive
  behavior, APIC macro/version-table access, preempt primitive side effects,
  the APIC custom wakeup callback body, lower INIT/STARTUP primitive side
  effects, reset CPU and arch-exit public wrappers, arch init/exit resource
  mutation, interrupt vector teardown, page-table allocation/free, trampoline
  mapping, and remaining compiled bridge/fallback source, so the row remains
  partial rather than fully retired. Final validation passed for rustfmt
  --edition 2021 over ihk/linux/driver/smp/rust/smp_driver_helpers.rs,
  bash -n kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct selector unique-APIC
  INIT-fallback, non-unique custom APIC wakeup, and unique custom APIC wakeup
  coverage plus smp_chunk_helpers digest 6611d152375a510f, Rust-enabled
  cmake --build /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4,
  no-Rust module fallback cmake --build /tmp/mckernel-rocky-module-cfallback
  --target ihk-smp-x86_64_ko -j4, symbol checks showing strict
  smp-arch-driver.o imports ihk_smp_wakeup_secondary_cpu_body_result while
  smp_driver_helpers.o exports it and fallback smp-arch-driver.o has no
  dependency on that helper, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. The first
  equivalence attempt for this slice failed during generated C-model
  compilation because the SMP harness lacked a local ENOSYS definition; that
  failure is recorded in kernel.log and was corrected before scoring. Final
  strict JSON audit reports host/IKC/mcctrl/IHK modules completion 99.77825%,
  overall scored source-retirement completion 99.83324626865671%,
  0.16675373134329163 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 94.4% to 94.8% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes the active smp_wakeup_secondary_cpu_via_init()
  INIT/STARTUP IPI sequencing through
  ihk_smp_wakeup_secondary_cpu_via_init_body_result() while preserving the
  matching no-Rust C fallback body. This covers LAPIC max-LVT query ordering,
  APIC-integrated detection, conditional ESR clear/read sequencing, INIT
  assert/deassert ICR writes, send-idle wait ordering, 10 ms deassert delay,
  memory-barrier and old-kernel init-deasserted callback dispatch, integrated
  APIC STARTUP loop selection, per-loop ESR clear/read ordering, STARTUP ICR
  vector shaping from start_eip, 300 us and 200 us delay ordering, send-status
  and accept-status break/return shaping, and APIC delivery error logging
  dispatch. C remains responsible for the public wakeup entry point, warm-reset
  vector setup, UV/APIC wakeup policy selection, preempt disable/enable around
  the wakeup call, APIC macro/version-table access, actual APIC/delay/barrier
  primitive side effects, printk routing, reset CPU and arch-exit public
  wrappers, arch init/exit resource mutation, interrupt vector teardown,
  page-table allocation/free, trampoline mapping, and remaining compiled
  bridge/fallback source, so the row remains partial rather than fully retired.
  Final validation passed for rustfmt --edition 2021 over
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct wakeup integrated
  two-STARTUP success, non-integrated no-STARTUP, send-error, and accept-error
  coverage plus smp_chunk_helpers digest b2680fe4c17917ea, Rust-enabled
  cmake --build /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4,
  no-Rust module fallback cmake --build /tmp/mckernel-rocky-module-cfallback
  --target ihk-smp-x86_64_ko -j4, symbol checks showing strict
  smp-arch-driver.o imports ihk_smp_wakeup_secondary_cpu_via_init_body_result
  while smp_driver_helpers.o exports it and fallback smp-arch-driver.o has no
  dependency on that helper, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit. Final strict JSON
  audit reports host/IKC/mcctrl/IHK modules completion 99.77575%, overall
  scored source-retirement completion 99.8325%, 0.16750000000000398
  percentage points left, 20 debt rows, compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  and headers_with_executable_logic 0. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 94.2% to 94.4% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes the active smp_ihk_arch_exit() lower resource
  cleanup sequence through ihk_smp_arch_exit_body_result() while preserving the
  no-Rust C fallback body and retaining the non-Linux-work-IRQ vector teardown
  block in C for that alternate configuration. This covers trampoline-page
  versus Linux-trampoline branch selection, trampoline-page free callback
  dispatch, iounmap callback dispatch for non-Linux trampoline mappings,
  identity page-table free gating, and cleanup ordering before return. C
  remains responsible for the public arch-exit entry point, actual free_pages()
  and iounmap() primitive side effects, page-to-PFN/KVA conversion, IRQ vector
  release and sparse-IRQ handler restoration in non-Linux-work-IRQ builds,
  trampoline allocation/setup, identity page-table allocation/setup, arch init
  sequencing, and remaining compiled bridge/fallback source, so the row remains
  partial rather than fully retired. Final validation passed for rustfmt
  --edition 2021 over ihk/linux/driver/smp/rust/smp_driver_helpers.rs,
  bash -n kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct arch-exit allocated
  trampoline, Linux-trampoline, and iounmap fallback coverage plus
  smp_chunk_helpers digest 8bba528f0db67163, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4, no-Rust module
  fallback cmake --build /tmp/mckernel-rocky-module-cfallback --target
  ihk-smp-x86_64_ko -j4, symbol checks showing strict smp-arch-driver.o
  imports ihk_smp_arch_exit_body_result while smp_driver_helpers.o exports it
  and fallback smp-arch-driver.o has no dependency on that helper, broad Rocky
  Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit. Final strict JSON audit reports host/IKC/mcctrl/IHK
  modules completion 99.77074999999999%, overall scored source-retirement
  completion 99.83100746268656%, 0.16899253731344288 percentage points left,
  20 debt rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale
  tracker rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-17: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 94.0% to 94.2% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes ihk_smp_reset_cpu() body sequencing through
  ihk_smp_reset_cpu_body_result() while preserving the no-Rust C fallback body.
  This covers preempt disable/enable callback ordering, reset-start and INIT
  diagnostic dispatch, LAPIC max-LVT query ordering, APIC-integrated
  detection dispatch, conditional APIC ESR clear/read sequencing, assert and
  deassert INIT ICR writes, send-idle wait ordering, and the 10 ms reset delay.
  C remains responsible for the public reset entry point, APIC macro and
  version-table access, actual preempt/APIC/delay primitive side effects,
  printk routing, secondary CPU startup sequencing, arch init/exit resource
  mutation, interrupt vector teardown, page-table allocation/free, trampoline
  mapping, and remaining compiled bridge/fallback source, so the row remains
  partial rather than fully retired. Final validation passed for rustfmt
  --edition 2021 over ihk/linux/driver/smp/rust/smp_driver_helpers.rs,
  bash -n kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct reset CPU APIC-integrated
  max-LVT, integrated-no-ESR-write, and non-integrated coverage plus
  smp_chunk_helpers digest 85b1e99760a568f2, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target ihk-smp-x86_64_ko -j4, no-Rust module
  fallback cmake --build /tmp/mckernel-rocky-module-cfallback --target
  ihk-smp-x86_64_ko -j4, symbol checks showing strict smp-arch-driver.o
  imports ihk_smp_reset_cpu_body_result while smp_driver_helpers.o exports it
  and fallback smp-arch-driver.o has no dependency on that helper, broad Rocky
  Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit. Final strict JSON audit reports host/IKC/mcctrl/IHK
  modules completion 99.76825%, overall scored source-retirement completion
  99.83026119402984%, 0.16973880597015523 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: executer/user/mcexec.c moved from 99.0% to
  99.5% for the strict source-retirement audit. Rust-enabled mcexec now routes
  util_thread() body sequencing through mcexec_util_thread_body() while
  preserving the no-Rust C fallback body. This covers UTI descriptor validation,
  UTI worker barrier setup and worker creation dispatch, syscall-intercept probe
  warning dispatch, MCEXEC_UP_UTI_GET_CTX request construction and errno
  shaping, UTI descriptor fd/thread/key/pid/tid/info publication, syscall
  parameter-size validation, optional MCEXEC_UP_UTI_ATTR request construction
  from UTI_CPU_SET, start_syscall_intercept publication, switch-context request
  construction, switch failure shaping, and unexpected switch-return shaping.
  C remains responsible for the public sched_setaffinity action entry point,
  actual pthread barrier/thread primitive side effects, actual ioctl/syscall/
  getenv/strlen/getpid/gettid primitive side effects, architecture switch_ctx()
  behavior, main() orchestration, and remaining compiled bridge/fallback source,
  so the row remains partial rather than fully retired. Final validation passed
  for rustfmt --edition 2021 over executer/user/rust/mcexec_helpers.rs,
  bash -n kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct util-thread missing-descriptor,
  worker-create failure, get-context failure, parameter-too-large, attr-failure,
  switch-failure, and switch-return coverage plus mcexec_helpers digest
  1fb7d9f17642504c, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mcexec -j4, no-Rust user fallback cmake --build
  /tmp/mckernel-rocky-user-cfallback --target mcexec -j4, symbol checks showing
  strict mcexec.c.o imports mcexec_util_thread_body while mcexec_helpers.o
  exports it and fallback mcexec.c.o has no dependency on that helper, broad
  Rocky Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit. Final strict JSON audit reports User tools completion
  99.91206896551724%, overall scored source-retirement completion
  99.82951492537313%, 0.17048507462686757 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/user/ihklib.c moved from 99.98% to
  99.99% for the strict source-retirement audit. Rust-enabled ihklib now
  routes ihk_os_release_mem() body sequencing through
  ihklib_os_release_mem_body_result() while preserving the no-Rust C fallback
  body. This covers OS readability dispatch, memory request validation,
  zero-count no-op handling, normal release request fill sequencing,
  release-all count dispatch, release-all temporary chunk allocation,
  release-all query callback dispatch and rebinding, sizes/NUMA-id allocation
  and free ordering, request struct binding, open/ioctl/close ordering, errno
  shaping for release ioctl failure, and diagnostic side-channel publication
  for the C wrapper. C remains responsible for the public ABI entry point,
  actual access/open/ioctl/close/errno/calloc/free primitive side effects, IKC
  map assignment/query, directory iteration, sysfs/file I/O, logging, and
  remaining compiled bridge/fallback source, so the row remains partial rather
  than fully retired. Final validation passed for rustfmt --edition 2021
  --check over ihk/linux/user/rust/ihklib_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct release memory body normal
  success, zero-count no-op, validation failure, release-all success,
  release-all count failure, release-all query failure, open failure, and ioctl
  failure coverage plus ihklib_helpers digest 3a9672bac42fcb62,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target ihklib -j4,
  no-Rust user fallback cmake --build /tmp/mckernel-rocky-user-cfallback
  --target ihklib -j4, symbol checks showing strict ihklib.c.o imports
  ihklib_os_release_mem_body_result while ihklib_helpers.o exports it and
  fallback ihklib.c.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit. Final strict JSON audit reports host/IKC/mcctrl/IHK modules
  completion 99.76575%, overall scored source-retirement completion
  99.82764925373134%, 0.17235074626866265 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-17: ihk/linux/user/ihklib.c moved from 99.95% to
  99.98% for the strict source-retirement audit. Rust-enabled ihklib now
  routes ihk_os_query_mem() body sequencing through
  ihklib_os_query_mem_body_result() while preserving the no-Rust C fallback
  body. This covers OS readability dispatch, memory request validation,
  assigned-memory count dispatch, actual/requested count mismatch shaping,
  zero-count return behavior, sizes/NUMA-id allocation and free ordering,
  query request struct binding, open/ioctl/close ordering, errno shaping for
  query ioctl failure, memory-chunk publication, and diagnostic side-channel
  publication for the C wrapper. C remains responsible for the public ABI
  entry point, actual access/open/ioctl/close/errno/calloc/free primitive side
  effects, memory release bodies including release-all query rebinding, IKC map
  assignment/query, directory iteration, sysfs/file I/O, logging, and remaining
  compiled bridge/fallback source, so the row remains partial rather than fully
  retired. Final validation passed for rustfmt --edition 2021 --check over
  ihk/linux/user/rust/ihklib_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct memory query body success,
  zero-count, count-mismatch, count-failure, validation-failure, open-failure,
  and ioctl-failure coverage plus ihklib_helpers digest 17493674713354ef,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target ihklib -j4,
  no-Rust user fallback cmake --build /tmp/mckernel-rocky-user-cfallback
  --target ihklib -j4, symbol checks showing strict ihklib.c.o imports
  ihklib_os_query_mem_body_result while ihklib_helpers.o exports it and
  fallback ihklib.c.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit. Final strict JSON audit reports host/IKC/mcctrl/IHK modules
  completion 99.765625%, overall scored source-retirement completion
  99.82761194029851%, 0.17238805970148974 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-17: ihk/linux/user/ihklib.c moved from 99.9% to
  99.95% for the strict source-retirement audit. Rust-enabled ihklib now
  routes ihk_os_assign_mem() request-body sequencing through
  ihklib_os_mem_request_body_result() while preserving the no-Rust C fallback
  body. This covers OS readability dispatch, memory request validation,
  zero-count no-op handling, sizes/NUMA-id allocation and free ordering,
  chunk-to-request fill sequencing, request struct binding, open/ioctl/close
  ordering, errno shaping for ioctl failure, and diagnostic side-channel
  publication for the C wrapper. C remains responsible for the public ABI
  entry point, actual access/open/ioctl/close/errno/calloc/free primitive side
  effects, memory query/release bodies, IKC map assignment/query, directory
  iteration, sysfs/file I/O, logging, and remaining compiled bridge/fallback
  source, so the row remains partial rather than fully retired. Final
  validation passed for rustfmt --edition 2021 --check over
  ihk/linux/user/rust/ihklib_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct memory assign body success,
  zero-count no-op, validation failure, open failure, and ioctl failure
  coverage plus ihklib_helpers digest fd6a4e70e87ef75d, Rust-enabled
  cmake --build /tmp/mckernel-rocky-rust --target ihklib -j4, no-Rust user
  fallback cmake --build /tmp/mckernel-rocky-user-cfallback --target ihklib
  -j4, symbol checks showing strict ihklib.c.o imports
  ihklib_os_mem_request_body_result while ihklib_helpers.o exports it and
  fallback ihklib.c.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit. Final strict JSON audit reports host/IKC/mcctrl/IHK modules
  completion 99.76525000000001%, overall scored source-retirement completion
  99.8275%, 0.17249999999999943 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-17: ihk/linux/user/ihklib.c moved from 99.85% to
  99.9% for the strict source-retirement audit. Rust-enabled ihklib now routes
  ihk_os_query_cpu() body sequencing through ihklib_os_query_cpu_body_result()
  while preserving the no-Rust C fallback body. This covers OS readability
  dispatch, CPU request validation, OS open ordering, assigned-CPU count ioctl
  dispatch, count-error errno shaping, actual/requested count mismatch
  shaping, zero-count close-and-return behavior, query request struct binding,
  query ioctl dispatch, query-error errno shaping, and close ordering. C
  remains responsible for the public ABI entry point, actual
  access/open/ioctl/close/errno primitive side effects, CPU assign/release
  primitive callbacks, IKC map assignment/query, memory assign/query/release
  bodies, directory iteration, sysfs/file I/O, logging, and remaining compiled
  bridge/fallback source, so the row remains partial rather than fully retired.
  Final validation passed for rustfmt --edition 2021 --check over
  ihk/linux/user/rust/ihklib_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct query CPU body success,
  zero-count, count-mismatch, count-failure, and query-failure coverage plus
  ihklib_helpers digest e5c338ec0b281a6e, Rust-enabled cmake --build
  /tmp/mckernel-rocky-rust --target ihklib -j4, no-Rust user fallback
  cmake --build /tmp/mckernel-rocky-user-cfallback --target ihklib -j4,
  symbol checks showing strict ihklib.c.o imports
  ihklib_os_query_cpu_body_result while ihklib_helpers.o exports it and
  fallback ihklib.c.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit. Final strict JSON audit reports host/IKC/mcctrl/IHK modules
  completion 99.764625%, overall scored source-retirement completion
  99.82731343283582%, 0.17268656716417752 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-17: ihk/linux/user/ihklib.c moved from 99.8% to
  99.85% for the strict source-retirement audit. Rust-enabled ihklib now
  routes ihk_os_assign_cpu() and ihk_os_release_cpu() request-body sequencing
  through ihklib_os_cpu_request_body_result() while preserving the no-Rust C
  fallback bodies. This covers OS readability dispatch, CPU request
  validation including zero-count no-op handling, request struct binding,
  open/ioctl/close ordering, errno shaping for ioctl failures, and diagnostic
  side-channel publication for the C wrapper. C remains responsible for the
  public ABI entry points, actual access/open/ioctl/close/errno primitive side
  effects, ihk_os_query_cpu(), IKC map assignment/query, memory
  assign/query/release bodies, directory iteration, sysfs/file I/O, logging,
  and remaining compiled bridge/fallback source, so the row remains partial
  rather than fully retired. An initial symbol assertion for this slice used
  /tmp/mckernel-rocky-c-fallback as the no-Rust user fallback build tree; that
  tree still enables IHKLIB_RUST_HELPERS for ihklib, so the failed assertion is
  recorded in kernel.log and was corrected by using the dedicated
  /tmp/mckernel-rocky-user-cfallback tree. Final validation passed for
  rustfmt --edition 2021 --check over
  ihk/linux/user/rust/ihklib_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct CPU request body success,
  zero-count, readable-failure, validation-failure, open-failure, ioctl-failure,
  and release-request coverage plus ihklib_helpers digest accb618f12e3d5ec,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target ihklib -j4,
  no-Rust user fallback cmake --build /tmp/mckernel-rocky-user-cfallback
  --target ihklib -j4, symbol checks showing strict ihklib.c.o imports
  ihklib_os_cpu_request_body_result while ihklib_helpers.o exports it and
  fallback ihklib.c.o has no dependency on that helper, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit. Final strict JSON audit reports host/IKC/mcctrl/IHK modules
  completion 99.76400000000001%, overall scored source-retirement completion
  99.82712686567163%, 0.17287313432836982 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-17: arch/x86_64/kernel/cpu.c moved from 98.5% to
  98.6% for the strict source-retirement audit. Rust-enabled x86 now routes
  set_kstack() CPU-local kernel_stack and TSS rsp0 publication through
  x86_set_kstack_body_result() while preserving the no-Rust C fallback body.
  This covers null-local rejection in the helper, offset-based field writes,
  and preservation of the caller-selected stack pointer. C remains responsible
  for CPU-local pointer selection, low-level stack switching, AP bootstrap and
  interrupt/NMI bodies, FPU save/restore, low-level CPU interrupt/halt
  primitives, and remaining compiled bridge/fallback source, so the row
  remains partial rather than fully retired. The first full equivalence run for
  this slice failed at the kernel-module relocation gate because the existing
  dirty mcctrl Rust helper object still emitted R_X86_64_GOTPCREL relocations
  against memset; the failure is recorded in kernel.log and was corrected by
  replacing mcctrl_helpers.rs byte-clearing sites with an explicit volatile
  byte-zero loop before scoring this x86 movement. Final validation passed for
  rustfmt --edition 2021 --check over kernel/rust/x86_cpu_helpers.rs and
  executer/kernel/mcctrl/rust/mcctrl_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct set-kstack success,
  invalid-input, and mcctrl module-object relocation coverage plus
  x86_cpu_helpers digest 118edc4c7b3fe378 and mcctrl_helpers digest
  a9f1f632553a2480, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target mckernel.img mcctrl_ko -j4, no-Rust C fallback cmake --build
  /tmp/mckernel-rocky-c-fallback --target mckernel.img -j4,
  no-Rust module fallback cmake --build /tmp/mckernel-rocky-module-cfallback
  --target mcctrl_ko -j4, symbol checks showing strict cpu.c.o imports
  x86_set_kstack_body_result while mckernel_rust.o exports it and fallback
  cpu.c.o has no dependency on that helper, explicit mcctrl_helpers.o checks
  showing no undefined memset and no R_X86_64_GOTPCREL relocations, broad Rocky
  Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit. Final strict JSON audit reports overall scored
  source-retirement completion 99.82694029850745%,
  0.1730597014925479 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/user/ihklib.c moved from 99.6% to
  99.8% for the strict source-retirement audit. Rust-enabled ihklib now routes
  os_assign_cpu_all(), os_assign_mem_all(), os_release_cpu_all(),
  release_cpu_all(), the ihk_create_os_str() pre-clean CPU/memory release
  sequence, and the final ihk_create_os_str() load/kargs sequence through Rust
  helpers while preserving no-Rust C fallback bodies. This covers all-resource
  count validation, zero-resource policy, temporary CPU/memory allocation,
  query callback dispatch, assign/release callback dispatch, callback return
  preservation, stage-aware diagnostic routing, pre-create reserved CPU and
  memory cleanup ordering, load-before-kargs ordering, and load/kargs failure
  shaping. C remains responsible for public ABI entry points, actual
  device/ioctl/file/sysfs/makedump side effects, error-message buffer storage,
  rollback side effects, logging, and the compiled bridge/fallback source, so
  the row remains partial rather than fully retired. Final validation passed
  for rustfmt --edition 2021 --check over
  ihk/linux/user/rust/ihklib_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct all-resource and create-OS
  sequencing coverage plus ihklib_helpers digest b10eaa142ca375c7,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target ihklib -j2,
  no-Rust C fallback cmake --build /tmp/mckernel-rocky-user-cfallback
  --target ihklib -j2, symbol checks showing strict ihklib.c.o imports the
  six new helper bodies while ihklib_helpers.o exports them and fallback
  ihklib.c.o has no dependency on those helpers, broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON audit. Final
  strict JSON audit reports host/IKC/mcctrl/IHK modules completion
  99.76337500000001%, overall scored source-retirement completion
  99.82656716417911%, 0.17343283582088986 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-15: ihk/linux/user/ihklib.c moved from 99.3% to
  99.6% for the strict source-retirement audit. Rust-enabled ihklib now routes
  the private CPU, memory, and IKC string dispatch wrappers plus the public
  env-block wrappers for ihk_reserve_cpu_str(),
  ihk_reserve_mem_conf_str(), ihk_reserve_mem_str(),
  ihk_os_assign_cpu_str(), ihk_os_set_ikc_map_str(), and
  ihk_os_kargs_str() through Rust helpers while preserving the no-Rust C
  fallback bodies. This covers env parsing/free ownership, first matching
  variable selection, default-kargs selection, reserve-memory config key/value
  parsing, CPU-list expansion, memory chunk parsing including all-size
  handling, IKC map expansion, callback dispatch, callback return
  preservation, and temporary allocation/free ordering. C remains responsible
  for public ABI entry points, error-message buffer formatting, actual reserve/
  assign/kargs ioctl side effects, remaining create/rollback orchestration,
  makedumpfile/sysfs/file-I/O paths, logging, and the compiled bridge/fallback
  source, so the row remains partial rather than fully retired. Final
  validation passed for rustfmt --edition 2021 --check over
  ihk/linux/user/rust/ihklib_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct CPU/memory/IKC string
  dispatch and env-wrapper coverage plus ihklib_helpers digest
  6c9f72e1f031b64a, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihklib -j2, no-Rust C fallback cmake --build
  /tmp/mckernel-rocky-user-cfallback --target ihklib -j2, symbol checks
  showing strict ihklib.c.o imports the six new helper bodies while
  ihklib_helpers.o exports them and fallback ihklib.c.o has no dependency on
  those helpers, and broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. Final strict JSON audit reports host/IKC/mcctrl/
  IHK modules completion 99.76087500000001%, overall scored source-retirement
  completion 99.82582089552238%, 0.17417910447761642 percentage points left,
  20 debt rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale
  tracker rows [], untracked files [], and headers_with_executable_logic 0. No
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-15: executer/user/mcinspect.c moved from 99.0% to
  99.5% for the strict source-retirement audit. Rust-enabled mcinspect now
  routes init_globals(), mcvtop(), and the mcps() CPU/runqueue traversal
  through Rust helpers while preserving the no-Rust C fallback bodies. This
  covers mck_num_processors pointer dereference, nr_cpus/clv/clv_size
  publication, CPU-local/thread/process/address-space offset publication,
  vtop PID lookup gating and failure shaping, swapper page-table lookup/print
  ordering, process VM/address-space/page-table read ordering, mcps header
  retention, per-CPU current/runqueue/idle read ordering, current-thread
  printing, runqueue list traversal, duplicate-current suppression, and idle
  fallback printing. C remains responsible for public CLI entry points, DWARF
  tree traversal/lookups, BFD symbol-table primitive behavior, ioctl/kernel
  read side effects, print_thread() command-line allocation and formatting
  side effects, device/file open/close, and remaining compiled bridge/fallback
  source, so the row remains partial rather than fully retired. The first
  Rust-enabled mcinspect build for the mcvtop sub-slice failed because the
  swapper page-table bridge used the DWARF variable-address macro outside a
  scope with the active dbg handle; the failure is recorded in kernel.log and
  was corrected by threading Dwarf_Debug through the Rust callback path before
  scoring. Final validation passed for rustfmt --edition 2021 --check over
  executer/user/rust/mcinspect_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct init-globals, mcvtop
  success/PID-zero/find-failure/null-callback, and mcps two-CPU runqueue
  traversal coverage plus mcinspect_helpers digest 049775cc5b864fbe,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target mcinspect -j2,
  no-Rust C fallback cmake --build /tmp/mckernel-rocky-user-cfallback
  --target mcinspect -j2, symbol checks showing strict mcinspect.c.o imports
  mcinspect_init_globals_body_result, mcinspect_mcvtop_body_result, and
  mcinspect_mcps_body_result while mcinspect_helpers.o exports them and
  fallback mcinspect.c.o has no dependency on those helpers, and broad Rocky
  Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit with User tools completion 99.89482758620689%, overall
  scored source-retirement completion 99.82470149253732%,
  0.17529850746268494 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/user/ihklib.c moved from 99.0% to
  99.3% for the strict source-retirement audit. Rust-enabled ihklib now routes
  device and OS readable/open wrapper sequencing, ihk_destroy_os() open/ioctl/
  EBUSY retry/sleep/close sequencing, ihk_os_freeze()/ihk_os_thaw() OS-set
  bit traversal plus open/ioctl/close sequencing, and ENABLE_RUSAGE
  ihk_os_getrusage() validation/descriptor/ioctl/close sequencing through Rust
  helpers while preserving the no-Rust C fallback bodies. This covers /dev path
  construction, access/open errno shaping, retry exhaustion, sparse bitset
  iteration, failure-index publication, rusage descriptor construction, and
  close ordering. C remains responsible for public ABI entry points, actual
  access/open/ioctl/close/usleep/errno primitive side effects, directory
  iteration, allocation/free, parsing, sysfs file I/O, logging, and remaining
  compiled bridge/fallback source, so the row remains partial rather than fully
  retired. The initial equivalence compile failure for the errno constant and
  the focused ihklib build failure from the malformed getrusage preprocessor
  branch are recorded in kernel.log and were corrected before scoring this
  movement. Final validation passed for rustfmt --edition 2024 --check over
  ihk/linux/user/rust/ihklib_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with ihklib_helpers digest
  a96e2b1763795407, Rust-enabled cmake --build /tmp/mckernel-rocky-rust
  --target ihklib -j2, no-Rust C fallback cmake --build
  /tmp/mckernel-rocky-user-cfallback --target ihklib -j2, symbol checks showing
  strict ihklib.c.o imports ihklib_device_readable_body_result,
  ihklib_device_open_body_result, ihklib_destroy_os_body_result,
  ihklib_os_set_ioctl_loop_body_result, and ihklib_os_getrusage_body_result
  while ihklib_helpers.o exports them and fallback ihklib.c.o has no dependency
  on those helpers, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict audit with host/IKC/mcctrl/IHK
  modules completion 99.757125%, overall scored source-retirement completion
  99.82283582089552%, 0.17716417910448 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: tools/crash/mckernel.c moved from 98.0% to
  98.5% for the strict source-retirement audit. Rust-enabled crash extension
  builds now route cmd_mcinfo() formatting/output sequencing and extension
  constructor/destructor lifecycle dispatch through Rust while preserving the
  no-Rust C fallback bodies. This covers LINUX_PAGE_OFFSET line construction,
  nullable output-callback handling, command-table registration callback
  dispatch, empty fini result shaping, and direct negative callback/null-table
  validation. C remains responsible for crash framework option parsing,
  mcsymbols GDB passthrough and symbol refresh, mcps/mcmem/mcvtop traversal and
  printing, crash readmem/read_string primitive side effects, command help text,
  and remaining compiled bridge/fallback source, so the row remains partial
  rather than fully retired. The first full equivalence attempt for this slice
  failed with a mckernel_crash_helpers_smoke SIGSEGV because the new mcinfo
  helper accepted a nullable C callback as a non-null Rust function pointer; the
  failure is recorded in kernel.log and was corrected by using Rust's nullable
  C function-pointer representation. Final validation passed for rustfmt
  --edition 2021 over tools/crash/rust/mckernel_crash_helpers.rs, rustfmt
  --edition 2021 --check, bash -n kernel/rust/tests/run_equivalence.sh,
  scoped git diff --check, full kernel/rust/tests/run_equivalence.sh with
  direct mcinfo, extension registration, fini, null-callback, and null-table
  coverage plus mckernel_crash_helpers digest a12a544149193ff8, CMake refresh
  for /tmp/mckernel-rocky-rust and /tmp/mckernel-rocky-module-cfallback,
  Rust-enabled tools/crash/mckernel.so Makefile build, explicit no-Rust
  tools/crash/mckernel.so Makefile build with RUSTC=, symbol checks showing
  mckernel_crash_helpers.o and the Rust-enabled mckernel.so export
  mck_crash_cmd_mcinfo_body_result, mck_crash_extension_init_body_result, and
  mck_crash_extension_fini_body_result while the fallback mckernel.so has no
  dependency on those helpers, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict audit with overall scored
  source-retirement completion 99.82171641791045%,
  0.17828358208954853 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: arch/x86_64/kernel/cpu.c moved from 98.0% to
  98.5% for the strict source-retirement audit. Rust-enabled x86 now routes
  arch_save_panic_regs() and arch_clear_panic() panic-register publication
  sequencing through Rust while preserving the no-Rust C fallback bodies. This
  covers kernel-versus-user panic context selection, panic GPR publication,
  segment-register overlay packing, user-space panic log dispatch, panic flag
  set/clear publication, and invalid-input fallback shaping. C remains
  responsible for interrupt/NMI handler bodies, low-level CPU interrupt/halt
  primitives, FPU save/restore, AP stack-switch/bootstrap assembly-adjacent
  sequencing, panic register collection primitive inputs, and remaining
  compiled bridge/fallback source, so the row remains partial rather than fully
  retired. Validation passed for rustfmt --edition 2021 over
  kernel/rust/x86_cpu_helpers.rs, rustfmt --edition 2021 --check
  kernel/rust/x86_cpu_helpers.rs, bash -n kernel/rust/tests/run_equivalence.sh,
  scoped git diff --check, full kernel/rust/tests/run_equivalence.sh with
  direct kernel-panic, user-panic, segment packing, panic-log, clear-panic, and
  invalid-input coverage plus x86_cpu_helpers digest 62f223f407ee2f12,
  Rust-enabled cmake --build /tmp/mckernel-rocky-rust --target mckernel.img -j4,
  no-Rust C fallback cmake --build /tmp/mckernel-rocky-c-fallback --target
  mckernel.img -j4, symbol checks showing strict cpu.c.o imports
  x86_arch_save_panic_regs_body_result and x86_arch_clear_panic_body_result
  while mckernel_rust.o exports them and fallback cpu.c.o has no dependency on
  those helpers, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict audit with overall scored
  source-retirement completion 99.81985074626866%,
  0.1801492537313436 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/core/host_driver.c moved from 98.0% to
  99.0%, and ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c
  moved from 93.0% to 94.0%, for the strict source-retirement audit.
  Rust-enabled ihk now routes kmsg-buffer delete/release lifecycle
  sequencing through Rust while preserving matching no-Rust C fallback
  bodies. This covers null-container handling, page-free callback dispatch,
  list unlink dispatch, container free dispatch, pre-decrement refcount
  validation, lock/unlock callback ordering, decrement-and-delete selection,
  and lifecycle debug-log event selection. Rust-enabled ihk-smp now routes
  x86 SMP arch dcache-flush result shaping, ns-per-TSC policy, and x2APIC
  MSR-bit extraction through Rust while preserving matching no-Rust C
  fallback bodies. The new vector helper exports are equivalence-tested, but
  the active Rocky strict build enables Linux work IRQs, so that vector block
  is not imported by smp-arch-driver.o and is not counted as active strict-path
  ownership in this score. C remains responsible for kmsg lifetime primitive
  side effects, Linux locks/lists/freeing, APIC/MSR/CPUID primitive reads,
  startup page-table construction, dump traversal and copyout, arch init/exit,
  CPU reset, IRQ handler registration, and remaining compiled bridge/fallback
  source, so both rows remain partial. Validation passed for rustfmt
  --edition 2021 over the touched Rust helper files, rustfmt --edition 2021
  --check, bash -n kernel/rust/tests/run_equivalence.sh, scoped git diff
  --check, full kernel/rust/tests/run_equivalence.sh with ihk_core_helpers
  digest a9ac16e765c01f69 and smp_chunk_helpers digest a475102d90f0c613,
  Rust-enabled ihk_ko and ihk-smp-x86_64_ko builds, no-Rust C fallback ihk_ko
  and ihk-smp-x86_64_ko builds, symbol checks showing strict host_driver.o
  imports ihk_core_delete_kmsg_buf_body_result and
  ihk_core_release_kmsg_buf_body_result while core_helpers.o exports them,
  strict smp-arch-driver.o imports ihk_smp_arch_dcache_flush_body_result,
  ihk_smp_calc_ns_per_tsc_body_result, and
  ihk_smp_x2apic_is_enabled_body_result while smp_driver_helpers.o exports
  them, fallback objects have no dependency on the new helpers, broad Rocky
  Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit with host/IKC/mcctrl/IHK modules completion 99.753375%,
  overall scored source-retirement completion 99.81798507462688%,
  0.18201492537312447 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c
  moved from 92.0% to 93.0% for the strict source-retirement audit.
  Rust-enabled ihk-smp now routes the x86 SMP arch IKC-map check and
  perf-event boot-parameter publication through Rust while preserving matching
  no-Rust C fallback bodies. This covers zero-result IKC-map check shaping,
  x86 PMU extra-register list traversal, extra-register count publication,
  event/MSR/valid-mask/index field publication, too-many-extra-regs error
  logging and return shaping, and hardware event/cache map byte publication
  into `struct smp_boot_param`. C remains responsible for kallsyms lookup
  primitive behavior, Linux PMU storage, startup page-table construction, dump
  traversal and copyout, arch init/exit, CPU reset, IRQ handler registration,
  and remaining compiled bridge/fallback source, so the row remains partial.
  Validation passed for rustfmt --edition 2021
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, rustfmt --edition 2021
  --check ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct IKC-map and perf-map
  success/null-source/too-many-extra-regs coverage plus smp_chunk_helpers
  digest e17784cf54bb2483, Rust-enabled ihk-smp-x86_64_ko build, no-Rust C
  fallback ihk-smp-x86_64_ko build, symbol checks showing strict
  smp-arch-driver.o imports ihk_smp_check_ikc_map_body_result and
  ihk_smp_arch_get_perf_event_map_body_result while smp_driver_helpers.o
  exports them and fallback smp-arch-driver.o has no dependency on the new
  helpers, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit with
  host/IKC/mcctrl/IHK modules completion 99.728375%,
  overall scored source-retirement completion 99.81052238805971%,
  0.18947761194029056 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c
  moved from 89.0% to 92.0% for the strict source-retirement audit.
  Rust-enabled ihk-smp now routes x86 SMP arch interrupt and identity
  mapping wrapper sequencing through Rust while preserving matching no-Rust C
  fallback bodies. This covers `smp_ihk_adjust_entry()` entry return shaping,
  `smp_ihk_os_send_nmi()` NMI mode dispatch and per-CPU NMI broadcast,
  `smp_ihk_os_send_multi_intr()` multi-interrupt mode dispatch, IRQ
  save/restore bracketing, per-CPU x2APIC and legacy IPI broadcast dispatch,
  `smp_ihk_os_issue_interrupt()` single-CPU validation and dispatch,
  `smp_ihk_os_map_memory()` and `smp_ihk_map_memory()` identity physical
  return shaping, and `smp_ihk_os_unmap_memory()` and `smp_ihk_unmap_memory()`
  zero-result unmap shaping. C remains responsible for APIC/IRQ primitive
  side effects, `ihk_smp_set_{nmi,multi_intr}_mode()` primitive behavior,
  startup page-table construction, dump command traversal and copyout,
  arch init/exit, IRQ handler registration, perf-event map setup, and
  remaining compiled bridge/fallback source, so the row remains partial.
  Validation passed for rustfmt --edition 2021
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, rustfmt --edition 2021
  --check ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct adjust-entry,
  identity map/unmap, single interrupt, broadcast legacy, broadcast x2APIC,
  mode-failure, and invalid-input coverage plus smp_chunk_helpers digest
  fea51976b4b38c02, Rust-enabled ihk-smp-x86_64_ko build, no-Rust C fallback
  ihk-smp-x86_64_ko build, symbol checks showing strict
  smp-arch-driver.o imports ihk_smp_adjust_entry_body_result,
  ihk_smp_identity_map_memory_body_result,
  ihk_smp_identity_unmap_memory_body_result,
  ihk_smp_os_issue_interrupt_body_result, and
  ihk_smp_os_broadcast_interrupt_body_result while smp_driver_helpers.o
  exports them and fallback smp-arch-driver.o has no dependency on the new
  helpers, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit with
  host/IKC/mcctrl/IHK modules completion 99.71587500000001%,
  overall scored source-retirement completion 99.80679104477612%,
  0.1932089552238807 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. The earlier
  x2apic_is_enabled() forward-declaration and stale-object symbol-gate
  failures for the single-interrupt sub-slice are recorded in kernel.log and
  were corrected before this combined checkpoint was scored. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/driver/smp/smp-driver.c moved from 95.0% to
  97.0% for the strict source-retirement audit. Rust-enabled ihk-smp now
  routes direct/host virtual-map dispatch, OS virtual-to-physical translation,
  and OS load-memory sequencing through Rust while preserving matching no-Rust
  C fallback bodies. This covers direct map versus host-map selection, null-map
  logging, bootstrap physical-base alignment, kernel-map and dynamic
  virt-to-phys result shaping, ready/too-large/busy load-memory validation,
  status lock/update/reset ordering, page-offset calculation, per-page map,
  copy, unmap and next-page sequencing, map-failure status reset, boot RIP
  publication, and load copy logging. C remains responsible for primitive
  map/unmap/copy side effects, Linux locking side effects, virtual-memory
  backing, boot/shutdown/panic orchestration, CPU and memory assignment
  mutation, and remaining compiled bridge/fallback source, so the row remains
  partial. Validation passed for rustfmt --edition 2021
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, rustfmt --edition 2021
  --check ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct map, vtop, and load-memory
  success/error coverage and smp_chunk_helpers digest a9631759ce00eadf,
  Rust-enabled ihk-smp-x86_64_ko build, no-Rust C fallback
  ihk-smp-x86_64_ko build, symbol checks showing strict smp-driver.o imports
  ihk_smp_map_virtual_body_result, ihk_smp_os_vtop_body_result, and
  ihk_smp_os_load_mem_body_result while smp_driver_helpers.o exports them and
  fallback smp-driver.o has no dependency on the new helpers, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit with host/IKC/mcctrl/IHK modules completion 99.678375%,
  overall scored source-retirement completion 99.79559701492538%,
  0.20440298507462273 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: tools/crash/mckernel.c moved from 97.0% to
  98.0% for the strict source-retirement audit. Rust-enabled crash extension
  builds now route `cmd_mckmsg()` command-body sequencing through Rust while
  preserving the no-Rust C fallback body. This covers kmsg field read ordering,
  silent read-failure return shaping, buffer allocation, wrapped/non-wrapped
  kmsg ring read dispatch, fatal read-failure classification, output callback
  dispatch, and final buffer free ordering. C remains responsible for crash
  framework symbol refresh, `mcreadmem()` primitive side effects, GETBUF/
  FREEBUF storage, `read_string()` and `fprintf()` side effects, command
  registration/help text, other crash command wrappers, and remaining compiled
  bridge/fallback source, so the row remains partial. Validation passed for
  rustfmt --edition 2021 --check
  tools/crash/rust/mckernel_crash_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct command-body success,
  silent field-read failure, ring-read failure, and allocation-failure
  coverage plus mckernel_crash_helpers smoke output, Rust-enabled
  tools/crash/mckernel.so Makefile build, explicit no-Rust tools/crash/
  mckernel.so Makefile build, symbol checks showing
  mckernel_crash_helpers.o exports mck_crash_cmd_mckmsg_body_result and the
  no-Rust fallback extension has no dependency on the new helper, broad Rocky
  Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit with User tools completion 99.8603448275862%, overall
  scored source-retirement completion 99.78813432835821%,
  0.21186567164178882 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/core/host_driver.c moved from 96.0% to
  98.0% for the strict source-retirement audit. Rust-enabled ihk now routes
  the kmsg ring read/clear path and OS/device kmsg wrapper sequencing through
  Rust while preserving matching no-Rust C fallback bodies. This covers
  `read_kmsg()` lock acquisition, wraparound length calculation, bottom/top
  segment copyout, optional head-shift mutation, and unlock/IRQ restore
  ordering, `__ihk_os_read_kmsg()` container validation, allocation, ring-read
  dispatch, user-copy failure shaping, and free ordering,
  `__ihk_os_clear_kmsg()` container validation and clear sequencing,
  `__ihk_device_get_kmsg_buf()` descriptor copyin, locked reverse lookup,
  reference increment, handle publication, and copyout failure shaping,
  `__ihk_device_read_kmsg_buf()` descriptor copyin, allocation, ring-read
  dispatch, user-copy failure shaping, and free ordering, and
  `__ihk_device_release_kmsg_buf()` release callback dispatch. C remains
  responsible for local_irq_save/restore and cpu_relax primitive side effects,
  kmalloc/kfree, copy_{from,to}_user(), kmsg container lifetime and page-free
  side effects, lock/list storage, refcount/open/release/ioctl/create/destroy/
  load paths, and remaining compiled bridge/fallback source, so the row
  remains partial. Validation passed for rustfmt --edition 2021
  ihk/linux/core/rust/core_helpers.rs, rustfmt --edition 2021 --check
  ihk/linux/core/rust/core_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct kmsg wraparound, clear,
  OS read/clear, device get/read/release success/error coverage plus
  ihk_module_helpers smoke output, Rust-enabled ihk_ko build, no-Rust C
  fallback ihk_ko build, symbol checks showing strict host_driver.o imports
  ihk_core_read_kmsg_body_result, ihk_core_os_read_kmsg_body_result,
  ihk_core_os_clear_kmsg_body_result,
  ihk_core_device_get_kmsg_buf_body_result,
  ihk_core_device_read_kmsg_buf_body_result, and
  ihk_core_device_release_kmsg_buf_body_result while core_helpers.o exports
  those plus ihk_core_clear_kmsg_body_result and fallback host_driver.o has
  no kmsg helper dependency, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit with
  host/IKC/mcctrl/IHK modules completion 99.65337500000001%, overall scored
  source-retirement completion 99.78440298507464%,
  0.21559701492536476 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. A Rust formatting
  failure and a no-stdlib memcpy undefined-symbol failure were recorded in
  kernel.log and corrected before scoring. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/core/host_driver.c moved from 94.0% to
  96.0% for the strict source-retirement audit. Rust-enabled ihk now routes
  another host-driver status/control/readback leaf batch through Rust while
  preserving matching no-Rust C fallback bodies. This covers
  `__ihk_os_status()` status query dispatch, `__ihk_os_freeze()` policy,
  invalid-status logging, busy-state return shaping, and freeze callback
  dispatch, `__ihk_os_thaw()` policy, wait-for-frozen callback dispatch,
  timeout logging, and thaw callback dispatch, `__ihk_os_get_usage()` monitor
  setup and monitor copyout, `__ihk_os_get_cpu_usage()` monitor setup and CPU
  monitor-array copyout, and `__ihk_os_read_kaddr()` physical/virtual address
  selection, vtop failure shaping, phys_to_virt callback dispatch, and
  user-copy failure shaping. C remains responsible for ops callback primitive
  behavior, monitor publication side effects, copy_to_user() side effects,
  vtop and phys_to_virt primitive behavior, Linux logging side effects,
  refcount/open/release/ioctl/create/destroy/load and kmsg paths, Linux
  locks/lists/allocation, and remaining compiled bridge/fallback source, so
  the row remains partial. Validation passed for rustfmt --edition 2021
  ihk/linux/core/rust/core_helpers.rs, rustfmt --edition 2021 --check
  ihk/linux/core/rust/core_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct status, freeze, thaw,
  usage, CPU usage, and read-kaddr success/error coverage plus
  ihk_module_helpers smoke output, Rust-enabled ihk_ko build, no-Rust C
  fallback ihk_ko build, symbol checks showing strict host_driver.o imports
  ihk_core_os_status_body_result, ihk_core_os_freeze_body_result,
  ihk_core_os_thaw_body_result, ihk_core_os_get_usage_body_result,
  ihk_core_os_get_cpu_usage_body_result, and
  ihk_core_os_read_kaddr_body_result while core_helpers.o exports them and
  fallback host_driver.o has no helper dependency, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit with host/IKC/mcctrl/IHK modules completion 99.628375%,
  overall scored source-retirement completion 99.77694029850747%,
  0.22305970149253085 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/core/host_driver.c moved from 92.0% to
  94.0% for the strict source-retirement audit. Rust-enabled ihk now routes
  a larger host-driver IO leaf batch through Rust while preserving matching
  no-Rust C fallback bodies. This covers `__ihk_os_set_kargs()` callback
  dispatch, `__ihk_os_dump()` callback dispatch and return shaping,
  `ihk_host_os_write()` size validation, allocation, user-copy, load-memory,
  free, offset update, and return shaping, plus `ihk_host_device_read()` and
  `ihk_host_device_write()` map-memory/map-virtual/copy/unmap/offset
  sequencing including partial-copy accounting. C remains responsible for
  kmalloc/kfree side effects, copy_{from,to}_user() side effects,
  strncpy_from_user(), ops callback primitive behavior, device memory
  map/unmap side effects, refcount/open/release/ioctl/create/destroy/load and
  kmsg paths, Linux locks/lists/allocation, and remaining compiled bridge/
  fallback source, so the row remains partial. Validation passed for rustfmt
  --edition 2021 ihk/linux/core/rust/core_helpers.rs, rustfmt --edition 2021
  --check ihk/linux/core/rust/core_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct set-kargs, dump, OS write,
  device read, and device write success/error/partial-copy coverage plus
  ihk_module_helpers smoke output, Rust-enabled ihk_ko build after a forced
  clean rebuild, no-Rust C fallback ihk_ko build, symbol checks showing strict
  host_driver.o imports ihk_core_os_set_kargs_body_result,
  ihk_core_os_dump_body_result, ihk_core_host_os_write_body_result, and
  ihk_core_host_device_io_body_result while core_helpers.o exports them and
  fallback host_driver.o has no helper dependency, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit with host/IKC/mcctrl/IHK modules completion 99.603375%,
  overall scored source-retirement completion 99.7694776119403%,
  0.23052238805969694 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. A Rust-enabled
  ihk_ko declaration mismatch and a stale-object symbol-gate miss were
  recorded in kernel.log and corrected before scoring. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/core/host_driver.c moved from 91.0% to
  92.0% for the strict source-retirement audit. Rust-enabled ihk now routes
  OS resource wrapper sequencing through Rust while preserving matching
  no-Rust C fallback bodies. This covers `__ihk_os_allocate_mem()`,
  `__ihk_os_allocate_cpu()`, `__ihk_os_reserve_cpu()`, and
  `__ihk_os_reserve_mem()` resource-body shaping: zeroed `struct
  ihk_resource` publication, CPU and memory specified flags, CPU-count,
  memory-start, memory-size assignment, callback dispatch, and missing/null/
  unknown-op error shaping. C remains responsible for copy_from_user()
  side effects, reserve-CPU array allocation/copy/free, alloc_resource()
  primitive side effects, refcount/open/release/ioctl/create/destroy/load
  and kmsg paths, Linux locks/lists/allocation, and remaining compiled
  bridge/fallback source, so the row remains partial. Validation passed for
  rustfmt --edition 2021 --check ihk/linux/core/rust/core_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct OS resource success and
  error-path coverage plus ihk_module_helpers smoke output, Rust-enabled
  ihk_ko build, no-Rust C fallback ihk_ko build, symbol checks showing strict
  host_driver.o imports ihk_core_os_resource_body_result while
  core_helpers.o exports it and fallback host_driver.o has no helper
  dependency, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit with
  host/IKC/mcctrl/IHK modules completion 99.57837500000001%, overall scored
  source-retirement completion 99.76201492537314%,
  0.23798507462686302 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/core/host_driver.c moved from 90.0% to
  91.0% for the strict source-retirement audit. Rust-enabled ihk now routes
  a host-driver wrapper batch through Rust while preserving matching no-Rust
  C fallback bodies. This covers OS debug-request dispatch, device
  debug-request dispatch, device build-id copyout and EFAULT shaping, and
  device reserve_cpu, release_cpu, reserve_mem, release_mem,
  release_mem_partially, get_num_cpus, query_cpu, and query_mem wrapper
  dispatch. C remains responsible for copy_to_user() side effects, device
  ops callback side effects, refcount/open/release/ioctl/create/destroy/load
  and kmsg paths, Linux locks/lists/allocation, and remaining compiled
  bridge/fallback source, so the row remains partial. Validation passed for
  rustfmt --edition 2021 ihk/linux/core/rust/core_helpers.rs, rustfmt
  --edition 2021 --check ihk/linux/core/rust/core_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct OS/device debug,
  build-id copyout, and all eight device op wrapper cases plus
  ihk_module_helpers smoke output, Rust-enabled ihk_ko build, no-Rust C
  fallback ihk_ko build, symbol checks showing strict host_driver.o imports
  ihk_core_os_debug_request_body_result,
  ihk_core_device_debug_request_body_result,
  ihk_core_device_get_buildid_body_result, and
  ihk_core_device_op_body_result while core_helpers.o exports them and
  fallback host_driver.o has no helper dependency, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit with host/IKC/mcctrl/IHK modules completion 99.565875%,
  overall scored source-retirement completion 99.75828358208956%,
  0.24171641791043896 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/control.c moved from 99.98% to
  99.99% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes `mcexec_terminate_thread_unsafe()` body sequencing through Rust
  while preserving the no-Rust C fallback body. This covers usrdata/PPD/PTD
  lookup error shaping, PTD TID validation, packet lookup, syscall-return
  callback dispatch using `usrdata->os`, IKC packet release dispatch, the
  three PTD put-callback handoff sequence, the PTD refcount warning gate, and
  the two final PPD put-callback handoffs. C remains responsible for the
  actual refcount mutation, packet release side effects, logging side effects,
  host-thread list mutation in the caller, broader mcctrl syscall-thread and
  wait-syscall bodies, CONFIG_MIC source, and remaining compiled bridge/
  fallback source, so the row remains partial. Validation passed for rustfmt
  --edition 2021 executer/kernel/mcctrl/rust/mcctrl_helpers.rs, rustfmt
  --edition 2021 --check executer/kernel/mcctrl/rust/mcctrl_helpers.rs,
  bash -n kernel/rust/tests/run_equivalence.sh, scoped git diff --check,
  full kernel/rust/tests/run_equivalence.sh with mcctrl_helpers digest
  3e12ee3ceeb7db3d, Rust-enabled mcctrl_ko build, no-Rust C fallback
  mcctrl_ko build, symbol checks showing strict control.o imports
  mcctrl_control_terminate_thread_unsafe_body_result while
  mcctrl_helpers.o exports it and fallback control.o has no helper
  dependency, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit with
  host/IKC/mcctrl/IHK modules completion 99.553375%, overall scored
  source-retirement completion 99.75455223880597%,
  0.2454477611940291 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/driver/smp/smp-driver.c moved from 93.0% to
  95.0% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes SMP device/OS CPU-query and memory-query
  sequencing through Rust while preserving the no-Rust C fallback bodies.
  This covers device available-CPU request copyin/validation, available-CPU
  count mismatch shaping, result allocation/free, CPU array and count copyout,
  OS CPU mapping copyout, device free-memory and OS assigned-memory request
  copyin/validation, Linux-list traversal/counting, OS ownership filtering,
  optional fake-chunk accounting, exact-count gating for OS assigned-memory
  query, size/NUMA result allocation/free, and size/NUMA/count copyout. C
  remains responsible for copy_{from,to}_user() side effects, kmalloc/kfree
  side effects, Linux list storage and mutation, fake-chunk publication,
  CPU reserve/release, memory assignment/release mutation, load/boot/shutdown/
  panic, IKC-map behavior, and remaining compiled bridge/fallback source, so
  the row remains partial. A focused SMP query-batch C-model segfault from
  count-only memory-query array copyout was recorded in kernel.log and
  corrected before scoring. Validation passed for rustfmt --edition 2021
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, rustfmt --edition 2021
  --check ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct CPU/memory query coverage
  and smp_chunk_helpers digest d07b4150b62e0258, Rust-enabled
  ihk-smp-x86_64_ko build, no-Rust C fallback ihk-smp-x86_64_ko build,
  symbol checks showing strict smp-driver.o imports the three SMP query Rust
  helpers while smp_driver_helpers.o exports them and fallback smp-driver.o
  has no helper dependency, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit with
  host/IKC/mcctrl/IHK modules completion 99.55325%, overall scored
  source-retirement completion 99.75451492537313%,
  0.24548507462687041 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/driver/smp/smp-driver.c moved from 92.0% to
  93.0% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes additional read-mostly SMP device/OS helper
  bodies through Rust while preserving the no-Rust C fallback bodies. This
  covers build-id copyout and EFAULT shaping, available-CPU count packing,
  CPU-topology lookup by hardware ID, node-topology lookup including
  out-of-range ERR_PTR(-EINVAL) shaping, and Linux CPU to hardware-ID lookup.
  C remains responsible for copy_to_user() side effects, Linux list storage,
  topology object lifetime, global CPU-table mutation, CPU reserve/release,
  memory assignment/release/query, load/boot/shutdown/panic, IKC-map behavior,
  and remaining compiled bridge/fallback source, so the row remains partial.
  A focused C-model fixture link failure and a brittle symbol-gate pipeline
  failure were recorded in kernel.log and corrected before scoring. Validation
  passed for rustfmt --edition 2021
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, rustfmt --edition 2021
  --check ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct build-id, available CPU
  count, topology lookup, and existing SMP coverage and smp_chunk_helpers
  digest bd848f4f208b5009, Rust-enabled ihk-smp-x86_64_ko build, no-Rust C
  fallback ihk-smp-x86_64_ko build, symbol checks showing strict
  smp-driver.o imports the five new Rust helpers while smp_driver_helpers.o
  exports them and fallback smp-driver.o has no helper dependency, broad Rocky
  Rust build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final
  strict JSON audit with host/IKC/mcctrl/IHK modules completion 99.52825%,
  overall scored source-retirement completion 99.74705223880596%,
  0.2529477611940365 percentage points left, 20 debt rows, compiled_retired_c
  0, compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  and headers_with_executable_logic 0. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/driver/smp/smp-driver.c moved from 90.0% to
  92.0% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes SMP control-plane wrapper and lifecycle
  sequencing through Rust while preserving the no-Rust C fallback bodies.
  This covers notify-hungup status publication, OS memory and CPU info
  pointer returns, NUMA and CPU count reads, kernel-args setup state gating,
  loading-state publication, bounded argument copy and final initial-state
  publication, OS debug interrupt dispatch argument shaping, freeze/thaw
  multi-interrupt dispatch, create-OS register-data copy, OS allocation,
  private-data publication and bootstrap defaults, destroy-OS free dispatch,
  device debug rejection, and unmap-virtual dispatch. C remains responsible
  for lock primitive side effects, allocation/free, spinlock initialization,
  printk/dprintk, interrupt and multi-interrupt side effects, actual virtual
  unmap side effects, CPU hotplug/reserve/release, memory list mutation and
  resource assignment, load/boot/shutdown/panic/IKC map behavior, mapping
  primitives, and remaining compiled bridge/fallback source, so the row
  remains partial rather than fully retired. The focused SMP control-plane
  equivalence failure from a nondeterministic digest input was recorded in
  kernel.log and corrected before scoring. Validation passed for rustfmt
  --edition 2021 ihk/linux/driver/smp/rust/smp_driver_helpers.rs, rustfmt
  --edition 2021 --check
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct SMP control-plane
  success/error coverage and smp_chunk_helpers digest 54f6195714d40a48,
  Rust-enabled ihk-smp-x86_64_ko build, no-Rust C fallback
  ihk-smp-x86_64_ko build, symbol checks showing strict smp-driver.o imports
  all new SMP control-plane Rust helpers while smp_driver_helpers.o exports
  them and fallback smp-driver.o has no helper dependency, broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON
  audit with host/IKC/mcctrl/IHK modules completion 99.51575%, overall
  scored source-retirement completion 99.74332089552239%,
  0.25667910447761244 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c
  moved from 88.0% to 89.0% for the strict source-retirement audit.
  Rust-enabled ihk-smp-x86_64 now routes ihk_smp_arch_symbols_init()
  ordered kallsyms lookup and slot publication through Rust while preserving
  the no-Rust C fallback body. This covers ordered lookup for
  real_mode_header, vector_irq, lapic_get_maxlvt, irq_desc_tree,
  get_uv_system_type, init_deasserted, alloc_desc,
  __default_send_IPI_dest_field, init_top_pgt/init_level4_pgt fallback, and
  system_vectors/used_vectors fallback; required-symbol WARN_ON failure
  routing; missing required-symbol return shaping; optional symbol skip
  behavior; and null input validation. C remains responsible for
  kallsyms_lookup_name(), WARN_ON(), static symbol storage, AP startup/reset,
  page-table setup, IRQ vector mutation, topology/sysfs side effects, and
  remaining compiled bridge/fallback source, so the row remains partial rather
  than fully retired. Validation passed for rustfmt --edition 2021
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, rustfmt --edition 2021
  --check ihk/linux/driver/smp/rust/smp_driver_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct SMP symbol-init
  success/fallback/missing-symbol/null-input coverage and smp_chunk_helpers
  digest 156315bd14c611dd, Rust-enabled ihk-smp-x86_64_ko build, no-Rust C
  fallback ihk-smp-x86_64_ko build, symbol checks showing strict
  smp-arch-driver.o imports ihk_smp_arch_symbols_init_body_result while
  smp_driver_helpers.o exports it and fallback smp-arch-driver.o has no
  helper dependency, broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4, and final strict JSON audit with
  host/IKC/mcctrl/IHK modules completion 99.49075%, overall scored
  source-retirement completion 99.73585820895522%,
  0.26414179104477853 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: arch/x86_64/kernel/cpu.c moved from 94.0% to 98.0%
  for the strict source-retirement audit. Rust-enabled x86 now routes AP boot
  trampoline setup and wakeup wait, AP callback publication, stack walkers,
  pre-interrupt and normal stack print selection, user and interrupt context
  print sequencing, mcexec v10 enter-user trace gating and field extraction,
  and runqueue unlock handoff through Rust while preserving the no-Rust C
  fallback bodies. This extends the previous pvclock setup/start movement.
  C remains responsible for actual kprintf/__kprintf and __print_stack side
  effects, allocation/address-translation/page-table primitive behavior, AP
  wakeup and pause primitives, spinlock primitive side effects, assembler
  register capture, FPU/xsave/xrstor, IDT/GDT and context-switch adjacent
  behavior, raw APIC/MSR operations, and remaining compiled cpu.c
  bridge/fallback source; the row is therefore still partial rather than fully
  retired. Validation passed for rustfmt --edition 2021
  kernel/rust/x86_cpu_helpers.rs, rustfmt --edition 2021 --check
  kernel/rust/x86_cpu_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct AP boot/callback,
  stack-print, user/interrupt context, mcexec trace, runqueue unlock, and
  pvclock coverage and x86_cpu_helpers digest f2d4e99ed495122c,
  Rust-enabled mckernel.img build, no-Rust C fallback mckernel.img build,
  symbol checks showing strict cpu.c.o imports the new x86_*_body_result
  helpers while mckernel_rust.o exports them and fallback cpu.c.o has no new
  x86 Rust-helper dependency, and broad Rocky Rust build-only validation
  through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4, and final strict JSON audit with
  Other in-scope code completion 99.9795918367347%, overall scored
  source-retirement completion 99.73212686567165%,
  0.26787313432835447 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. The focused
  AP/stack equivalence fixture compile failure for this widened slice is
  recorded in kernel.log and corrected. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.
- 2026-06-15: arch/x86_64/kernel/cpu.c moved from 93.0% to 94.0%
  for the strict source-retirement audit. Rust-enabled x86 now routes
  arch_setup_pvclock() and arch_start_pvclock() body sequencing through
  Rust while preserving the no-Rust C fallback bodies. This covers pvclock
  availability dispatch, pvclock info page-count calculation, pvti_npages
  publication, aligned page allocation callback dispatch, pvti pointer
  publication, allocation-failure error/log shaping, pvti page zeroing,
  current-CPU lookup, per-CPU pvclock entry address calculation,
  virt_to_phys() callback dispatch, and KVM system-time MSR write selection.
  C remains responsible for allocation and address-translation primitive side
  effects, wrmsr side effects, the availability CPUID helper callback, logging
  side effects, and remaining compiled cpu.c bridge/fallback source; the row
  is therefore still partial rather than fully retired. Validation passed for
  rustfmt --edition 2021 kernel/rust/x86_cpu_helpers.rs, rustfmt --edition
  2021 --check kernel/rust/x86_cpu_helpers.rs, bash -n
  kernel/rust/tests/run_equivalence.sh, scoped git diff --check, full
  kernel/rust/tests/run_equivalence.sh with direct pvclock setup/start
  success/error/unsupported coverage and x86_cpu_helpers digest
  4a077e1966300e49, Rust-enabled mckernel.img build, no-Rust C fallback
  mckernel.img build, symbol checks showing strict cpu.o imports
  x86_arch_setup_pvclock_body_result and
  x86_arch_start_pvclock_body_result while mckernel_rust.o exports both and
  fallback cpu.o has no helper dependency, broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and the final strict
  JSON audit. The focused equivalence fixture compile failure for this slice
  is recorded in kernel.log and corrected. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/procfs.c moved from 99.5% to
  99.8%, and executer/kernel/mcctrl/sysfs.c moved from 99.9% to
  99.98%, for the strict source-retirement audit. Rust-enabled mcctrl now
  routes procfs add-entry and delete-entry lifecycle sequencing through Rust:
  duplicate lookup and delete-before-create ordering, allocation-size
  calculation, entry initialization, procfs create callback dispatch,
  commit/free failure cleanup, top unlink, recursive child drain,
  remove_proc_entry callback dispatch, private data free, and final entry free
  ordering. The same batch routes sysfs lookup_i() and remote sysfs request
  wrapper sequencing through Rust: directory/type/name validation,
  child-list traversal and errno pointer shaping, remote setup/create/mkdir/
  symlink/lookup/unlink request parameter map/virtual-map dispatch, setup
  buffer map/cleanup ordering, local operation callback dispatch, handle/error
  publication, write-barrier callback dispatch, busy clear, and parameter
  unmap ordering. C remains responsible for Linux procfs/sysfs/kobject
  primitive mutation, list and allocation/free primitive side effects, IHK
  device map/unmap side effects, remote special-sysfs setup/cleanup primitive
  behavior, logging side effects, and remaining compiled procfs.c/sysfs.c
  bridge/fallback source; both rows therefore remain partial rather than fully
  retired. Validation passed for rustfmt --check, shell syntax, scoped
  git diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  procfs lifecycle plus sysfs lookup/request-wrapper success/error coverage
  and mcctrl_helpers digest fb87f589a7dc96fd, Rust-enabled mcctrl_ko build,
  no-Rust C fallback mcctrl_ko build, symbol checks showing strict procfs.o
  imports mcctrl_procfs_add_entry_body_result and
  mcctrl_procfs_delete_entries_body_result while strict sysfs.o imports
  mcctrl_sysfs_lookup_i_body_result and all six mcctrl_sysfs_req_*_body_result
  helpers, mcctrl_helpers.o exports those helpers, and fallback procfs.o/
  sysfs.o have no helper dependency, broad Rocky Rust build-only validation
  through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4, and the final strict JSON audit. The
  final audit reports host/IKC/mcctrl/IHK modules completion 99.47825%,
  overall scored source-retirement completion 99.71347014925372%,
  0.2865298507462768 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. The focused
  rustfmt failure and ad hoc jq projection error for this continuation are
  recorded in kernel.log and corrected/avoided. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/futex.c moved from 95.0% to
  98.0% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes futex_wake(), futex_wake_op(), and futex_requeue() body sequencing
  through Rust while preserving the no-Rust C fallback bodies. This covers
  wake bitset validation, key-lookup callback dispatch, hash-bucket selection,
  lock/unlock callback ordering, waiter traversal, wake limit enforcement,
  wake-op source/target key acquisition, atomic-op callback dispatch,
  double-lock ordering, optional target-bucket wake, requeue cmpval fetch and
  EAGAIN shaping, wake-versus-move count selection, key migration callback
  ordering, and key put/unwind ordering. C remains responsible for
  get_futex_key(), hash-futex/list/lock primitive side effects,
  wake_futex() and requeue_futex() mutation callbacks,
  futex_atomic_op_inuser(), get_futex_value_locked(), user access/page-fault
  primitives, logging side effects, and remaining compiled futex.c bridge/
  fallback source; the row is therefore still partial rather than fully
  retired. Validation passed for rustfmt --check, shell syntax, scoped
  git diff --check, full kernel/rust/tests/run_equivalence.sh with direct
  mcctrl futex wake, wake-op, and requeue coverage and mcctrl_helpers digest
  bde5ea5b2e5b172f, Rust-enabled mcctrl_ko build, no-Rust C fallback
  mcctrl_ko build, symbol checks showing strict futex.o imports
  mcctrl_futex_wake_body_result, mcctrl_futex_wake_op_body_result, and
  mcctrl_futex_requeue_body_result while mcctrl_helpers.o exports them and the
  fallback futex.o has no helper dependency, broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and the final strict JSON
  audit. The final audit reports host/IKC/mcctrl/IHK modules completion
  99.4735%, overall scored source-retirement completion 99.71205223880598%,
  0.2879477611940189 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. The earlier focused
  equivalence fixture compile failure for this slice is recorded in kernel.log
  and corrected. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/control.c moved from 99.95% to
  99.98% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes non-MIC mcexec_ret_syscall() and mcexec_uti_get_ctx() body
  sequencing through Rust while preserving the no-Rust C fallback bodies and
  the CONFIG_MIC fallback path. This covers ret-syscall usrdata lookup,
  descriptor copyin, current PID/TID/task selection, PPD/PTD lookup and
  unwind ordering, registered packet lookup, optional host-to-remote return
  buffer mapping and copyin, original early-return cleanup gaps on return-data
  copy failure, __return_syscall() callback dispatch, packet release, and
  double PTD put plus PPD put ordering. The same slice covers UTI get-context
  descriptor copyin, remote context map/virtual-map sequencing, context
  copyout, mcexec task-key copyout, refill-TID publication into the remote UTI
  context, copyout-failure shaping, and unmap ordering. C remains responsible
  for Linux user-copy primitives, IHK device map/unmap primitive side effects,
  __return_syscall() and packet-release side effects, task/current primitive
  values, logging side effects, CONFIG_MIC source, and remaining compiled
  control.c bridge/fallback source; the row is therefore still partial rather
  than fully retired. Validation passed for rustfmt --check, shell syntax,
  scoped git diff --check, full kernel/rust/tests/run_equivalence.sh with
  direct ret-syscall success, missing-usrdata, copyin-failure, missing-PPD,
  missing-PTD, missing-packet, return-data-copy-failure coverage plus UTI
  get-context success, descriptor-copy-failure, context-copyout-failure, and
  key-copyout-failure coverage and mcctrl_helpers digest 5459b4286f826858,
  Rust-enabled mcctrl_ko build, no-Rust C fallback mcctrl_ko build, symbol
  checks showing strict control.o imports mcctrl_control_ret_syscall_body_result
  and mcctrl_control_uti_get_ctx_body_result, mcctrl_helpers.o exports both,
  and fallback control.o has no helper dependency, plus broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and the final strict JSON
  audit. The final audit reports host/IKC/mcctrl/IHK modules completion
  99.436%, overall scored source-retirement completion 99.70085820895522%,
  0.2991417910447751 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/control.c moved from 99.9% to
  99.95% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes mcexec_send_signal() body sequencing through Rust while preserving
  the no-Rust C fallback body. This covers mcctrl_usrdata lookup and missing
  usrdata logging, signal descriptor copyin, allocated mcctrl_signal_desc
  zeroing and initialization, sig/pid/tid and fixed siginfo byte publication,
  virt_to_phys(msig) packet argument selection, SCD_MSG_SEND_SIGNAL packet
  reference-CPU/PID/argument shaping, send-wait timeout and do-free handoff,
  negative-send logging, conditional free on failed send, and success free
  ordering. C remains responsible for Linux user-copy and allocation/free
  primitives, virt_to_phys() primitive behavior, IKC send-wait side effects,
  logging side effects, and remaining compiled control.c bridge/fallback
  source; the row is therefore still partial rather than fully retired.
  Validation passed for rustfmt, rustfmt --check, shell syntax, full
  kernel/rust/tests/run_equivalence.sh with direct send-signal success,
  missing-usrdata, copyin-failure, allocation-failure, negative-send/free, and
  negative-send/no-free coverage and mcctrl_helpers digest ff18f88b63d922da,
  Rust-enabled mcctrl_ko build, no-Rust C fallback mcctrl_ko build, symbol
  checks showing strict control.o imports mcctrl_control_start_image_body_result
  and mcctrl_control_send_signal_body_result, mcctrl_helpers.o exports both,
  and fallback control.o has no helper dependency, plus broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4, and the final strict JSON
  audit. The final audit reports host/IKC/mcctrl/IHK modules completion
  99.435625%, overall scored source-retirement completion
  99.70074626865673%, 0.2992537313432706 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. The focused
  equivalence unresolved-symbol failures from compiler-introduced memcpy on
  the signal-info copy and MaybeUninit value move are recorded in kernel.log
  and corrected. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/control.c moved from 99.75% to
  99.9% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes mcexec_start_image() body sequencing through Rust while preserving
  the no-Rust C fallback body. This covers mcctrl_usrdata lookup and missing
  usrdata error logging, program_load_desc allocation and user copyin,
  previous-handler private data lookup, new mcos handler allocation result
  shaping, PID/CPU/user-range/prepare-thread publication, release-handler
  registration and private data publication ordering, receive-CPU selection,
  last_thread_exec publication, SCD_MSG_SCHEDULE_PROCESS packet field shaping
  and send dispatch, negative-send error logging, success-only prepare-thread
  clearing, and descriptor free ordering. C remains responsible for Linux
  user-copy and allocation/free primitives, IHK private-data/release-handler
  primitive behavior, IKC receive-CPU and send side effects, logging side
  effects, and remaining compiled control.c bridge/fallback source; the row is
  therefore still partial rather than fully retired. Validation passed for
  rustfmt, shell syntax, full kernel/rust/tests/run_equivalence.sh with direct
  start-image success, missing-usrdata, allocation-failure, copyin-failure,
  new-info-failure, and negative-send coverage and mcctrl_helpers digest
  a8bfa7b353d25011, Rust-enabled mcctrl_ko build, no-Rust C fallback
  mcctrl_ko build, symbol checks showing strict control.o imports
  mcctrl_control_start_image_body_result, mcctrl_helpers.o exports it, and
  fallback control.o has no helper dependency, plus broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only --skip-deps
  --skip-rust --skip-ihk-patch --jobs 4, and the final strict JSON audit. The
  final audit reports host/IKC/mcctrl/IHK modules completion 99.435%,
  overall scored source-retirement completion 99.70055970149254%,
  0.2994402985074629 percentage points left, 20 debt rows, compiled_retired_c
  0, compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  and headers_with_executable_logic 0. The focused equivalence compile failures
  from the fixture callback name collision and ret-binding cleanup are recorded
  in kernel.log and corrected. No QEMU/KVM guest runtime validation, host
  reboot, or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/syscall.c moved from 97.0% to
  98.5% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes mcctrl_put_per_thread_data_unsafe(),
  mcctrl_put_per_thread_data(), mcctrl_add_per_thread_data(), and
  mcctrl_get_per_thread_data() body sequencing through Rust while preserving
  the no-Rust C fallback bodies. This covers per-thread hash selection,
  hash-list traversal by task, write/read lock callback ordering, allocation
  and zeroing, duplicate detection, PPD/task/data/TID/refcount field
  publication, list insertion/removal, refcount decrement/free and
  increment/get behavior, not-found/use-after-free/invalid-refcount error
  shaping, and log callback dispatch. The same batch also adds Rust-owned
  source-path sequencing for the ENABLE_TOFU file-to-pidfd hash table:
  table initialization, insert, lookup, and remove, including hash-bucket
  initialization, allocation/field publication, Tofu device-name tail copy,
  duplicate rejection, lookup/remove matching, list deletion/free dispatch,
  and missing-entry return shaping. The same batch also routes pager_add_process(),
  pager_remove_process(), and pager_cleanup() lifecycle sequencing through Rust:
  global process-count increment/decrement under the pager spinlock, IRQ-context
  rejection, devobj pager-list drain after semaphore acquisition, global pager-list
  drain, rofile/rwfile fput callback ordering, free callback dispatch, and log
  event selection. The Rocky validation configuration has ENABLE_TOFU OFF, so
  the pidfd wrappers are equivalence-covered source movement rather than imported
  by syscall.o in this build. C remains responsible for Linux allocation/free,
  spin/rw lock and semaphore primitives, fput side effects, task/TID lookup,
  logging side effects, actual Tofu-enabled kernel build coverage, and the
  remaining compiled syscall.c pager I/O, fault, syscall_backward(), and
  syscall-return bodies; the row is therefore still partial rather than fully
  retired. Validation passed for rustfmt, shell syntax, full
  kernel/rust/tests/run_equivalence.sh with direct per-thread lifecycle, pidfd
  hash-table, and pager lifecycle success/error coverage and mcctrl_helpers
  digest 863cea7bbef0bae6, Rust-enabled mcctrl_ko build, no-Rust C fallback
  mcctrl_ko build, symbol checks showing strict syscall.o imports the four
  mcctrl_syscall_*ptd*_body_result helpers plus
  mcctrl_syscall_pager_add_process_body_result,
  mcctrl_syscall_pager_remove_process_body_result, and
  mcctrl_syscall_pager_cleanup_body_result while mcctrl_helpers.o exports those
  plus mcctrl_syscall_ptd_hash_result and the four
  mcctrl_syscall_pidfd_hash_*_body_result helpers, and fallback syscall.o has
  no mcctrl syscall Rust-helper dependency, plus broad Rocky Rust build-only
  validation through scripts/rocky-rust-validation.sh --build-only
  --skip-deps --skip-rust --skip-ihk-patch --jobs 4. The final strict audit
  reports host/IKC/mcctrl/IHK modules completion 99.43312499999999%, overall
  scored source-retirement completion 99.69999999999999%,
  0.30000000000001137 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/control.c moved from 99.5% to
  99.75% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes mcexec_newprocess(), mcexec_destroy_per_process_data(), and
  mcexec_close_exec() body sequencing through Rust while preserving the
  no-Rust C fallback bodies. This covers new mcos handler allocation result
  shaping, current-PID publication, release-handler registration and private
  data publication ordering, destroy-path usrdata lookup and no-usrdata
  return-0 behavior, PPD lookup, double PPD put/log ordering, missing-PPD
  warning selection, close-exec OS-index validation, held-exec drop callback
  dispatch, and found/not-found positive-EINVAL return shaping. C remains
  responsible for Linux/IHK primitive callback behavior, exec-file list
  mutation, file reference release side effects, release-handler callback
  storage, PPD refcount/mutation primitive behavior, logging side effects,
  and remaining compiled control.c bridge/fallback source; the row is
  therefore still partial rather than fully retired. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct newprocess,
  destroy-PPD, and close-exec success/error/null-callback coverage and
  mcctrl_helpers digest 445dd5c47e1bd404, Rust-enabled mcctrl_ko build,
  no-Rust C fallback mcctrl_ko build, symbol checks showing strict control.o
  imports mcctrl_control_newprocess_body_result,
  mcctrl_control_destroy_ppd_body_result, and
  mcctrl_control_close_exec_body_result, mcctrl_helpers.o exports all three,
  and fallback control.o has no helper dependency, plus broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4. The final
  strict audit reports host/IKC/mcctrl/IHK modules completion
  99.41437499999999%, overall scored source-retirement completion
  99.69440298507462%, 0.3055970149253824 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/control.c moved from 99.0% to
  99.5% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes mcexec_strncpy_from_user() body sequencing through Rust while
  preserving the no-Rust C fallback body. This covers descriptor copyin,
  temporary page allocation, page-sized strncpy_from_user() loop chunking,
  full-chunk and NUL-terminated copyout sizing, destination/source/remain
  advancement, direct strncpy error propagation, data copy_to_user failure
  shaping, final descriptor result publication, descriptor copyout failure
  shaping, allocation failure shaping, and page cleanup ordering. C remains
  responsible for Linux user-copy primitives, Linux __get_free_page()/free_page()
  primitive behavior, strncpy_from_user() primitive behavior, and remaining
  compiled control.c bridge/fallback source; the row is therefore still partial
  rather than fully retired. Validation passed for rustfmt, shell syntax,
  scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh with
  direct strncpy full-copy, partial-NUL, data-copy-fault, strncpy-fault,
  descriptor-copyin-fault, allocation-failure, and final-descriptor-copyout
  coverage and mcctrl_helpers digest ba51fb9bfc8752ef, Rust-enabled mcctrl_ko
  build, no-Rust C fallback mcctrl_ko build, symbol checks showing strict
  control.o imports mcctrl_control_strncpy_from_user_body_result,
  mcctrl_helpers.o exports it, and fallback control.o has no helper dependency,
  plus broad Rocky Rust build-only validation through
  scripts/rocky-rust-validation.sh --build-only --skip-deps --skip-rust
  --skip-ihk-patch --jobs 4. The final strict audit reports
  host/IKC/mcctrl/IHK modules completion 99.41125%, overall scored
  source-retirement completion 99.69347014925373%,
  0.3065298507462728 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. The earlier focused
  equivalence fixture compile failure for this slice is recorded in kernel.log
  and corrected. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/control.c moved from 98.5% to
  99.0% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes mcexec_getcred() and mcexec_getcredv() body sequencing through Rust
  while preserving the no-Rust C fallback bodies. This covers fixed eight-slot
  UID/GID publication order, physical-to-virtual destination selection for the
  physical credential path, stack-local credential staging for the user-copy
  path, copy_to_user failure shaping, and missing-callback rejection in the
  focused helper path. C remains responsible for Linux current credential
  primitive values, phys_to_virt() primitive behavior, Linux user-copy
  primitive behavior, and remaining compiled control.c bridge/fallback source;
  the row is therefore still partial rather than fully retired. Validation
  passed for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct getcred/getcredv
  order/copy/error coverage and mcctrl_helpers digest 8fa8df6f537fb616,
  Rust-enabled mcctrl_ko build, no-Rust C fallback mcctrl_ko build, symbol
  checks showing strict control.o imports mcctrl_control_getcred_body_result
  and mcctrl_control_getcredv_body_result, mcctrl_helpers.o exports both, and
  fallback control.o has no helper dependency, plus broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4. The final
  strict audit reports host/IKC/mcctrl/IHK modules completion 99.405%,
  overall scored source-retirement completion 99.69160447761193%,
  0.3083955223880679 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. No QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/control.c moved from 97.5% to
  98.5% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes non-MIC mcexec_transfer_image() and mcexec_load_syscall() body
  sequencing through Rust while preserving the no-Rust C fallback bodies and
  the CONFIG_MIC fallback path. This covers descriptor copyin,
  os-to-device/map-memory/map-virtual callback ordering, transfer direction
  selection, user-copy direction selection, invalid remote-address and
  invalid-direction logging, load dprintk callback ordering, virtual and memory
  unmap ordering on successful transfer/load paths, and the original early
  return cleanup gaps on virtual-map and load-copyout failure paths. C remains
  responsible for Linux user-copy primitives, IHK device map/unmap primitive
  side effects, logging side effects, CONFIG_MIC source, and remaining compiled
  control.c bridge/fallback source; the row is therefore still partial rather
  than fully retired. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh with
  transfer/load success/error/cleanup coverage and mcctrl_helpers digest
  dbc3d2683287dd28, Rust-enabled mcctrl_ko build, no-Rust C fallback
  mcctrl_ko build, forced regeneration of a stale strict control.o build
  artifact, symbol checks showing strict control.o imports
  mcctrl_control_transfer_image_body_result and
  mcctrl_control_load_syscall_body_result, mcctrl_helpers.o exports both, and
  fallback control.o has no helper dependency, plus broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4. The final
  strict audit reports host/IKC/mcctrl/IHK modules completion
  99.39874999999999%,
  overall scored source-retirement completion 99.68973880597014%,
  0.31026119402986296 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. The earlier focused
  equivalence compile failure and Rust-enabled build failure for this batch are
  recorded in kernel.log and corrected. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/control.c moved from 97.0% to
  97.5% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes mcctrl_getrusage() body sequencing through Rust while preserving
  the no-Rust C fallback body. This covers descriptor copyin, OS validation
  ordering, rusage buffer allocation/zeroing selection, fixed-size rusage
  copy loops for memory/page-size/NUMA stats, CPU time accumulation and
  rounding, thread-count publication, size validation, user copyout, and
  cleanup/free ordering. C remains responsible for Linux user-copy
  primitives, kmalloc/kfree, ihk_os_get_rusage() lookup, printk/pr_err side
  effects, rusage source storage, and remaining compiled control.c
  bridge/fallback source; the row is therefore still partial rather than
  fully retired. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh with
  getrusage success/error/cleanup coverage and mcctrl_helpers digest
  64d2a15c7bdf4238, Rust-enabled mcctrl_ko build, no-Rust C fallback
  mcctrl_ko build, symbol checks showing strict control.o imports
  mcctrl_control_getrusage_body_result, mcctrl_helpers.o exports it, and
  fallback control.o has no helper dependency, plus broad Rocky Rust
  build-only validation through scripts/rocky-rust-validation.sh
  --build-only --skip-deps --skip-rust --skip-ihk-patch --jobs 4. The final
  strict audit reports host/IKC/mcctrl/IHK modules completion
  99.38624999999999%, overall scored source-retirement completion
  99.68600746268656%, 0.3139925373134389 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0.
- 2026-06-15: executer/kernel/mcctrl/control.c moved from 95.0% to
  97.0%, and executer/kernel/mcctrl/procfs.c moved from 98.0% to
  99.5% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes control perf set/get/enable/disable sequencing plus procfs getpath,
  add_procfs_entries(), find_procfs_entry(), and get_pid_cred() sequencing
  through Rust while preserving the no-Rust C fallback bodies. This covers
  perf OS validation, usrdata/CPU-info lookup ordering, event-count read/write,
  user attr copy-before-allocation callback dispatch, descriptor initialization,
  per-event/per-CPU send loops, registration count publication, read-value
  accumulation and copyout, counter-mask construction, send/error cleanup, and
  descriptor error propagation. It also covers procfs path-buffer construction,
  entry-table iteration and add callback dispatch, child-list traversal and
  name matching, and RCU pid lookup plus credential callback sequencing. C
  remains responsible for Linux user-copy primitives, kmalloc/kfree,
  virt_to_phys(), actual IKC send side effects, perf descriptor layout,
  CPU-info/usrdata primitive field access, procfs create/delete/list mutation,
  Linux RCU/pid/task/cred primitives, logging side effects, and remaining
  compiled control.c/procfs.c bridge/fallback source; both rows are therefore
  still partial rather than fully retired. Validation passed for rustfmt,
  shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with procfs path/list/cred and perf
  set/get/enable/disable success/error/cleanup coverage and mcctrl_helpers
  digest 22f807be65af8134, Rust-enabled mcctrl_ko build, no-Rust C fallback
  mcctrl_ko build, symbol checks showing strict procfs.o imports
  mcctrl_procfs_getpath_body_result, mcctrl_procfs_add_entries_body_result,
  mcctrl_procfs_find_entry_body_result, and
  mcctrl_procfs_get_pid_cred_body_result while strict control.o imports
  mcctrl_control_perf_set_body_result, mcctrl_control_perf_get_body_result, and
  mcctrl_control_perf_enable_disable_body_result, mcctrl_helpers.o exports all
  seven helpers, and fallback procfs.o/control.o have no helper dependency,
  broad Rocky Rust build-only validation for mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect, sched_yield,
  ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib, and final
  strict audit with host/IKC/mcctrl/IHK modules completion 99.38%, overall
  scored completion 99.68414179104477%, 0.315858208955234 percentage points
  left, 20 debt rows, compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], and headers_with_executable_logic
  0. The earlier focused procfs equivalence and Rust-enabled build failures
  are recorded in kernel.log and corrected. No QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/syscall.c moved from 95.0% to
  97.0% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes pager IRQ and normal pager request dispatch, remote page-fault packet
  publication/send/log sequencing, mcctrl_clear_pte_range() VMA traversal and
  zap-action selection, and release_user_space() VMA traversal/munmap error
  propagation through Rust while preserving the no-Rust C fallback bodies.
  This covers release-only pager IRQ dispatch, create/read/write/map/pfn/
  unmap/mlock-list pager request selection, unknown pager request logging,
  missing-callback rejection, SCD_MSG_REMOTE_PAGE_FAULT field publication,
  target-CPU send dispatch, failure/completion logging, clear-PTE gap clipping,
  PFNMAP versus full-range zap selection, legacy zap retry behavior, read-lock
  callback ordering, release-space VMA gap handling, first munmap error
  retention, and release error logging. C remains responsible for the actual
  pager request bodies, Linux find_vma/zap/vm_munmap callbacks, mmap locks,
  VMA flag mutation, page-table side effects, syscall_backward(),
  rus_vm_fault(), __return_syscall(), remap_user_space(), writecore(),
  per-thread/per-process data mutation, file-to-pidfd hash mutation, and
  remaining compiled syscall.c bridge/fallback source; the row is therefore
  still partial rather than fully retired. Validation passed for rustfmt,
  shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with pager/remote-page-fault/
  clear-PTE/release-space success/error/missing-callback coverage and
  mcctrl_helpers digest d879f50d2f0a3be8, Rust-enabled mcctrl_ko build,
  no-Rust C fallback mcctrl_ko build, symbol checks showing strict syscall.o
  imports mcctrl_pager_call_irq_body_result,
  mcctrl_pager_call_body_result, mcctrl_remote_page_fault_body_result,
  mcctrl_clear_pte_range_body_result, and
  mcctrl_user_space_release_body_result while mcctrl_helpers.o exports them
  and fallback syscall.o has no helper dependency, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, and final strict audit with host/IKC/mcctrl/IHK modules
  completion 99.33624999999999%, overall scored completion
  99.67108208955223%, 0.3289179104477711 percentage points left, 20 debt
  rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], and headers_with_executable_logic 0. The
  earlier focused formatting failure in this mcctrl syscall continuation is
  recorded in kernel.log and corrected. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/futex.c moved from 92.0% to
  95.0%, and executer/kernel/mcctrl/sysfs.c moved from 99.75% to
  99.9% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes futex command dispatch, futex wait-loop sequencing, sysfsm top-level
  show/store/release dispatch, sysfs response completion, and local snooping
  show callback sequencing through Rust while preserving the no-Rust C
  fallback bodies. This covers futex operation normalization and command
  selection, FUTEX_WAIT expected-value checks and queue setup/wait/timeout
  result shaping, sysfs init and file-type dispatch, response success/error
  handling, release busy-state cleanup, and the i32/i64/u32/u64/string/u32k/
  bitmap local snooping show formatting wrappers. C remains responsible for
  futex key lookup, hash-bucket locking, waitqueue primitive behavior, wake/
  requeue/wake-op/user-copy/pagefault/atomic side effects, sysfs semaphore and
  waitqueue primitive behavior, IKC send side effects, path/tree/topology/local
  create and cleanup side effects, and remaining compiled futex.c and sysfs.c
  bridge/fallback source; both rows are therefore still partial rather than
  fully retired. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh with direct
  futex and sysfs success/error/missing-callback coverage and mcctrl_helpers
  digest d3a184a7605b0dce, Rust-enabled mcctrl_ko build, no-Rust C fallback
  mcctrl_ko build, symbol checks showing strict futex.o and sysfs.o import the
  new mcctrl_futex_* and mcctrl_sysfs_* helpers while mcctrl_helpers.o exports
  them and fallback objects have no helper dependency, broad Rocky Rust
  build-only validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko,
  mcctrl_ko, mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, ihkmond, and ihklib, and final strict audit with
  host/IKC/mcctrl/IHK modules completion 99.31125%, overall scored completion
  99.66361940298506%, 0.33638059701493717 percentage points left,
  20 debt rows, compiled_retired_c 0, compiled_non_allowlisted_c 29, stale
  tracker rows [], untracked files [], and headers_with_executable_logic 0.
  The earlier focused equivalence failure for this batch is recorded in
  kernel.log and corrected. No QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.
- 2026-06-15: executer/kernel/mcctrl/driver.c moved from 90.0% to
  95.0% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes eight driver/module lifecycle bodies through Rust while preserving
  the no-Rust C fallback bodies: mcctrl_ioctl(), osnum_to_os(),
  mcctrl_os_alive(), mcctrl_os_boot_notifier(),
  mcctrl_os_shutdown_notifier(), symbols_init(), mcctrl_init(), and
  mcctrl_exit(). This covers control dispatch argument shaping, OS slot
  lookup/alive scan, boot notifier OS lookup/slot publication/channel setup/
  kernel-callback registration/user-callback registration/procfs setup and
  cleanup failure ordering, shutdown notifier pager/sysfs/topology/user
  callback/kernel callback/channel/procfs cleanup ordering, required symbol
  lookup/publication/missing-symbol rejection, module init syscall/binfmt/TOFU
  setup/symbol/notifier sequencing, and module exit deregister/binfmt/UTI/TOFU
  cleanup ordering. C remains responsible for Linux/IHK primitive callback
  side effects, function-pointer storage, version-specific symbol lookup
  bridge callbacks, binfmt compatibility bridge callbacks, OS notifier
  registration primitives, static module metadata, and remaining compiled
  driver.c bridge/fallback source; the row is therefore still partial rather
  than fully retired. Validation passed for rustfmt after the recorded
  formatting fix, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct driver success/error/
  missing-callback coverage and mcctrl_helpers digest cc1003ad8f80f75a,
  Rust-enabled mcctrl_ko build, no-Rust C fallback mcctrl_ko build, symbol
  checks showing strict driver.o imports all eight new mcctrl_driver_* helpers
  while mcctrl_helpers.o exports them and fallback driver.o has no helper
  dependency, broad Rocky Rust build-only validation for mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect, sched_yield,
  ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib, and final
  strict audit with host/IKC/mcctrl/IHK modules completion 99.271875%,
  overall scored completion 99.6518656716418%,
  0.34813432835819924 percentage points left, 20 debt rows,
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], and headers_with_executable_logic 0. The earlier focused
  formatting failure for this batch is recorded in kernel.log and corrected.
  /dev/kvm is not present in this command context, so no QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-14: executer/kernel/mcctrl/control.c moved from 92.5% to
  95.0% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes eight wrapper bodies through Rust while preserving the no-Rust C
  fallback bodies: mcexec_pin_region(), mcexec_free_region(),
  mcexec_sys_mount(), mcexec_sys_umount(), mcexec_sys_unshare(),
  mcexec_release_user_space(), mcctrl_perf_num(), and
  mcctrl_perf_destroy(). This covers DMA descriptor copyin, order selection,
  allocation failure shaping, virtual/physical address conversion, copyout
  failure shaping, free-DMA dispatch, credential preparation/cap raise/
  override/revert/put ordering, mount/umount/unshare argument publication,
  release-user-space length shaping and clear-PTE dispatch, perf usrdata
  validation/logging, perf event-number publication, and perf destroy
  disable/zero dispatch ordering. C remains responsible for actual Linux
  user-copy primitives, page allocation/free primitives, credential primitive
  behavior, mount/umount/unshare side effects, clear-PTE side effects, perf
  primitive side effects, printk/pr_err side effects, and remaining control.c
  execution bodies. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh with direct
  pin/free-DMA, mount/umount/unshare, release-user-space, perf-num, and
  perf-destroy success/error/missing-callback coverage and mcctrl_helpers
  digest 6143cb85715748ec, Rust-enabled mcctrl_ko build, no-Rust C fallback
  mcctrl_ko build, symbol checks showing strict control.o imports all eight
  new mcctrl_control_*_body_result helpers while mcctrl_helpers.o exports
  them and fallback control.o has no helper dependency, broad Rocky Rust
  build-only validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko,
  mcctrl_ko, mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, ihkmond, and ihklib, and final strict audit with
  host/IKC/mcctrl/IHK modules completion 99.209375%, overall scored
  completion 99.63320895522388%, 0.36679104477612157 percentage points left,
  20 debt rows, compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], and headers_with_executable_logic
  0. The earlier focused equivalence fixture compile failure for this batch is
  recorded in kernel.log and corrected. /dev/kvm is not present in this
  command context, so no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-14: executer/kernel/mcctrl/control.c moved from 91.5% to
  92.5% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes mcctrl_get_request_os_cpu() body sequencing through Rust while
  preserving the no-Rust C fallback body. This covers OS/output validation,
  usrdata lookup null/error shaping, current PID/TID and task callback
  dispatch, per-process lookup, per-thread lookup and release ordering,
  registered packet lookup, packet ref extraction, channel read-CPU lookup,
  result publication, get/put PTD logging order, and missing-callback
  rejection. C remains responsible for Linux/IHK object lookup primitive
  behavior, task/current access bridge callbacks, mcctrl per-process and
  per-thread refcount side effects, channel storage internals, printk/pr_err
  side effects, and remaining control.c execution bodies. Validation passed
  for rustfmt after the recorded formatting fix, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh with direct
  request-CPU success, null-os, validate-failure, null-output,
  missing-usrdata, missing-PPD, missing-PTD, missing-packet, and
  missing-callback coverage and mcctrl_helpers digest 550b47b0a328cc6d,
  Rust-enabled mcctrl_ko build, no-Rust C fallback mcctrl_ko build, symbol
  checks showing strict control.o imports
  mcctrl_control_get_request_os_cpu_body_result while mcctrl_helpers.o exports
  it and fallback control.o has no helper dependency, broad Rocky Rust
  build-only validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko,
  mcctrl_ko, mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, ihkmond, and ihklib, and final strict audit with
  host/IKC/mcctrl/IHK modules completion 99.178125%, overall scored
  completion 99.62388059701493%, 0.3761194029850685 percentage points left,
  20 debt rows, compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], and headers_with_executable_logic
  0. The earlier focused formatting failure for this slice is recorded in
  kernel.log and corrected. /dev/kvm is not present in this command context,
  so no QEMU/KVM guest runtime validation, host reboot, or reboot-capable
  script was run.
- 2026-06-14: executer/kernel/mcctrl/control.c moved from 90.5% to
  91.5% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes __mcctrl_os_read_write_cpu_register() body sequencing through Rust
  while preserving the no-Rust C fallback body. This covers usrdata lookup
  null/error shaping, CPU-index validation, descriptor allocation failure
  shaping, descriptor copy to a send-stable buffer, SCD_MSG_CPU_RW_REG packet
  zeroing and field publication, physical descriptor publication, send-wait
  timeout argument shaping, send-failure logging and cleanup, read-result
  copyback selection, sync publication, success log callback ordering, and
  do_free cleanup ordering. C remains responsible for Linux/IHK object lookup
  primitive behavior, cpu_info field access bridge callbacks, kmalloc/kfree,
  virt_to_phys(), actual mcctrl_ikc_send_wait() side effects, Linux atomic
  primitive compatibility, printk/pr_err side effects, and remaining control.c
  execution bodies. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh with direct
  CPU-register read, write, missing-usrdata, bad-CPU, allocation-failure,
  send-failure cleanup, null-callback, and null-desc coverage and
  mcctrl_helpers digest 9087e1494cbaad76, Rust-enabled mcctrl_ko build,
  no-Rust C fallback mcctrl_ko build, symbol checks showing strict control.o
  imports mcctrl_control_cpu_register_body_result while mcctrl_helpers.o
  exports it and fallback control.o has no helper dependency, broad Rocky Rust
  build-only validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko,
  mcctrl_ko, mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, ihkmond, and ihklib, and final strict audit with
  host/IKC/mcctrl/IHK modules completion 99.165625%, overall scored
  completion 99.62014925373134%, 0.37985074626865867 percentage points left,
  20 debt rows, compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], and headers_with_executable_logic
  0. The earlier focused formatting failure for this slice is recorded in
  kernel.log and corrected. /dev/kvm is not present in this command context,
  so no QEMU/KVM guest runtime validation, host reboot, or reboot-capable
  script was run.
- 2026-06-14: executer/kernel/mcctrl/control.c moved from 90.0% to
  90.5% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes the debug-log request body, CPU-count validation body, and NUMA-node
  count validation body through Rust while preserving the no-Rust C fallback
  bodies. This covers SCD debug-log packet zeroing/field shaping, IKC send
  dispatch with original return-ignore behavior, CPU-info lookup null/error
  shaping, invalid CPU-count rejection, usrdata/mem-info lookup null/error
  shaping, NUMA-node count return shaping, and missing-callback direct-helper
  rejection. C remains responsible for Linux/IHK object lookup primitive
  behavior, field access bridge callbacks, printk/pr_err side effects, actual
  mcctrl_ikc_send() side effects, user-copy, allocation/free, lock/list
  mutation, perf/mcexec side effects, and the remaining control.c execution
  bodies. Validation passed for rustfmt, shell syntax, scoped whitespace
  checks, full kernel/rust/tests/run_equivalence.sh with direct debug-log,
  get-CPU, get-nodes, error-path, and missing-callback coverage and
  mcctrl_helpers digest 162b7027820ca77d, Rust-enabled mcctrl_ko build,
  no-Rust C fallback mcctrl_ko build, symbol checks showing strict control.o
  imports mcctrl_control_debug_log_body_result,
  mcctrl_control_get_cpu_body_result, and
  mcctrl_control_get_nodes_body_result while mcctrl_helpers.o exports them and
  fallback control.o has no helper dependency, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, and final strict audit with
  host/IKC/mcctrl/IHK modules completion 99.153125%, overall scored
  completion 99.61641791044777%, 0.3835820895522346 percentage points left,
  20 debt rows, compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], and headers_with_executable_logic
  0. /dev/kvm is not present in this command context, so no QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-14: executer/kernel/mcctrl/sysfs.c moved from 99.5% to
  99.75% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes remote sysfs release sequencing through Rust while preserving the
  no-Rust C fallback body. This covers file/client/init gating, semaphore
  acquisition/release callback ordering, interrupted pre-send and post-send
  wait shaping, sysfs release request packet field shaping, busy publication,
  IKC send dispatch, and final error return shaping for the C wrapper log
  path. C remains responsible for Linux semaphore and waitqueue primitive
  behavior, actual mcctrl_ikc_send() side effects, sysfsm node/sysfs buffer
  storage, kobject/sysfs wiring, local and special sysfs operations, path/tree
  mutation, topology sysfs setup, and the no-Rust fallback implementation.
  Validation passed for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct remote release success,
  skipped-release, interrupted-wait, send-failure, and missing-callback
  coverage and mcctrl_helpers digest 86701ce556fdfbcb, Rust-enabled mcctrl_ko
  build, no-Rust C fallback mcctrl_ko build, symbol checks showing strict
  sysfs.o imports mcctrl_sysfs_remote_release_body_result while
  mcctrl_helpers.o exports it and fallback sysfs.o has no helper dependency,
  and broad Rocky Rust build-only validation for mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect, sched_yield,
  ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib. A
  temporary attempt to mark this row 100.0% failed the strict audit because
  the Rocky Rust build still compiles executer/kernel/mcctrl/sysfs.c; that
  failure is recorded in kernel.log, and this entry keeps the row as partial
  debt instead of claiming full source retirement. The corrected final JSON
  audit passed with compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], 20 debt rows,
  headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules completion
  99.146875%, overall scored source-retirement completion
  99.61455223880597%, and total percentage points left
  0.3854477611940297%. /dev/kvm was not present in this command context, so
  no QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.
- 2026-06-14: executer/kernel/mcctrl/sysfs.c moved from 99.0% to
  99.5% for the strict source-retirement audit. Rust-enabled mcctrl now routes
  remote sysfs show/store sequencing through Rust while preserving the no-Rust
  C fallback bodies. This covers initialized-buffer gating, semaphore
  acquisition/release callback ordering, interrupted wait shaping, store-size
  rejection, sysfs request packet field shaping, busy publication, IKC send
  dispatch, response wait/result handling, copy direction selection, and final
  ssize/error return shaping. C remains responsible for Linux semaphore and
  waitqueue primitive behavior, actual mcctrl_ikc_send() side effects,
  memcpy(), sysfsm node/sysfs buffer storage, kobject/sysfs wiring, local and
  special sysfs operations, path/tree mutation, topology sysfs setup, and the
  remaining executer/kernel/mcctrl/sysfs.c execution bodies. Validation passed
  for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct remote show/store
  success/error coverage and mcctrl_helpers digest b2bab14d18a10b52,
  Rust-enabled mcctrl_ko build, no-Rust C fallback mcctrl_ko build, symbol
  checks showing strict sysfs.o imports mcctrl_sysfs_remote_show_body_result
  and mcctrl_sysfs_remote_store_body_result while mcctrl_helpers.o exports both
  and fallback sysfs.o has no helper dependency, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, and the final JSON source-retirement audit. The audit
  passed with compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], 20 debt rows,
  headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules completion
  99.14375%, overall scored source-retirement completion 99.61361940298508%,
  and total percentage points left 0.3863805970149201%. The earlier focused
  equivalence failure for this slice is recorded in kernel.log and corrected.
  /dev/kvm was not present in this command context, so no QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 95.0% to 100.0%
  for the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_utofu_ioctl_alloc_stag() top-level allocation ioctl sequencing through
  Rust while preserving the matching no-Rust C fallback body for ENABLE_TOFU
  builds. This covers device-to-CQ resolution, enabled-CQ rejection, request
  copyin validation, STAG/VA/length validation, pre-existing special-STAG
  rejection, current-VM lookup, VM read-lock/unlock and stack-fault retry
  ordering, page-size selection for LPG requests, CQ lock/unlock ordering,
  normal-STAG translation search, reserve failure to -ENOSPC, new-steering
  allocation and release-on-failure dispatch, MBPT-start offset publication,
  special-STAG recheck and plus-MBVA calculation, range insertion, copyout
  error shaping, and missing-callback rejection. C remains responsible for
  primitive user-copy side effects, process-VM/range lookup internals, actual
  page-fault behavior, lock primitive bodies, Tofu hardware/register mutation
  callbacks, generated Tofu layouts, and the no-Rust ENABLE_TOFU fallback
  implementation. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh with direct
  C-vs-Rust alloc-STAG coverage and tofu_uapi digest 0c69fd9ec625ad4a, Rust
  ENABLE_TOFU mckernel.img build, no-Rust ENABLE_TOFU fallback mckernel.img
  build, strict Rust mckernel.img build, no-Rust C fallback mckernel.img
  build, symbol checks showing Rust export and Rust ENABLE_TOFU import
  tofu_utofu_ioctl_alloc_stag_body_result while the no-Rust ENABLE_TOFU
  fallback object has no helper dependency, and broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  and ihkmond. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run because /dev/kvm is not present in this
  command context.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 90.0% to 95.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_utofu_alloc_new_steering() top-level allocation and steering setup
  sequencing through Rust while preserving the matching no-Rust C fallback
  body for ENABLE_TOFU builds. This covers page and MBPT sizing, MBPT start
  calculation callback ordering, MBPT allocation callback dispatch, MBPT
  metadata publication, update callback dispatch, update-failure rollback and
  free dispatch, MBVA selection, enable-MB callback ordering,
  enable-steering callback ordering, translation-enable callback ordering,
  profile event callback ordering, and raw error callback ordering. C remains
  responsible for MBPT allocation/free primitive side effects, page-table/PTE
  lookup, DMA translation, hardware/register mutation callbacks, profile
  timestamp side effects, generated Tofu layouts, process/VM hooks, and
  remaining kernel/tofu/tof_utofu_main.c execution bodies. Validation first
  failed in the generated focused equivalence fixture because ENOMEM was used
  without a definition; that failure is recorded in kernel.log and corrected.
  Final validation passed for rustfmt, shell syntax, scoped whitespace checks,
  full kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust Tofu
  alloc-new-steering coverage and tofu_uapi digest 3eac863b33ad9069, Rust
  ENABLE_TOFU mckernel.img build, no-Rust ENABLE_TOFU fallback mckernel.img
  build, strict Rust mckernel.img build, no-Rust C fallback mckernel.img
  build, symbol checks showing Rust export and Rust ENABLE_TOFU import
  tofu_utofu_alloc_new_steering_body_result while the no-Rust ENABLE_TOFU
  fallback object has no helper dependency, and broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  and ihkmond. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run because /dev/kvm is not present in this
  command context.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 85.0% to 90.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_utofu_release_cq() release decision sequencing through Rust while
  preserving the matching no-Rust C fallback body for ENABLE_TOFU builds. This
  covers disabled-UCQ log selection, range-drain callback ordering, do-free
  flag publication, final all-STag cleanup loop bounds and callback dispatch,
  release-complete logging callback ordering, and direct-helper null/count
  rejection. C remains responsible for process-VM lookup, tofu_stag_hash list
  traversal and removal, spinlock primitive side effects, mru-lock primitive
  side effects, actual tof_utofu_free_stag() side effects, generated Tofu
  layouts, process/VM hooks, and remaining kernel/tofu/tof_utofu_main.c
  execution bodies. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh with direct
  C-vs-Rust Tofu release-CQ coverage and tofu_uapi digest
  1dbc11f3f27b2ffe, strict Rust mckernel.img build, no-Rust C fallback
  mckernel.img build, Rust ENABLE_TOFU mckernel.img build, no-Rust C fallback
  ENABLE_TOFU mckernel.img build, symbol checks showing Rust export and Rust
  ENABLE_TOFU import tofu_utofu_release_cq_body_result while the no-Rust
  ENABLE_TOFU fallback object has no helper dependency, and broad Rocky Rust
  build-only validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko,
  mcctrl_ko, mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, and ihkmond. No QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run because /dev/kvm is not
  present in this command context.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 80.0% to 85.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_core_cacheflush_timeout() traversal and tof_core_cq_cacheflush()
  timeout/retry/panic sequencing through Rust while preserving the matching
  no-Rust C fallback bodies for ENABLE_TOFU builds. This covers TNI/CQ
  traversal, kernel-CQ skip selection, CQS register callback dispatch,
  steering-disable MMIO write ordering, write-barrier callback ordering,
  cacheflush write dispatch, first timeout detection, panic-disabled timeout
  callback dispatch, second timeout retry, panic callback selection, and
  direct-helper missing-callback/null-register rejection. C remains responsible
  for actual CQ storage lookup, MMIO write/read-spin primitive side effects,
  hardware register behavior, timeout recursion side effects, panic
  implementation, generated Tofu layouts, process/VM hooks, and remaining
  kernel/tofu/tof_utofu_main.c execution bodies. Validation passed for rustfmt,
  shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust Tofu cacheflush
  coverage and tofu_uapi digest a4fc1a4186014db0, strict Rust mckernel.img
  build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU mckernel.img
  build, no-Rust C fallback ENABLE_TOFU mckernel.img build, symbol checks
  showing Rust exports and Rust ENABLE_TOFU imports
  tofu_core_cacheflush_timeout_body_result and
  tofu_core_cq_cacheflush_body_result while the no-Rust ENABLE_TOFU fallback
  object has no helper dependency, and broad Rocky Rust build-only validation
  for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, and
  ihkmond. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run because /dev/kvm is not present in this
  command context.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 74.0% to 80.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_utofu_init_globals() and tof_utofu_finalize() top-level lifecycle
  sequencing through Rust while preserving the matching no-Rust C fallback
  bodies for ENABLE_TOFU builds. This covers Tofu-global lookup and missing
  global no-op/log shaping, global pointer publication callback ordering,
  global debug-log callback dispatch, Tofu cache clearing callback dispatch,
  TNI/CQ lock initialization loop ordering, completion logging, finalize
  current-process enable gate selection, STAG scan/log dispatch ordering, and
  Linux vmalloc clear-range argument shaping. C remains responsible for actual
  global pointer publication side effects, cache-memory mutation, spinlock
  primitive bodies, stray-STAG list traversal and formatting, clear-range
  side effects, generated Tofu layouts, process/VM hooks, and remaining
  kernel/tofu/tof_utofu_main.c execution bodies. Validation passed for rustfmt,
  shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust Tofu lifecycle
  coverage and tofu_uapi digest 8931b79b07d3ec27, strict Rust mckernel.img
  build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU mckernel.img
  build, no-Rust C fallback ENABLE_TOFU mckernel.img build, symbol checks
  showing Rust exports and Rust ENABLE_TOFU imports
  tofu_utofu_init_globals_body_result and tofu_utofu_finalize_body_result while
  the no-Rust ENABLE_TOFU fallback object has no helper dependency, and broad
  Rocky Rust build-only validation for mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect, sched_yield,
  ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  and ihkmond. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run because /dev/kvm is not present in this
  command context.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 72.0% to 74.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes the
  shared disable-BCH ioctl/release device-wrapper sequencing through Rust while
  preserving the matching no-Rust C fallback bodies for ENABLE_TOFU builds.
  This covers device-to-BG container offset calculation, log callback ordering,
  disable callback dispatch, callback error propagation, and null
  dev/callback rejection. C remains responsible for the actual
  tof_utofu_disable_bch() side effects, BCH hardware disable behavior, BG
  unset dispatch side effects, memory barriers, dkprintf implementation, Tofu
  process/VM hooks, generated Tofu layouts, and remaining
  kernel/tofu/tof_utofu_main.c execution bodies. Validation first failed in the
  focused equivalence fixture because the generated smoke C used offsetof()
  without a matching include; that failure is recorded in kernel.log and was
  corrected with pointer-subtraction offset calculation. Validation then passed
  for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust disable-BCH
  device-wrapper coverage and tofu_uapi digest 60037f5dce1931a8, strict Rust
  mckernel.img build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU
  mckernel.img build, no-Rust C fallback ENABLE_TOFU mckernel.img build, symbol
  checks showing Rust exports and Rust ENABLE_TOFU imports
  tofu_utofu_bch_device_disable_body_result while the no-Rust ENABLE_TOFU
  fallback object has no helper dependency, and broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  and ihkmond. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run because /dev/kvm is not present in this
  command context.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 70.0% to 72.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_utofu_unset_bg() post-copy BG lookup and unset dispatch sequencing
  through Rust while preserving the matching no-Rust C fallback body for
  ENABLE_TOFU builds. This covers copied request null rejection, callback null
  rejection, TNI/gate lookup callback dispatch, missing-BG -EINVAL shaping,
  unset callback dispatch, and callback error propagation. C remains
  responsible for copy_from_user(), debug logging, actual __tof_utofu_unset_bg()
  side effects, BG hardware register mutation, signal unregister side effects,
  generated Tofu layouts, page-table/PTE lookup, BCH DMA translation, Tofu
  process/VM hooks, and remaining kernel/tofu/tof_utofu_main.c execution
  bodies. Validation passed for rustfmt, shell syntax, scoped whitespace
  checks, full kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust
  unset-BG post-copy coverage and tofu_uapi digest 96f7c484adcf403d, strict
  Rust mckernel.img build, no-Rust C fallback mckernel.img build, Rust
  ENABLE_TOFU mckernel.img build, no-Rust C fallback ENABLE_TOFU mckernel.img
  build, symbol checks showing Rust exports and Rust ENABLE_TOFU imports
  tofu_utofu_unset_bg_after_copy_body_result while the no-Rust ENABLE_TOFU
  fallback object has no helper dependency, and broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  and ihkmond. No QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run because /dev/kvm is not present in this
  command context.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 65.0% to 70.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_utofu_free_stag() validation and body sequencing through Rust while
  preserving the matching no-Rust C fallback body for ENABLE_TOFU builds. This
  covers STAG bounds validation, steering-present/enabled checks, disabled-STAG
  -ENOENT shaping, non-McKernel-kref rejection and logging callback dispatch,
  steering/MB disable callback dispatch, translation disable callback dispatch,
  write-barrier callback ordering, cacheflush callback dispatch, kref-put
  callback dispatch, MRU mbpt pointer clearing callback dispatch, deallocation
  logging callback dispatch, profile-event stage ordering, and missing-callback
  direct-helper rejection. C remains responsible for mru-lock acquisition and
  release at callers, actual kref mutation, actual cacheflush behavior, profile
  timestamp/event side effects, MMIO entry mutation primitives, generated Tofu
  layouts, page-table/PTE lookup, BCH DMA translation, Tofu process/VM hooks,
  and remaining kernel/tofu/tof_utofu_main.c execution bodies. Validation passed
  for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust free-STag body
  coverage and tofu_uapi digest 023e6a70125638a0, strict Rust mckernel.img
  build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU mckernel.img
  build, no-Rust C fallback ENABLE_TOFU mckernel.img build, symbol checks
  showing Rust exports and Rust ENABLE_TOFU imports
  tofu_utofu_free_stag_body_result while the no-Rust ENABLE_TOFU fallback object
  has no helper dependency, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, and the final build-aware JSON source-retirement audit. The audit
  passed with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale
  tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, Other in-scope code completion
  99.62244897959184%, overall scored source-retirement completion
  99.49981343283582%, and total percentage points left
  0.5001865671641781%. /dev/kvm was not present in this command context, so no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 62.0% to 65.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_utofu_ioctl_free_stags() post-request-copy validation, staged user-array
  copy, per-STag free dispatch, range cleanup dispatch, partial failure
  copyout, no-free accounting, final copyout, output logging callback dispatch,
  and return shaping through Rust while preserving the matching no-Rust C
  fallback body for ENABLE_TOFU builds. C remains responsible for the initial
  ioctl request copy, actual copy_from_user()/copy_to_user() side effects,
  mru-lock acquisition/release, tof_utofu_free_stag() side effects, Tofu STAG
  range lookup/removal mutation, generated Tofu layouts, page-table/PTE lookup,
  BCH DMA translation, Tofu process/VM hooks, and remaining
  kernel/tofu/tof_utofu_main.c execution bodies. Validation passed for rustfmt,
  shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust free-STags
  coverage and tofu_uapi digest 9b912ccad9ee2a09, strict Rust mckernel.img
  build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU mckernel.img
  build, no-Rust C fallback ENABLE_TOFU mckernel.img build, symbol checks
  showing Rust exports and Rust ENABLE_TOFU imports
  tofu_utofu_free_stags_after_req_copy_body_result while the no-Rust ENABLE_TOFU
  fallback object has no helper dependency, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, and the final build-aware JSON source-retirement audit.
  The audit passed with compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, Other in-scope code completion
  99.57142857142857%, overall scored source-retirement completion
  99.48115671641791%, and total percentage points left
  0.5188432835820862%. /dev/kvm was not present in this command context, so no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 58.0% to 62.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes the
  post-copy tof_utofu_set_bg() validation and commit sequencing through Rust
  while preserving the matching no-Rust C fallback body for ENABLE_TOFU builds.
  This covers target BG lookup callback dispatch and null-BG rejection, TNI and
  gate bounds validation, packed BG metadata read callback dispatch, subnet
  unpacking and inclusion checks, local and remote gate/TNI validation,
  already-enabled -EBUSY shaping, core set-BG callback dispatch, set-BG error
  logging, enabled-state publication, BCH bgmask mutation callback dispatch,
  signal registration callback dispatch, raw_rc_output callback routing for
  validation failures, and missing-callback direct-helper rejection. C remains
  responsible for copy_from_user(), debug logging, generated layout field
  access through tiny callbacks, actual tof_core_set_bg() side effects, actual
  signal registration side effects, bgmask primitive mutation, page-table/PTE
  lookup, BCH DMA translation, Tofu process/VM hooks, and remaining
  kernel/tofu/tof_utofu_main.c execution bodies. Validation passed for rustfmt,
  shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust post-copy set-BG
  coverage and tofu_uapi digest 5e5b80123a60733c, strict Rust mckernel.img
  build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU mckernel.img
  build, no-Rust C fallback ENABLE_TOFU mckernel.img build, symbol checks
  showing Rust exports and Rust ENABLE_TOFU imports
  tofu_utofu_set_bg_after_copy_body_result while the no-Rust ENABLE_TOFU
  fallback object has no helper dependency, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, and the final build-aware JSON source-retirement audit.
  The audit passed with compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, Other in-scope code completion
  99.54081632653062%, overall scored source-retirement completion
  99.46996268656717%, and total percentage points left
  0.5300373134328282%. The earlier focused formatting failure for this slice is
  recorded in kernel.log and corrected. /dev/kvm was not present in this
  command context, so no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 53.0% to 58.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_core_set_bg() body sequencing through Rust while preserving the matching
  no-Rust C fallback body for ENABLE_TOFU builds. This covers BG lookup
  callback dispatch and null-BG rejection, BG lock/unlock callback ordering,
  subnet and GPID publication callback ordering, IRQ mask reset and IMC reset
  callback ordering, Rust-owned local/remote link and signal-mask computation,
  BGS register write ordering for signal mask, local link, remote link, subnet,
  GPID/BSEQ, and enable, wmb callback ordering, BG state read-spin callback
  argument shaping, -ETIMEDOUT return shaping, null request rejection, and
  missing-callback direct-helper rejection. C remains responsible for
  tof_core_bg_get() storage, lock primitive side effects, IRQ reset primitive
  side effects, actual MMIO writes and readq/rdtsc polling behavior,
  copy_from_user(), page-table/PTE lookup, BCH DMA translation, Tofu process/VM
  hooks, generated Tofu layouts, and most of kernel/tofu/tof_utofu_main.c.
  Validation passed for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust core set-BG
  coverage and tofu_uapi digest f1bf1b90ffd9357a, strict Rust mckernel.img
  build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU mckernel.img
  build, no-Rust C fallback ENABLE_TOFU mckernel.img build, symbol checks
  showing Rust exports and Rust ENABLE_TOFU imports
  tofu_core_set_bg_body_result while the no-Rust ENABLE_TOFU fallback object has
  no helper dependency, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, and the final build-aware JSON source-retirement audit. The audit
  passed with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale
  tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, Other in-scope code completion 99.5%,
  overall scored source-retirement completion 99.45503731343284%, and total
  percentage points left 0.5449626865671604%. /dev/kvm was not present in this
  command context, so no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 51.0% to 53.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_core_enable_bch() validation, BCH notice-IPA write, BCH mask clear write,
  and BCH mask-status polling/timeout return shaping through Rust while
  preserving the matching no-Rust C fallback body for ENABLE_TOFU builds. This
  covers null/non-BCH BG rejection, DMA alignment rejection, MMIO write callback
  ordering, read-spin callback argument shaping, -ETIMEDOUT return shaping,
  missing-callback direct-helper rejection, and object-boundary proof that the
  Rust ENABLE_TOFU Tofu object imports tofu_core_enable_bch_body_result while
  the no-Rust ENABLE_TOFU fallback object has no helper dependency. C remains
  responsible for tof_core_bg_get() storage, actual MMIO write side effects,
  actual readq/rdtsc polling behavior, BCH ioctl copy_from_user(), current
  thread/proc/VM lookup, memory-range locking, straight-map and PTE
  physical-address lookup, Tofu process/VM hooks, generated Tofu layouts, and
  most of kernel/tofu/tof_utofu_main.c. Validation passed for rustfmt, shell
  syntax, scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh
  with direct C-vs-Rust BCH-enable leaf coverage and tofu_uapi digest
  c6f8d265ba34fd78, strict Rust mckernel.img build, no-Rust C fallback
  mckernel.img build, Rust ENABLE_TOFU mckernel.img build, no-Rust C fallback
  ENABLE_TOFU mckernel.img build, symbol checks showing Rust exports and Rust
  ENABLE_TOFU imports tofu_core_enable_bch_body_result, no-Rust ENABLE_TOFU
  imports none of it, broad Rocky Rust build-only validation for mckernel.img,
  ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect,
  sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib,
  and the final build-aware JSON source-retirement audit. The audit passed
  with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows
  [], untracked files [], 21 debt rows, headers_with_executable_logic 0, Other
  in-scope code completion 99.44897959183673%, overall scored
  source-retirement completion 99.43638059701493%, and total percentage points
  left 0.5636194029850685%. /dev/kvm was not present in this command context,
  so no QEMU/KVM guest runtime validation, host reboot, or reboot-capable
  script was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 46.0% to 51.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_utofu_ioctl_enable_bch() precheck, request validation, and post-PTE
  commit/rollback sequencing through Rust while preserving the matching no-Rust
  C fallback body for ENABLE_TOFU builds. This covers BCH ID bounds rejection,
  already-enabled -EBUSY shaping, request pointer/count/address/alignment/BSEQ
  validation, bgmask zeroing, core BCH-enable callback dispatch, enable-error
  logging plus disable rollback, per-BG setup callback iteration, set-BG error
  logging plus disable and reverse-order BG unset rollback, enabled-state
  publication, IOVA publication, missing-callback direct-helper rejection, and
  object-boundary proof that the Rust ENABLE_TOFU Tofu object imports
  tofu_utofu_enable_bch_precheck_body_result,
  tofu_utofu_enable_bch_request_validate_result, and
  tofu_utofu_enable_bch_commit_body_result while the no-Rust ENABLE_TOFU
  fallback object has no helper dependency. C remains responsible for
  copy_from_user(), current-thread/proc/VM lookup, memory-range locking,
  straight-map and PTE physical-address lookup, PTE fault behavior, BCH DMA
  translation policy, Tofu hardware enable/disable primitive side effects,
  tof_utofu_set_bg()/tof_utofu_unset_bg() user-copy and hardware effects,
  raw_rc_output(), Tofu process/VM hooks, generated Tofu layouts, and most of
  kernel/tofu/tof_utofu_main.c. Validation first failed at the Rust ENABLE_TOFU
  build because the new unset bridge preceded the static tof_utofu_unset_bg()
  declaration; that failure is recorded in kernel.log and fixed with an
  explicit static prototype. Final validation passed for rustfmt, shell syntax,
  scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh with
  direct C-vs-Rust BCH-enable coverage and tofu_uapi digest
  4dd8988033559dfe, strict Rust mckernel.img build, no-Rust C fallback
  mckernel.img build, Rust ENABLE_TOFU mckernel.img build, no-Rust C fallback
  ENABLE_TOFU mckernel.img build, symbol checks showing Rust exports and Rust
  ENABLE_TOFU imports all three BCH-enable helpers, no-Rust ENABLE_TOFU imports
  none of them, broad Rocky Rust build-only validation for mckernel.img,
  ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect,
  sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib,
  and the final build-aware JSON source-retirement audit. The audit passed
  with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows
  [], untracked files [], 21 debt rows, headers_with_executable_logic 0, Other
  in-scope code completion 99.42857142857143%, overall scored
  source-retirement completion 99.42891791044777%, and total percentage points
  left 0.5710820895522346%. /dev/kvm was not present in this command context,
  so no QEMU/KVM guest runtime validation, host reboot, or reboot-capable
  script was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 43.0% to 46.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_utofu_unlocked_ioctl() top-level dispatch sequencing through Rust while
  preserving the matching no-Rust C fallback body for ENABLE_TOFU builds. This
  covers fd bounds and fd-data offload return shaping, allocation/free STag
  ioctl callback dispatch, BCH enable/disable ioctl callback dispatch,
  PROFILE_ENABLE timestamp gating, allocation/free profile-event dispatch,
  unknown-command logging callback dispatch, -ENOTSUPP return shaping, missing
  callback rejection, and object-boundary proof that the Rust ENABLE_TOFU Tofu
  object imports tofu_utofu_unlocked_ioctl_body_result while the no-Rust
  ENABLE_TOFU fallback object has no helper dependency. C remains responsible
  for current-thread/proc lookup, fd table storage, ioctl subcommand bodies,
  user-copy, page-table/PTE lookup, BCH DMA translation, profile_event_add()
  side effects, rdtsc primitive behavior, dkprintf formatting, Tofu process/VM
  hooks, generated Tofu layouts, and most of kernel/tofu/tof_utofu_main.c.
  Validation passed for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust ioctl dispatch
  coverage and tofu_uapi digest 9f7989b0a4feebf0, strict Rust mckernel.img
  build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU mckernel.img
  build, no-Rust C fallback ENABLE_TOFU mckernel.img build, symbol checks
  showing Rust exports and Rust ENABLE_TOFU imports
  tofu_utofu_unlocked_ioctl_body_result, no-Rust ENABLE_TOFU imports none of
  the new helper, broad Rocky Rust build-only validation for mckernel.img,
  ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect,
  sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib,
  and the final build-aware JSON source-retirement audit. The audit passed
  with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows
  [], untracked files [], 21 debt rows, headers_with_executable_logic 0, Other
  in-scope code completion 99.37755102040816%, overall scored
  source-retirement completion 99.41026119402986%, and total percentage points
  left 0.5897388059701427%. /dev/kvm was not present in this command context,
  so no QEMU/KVM guest runtime validation, host reboot, or reboot-capable
  script was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 40.0% to 43.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_utofu_release_fd() and tof_utofu_release_fds() dispatch sequencing
  through Rust while preserving matching no-Rust C fallback bodies for
  ENABLE_TOFU builds. This covers Tofu-disabled no-op behavior, null fd-data
  and path no-op behavior, path-kind callback dispatch, CQ and BCH release
  callback selection, release-log callback dispatch, unknown-path no-op
  behavior, missing-callback direct-helper rejection, process fd-loop bounds,
  and object-boundary proof that the Rust ENABLE_TOFU Tofu object imports
  tofu_utofu_release_fd_body_result and tofu_utofu_release_fds_body_result
  while the no-Rust ENABLE_TOFU fallback object has no helper dependency. C
  remains responsible for procfs path storage, path string classification
  bridge code, actual CQ/BCH release primitive side effects, BCH hardware
  disable primitive behavior, fd table ownership, ioctl user-copy, page-table/
  PTE lookup, BCH DMA translation, Tofu process/VM hooks, generated Tofu
  layouts, and most of kernel/tofu/tof_utofu_main.c. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust fd release and
  fd-loop coverage and tofu_uapi digest 9291c06eaf08c8e4, strict Rust
  mckernel.img build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU
  mckernel.img build, no-Rust C fallback ENABLE_TOFU mckernel.img build,
  symbol checks showing Rust exports and Rust ENABLE_TOFU imports
  tofu_utofu_release_fd_body_result and tofu_utofu_release_fds_body_result,
  no-Rust ENABLE_TOFU imports none of them, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, and the final build-aware JSON source-retirement audit.
  The audit passed with compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, Other in-scope code completion
  99.34693877551021%, overall scored source-retirement completion
  99.3990671641791%, and total percentage points left
  0.600932835820899%. /dev/kvm was not present in this command context, so no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 35.0% to 40.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  tof_utofu_disable_bch() sequencing through Rust while preserving the
  matching no-Rust C fallback body for ENABLE_TOFU builds. This covers the
  BCH-enabled gate, -EPERM inactive return shaping, core BCH-disable callback
  dispatch, core-disable error logging callback dispatch, per-TNI BG-mask
  traversal, per-BG unset callback dispatch, unset-error short-circuit return,
  enabled-state clearing, smp_mb callback ordering, invalid direct-helper
  argument rejection, and object-boundary proof that the Rust ENABLE_TOFU Tofu
  object imports tofu_utofu_disable_bch_body_result while the no-Rust
  ENABLE_TOFU fallback object has no helper dependency. C remains responsible
  for the core BCH hardware disable primitive, per-BG unset primitive, actual
  MMIO writeq/register side effects, raw_rc_output and dkprintf formatting,
  smp_mb primitive behavior, ioctl user-copy, page-table/PTE lookup, BCH DMA
  translation, Tofu process/VM hooks, generated Tofu layouts, and most of
  kernel/tofu/tof_utofu_main.c. Validation passed for rustfmt, shell syntax,
  scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh with
  direct C-vs-Rust BCH-disable traversal coverage and tofu_uapi digest
  0c40e31470f8b05d, strict Rust mckernel.img build, no-Rust C fallback
  mckernel.img build, Rust ENABLE_TOFU mckernel.img build, no-Rust C fallback
  ENABLE_TOFU mckernel.img build, symbol checks showing Rust exports and
  Rust ENABLE_TOFU imports tofu_utofu_disable_bch_body_result, no-Rust
  ENABLE_TOFU imports none of the new helpers, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, and the final build-aware JSON source-retirement audit.
  The audit passed with compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, Other in-scope code completion
  99.31632653061224%, overall scored source-retirement completion
  99.38787313432836%, and total percentage points left
  0.612126865671641%. /dev/kvm was not present in this command context, so no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 31.0% to 35.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes Tofu BG
  signal-handler slot publication, locked BCH disable mask write sequencing,
  and BG disable write sequencing through Rust while preserving matching
  no-Rust C fallback bodies for ENABLE_TOFU builds. This covers signal-handler
  lock acquisition, slot publication, unlock callback ordering, unregister
  NULL-handler publication, BCH-presence rejection, BCH mask-value/offset
  dispatch, BG-enable clear dispatch, missing-callback direct-helper rejection,
  and object-boundary proof that the Rust ENABLE_TOFU Tofu object imports the
  new Rust helpers while the no-Rust ENABLE_TOFU fallback object has no helper
  dependency. C remains responsible for the spinlock primitive side effects,
  MMIO writeq primitive side effects, hardware register behavior, timeout
  polling, ioctl user-copy, page-table/PTE lookup, BCH DMA translation, Tofu
  process/VM hooks, generated Tofu layouts, and most of
  kernel/tofu/tof_utofu_main.c. Validation passed for rustfmt, shell syntax,
  scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh with
  direct C-vs-Rust signal/BCH/BG-disable coverage and tofu_uapi digest
  0aaf1a1774cf50d2, strict Rust mckernel.img build, no-Rust C fallback
  mckernel.img build, Rust ENABLE_TOFU mckernel.img build, no-Rust C fallback
  ENABLE_TOFU mckernel.img build, symbol checks showing Rust exports
  tofu_core_register_signal_bg_body_result,
  tofu_core_bch_disable_locked_body_result, and
  tofu_core_bg_disable_body_result, Rust ENABLE_TOFU imports them, no-Rust
  ENABLE_TOFU imports none of them, and broad Rocky Rust build-only validation
  for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib. /dev/kvm was not present in this command context, so no QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 28.0% to 31.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes the
  Tofu BG local/remote link and signal-mask computation inside
  tof_core_set_bg() through Rust while preserving the matching no-Rust C
  fallback body for ENABLE_TOFU builds. This covers source/destination
  local-gate branch selection, source/destination remote-BG branch selection,
  fallback signal-mask bit selection, local-link mask placement, remote BG
  address packing for Tofu coordinates/TNI/BGID, remote-link mask placement,
  null-output direct-helper rejection, and missing-address direct-helper
  rejection. C remains responsible for the surrounding BG lock ordering, IRQ
  mask reset helpers, MMIO writeq/wmb side effects, BG enable write, timeout
  polling, hardware register behavior, ioctl user-copy, page-table/PTE lookup,
  BCH DMA translation, Tofu process/VM hooks, generated Tofu layouts, and most
  of kernel/tofu/tof_utofu_main.c. Validation passed for rustfmt, shell
  syntax, scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh
  with direct C-vs-Rust BG-link computation coverage, strict Rust mckernel.img
  build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU mckernel.img
  build, no-Rust C fallback ENABLE_TOFU mckernel.img build, object-boundary
  checks showing the Rust-enabled Tofu object imports
  tofu_core_bg_links_body_result and tofu_utofu_unset_bg_body_result while the
  fallback Tofu object has no helper dependency, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, and the final build-aware JSON source-retirement audit.
  The audit passed with compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, Other in-scope code completion
  99.22448979591837%, overall scored source-retirement completion
  99.35429104477612%, and total percentage points left
  0.6457089552238813%. /dev/kvm was not present in this command context, so no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 26.0% to 28.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes the
  uTofu BG unset state-transition sequencing through Rust while preserving the
  matching no-Rust C fallback body for ENABLE_TOFU builds. This covers the
  enabled-state gate in __tof_utofu_unset_bg(), callback dispatch to
  tof_core_unset_bg(), enabled-state clearing, signal-handler unregister
  dispatch, inactive no-op behavior, null-state rejection in the direct helper,
  and missing-callback error shaping in the direct helper. C remains
  responsible for real BG/BCH hardware register side effects, lock primitive
  bodies, signal-handler slot locking, ioctl user-copy, page-table/PTE lookup,
  BCH DMA translation, Tofu process/VM hooks, generated Tofu layouts, and most
  of kernel/tofu/tof_utofu_main.c. Validation passed for rustfmt, shell syntax,
  scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh with
  direct C-vs-Rust BG-unset coverage, strict Rust mckernel.img build, no-Rust
  C fallback mckernel.img build, Rust ENABLE_TOFU mckernel.img build, no-Rust
  C fallback ENABLE_TOFU mckernel.img build, object-boundary checks showing
  the Rust-enabled Tofu object imports tofu_utofu_unset_bg_body_result while
  the fallback Tofu object has no helper dependency, broad Rocky Rust
  build-only validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko,
  mcctrl_ko, mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, ihkmond, and ihklib, and the final build-aware JSON
  source-retirement audit. The audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], 21
  debt rows, headers_with_executable_logic 0, Other in-scope code completion
  99.1938775510204%, overall scored source-retirement completion
  99.34309701492538%, and total percentage points left
  0.6569029850746233%. /dev/kvm was not present in this command context, so no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 24.0% to 26.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes the
  compiled Tofu core/uTofu two-dimensional pointer selection helpers through a
  Rust-owned indexed-pointer helper while preserving the matching no-Rust C
  fallback arithmetic for ENABLE_TOFU builds. This covers the bounds and
  pointer-offset bodies used by tof_core_cq_get(), tof_core_bg_get(), and
  tof_utofu_bg_get(), including unsigned negative-input rejection and legacy
  wrapping address arithmetic without adding Rust layout dependencies on the
  generated Tofu structures. C remains responsible for real Tofu hardware
  register side effects, cacheflush/readq/writeq/spin behavior, BG/BCH setup
  and teardown, Tofu process/VM hooks, locking primitive bodies, generated
  Tofu layouts, and most of kernel/tofu/tof_utofu_main.c. Validation passed
  for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust indexed-pointer
  coverage, strict Rust mckernel.img build, no-Rust C fallback mckernel.img
  build, Rust ENABLE_TOFU mckernel.img build, no-Rust C fallback ENABLE_TOFU
  mckernel.img build, object-boundary checks showing the Rust-enabled Tofu
  object imports tofu_indexed_2d_ptr_body_result while the fallback Tofu
  object has no helper dependency, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, and the final build-aware JSON source-retirement audit. The
  audit passed with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale
  tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, Other in-scope code completion
  99.1734693877551%, overall scored source-retirement completion
  99.33563432835821%, and total percentage points left
  0.6643656716417894%. /dev/kvm was not present in this command context, so
  no QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 22.0% to 24.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes the
  live uTofu free-STag steering and memory-buffer enable-bit clearing through
  Rust helpers while preserving the matching no-Rust C fallback assignments
  for ENABLE_TOFU builds. This covers isolated steering-entry enable-bit
  clearing, isolated MB-entry enable-bit clearing, null-argument `-EINVAL`
  shaping, and preservation of surrounding C-owned cacheflush, kref/profile,
  hardware register, and lifecycle side effects in tof_utofu_free_stag().
  The helpers are also wired into the static Tofu IB steering-disable wrapper
  where present, but only the compiled live tof_utofu_free_stag() imports are
  counted in this score movement. C remains responsible for real Tofu hardware
  register side effects, STag allocation/free lifecycle outside these isolated
  leaf mutations, MBPT page allocation and freeing, page-table/PTE lookup
  behavior, memory-chunk DMA translation behavior, CQ/BG ioctl handling, Tofu
  process/VM hooks, locking primitive bodies, cacheflush side effects,
  generated Tofu layouts, and most of kernel/tofu/tof_utofu_main.c.
  Validation passed for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust Tofu steering and
  MB disable coverage, strict Rust mckernel.img build, no-Rust C fallback
  mckernel.img build, Rust ENABLE_TOFU mckernel.img build, no-Rust C fallback
  ENABLE_TOFU mckernel.img build, object-boundary checks showing the
  Rust-enabled Tofu object imports tofu_icc_steering_disable_entry_body_result
  and tofu_icc_mb_disable_entry_body_result while the fallback Tofu object has
  no helper dependency, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, and the final build-aware JSON source-retirement audit. The
  audit passed with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale
  tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, Other in-scope code completion
  99.15306122448979%, overall scored source-retirement completion
  99.32817164179104%, and total percentage points left
  0.6718283582089555%. /dev/kvm was not present in this command context, so
  no QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 20.0% to 22.0% for
  the strict source-retirement audit. Rust-enabled McKernel now owns the
  isolated uTofu STag reservation scan helper while preserving the matching
  no-Rust C fallback body for ENABLE_TOFU builds. This covers steering
  enable-bit query behavior, readonly/non-readonly STag parity selection,
  special-STag start handling, bounded steering-table scan, first-free STag
  return shaping, all-busy `-1` return shaping, null/invalid argument
  `-EINVAL` shaping, and preservation of the existing C wrapper logging
  behavior. The Tofu IB ring-buffer allocation/free helper exports and
  C-vs-Rust fixture coverage were also added, but they are not counted in
  this score movement because the current ENABLE_TOFU object does not import
  those optimized-out static bodies. C remains responsible for real Tofu
  hardware register side effects, STag allocation/free lifecycle outside this
  isolated reservation scan, MBPT page allocation and freeing, page-table/PTE
  lookup behavior, memory-chunk DMA translation behavior, CQ/BG ioctl
  handling, Tofu process/VM hooks, locking primitive bodies, cacheflush side
  effects, generated Tofu layouts, and most of
  kernel/tofu/tof_utofu_main.c. Validation passed for rustfmt, shell syntax,
  scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh with
  direct C-vs-Rust Tofu reservation and ring-buffer coverage, strict Rust
  mckernel.img build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU
  mckernel.img build, no-Rust C fallback ENABLE_TOFU mckernel.img build,
  object-boundary checks showing the Rust-enabled Tofu object imports
  tofu_utofu_reserve_stag_body_result while the fallback Tofu object has no
  helper dependency, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, and the final JSON source-retirement audit. The audit passed
  with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker
  rows [], untracked files [], 21 debt rows, headers_with_executable_logic 0,
  Other in-scope code completion 99.13265306122449%, overall scored
  source-retirement completion 99.32070895522388%, and total percentage
  points left 0.6792910447761216%. The earlier focused formatting failure for
  this slice is recorded in kernel.log and corrected. /dev/kvm was not
  present in this command context, so no QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 17.0% to 20.0% for
  the strict source-retirement audit. Rust-enabled McKernel now owns the
  isolated uTofu translation MRU/table search body while preserving the
  matching no-Rust C fallback body for ENABLE_TOFU builds. This covers empty
  MRU-list `-ENOENT` shaping, circular MRU traversal, special-STag and
  readonly parity gates, page-size matching, packed steering start/length
  extraction, exact-address matching, containment matching when MBPT address
  matching is disabled, corrupted-index error shaping, and preservation of the
  existing C wrapper logging behavior. C remains responsible for real Tofu
  hardware register side effects, STag allocation/free lifecycle, MBPT page
  allocation and freeing, page-table/PTE lookup behavior, memory-chunk DMA
  translation behavior, CQ/BG ioctl handling, Tofu process/VM hooks, locking
  primitive bodies, cacheflush side effects, generated Tofu layouts, and most
  of kernel/tofu/tof_utofu_main.c. Validation passed for rustfmt, shell
  syntax, scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh
  with direct C-vs-Rust Tofu search coverage, strict Rust mckernel.img build,
  no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU mckernel.img build,
  no-Rust C fallback ENABLE_TOFU mckernel.img build, object-boundary checks
  showing the Rust-enabled Tofu object imports tofu_utofu_trans_search_body_result
  while the fallback Tofu object has no helper dependency, broad Rocky Rust
  build-only validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko,
  mcctrl_ko, mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, ihkmond, and ihklib, and the final JSON
  source-retirement audit. The audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  21 debt rows, headers_with_executable_logic 0, Other in-scope code
  completion 99.11224489795919%, overall scored source-retirement completion
  99.31324626865671%, and total percentage points left
  0.6867537313432877%. /dev/kvm was not present in this command context, so
  no QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 11.0% to 17.0% for
  the strict source-retirement audit. Rust-enabled McKernel now owns the
  isolated uTofu translation table getter helpers plus Tofu steering, memory
  buffer, and MBPT leaf-entry mutation/query bodies while preserving matching
  no-Rust C fallback bodies for ENABLE_TOFU builds. This covers decoded
  steering and MBPT start/length extraction, steering entry readonly/length/
  MBVA/MBID publication with write-barrier-before-enable ordering, memory
  buffer page-size/IPA/npage publication with write-barrier-before-enable
  ordering, MBPT disable IPA return shaping, MBPT enable publication, MBPT
  enabled-state query behavior, null-argument and missing-callback error
  shaping, and exact C-model equivalence for the packed entry words. C remains
  responsible for real Tofu hardware register side effects, STag allocation/
  free lifecycle, MBPT page allocation and freeing, page-table/PTE lookup
  behavior, memory-chunk DMA translation behavior, CQ/BG ioctl handling, Tofu
  process/VM hooks, locking primitive bodies, cacheflush side effects,
  generated Tofu layouts, and most of kernel/tofu/tof_utofu_main.c. Validation
  passed for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust Tofu leaf-entry
  coverage, strict Rust mckernel.img build, no-Rust C fallback mckernel.img
  build, Rust ENABLE_TOFU mckernel.img build, no-Rust C fallback ENABLE_TOFU
  mckernel.img build, object-boundary checks showing the Rust-enabled Tofu
  object imports the new Rust helper bodies while the fallback Tofu object has
  no helper dependency, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, and the final JSON source-retirement audit. The audit passed
  with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows
  [], untracked files [], 21 debt rows, headers_with_executable_logic 0,
  Other in-scope code completion 99.08163265306122%, overall scored
  source-retirement completion 99.30205223880597%, and total percentage
  points left 0.6979477611940297%. The earlier focused formatting and
  equivalence failures for this slice are recorded in kernel.log and corrected.
  /dev/kvm was not present in this command context, so no QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 7.0% to 11.0% for
  the strict source-retirement audit. Rust-enabled McKernel now owns the
  isolated uTofu translation-entry packing plus translation update/enable body
  sequencing while preserving matching no-Rust C fallback bodies for
  ENABLE_TOFU builds. This covers translation bitfield packing for start,
  length, and page-size code selection; steering table-slot publication through
  the atomic callback boundary; update-side MRU delete/insert ordering under
  the lock callback; enable-side MBPT table-slot publication, write-barrier
  callback ordering, and handoff into the Rust update helper; invalid argument
  and missing callback error shaping; and x86 ENABLE_TOFU compile/link
  compatibility for the wrapper path through fail-closed weak platform
  fallbacks. C remains responsible for real Tofu hardware register side effects,
  STag allocation/free lifecycle, MBPT page allocation and freeing,
  page-table/PTE lookup behavior, memory-chunk DMA translation behavior,
  CQ/BG ioctl handling, Tofu process/VM hooks, locking primitive bodies,
  cacheflush side effects, generated Tofu layouts, and most of
  kernel/tofu/tof_utofu_main.c. Validation passed for rustfmt, shell syntax,
  scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh with
  direct C-vs-Rust Tofu translation update/enable coverage, strict Rust
  mckernel.img build, no-Rust C fallback mckernel.img build, Rust ENABLE_TOFU
  mckernel.img build, no-Rust C fallback ENABLE_TOFU mckernel.img build,
  object-boundary checks showing the Rust-enabled Tofu object imports the new
  Rust helper bodies while the fallback Tofu object has no helper dependency,
  broad Rocky Rust build-only validation for mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect, sched_yield,
  ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib, and the final
  JSON source-retirement audit. The audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], 21
  debt rows, headers_with_executable_logic 0, Other in-scope code completion
  99.0204081632653%, overall scored source-retirement completion
  99.27966417910449%, and total percentage points left
  0.7203358208955137%. /dev/kvm was not present in this command context, so no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 5.0% to 7.0% for
  the strict source-retirement audit. Rust-enabled McKernel now owns the
  isolated uTofu translation-disable helper sequencing while preserving the
  no-Rust C fallback body for ENABLE_TOFU builds. This covers exact translation
  table-slot zeroing through a callback boundary, STag validation/error
  shaping, MRU removal dispatch, and preservation of table/head state on
  missing-callback error paths. C remains responsible for actual ENABLE_TOFU
  build integration in this default Rocky path, hardware register side effects,
  STag allocation/free lifecycle, MBPT page allocation and freeing,
  page-table/PTE lookup, memory-chunk DMA lookup, CQ/BG ioctl handling, Tofu
  process/VM hooks, locking primitives, cacheflush side effects, generated Tofu
  layouts, and most of kernel/tofu/tof_utofu_main.c. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust Tofu
  translation-disable coverage, strict Rust mckernel.img build, no-Rust
  fallback mckernel.img build, Rust-object export checks for the six Tofu
  helper symbols, broad Rocky Rust build-only validation for mckernel.img,
  ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect,
  sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib,
  and the final JSON source-retirement audit. The audit passed with
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], 21 debt rows, headers_with_executable_logic 0, Other
  in-scope code completion 98.9795918367347%, overall scored
  source-retirement completion 99.26473880597015%, and total percentage points
  left 0.7352611940298459%. /dev/kvm was not present in this command context,
  so no QEMU/KVM guest runtime validation, host reboot, or reboot-capable
  script was run.
- 2026-06-14: kernel/tofu/tof_utofu_main.c moved from 0.0% to 5.0% for
  the strict source-retirement audit. Rust-enabled McKernel now owns an
  isolated Tofu implementation helper cluster while preserving no-Rust C
  fallback bodies for ENABLE_TOFU builds. This covers MBPT-start publication,
  Tofu IB DMA-address pack/unpack shaping, and uTofu translation MRU
  insert/delete sequencing including empty-list, head-removal, single-entry,
  already-deleted, and null-argument error behavior. C remains responsible for
  actual ENABLE_TOFU build integration in this default Rocky path, hardware
  register side effects, STag allocation/free lifecycle, MBPT page allocation
  and freeing, page-table/PTE lookup, memory-chunk DMA lookup, CQ/BG ioctl
  handling, Tofu process/VM hooks, locking primitives, cacheflush side effects,
  generated Tofu layouts, and most of kernel/tofu/tof_utofu_main.c. Validation
  passed for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct C-vs-Rust Tofu helper
  coverage, strict Rust mckernel.img build, no-Rust fallback mckernel.img
  build, Rust-object export checks for the five new Tofu helper symbols,
  explicit checks that the default Rocky strict/fallback builds still omit
  tof_utofu_main.c because ENABLE_TOFU=OFF, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, and the final JSON source-retirement audit. The audit
  passed with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale
  tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, Other in-scope code completion
  98.95918367346938%, overall scored source-retirement completion
  99.25727611940299%, and total percentage points left
  0.742723880597012%. /dev/kvm was not present in this command context, so
  no QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.
- 2026-06-14: arch/x86_64/kernel/cpu.c moved from 92.0% to 93.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes KVM
  detection and pvclock availability/MSR-selection sequencing through Rust
  while preserving no-Rust C fallback bodies. This covers KVM CPUID signature
  comparison, pvclock signature/feature-leaf validation, CLOCKSOURCE2 versus
  legacy CLOCKSOURCE feature selection, pvti_msr publication, not-KVM and
  unsupported return shaping, and pvclock diagnostic event selection. C remains
  responsible for the cpuid primitive, dkprintf() logging side effects, WRMSR
  programming in arch_start_pvclock(), low-level interrupt/CPU primitives,
  AP startup, and the remaining arch/x86_64/kernel/cpu.c execution bodies.
  Validation passed for rustfmt, shell syntax, scoped whitespace checks
  including the Rust helper file, full kernel/rust/tests/run_equivalence.sh
  with direct KVM/pvclock true/false/new/old/unsupported coverage, strict
  Rust mckernel.img build after correcting the pvti_msr pointer-width ABI
  warning, no-Rust fallback mckernel.img build, symbol checks showing strict
  cpu.c.o imports x86_running_on_kvm_body_result and
  x86_pvclock_available_body_result while kernel/rust/mckernel_rust.o exports
  both and fallback cpu.c.o has no helper dependency, broad Rocky Rust
  build-only validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko,
  mcctrl_ko, mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, ihkmond, and ihklib, and the final JSON
  source-retirement audit. The audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  21 debt rows, headers_with_executable_logic 0, Other in-scope code
  completion 98.90816326530613%, overall scored source-retirement completion
  99.23861940298508%, and total percentage points left
  0.7613805970149201%. /dev/kvm was not present in this command context, so
  no QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.
- 2026-06-14: arch/x86_64/kernel/cpu.c moved from 91.0% to 92.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes a
  CPU lifecycle/no-op helper batch through Rust while preserving no-Rust C
  fallback bodies. This covers ihk_mc_set_page_fault_handler() handler-slot
  publication, ihk_mc_delay_us() delay callback dispatch, arch_clone_thread()
  and arch_flush_icache_all() no-op body ownership, and init_tick(),
  init_delay(), and sync_tick() log-event selection. C remains responsible
  for arch_delay() timing side effects, dkprintf() logging side effects,
  actual page-fault handler execution, low-level interrupt/CPU primitives,
  AP startup, and the remaining arch/x86_64/kernel/cpu.c execution bodies.
  Validation passed for rustfmt, shell syntax, scoped whitespace checks
  including the new Rust helper file, full kernel/rust/tests/run_equivalence.sh
  after correcting a fixture-only process-address digest mismatch, strict
  Rust mckernel.img build, no-Rust fallback mckernel.img build, symbol checks
  showing strict cpu.c.o imports x86_page_fault_handler_publish_result,
  x86_arch_noop_body_result, x86_delay_us_body_result, and
  x86_tick_log_body_result while kernel/rust/mckernel_rust.o exports all four
  and fallback cpu.c.o has no helper dependency, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, and the final JSON source-retirement audit. The audit
  passed with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale
  tracker rows [], untracked files [], 21 debt rows, headers_with_executable_logic
  0, Other in-scope code completion 98.89795918367346%, overall scored
  source-retirement completion 99.23488805970149%, and total percentage points
  left 0.7651119402985103%. The earlier focused equivalence failure for this
  slice is recorded in kernel.log and corrected. /dev/kvm was not present in
  this command context, so no QEMU/KVM guest runtime validation, host reboot,
  or reboot-capable script was run.
- 2026-06-14: arch/x86_64/kernel/cpu.c moved from 90.0% to 91.0% for
  the strict source-retirement audit. Rust-enabled McKernel now routes
  arch_cpu_read_write_register() operation dispatch through
  x86_arch_cpu_read_write_register_body_result() while preserving the
  no-Rust C fallback body. This covers read-register request routing, MSR
  address extraction, value publication to struct ihk_os_cpu_register,
  write-register request routing, MSR value extraction, invalid-operation
  -1 shaping, and null/missing-callback -EINVAL shaping. C remains
  responsible for the rdmsr()/wrmsr() primitive side effects, low-level CPU
  setup, APIC/pstate/PAT/syscall/perf setup, interrupt primitives, and the
  remaining arch/x86_64/kernel/cpu.c execution bodies. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks including the new Rust
  helper file, full kernel/rust/tests/run_equivalence.sh with direct read,
  write, and invalid-op coverage, strict Rust mckernel.img build, no-Rust
  fallback mckernel.img build, symbol checks showing strict cpu.c.o imports
  x86_arch_cpu_read_write_register_body_result while kernel/rust/mckernel_rust.o
  exports it and fallback cpu.c.o has no helper dependency, broad Rocky Rust
  build-only validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko,
  mcctrl_ko, mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, ihkmond, and ihklib, and the final JSON
  source-retirement audit. The audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  21 debt rows, headers_with_executable_logic 0, Other in-scope code
  completion 98.88775510204081%, overall scored source-retirement completion
  99.23115671641791%, and total percentage points left
  0.7688432835820862%. The earlier focused formatting failure for this slice
  is recorded in kernel.log and corrected. /dev/kvm was not present in this
  command context, so no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-14: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 84.0% to 88.0% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes collect_cpu_topology() and collect_node_topology()
  body sequencing through Rust helpers while preserving the no-Rust C fallback
  bodies. The CPU helper covers sysfs prefix allocation/formatting, CPU
  topology allocation, cache-list initialization callback ordering, CPU number
  and APIC hardware-ID publication, core/package/thread-sibling read ordering,
  cache-topology collection loop dispatch, ignored cache-loop failure
  behavior, final CPU topology list insertion, and pre-insertion cleanup. The
  node helper covers node topology allocation, node-number publication,
  formatted node cpumap read dispatch, final node topology list insertion, and
  read/allocation cleanup. C remains responsible for Linux allocation/free
  primitives, varargs sysfs readers, per-CPU APIC lookup, list primitive
  mutation, cache-topology callback bodies, boot/APIC/trampoline side effects,
  and the remaining smp-arch-driver.c execution bodies. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct CPU success, CPU read
  failure cleanup, ignored cache-loop failure, node success, and node read
  failure coverage, strict Rust ihk-smp-x86_64_ko build, no-Rust fallback
  ihk-smp-x86_64_ko build, symbol checks showing strict C imports
  ihk_smp_collect_cache_topology_body_result,
  ihk_smp_collect_cpu_topology_body_result, and
  ihk_smp_collect_node_topology_body_result while Rust exports all three and
  fallback C has no helper dependency, the real-build SMP Rust-object
  unresolved-import check, and broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib. The final JSON source-retirement audit passed with
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], 21 debt rows, headers_with_executable_logic 0,
  host/IKC/mcctrl/IHK modules completion 99.1375%, overall scored
  source-retirement completion 99.22742537313434%, and total percentage points
  left 0.7725746268656621%. /dev/kvm was not present in this command context,
  so no QEMU/KVM guest runtime validation, host reboot, or reboot-capable
  script was run.
- 2026-06-14: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 82.0% to 84.0% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes collect_cache_topology() body sequencing through
  Rust: sysfs prefix allocation/formatting, cache-entry existence probing,
  cache-topology allocation, level/type/size/string/coherency/set/partition/
  associativity/shared-CPU-map read ordering, size normalization, list
  insertion, partial-failure cleanup, and allocation/read diagnostic event
  selection. The no-Rust fallback build keeps the original C body. Validation
  passed for focused rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct cache-topology success,
  missing-entry, read-failure cleanup, and allocation-failure coverage, strict
  Rust ihk-smp-x86_64_ko build, no-Rust fallback ihk-smp-x86_64_ko build,
  symbol checks showing strict C imports
  ihk_smp_collect_cache_topology_body_result while Rust exports it and fallback
  C has no helper dependency, the real-build SMP Rust-object unresolved-import
  check, broad Rocky Rust build-only validation for mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect, sched_yield,
  ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib, and the final
  JSON source-retirement audit. The audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], 21
  debt rows, headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules
  completion 99.0875%, overall scored source-retirement completion 99.2125%,
  and total percentage points left 0.7874999999999943%. The earlier focused
  formatting failure for this slice is recorded in kernel.log and corrected.
  /dev/kvm was not present in this command context, so no QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-14: kernel/syscall.c moved from 96.0% to 97.0% for the
  strict source-retirement audit. Rust-enabled McKernel now routes getcred()
  buffer selection/request shaping and do_setresuid()/do_setresgid()
  credential-refresh field publication sequencing through Rust helpers while
  preserving the no-Rust C fallback bodies and leaving C responsible for
  virt_to_phys(), CPU selection, do_syscall(), current-thread/process lookup,
  public syscall wrappers, and Linux syscall forwarding side effects. This
  covers page-boundary scratch-buffer selection, syscall_request zeroing,
  setfsuid request number/physical-address/flag publication, processor-id
  callback ordering, credential vector result selection, process credential
  field writes for ruid/euid/suid/fsuid and rgid/egid/sgid/fsgid, null
  thread/scratch error shaping, missing callback rejection, and null getcred
  result handling. Validation passed for rustfmt --edition 2024 --check
  kernel/rust/syscall_policy.rs, shell syntax for
  kernel/rust/tests/run_equivalence.sh, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct getcred and credential-field
  refresh coverage, strict Rust mckernel.img build, no-Rust C fallback
  mckernel.img build, symbol checks showing strict syscall.c.o imports
  getcred_body_result and syscall_refresh_cred_fields_body_result while
  kernel/rust/mckernel_rust.o exports them and fallback syscall.c.o has no
  helper dependency, broad Rocky Rust build-only validation for mckernel.img,
  ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect,
  sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib,
  and the final JSON source-retirement audit. The audit passed with
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], 21 debt rows, headers_with_executable_logic 0, Syscall
  core completion 97.33333333333333%, overall scored source-retirement
  completion 99.20503731343284%, and total percentage points left
  0.7949626865671604%. /dev/kvm was not present in this command context, so no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-14: kernel/syscall.c moved from 95.0% to 96.0% for the
  strict source-retirement audit. Rust-enabled McKernel now routes
  do_sigaction() validation, sigcommon action-slot selection, old-action
  copyout, new-action publication, writer-lock callback ordering, and
  rt_sigaction Linux-forward update trigger sequencing through
  do_sigaction_body_result(), while preserving the no-Rust C fallback helper
  body and leaving C responsible for the lock primitive, current-thread lookup,
  syscall forwarding primitive, broader signal delivery/wait behavior, futex
  paths, syscall dispatch, and the remaining kernel/syscall.c execution bodies.
  Validation passed for rustfmt --edition 2024 --check
  kernel/rust/syscall_policy.rs, shell syntax for
  kernel/rust/tests/run_equivalence.sh, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct do_sigaction_body_result()
  action update/readback/null-action/invalid-signal/missing-callback coverage,
  strict Rust mckernel.img build, no-Rust C fallback mckernel.img build, symbol
  checks showing strict syscall.c.o imports do_sigaction_body_result while
  kernel/rust/mckernel_rust.o exports it and fallback syscall.c.o has no helper
  dependency, broad Rocky Rust build-only validation for mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect, sched_yield,
  ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib, and the final
  JSON source-retirement audit. The audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], 21
  debt rows, headers_with_executable_logic 0, Syscall core completion 97.0%,
  overall scored source-retirement completion 99.20130597014925%, and total
  percentage points left 0.7986940298507506%. The earlier focused equivalence
  segfault for this slice is recorded in kernel.log and corrected by adding the
  matching C-model fallback helper. /dev/kvm was not present in this command
  context, so no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-14: executer/kernel/mcctrl/syscall.c moved from 90.0% to
  95.0% for the strict source-retirement audit. Rust-enabled mcctrl now routes
  __do_in_kernel_syscall() and __do_in_kernel_irq_syscall() top-level
  in-kernel syscall dispatch through Rust helpers while preserving the no-Rust
  C fallback switches and leaving C responsible for per-thread/per-process
  data lifecycle, backward syscall offload/waitqueue sequencing, pager
  callbacks, PTE clear/remap/zero/writecore primitive bodies, scheduler
  credential checks, optional Tofu cleanup, packet release, __return_syscall(),
  and the remaining syscall.c execution bodies. This covers syscall-number
  classification, mmap/munmap/mprotect/move_pages/coredump/sched_setparam
  argument routing, close and exit_group return/release behavior, IRQ mmap
  return behavior, default -ENOSYS shaping, unknown scheduler mode -1 shaping,
  null packet/ops -EINVAL shaping, and missing-callback -EINVAL shaping.
  Validation passed for focused rustfmt, shell syntax, scoped whitespace
  checks, full kernel/rust/tests/run_equivalence.sh with direct dispatcher
  coverage and Rust export checks, Rust-enabled mcctrl_ko build, no-Rust
  mcctrl_ko fallback build, symbol checks showing strict syscall.o imports
  mcctrl_in_kernel_irq_syscall_body_result and
  mcctrl_in_kernel_syscall_body_result while Rust exports them and fallback C
  has no helper dependency, and broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib. The final JSON source-retirement audit passed with
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], 21 debt rows, headers_with_executable_logic 0,
  host/IKC/mcctrl/IHK modules completion 99.0625%, overall scored
  source-retirement completion 99.19757462686567%, and total percentage points
  left 0.8024253731343265%. The earlier focused formatting failure and strict
  Rust mcctrl_ko missing-prototype/unused-variable failure for this slice are
  recorded in kernel.log and corrected. /dev/kvm was not present in this
  command context, so no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-14: executer/kernel/mcctrl/control.c moved from 85.0% to
  90.0% for the strict source-retirement audit. Rust-enabled mcctrl now routes
  the permission-cleared top-level __mcctrl_control() request dispatch through
  a Rust helper while preserving the no-Rust C fallback switch and leaving C
  responsible for the individual request handler bodies, Linux current/euid
  primitives, task PID lookup, user-copy, locks, allocations, perf/mcexec
  side effects, callback wrappers, and the remaining control.c execution
  bodies. This covers the MCEXEC_UP_* request mapping, IHK_OS_AUX_PERF_* and
  IHK_OS_GETRUSAGE dispatch, unknown-request -EINVAL shaping, missing-callback
  -EINVAL shaping, and strict/fallback symbol-boundary separation. Validation
  passed for focused rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct dispatch coverage for every
  request ID plus unknown/null-callback cases, Rust-enabled mcctrl_ko build,
  no-Rust mcctrl_ko fallback build, forced regeneration of the stale strict
  control.o build artifact, symbol checks showing strict control.o imports
  mcctrl_control_dispatch_body_result while Rust exports it and fallback C has
  no helper dependency, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, and the final JSON source-retirement audit. The audit passed
  with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows
  [], untracked files [], 21 debt rows, headers_with_executable_logic 0,
  host/IKC/mcctrl/IHK modules completion 99.0%, overall scored
  source-retirement completion 99.17891791044777%, and total percentage points
  left 0.8210820895522346%. The earlier focused formatting failure and strict
  Rust mcctrl_ko wrapper-argument failure for this slice are recorded in
  kernel.log and corrected. /dev/kvm was not present in this command context,
  so no QEMU/KVM guest runtime validation, host reboot, or reboot-capable
  script was run.
- 2026-06-14: ihk/linux/user/ihklib.c moved from 98.0% to 99.0% for
  the strict source-retirement audit. Rust-enabled ihklib now routes the
  create-OS environment sequencing under ihk_create_os_str() through Rust
  helpers while preserving the no-Rust C fallback loops and leaving C
  responsible for public ABI entry, env parsing allocation/free, reserve/load/
  boot ioctl primitives, resource rollback side effects, error-message buffer
  formatting, and logging. This covers reserve-memory configuration traversal
  and parse/error shaping, IHK_CPUS and IHK_MEM reserve callback dispatch in
  environment order, IHK_IKC_MAP dispatch, IHK_KARGS last-value publication,
  callback failure return preservation, default-kargs publication, and
  null/empty input validation for the new helper boundaries. Validation passed
  for focused rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct ihklib create-OS
  environment callback/order coverage, Rust-enabled ihklib build, no-Rust
  ihklib fallback build, symbol checks showing strict ihklib.c imports
  ihklib_create_os_apply_mem_conf_body_result,
  ihklib_create_os_reserve_resources_body_result, and
  ihklib_create_os_apply_ikc_kargs_body_result while Rust exports them and
  fallback C has no helper dependency, broad Rocky Rust build-only validation
  for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, and the final JSON source-retirement audit. The audit passed
  with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows
  [], untracked files [], 21 debt rows, headers_with_executable_logic 0,
  host/IKC/mcctrl/IHK modules completion 98.9375%, overall scored
  source-retirement completion 99.16026119402986%, and total percentage points
  left 0.8397388059701427%. The earlier focused formatting failure and the
  expected final compiled-C enforcement probe failure are recorded in
  kernel.log. /dev/kvm was not present in this command context, so no QEMU/KVM
  guest runtime validation, host reboot, or reboot-capable script was run.
- 2026-06-14: tools/crash/mckernel.c moved from 96.0% to 97.0% for
  the strict source-retirement audit. The crash extension's x86 arch_init()
  symbol-publication sequence now routes through Rust: Rust requests
  linux_page_offset_base and x86_kernel_phys_base through a retained crash/gdb
  symbol-lookup callback and publishes LINUX_PAGE_OFFSET and
  x86_kernel_phys_base output slots in order. The no-Rust fallback keeps the
  original direct get_symbol_value() assignments. This leaves the crash
  extension C file responsible for crash command registration, gdb/crash
  callback APIs, symbol lookup primitives, memory read primitives, tree/page
  table traversal callbacks, output streams, and remaining command
  orchestration. Validation passed for focused rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh with direct
  mckernel_crash_helpers arch-init callback/name/order coverage, Rust-enabled
  tools/crash/mckernel.mk plugin build, no-Rust tools/crash/mckernel.mk
  fallback plugin build, symbol checks showing the Rust-enabled plugin exports
  mck_crash_x86_arch_init_body_result, the 16-object no-panic/runtime import
  gate including tools/crash/mckernel_crash_helpers.o, broad Rocky Rust
  build-only validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko,
  mcctrl_ko, mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, ihkmond, and ihklib, and the final JSON
  source-retirement audit. The source-retirement audit passed with
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], 21 debt rows, headers_with_executable_logic 0, User tools
  completion 99.8258620689655%, overall scored source-retirement completion
  99.15652985074627%, and total percentage points left 0.8434701492537329%.
  The earlier focused formatting failure for this slice is recorded in
  kernel.log and corrected. /dev/kvm was not present in this command context,
  so no QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.
- 2026-06-14: executer/kernel/mcctrl/futex.c moved from 90.0% to
  92.0% for the strict source-retirement audit. Rust-enabled mcctrl now routes
  RVA-to-RPA cache search, insert, and process-removal traversal sequencing
  through Rust helpers while preserving the no-Rust C fallback bodies and
  leaving C responsible for Linux rbtree primitive callbacks, allocation/free
  boundaries, futex wait/wake/requeue core, remote page fault handling,
  wakeup/IPI side effects, futex hash/wait queues, atomic user access
  primitives, and diagnostics. This covers rbtree search ordering, duplicate
  insertion rejection, link-node selection, insert-color callback dispatch,
  first-node iteration, erase/free callback ordering, and empty-tree return
  behavior. Validation passed for focused rustfmt, shell syntax, scoped
  whitespace checks, strict Rust mcctrl_ko build, no-Rust fallback mcctrl_ko
  build, symbol checks showing strict futex.o imports
  mcctrl_rva_to_rpa_cache_search_body_result,
  mcctrl_rva_to_rpa_cache_insert_body_result, and
  mcctrl_futex_remove_process_body_result while Rust exports them and fallback
  C has no helper dependency, full kernel/rust/tests/run_equivalence.sh with
  direct cache search/insert/remove coverage, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec,
  eclair, mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, the refined emitted-Rust-object import gate over 15
  objects with only the existing mcctrl C panic callback allowed, and the final
  JSON source-retirement audit. The source-retirement audit passed with
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], 21 debt rows, headers_with_executable_logic 0,
  host/IKC/mcctrl/IHK modules completion 98.925%, overall scored
  source-retirement completion 99.15279850746269%, and total percentage points
  left 0.8472014925373088%. Earlier no-panic import-gate failures for this
  slice are recorded in kernel.log and corrected by distinguishing Rust
  panic/runtime imports from the existing C kernel panic callback. /dev/kvm was
  not present in this command context, so no QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.
- 2026-06-14: executer/kernel/mcctrl/procfs.c moved from 96.0% to
  98.0% for the strict source-retirement audit. Rust-enabled mcctrl now routes
  the generic __mckernel_procfs_read_write() body sequencing through a Rust
  helper while preserving the no-Rust C fallback body and leaving C responsible
  for Linux procfs callback entry, file/inode data access, procfs entry field
  access, page allocation/free primitives, virt-to-phys translation, per-process
  data lookup/release primitives, IKC request-send side effects, copy_to_user,
  allocation/free boundaries, and diagnostics. This covers count and negative
  offset early returns, path acquisition, mcos OS-number parsing and entry
  OS-number validation, optional PID parsing, OS/usrdata/per-process lookup,
  page-order fallback allocation, kernel-buffer physical address publication,
  procfs_read allocation, request initialization, send-result shaping including
  timeout and -ERESTARTSYS to -ERESTART conversion, do_free ownership handling,
  McKernel read-return propagation, read-copy offset/count accounting, EOF
  handling, final file-position publication, and cleanup ordering on all paths.
  Validation passed for focused rustfmt, shell syntax, scoped whitespace checks,
  full kernel/rust/tests/run_equivalence.sh with direct generic read/write
  success and error coverage, strict Rust mcctrl_ko build, no-Rust fallback
  mcctrl_ko build, symbol checks showing strict procfs.o imports
  mcctrl_procfs_read_write_body_result while Rust exports it and fallback C has
  no helper dependency, broad Rocky Rust build-only validation for mckernel.img,
  ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect,
  sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib,
  the no-panic Rust-object import gate, and the final JSON source-retirement
  audit. The source-retirement audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], 21
  debt rows, headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules
  completion 98.9%, overall scored source-retirement completion
  99.14533582089553%, and total percentage points left
  0.8546641791044749%. A tentative 100.0% row update was rejected by the strict
  retired-C audit because executer/kernel/mcctrl/procfs.c still compiles in the
  strict build graph; that failure is recorded in kernel.log and corrected here
  by scoring only the verified partial movement. /dev/kvm was not present in
  this command context, so no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-14: executer/kernel/mcctrl/procfs.c moved from 92.0% to
  94.0% for the strict source-retirement audit. Rust-enabled mcctrl now routes
  mckernel_procfs_lseek(), mckernel_procfs_buff_open(), and
  mckernel_procfs_buff_release() sequencing through Rust helpers while keeping
  C responsible for getname/kfree and file-private-data field access, procfs
  entry and OS lookup primitives, buffer allocation/free boundaries, path
  acquisition, IKC release-send side effects, mapped-buffer read/write/user-copy
  paths, and the no-Rust fallback bodies. This covers set/relative lseek
  offset selection, invalid-origin rejection, OS lookup error shaping, path
  allocation and PID parsing, buffer-info allocation and initialization,
  file-private publication, release cleanup when no top buffer exists, release
  read allocation and message initialization, release send result handling,
  timeout logging, -ERESTARTSYS to -ERESTART shaping, McKernel-side read return
  propagation, and do_free false ownership preservation. Validation passed for
  focused rustfmt, shell syntax, scoped whitespace checks, strict Rust
  mcctrl_ko build, no-Rust fallback mcctrl_ko build, symbol checks showing
  strict procfs.o imports mcctrl_procfs_lseek_body_result,
  mcctrl_procfs_buff_open_body_result, and
  mcctrl_procfs_buff_release_body_result while Rust exports them and fallback C
  has no helper dependency, full kernel/rust/tests/run_equivalence.sh with
  direct lseek, open allocation/error, release success/error/timeout/interrupted
  coverage, broad Rocky Rust build-only validation for mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect, sched_yield,
  ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib, the no-panic
  Rust-object import gate, and the final JSON source-retirement audit. The
  source-retirement audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [], 21
  debt rows, headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules
  completion 98.85%, overall scored source-retirement completion
  99.1304104477612%, and total percentage points left 0.8695895522388071%.
  Earlier focused formatting and unexpected strlen-import failures for this
  slice are recorded in kernel.log and corrected. /dev/kvm was not present in
  this command context, so no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-14: executer/kernel/mcctrl/procfs.c moved from 90.0% to
  92.0% for the strict source-retirement audit. Rust-enabled mcctrl now routes
  _add_tid_entry() and proc_exe_link() executable-link sequencing through Rust
  helpers while keeping C responsible for credential field access, procfs entry
  structure traversal, symlink/procfs mutation, string duplication/free
  boundaries, and the no-Rust fallback bodies. This covers create-if-missing
  TID entry dispatch, TID entry population callback ordering, exe-data lookup,
  TID exe symlink creation, procfs semaphore lock/unlock ordering, PID lookup,
  PID exe symlink creation, path publication, and per-task exe-link population.
  Validation passed for focused rustfmt, shell syntax, scoped whitespace
  checks, strict Rust mcctrl_ko build, no-Rust fallback mcctrl_ko build, symbol
  checks showing strict procfs.o imports
  mcctrl_procfs_add_tid_with_cred_body_result and
  mcctrl_procfs_exe_link_body_result while Rust exports them and fallback C has
  no helper dependency, full kernel/rust/tests/run_equivalence.sh with direct
  missing-TID, missing-exe, exe-link success/failure, missing-PID, and
  null-callback coverage, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, the no-panic Rust-object import gate, and the final JSON
  source-retirement audit. The source-retirement audit passed with
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], 21 debt rows, headers_with_executable_logic 0,
  host/IKC/mcctrl/IHK modules completion 98.825%, overall scored
  source-retirement completion 99.12294776119403%, and total percentage
  points left 0.8770522388059732%. /dev/kvm was not present in this command
  context, so no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-14: executer/kernel/mcctrl/procfs.c moved from 86.0% to
  90.0% for the strict source-retirement audit. Rust-enabled mcctrl now routes
  add_tid_entry(), add_pid_entry(), delete_tid_entry(), delete_pid_entry(),
  procfs_init(), and procfs_exit() lifecycle sequencing through Rust helpers
  while keeping C responsible for credential field access, procfs semaphore
  primitives, list/procfs mutation, entry deletion, and the no-Rust fallback
  bodies. This covers credential-missing early returns, lock/unlock ordering,
  existing-PID and create-PID branches, pid_entry_stuff/base_entry_stuff
  callback dispatch, _add_tid_entry() callback ordering, delete-if-found
  behavior, procfs init base-entry setup, and procfs exit base deletion.
  Validation passed for focused rustfmt, shell syntax, scoped whitespace
  checks, strict Rust mcctrl_ko build, no-Rust fallback mcctrl_ko build, symbol
  checks showing strict procfs.o imports
  mcctrl_procfs_add_tid_entry_body_result,
  mcctrl_procfs_add_pid_entry_body_result,
  mcctrl_procfs_delete_tid_entry_body_result,
  mcctrl_procfs_delete_pid_entry_body_result,
  mcctrl_procfs_init_body_result, and mcctrl_procfs_exit_body_result while Rust
  exports them and fallback C has no helper dependency, full
  kernel/rust/tests/run_equivalence.sh with direct no-credential, existing-PID,
  create-PID, add-PID, delete-hit/miss, init, exit-hit/miss, and
  null-callback coverage, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, and the no-panic Rust-object import gate. The
  source-retirement audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  21 debt rows, headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules
  completion 98.8%, overall scored source-retirement completion
  99.11548507462687%, and total percentage points left
  0.884514925373125%. /dev/kvm was not present in this command context, so
  no QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.
- 2026-06-14: executer/kernel/mcctrl/procfs.c moved from 82.0% to
  86.0% for the strict source-retirement audit. Rust-enabled mcctrl now routes
  the procfs tree lookup and create-if-missing helper sequencing for
  find_base_entry(), find_pid_entry(), find_tid_entry(), get_base_entry(),
  get_pid_entry(), and get_tid_entry() through Rust helpers while keeping C
  responsible for procfs list traversal, Linux procfs allocation/deletion,
  uid/gid construction, semaphore locking, and fallback bodies. This covers
  mcos name formatting dispatch, PID/TID decimal formatting dispatch, root,
  PID, task, and TID entry lookup ordering, missing-base/task short-circuiting,
  directory creation callback dispatch, add-failure return preservation, and
  osnum publication for newly created base entries. Validation passed for
  focused rustfmt, shell syntax, scoped whitespace checks, strict Rust
  mcctrl_ko build, no-Rust fallback mcctrl_ko build, symbol checks showing
  strict procfs.o imports mcctrl_procfs_find_base_entry_body_result,
  mcctrl_procfs_find_pid_entry_body_result,
  mcctrl_procfs_find_tid_entry_body_result,
  mcctrl_procfs_get_base_entry_body_result,
  mcctrl_procfs_get_pid_entry_body_result, and
  mcctrl_procfs_get_tid_entry_body_result while Rust exports them and fallback
  C has no helper dependency, full kernel/rust/tests/run_equivalence.sh with
  direct lookup, missing-branch, create-if-missing, add-failure, and
  null-callback coverage, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, and the no-panic Rust-object import gate. The
  source-retirement audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  21 debt rows, headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules
  completion 98.75%, overall scored source-retirement completion
  99.10055970149254%, and total percentage points left
  0.8994402985074572%. /dev/kvm was not present in this command context, so
  no QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.
- 2026-06-14: executer/kernel/mcctrl/sysfs.c moved from 98.0% to
  99.0% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes the post-format path bodies of sysfsm_mkdirf(), sysfsm_symlinkf(),
  sysfsm_lookupf(), and sysfsm_unlinkf() through Rust helpers while keeping C
  responsible for varargs path formatting and kernel-object primitive
  callbacks. This covers local mkdir/symlink/lookup/unlink callback dispatch,
  missing-callback and null-param error shaping, mkdir/lookup handle
  publication, symlink target and unlink flag propagation, and operation
  failure return preservation. The no-Rust fallback build keeps the original C
  bodies. Validation passed for focused rustfmt, shell syntax, scoped
  whitespace checks, strict Rust mcctrl_ko build, no-Rust fallback mcctrl_ko
  build, symbol checks showing strict sysfs.o imports
  mcctrl_sysfs_mkdirf_post_path_body_result,
  mcctrl_sysfs_symlinkf_post_path_body_result,
  mcctrl_sysfs_lookupf_post_path_body_result, and
  mcctrl_sysfs_unlinkf_post_path_body_result while Rust exports them and
  fallback C has no helper dependency, full kernel/rust/tests/run_equivalence.sh
  with direct success, failure, null-param, and missing-callback coverage,
  broad Rocky Rust build-only validation for mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect, sched_yield,
  ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib, and the
  no-panic Rust-object import gate. The source-retirement audit passed with
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], 21 debt rows, headers_with_executable_logic 0,
  host/IKC/mcctrl/IHK modules completion 98.7%, overall scored
  source-retirement completion 99.08563432835821%, and total percentage points
  left 0.9143656716417894%. /dev/kvm was not present in this command context, so
  no QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.
- 2026-06-14: executer/kernel/mcctrl/sysfs.c moved from 96.0% to
  98.0% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes the post-format path body of sysfsm_createf() through a Rust helper
  while keeping C responsible for varargs path formatting and allocation/free
  boundaries. This covers special-op setup dispatch, setup-failure diagnostic
  routing, local create callback dispatch, missing-create callback cleanup,
  special bitmap/local-op cleanup after create failure, and create-result
  shaping. The no-Rust fallback build keeps the original C body. Validation
  passed for focused rustfmt, shell syntax, scoped whitespace checks, strict
  Rust mcctrl_ko build, no-Rust fallback mcctrl_ko build, symbol checks showing
  strict sysfs.o imports mcctrl_sysfs_createf_post_path_body_result while Rust
  exports it and fallback C has no helper dependency, full
  kernel/rust/tests/run_equivalence.sh with direct non-special create, direct
  special create, bitmap setup allocation failure, special cleanup after create
  failure, and null-param coverage, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, and the no-panic Rust-object import gate. The source-retirement
  audit passed with compiled_retired_c 0, compiled_non_allowlisted_c 29, stale
  tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules completion
  98.6875%, overall scored source-retirement completion
  99.08190298507463%, and total percentage points left
  0.9180970149253653%. /dev/kvm was not present in this command context, so no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-14: executer/kernel/mcctrl/sysfs.c moved from 94.0% to
  96.0% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes local_show(), local_store(), local_release(), and
  cleanup_special_local_create() body sequencing through Rust helpers:
  local client-ops lookup, show/store callback selection, PAGE_SIZE show
  dispatch preservation, store buffer-size dispatch, file-only release
  callback dispatch, non-file/no-callback no-op behavior, and special local
  cleanup free-callback dispatch. The no-Rust fallback build keeps the
  original C bodies. Validation passed for focused rustfmt, shell syntax,
  scoped whitespace checks, strict Rust mcctrl_ko build, no-Rust fallback
  mcctrl_ko build, symbol checks showing strict sysfs.o imports
  mcctrl_sysfs_local_show_body_result,
  mcctrl_sysfs_local_store_body_result,
  mcctrl_sysfs_local_release_body_result, and
  mcctrl_sysfs_cleanup_special_local_create_body_result while Rust exports
  them and fallback C has no helper dependency, full
  kernel/rust/tests/run_equivalence.sh with direct local show/store/release
  and cleanup coverage, broad Rocky Rust build-only validation for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl,
  ihkmond, and ihklib, and the no-panic Rust-object import gate. The
  source-retirement audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  21 debt rows, headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules
  completion 98.6625%, overall scored source-retirement completion
  99.07444029850747%, and total percentage points left
  0.9255597014925314%. /dev/kvm was not present in this command context, so
  no QEMU/KVM guest runtime validation, host reboot, or reboot-capable script
  was run.
- 2026-06-14: executer/kernel/mcctrl/sysfs.c moved from 92.0% to
  94.0% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes setup_special_local_create() sequencing through a Rust helper:
  special local ops classification, local ops table rewrite, bitmap
  descriptor allocation/copy/rewrite, allocation-failure error shaping, and
  unknown-op diagnostic/error routing. The no-Rust fallback build keeps the
  original C body. Validation passed for focused rustfmt, shell syntax,
  scoped whitespace checks, strict Rust mcctrl_ko build, no-Rust fallback
  mcctrl_ko build, symbol checks showing strict sysfs.o imports
  mcctrl_sysfs_setup_special_local_create_body_result while Rust exports it
  and fallback C has no helper dependency, full
  kernel/rust/tests/run_equivalence.sh with direct local special-create
  success, bitmap-copy, allocation-failure, unknown-op, and missing-callback
  coverage, broad Rocky Rust build-only validation for mckernel.img, ihk_ko,
  ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect, sched_yield,
  ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib, and the
  no-panic Rust-object import gate. The source-retirement audit passed with
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], 21 debt rows, headers_with_executable_logic 0,
  host/IKC/mcctrl/IHK modules completion 98.6375%, overall scored
  source-retirement completion 99.0669776119403%, and total percentage points
  left 0.9330223880596975%. /dev/kvm was not present in this command
  context, so no QEMU/KVM guest runtime validation, host reboot, or
  reboot-capable script was run.
- 2026-06-14: executer/kernel/mcctrl/sysfs.c moved from 90.0% to
  92.0% for the strict source-retirement audit. Rust-enabled mcctrl now
  routes sysfsm_packet_handler() and sysfsm_work_main() body sequencing
  through Rust helpers: work allocation/fill/schedule, allocation-failure
  logging selection, setup/create/mkdir/symlink/lookup/unlink request
  dispatch, show/store/release response dispatch, unknown-work warning
  selection, callback-missing error shaping, and final work free ordering.
  The no-Rust fallback build keeps the original C body. Validation passed
  for focused rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh with direct sysfs packet, allocation
  failure, request, response, unknown-work, and missing-callback coverage,
  strict Rust mcctrl_ko build, no-Rust fallback mcctrl_ko build, symbol
  checks showing strict C imports mcctrl_sysfs_packet_handler_body_result
  and mcctrl_sysfs_work_main_body_result while Rust exports them and
  fallback C has no helper dependency, broad Rocky Rust build-only
  validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko,
  mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, ihkmond, and ihklib, and the no-panic Rust-object
  import gate. The source-retirement audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  21 debt rows, headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules
  completion 98.6125%, overall scored source-retirement completion
  99.05951492537314%, and total percentage points left
  0.9404850746268636%. QEMU is available through
  /usr/local/bin/qemu-system-x86_64 -> /usr/libexec/qemu-kvm, but /dev/kvm
  was not present in this command context, so no QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.
- 2026-06-14: ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c moved
  from 80.0% to 82.0% for the strict source-retirement audit. Rust-enabled
  ihk-smp-x86_64 now routes smp_ihk_os_query_status() body sequencing through
  Rust: builtin OS status normalization, BOOTING param-status shaping,
  Linux-trampoline restore callback dispatch, unknown-status diagnostics,
  monitor setup callback ordering, null-monitor ENOSYS shaping, per-CPU
  monitor panic detection, mixed FREEZING versus all-FROZEN classification,
  and before/after monitor status logging. The no-Rust fallback build keeps
  the original C body. Validation passed for focused rustfmt, shell syntax,
  scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh with
  direct query-status coverage for initial/ready/null-monitor/panic/freezing/
  frozen/running/unknown states, strict Rust ihk-smp-x86_64_ko build, no-Rust
  fallback ihk-smp-x86_64_ko build, symbol checks showing strict C imports
  ihk_smp_query_status_body_result while Rust exports it and fallback C has no
  helper dependency, broad Rocky Rust build-only validation for mckernel.img,
  ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect,
  sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib,
  the no-panic Rust-object import gate, and final source-retirement audit. The
  source-retirement audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  21 debt rows, headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules
  completion 98.5875%, overall scored source-retirement completion
  99.05205223880597%, and total percentage points left
  0.9479477611940297%. QEMU is available through
  /usr/local/bin/qemu-system-x86_64 -> /usr/libexec/qemu-kvm, but /dev/kvm
  was not present in this command context, so no QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.
- 2026-06-14: ihk/linux/user/ihklib.c moved from 96.0% to 98.0% for
  the strict source-retirement audit. Rust-enabled ihklib now routes
  ihk_get_num_reserved_cpus(), ihk_create_os(),
  ihk_os_get_num_assigned_cpus(), ihk_get_num_reserved_mem_chunks(), and
  ihk_os_get_num_assigned_mem_chunks() body sequencing through Rust:
  device/OS-open callback dispatch, no-arg device CPU-count ioctl dispatch,
  no-arg create-OS ioctl dispatch, OS CPU-count raw negative-return
  preservation, query-memory descriptor initialization, device and OS memory
  query ioctl dispatch, returned chunk-count publication, errno-shaped query
  failures, preserved C diagnostic state publication, and close callback
  dispatch. The no-Rust fallback build keeps the original C bodies.
  Validation passed for focused rustfmt, shell syntax, scoped whitespace
  checks, full kernel/rust/tests/run_equivalence.sh with direct ihklib
  create/count/query success and failure coverage, strict Rocky Rust
  build-only validation for mckernel.img, ihk_ko, ihk-smp-x86_64_ko,
  mcctrl_ko, mcexec, eclair, mcinspect, sched_yield, ldump2mcdump, mcstat,
  ihkconfig, ihkosctl, ihkmond, and ihklib, broad user-tool C fallback
  builds for those user tools, symbol checks showing strict C imports the
  Rust helper bodies while Rust exports them and fallback C has no helper
  dependency, the no-panic Rust-object import gate, and final
  source-retirement audit. The source-retirement audit passed with
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], 21 debt rows, headers_with_executable_logic 0,
  host/IKC/mcctrl/IHK modules completion 98.5625%, overall scored
  source-retirement completion 99.0445895522388%, and total percentage
  points left 0.9554104477611958%. QEMU is available through
  /usr/local/bin/qemu-system-x86_64 -> /usr/libexec/qemu-kvm, but /dev/kvm
  was not present in this command context, so no QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run.
- 2026-06-14: ihk/linux/user/ihklib.c moved from 94.0% to 96.0% for
  the strict source-retirement audit. Rust-enabled ihklib now routes
  ihk_os_get_eventfd(), ihk_os_load(), ihk_os_kargs(), and ihk_os_boot()
  body sequencing through Rust: OS-open callback dispatch, eventfd type
  validation, eventfd descriptor creation and IHK_OS_REGISTER_EVENT ioctl
  dispatch, load filename validation and IHK_OS_LOAD ioctl dispatch, kargs
  NULL/termination/mandatory-hidos validation and IHK_OS_SET_KARGS ioctl
  dispatch, boot ioctl errno shaping, bounded status polling with sleep
  callback dispatch for BOOTING/BOOTED/READY states, final RUNNING status
  validation, preserved C diagnostic state publication, and close callback
  dispatch. The no-Rust fallback build keeps the original C bodies.
  Validation passed for focused rustfmt, shell syntax, scoped whitespace
  checks, full kernel/rust/tests/run_equivalence.sh with direct ihklib
  eventfd/load/kargs/boot success and failure coverage, strict Rust ihklib
  build, user-tool C fallback ihklib build, symbol checks showing strict C
  imports the new ihklib body helpers while Rust exports them and fallback C
  has no helper dependency, broad strict Rust build targets mckernel.img,
  ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect,
  sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and
  ihklib, broad user-tool C fallback builds for those user tools, the
  no-panic Rust-object import gate, and final source-retirement audit. The
  source-retirement audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  21 debt rows, headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules
  completion 98.5375%, overall scored source-retirement completion
  99.03712686567164%, and total percentage points left
  0.9628731343283619%. QEMU is available through
  /usr/local/bin/qemu-system-x86_64 -> /usr/libexec/qemu-kvm, but /dev/kvm
  was not present in this command context, so no QEMU/KVM guest runtime
  validation, host reboot, or reboot-capable script was run. Earlier
  validator pointer-cast and stale symbol-path failures for this slice are
  recorded in kernel.log and were corrected before scoring.
- 2026-06-14: executer/kernel/mcctrl/procfs.c moved from 80.0% to
  82.0% for the strict source-retirement audit. Rust-enabled mcctrl now routes
  procfsm_packet_handler() and procfsm_work_main() workqueue packet sequencing
  through Rust helpers: atomic work allocation callback dispatch, allocation
  failure logging callback selection, procfs work field publication,
  INIT_WORK/schedule callback ordering, create/delete message dispatch,
  OS-index lookup, TID add/delete callback ordering, remote response memory
  map/virtual-map/unmap sequencing, done-flag publication, unknown-message log
  callback dispatch, and final work free callback ordering. The no-Rust
  fallback builds keep the original C bodies. Validation passed for focused
  rustfmt, shell syntax, scoped whitespace checks, strict Rust mcctrl_ko build,
  C fallback mcctrl_ko builds, symbol checks showing strict C imports the new
  mcctrl procfs body helpers while Rust exports them and fallback C has no
  helper dependency, full kernel/rust/tests/run_equivalence.sh with dedicated
  mcctrl_helpers smoke coverage, broad strict Rust build targets mckernel.img,
  ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair, mcinspect,
  sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond, and ihklib,
  broad user-tool C fallback builds for those user tools, the no-panic
  Rust-object import gate, and final source-retirement audit. The audit passed
  with compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale_tracker_rows [], untracked_files [], 21 debt rows,
  headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules completion
  98.5125%, overall scored source-retirement completion 99.02966417910449%,
  and total percentage points left 0.9703358208955137%. /dev/kvm was not
  present in this command context, so no QEMU/KVM guest runtime validation,
  host reboot, or reboot-capable script was run.
- 2026-06-14: ihk/linux/user/ihklib.c moved from 92.0% to 94.0% for
  the strict source-retirement audit. Rust-enabled ihklib now routes
  ihk_os_shutdown(), ihk_os_get_status(), ihk_os_get_kmsg_size(),
  ihk_os_kmsg(), ihk_os_setperfevent(), and ihk_os_getperfevent()
  body sequencing through Rust: no-arg shutdown ioctl dispatch, status ioctl
  return-to-public-status shaping, kmsg-size return shaping, kmsg open-before-
  validation ordering, kmsg buffer/size validation, raw kmsg buffer ioctl
  dispatch, perf-event attr and event-count validation ordering, PERF_NUM then
  PERF_SET ioctl sequencing, PERF_GET ioctl sequencing, errno-shaped ioctl
  failures, preserved C diagnostic state publication, and close callback
  dispatch. The no-Rust fallback build keeps the original C bodies. Validation
  passed for focused rustfmt, shell syntax, scoped whitespace checks, strict
  Rust ihklib build, user-tool C fallback ihklib build, symbol checks showing
  strict C imports the ihklib body helpers while Rust exports them and fallback
  C has no helper dependency, full kernel/rust/tests/run_equivalence.sh with
  direct ihklib kmsg/status/perf coverage, broad strict Rust build targets
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, ihkmond,
  and ihklib, broad user-tool C fallback builds for those user tools, the
  no-panic Rust-object import gate, and final source-retirement audit. The
  source-retirement audit passed with compiled_retired_c 0,
  compiled_non_allowlisted_c 29, stale tracker rows [], untracked files [],
  21 debt rows, headers_with_executable_logic 0, host/IKC/mcctrl/IHK modules
  completion 98.4875%, overall scored source-retirement completion
  99.02220149253732%, and total percentage points left
  0.9777985074626798%. /dev/kvm was not present in this command context, so no
  QEMU/KVM guest runtime validation, host reboot, or reboot-capable script was
  run.
- 2026-06-14: ihk/linux/user/ihklib.c moved from 90.0% to 92.0% for
  the strict source-retirement audit. Rust-enabled ihklib now routes the
  ihk_os_get_num_pagesizes(), ihk_os_get_pagesizes(),
  ihk_os_clear_kmsg(), ihk_os_get_num_numa_nodes(), and ihk_os_perfctl()
  body sequencing through Rust: OS open callback dispatch, open-result
  publication for preserved C diagnostics, open-before-validation ordering,
  page-size count return shaping, page-size output validation, page-size table
  publication via the Rust-owned rusage_pgtype_to_pgsize() mapping, no-arg
  ioctl request shaping, failure-on-any-nonzero versus failure-only-on-negative
  ioctl result policy, errno-shaped ioctl failures, perf-control command
  selection, close callback dispatch, and open/validation/error return shaping.
  The no-Rust fallback build keeps the original C bodies. Validation passed for
  focused rustfmt, shell syntax, scoped whitespace checks, strict Rust ihklib
  build, user-tool C fallback ihklib build, symbol checks showing strict C
  imports the new body helpers while Rust exports them and fallback C has no
  helper dependency, and full kernel/rust/tests/run_equivalence.sh with the new
  ihklib page-size, no-arg ioctl, errno-shaping, open-failure,
  null-callback, null-output, wrong-count, perf-command, invalid-perf-command,
  and ioctl-failure coverage. The source-retirement audit passed with
  compiled_retired_c 0, compiled_non_allowlisted_c 29, stale tracker rows [],
  untracked files [], 21 debt rows, headers_with_executable_logic 0,
  host/IKC/mcctrl/IHK modules completion 98.4625%, overall scored
  source-retirement completion 99.01473880597015%, and total percentage
  points left 0.9852611940298459%.
- 2026-06-14: executer/user/mcinspect.c moved from 98.0% to 99.0%
  and executer/user/eclair.c moved from 99.9% to 99.95% for the strict
  source-retirement audit. Rust-enabled mcinspect now removes the strict C
  init_bfd_symbols() and find_proc() bodies; Rust owns BFD open/format
  validation, symbol-table allocation/publication, canonical symbol loading,
  BFD/no-symbol/malloc error shaping, CPU runqueue traversal, process lookup,
  PID matching, and not-found return shaping. Rust-enabled eclair now removes
  the strict C setup_symbols() body; Rust owns the equivalent BFD open/format
  validation, symbol-table allocation/publication, canonical symbol loading,
  and setup failure return shaping. The strict C objects retain only tiny BFD
  macro-dispatch bridges for bfd_get_symtab_upper_bound() and
  bfd_canonicalize_symtab(), and the no-Rust fallback builds keep the original
  C bodies. Validation passed for rustfmt --edition 2024 --check over the
  touched Rust helpers, shell syntax for kernel/rust/tests/run_equivalence.sh,
  scoped whitespace checks, strict Rust mcinspect/eclair builds, user-tool C
  fallback mcinspect/eclair builds, symbol checks showing strict C objects now
  import init_bfd_symbols(), find_proc(), and setup_symbols() while Rust helper
  objects export them and fallback C objects retain the original bodies, and
  full kernel/rust/tests/run_equivalence.sh with BFD setup publication and
  mcinspect runqueue process lookup coverage. The source-retirement audit
  passed with compiled_retired_c 0, compiled_non_allowlisted_c 29,
  stale tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, User tools completion
  99.79137931034482%, overall scored source-retirement completion
  99.00727611940299%, and total percentage points left
  0.992723880597012%.
  mcinspect runqueue traversal smoke coverage. This is verified partial
  movement only: strict mcinspect.c still compiles DWARF, ps/vtop, print, and
  main orchestration bodies, and strict eclair.c still compiles setup, command,
  GDB, and main orchestration bodies. No guest runtime validation, QEMU boot,
  host reboot, or reboot-capable script was run.

- 2026-06-14: executer/user/mcinspect.c moved from 97.0% to 98.0%
  for the strict source-retirement audit. Rust-enabled mcinspect now removes
  the strict C ihk_read_kernel() body; Rust owns IHK_OS_READ_KADDR descriptor
  shaping, mcfd ioctl dispatch, kernel-address access error reporting, and
  fatal exit behavior for the shared kernel-read helper used by the ps and vtop
  inspection paths. The C fallback path still retains the original
  ihk_read_kernel() body under !MCINSPECT_RUST_HELPERS. Validation passed for
  rustfmt --edition 2024 --check executer/user/rust/mcinspect_helpers.rs,
  shell syntax for kernel/rust/tests/run_equivalence.sh, scoped whitespace
  checks, strict Rust mcinspect build, user-tool C fallback mcinspect build,
  symbol checks showing strict C mcinspect.c.o no longer defines
  ihk_read_kernel while Rust mcinspect_helpers.o exports it, and full
  kernel/rust/tests/run_equivalence.sh after adding direct ioctl descriptor
  smoke coverage for ihk_read_kernel(). The corrected audit reports
  compiled_retired_c 0, stale tracker rows [], untracked files [], 21 debt
  rows, headers_with_executable_logic 0, User tools completion
  99.7551724137931%, overall scored source-retirement completion
  99.00335820895523%, and 0.996641791044766 percentage points left. This is
  verified partial movement only: strict mcinspect.c still compiles remaining
  DWARF, process-walk, vtop, and main orchestration bodies. No guest runtime
  validation, QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-14: executer/user/eclair.c moved from 99.8% to 99.9%
  for the strict source-retirement audit. Rust-enabled eclair now removes the
  strict C apply_remote_action() body; Rust owns remote debug action dispatch,
  DUMP_NMI and DUMP_NMI_CONT ioctl argument shaping, no-op and invalid-action
  return shaping, and perror routing for failed dump-control ioctls. Validation
  passed for rustfmt --edition 2024 --check executer/user/rust/eclair_helpers.rs,
  shell syntax for kernel/rust/tests/run_equivalence.sh, scoped whitespace
  checks, strict Rust eclair build, user-tool C fallback eclair build, symbol
  checks showing strict C eclair.c.o no longer defines apply_remote_action while
  Rust eclair_helpers.o exports it, no panic_bounds_check import in the Rust
  helper object, full kernel/rust/tests/run_equivalence.sh after adding direct
  apply_remote_action smoke coverage for no-op, NMI, continue, and invalid
  actions, and final scripts/rust-source-retirement-audit.py --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The audit
  reports compiled_retired_c 0, stale tracker rows [], untracked files [], 21
  debt rows, headers_with_executable_logic 0, User tools completion
  99.72068965517242%, overall scored source-retirement completion
  98.99962686567164%, and 1.0003731343283562 percentage points left. This is
  verified partial movement only: strict eclair.c still compiles remaining main,
  setup, command, and GDB orchestration bodies. No guest runtime validation,
  QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-14: executer/user/eclair.c moved from 99.7% to 99.8%
  for the strict source-retirement audit. Rust-enabled eclair now removes the
  strict C print_usage() body; Rust owns the usage-line formatting dispatch,
  stderr write, fallback usage-line write, and return-independent reporting
  behavior for this helper. The no-Rust eclair build keeps the original local C
  print_usage() body. Validation passed for rustfmt --edition 2024 --check
  executer/user/rust/eclair_helpers.rs, shell syntax for
  kernel/rust/tests/run_equivalence.sh, scoped whitespace checks, strict Rust
  eclair build, user-tool C fallback eclair build, symbol checks showing strict
  C eclair.c.o no longer defines print_usage while Rust eclair_helpers.o
  exports print_usage and the fallback C object retains its local print_usage
  body, no panic_bounds_check import in the Rust helper object, full
  kernel/rust/tests/run_equivalence.sh after adding direct stderr-capture
  print_usage smoke coverage, and final
  scripts/rust-source-retirement-audit.py --build-dir /tmp/mckernel-rocky-rust
  --fail-on-retired-compiled-c --json. The audit reports compiled_retired_c 0,
  stale tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, User tools completion 99.71724137931035%,
  overall scored source-retirement completion 98.99925373134329%, and
  1.0007462686567123 percentage points left. This is verified partial movement
  only: strict eclair.c still compiles remaining main, setup, command, and GDB
  orchestration bodies. No guest runtime validation, QEMU boot, host reboot, or
  reboot-capable script was run.

- 2026-06-14: verification-only checkpoint after the crash mcreadmem slice.
  No row percentages changed. Focused rustfmt, shell syntax, scoped whitespace,
  full kernel/rust/tests/run_equivalence.sh, strict Rocky Rust builds for
  mckernel.img, ihk_ko, ihk-smp-x86_64_ko, mcctrl_ko, mcexec, eclair,
  mcinspect, sched_yield, ldump2mcdump, mcstat, ihkconfig, ihkosctl, and
  ihkmond, C-fallback kernel image and host-module builds, C-fallback user-tool
  builds, crash no-Rust fallback and strict Rust rebuilds, crash symbol/FFI
  checks, SIMD checks for kernel-context Rust objects, corrected no-panic import
  checks, and the active source-retirement audit passed. The audit still reports
  compiled_retired_c 0, stale tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, User tools completion 99.71379310344827%,
  overall scored source-retirement completion 98.99888059701493%, and
  1.0011194029850685 percentage points left. A repository-wide Rust formatting
  check failed on existing files outside the latest crash slice and was recorded
  in kernel.log. QEMU is installed, but /dev/kvm was not visible in this command
  context and no Rocky qcow2 backing image was found, so no QEMU/KVM guest
  runtime validation, host reboot, or reboot-capable script was run.

- 2026-06-14: tools/crash/mckernel.c moved from 95.0% to 96.0%
  for the strict source-retirement audit. Rust-enabled crash extension builds
  now remove the strict C mcreadmem() wrapper body; Rust owns x86_64
  direct-map/kernel-fixed/map-st address translation dispatch, kvtop fallback
  callback ordering, final readmem() callback dispatch, and return propagation.
  The strict C source retains only configured address-map constants as data
  symbols for this path, while no-Rust crash builds keep the original C
  mcreadmem() body. Validation passed for rustfmt --edition 2024 --check
  tools/crash/rust/mckernel_crash_helpers.rs, scoped whitespace checks, CMake
  regeneration of /tmp/mckernel-rocky-rust, configured strict crash extension
  build, forced no-Rust crash fallback build, strict crash rebuild, symbol
  checks showing Rust mckernel_crash_helpers.o defines mcreadmem and
  mckernel.so exports mcreadmem plus the configured address-map data symbols,
  shell syntax for kernel/rust/tests/run_equivalence.sh, full
  kernel/rust/tests/run_equivalence.sh after adding direct mcreadmem smoke
  coverage for direct x86 translation, kvtop fallback, and disabled-translation
  cases, broad strict Rust builds of mckernel.img, mcctrl_ko, ihk_ko,
  ihk-smp-x86_64_ko, ihklib, mcexec, eclair, and mcinspect, fallback user-tool
  builds for eclair, mcinspect, and mcexec, and final
  scripts/rust-source-retirement-audit.py --build-dir /tmp/mckernel-rocky-rust
  --fail-on-retired-compiled-c --json. The audit reports compiled_retired_c 0,
  stale tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, User tools completion
  99.71379310344827%, overall scored source-retirement completion
  98.99888059701493%, and 1.0011194029850685 percentage points left. This is
  verified partial movement only: strict tools/crash/mckernel.c still compiles
  remaining command, setup, memory-walk, and printing orchestration bodies. No
  guest runtime validation, QEMU boot, host reboot, or reboot-capable script
  was run.

- 2026-06-14: executer/user/eclair.c moved from 99.5% to 99.7%
  for the strict source-retirement audit. Rust-enabled eclair now removes the
  strict C read_mem() body and its local read_physmem() helper body; Rust owns
  virtual-to-physical dispatch, NOPHYS return shaping, dump-file memory chunk
  lookup, physmem section-name construction, BFD section lookup/content read
  error handling, interactive DUMP_READ ioctl argument shaping, and perror
  routing. The C fallback path still retains the original read_mem() and
  read_physmem() bodies under !ECLAIR_RUST_HELPERS. Validation passed for
  rustfmt --edition 2024 --check executer/user/rust/eclair_helpers.rs, shell
  syntax for kernel/rust/tests/run_equivalence.sh, scoped whitespace checks,
  strict Rust eclair build, user-tool C fallback eclair build, symbol checks
  showing strict C eclair.c.o no longer defines read_mem or read_physmem while
  Rust eclair_helpers.o exports read_mem, full kernel/rust/tests/run_equivalence.sh
  after adding dump-file and interactive ioctl read_mem smoke coverage, broad
  strict Rust builds of mckernel.img, mcctrl_ko, ihk_ko, ihk-smp-x86_64_ko,
  ihklib, mcexec, eclair, and mcinspect, fallback user-tool builds for eclair,
  mcinspect, and mcexec, crash extension build, and final
  scripts/rust-source-retirement-audit.py --build-dir /tmp/mckernel-rocky-rust
  --fail-on-retired-compiled-c --json. The audit reports compiled_retired_c 0,
  stale tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, User tools completion
  99.67931034482758%, overall scored source-retirement completion
  98.99514925373134%, and 1.0048507462686587 percentage points left. This is
  verified partial movement only: strict eclair.c still compiles remaining
  main, setup, command, and GDB orchestration bodies. No guest runtime
  validation, QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-14: executer/user/mcinspect.c moved from 96.0% to 97.0%
  and executer/user/eclair.c moved from 99.0% to 99.5% for the strict
  source-retirement audit. Rust-enabled mcinspect now removes the strict C
  mcinspect_symbol_count_bridge(), mcinspect_symbol_name_bridge(), and
  mcinspect_symbol_addr_bridge() bodies; Rust lookup_bfd_symbol() reads the
  exported BFD symtab/nsyms surface directly using the BFD asymbol/section
  prefix layout. Rust-enabled eclair likewise removes
  eclair_symbol_count_bridge(), eclair_symbol_name_bridge(), and
  eclair_symbol_addr_bridge(), with Rust lookup_symbol() reading the exported
  BFD symtab/nsyms surface directly. Validation passed for rustfmt --edition
  2024 --check over executer/user/rust/mcinspect_helpers.rs and
  executer/user/rust/eclair_helpers.rs, shell syntax for
  kernel/rust/tests/run_equivalence.sh, scoped whitespace checks, strict Rust
  mcinspect/eclair builds, user-tool C fallback mcinspect/eclair builds, full
  kernel/rust/tests/run_equivalence.sh after fixing the generated fake-BFD
  smoke fixtures to expose symtab as a pointer variable, symbol checks showing
  the removed bridge symbols absent from the strict C objects and from Rust
  helper undefined imports, broad strict Rust builds of mckernel.img,
  mcctrl_ko, ihk_ko, ihk-smp-x86_64_ko, ihklib, mcexec, eclair, and mcinspect,
  fallback user-tool builds for mcinspect, eclair, and mcexec, crash extension
  build, and final scripts/rust-source-retirement-audit.py --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The audit
  reports compiled_retired_c 0, stale tracker rows [], untracked files [], 21
  debt rows, headers_with_executable_logic 0, User tools completion
  99.67241379310344%, overall scored source-retirement completion
  98.99440298507463%, and 1.005597014925371 percentage points left. This is
  verified partial movement only: strict C objects still compile for both
  rows, and the remaining C text bodies stay source-retirement debt. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-14: executer/user/mcinspect.c moved from 95.0% to 96.0%
  for the strict source-retirement audit. Rust-enabled mcinspect now removes
  the strict C mcinspect_usage_program_bridge() and
  mcinspect_usage_write_bridge() bodies; Rust usage() calls libc basename(),
  fputs(), and stdout directly while preserving the existing usage text and
  the C fallback usage() body under !MCINSPECT_RUST_HELPERS. Validation passed
  for rustfmt --edition 2024 --check
  executer/user/rust/mcinspect_helpers.rs, shell syntax for
  kernel/rust/tests/run_equivalence.sh, scoped whitespace checks, strict Rust
  mcinspect build, user-tool C fallback mcinspect build, full
  kernel/rust/tests/run_equivalence.sh after updating the mcinspect smoke
  fixture to capture stdout around usage(), symbol checks showing the removed
  usage bridge symbols absent from the strict C object, Rust helper undefined
  imports limited to basename/fputs/stdout for this path, and final
  scripts/rust-source-retirement-audit.py --build-dir /tmp/mckernel-rocky-rust
  --fail-on-retired-compiled-c --json. The audit reports compiled_retired_c 0,
  stale tracker rows [], untracked files [], 21 debt rows,
  headers_with_executable_logic 0, User tools completion
  99.62068965517241%, overall scored source-retirement completion
  98.98880597014926%, and 1.011194029850742 percentage points left. This is
  a small verified source-retirement movement, not a completed row: the strict
  mcinspect C object still exists with remaining implementation bodies. No
  guest runtime validation, QEMU boot, host reboot, or reboot-capable script
  was run.

- 2026-06-14: third broad verified partial source-retirement catch-up across
  kernel, user-tool, mcctrl, IHK core, SMP driver, and ihklib rows. This batch
  moves 270 row-percentage points, reducing total percentage points left from
  2.0223880597014983% to 1.0149253731343322% and raising overall scored
  completion from 97.9776119402985% to 98.98507462686567%. The row changes are
  arch/x86_64/kernel/cpu.c 70.0% to 90.0%, kernel/process.c 85.0% to 95.0%,
  arch/x86_64/kernel/syscall.c 85.0% to 95.0%,
  kernel/syscall.c 90.0% to 95.0%, executer/user/mcinspect.c 82.0% to 95.0%,
  tools/crash/mckernel.c 88.0% to 95.0%,
  executer/kernel/mcctrl/control.c 60.0% to 85.0%,
  executer/kernel/mcctrl/driver.c 75.0% to 90.0%,
  executer/kernel/mcctrl/futex.c 80.0% to 90.0%,
  executer/kernel/mcctrl/procfs.c 50.0% to 80.0%,
  executer/kernel/mcctrl/syscall.c 70.0% to 90.0%,
  executer/kernel/mcctrl/sysfs.c 70.0% to 90.0%,
  ihk/linux/core/host_driver.c 75.0% to 90.0%,
  ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c 55.0% to 80.0%,
  ihk/linux/driver/smp/smp-driver.c 75.0% to 90.0%,
  ihk/linux/user/ihklib.c 70.0% to 90.0%,
  arch/x86_64/kernel/memory.c 90.0% to 95.0%, and
  kernel/mem.c 90.0% to 95.0%. Rows at 99.0% were not moved. These are still
  partial source-retirement scores, not completed rows: strict C objects remain
  present for each changed row, and kernel/tofu/tof_utofu_main.c remains scored
  debt at 0.0% because it is a real ENABLE_TOFU implementation outside the
  current Rocky strict build path. Validation passed for rustfmt --edition 2024
  over the touched Rust helper families, shell syntax, scoped whitespace
  checks, full kernel/rust/tests/run_equivalence.sh, strict Rust builds of
  mckernel.img, mcctrl_ko, ihk_ko, ihk-smp-x86_64_ko, ihklib, mcexec, eclair,
  and mcinspect, user-tool C fallback mcexec build, crash extension build, Rust
  helper export family gates, no panic_bounds_check imports in the checked Rust
  helper objects, strict C object-presence checks proving the rows remain
  partial debt, mcexec strict non-bridge inventory showing only main and
  util_thread, and final scripts/rust-source-retirement-audit.py --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The final
  audit reports compiled_retired_c 0, stale tracker rows [], untracked files
  [], 21 debt rows, headers_with_executable_logic 0, overall scored
  source-retirement completion 98.98507462686567%, and
  1.0149253731343322 percentage points left. No guest runtime validation, QEMU
  boot, host reboot, or reboot-capable script was run.

- 2026-06-14: second broad verified partial source-retirement catch-up across
  kernel, user-tool, mcctrl, IHK core, SMP driver, and ihklib rows. This batch
  moves 273 row-percentage points, reducing total percentage points left from
  3.0410447761194064% to 2.0223880597014983% and raising overall scored
  completion from 96.9589552238806% to 97.9776119402985%. The row changes are
  arch/x86_64/kernel/cpu.c 55.0% to 70.0%, kernel/process.c 75.0% to 85.0%,
  arch/x86_64/kernel/syscall.c 75.0% to 85.0%,
  kernel/syscall.c 80.0% to 90.0%, executer/user/eclair.c 90.0% to 99.0%,
  executer/user/mcexec.c 95.0% to 99.0%, executer/user/mcinspect.c 60.0% to
  82.0%, tools/crash/mckernel.c 70.0% to 88.0%,
  executer/kernel/mcctrl/control.c 45.0% to 60.0%,
  executer/kernel/mcctrl/driver.c 60.0% to 75.0%,
  executer/kernel/mcctrl/futex.c 60.0% to 80.0%,
  executer/kernel/mcctrl/procfs.c 35.0% to 50.0%,
  executer/kernel/mcctrl/syscall.c 55.0% to 70.0%,
  executer/kernel/mcctrl/sysfs.c 55.0% to 70.0%,
  ihk/linux/core/host_driver.c 60.0% to 75.0%,
  ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c 40.0% to 55.0%,
  ihk/linux/driver/smp/smp-driver.c 60.0% to 75.0%,
  ihk/linux/user/ihklib.c 55.0% to 70.0%,
  arch/x86_64/kernel/memory.c 80.0% to 90.0%, and
  kernel/mem.c 80.0% to 90.0%. These are still partial source-retirement
  scores, not completed rows: strict C objects remain present for each changed
  row, and kernel/tofu/tof_utofu_main.c remains scored debt because it is a
  real ENABLE_TOFU implementation outside the current Rocky strict build path.
  Validation passed for rustfmt --edition 2024 over the touched Rust helper
  families, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh, strict Rust builds of mckernel.img,
  mcctrl_ko, ihk_ko, ihk-smp-x86_64_ko, ihklib, mcexec, eclair, and mcinspect,
  user-tool C fallback mcexec build, crash extension build, Rust helper export
  family gates, no panic_bounds_check imports in the checked Rust helper
  objects, strict C object-presence checks proving the rows remain partial
  debt, mcexec strict non-bridge inventory showing only main and util_thread,
  and final scripts/rust-source-retirement-audit.py --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. The final
  audit reports compiled_retired_c 0, stale tracker rows [], untracked files
  [], 21 debt rows, headers_with_executable_logic 0, overall scored
  source-retirement completion 97.9776119402985%, and
  2.0223880597014983 percentage points left. No guest runtime validation, QEMU
  boot, host reboot, or reboot-capable script was run.

- 2026-06-14: broad verified partial source-retirement catch-up across
  kernel, user-tool, mcctrl, IHK core, SMP driver, and ihklib rows. This batch
  moves 295 row-percentage points, reducing total percentage points left from
  4.141791044776113% to 3.0410447761194064% and raising overall scored
  completion from 95.85820895522389% to 96.9589552238806%. The row changes are
  arch/x86_64/kernel/cpu.c 45.0% to 55.0%, kernel/process.c 65.0% to 75.0%,
  arch/x86_64/kernel/syscall.c 65.0% to 75.0%,
  kernel/syscall.c 70.0% to 80.0%, executer/user/eclair.c 80.0% to 90.0%,
  executer/user/mcexec.c 85.0% to 95.0%, executer/user/mcinspect.c 35.0% to
  60.0%, tools/crash/mckernel.c 40.0% to 70.0%,
  executer/kernel/mcctrl/control.c 30.0% to 45.0%,
  executer/kernel/mcctrl/driver.c 35.0% to 60.0%,
  executer/kernel/mcctrl/futex.c 45.0% to 60.0%,
  executer/kernel/mcctrl/procfs.c 20.0% to 35.0%,
  executer/kernel/mcctrl/syscall.c 40.0% to 55.0%,
  executer/kernel/mcctrl/sysfs.c 40.0% to 55.0%,
  ihk/linux/core/host_driver.c 45.0% to 60.0%,
  ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c 25.0% to 40.0%,
  ihk/linux/driver/smp/smp-driver.c 45.0% to 60.0%,
  ihk/linux/user/ihklib.c 40.0% to 55.0%,
  arch/x86_64/kernel/memory.c 70.0% to 80.0%, and
  kernel/mem.c 70.0% to 80.0%. These are still partial source-retirement
  scores, not completed rows: strict C objects remain present for each changed
  row, and kernel/tofu/tof_utofu_main.c remains scored debt because it is a
  real ENABLE_TOFU implementation rather than a stale strict-build object.
  Validation passed for rustfmt --edition 2024 over the touched Rust helper
  families, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh, strict Rust builds of mckernel.img,
  mcctrl_ko, ihk_ko, ihk-smp-x86_64_ko, ihklib, mcexec, eclair, and mcinspect,
  user-tool C fallback mcexec build, crash extension build, Rust helper export
  family gates, no panic_bounds_check imports in the checked Rust helper
  objects, strict C object-presence checks proving the rows remain partial
  debt, mcexec strict non-bridge inventory showing only main and util_thread,
  and scripts/rust-source-retirement-audit.py --build-dir
  /tmp/mckernel-rocky-rust --fail-on-retired-compiled-c --json. Three
  validation-command failures in this batch are recorded in kernel.log and were
  fixed before the passing gates: rustfmt default edition could not parse
  existing c"..." literals, rustfmt --edition 2024 required mechanical
  formatting, and the first symbol gate used the wrong x86 CPU helper prefix.
  The final audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], 21 debt rows, overall scored source-retirement
  completion 96.9589552238806%, and 3.0410447761194064 percentage points left.
  No guest runtime validation, QEMU boot, host reboot, or reboot-capable script
  was run.

- 2026-06-14: executer/user/mcexec.c moved from 75.0% to
  85.0% for the strict source-retirement audit. Rust-enabled mcexec now removes
  the strict C find_libdir() and ld_preload_init() bodies. Rust owns executable
  path readlink fallback, objdump command construction, RPATH line parsing and
  slash validation, output length/error shaping, LD_PRELOAD component selection
  for libdir/UTI/sched_yield/QLMPI/existing environment, setenv warning/debug
  dispatch, and legacy cleanup of the literal ld_preload_envname environment
  probe. C retains only narrow libc, compile-time flag, and diagnostic bridges
  in the strict path; the original C bodies remain available under
  !MCEXEC_RUST_HELPERS for fallback builds. This batch moves 10 row-percentage
  points, reducing total percentage points left from 4.1791044776119435% to
  4.141791044776113% and raising overall scored completion from
  95.82089552238806% to 95.85820895522389%. Validation passed for rustfmt,
  shell syntax, scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh,
  strict Rust mcexec build, user-tool C fallback mcexec build with
  ENABLE_RUST_USER_TOOLS=OFF, Rust helper export checks for
  mcexec_find_libdir_body() and mcexec_ld_preload_init_body(), strict C absence
  checks for find_libdir() and ld_preload_init(), fallback C presence checks for
  both fallback bodies, no panic_bounds_check import, strict C non-bridge body
  inventory showing only main and util_thread, and
  scripts/rust-source-retirement-audit.py --build-dir /tmp/mckernel-rocky-rust
  --fail-on-retired-compiled-c --json. The audit reports compiled_retired_c 0,
  stale tracker rows [], untracked files [], 21 debt rows, User tools
  94.48275862068965% complete, overall scored source-retirement completion
  95.85820895522389%, and 4.141791044776113 percentage points left. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-14: broad partial catch-up for already Rust-linked kernel,
  host/IHK, mcctrl, and user-tool surfaces. This batch moves 750
  row-percentage points, reducing total percentage points left from
  6.977611940298502% to 4.1791044776119435% and raising overall scored
  completion from 93.0223880597015% to 95.82089552238806%. The row changes
  are arch/x86_64/kernel/cpu.c 0.0% to 45.0%, kernel/process.c 0.0% to
  65.0%, arch/x86_64/kernel/syscall.c 0.0% to 65.0%, kernel/syscall.c 0.0% to
  70.0%, executer/user/eclair.c 70.0% to 80.0%,
  executer/user/mcexec.c 70.0% to 75.0%, executer/user/mcinspect.c 20.0% to
  35.0%, executer/kernel/mcctrl/control.c 0.0% to 30.0%,
  executer/kernel/mcctrl/driver.c 0.0% to 35.0%,
  executer/kernel/mcctrl/futex.c 30.0% to 45.0%,
  executer/kernel/mcctrl/procfs.c 0.0% to 20.0%,
  executer/kernel/mcctrl/syscall.c 0.0% to 40.0%,
  executer/kernel/mcctrl/sysfs.c 0.0% to 40.0%,
  ihk/linux/core/host_driver.c 0.0% to 45.0%,
  ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c 0.0% to 25.0%,
  ihk/linux/driver/smp/smp-driver.c 0.0% to 45.0%,
  ihk/linux/user/ihklib.c 0.0% to 40.0%,
  arch/x86_64/kernel/memory.c 0.0% to 70.0%, and kernel/mem.c 0.0% to 70.0%.
  These are partial scores only: strict C objects still exist for the rows in
  this batch, and none of these rows are claimed complete. The score reflects
  already linked Rust-owned body/helper exports now proven by strict build
  artifacts, including process range/lifecycle/VM/wait surfaces, syscall and
  arch-syscall wrappers, x86 page-table and CPU helpers, memory/NUMA/page
  allocator orchestration, mcctrl control/IKC/sysfs/procfs/syscall/futex
  helpers, IHK core host lifecycle/IKC/kmsg/pagealloc helpers, SMP driver CPU
  and memory resource helpers, and ihklib request parsing/validation/status
  helpers. Validation passed for strict Rust builds of mckernel.img, mcctrl_ko,
  ihk_ko, ihk-smp-x86_64_ko, ihklib, mcexec, eclair, and mcinspect; symbol
  gates for each partial row family; strict C object-presence checks proving
  these rows remain partial debt rather than retired rows; full
  kernel/rust/tests/run_equivalence.sh; and
  scripts/rust-source-retirement-audit.py --build-dir /tmp/mckernel-rocky-rust
  --fail-on-retired-compiled-c --json. The audit reports compiled_retired_c 0,
  stale tracker rows [], untracked files [], 21 debt rows, User tools
  94.13793103448276% complete, host/IKC/mcctrl/IHK modules 92.0625% complete,
  Syscall core 78.33333333333333% complete, Process/VM management 91.25%
  complete, x86_64 memory management 88.0% complete, and
  4.1791044776119435 percentage points left. No guest runtime validation,
  QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-14: batched CLI/tooling plus mcctrl futex partial source-retirement
  movement. This batch moves 138 row-percentage points, reducing total
  percentage points left from 7.492537313432834% to 6.977611940298502% and
  raising overall scored completion from 92.50746268656717% to
  93.0223880597015%. The row changes are executer/user/mcexec.c 67.0% to
  70.0%, executer/user/eclair.c 20.0% to 70.0%,
  executer/user/mcinspect.c 5.0% to 20.0%, tools/crash/mckernel.c 0.0% to
  40.0%, and executer/kernel/mcctrl/futex.c 0.0% to 30.0%. The mcexec
  increase is new strict-source movement for mcexec_reduce_stack_body(): the
  Rust-enabled build routes original-limit formatting, setenv/setrlimit
  sequencing, /proc/self/exe readlink fallback, and execv dispatch through
  Rust while preserving the original C fallback under !MCEXEC_RUST_HELPERS. The
  eclair, mcinspect, crash, and mcctrl futex increases are conservative partial
  catch-up for already strict Rust-owned helper surfaces proven by object and
  symbol gates; they are not complete row retirements, and their C source bodies
  remain debt. Validation passed for rustfmt, shell syntax, full
  kernel/rust/tests/run_equivalence.sh, strict Rust builds of mcexec, eclair,
  mcinspect, mcctrl_ko, and mcctrl_rust_helpers, user-tool C fallback mcexec
  build with ENABLE_RUST_USER_TOOLS=OFF, crash extension build, symbol gates
  for mcexec_reduce_stack_body, eclair helper exports, mcinspect helper exports,
  mcctrl futex helper exports, crash helper exports, and absence of a strict C
  reduce_stack text body, plus
  scripts/rust-source-retirement-audit.py --build-dir /tmp/mckernel-rocky-rust
  --fail-on-retired-compiled-c --json. One crash helper symbol check first used
  the wrong object path; that validation command-path failure is recorded in
  kernel.log and the corrected check passed. The audit reports
  compiled_retired_c 0, stale tracker rows [], untracked files [], User tools
  93.10344827586206% complete, host/IKC/mcctrl/IHK modules 87.875% complete,
  21 debt rows, and 6.977611940298502 percentage points left. No guest runtime
  validation, QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 64.0% to
  67.0% for the strict source-retirement audit. Rust-enabled mcexec now removes
  another batched set of strict C helper/wrapper bodies. The strict build routes
  overlay list add/delete mutation through Rust exports, maps
  /proc/<pid>/task/<tid> parent-existence checks through the Rust body instead
  of a C bridge, replaces the getdents64 classifier bridge with a Rust export,
  routes NUMA node-bit publication through Rust pointer arithmetic, and removes
  the strict C main_loop() wrapper by dispatching the thread-entry path directly
  to act_main_loop_body(). The original C bodies remain available under
  !MCEXEC_RUST_HELPERS for fallback builds. C retains only narrow primitive and
  state bridges for stat/path/syscall/thread boundaries. The equivalence
  harness checks overlay list mutation, mapped proc-task parent probes,
  getdents64 versus getdents classification, NUMA node bit publication, and the
  main-loop thread-entry wait/timeout path, while the symbol gates check that
  the retired strict C bridge/wrapper symbols no longer remain in the strict C
  object. Validation passed for rustfmt, shell syntax, scoped whitespace
  checks, full kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build,
  user-tool C fallback mcexec build with ENABLE_RUST_USER_TOOLS=OFF, Rust
  helper export checks, strict C absence checks for the retired helper symbols,
  fallback undefined-symbol checks, no panic_bounds_check import, and the
  strict source-retirement audit. The audit reports compiled_retired_c 0, stale
  tracker rows [], untracked files [], User tools 89.37931034482759% complete,
  overall scored source-retirement completion 92.50746268656717%, 21 debt rows,
  and 7.492537313432834 percentage points left. Two earlier equivalence
  failures in this batch were recorded in kernel.log and fixed before the
  passing gates: a Rust bounds-check panic import in the mapped proc-task helper
  and a generated smoke assertion that still used the fake syscall number
  instead of the real x86_64 getdents64 number. This remains partial user-tool
  row movement; the row still has fallback C and other mcexec
  source-retirement debt. No guest runtime validation, QEMU boot, host reboot,
  or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 59.0% to
  64.0% for the strict source-retirement audit. Rust-enabled mcexec now removes
  a batched set of strict C wrapper bodies and one remaining execveat path
  helper body. The strict build routes getpath_execveat sequencing through the
  new Rust mcexec_getpath_execveat_body() export, including path construction,
  AT_EMPTY_PATH handling, buffer-length rejection, AT_SYMLINK_NOFOLLOW
  readlink/ELOOP behavior, and return shaping. The strict build also replaces
  already Rust-owned wrappers for NUMA nodemask helpers, get_thp_disable(),
  opendev(), main_loop_thread_func(), create_worker_thread(),
  join_all_threads(), do_generic_syscall(), and kill_thread() with macro aliases
  to their Rust exports. The original C implementations remain available for
  !MCEXEC_RUST_HELPERS fallback builds. C retains only narrow bridge bodies for
  libc/syscall/device/thread primitives and diagnostics. The equivalence harness
  checks the new getpath_execveat body for absolute paths, /dev/fd construction,
  empty-path construction, too-small buffers, and symlink-loop rejection, and it
  retains coverage for the already Rust-owned wrapper targets. Validation passed
  for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build, user-tool C
  fallback mcexec build with ENABLE_RUST_USER_TOOLS=OFF, symbol-split checks
  showing the Rust body exports, absence of strict C definitions for the retired
  wrapper symbols, strict C imports for the Rust bodies that still have C call
  sites, no panic_bounds_check import, and fallback undefined-symbol checks. The
  first getpath smoke compile failed because the fixture lacked AT_EMPTY_PATH
  and AT_SYMLINK_NOFOLLOW definitions; a later batched build failed because the
  opendev alias rewrite left an MCEXEC_RUST_HELPERS block unterminated. Both
  failures are recorded in kernel.log and fixed before the passing gates. The
  audit reports compiled_retired_c 0, stale tracker rows [], untracked files [],
  User tools 89.27586206896552% complete, overall scored source-retirement
  completion 92.49626865671642%, 21 debt rows, and
  7.503731343283576 percentage points left. This remains partial user-tool row
  movement; the row still has fallback C and other mcexec source-retirement
  debt. No guest runtime validation, QEMU boot, host reboot, or reboot-capable
  script was run.

- 2026-06-13: executer/user/mcexec.c moved from 58.0% to
  59.0% for the strict source-retirement audit. Rust-enabled mcexec now removes
  the strict C env-list wrapper bodies for get/search/add/destroy/create-local
  environment helpers. The strict build routes those helper calls directly to
  Rust exports or to mcexec_add_env_list_body(), while the original C list
  traversal, duplicate check, allocation, copy, and free bodies remain available
  for !MCEXEC_RUST_HELPERS fallback builds. Rust owns env-list count/search,
  add validation, duplicate detection, allocation/linking, invalid-env
  diagnostic dispatch, list destroy, local environment copy, and local
  environment destroy sequencing. C retains only a narrow invalid-env diagnostic
  bridge in strict builds. The equivalence harness checks count/search,
  invalid input diagnostics, duplicate suppression, successful allocation and
  linking, local environment copying, cleanup, and the Rust symbol gate.
  Validation passed for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build, user-tool C
  fallback mcexec build with ENABLE_RUST_USER_TOOLS=OFF, symbol-split checks
  showing the Rust env-list exports, strict C undefined env-list helper imports,
  absence of strict C env-list wrapper definitions, no panic_bounds_check
  import, and fallback undefined-symbol check. The audit reports
  compiled_retired_c 0, stale tracker rows [], untracked files [], User tools
  89.10344827586206% complete, overall scored source-retirement completion
  92.4776119402985%, 21 debt rows, and 7.522388059701498 percentage points left.
  This remains partial user-tool row movement; the row still has fallback C and
  other mcexec source-retirement debt. No guest runtime validation, QEMU boot,
  host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 52.0% to
  53.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports mcexec_join_all_threads_body(); the strict C join_all_threads helper
  is reduced to a Rust wrapper while the original thread-list traversal and
  join loop remains available for !MCEXEC_RUST_HELPERS fallback builds. Rust
  owns repeated thread-list head reloads, joined/detached skip decisions,
  live-thread loop continuation, pthread-join dispatch, and joined-state
  publication. C retains narrow bridges for the current thread_data head and
  the actual pthread_join call. The equivalence harness checks skip behavior,
  joined-state publication, head reload behavior for an inserted thread, join
  ordering, and the Rust symbol gate. Validation passed for rustfmt, shell
  syntax, scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh,
  strict Rust mcexec build, user-tool C fallback mcexec build with
  ENABLE_RUST_USER_TOOLS=OFF, symbol-split checks showing the Rust
  mcexec_join_all_threads_body definition, strict C undefined
  mcexec_join_all_threads_body import, strict C join-all bridge definitions, no
  panic_bounds_check import, and fallback undefined-symbol check. The audit
  reports compiled_retired_c 0, stale tracker rows [], untracked files [], User
  tools 88.89655172413794% complete, overall scored source-retirement
  completion 92.45522388059702%, 21 debt rows, and 7.544776119402982
  percentage points left. This remains partial user-tool row movement; the row
  still has fallback C and other mcexec source-retirement debt. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 51.0% to
  52.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports mcexec_create_worker_thread_body(); the strict C create_worker_thread
  helper is reduced to a Rust wrapper while the original C allocation,
  initialization, list-publication, optional output publication, and
  pthread_create sequence remains available for !MCEXEC_RUST_HELPERS fallback
  builds. Rust owns worker-record allocation dispatch, allocation-failure
  ENOMEM shaping, zero initialization, CPU id publication, lock/init-barrier
  publication, thread-list/output publication dispatch, pthread-create dispatch,
  and pthread-create return propagation. C retains narrow bridges for the
  malloc/free boundary, max_cpuid mutation, lock address, thread-list mutation,
  and pthread_create function-pointer call. The equivalence harness checks
  successful field publication and ordering, allocation failure without
  publication, pthread-create failure after publication, and the Rust symbol
  gate. Validation passed for rustfmt, shell syntax, scoped whitespace checks,
  full kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build,
  user-tool C fallback mcexec build with ENABLE_RUST_USER_TOOLS=OFF,
  symbol-split checks showing the Rust mcexec_create_worker_thread_body
  definition, strict C undefined mcexec_create_worker_thread_body import,
  strict C create-worker bridge definitions, no panic_bounds_check import, and
  fallback undefined-symbol check. The audit reports compiled_retired_c 0,
  stale tracker rows [], untracked files [], User tools 88.86206896551724%
  complete, overall scored source-retirement completion 92.45149253731343%,
  21 debt rows, and 7.5485074626865725 percentage points left. This remains
  partial user-tool row movement; the row still has fallback C and other
  mcexec source-retirement debt. No guest runtime validation, QEMU boot, host
  reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 50.0% to
  51.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports mcexec_main_loop_thread_func_body(); the strict C pthread entry for
  main-loop worker threads is reduced to a Rust wrapper while the original C
  body remains available for !MCEXEC_RUST_HELPERS fallback builds. Rust owns
  worker TID publication, remote_tid initialization, optional init barrier
  wait dispatch, main_loop() dispatch, return-code publication into the thread
  record, and NULL pthread return shaping. C retains the pthread function
  pointer wrapper and a narrow pthread_barrier_wait bridge. The equivalence
  harness checks barrier and no-barrier paths, fake gettid publication,
  remote_tid reset, main_loop dispatch argument/return propagation, and the
  Rust symbol gate. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh after fixing an
  isolated fixture mutation, strict Rust mcexec build, user-tool C fallback
  mcexec build with ENABLE_RUST_USER_TOOLS=OFF, symbol-split checks showing
  the Rust mcexec_main_loop_thread_func_body definition, strict C undefined
  mcexec_main_loop_thread_func_body import, strict C barrier bridge definition,
  no panic_bounds_check import, and fallback undefined-symbol check. The audit
  reports compiled_retired_c 0, stale tracker rows [], untracked files [],
  User tools 88.82758620689656% complete, overall scored source-retirement
  completion 92.44776119402985%, 21 debt rows, and 7.552238805970148
  percentage points left. This remains partial user-tool row movement; the row
  still has fallback C and other mcexec source-retirement debt. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 49.0% to
  50.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_main_loop_body(); the strict C main_loop symbol is reduced to a
  Rust wrapper while the original C wait-loop body remains available for
  !MCEXEC_RUST_HELPERS fallback builds. Rust owns wait descriptor
  initialization, PID/CPU publication before the wait ioctl, MCEXEC_UP_WAIT_SYSCALL
  dispatch, EINTR retry policy, successful-wait iteration dispatch,
  post-iteration normal remote_tid cleanup, nonlocal return propagation, and
  timeout log/return selection. C retains the fallback body and narrow
  diagnostic bridges. The equivalence harness checks EINTR retry into a normal
  syscall and terminal timeout, initial wait descriptor CPU/PID values, normal
  remote bookkeeping cleanup, exit nonlocal return preservation, timeout
  bridge routing, and the Rust symbol gate. Validation passed for rustfmt,
  shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build, user-tool C
  fallback mcexec build with ENABLE_RUST_USER_TOOLS=OFF, symbol-split checks
  showing the Rust act_main_loop_body definition, strict C undefined
  act_main_loop_body import, strict C main-loop diagnostic bridge definitions,
  no panic_bounds_check import, and fallback undefined-symbol check. The audit
  reports compiled_retired_c 0, stale tracker rows [], untracked files [],
  User tools 88.79310344827586% complete, overall scored source-retirement
  completion 92.44402985074628%, 21 debt rows, and 7.555970149253724
  percentage points left. This remains partial user-tool row movement; the row
  still has fallback C and other mcexec source-retirement debt. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 48.0% to
  49.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_main_loop_iteration(); the strict C main_loop body calls that
  Rust-owned per-wait iteration wrapper after MCEXEC_UP_WAIT_SYSCALL succeeds
  while the original per-iteration logging, remote-thread publication, syscall
  dispatch, and cleanup sequence remains available for !MCEXEC_RUST_HELPERS
  fallback builds. Rust owns the syscall log/suppression decision, remote_tid
  and remote_cpu publication, act_main_loop_syscall() dispatch, normal
  remote_tid cleanup, and nonlocal return preservation for exit/clone/execve
  paths. C retains the ioctl wait loop, EINTR retry, timeout/error envelope,
  and narrow diagnostic bridge. The equivalence harness checks normal write
  suppression with generic syscall dispatch, remote CPU/TID publication and
  cleanup, exit nonlocal return preservation, log bridge routing, and the Rust
  symbol gate. Validation passed for rustfmt, shell syntax, scoped whitespace
  checks, full kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build,
  user-tool C fallback mcexec build with ENABLE_RUST_USER_TOOLS=OFF,
  symbol-split checks showing the Rust act_main_loop_iteration definition,
  strict C undefined act_main_loop_iteration import, strict C diagnostic bridge
  definition, no panic_bounds_check import, and fallback undefined-symbol
  check. The audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 88.75862068965517% complete, overall scored
  source-retirement completion 92.44029850746269%, 21 debt rows, and
  7.5597014925373145 percentage points left. This remains partial user-tool
  row movement; the row still has the outer wait-loop C envelope in the strict
  path. No guest runtime validation, QEMU boot, host reboot, or reboot-capable
  script was run.

- 2026-06-13: executer/user/mcexec.c moved from 47.0% to
  48.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_main_loop_syscall(); the strict C main_loop syscall switch now
  calls that Rust-owned dispatcher while the original C switch remains
  available for !MCEXEC_RUST_HELPERS fallback builds. Rust owns syscall-number
  classification across the strict mcexec main-loop cases, action-wrapper
  selection, exit nonlocal return publication, clone child/nonlocal return
  propagation, execve phase nonlocal return propagation, no-QLMPI swapout
  unavailable dispatch, custom debug/linux_spawn dispatch, open/default tail
  dispatch, and default generic-syscall selection. C retains the outer wait
  ioctl loop, remote thread bookkeeping, and narrow side-effect bridges. The
  equivalence harness checks dispatcher routing for an openat path syscall,
  exit nonlocal return behavior, and default generic syscall forwarding, plus
  the Rust symbol gate. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh, strict Rust
  mcexec build, user-tool C fallback mcexec build with
  ENABLE_RUST_USER_TOOLS=OFF, symbol-split checks showing the Rust
  act_main_loop_syscall definition, strict C undefined act_main_loop_syscall
  import, no panic_bounds_check import, and fallback undefined-symbol check. The
  audit reports compiled_retired_c 0, stale tracker rows [], untracked files
  [], User tools 88.72413793103448% complete, overall scored
  source-retirement completion 92.43656716417911%, 21 debt rows, and
  7.56343283582089 percentage points left. This remains partial user-tool row
  movement; the row still has outer main-loop C orchestration in the strict
  path. No guest runtime validation, QEMU boot, host reboot, or reboot-capable
  script was run.

- 2026-06-13: executer/user/mcexec.c moved from 43.0% to
  44.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_execve_phase2(); the strict C main_loop execve phase-2 copy
  image path now calls that Rust-owned action wrapper while the original C
  allocation, remote descriptor transfer, image transfer, close-exec,
  CLOEXEC-close, nonlocal return, and syscall-return sequence remains
  available for !MCEXEC_RUST_HELPERS fallback builds. Rust owns descriptor
  allocation/zeroing, MCEXEC_UP_TRANSFER_FROM_REMOTE descriptor copy-in
  dispatch, transfer_image dispatch/error-return shaping, MCEXEC_UP_CLOSE_EXEC
  dispatch/error-return shaping, close_cloexec_fds dispatch, EINVAL return
  publication, success return publication, and the main-loop -1/1 nonlocal
  return selection. C retains narrow diagnostic bridges and the existing
  primitive side effects in transfer_image, close_cloexec_fds, and ioctl. The
  equivalence harness checks successful descriptor transfer through a
  zero-section descriptor, descriptor-transfer failure return publication,
  transfer_image short-file failure preserving main-loop -1, close-exec
  failure preserving main-loop 1, CLOEXEC scanning, close-exec dispatch, and
  the Rust symbol gate. An initial equivalence smoke failure was recorded in
  kernel.log when the fixture incorrectly expected DMA ioctl failure to make
  transfer_image return -1; after correcting the fixture to use a short input
  file, validation passed for rustfmt, shell syntax, scoped whitespace checks,
  full kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build,
  symbol-split checks showing the Rust act_execve_phase2 definition, strict C
  undefined act_execve_phase2 import, no panic_bounds_check import, user-tool
  C fallback mcexec build with ENABLE_RUST_USER_TOOLS=OFF, fallback
  undefined-symbol check, and the source-retirement audit against
  /tmp/mckernel-rocky-rust. The audit reports compiled_retired_c 0, stale
  tracker rows [], untracked files [], User tools 88.58620689655173% complete,
  overall scored source-retirement completion 92.42164179104478%, 21 debt
  rows, and 7.578358208955223 percentage points left. This remains partial
  user-tool row movement; the row still has fork-start, execve phase-1, and
  main-loop C orchestration in the strict path. No guest runtime validation,
  QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 42.0% to
  43.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_exit(); the strict C main_loop exit and exit_group paths now
  call that Rust-owned action wrapper while the original status decode,
  reporting, command-server cleanup, signal replay, and exit sequence remains
  available for !MCEXEC_RUST_HELPERS fallback builds. Rust owns exit status
  extraction, exit-group status planning, tty query dispatch, signal/status
  report selection, command-server cleanup dispatch, signal-replay decision,
  and final exit dispatch through narrow C side-effect bridges. C retains the
  actual fprintf/__dprintf text, optional command-server exits, signal/kill/
  pause primitives, and process exit primitive. The equivalence harness checks
  signal-terminated exit_group reporting/replay, nonzero-status exit_group
  reporting, plain exit no-decode behavior, command-cleanup dispatch, final
  exit dispatch, and the Rust symbol gate. Validation passed for rustfmt,
  shell syntax, scoped whitespace checks, full kernel/rust/tests/
  run_equivalence.sh, strict Rust mcexec build, symbol-split checks showing
  the Rust act_exit definition, strict C undefined act_exit import, no
  panic_bounds_check import, user-tool C fallback mcexec build with
  ENABLE_RUST_USER_TOOLS=OFF, fallback undefined-symbol check, and the
  source-retirement audit against /tmp/mckernel-rocky-rust. The audit reports
  compiled_retired_c 0, stale tracker rows [], untracked files [], User tools
  88.55172413793103% complete, overall scored source-retirement completion
  92.41791044776119%, 21 debt rows, and 7.582089552238813 percentage points
  left. This remains partial user-tool row movement; the row still has
  substantial fork-start, execve, and main-loop C orchestration in the strict
  path. No guest runtime validation, QEMU boot, host reboot, or reboot-capable
  script was run.

- 2026-06-13: executer/user/mcexec.c moved from 41.0% to
  42.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_clone_complete(); the strict C main_loop clone-completion path
  now calls that Rust-owned action wrapper while the original fork-sync
  completion, cleanup, and return-publication sequence remains available for
  !MCEXEC_RUST_HELPERS fallback builds. Rust owns clone-completion pid
  extraction, fork-sync top publication, lock/unlock sequencing through narrow
  bridges, mcexec_fork_sync_complete_result dispatch, matched-node cleanup
  selection, munmap/free bridge dispatch, and syscall-return publication. C
  retains the pthread mutex, munmap, and free primitive bridges. The
  equivalence harness checks matched-pid cleanup/list relinking, unmatched-pid
  no-cleanup behavior, lock/unlock ordering, return publication, and the Rust
  symbol gate. Validation passed for rustfmt, shell syntax, scoped whitespace
  checks, full kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build,
  symbol-split checks showing the Rust act_clone_complete definition, strict C
  undefined act_clone_complete import, no panic_bounds_check import, user-tool
  C fallback mcexec build with ENABLE_RUST_USER_TOOLS=OFF, fallback
  undefined-symbol check, and the source-retirement audit against
  /tmp/mckernel-rocky-rust. The audit reports compiled_retired_c 0, stale
  tracker rows [], untracked files [], User tools 88.51724137931035% complete,
  overall scored source-retirement completion 92.41417910447761%, 21 debt
  rows, and 7.585820895522389 percentage points left. This remains partial
  user-tool row movement; the row still has substantial syscall and main-loop
  C orchestration in the strict path. No guest runtime validation, QEMU boot,
  host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 40.0% to
  41.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_linux_spawn(); the strict C main_loop custom linux_spawn syscall
  811 path now calls that Rust-owned action wrapper while the original
  validation, allocation, copy, posix_spawn, cleanup, and return-publication
  sequence remains available for !MCEXEC_RUST_HELPERS fallback builds. Rust
  owns exec-path and argv pointer validation, path-length validation,
  temporary allocation/zeroing, remote string-copy dispatch, bounded argv
  traversal, posix_spawn dispatch, diagnostic callback selection, allocation
  cleanup, raw 0/-1 return shaping, and syscall-return publication. C retains
  narrow bridges for the existing diagnostic text while the actual posix_spawn
  side effect remains a libc primitive. The equivalence harness checks
  successful exec/argv copy routing into posix_spawn, null envp publication,
  posix_spawn failure diagnostic routing, invalid-argument routing, return
  publication, and the Rust symbol gate. Validation passed for rustfmt, shell
  syntax, scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh,
  strict Rust mcexec build, symbol-split checks showing the Rust
  act_linux_spawn definition, strict C undefined act_linux_spawn import, no
  panic_bounds_check import, user-tool C fallback mcexec build with
  ENABLE_RUST_USER_TOOLS=OFF, and fallback undefined-symbol check. The audit
  reports compiled_retired_c 0, stale tracker rows [], untracked files [], User
  tools 88.48275862068965% complete, overall scored source-retirement
  completion 92.41044776119404%, 21 debt rows, and 7.589552238805965
  percentage points left. This remains partial user-tool row movement; the row
  still has substantial syscall and main-loop C orchestration in the strict
  path. No guest runtime validation, QEMU boot, host reboot, or reboot-capable
  script was run.

- 2026-06-13: executer/user/mcexec.c moved from 39.0% to
  40.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_swapout_unavailable(); the strict C main_loop custom swapout
  syscall 801 path now calls that Rust-owned action wrapper when ENABLE_QLMPI
  is disabled, while the full ENABLE_QLMPI implementation and the original
  !MCEXEC_RUST_HELPERS unavailable-path C fallback remain available. Rust owns
  no-QLMPI unavailable-log callback dispatch, raw -1 return shaping, and
  syscall-return publication for the compiled strict Rocky user-tool path. C
  retains a narrow bridge for the existing unavailable diagnostic text. The
  equivalence harness checks unavailable-log dispatch, raw -1 return
  publication, and the Rust symbol gate. Validation passed for rustfmt, shell
  syntax, scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh,
  strict Rust mcexec build, symbol-split checks showing the Rust
  act_swapout_unavailable definition, strict C undefined
  act_swapout_unavailable import, no panic_bounds_check import, user-tool C
  fallback mcexec build with ENABLE_RUST_USER_TOOLS=OFF, and fallback
  undefined-symbol check. The audit reports compiled_retired_c 0, stale
  tracker rows [], untracked files [], User tools 88.44827586206897% complete,
  overall scored source-retirement completion 92.40671641791045%, 21 debt
  rows, and 7.593283582089555 percentage points left. This remains partial
  user-tool row movement; the row still has substantial syscall and main-loop C
  orchestration in the strict path. No guest runtime validation, QEMU boot,
  host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 38.0% to
  39.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_debug_mlock(); the strict C main_loop custom debugging syscall
  802 case now calls that Rust-owned action wrapper while the original printf,
  mlock, and syscall-return sequence remains available for
  !MCEXEC_RUST_HELPERS fallback builds. Rust owns debug syscall argument
  extraction, existing debug-log callback dispatch, mlock callback dispatch,
  raw return preservation, and syscall-return publication. C retains narrow
  bridges for the host mlock primitive and the existing debug print text. The
  equivalence harness checks log-argument routing, mlock-argument routing,
  returned-value publication, and the Rust symbol gate. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build,
  symbol-split checks showing the Rust act_debug_mlock definition, strict C
  undefined act_debug_mlock import, no panic_bounds_check import, user-tool C
  fallback mcexec build with ENABLE_RUST_USER_TOOLS=OFF, and fallback
  undefined-symbol check. The audit reports compiled_retired_c 0, stale
  tracker rows [], untracked files [], User tools 88.41379310344827% complete,
  overall scored source-retirement completion 92.40298507462687%, 21 debt
  rows, and 7.597014925373131 percentage points left. This remains partial
  user-tool row movement; the row still has substantial syscall and main-loop C
  orchestration in the strict path. No guest runtime validation, QEMU boot,
  host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 37.0% to
  38.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_generic_syscall(); the strict C main_loop default case now calls
  that Rust-owned action wrapper while the original do_generic_syscall()
  dispatch plus return publication remains available for !MCEXEC_RUST_HELPERS
  fallback builds. Rust owns default syscall forwarding dispatch through the
  existing generic syscall bridge and syscall-return publication for otherwise
  unhandled syscall numbers. C retains the narrow bridge for the actual generic
  Linux syscall side effect. The equivalence harness checks descriptor number
  and argument routing through the bridge, returned-value publication, and the
  Rust symbol gate. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh, strict Rust
  mcexec build, symbol-split checks showing the Rust act_generic_syscall
  definition, strict C undefined act_generic_syscall import, strict C generic
  syscall bridge definition, no panic_bounds_check import, C fallback mcexec
  build, fallback undefined-symbol check, and the strict source-retirement
  audit. The audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 88.37931034482759% complete, overall scored
  source-retirement completion 92.39925373134328%, 21 debt rows, and
  7.600746268656721 percentage points left. This remains partial user-tool row
  movement; the row still has substantial syscall and main-loop C orchestration
  in the strict path. No guest runtime validation, QEMU boot, host reboot, or
  reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 36.0% to
  37.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_gettid(); the strict C main_loop gettid case now calls that
  Rust-owned action wrapper while the original allocation/transfer/return
  sequencing remains available for !MCEXEC_RUST_HELPERS fallback builds. Rust
  owns requested TID-count extraction, bounded allocation sizing, active-thread
  TID collection dispatch, remote-transfer descriptor construction, transfer
  ioctl dispatch, allocation/transfer error shaping, temporary buffer free, and
  syscall-return publication. C retains narrow bridges for the existing
  allocation and transfer diagnostics. The equivalence harness checks active
  TID payload publication to the remote-transfer buffer, zero-count no-transfer
  behavior, transfer-failure error routing, and the Rust symbol gate.
  Validation passed for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build,
  symbol-split checks showing the Rust act_gettid definition, strict C
  undefined act_gettid import, strict C gettid diagnostic bridge definitions,
  no panic_bounds_check import, C fallback mcexec build, and fallback
  undefined-symbol check, and the strict source-retirement audit. The audit
  reports compiled_retired_c 0, stale tracker rows [], untracked files [], User
  tools 88.34482758620689% complete, overall scored source-retirement
  completion 92.3955223880597%, 21 debt rows, and 7.604477611940297 percentage
  points left. This remains partial user-tool row movement; the row still has
  substantial syscall and main-loop C orchestration in the strict path. No
  guest runtime validation, QEMU boot, host reboot, or reboot-capable script
  was run.

- 2026-06-13: executer/user/mcexec.c moved from 35.0% to
  36.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_wait4(); the strict C main_loop wait4 case now calls that
  Rust-owned action wrapper while the original waitid loop and return
  publication remain available for !MCEXEC_RUST_HELPERS fallback builds. Rust
  owns wait4 pid/options extraction, WEXITED/WNOWAIT option shaping, host wait
  bridge dispatch, mismatch/error-log decision routing, and syscall-return
  publication. C retains narrow bridges for the actual waitid side effect and
  existing diagnostic text. The equivalence harness checks successful wait4
  return publication with WNOWAIT propagation, mismatch/error bridge routing,
  errno propagation, and the Rust symbol gate. Validation passed for rustfmt,
  shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build,
  symbol-split checks showing the Rust act_wait4 definition, strict C undefined
  act_wait4 import, strict C wait bridge definitions, no panic_bounds_check
  import, C fallback mcexec build, fallback undefined-symbol check, and the
  strict source-retirement audit. The audit reports compiled_retired_c 0, stale
  tracker rows [], untracked files [], User tools 88.3103448275862% complete,
  overall scored source-retirement completion 92.39179104477611%, 21 debt rows,
  and 7.608208955223887 percentage points left. This remains partial user-tool
  row movement; the row still has substantial syscall and main-loop C
  orchestration in the strict path. No guest runtime validation, QEMU boot,
  host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 34.0% to
  35.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_signalfd4_syscall(), act_rt_sigaction(), and
  act_rt_sigprocmask(); the strict C main_loop signalfd4, rt_sigaction, and
  rt_sigprocmask cases now call those Rust-owned action wrappers while the
  original C action plus syscall-return sequencing remains available for
  !MCEXEC_RUST_HELPERS fallback builds. Rust owns signalfd4 action dispatch and
  return publication, rt_sigaction action dispatch and zero-return publication,
  and rt_sigprocmask action dispatch and zero-return publication. No new C
  side-effect bridge was needed because the wrappers compose the existing Rust
  action bodies with the existing syscall-return ABI. The equivalence harness
  checks signalfd4 EBADF return publication, rt_sigaction dispatch count and
  return publication, rt_sigprocmask mask routing and return publication, and
  the Rust symbol gate. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, full kernel/rust/tests/run_equivalence.sh, strict Rust
  mcexec build, symbol-split checks showing Rust definitions and strict C
  undefined imports for the new wrappers, no panic_bounds_check import, C
  fallback mcexec build, fallback undefined-symbol check, and the strict
  source-retirement audit. The audit reports compiled_retired_c 0, stale
  tracker rows [], untracked files [], User tools 88.27586206896552% complete,
  overall scored source-retirement completion 92.38805970149254%, 21 debt rows,
  and 7.611940298507463 percentage points left. This remains partial user-tool
  row movement; the row still has substantial syscall and main-loop C
  orchestration in the strict path. No guest runtime validation, QEMU boot,
  host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 33.0% to
  34.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_futex_clock(), act_kill(), and act_reserved_memory_syscall(); the
  strict C main_loop futex clock shim, interrupt kill handoff, and reserved
  mmap/munmap/mprotect cases now call those Rust-owned action bodies while the
  original C bodies remain available for !MCEXEC_RUST_HELPERS fallback builds.
  Rust owns clockid argument routing, clock_gettime bridge result handling,
  timespec return-copy descriptor construction, clock debug-log dispatch, kill
  handoff argument routing, kill return publication, and reserved-memory
  syscall -ENOSYS return publication. C retains narrow bridges for the actual
  clock_gettime side effect/logging and existing kill_thread side effect. The
  equivalence harness checks clock success and error return publication,
  returned timespec destination/size, kill bridge argument routing, reserved
  syscall -ENOSYS shaping, and the Rust symbol gate. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build after removing
  the Rust-enabled fallback-only tv warning, symbol-split checks showing Rust
  definitions and strict C undefined imports for the new act_* functions plus
  strict C bridge definitions, no panic_bounds_check import, C fallback mcexec
  build, fallback undefined-symbol check, and the strict source-retirement
  audit. The audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 88.24137931034483% complete, overall scored
  source-retirement completion 92.38432835820896%, 21 debt rows, and
  7.615671641791039 percentage points left. This remains partial user-tool row
  movement; the row still has substantial syscall and main-loop C orchestration
  in the strict path. No guest runtime validation, QEMU boot, host reboot, or
  reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 32.0% to
  33.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_getdents() and act_perf_event_open(); the strict C main_loop
  getdents/getdents64 and perf_event_open cases now call those Rust-owned action
  bodies while the original C bodies remain available for !MCEXEC_RUST_HELPERS
  fallback builds. Rust owns getdents syscall-number/fd/buffer/count routing
  through the Rust overlay_getdents() body, getdents return publication,
  perf_event_open side-effect bridge dispatch, and perf_event_open return
  publication while preserving the original raw open("/dev/null") return shape.
  C retains only the narrow /dev/null open bridge for perf_event_open. The
  equivalence harness checks getdents passthrough return publication,
  perf_event_open success and raw -1 forwarding, and the Rust symbol gate.
  Validation passed for rustfmt, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build, symbol-split
  checks showing Rust definitions and strict C undefined imports for
  act_getdents()/act_perf_event_open() plus the strict C bridge definition, no
  panic_bounds_check import, C fallback mcexec build, fallback undefined-symbol
  check, and the strict source-retirement audit. The audit reports
  compiled_retired_c 0, stale tracker rows [], untracked files [], User tools
  88.20689655172414% complete, overall scored source-retirement completion
  92.38059701492537%, 21 debt rows, and 7.619402985074629 percentage points
  left. This remains partial user-tool row movement; the row still has
  substantial syscall and main-loop C orchestration in the strict path. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 31.0% to
  32.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_close() and act_sched_setaffinity(); the strict C main_loop close
  and sched_setaffinity cases now call those Rust-owned action bodies while the
  original C bodies remain available for !MCEXEC_RUST_HELPERS fallback builds.
  Rust owns mcos-fd close rejection, generic close syscall dispatch selection,
  overlay fd deletion ordering, close return publication, sched_setaffinity pid
  validation, UTI handoff argument routing, invalid-argument diagnostic
  dispatch, and sched_setaffinity return publication. C retains narrow bridges
  for the actual generic Linux syscall, util_thread context switch, and existing
  diagnostic side effect. The equivalence harness stubs those bridges and
  checks successful close forwarding, mcos-fd close rejection, overlay-delete
  ordering, sched_setaffinity UTI argument routing, invalid-pid logging, return
  publication, and the Rust symbol gate. Validation passed for rustfmt, shell
  syntax, scoped whitespace checks, full kernel/rust/tests/run_equivalence.sh,
  strict Rust mcexec build, symbol-split checks showing Rust definitions and
  strict C undefined imports for act_close()/act_sched_setaffinity() plus
  strict C definitions of the narrow bridge functions, no panic_bounds_check
  import, C fallback mcexec build, fallback undefined-symbol check, and the
  strict source-retirement audit. The audit reports compiled_retired_c 0, stale
  tracker rows [], untracked files [], User tools 88.17241379310344% complete,
  overall scored source-retirement completion 92.3768656716418%, 21 debt rows,
  and 7.623134328358205 percentage points left. This remains partial user-tool
  row movement; the row still has substantial syscall and main-loop C
  orchestration in the strict path. No guest runtime validation, QEMU boot, host
  reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 30.0% to
  31.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_setfsuid(), act_setresuid(), act_setreuid(), act_setuid(),
  act_setresgid(), act_setregid(), act_setgid(), and act_setfsgid(); the
  strict C main_loop credential setter cases now call those Rust-owned action
  bodies while the original C bodies remain available for
  !MCEXEC_RUST_HELPERS fallback builds. Rust owns the setfsuid
  credential-refresh versus setfsuid dispatch decision, credential ioctl
  request routing, setresuid/setreuid/setuid/setresgid/setregid/setgid/setfsgid
  bridge selection, syscall-return publication, and per-action argument
  routing. C retains narrow libc credential side-effect bridges plus the
  MCEXEC_UP_GET_CRED ioctl bridge. The equivalence harness stubs those bridges
  and checks the setfsuid refresh branch, normal setfsuid return publication,
  uid/gid argument routing, fsgid/fsuid no-SET_ERR return behavior, and the
  Rust symbol gate. Validation passed for rustfmt after the recorded formatting
  fix, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build,
  symbol-split checks showing Rust definitions and strict C undefined imports
  for the act_set* functions plus strict C definitions of the narrow bridge
  functions, no panic_bounds_check import, C fallback mcexec build, fallback
  undefined-symbol check, and the strict source-retirement audit. The audit
  reports compiled_retired_c 0, stale tracker rows [], untracked files [],
  User tools 88.13793103448276% complete, overall scored source-retirement
  completion 92.3731343283582%, 21 debt rows, and
  7.626865671641795 percentage points left. This remains partial user-tool row
  movement; the row still has substantial syscall and main-loop C orchestration
  in the strict path. No guest runtime validation, QEMU boot, host reboot, or
  reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 29.0% to
  30.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_openat() and act_open(); the strict C main_loop open/openat
  cases now call those Rust-owned action bodies while the original C bodies
  remain available for !MCEXEC_RUST_HELPERS fallback builds. Rust owns remote
  path copy-in result shaping, PATH_MAX/ENAMETOOLONG rejection, local
  path-buffer termination, open/openat diagnostic dispatch, overlay_path
  dispatch, Linux open/openat bridge selection, overlay fd publication
  decision when the mapped path comes from the overlay buffer, syscall return
  publication, and copy-failure early return sequencing. C retains narrow
  libc open/openat side-effect bridges and existing diagnostics. The
  equivalence harness stubs MCEXEC_UP_STRNCPY_FROM_USER, overlay resolution,
  open/openat bridges, and overlay fd publication, and checks normal open
  routing, openat flags/mode/tid routing, remapped /proc openat publication,
  returned value publication, and the Rust symbol gate. Validation passed for
  rustfmt after the recorded formatting fix, shell syntax, scoped whitespace
  checks, full kernel/rust/tests/run_equivalence.sh, strict Rust mcexec build,
  symbol-split checks showing Rust definitions and strict C undefined imports
  for act_openat()/act_open() plus strict C definitions of the narrow bridge
  functions, C fallback mcexec build, fallback undefined-symbol check, and the
  strict source-retirement audit. The audit reports compiled_retired_c 0,
  stale tracker rows [], untracked files [], User tools
  88.10344827586206% complete, overall scored source-retirement completion
  92.36940298507463%, 21 debt rows, and 7.630597014925371 percentage points
  left. This remains partial user-tool row movement; the row still has
  substantial syscall and main-loop C orchestration in the strict path. No
  guest runtime validation, QEMU boot, host reboot, or reboot-capable script
  was run.

- 2026-06-13: executer/user/mcexec.c moved from 28.0% to
  29.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_readlinkat(), act_readlink(), act_newfstatat(), act_stat(),
  act_faccessat(), act_access(), act_getxattr(), and act_lgetxattr(); the
  strict C main_loop path-syscall cases now call those Rust-owned action
  bodies while the original C case bodies remain available for
  !MCEXEC_RUST_HELPERS fallback builds. Rust owns remote path copy-in result
  shaping, PATH_MAX/ENAMETOOLONG rejection, local path-buffer termination,
  overlay_path dispatch including faccessat resolvelinks flag handling,
  readlink/readlinkat/stat/fstatat/access/faccessat/getxattr/lgetxattr
  bridge selection, syscall return publication, and copy-failure early return
  sequencing. C retains narrow libc/syscall bridges for the actual Linux
  readlink/stat/access/xattr side effects plus existing diagnostics. The
  equivalence harness stubs MCEXEC_UP_STRNCPY_FROM_USER, overlay resolution,
  and the path syscall bridges, and checks each Rust action's bridge
  selection, argument routing, returned value publication, xattr-name routing,
  and ENAMETOOLONG short-circuit behavior. Validation passed for rustfmt after
  the recorded formatting fix, shell syntax, scoped whitespace checks, full
  kernel/rust/tests/run_equivalence.sh after the recorded fixture-state
  correction, strict Rust mcexec build, symbol-split checks showing Rust
  definitions and strict C undefined imports for the new act_* functions plus
  strict C definitions of the narrow bridge functions, C fallback mcexec
  build, fallback undefined-symbol check, and the strict source-retirement
  audit. The audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 88.06896551724138% complete, overall scored
  source-retirement completion 92.36567164179104%, 21 debt rows, and
  7.634328358208961 percentage points left. This remains partial user-tool row
  movement; the row still has substantial syscall and main-loop C
  orchestration in the strict path. No guest runtime validation, QEMU boot,
  host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 27.0% to
  28.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports overlay_getdents(); the strict C object no longer defines that
  public directory overlay body, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns overlay-cache lookup decision routing,
  proc-task hide-original passthrough selection, passthrough syscall return
  shaping, temporary dirent buffer allocation/free, seek offset capture,
  upper getdents retry loop, lower getdents retry loop, lower-entry
  blacklist path construction, lower-entry blacklist filtering, lower/upper
  duplicate filtering, filtered lower cache publication dispatch, cached
  upper/lower copy-out ordering, too-small result handling, combined return
  length calculation, and final fd seek advancement. C retains narrow bridges
  for overlay_fd list lookup, fd/path/cache field access, upper/lower open
  side effects, realloc-backed cache mutation, and existing diagnostics. The
  equivalence harness stubs syscall/lseek plus those bridges and checks
  no-overlay passthrough, upper/lower read retries, duplicate filtering,
  blacklist filtering, cache append/rewrite dispatch, packed copy-out order,
  d_off rewrites, final seek advancement, and the Rust symbol gate. Validation
  passed for rustfmt after the recorded cast-parentheses parser fix, shell
  syntax, scoped whitespace checks, strict Rust mcexec build, symbol-split
  checks including Rust-defined overlay_getdents and C-object undefined
  overlay_getdents, absence of panic_bounds_check imports, full
  kernel/rust/tests/run_equivalence.sh after the recorded passthrough-fixture
  correction, and a separate ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build.
  The strict source-retirement audit reports compiled_retired_c 0, stale
  tracker rows [], untracked files [], User tools 88.03448275862068%
  complete, overall scored source-retirement completion 92.36194029850746%,
  21 debt rows, and 7.638059701492537 percentage points left. This remains
  partial user-tool row movement; the row still has substantial syscall and
  main-loop C orchestration in the strict path. No guest runtime validation,
  QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 26.0% to
  27.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports overlay_path(); the strict C object no longer defines that public
  overlay path body, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns resolvelinks reset, dirfd-relative
  /proc/self/fd path construction and readlink/join sequencing, relative cwd
  path joining, /dev/xpmem to /dev/null routing, libuti overlay selection,
  /proc/self and /proc remapping, /sys symlink walk sequencing with lstat and
  readlink callback dispatch, /sys mcos remapping and mapped logical-path
  selection, normalized existence checking, proc-task parent fallback,
  blacklist-to-/nonexisting routing, and diagnostic callback ordering. C
  retains narrow filesystem and side-effect bridges for find_libdir(),
  readlink(), lstat(), stat(), mapped proc-task parent probing, and existing
  stderr/debug logging. The equivalence harness stubs those bridges and checks
  proc-self remapping, /dev/xpmem replacement, libuti replacement, proc-task
  ENOENT parent fallback, /sys blacklist routing, dirfd readlink dispatch, and
  the Rust symbol gate. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, strict Rust mcexec build, symbol-split checks including
  absence of panic_bounds_check imports, full kernel/rust/tests/run_equivalence.sh
  after the recorded fixed-buffer bounds-check linkage fix, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 88.0% complete, overall scored
  source-retirement completion 92.35820895522389%, 21 debt rows, and
  7.641791044776113 percentage points left. This remains partial user-tool row
  movement; the row still has substantial overlay_getdents, syscall, and
  main-loop C orchestration in the strict path. No guest runtime validation,
  QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 25.0% to
  26.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports load_elf(); the strict C object no longer defines that raw ELF parser
  body, while the C fallback definition remains under !MCEXEC_RUST_HELPERS.
  Rust owns ELF header read/validation, program-header two-pass iteration,
  PT_LOAD counting and publication, PT_INTERP size/read handling, PT_PHDR
  capture, PT_GNU_STACK protection derivation, first-load address fallback
  calculation, relocation selection for interpreter-backed ET_DYN images,
  MCEXEC_UP_GET_CREDV dispatch, PID/PGID/AT_* metadata publication, and parser
  failure return shaping. C retains only descriptor allocation/layout writes,
  credential-vector pointer exposure, clock-tick lookup, and existing
  diagnostics as narrow bridges because program_load_desc layout has
  configuration-dependent fields. The equivalence harness now writes a minimal
  real ELF64 image for the recursive shebang/interpreter path, checks Rust
  load_elf() section publication, stack protection, AT_PHDR fallback, AT_PHENT,
  AT_PHNUM, AT_ENTRY, AT_CLKTCK, credential ioctl routing, open-exec and
  exec_path sequencing, and the Rust symbol gate. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, strict Rust mcexec build,
  symbol-split checks, full kernel/rust/tests/run_equivalence.sh after the
  recorded PROT_* fixture include fix, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.96551724137932% complete, overall scored
  source-retirement completion 92.3544776119403%, 21 debt rows, and
  7.645522388059703 percentage points left. This remains partial user-tool row
  movement; the row still has substantial overlay_getdents, overlay_path,
  syscall, and main-loop C orchestration in the strict path. No guest runtime
  validation, QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 24.0% to
  25.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports load_elf_desc(); the strict C object no longer defines that
  executable descriptor loader body, while the C fallback definition remains
  under !MCEXEC_RUST_HELPERS. Rust owns executable access/stat/open checks,
  zero-length and header-read validation, shebang detection/trimming and
  publication, MCEXEC_UP_OPEN_EXEC dispatch, exec_path absolute/relative
  publication, load_elf() parser callback dispatch, interpreter search/open
  and load_interp() handoff, descriptor publication, section-count logging,
  and loader failure return shaping. C retains the lower-level load_elf()
  parser body plus narrow stat, exec_path, and diagnostic bridges because full
  descriptor layout publication and credential-vector parsing remain in the
  next loader batch. The equivalence harness now supplies real temporary
  shebang/interpreter files, stubs load_elf(), checks recursive shebang
  resolution through Rust load_elf_desc(), open-exec routing, exec_path
  publication, stat/open sequencing, descriptor publication, section-count
  logging, failure routing, and the Rust symbol gate. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, strict Rust mcexec build,
  symbol-split checks, full kernel/rust/tests/run_equivalence.sh, and a
  separate ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.93103448275862% complete, overall scored
  source-retirement completion 92.35074626865672%, 21 debt rows, and
  7.649253731343279 percentage points left. This remains partial user-tool row
  movement; the row still has substantial raw ELF parser, overlay_getdents,
  overlay_path, syscall, and main-loop C orchestration in the strict path. No
  guest runtime validation, QEMU boot, host reboot, or reboot-capable script
  was run.

- 2026-06-13: executer/user/mcexec.c moved from 23.0% to
  24.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports load_interp(); the strict C object no longer defines that ELF
  interpreter loader body, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns ELF header read/validation, ELF magic
  rejection, program-header scan/counting, descriptor reallocation sizing,
  PT_LOAD section publication, segment protection derivation, interpreter
  entry and alignment publication, PT_INTERP-on-interpreter rejection, and
  read/realloc diagnostic routing. C retains only narrow descriptor/layout and
  diagnostic bridges because FILE ownership, existing diagnostics, and the
  surrounding load_elf_desc() parser remain part of the larger loader batch.
  The equivalence harness builds a synthetic interpreter ELF with two PT_LOAD
  entries, checks published section fields/protections, entry, alignment,
  section count, digest mixing, and the Rust symbol gate. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, strict Rust mcexec build,
  symbol-split checks, full kernel/rust/tests/run_equivalence.sh, and a
  separate ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.89655172413794% complete, overall scored
  source-retirement completion 92.34701492537313%, 21 debt rows, and
  7.652985074626869 percentage points left. This remains partial user-tool row
  movement; the row still has substantial ELF loader, overlay_getdents,
  overlay_path, syscall, and main-loop C orchestration in the strict path. No
  guest runtime validation, QEMU boot, host reboot, or reboot-capable script
  was run.

- 2026-06-13: executer/user/mcexec.c moved from 22.0% to
  23.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports load_elf_desc_shebang(); the strict C object no longer defines that
  recursive shebang loader body, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns interpreter lookup dispatch, load_elf_desc()
  parser dispatch, recursive shebang resolution, optional argv-extension
  handling, NULL shebang-argv behavior, ENOMEM return shaping from argv
  extension failure, and lookup/load diagnostic dispatch. C retains only the
  load_elf_desc() parser body plus narrow diagnostic bridges because ELF
  parsing and FILE/program descriptor mutation are still part of the larger
  loader batch. The equivalence harness stubs load_elf_desc(), exercises a
  two-level script-to-interpreter recursion through the real Rust
  lookup_exec_path() and argv-extension helper, checks interpreter argument
  splitting, lookup failure routing, load failure routing, and the Rust symbol
  gate. Validation passed for rustfmt, shell syntax, scoped whitespace checks,
  strict Rust mcexec build, symbol-split checks, full
  kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.86206896551724% complete, overall scored
  source-retirement completion 92.34328358208955%, 21 debt rows, and
  7.656716417910445 percentage points left. This remains partial user-tool row
  movement; the row still has substantial ELF loader, overlay_getdents,
  overlay_path, syscall, and main-loop C orchestration in the strict path. No
  guest runtime validation, QEMU boot, host reboot, or reboot-capable script
  was run.

- 2026-06-13: executer/user/mcexec.c moved from 21.0% to
  22.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports transfer_image(); the strict C object no longer defines that
  executable image-transfer body, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns program-section iteration, page-aligned
  start/end calculation, file seek dispatch and error shaping, per-page DMA
  buffer zeroing, unaligned first-page copy sizing, file-read/ferror/feof
  error shaping, remaining-file-length updates, remote-transfer packet
  publication, ioctl dispatch, DMA perror break behavior, and final success
  return shaping. C retains narrow descriptor field bridges plus diagnostic
  bridges for FILE/struct program_load_desc layout and existing stderr/debug
  side effects. The equivalence harness uses a real temporary FILE and a
  deliberately unaligned image section, stubs the MCEXEC_UP_TRANSFER ioctl,
  and checks section offset/filesz publication, transfer packet fields, one
  upload before no-more-data break, DMA buffer zeroing, and the Rust symbol
  gate. Validation passed for rustfmt, shell syntax, scoped whitespace checks,
  strict Rust mcexec build, symbol-split checks, full
  kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.82758620689656% complete, overall scored
  source-retirement completion 92.33955223880596%, 21 debt rows, and
  7.660447761194035 percentage points left. This remains partial user-tool row
  movement; the row still has substantial loader, overlay_getdents,
  overlay_path, syscall, and main-loop C orchestration in the strict path. No
  guest runtime validation, QEMU boot, host reboot, or reboot-capable script
  was run.

- 2026-06-13: executer/user/mcexec.c moved from 20.0% to
  21.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports lookup_exec_path(); the strict C object no longer defines that
  executable lookup body, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns absolute-vs-relative filename
  classification, single-component execvp dispatch, COKERNEL_PATH/PATH
  scanning including empty PATH components, non-execvp direct access checks,
  COKERNEL_EXEC_ROOT prefixing, path publication, access errno return shaping,
  lstat errno return shaping, not-found and length-error return shaping, and
  success/stat-error log dispatch. C retains narrow lstat and log bridges
  because those preserve libc struct stat and existing diagnostic side effects.
  The equivalence harness stubs access and lookup bridges, then checks PATH hit
  selection, trailing-empty PATH component behavior, non-execvp local success
  and access failure, relative passthrough, COKERNEL_EXEC_ROOT absolute-path
  prefixing, lstat error return, length errors, and the Rust symbol gate.
  Validation passed for rustfmt, shell syntax, scoped whitespace checks, strict
  Rust mcexec build, symbol-split checks, full
  kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.79310344827586% complete, overall scored
  source-retirement completion 92.33582089552239%, 21 debt rows, and
  7.664179104477611 percentage points left. This remains partial user-tool row
  movement; the row still has substantial loader, transfer_image,
  overlay_getdents, overlay_path, syscall, and main-loop C orchestration in the
  strict path. No guest runtime validation, QEMU boot, host reboot, or
  reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 19.0% to
  20.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports overlay_addfd() and overlay_delfd(); the strict C object no longer
  defines those public overlay fd wrapper bodies, while the C fallback
  definitions remain under !MCEXEC_RUST_HELPERS. Rust owns overlay-add path
  preparation dispatch, ignored-path selection, transformed proc/sys path
  publication routing, too-long error routing, and overlay-delete fd dispatch.
  C retains narrow publish/delete bridges for allocation, spinlock/list
  mutation, close/free side effects, and the overlay_fd layout. The equivalence
  harness stubs those bridges and checks ignored paths, proc/sys transformed
  path publication, path-length publication, too-long routing, delete dispatch,
  and Rust symbol gates. The first equivalence run in this slice failed because
  the synthetic long path did not exceed the transformed output limit; that
  failure is recorded in kernel.log and the fixture was corrected to use an
  8 KiB path. Validation then passed for rustfmt, shell syntax, scoped
  whitespace checks, strict Rust mcexec build, symbol-split checks, full
  kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.75862068965517% complete, overall scored
  source-retirement completion 92.33208955223881%, 21 debt rows, and
  7.667910447761187 percentage points left. This remains partial user-tool row
  movement; the row still has substantial loader, overlay_getdents, syscall,
  and main-loop C orchestration in the strict path. No guest runtime
  validation, QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 18.0% to
  19.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports overlay_blacklist(); the strict C object no longer defines that
  blacklist decision body or the C blacklist pattern table, while the C
  fallback definition and table remain under !MCEXEC_RUST_HELPERS. Rust owns
  /proc task-path validation dispatch, /sys prefix filtering, CPU and NUMA
  node bound decisions, CPU/node membership rejection, hard sysfs/cgroup/PCI
  blacklist cases, and -ENOENT return shaping. C retains only narrow bridges
  for mcosid access, stat() existence checks, and CPU_ISSET_S/numa_node_set()
  membership because those depend on local process state and libc/kernel CPU-set
  representation. The equivalence harness stubs those bridges and checks proc
  task success/failure, non-sys pass-through, CPU and node bounds, memory/has/
  cgroup/PCI hard blacklist cases, CPU-node membership accept/reject, and the
  Rust symbol gate. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, strict Rust mcexec build, symbol-split checks, full
  kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.72413793103448% complete, overall scored
  source-retirement completion 92.32835820895522%, 21 debt rows, and
  7.671641791044777 percentage points left. This remains partial user-tool row
  movement; the row still has substantial loader, fd overlay mutation,
  overlay_getdents, syscall, and main-loop C orchestration in the strict path.
  No guest runtime validation, QEMU boot, host reboot, or reboot-capable script
  was run.

- 2026-06-13: executer/user/mcexec.c moved from 17.0% to
  18.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports init_worker_threads(); the strict C object no longer defines that
  worker-pool initialization body, while the C fallback definition remains
  under !MCEXEC_RUST_HELPERS. Rust owns the n_threads-based barrier count,
  max-CPU reset ordering, worker creation loop bounds, create-failure error
  routing and negative return shaping, and final init barrier wait dispatch.
  C retains only narrow pthread initialization/wait, static max_cpuid reset,
  create_worker_thread(), and error-print bridges because create_worker_thread()
  is still shared with the UTI path. The equivalence harness stubs those
  bridges and checks both the successful n_threads=2 path and a second-create
  failure path, including no final wait after failure and the Rust symbol gate.
  Validation passed for rustfmt, shell syntax, scoped whitespace checks, strict
  Rust mcexec build, symbol-split checks, full
  kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.6896551724138% complete, overall scored
  source-retirement completion 92.32462686567165%, 21 debt rows, and
  7.675373134328353 percentage points left. This remains partial user-tool row
  movement; the row still has substantial loader, syscall, overlay, and
  main-loop C orchestration in the strict path. No guest runtime validation,
  QEMU boot, host reboot, or reboot-capable script was run.

- 2026-06-13: executer/user/mcexec.c moved from 16.0% to
  17.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports sendsig(); the strict C object no longer defines that signal handler
  body, while the C fallback definition remains under !MCEXEC_RUST_HELPERS.
  Rust owns local-signal and SIGCHLD skip/reset ordering, thread-data scan and
  terminate gates, remote TID/CPU selection, MCEXEC_UP_SEND_SIGNAL descriptor
  fill and dispatch, UTI rt_sigaction lookup dispatch, SIG_IGN/SIG_DFL/action
  selection, handler invocation bracketing, and final SIG_THREAD reset
  ordering. C retains only narrow siginfo field and default-action bridges for
  platform siginfo_t access and the existing non-returning default signal side
  effect. The equivalence harness stubs the mcexec globals and ioctls, then
  checks local LOCALSIG skip/reset, non-UTI descriptor forwarding, UTI ignored
  action handling, UTI handler bracketing, copied signal info, and the Rust
  symbol gate. Validation passed for rustfmt, shell syntax, scoped whitespace
  checks, strict Rust mcexec build, symbol-split checks, full
  kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.65517241379311% complete, overall scored
  source-retirement completion 92.32089552238806%, 21 debt rows, and
  7.6791044776119435 percentage points left. This remains partial user-tool row
  movement; the row still has substantial loader, syscall, overlay,
  worker-thread, and main-loop C orchestration in the strict path. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-13: executer/user/mcexec.c moved from 15.0% to
  16.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_signalfd4(); the strict C object no longer defines that
  signalfd action body, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns signalfd mode dispatch, SFD-to-O flag
  mapping, private sigfd list insertion/removal, read-fd lookup, close
  ordering, push-signal write dispatch, bad-fd shaping, partial-write error
  routing, and default-mode return shaping. C retains only narrow write and
  stderr error bridges for the existing libc side effects. The equivalence
  harness stubs pipe2, close, and the write/error bridges, then checks create,
  bad-fd push, successful push, partial-write failure, bad-fd close, close
  ordering, post-close lookup, and the Rust symbol gate. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, strict Rust mcexec build,
  symbol-split checks, full kernel/rust/tests/run_equivalence.sh, and a
  separate ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.62068965517241% complete, overall scored
  source-retirement completion 92.31716417910448%, 21 debt rows, and
  7.682835820895519 percentage points left. This remains partial user-tool
  row movement; the row still has substantial loader, syscall, signal,
  overlay, worker-thread, and main-loop C orchestration in the strict path. No
  guest runtime validation, QEMU boot, host reboot, or reboot-capable script
  was run.
- 2026-06-13: executer/user/mcexec.c moved from 14.0% to
  15.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_sigprocmask(); the strict C object no longer defines that
  signal-mask update body, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns syscall-wait descriptor mask extraction and
  dispatches through a C bridge that preserves the libc sigset_t, LOCALSIG
  deletion, and sigprocmask(SIG_SETMASK) side effect. The equivalence harness
  uses a fake syscall wait descriptor, checks the forwarded mask, mixes the
  bridge digest, and requires the Rust symbol gate. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, strict Rust mcexec build,
  symbol-split checks, full kernel/rust/tests/run_equivalence.sh, and a
  separate ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.58620689655173% complete, overall scored
  source-retirement completion 92.31343283582089%, 21 debt rows, and
  7.68656716417911 percentage points left. This remains partial user-tool row
  movement; the row still has substantial loader, syscall, signal, overlay,
  worker-thread, and main-loop C orchestration in the strict path. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-13: executer/user/mcexec.c moved from 13.0% to
  14.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports act_sigaction(); the strict C object no longer defines that signal
  action update body, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns syscall-wait descriptor argument reads,
  SIGCHLD/LOCALSIG skip policy, SIG_IGN classification, and install dispatch,
  while a C bridge preserves the libc sigaction layout plus existing sendsig
  handler installation. The equivalence harness uses a fake syscall wait
  descriptor, checks skipped SIGCHLD and LOCALSIG paths, verifies ignored and
  handled install dispatch, and requires the Rust symbol gate. Validation
  passed for rustfmt, shell syntax, scoped whitespace checks, strict Rust
  mcexec build, symbol-split checks, full kernel/rust/tests/run_equivalence.sh,
  and a separate ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.55172413793103% complete, overall scored
  source-retirement completion 92.30970149253731%, 21 debt rows, and
  7.6902985074626855 percentage points left. This remains partial user-tool row
  movement; the row still has substantial loader, syscall, signal, overlay,
  worker-thread, and main-loop C orchestration in the strict path. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-13: executer/user/mcexec.c moved from 12.0% to
  13.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports init_sigaction(); the strict C object no longer defines that signal
  initialization body, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns master-tid publication, signal range
  iteration, and the SIGKILL/SIGSTOP/SIGCHLD/SIGTSTP/SIGTTIN/SIGTTOU skip
  policy, while C bridge callbacks preserve the libc sigaction layout and
  existing sendsig handler installation. The equivalence harness stubs the
  master-tid and install callbacks, checks the expected 58 installed signals,
  verifies the skipped signals, and requires the Rust symbol gate. Validation
  passed for rustfmt, shell syntax, scoped whitespace checks, strict Rust
  mcexec build, symbol-split checks, full kernel/rust/tests/run_equivalence.sh,
  and a separate ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.51724137931035% complete, overall scored
  source-retirement completion 92.30597014925372%, 21 debt rows, and
  7.694029850746276 percentage points left. This remains partial user-tool row
  movement; the row still has substantial loader, syscall, signal, overlay,
  worker-thread, and main-loop C orchestration in the strict path. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-13: executer/user/mcexec.c moved from 11.0% to
  12.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports close_cloexec_fds(); the strict C object no longer defines that
  close-on-exec descriptor scan body, while the C fallback definition remains
  under !MCEXEC_RUST_HELPERS. Rust owns the sysconf(_SC_OPEN_MAX) range
  selection, McKernel fd skip, fcntl(F_GETFD) dispatch, FD_CLOEXEC bit test,
  and close() dispatch, including parity with the existing C behavior for
  negative fcntl() results. The equivalence harness stubs sysconf(), fcntl(),
  and close(), checks the scanned fd count, skipped mcos fd, closed fd list,
  and requires the Rust symbol gate. Validation passed for rustfmt, shell
  syntax, scoped whitespace checks, strict Rust mcexec build, symbol-split
  checks, full kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.48275862068965% complete, overall scored
  source-retirement completion 92.30223880597015%, 21 debt rows, and
  7.697761194029852 percentage points left. This remains partial user-tool row
  movement; the row still has substantial loader, syscall, signal, overlay,
  worker-thread, and main-loop C orchestration in the strict path. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-13: executer/user/mcexec.c moved from 10.0% to
  11.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports print_flat(); the strict C object no longer defines that flattened
  string debug-output wrapper, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns the flattened count load, offset iteration,
  and string-entry dispatch, while C bridge callbacks preserve the existing
  debug-print formatting. The equivalence harness stubs the count and entry
  callbacks, drives a two-string flattened buffer, checks dispatch counts and
  digest, and requires the Rust symbol gate. Validation passed for rustfmt,
  shell syntax, scoped whitespace checks, strict Rust mcexec build, full
  kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.44827586206897% complete, overall scored
  source-retirement completion 92.29850746268657%, 21 debt rows, and
  7.7014925373134275 percentage points left. This remains partial user-tool row
  movement; the row still has substantial loader, syscall, signal, overlay,
  worker-thread, and main-loop C orchestration in the strict path. No guest
  runtime validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-13: executer/user/mcexec.c moved from 9.0% to
  10.0% for the strict source-retirement audit. Rust-enabled mcexec now
  exports print_desc(); the strict C object no longer defines that live
  descriptor debug-output wrapper, while the C fallback definition remains
  under !MCEXEC_RUST_HELPERS. Rust owns the public print_desc() sequencing,
  descriptor summary dispatch, and section-loop dispatch, while C bridge
  callbacks preserve field access and the existing debug-print formatting. The
  equivalence harness stubs descriptor field and print callbacks, checks the
  intro/main/two-section dispatch counts and digest, and requires the Rust
  symbol gate. Validation passed for rustfmt, shell syntax, scoped whitespace
  checks, strict Rust mcexec build, full kernel/rust/tests/run_equivalence.sh,
  and a separate ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.41379310344827% complete, overall scored
  source-retirement completion 92.29477611940298%, 21 debt rows, and
  7.705223880597018 percentage points left. This remains
  partial user-tool row movement; the row still has substantial loader,
  syscall, signal, overlay, worker-thread, and main-loop C orchestration in the
  strict path. No guest runtime validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-13: executer/user/mcexec.c moved from 8.0% to
  9.0% for the strict source-retirement audit. Rust-enabled mcexec now exports
  search_file(); the strict C object no longer defines that interpreter-path
  lookup body, while the C fallback definition remains under
  !MCEXEC_RUST_HELPERS. Rust owns direct access-hit selection, altroot modified
  path construction, second access check, miss return shaping, and path-too-long
  bridge dispatch for the existing error log. The equivalence harness stubs
  access(), altroot, and overflow logging, then checks direct-hit, altroot-hit,
  miss, and path-too-long behavior plus the Rust symbol gate. Validation passed
  for rustfmt, shell syntax, scoped whitespace checks, strict Rust mcexec
  build, full kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.37931034482759% complete, overall scored
  source-retirement completion 92.2910447761194%, 21 debt rows, and
  7.708955223880594 percentage points left. This remains partial
  user-tool row movement; the row still has substantial loader, syscall,
  signal, overlay, worker-thread, and main-loop C orchestration in the strict
  path. No guest runtime validation, QEMU boot, host reboot, or reboot-capable
  script was run.
- 2026-06-13: executer/user/mcexec.c moved from 7.0% to
  8.0% for the strict source-retirement audit. Rust-enabled mcexec now exports
  do_strncpy_from_user(); the strict C object no longer defines that live
  syscall string-copy wrapper body, while the C fallback definition remains
  under !MCEXEC_RUST_HELPERS. Rust owns strncpy_from_user_desc field shaping,
  MCEXEC_UP_STRNCPY_FROM_USER ioctl dispatch, result writeback return shaping,
  and -errno/perror failure routing for this wrapper. The equivalence harness
  intercepts ioctl()/perror(), writes back the descriptor result, and checks
  success and error paths including errno preservation after the recorded
  fixture-buffer correction. Validation passed for rustfmt, shell syntax,
  scoped whitespace checks, strict Rust mcexec build, full
  kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. The strict
  source-retirement audit reports compiled_retired_c 0, stale tracker rows [],
  untracked files [], User tools 87.34482758620689% complete, overall scored
  source-retirement completion 92.28731343283582%, 21 debt rows, and
  7.712686567164184 percentage points left. This remains partial
  user-tool row movement; the row still has substantial loader, syscall,
  signal, overlay, worker-thread, and main-loop C orchestration in the strict
  path. No guest runtime validation, QEMU boot, host reboot, or reboot-capable
  script was run.
- 2026-06-13: executer/user/mcexec.c moved from 6.0% to
  7.0% for the strict source-retirement audit. Rust-enabled mcexec now exports
  do_syscall_return() and do_syscall_load(); the strict C object no longer
  defines those public syscall-response/load wrapper bodies, while C fallback
  definitions remain under !MCEXEC_RUST_HELPERS. Rust owns descriptor field
  shaping, ioctl request selection, and perror-on-failure routing for these
  wrappers. The equivalence harness intercepts ioctl()/perror() and directly
  checks the syscall_ret_desc and syscall_load_desc ABI fields for both success
  and error paths, and the symbol gate now requires both names in the Rust
  helper object. Validation passed for rustfmt, shell syntax, scoped whitespace
  checks, strict Rust mcexec build, full kernel/rust/tests/run_equivalence.sh,
  and a separate ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. This
  strict source-retirement audit reports compiled_retired_c 0, stale tracker
  rows [], untracked files [], User tools 87.3103448275862% complete, overall
  scored source-retirement completion 92.28358208955224%, 21 debt rows, and
  7.71641791044776 percentage points left. This
  remains partial user-tool row movement; the row still has substantial loader,
  syscall, signal, overlay, worker-thread, and main-loop C orchestration in the
  strict path. No guest runtime validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-13: executer/user/mcexec.c moved from 5.0% to
  6.0% for the strict source-retirement audit. Rust-enabled mcexec now exports
  gettid() and tgkill(); the strict C object no longer defines those public
  wrapper bodies, while C fallback definitions remain under
  !MCEXEC_RUST_HELPERS. The equivalence harness directly checks gettid() and
  tgkill() through the Rust-exported ABI and the symbol gate now requires both
  names in the Rust helper object. Validation passed for rustfmt, shell
  syntax, scoped whitespace checks, strict Rust mcexec build, full
  kernel/rust/tests/run_equivalence.sh after the recorded fixture-header
  correction, and a separate ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build.
  The strict source-retirement audit reports compiled_retired_c 0, stale
  tracker rows [], untracked files [], User tools 87.27586206896552% complete,
  overall scored source-retirement completion 92.27985074626865%, 21 debt
  rows, and 7.72014925373135 percentage points left.
  This remains partial user-tool row movement; the row still has substantial
  loader, syscall, signal, overlay, worker-thread, and main-loop C
  orchestration in the strict path. No guest runtime validation, QEMU boot,
  host reboot, or reboot-capable script was run.
- 2026-06-13: executer/user/mcexec.c moved from 4.0% to
  5.0% for the strict source-retirement audit. Rust-enabled mcexec now exports
  atobytes(); the strict C object no longer defines that public helper body and
  instead retains only an errno-publication bridge for this path. The C
  fallback atobytes() body remains under !MCEXEC_RUST_HELPERS. The equivalence
  harness directly checks the Rust-exported atobytes() ABI for suffix scaling
  and ERANGE/zero behavior, and the symbol gate now requires atobytes in the
  Rust helper object. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, strict Rust mcexec build, full
  kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. This remains partial
  user-tool row movement; the row still has substantial loader, syscall,
  signal, overlay, worker-thread, and main-loop C orchestration in the strict
  path. No guest runtime validation, QEMU boot, host reboot, or reboot-capable
  script was run.
- 2026-06-13: executer/user/mcexec.c moved from 3.0% to
  4.0% for the strict source-retirement audit. Rust-enabled mcexec now exports
  print_usage(); the strict C object no longer defines that public helper body
  and instead references the Rust export, while C retains only the compile-time
  add-envs selector and raw stderr write bridge for this path. The C fallback
  print_usage() body remains under !MCEXEC_RUST_HELPERS. The equivalence
  harness captures the bridge writes and directly checks the Rust-exported
  print_usage() ABI for both normal and ADD_ENVS-style usage output, and the
  symbol gate now requires print_usage in the Rust helper object. Validation
  passed for rustfmt, shell syntax, scoped whitespace checks, strict Rust
  mcexec build, full kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. This remains partial
  user-tool row movement; the row still has substantial loader, syscall,
  signal, overlay, worker-thread, and main-loop C orchestration in the strict
  path. No guest runtime validation, QEMU boot, host reboot, or reboot-capable
  script was run.
- 2026-06-13: executer/user/mcexec.c moved from 0.0% to
  3.0% for the strict source-retirement audit. Rust-enabled mcexec now exports
  flatten_strings() and copy_dirents(); the strict C object no longer defines
  those public helper bodies and instead references the Rust exports, while C
  retains the copy_dirents syscall-kind bridge plus loader, syscall, signal,
  overlay, worker-thread, and main-loop orchestration. C fallback definitions
  for the moved helper symbols remain under !MCEXEC_RUST_HELPERS. The
  equivalence harness now stubs the bridge layer and directly checks both
  Rust-exported ABI names, including flat-string layout and dirent copy/count
  shaping. Validation passed for rustfmt, shell syntax, scoped whitespace
  checks, strict Rust mcexec build, full kernel/rust/tests/run_equivalence.sh,
  and a separate ENABLE_RUST_USER_TOOLS=OFF mcexec fallback build. This is a
  conservative partial user-tool row movement; the row remains active
  source-retirement debt. No guest runtime validation, QEMU boot, host reboot,
  or reboot-capable script was run.
- 2026-06-13: executer/user/mcinspect.c moved from 0.0% to
  5.0% for the strict source-retirement audit. Rust-enabled mcinspect now
  exports usage() and lookup_bfd_symbol(); the strict C object no longer
  defines those public helper bodies and retains only usage/symbol bridge
  accessors plus the DWARF walking, kernel-read, process/thread display,
  vtop, initialization, and main orchestration. C fallback definitions for
  usage() and lookup_bfd_symbol() remain under !MCINSPECT_RUST_HELPERS. The
  equivalence harness now stubs the bridge layer and directly checks the
  Rust-exported usage and symbol lookup ABI names, including missing-symbol
  NOSYMBOL shaping. Validation passed for rustfmt, shell syntax, scoped
  whitespace checks, strict Rust mcinspect build, full
  kernel/rust/tests/run_equivalence.sh after the recorded UINTPTR_MAX fixture
  fix, and a separate ENABLE_RUST_USER_TOOLS=OFF mcinspect fallback build.
  This is a conservative partial user-tool row movement; the row remains
  active source-retirement debt. No guest runtime validation, QEMU boot, host
  reboot, or reboot-capable script was run.
- 2026-06-13: executer/user/eclair.c moved from 0.0% to
  20.0% for the strict source-retirement audit. Rust-enabled eclair now
  exports lookup_symbol(), read_64(), read_32(), read_symbol_64(),
  print_bin(), and intr_handler(); the strict C object no longer defines
  those public helper bodies and retains only main/read_mem plus setup,
  dump, command, read-physmem, and bridge/static orchestration. C fallback
  definitions for the moved helper symbols remain under !ECLAIR_RUST_HELPERS,
  while the strict Rust path uses narrow bridge accessors for the BFD symbol
  table and existing C read_mem/stdio/signal primitives. The equivalence
  harness now stubs the bridge layer and directly checks the Rust-exported
  ABI names, lookup miss behavior, sized read wrappers, read-by-symbol
  dispatch, binary formatting, and SIGINT forwarding. Validation passed for
  rustfmt, shell syntax, scoped whitespace checks, strict Rust eclair build,
  full kernel/rust/tests/run_equivalence.sh, and a separate
  ENABLE_RUST_USER_TOOLS=OFF eclair fallback build. This is a partial user
  tool row movement, not full source retirement; the next >=1 overall-point
  checkpoint still requires multiple full-row retirements or a much larger
  multi-row partial batch. No guest runtime validation, QEMU boot, host
  reboot, or reboot-capable script was run.
- 2026-06-13: executer/kernel/mcctrl/sysfs_files.c moved to
  100.0% for the strict Rust source-retirement audit. Rust-enabled mcctrl
  Kbuild now omits sysfs_files.c, while executer/kernel/mcctrl/rust/
  mcctrl_helpers.rs exports setup_sysfs_files(), free_topology_info(),
  setup_local_snooping_files(), the CPU/NUMA mapping helpers, and
  show_int_ops. Rust now owns the active sysfs setup sequencing for local
  snooping files, CPU/cache topology collection and file publication, NUMA
  topology collection, distance-string publication, CPU/node symlink
  publication, topology teardown loops, and setup-complete publication.
  executer/kernel/mcctrl/driver.c retains primitive Linux-kernel bridge
  callbacks for allocation/free, field access, list insertion/removal, cpumask
  operations, topology queries, node distance, and cpu_to_node while that row
  remains active debt. ihk/ikc/linux.c was corrected to use Linux
  kmalloc()/kfree() in its retained helper-off fallback body instead of the
  McKernel tracked allocator wrappers. Validation passed for rustfmt,
  scoped whitespace checks, strict Rust mcctrl_ko build, helper-off C fallback
  mcctrl_ko build after the recorded fallback allocator fix, and symbol/build
  graph checks showing sysfs_files.c absent from the active strict mcctrl
  object graph. The strict source-retirement audit now reports 21 debt rows,
  29 compiled non-allowlisted C sources, 0 compiled retired C sources, host/
  IKC/mcctrl/IHK modules at 87.5% complete, overall scored source-retirement
  completion at 92.16417910447761%, and 7.835820895522389 percentage points
  left. This retires one scored row, below the preferred one-point batch size;
  the remaining rows are larger and still need multi-row movement for the next
  >=1-point checkpoint. No guest runtime validation, module insertion, QEMU
  boot, host reboot, or reboot-capable script was run.
- 2026-06-13: lib/include/list.h moved from 87.2% to
  100.0% for the strict source-retirement header audit, and lib/list.c was
  added as the C fallback body outside the strict Rust build path. The public
  list header is now declaration-only apart from object constants, while
  container/list-entry/list-iteration/LIST_HEAD users were expanded across the
  active x86_64 path, deferred arm64 compatibility path, IKC users, and
  mcexec's local overlay list. The equivalence harness now links the fallback
  list object into C-side comparisons that need out-of-line list helpers, and
  its generated allocator fixtures no longer depend on the retired public list
  macros. Validation passed for the scoped diff whitespace check, no-hit
  project-list macro scan, strict Rust mckernel.img build, C-fallback
  mckernel.img build, strict Rust mcexec build, the full
  kernel/rust/tests/run_equivalence.sh harness after the recorded harness
  fixes, and the strict source-retirement audit with
  --fail-on-retired-compiled-c. The audit now reports 22 debt rows,
  0 executable-header hits, 30 compiled non-allowlisted C sources,
  0 compiled retired C sources, Shared primitives at 100.0% complete, overall
  scored source-retirement completion at 91.7910447761194%, and
  8.208955223880594 percentage points left. This was a safe below-one-point
  header/fallback slice; future >=1-point movement still needs a multi-row
  implementation batch. No guest runtime validation, module-load validation,
  QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-13: lib/include/bitops.h moved from 84.4% to
  100.0% for the strict source-retirement header audit. The remaining
  executable bitops compatibility macros were retired from the public header
  after BIT(), BITS_TO_LONGS(), DECLARE_BITMAP(), and for_each_set_bit()
  users were expanded across the active x86_64 path plus deferred arm64 and
  mcctrl compatibility header surfaces. The header now retains declaration
  and object-constant compatibility only; lib/include/list.h is the only
  remaining executable-header hit in the strict audit. Validation passed for
  the scoped diff whitespace check, strict Rust mckernel.img build,
  C-fallback mckernel.img build, the full kernel/rust/tests/run_equivalence.sh
  harness, and the strict source-retirement audit with
  --fail-on-retired-compiled-c. The audit now reports 23 debt rows,
  1 executable-header hit, 30 compiled non-allowlisted C sources,
  0 compiled retired C sources, Shared primitives at
  99.63428571428571% complete, overall scored source-retirement completion at
  91.7123595505618%, and 8.2876404494382 percentage points left. This was a
  safe below-one-point header slice; the remaining shared-primitives header
  debt is smaller still, and the next >=1-point movement needs a multi-row
  implementation batch. No guest runtime validation, module-load validation,
  QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-13: kernel/include/kmalloc.h moved from 95.0% to
  100.0% for the strict source-retirement header audit. The tracked
  kmalloc/kfree macro layer was retired from the public header after 227
  McKernel-side allocation/free call sites were expanded to
  kmalloc_tracked()/kfree_tracked() with explicit __FILE__/__LINE__
  arguments. Strict Rust now exports kmalloc_tracked(), kfree_tracked(),
  kmalloc(), and kfree() from kernel/rust/mem_helpers.rs, while kernel/mem.c
  keeps matching C fallback definitions outside the Rust helper build. The
  old memcheck macro was removed without fabricating a new wrapper because
  there are no in-tree users and the referenced _memcheck symbol is not
  present in the strict path. Validation passed for rustfmt, focused
  no-macro scan on kmalloc.h, scoped diff whitespace check after one recorded
  trailing-whitespace fix, strict Rust mckernel.img build after one recorded
  _memcheck link fix, C-fallback mckernel.img build, the full
  kernel/rust/tests/run_equivalence.sh harness after adding the tracked
  allocator test stub, and the strict source-retirement audit with
  --fail-on-retired-compiled-c. The audit now reports 24 debt rows,
  2 executable-header hits, 30 compiled non-allowlisted C sources,
  0 compiled retired C sources, Other in-scope code at
  97.95918367346938% complete, overall scored source-retirement completion at
  91.65393258426965%, and 8.346067415730346 percentage points left. This was
  a safe below-one-point header slice; the remaining >=1-point batches require
  retiring multiple active implementation source rows, not merely replacing
  wrappers. No guest runtime validation, module-load validation, QEMU boot,
  host reboot, or reboot-capable script was run.
- 2026-06-13: lib/include/ihk/mm.h moved from 94.0% to 100.0%
  for the strict source-retirement header audit. The allocator/free
  convenience macro layer was retired from the public header after 115
  in-tree call sites were expanded to the tracked low-level allocation/free
  entry points with explicit __FILE__/__LINE__ arguments. Strict Rust now
  exports source-compatible allocator/free wrapper symbols from
  kernel/rust/mem_helpers.rs, while kernel/mem.c keeps matching C fallback
  definitions outside the Rust helper build. The remaining unlikely()
  fallback macro was removed from this header as well, so mm.h now exposes
  declarations and constants only for this audit. Validation passed for
  rustfmt after one recorded formatting failure, strict Rust mckernel.img
  build, C-fallback mckernel.img build, the full
  kernel/rust/tests/run_equivalence.sh harness, and the strict
  source-retirement audit with --fail-on-retired-compiled-c. The audit now
  reports 25 debt rows, 3 executable-header hits, 30 compiled
  non-allowlisted C sources, 0 compiled retired C sources, Shared primitives
  at 99.18857142857142% complete, overall scored source-retirement
  completion at 91.63520599250936%, and 8.364794007490644 percentage points
  left. No guest runtime validation, module-load validation, QEMU boot, host
  reboot, or reboot-capable script was run.
- 2026-06-13: kernel/include/profile.h moved from 89.4% to 100.0%
  for the strict source-retirement header audit. The non-installed user-space
  profile convenience wrappers no longer carry static inline syscall bodies in
  the kernel header; the active source tree has no external callers of those
  wrapper names, and the header now exposes declarations only for that branch.
  Validation passed for the no-hit executable-header scan on profile.h, scoped
  diff whitespace check, strict Rust mckernel.img build, C-fallback
  mckernel.img build, the full kernel/rust/tests/run_equivalence.sh harness,
  and the strict source-retirement audit with --fail-on-retired-compiled-c.
  The audit now reports 26 debt rows, 4 executable-header hits,
  30 compiled non-allowlisted C sources, 0 compiled retired C sources,
  Other in-scope code at 97.90816326530613% complete, overall scored
  source-retirement completion at 91.612734082397%, and
  8.387265917603003 percentage points left. No guest runtime validation,
  module-load validation, QEMU boot, host reboot, or reboot-capable script was
  run.
- 2026-06-13: kernel/include/cls.h moved from 97.4% to 100.0%
  for the strict source-retirement header audit. The CPU-local field access
  macro layer was retired from the public header, and active x86_64 C callsites
  now spell the existing `get_this_cpu_local_var()->field` access directly.
  lib/include/list.h also dropped its pointer-arithmetic offsetof fallback in
  favor of the compiler builtin alias, but list.h still has executable
  container/list iteration macros and remains partial. Validation passed for
  the no-hit cpu_local_var callsite scan, scoped diff whitespace check after a
  recorded trailing-whitespace fix, strict Rust mckernel.img build, C-fallback
  mckernel.img build, the full kernel/rust/tests/run_equivalence.sh harness,
  and the strict source-retirement audit with --fail-on-retired-compiled-c.
  The audit now reports 27 debt rows, 5 executable-header hits,
  30 compiled non-allowlisted C sources, 0 compiled retired C sources,
  Other in-scope code at 97.8% complete, overall scored source-retirement
  completion at 91.57303370786516%, and 8.426966292134836 percentage points
  left. No guest runtime validation, module-load validation, QEMU boot,
  host reboot, or reboot-capable script was run.
- 2026-06-13: lib/include/bitops.h moved from 65.6% to 84.4%
  for the strict source-retirement header audit, and
  kernel/include/xpmem_private.h no longer contributes executable header
  logic after its XPMEM debug/assert/page-offset macros were localized to the
  C implementation file. BIT_WORD(), ALIGN-family, and IS_ALIGNED() are now
  Rust-owned exported helper calls in the strict Rust path with C fallback
  definitions retained for fallback builds. Validation passed for rustfmt with
  the module's Rust 2021 parsing needs, shell syntax checks, the full
  kernel/rust/tests/run_equivalence.sh harness including bitops_header,
  bitops, and ihk_module_helpers coverage, strict Rust mckernel.img build,
  C-fallback mckernel.img build, and the strict source-retirement audit with
  --fail-on-retired-compiled-c. The audit now reports 28 debt rows,
  6 executable-header hits, 30 compiled non-allowlisted C sources,
  0 compiled retired C sources, Shared primitives at 99.01714285714286%
  complete, host/IKC/mcctrl/IHK modules at 86.25% complete, and overall
  scored source-retirement completion at 91.56329588014982% with
  8.436704119850177 percentage points left. This was the largest safe
  shared-primitives header slice below one overall point because remaining
  movement in this batch is long-tail executable header logic, not whole C
  source retirement. Several equivalence harness link/runtime expectation
  failures were recorded in kernel.log and fixed before the passing rerun. No
  guest runtime validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-13: kernel/include/syscall.h moved from 91.0% to 100.0%
  for the strict source-retirement header audit. The syscall number table is
  now explicit in the header instead of generated by local executable macros,
  the syscall declaration macros were removed from the header, and the
  time-conversion helper bodies moved behind Rust-owned exported helpers in
  the strict Rust path while preserving C fallback definitions outside the
  Rust helper build. Validation passed for the no-hit executable-header scan
  on kernel/include/syscall.h, audit script Python bytecode check, rustfmt,
  strict Rust mckernel.img build, C-fallback mckernel.img build, scoped diff
  whitespace check, and the strict source-retirement audit with
  --fail-on-retired-compiled-c. The audit now reports 31 debt rows,
  7 executable-header hits, 35 compiled non-allowlisted C sources,
  0 compiled retired C sources, Syscall core at 33.333333333333336%
  complete, and overall scored source-retirement completion at
  90.3692883895131% with 9.630711610486898 percentage points left. One
  scoped diff whitespace check failed on trailing whitespace in
  kernel/syscall.c and was recorded in kernel.log before the whitespace-only
  fix. No guest runtime validation, module-load validation, QEMU boot, host
  reboot, or reboot-capable script was run.
- 2026-06-13: source-retirement audit boundary handling was tightened and the
  current Rocky validation scope was cleaned up. The audit now excludes
  executable-header hits from rows explicitly tracked as n/a because those
  rows are allowlisted compatibility, external, generated, test, or inactive
  target boundaries rather than scored McKernel-owned implementation debt.
  executer/include/defs.h is now n/a as upstream crash-suite compatibility
  material; kernel/include/lwk/compiler.h and
  kernel/include/lwk/compiler-gcc.h are now n/a as compiler compatibility
  surfaces; ihk/linux/include/ihk/affinity.h,
  ihk/linux/include/ihk/misc/debug.h, and lib/include/ihk/debug.h are now n/a
  as public compatibility/debug macro surfaces; and
  executer/user/ql_mpiexec_start.c is now n/a for the current Rocky validation
  path because AGENTS.md explicitly says not to count ql_mpiexec_* unless a
  configured validation tree has ENABLE_QLMPI=ON, while the active strict tree
  has ENABLE_QLMPI=OFF. kernel/tofu/tof_utofu_main.c remains scored debt even
  though ENABLE_TOFU=OFF, because it is McKernel-owned and this tracker has no
  matching TOFU exclusion rule. Validation passed for the audit script Python
  bytecode check, diff whitespace check, strict source-retirement audit, and
  strict audit with --fail-on-retired-compiled-c. The audit now reports 34
  debt rows, 10 executable-header hits, 35 compiled non-allowlisted C sources,
  0 compiled retired C sources, host/IKC/mcctrl/IHK modules at 84.6%
  complete, and overall scored source-retirement completion at
  90.21573033707864% with 9.78426966292136 percentage points left. No guest
  runtime validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-13: ihk/linux/core/ops_wrappers.h and
  ihk/linux/driver/smp/smp-driver.h moved to 100.0% for the
  source-retirement header audit. ops_wrappers.h no longer uses local
  function-like wrapper-generation macros for IHK OS/device ops dispatch; the
  wrappers are spelled out explicitly with the same static names, callback
  checks, return values, and error fallbacks. smp-driver.h no longer carries
  variadic debug macros or the ARCHDRV_CHKANDJUMP control-flow macro; SMP
  debug/error logging and check-and-jump decisions now use real module
  functions while preserving the existing ret/fn_fail branch behavior. The
  arm64 SMP driver received only the required callsite rename needed by the
  shared header API and is not counted as active campaign progress. Validation
  passed for no-hit executable-header scans of both headers, Rust-enabled
  ihk_ko and ihk-smp-x86_64_ko builds, no-Rust module C-fallback ihk_ko and
  ihk-smp-x86_64_ko builds, audit script Python bytecode check, diff
  whitespace checks, and the strict source-retirement audit with
  --fail-on-retired-compiled-c. The audit now reports 32 debt rows,
  8 executable-header hits, 35 compiled non-allowlisted C sources,
  0 compiled retired C sources, host/IKC/mcctrl/IHK modules at 85.0%
  complete, and overall scored source-retirement completion at
  90.33558052434456% with 9.664419475655436 percentage points left. No guest
  runtime validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-13: ihk/linux/user/devtest.c moved from 0.0% to 100.0%
  for the Rust-enabled user-tool path. ihk/linux/user/rust/devtest.rs now
  owns the small devtest command body for usage validation, /dev/mcd path
  construction, open failure reporting, read/mmap no-op action dispatch, and
  unknown-action reporting, while the previously unbuilt C source remains
  fallback-only outside the strict Rust path. The non-smp builtin x86
  cokernel/host-driver rows ihk/cokernel/builtin/x86/bootparam.h,
  ihk/cokernel/builtin/x86/setup.c, ihk/linux/driver/builtin/dmacore.c, and
  ihk/linux/driver/builtin/ihk_builtin_driver.c are now marked n/a because the
  active retirement campaign is smp-x86/x86_64; this is scope cleanup, not
  Rust implementation ownership movement. Validation passed for rustfmt,
  strict build regeneration, the Rust devtest target build, strict
  mckernel.img build, no-Rust fallback regeneration and mckernel.img build,
  devtest usage/open-failure smoke checks, the devtest Rust object main-symbol
  check, git diff whitespace checks, and the strict source-retirement audit.
  The audit now
  reports 41 debt rows, 20 executable-header hits, 35 compiled
  non-allowlisted C sources, 0 compiled retired C sources, host/IKC/mcctrl/IHK
  modules at 83.75975609756098% complete, and overall scored
  source-retirement completion at 89.38795620437956% with
  10.612043795620437 percentage points left. The first devtest target build
  attempt failed before reconfigure because the existing build graph was stale;
  that failure is recorded in kernel.log. No guest runtime validation,
  module-load validation, QEMU boot, host reboot, or reboot-capable script was
  run.
- 2026-06-13: kernel/include/lwk/stddef.h moved from 85.0% to 100.0%
  for the source-retirement header audit. The header no longer carries a
  function-like offsetof(TYPE, MEMBER) fallback body; it now aliases the
  compiler offsetof builtin surface directly, removing the executable macro
  logic from this McKernel-owned header. executer/include/md5.h,
  executer/user/md5.c, and tools/dwarf-extract-struct/dwarf-extract-struct.c
  are now marked n/a because their source provenance is explicit third-party
  code rather than McKernel-owned retirement debt. Deferred MIC/attached rows
  are also now marked n/a because the active campaign scope is smp-x86/x86_64;
  this is baseline authority cleanup, not Rust implementation ownership
  movement. Validation passed for the no-hit offsetof function-like macro
  scan, strict Rust mckernel.img build, no-Rust fallback mckernel.img build,
  git diff whitespace checks, and the strict source-retirement audit. The
  audit now reports 46 debt rows, 20 executable-header hits, 35 compiled
  non-allowlisted C sources, 0 compiled retired C sources, host/IKC/mcctrl/IHK
  modules at 79.55581395348837% complete, and overall scored
  source-retirement completion at 88.00647482014388% with
  11.993525179856121 percentage points left. No guest runtime validation,
  module-load validation, QEMU boot, host reboot, or reboot-capable script was
  run.
- 2026-06-12: ihk/linux/include/ihk/ihklib_private.h moved from 82.0% to
  100.0% for the source-retirement header audit. The private user-space kmsg
  logging loop no longer lives in the header; ihklib_private.h now only
  declares ihklib_printk(), ihklib.c keeps the C-variadic formatting boundary,
  and ihk/linux/user/rust/ihklib_helpers.rs owns the /dev/kmsg open/write/
  close loop for Rust-enabled ihklib builds. Validation passed for rustfmt,
  Rust-enabled ihklib build, C-fallback ihklib and ihkmond builds, git diff
  whitespace checks, and the strict source-retirement audit. This is a
  limited safe slice below one overall percentage point because the remaining
  larger candidates are deferred MIC paths, external tooling, or broad
  compiled C execution bodies. No guest runtime validation, module-load
  validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-12: ihk/linux/core/mikc.c moved from 0.0% to 100.0% for the
  strict Rust IHK core-module path. ihk/linux/core/rust/core_helpers.rs now
  exports the public host MIKC wrapper surface for master-channel init/finalize,
  regular/master channel accessors, listener and wait-list accessors,
  interrupt dispatch, master packet-handler dispatch, unique channel IDs, and
  Linux workqueue handoff. The Linux primitive bridge callbacks and
  EXPORT_SYMBOL metadata moved into host_driver.c so the strict Rust-enabled
  ihk.ko source list can omit mikc.c while retaining C fallback source outside
  that strict path. Validation passed for rustfmt, ihk_core_rust_helpers
  build, clean ihk_ko rebuild, Rust public wrapper symbol checks,
  host_driver.c bridge-symbol checks, strict Kbuild absence checks for
  ihk/linux/core/mikc.c, git diff whitespace checks, and the strict
  source-retirement audit. No module-load validation, QEMU boot, host reboot,
  or reboot-capable script was run.
- 2026-06-12: ihk/cokernel/smp/x86_64/setup.c moved from 0.0% to
  100.0% for the strict Rust kernel path. kernel/rust/x86_setup.rs now owns
  the SMP x86 setup exported bodies for boot entry/phase sequencing,
  boot-parameter remapping, kmsg setup, CPU-info construction, memory/core/
  NUMA query helpers, MIKC queue physical publication, host IPI vector
  dispatch, monitor/rusage/futex/multi-interrupt publication, memory map/
  unmap accessors, warm-reset setup, DMA init handoff, dump-page accessors,
  and perf event/extra-register query helpers. The C setup source is removed
  from the strict mckernel.img build when Rust is enabled, while fallback C
  source selection remains available outside that strict path. Validation
  passed for rustfmt on the new Rust setup module, strict mckernel_rust_obj
  build, strict mckernel.img link, exported-symbol checks against
  /tmp/mckernel-rocky-rust/kernel/rust/mckernel_rust.o, strict build-graph
  absence checks for ihk/cokernel/smp/x86_64/setup.c, git diff whitespace
  checks, and the strict source-retirement audit. No guest runtime validation,
  module-load validation, QEMU boot, host reboot, or reboot-capable script was
  run.
- 2026-06-12: ihk/cokernel/builtin/x86/dma.c and
  ihk/cokernel/builtin/x86/ikc.c moved from 0.0% to 100.0% for the strict
  source-retirement audit. These rows share the same exported ABI names as the
  already verified active SMP x86 Rust bodies: kernel/rust/builtin_dma.rs
  exports builtin_mc_dma_init and ihk_mc_dma_request, while
  kernel/rust/smp_ikc.rs exports ihk_mc_ikc_init_first,
  ihk_ikc_send_interrupt, ihk_mc_interrupt_host, and per_cpu_irq_work. The
  strict Rocky Rust object publishes those symbols, and the old builtin x86 C
  sources are absent from the strict compile graph. Validation passed for
  exported-symbol checks against /tmp/mckernel-rocky-rust/kernel/rust/
  mckernel_rust.o, strict build-graph absence checks for the two builtin C
  sources, git diff whitespace checks, and the strict source-retirement audit.
  No guest runtime validation, module-load validation, QEMU boot, host reboot,
  or reboot-capable script was run.
- 2026-06-12: executer/user/qlmpilib.c moved from 0.0% to 100.0% for the
  Rust-enabled optional QLMPI path. Rust-enabled QLMPI CMake now selects
  rust/qlmpilib.rs plus the tiny qlmpi_mpi_abi.c MPI boundary shim for
  libqlmpi, while ENABLE_RUST_USER_TOOLS=OFF keeps the original C fallback.
  The Rust body owns QL_NAME/QL_PARAM_PATH/QL_SWAP_PATH discovery, PMPI init
  wrapper sequencing, Fortran mpi_init_ wrapper sequencing, QL mode
  initialization, swapout syscall dispatch, PMI barrier dispatch, parameter
  file command/argv/env parsing, escape decoding, mck_ql_argc/mck_ql_argv/
  mck_ql_env publication, and process environ replacement. The retained C shim
  is limited to MPI macro/function ABI that Rust cannot name portably:
  PMPI_Init, pmpi_init_, MPI_Comm_rank(MPI_COMM_WORLD), and MPI_SUCCESS.
  Validation passed for rustfmt, direct Rust object build, fake-MPI C shim
  build, Rust and C fallback shared-object link/symbol checks, a ql-disabled
  MPI_Init/ql_client parity smoke, a ql-enabled parameter-file parser parity
  smoke with local syscall stubs, git diff whitespace checks, and the strict
  source-retirement audit. The full ENABLE_QLMPI CMake configure remains
  blocked by local FindMPI behavior and is recorded in kernel.log. No guest
  runtime validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: executer/user/libqlfort.c, executer/user/ql_server.c,
  and executer/user/ql_talker.c moved from 0.0% to 100.0% for the
  Rust-enabled optional QLMPI paths. Rust-enabled QLMPI CMake now selects
  rust/qlfort.rs for the Fortran argument interposer, rust/ql_server.rs for
  the Unix-domain socket coordinator, and the existing rust/ql_talker.rs for
  the talker client, while ENABLE_RUST_USER_TOOLS=OFF keeps the original C
  fallback sources. The Rust qlfort body owns RTLD_NEXT symbol discovery,
  GNU/Intel Fortran iargc/getarg dispatch, McKernel QL argv publication, and
  fallback-to-next-symbol behavior. The Rust ql_server body owns socket-file
  setup, daemon socket/listen/select sequencing, client table lookup and
  cleanup, QL command receive/send framing, and E/R/N/F command routing.
  qlmpilib.c remains C-owned because safe retirement needs real MPI binding
  generation or an explicitly accepted C ABI shim for MPI_COMM_WORLD and the
  PMPI entry points; counting it here would hide an ABI gap. Validation passed
  for rustfmt, direct Rust object builds for qlfort/ql_server/ql_talker,
  Rust executable/shared-object links and public-symbol checks, matching C
  fallback object/link checks, no-argument CLI parity smokes for ql_server and
  ql_talker, CMake source-selection inspection for the three optional QLMPI
  targets, and a normal non-QLMPI top-level configure. Temporary fake-MPI
  top-level ENABLE_QLMPI configure attempts failed in FindMPI before target
  generation and were recorded in kernel.log. No guest runtime validation,
  module-load validation, QEMU boot, host reboot, or reboot-capable script was
  run.
- 2026-06-12: executer/user/lib/uti/src/linux/uti.c,
  executer/user/lib/uti/src/mckernel/uti.c,
  executer/user/lib/uti/test/src/create.c, and
  executer/user/lib/uti/test/src/attr.c moved from 0.0% to 100.0%
  for the Rust-enabled optional UTI paths. Rust-enabled UTI builds now compile
  src/common_rust.rs plus the selected resource-manager Rust body
  src/linux/uti_rust.rs or src/mckernel/uti_rust.rs, while the nested UTI
  tests compile Rust-owned create_rust.rs and attr_rust.rs objects instead of
  the C tests. The Linux Rust body owns hwloc topology initialization,
  shared nthr_per_pu setup, NUMA/cache/environment/fabric affinity shaping,
  round-robin CPU selection, pthread affinity/scheduler attribute setup, and
  uti_pthread_create() dispatch. The McKernel Rust body owns the clone
  indication syscall sequencing and pthread_create/error/log behavior. The C
  fallback bodies remain available outside --enable-rust; the Linux fallback
  also gained a local hwloc OS-device scan and an empty-cpuset round-robin
  guard so fallback validation no longer depends on infiniband/verbs.h or
  crashes on single-NUMA validation hosts. Validation passed for rustfmt,
  autoreconf, nested UTI git diff whitespace checks, fresh Rust-enabled
  linux and mckernel configure/build/make check runs, fresh no-Rust linux and
  mckernel fallback configure/build/make check runs, Rust-enabled build-graph
  absence of active UTI C sources and C tests, fallback build-graph presence
  of C sources, and libuti.so symbol checks for the public UTI exports. Earlier
  failed autoreconf, invocation, link, external-header, and fallback attr-test
  attempts were recorded in kernel.log before the related fixes. No guest
  runtime validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: kernel/include/rbtree.h moved from 97.4% to 100.0%
  for the strict executable-header audit. The final rb_entry() typed wrapper
  macro was retired from the rbtree header; active process, syscall, and x86
  SMP host-driver call sites now use the already-tracked container_of()
  compatibility surface directly. The rbtree header now preserves only layout,
  RB_ROOT, function declarations, and declaration-style compatibility aliases.
  Validation passed for a no-hit executable-header scan on rbtree.h, a
  non-test non-arm rb_entry() use scan, the strict mckernel.img build, the
  ihk-smp-x86_64_ko non-reboot module build, and the strict
  source-retirement audit. No guest runtime validation, module-load validation,
  QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-12: ihk/linux/driver/smp/bootparam.h moved to 100.0% for
  the strict executable-header audit. The stale SMP boot parameter header no
  longer carries executable CORE_SET/CORE_CLR/CORE_ISSET/CORE_ZERO macros or
  the CORE_ISSET_ANY static inline body; it now preserves only the constants,
  coreset layout, boot-parameter layout, and external boot_param declaration.
  The active x86_64 SMP build already routes the corresponding helper behavior
  through ihk_smp_core_* functions,
  with Rust-owned exports in the x86_64 IHK module helper build and C fallback
  outside that Rust-helper path. Validation passed for a focused executable
  header scan, git diff whitespace checking, the ihk-smp-x86_64_ko non-reboot
  build target, and the strict source-retirement audit. No guest runtime
  validation, module-load validation, QEMU boot, host reboot, or reboot-capable
  script was run.
- 2026-06-12: executer/user/lib/uti/test/src/include/util.h moved to
  100.0% for the strict executable-header audit. The test utility header now
  declares fixed-signature uti_test_okng() helpers instead of carrying
  executable OKNG/NG/OKNGNOJUMP variadic control-flow macros. Rust-enabled UTI
  builds implement the test result formatting and pass/fail return shaping in
  src/common_rust.rs, including the existing "[ OK ] " and "[ NG ] " output
  prefixes. The C test files now make the old jump/no-jump decisions
  explicitly at each call site. The no-Rust fallback implements matching
  default-visible helpers in src/common.c. Validation passed for rustfmt,
  focused header executable-logic scanning, nested UTI whitespace checks, a
  fresh --enable-rust UTI configure/build/make check run, a logged no-Rust
  fallback visibility failure followed by the default-visibility fix and a
  passing fallback configure/build/make check rerun, the optional CMake libuti
  target build, helper symbol checks in Rust-enabled and fallback libuti.so,
  and the strict source-retirement audit. No UTI workload, guest runtime
  validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: executer/user/lib/uti/src/include/uti_impl.h moved to
  100.0% for the strict executable-header audit. The private UTI implementation
  header no longer carries the variadic pr_level/pr_err/pr_warn/pr_debug macro
  bodies; it now declares the Rust-owned uti_should_log() predicate and keeps
  only the remaining layout/type declarations plus the object-like
  UTI_NWORDS_NUMA_SET constant expression. Rust-enabled UTI builds export
  uti_should_log() from src/common_rust.rs. The no-Rust fallback implements
  the same predicate in src/common.c. Because stable Rust cannot define a
  C-variadic helper, the still-unretired src/linux/uti.c and
  src/mckernel/uti.c bodies now keep local C varargs vfprintf wrappers for
  their existing logging call sites; those source files remain tracked debt
  until their larger runtime bodies are retired. Validation passed for
  rustfmt, nested UTI whitespace checks, a focused header executable-logic
  scan, fresh --enable-rust and fallback UTI configure/build/make check runs,
  the optional CMake libuti target build, symbol checks for the Rust
  uti_should_log export and fallback local symbol, and the strict
  source-retirement audit. No UTI workload, guest runtime validation,
  module-load validation, QEMU boot, host reboot, or reboot-capable script was
  run.
- 2026-06-12: executer/user/lib/uti/src/include/uti.h moved to
  100.0% for the strict executable-header audit. The public UTI header now
  keeps declarations for uti_set_loglevel() and the UTI_ATTR_* helpers instead
  of executable static inline bodies. Rust-enabled UTI builds export those C
  ABI helper bodies from src/common_rust.rs, while the no-Rust configure path
  keeps matching fallback definitions in src/common.c. The UTI shared-library
  export filter now includes both uti_* and UTI_ATTR_* symbols so existing
  callers link through the same public names. Validation passed for rustfmt,
  generated configure shell syntax, nested UTI whitespace checks, a fresh
  --enable-rust libuti build and make check run, the optional CMake libuti
  target build, exported-symbol checks for uti_set_loglevel and all UTI_ATTR_*
  helpers, a fresh fallback libuti build and make check run, and the strict
  source-retirement audit. No UTI workload, guest runtime validation,
  module-load validation, QEMU boot, host reboot, or reboot-capable script was
  run.
- 2026-06-12: executer/user/lib/uti/src/common.c moved to 100.0%
  for the Rust-enabled optional UTI library target. The UTI subproject now
  has an explicit --enable-rust configure switch that builds
  src/common_rust.rs into src/common_rust.o and omits src/common.c from
  libuti when Rust user tools are enabled, while the default no-Rust UTI
  configure path keeps src/common.c as the fallback source. The Rust object
  owns the exported uti_loglevel storage with the same default
  UTI_LOGLEVEL_WARN value and C ABI data symbol shape. Top-level
  ENABLE_UTI=ON plus ENABLE_RUST_USER_TOOLS=ON CMake now passes
  --enable-rust into the libuti ExternalProject. Validation passed for
  rustfmt, generated configure shell syntax, focused whitespace checks,
  a fresh out-of-tree UTI --enable-rust --with-rm=mckernel configure/build,
  the existing optional CMake libuti target build, Makefile/config-log checks
  showing --enable-rust and src/common_rust.o with src/common.c commented out
  of the enabled source list, and libuti.so symbol checks showing the
  exported uti_loglevel data symbol. No UTI workload, guest runtime
  validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: executer/user/syscall_intercept.c moved to 100.0% for
  the Rust-enabled optional x86_64 UTI target. Rust-enabled user-tool CMake now
  builds executer/user/rust/syscall_intercept.rs into syscall_intercept.o and
  omits syscall_intercept.c from mck_syscall_intercept while keeping the C
  source available for non-Rust fallback builds. The Rust body owns the
  syscall-intercept hook installation, UTI descriptor publication, McKernel
  TID result override, forwarded syscall-stack request setup for futex/brk/
  mmap/munmap/mprotect/mremap, ioctl dispatch to MCEXEC_UP_SYSCALL_THREAD,
  syscall-stack return shaping, remote exit/exit_group termination request
  setup, clone/fork/vfork/execve -ENOSYS shaping, syscall-profile counter
  updates, and the private UTI descriptor query path. It preserves the
  existing raw uti_syscall0/1/3 wrapper boundary from
  archdep_uti_x86_64.rs and the external libsyscall_intercept hook-point
  variable. Validation passed for rustfmt, git diff --check on the touched
  CMake/Rust files, separate ENABLE_UTI=ON Rust user-tools
  mck_syscall_intercept build, build-graph absence of syscall_intercept.c,
  shared-object symbol/RPATH checks, init/fini array checks for the Rust
  object, LD_PRELOAD pass-through smoke with /bin/true before interception is
  started, default strict Rust and C-fallback mckernel.img builds, the full
  kernel/rust/tests/run_equivalence.sh harness, and the strict source-
  retirement audit. The Rust hook recomputes gettid() for each intercepted
  syscall instead of caching the result in C __thread storage; this preserves
  the classification decision without retaining a C TLS shim. No UTI workload,
  guest runtime validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: arch/x86_64/kernel/include/ihk/atomic.h moved from
  78.0% to 100.0% for the source-retirement header audit. The remaining
  type-generic xchg/cmpxchg executable macro bodies were retired from the x86
  atomic header and replaced at manycore call sites with typed Rust-owned
  exchange and compare-exchange helper exports for int, unsigned long, and
  pointer storage. Strict Rust owns xchg4(), atomic_xchg_ulong(),
  atomic_xchg_ptr(), atomic_cmpxchg_int(), atomic_cmpxchg_ulong(), and
  atomic_cmpxchg_ptr() in kernel/rust/atomic_helpers.rs, while the no-Rust
  fallback path keeps equivalent arch/x86_64/kernel/cpu.c definitions under
  MCKERNEL_RUST_ATOMIC_HELPERS. Lock-free list, MCS/rwspinlock, page-table
  swap, file-object page publication, syscall status, clone TID, timer,
  process, and futex status call sites no longer depend on generic executable
  macros from the header. The shared IKC queue now routes the manycore path
  through atomic_cmpxchg_ulong() while retaining Linux-host cmpxchg() only in
  the non-manycore branch. The generated equivalence harness gained typed
  helper coverage and weak C-model fixture stubs; validation passed for
  run_equivalence.sh shell syntax, focused atomic-header executable scans,
  strict mckernel_rust_obj and mckernel.img builds, no-Rust fallback
  mckernel.img build, strict/fallback symbol definition checks, git
  diff --check for the touched files, the full kernel/rust/tests/
  run_equivalence.sh harness with atomic digest 61d64c52be395c8d, and the
  strict source-retirement audit. No guest runtime validation, module-load
  validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-12: arch/x86_64/kernel/include/ihk/atomic.h moved from
  62.0% to 78.0% for the source-retirement header audit. The named
  ihk_atomic_t, ihk_atomic64_t, xchg8(), atomic_cmpxchg4/8(), long-add, and
  compare-and-swap helper bodies are no longer executable static inline
  fallback code in the x86 atomic header; the header now exposes declarations
  for those named helpers in both strict Rust and no-Rust fallback builds.
  Strict Rust continues to own the implementations through
  kernel/rust/atomic_helpers.rs, while arch/x86_64/kernel/cpu.c now carries
  matching no-Rust fallback definitions under MCKERNEL_RUST_ATOMIC_HELPERS.
  The equivalence harness gained weak C-model atomic stubs for generated C
  fixtures that link retired C objects directly, preserving the old C fixture
  behavior after the header stopped supplying inline bodies. The row remains
  partial because the type-generic __xg(), xchg4(), __xchg_op(), xchg(),
  __raw_cmpxchg(), __cmpxchg(), and cmpxchg() macros still carry executable
  inline assembly in the header. Validation passed for run_equivalence.sh
  shell syntax, focused atomic-header executable scans, strict
  mckernel_rust_obj and mckernel.img builds, no-Rust fallback mckernel.img
  build, strict/fallback symbol definition checks, linked-image unresolved
  symbol checks, the full kernel/rust/tests/run_equivalence.sh harness, and
  the strict source-retirement audit. The first three full equivalence attempts
  exposed generated C fixture links that still needed weak C-model atomic
  helpers for mem_c.o, x86_memory_helpers_c.o, and string_c.o; each failure was
  recorded in kernel.log before the harness was corrected. No guest runtime
  validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: arch/x86_64/kernel/include/ihk/atomic.h moved from
  58.8% to 62.0% for the source-retirement header audit. The active
  IHK_ATOMIC_INIT and IHK_ATOMIC64_INIT executable initializer macros were
  removed from x86 kernel use sites and replaced by explicit ABI-stable
  struct initializers. The ihk_atomic_inc_return() and
  ihk_atomic_dec_return() wrapper macros were retired from the header and are
  now Rust-owned exported ABI helpers in kernel/rust/atomic_helpers.rs,
  preserving the old add/sub-return behavior. The no-Rust fallback path keeps
  equivalent C definitions in arch/x86_64/kernel/cpu.c under the existing
  MCKERNEL_RUST_ATOMIC_HELPERS gate. The row remains partial because
  xchg/cmpxchg macro logic and fallback static inline atomic bodies still
  remain executable C header debt. Validation passed for rustfmt,
  run_equivalence.sh shell syntax, focused scans showing no active
  IHK_ATOMIC_INIT/IHK_ATOMIC64_INIT or ihk_atomic_inc_return/
  ihk_atomic_dec_return macro definitions in the x86 atomic header, strict
  mckernel_rust_obj and mckernel.img builds, no-Rust fallback mckernel.img
  build, strict/fallback symbol definition checks, linked-image unresolved
  symbol checks, the full kernel/rust/tests/run_equivalence.sh harness with
  atomic digest 3da919875528b104, and the strict source-retirement audit. The
  first full equivalence run exposed a missing C-fixture definition for the
  newly externalized inc/dec-return helpers in process_helpers_c; that failure
  was recorded in kernel.log, the generated fixture was corrected with weak
  C-model helpers, and the harness then passed. No guest runtime validation,
  module-load validation, QEMU boot, host reboot, or reboot-capable script was
  run.
- 2026-06-12: arch/x86_64/kernel/include/arch/cpu.h moved from
  78.0% to 100.0% for the source-retirement header audit. The remaining
  smp_load_acquire() and smp_store_release() executable type-generic macros
  were removed from the header and replaced at kernel call sites with
  Rust-owned typed volatile acquire/release helpers for unsigned long,
  unsigned int, int, and pointer slots. Strict Rust now exports those helper
  ABI names from kernel/rust/x86_cpu_helpers.rs with volatile load/store and
  compiler-barrier ordering matching the old macro shape; the no-Rust fallback
  path keeps equivalent C definitions in arch/x86_64/kernel/cpu.c under the
  existing MCKERNEL_RUST_X86_CPU_HELPERS gate. The arch/cpu.h header is now
  declaration-only for this audit. Validation passed for rustfmt,
  run_equivalence.sh shell syntax, focused header scans showing no executable
  logic in arch/cpu.h, strict mckernel_rust_obj and mckernel.img builds,
  no-Rust fallback mckernel.img build, strict/fallback typed helper symbol
  definition checks, linked-image unresolved-symbol checks, the full
  kernel/rust/tests/run_equivalence.sh harness with typed acquire/release
  coverage, and the strict source-retirement audit. The first equivalence run
  exposed a missing C-fixture link for smp_load_acquire_ptr; that failure was
  recorded in kernel.log, the generated fixture stubs were corrected, and the
  harness then passed. No guest runtime validation, module-load validation,
  QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-12: arch/x86_64/kernel/include/arch/cpu.h moved from
  53.9% to 78.0% for the source-retirement header audit. The concrete x86
  barrier bodies mb(), rmb(), wmb(), smp_mb(), smp_rmb(), smp_wmb(), and
  arch_barrier() are now declarations only in the header and are owned by Rust
  exports in kernel/rust/x86_cpu_helpers.rs. The Rust path preserves the
  original mfence/lfence/sfence instruction selection and compiler-barrier-only
  behavior for smp_wmb()/arch_barrier(); the no-Rust fallback path keeps
  equivalent C definitions in arch/x86_64/kernel/cpu.c under the existing
  MCKERNEL_RUST_X86_CPU_HELPERS gate. The row remains partial because
  smp_load_acquire() and smp_store_release() are still type-generic executable
  expression macros. Validation passed for rustfmt, run_equivalence.sh shell
  syntax, focused header scans showing no remaining inline assembly barrier
  bodies in arch/cpu.h, strict mckernel_rust_obj and mckernel.img builds,
  no-Rust fallback mckernel.img build, strict/fallback barrier symbol
  definition checks, linked-image unresolved-symbol checks, the full
  kernel/rust/tests/run_equivalence.sh harness with expanded x86 barrier
  coverage, and the strict source-retirement audit. No guest runtime
  validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: arch/x86_64/kernel/include/registers.h moved from
  95.0% to 100.0% for the source-retirement header audit. The remaining
  executable x86 register compatibility macros were removed from the header:
  REGS_GET_STACK_POINTER() is now a Rust-owned exported accessor,
  CVAL()/CVAL2() are Rust-owned exported perf-event value helpers, and static
  perf-map initializers now carry the equivalent literal values instead of
  depending on executable header macro expansion. The no-Rust fallback path
  keeps equivalent C definitions in arch/x86_64/kernel/cpu.c under the
  existing MCKERNEL_RUST_X86_CPU_HELPERS gate. Validation passed for rustfmt,
  run_equivalence.sh shell syntax, focused executable-header scans with no
  remaining hits in registers.h, strict mckernel_rust_obj and mckernel.img
  builds, no-Rust fallback mckernel.img build, strict/fallback symbol
  definition checks for CVAL/CVAL2/REGS_GET_STACK_POINTER, linked-image
  unresolved-symbol checks, the full kernel/rust/tests/run_equivalence.sh
  harness with the x86 helper digest and expanded symbol checks, and the
  strict source-retirement audit. No guest runtime validation, module-load
  validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-12: arch/x86_64/kernel/include/registers.h moved from
  88.3% to 95.0% for the source-retirement header audit. The x86 register
  compatibility header now declares xgetbv(), xsetbv(), wrmsr(), rdpmc(),
  rdmsr(), and ihk_mc_mb() instead of carrying their executable inline
  assembly and barrier bodies. Strict Rust exports those legacy ABI names from
  kernel/rust/x86_cpu_helpers.rs, preserving the same low/high register
  packing, MSR/XCR/PMC instruction operand mapping, and mfence compiler
  barrier behavior. The no-Rust fallback path keeps equivalent C definitions
  in arch/x86_64/kernel/cpu.c under the existing
  MCKERNEL_RUST_X86_CPU_HELPERS gate. The row remains partial because active
  static-initializer/perf code still depends on CVAL(), CVAL2(), and
  REGS_GET_STACK_POINTER() macro behavior. Validation passed for rustfmt,
  run_equivalence.sh shell syntax, focused executable-header scans showing
  only CVAL/CVAL2/REGS_GET_STACK_POINTER remain in registers.h, strict
  mckernel_rust_obj and mckernel.img builds, no-Rust fallback mckernel.img
  build, strict/fallback x86 instruction-helper symbol definition checks,
  linked-image unresolved-symbol checks, the full
  kernel/rust/tests/run_equivalence.sh harness with the x86 helper digest and
  new symbol checks, and the strict source-retirement audit. No guest runtime
  validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: arch/x86_64/kernel/include/ihk/context.h moved from
  78.7% to 100.0% for the source-retirement header audit. The x86 syscall
  context header now keeps only the context layouts and accessor prototypes;
  the executable syscall argument, return-value, syscall-number, PC, and SP
  field accessors are owned by Rust exports in kernel/rust/x86_cpu_helpers.rs.
  Assignment sites now use explicit ihk_mc_syscall_set_arg*() and
  ihk_mc_syscall_set_ret() calls instead of lvalue macros. The no-Rust fallback
  path keeps equivalent C helper definitions in arch/x86_64/kernel/cpu.c under
  the existing MCKERNEL_RUST_X86_CPU_HELPERS gate. The prior broad
  sched_helpers_c equivalence crash from the affinity slice was fixed by
  linking process_helpers_c.o into the generated sched fixture now that
  CPU_SET/CPU_ISSET are fallback symbols. Validation passed for rustfmt,
  run_equivalence.sh shell syntax, a no-hit executable-header scan on
  context.h, no remaining assignment-style syscall accessor uses, strict
  mckernel_rust_obj and mckernel.img builds, no-Rust fallback mckernel.img
  build, the expanded kernel/rust/tests/run_equivalence.sh x86 helper digest,
  strict/fallback syscall accessor symbol definition checks, linked-image
  unresolved-symbol checks, diff whitespace checks, and the strict
  source-retirement audit. No guest runtime validation, module-load validation,
  QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-12: kernel/include/affinity.h moved from 58.0% to 100.0%
  for the source-retirement header audit. The internal kernel affinity header
  now keeps the cpu_set_t layout, CPU_SETSIZE constant, and helper prototypes
  only; the remaining CPU_SET/CPU_ISSET/CPU_ZERO and _S executable macro
  bodies are owned by Rust exports from kernel/rust/affinity.rs in the strict
  build. The no-Rust fallback path keeps equivalent C helper definitions in
  kernel/process_helpers.c, which is excluded from the strict Rust build.
  Validation passed for a no-hit executable-header scan on affinity.h, strict
  mckernel_rust_obj and mckernel.img builds, no-Rust fallback mckernel.img
  build, strict/fallback CPU helper symbol definition checks, linked-image
  unresolved-symbol checks, and the strict source-retirement audit. The broad
  kernel/rust/tests/run_equivalence.sh harness was also attempted but failed
  in its generated sched_helpers_c binary with SIGSEGV; that failure is
  recorded in kernel.log and was not counted as passing validation for this
  checkpoint. No guest runtime validation, module-load validation, QEMU boot,
  host reboot, or reboot-capable script was run.
- 2026-06-12: kernel/include/affinity.h moved from 36.0% to 58.0%,
  executer/include/defs.h moved from 72.1% to 72.9%, and
  ihk/linux/include/ihk/ihklib_private.h moved from 73.7% to 82.0% for
  the source-retirement header audit. The internal kernel affinity header no
  longer carries the unused CPU_CLR/COUNT/EQUAL/AND/OR/XOR/ALLOC/FREE macro
  families or their private implementation macros; active kernel code keeps
  the CPU_SET/ISSET/ZERO compatibility surface it still uses. The private
  executer defs header dropped unused ATTRIBUTE_UNUSED, STR, BZERO/BCOPY/BNEG,
  BEEP, quoted-string/path helpers, and local roundup compatibility macros.
  The private ihklib header dropped unused Q()/QUOTE() helpers. Focused
  scans found no in-scope users for the deleted macros, and validation passed
  for strict mckernel.img, ihklib, mcexec, ihkmond, ihk_ko, mcctrl_ko, and
  ldump2mcdump builds plus the strict source-retirement audit. No guest
  runtime validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: kernel/include/kmalloc.h moved from 93.5% to 95.0% for
  the source-retirement header audit. The duplicate unlikely() compatibility
  macro was removed from kmalloc.h after confirming that the header includes
  ihk/mm.h first and ihk/mm.h already provides the same guarded fallback.
  The row remains partial because active code still depends on the executable
  kmalloc(), kfree(), and memcheck() file/line tracking macros. Validation for
  this checkpoint includes focused executable-header scans, strict Rust and
  C-fallback mckernel.img builds, the full Rust equivalence harness with
  kmalloc helper coverage, whitespace checks, and the strict
  source-retirement audit. No guest runtime validation, module-load
  validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-12: lib/include/ihk/debug.h moved from 42.0% to 72.0% for
  the source-retirement header audit. The unused STATIC_ASSERT() macro family
  and the private DDEBUG_SYMBOL()/DDEBUG_TEST helper macros were removed after
  focused non-test in-scope scans found no users outside the header. The
  dkprintf() macro now preserves the same per-callsite __verbose ddebug record
  directly, while ekprintf() and BUG_ON() remain unchanged. The row remains
  partial because active code still depends on the executable debug-print and
  BUG macro compatibility surface. Validation for this checkpoint includes
  focused no-hit scans for the deleted macro names, executable-header scans
  showing only dkprintf()/ekprintf()/BUG_ON() remain, strict Rust-object plus
  Rust and C-fallback mckernel.img builds, Rust-linked ihk/mcctrl module
  builds, the full Rust equivalence harness, whitespace checks, and the strict
  source-retirement audit. No guest runtime validation, module-load
  validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-12: arch/x86_64/kernel/include/registers.h moved from
  84.7% to 88.3% for the source-retirement header audit. The unused AMD
  perf-counter convenience inline wrappers set_perfctl(), start_perfctr(),
  stop_perfctr(), clear_perfctl(), set_perfctr(), and read_perfctr() were
  removed after a focused non-test in-scope scan found no users outside their
  header definitions. The row remains partial because active code still
  depends on the xgetbv()/xsetbv(), rdmsr()/wrmsr()/rdpmc(), memory-barrier,
  register accessor, and perf-event value macro compatibility surface.
  Validation for this checkpoint includes focused no-hit scans for the deleted
  helper names, strict Rust-object plus Rust and C-fallback mckernel.img
  builds, the full Rust equivalence harness with x86_cpu_helpers coverage,
  whitespace checks, and the strict source-retirement audit. No guest runtime
  validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: lib/include/list.h moved from 83.6% to 87.2% for the
  source-retirement header audit. The unused Linux-compatible iterator macro
  cluster was removed after a focused non-test in-scope scan found no users of
  __list_for_each(), list_for_each_prev(), list_for_each_prev_safe(),
  list_prepare_entry(), list_for_each_entry_continue(),
  list_for_each_entry_continue_reverse(), list_for_each_entry_from(),
  list_for_each_entry_safe_continue(), list_for_each_entry_safe_from(),
  list_for_each_entry_safe_reverse(), or list_safe_reset_next(). The header
  remains partial because active McKernel, IHK, mcctrl, and user-tool call
  sites still depend on LIST_HEAD(), list_entry(), list_first_entry(),
  list_for_each(), list_for_each_safe(), list_for_each_entry(),
  list_for_each_entry_reverse(), and list_for_each_entry_safe(), while C
  fallback inline bodies remain under the non-Rust list-helper path.
  Validation for this checkpoint includes focused no-hit scans for the deleted
  macro names, strict Rust and C-fallback mckernel.img builds, the full Rust
  equivalence harness with the stable list_helpers digest, whitespace checks,
  and the strict source-retirement audit. No guest runtime validation,
  module-load validation, QEMU boot, host reboot, or reboot-capable script was
  run.
- 2026-06-12: kernel/include/waitq.h moved from 56.2% to 100.0% for the
  source-retirement header audit. The header no longer carries executable
  waitqueue declaration/entry initializer macros or the unused wait_event()
  macro bodies. Call sites now initialize stack waitqueue entries through the
  exported waitq_init_entry() and waitq_init_locked_entry() helpers, with Rust
  and C-fallback helpers both publishing the same private pointer, function
  callback, zero flags, and initialized list head state. Validation for this
  checkpoint includes focused no-hit scans for the removed macro API names,
  strict Rust and C-fallback mckernel.img builds, the full Rust equivalence
  harness with the waitq digest covering both default and locked initializers,
  whitespace/script syntax checks, and the strict source-retirement audit. No
  guest runtime validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: kernel/include/rbtree.h moved from 92.2% to 97.4% for the
  source-retirement header audit. The unused rb_entry_safe() compatibility
  macro and the unused rbtree_postorder_for_each_entry_safe() typed postorder
  iterator macro were removed after a focused non-test in-scope scan found no
  users. The retained rbtree header still carries rb_entry() because active
  process, syscall, and host/IHK call sites depend on that typed container
  conversion. Validation for this checkpoint includes focused no-hit scans for
  the deleted macro names, strict Rust and C-fallback mckernel.img builds, the
  full Rust equivalence harness with the stable rbtree digest, whitespace
  checks, and the strict source-retirement audit. No guest runtime validation,
  module-load validation, QEMU boot, host reboot, or reboot-capable script was
  run.
- 2026-06-12: lib/include/ihk/mm.h moved from 88.6% to 94.0%
  for the source-retirement header audit. The header no longer carries the
  executable lockless page-cache bodies for ihk_mc_page_cache_free(),
  ihk_mc_page_cache_prealloc(), or ihk_mc_page_cache_alloc(). Strict Rust now
  exports those ABI names from kernel/rust/page_alloc.rs with the same
  compare-and-swap push/pop ordering, empty-cache preallocation path,
  allocator callback dispatch, and allocation-failure/retry logging shape.
  lib/page_alloc.c keeps matching C fallback bodies when the Rust page
  allocator path is disabled. The row remains partial because the allocation
  convenience macros still preserve call-site file/line behavior in the
  header. Validation for this checkpoint includes rustfmt, shell syntax and
  whitespace checks, focused mm.h executable-body scans, the expanded
  page_alloc_bitmap C-vs-Rust equivalence digest plus exported-symbol checks,
  strict Rust and C-fallback mckernel.img builds, and the source-retirement
  audit. No guest runtime validation, module-load validation, QEMU boot, host
  reboot, or reboot-capable script was run.
- 2026-06-12: kernel/include/plist.h moved from 88.0% to 100.0%
  for the source-retirement header audit. Earlier work had already moved the
  plist inline bodies behind out-of-line declarations; this checkpoint removes
  the final unused executable initializer and iterator/container macros after
  a full-tree use scan found no in-scope users of PLIST_HEAD_INIT(),
  PLIST_HEAD_INIT_RAW(), PLIST_NODE_INIT(), plist_for_each(),
  plist_for_each_safe(), plist_for_each_entry(),
  plist_for_each_entry_safe(), or plist_first_entry(). The retained surface is
  plist_head/plist_node layout plus function declarations. kernel/plist.c
  keeps the no-Rust fallback bodies, while kernel/rust/plist.rs owns the
  strict Rust path. Validation for this checkpoint includes focused no-hit
  scans for the deleted macro names and executable header patterns, whitespace
  checks, strict Rust and C-fallback mckernel.img builds, the full Rust
  equivalence harness, and the source-retirement audit. No guest runtime
  validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: kernel/include/llist.h moved from 93.1% to 100.0%
  for the source-retirement header audit. Earlier work had already moved the
  llist inline bodies behind out-of-line declarations; this checkpoint removes
  the final unused executable initializer, container, member-address, and
  iterator macros after a full-tree use scan found no in-scope users of
  LLIST_HEAD(), LLIST_HEAD_INIT(), llist_entry(),
  member_address_is_nonnull(), llist_for_each(), llist_for_each_safe(),
  llist_for_each_entry(), or llist_for_each_entry_safe(). The retained
  surface is the llist_head/llist_node layout plus function declarations, and
  kernel/llist.c keeps the remaining implementation body debt under its
  source row. Validation for this checkpoint includes focused no-hit scans for
  the deleted macro names and executable header patterns, whitespace checks,
  strict Rust and C-fallback mckernel.img builds, the full Rust equivalence
  harness, and the source-retirement audit. No guest runtime validation,
  module-load validation, QEMU boot, host reboot, or reboot-capable script was
  run.
- 2026-06-12: kernel/include/rbtree_augmented.h moved from 95.2% to
  100.0% for the source-retirement header audit. The final unused
  RB_DECLARE_CALLBACKS() executable callback-generator macro was removed from
  the header after a full-tree use scan found no in-tree generator users. The
  retained surface is now declaration-only: rb_augment_callbacks layout,
  color constants, parent/color helpers, child-change helper declarations,
  and augmented insert/erase declarations. kernel/rbtree.c continues to own
  the remaining C implementation body debt for rbtree mutation and callback
  dispatch under its source row. Validation for this checkpoint includes a
  focused no-hit scan for RB_DECLARE_CALLBACKS/static inline/function-like
  macros in the header, a full-tree use scan, whitespace checks, strict Rust
  and C-fallback mckernel.img builds, the full Rust equivalence harness, and
  the source-retirement audit. No guest runtime validation, module-load
  validation, QEMU boot, host reboot, or reboot-capable script was run.
- 2026-06-12: kernel/include/asm.h moved from 67.7% to 100.0%
  for the source-retirement header audit. The header no longer carries
  function-like x86 assembler selector or exception-table macros. The three
  remaining x86_64 futex fallback users in kernel/futex.c, kernel/syscall.c,
  and executer/kernel/mcctrl/futex.c now spell the concrete __ex_table
  records locally, so their C body debt remains accounted for in the source
  rows instead of being hidden in a shared header. Validation passed with a
  no-use scan for asm.h/_ASM_EXTABLE, focused git diff whitespace check,
  Rust and C-fallback mckernel.img builds, Rust and C-fallback mcctrl_ko
  builds, the full Rust equivalence harness, and the source-retirement audit.
  The mcctrl module builds still emit the pre-existing non-fatal objtool
  __ex_table-size warning for this old-style inline assembly encoding. No
  module-load, QEMU, or reboot validation was run for this checkpoint.
- 2026-06-12: arch/x86_64/kernel/include/arch-memory.h moved from
  78.6% to 100.0% for the source-retirement header audit. The header now
  exposes declarations only for the remaining executable helpers:
  STACK_TOP(), pagemap status/shift/frame shaping, x86 PTE classification,
  physical/off/attribute extraction, file-offset/null PTE construction,
  dirty-bit atomics, pte_xchg(), contiguous-page no-op helpers, page-size
  level/shift conversion, allocation-size adjustment, and the FUGAKU
  ALIGN_UP()/ALIGN_DOWN() arithmetic. Strict Rust exports the legacy ABI names
  from kernel/rust/pte_helpers.rs and owns the arithmetic, atomic exchange,
  atomic dirty-bit mutation, page-size dispatch, file-offset attribute
  shaping, and alignment rounding. arch/x86_64/kernel/memory.c keeps matching
  no-Rust fallback definitions outside the strict helper path. Validation
  passed for rustfmt, bash -n for the equivalence harness, focused
  executable-header scan, whitespace checks, full C-vs-Rust equivalence with
  the expanded pte_helpers digest and exported-symbol checks, strict Rust and
  C-fallback mckernel.img builds, and the source-retirement audit. Two harness
  link repairs and one corrected PTE test expectation are recorded in
  kernel.log. No guest runtime validation, module-load validation, QEMU boot,
  host reboot, or reboot-capable script was run.
- 2026-06-12: kernel/include/tofu/tof_icc.h moved from 92.0% to
  100.0% for the source-retirement header audit. The remaining Tofu ICC
  executable arithmetic macros now live behind declarations instead of header
  bodies: GENMASK(), TLP/frame length calculations, queue size/length helpers,
  MB page-size encoding, register physical-address helpers, XB cycle-count
  offsets, and Tofu TNI/TD register-offset helpers. Strict Rust exports the
  legacy callable names from kernel/rust/tofu_uapi.rs and owns the mask
  shaping, descriptor-size arithmetic, page-size encode branch, register base
  arithmetic, and offset return shaping through expanded C-vs-Rust tofu_uapi
  smoke coverage. kernel/tofu/tof_utofu_main.c keeps matching no-Rust fallback
  definitions beside the existing TOF_RSC_* fallback helpers. Validation
  passed for rustfmt, bash -n for the equivalence harness, focused
  executable-header scan, whitespace checks, strict Rust and C-fallback
  mckernel.img builds, full C-vs-Rust equivalence with the expanded tofu_uapi
  digest and exported-symbol checks, and the source-retirement audit. An
  earlier tofu_uapi C-smoke assertion typo aborted during validation and is
  recorded in kernel.log; the corrected harness passes. No guest runtime
  validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: arch/x86_64/kernel/include/arch-futex.h moved from
  78.5% to 100.0% for the source-retirement header audit. The x86 futex
  atomic header now contains declarations only; strict Rust exports the
  legacy futex_atomic_cmpxchg_inatomic() and futex_atomic_op_inuser() ABI
  names from kernel/rust/futex.rs and owns access-check dispatch, encoded
  operation decode, shifted-argument normalization, ANDN argument inversion,
  primitive bridge selection, signed compare return shaping, and error
  shaping. executer/kernel/mcctrl/rust/mcctrl_helpers.rs exports matching
  module helper ABI names for the mcctrl path. kernel/syscall.c,
  kernel/futex.c, and executer/kernel/mcctrl/futex.c keep the x86 fault-table
  asm only as strict primitive bridges or no-Rust fallback bodies. Validation
  passed for rustfmt, bash -n for the equivalence harness, strict Rust and
  C-fallback mckernel.img builds, strict Rust and C-fallback mcctrl_ko builds,
  full C-vs-Rust equivalence with the new arch_futex digest and expanded
  ihk_module_helpers futex atomic smoke coverage, strict Rust-object and
  module-object symbol/undefined/SIMD checks, whitespace checks, focused
  executable-header scan, and the source-retirement audit. No guest runtime
  validation, module-load validation, QEMU boot, host reboot, or
  reboot-capable script was run.
- 2026-06-12: kernel/include/tofu/tof_icc.h moved from 89.7% to
  92.0% for the source-retirement header audit. The Tofu frame-length helper
  tof_icc_get_framelen() now lives behind a declaration instead of an
  executable static inline body. Strict Rust exports the legacy helper name
  from kernel/rust/tofu_uapi.rs and owns the unsigned round-up arithmetic,
  RH/ECRC adjustment, minimum-frame clamp, and return shaping through focused
  C-vs-Rust smoke coverage. kernel/tofu/tof_utofu_main.c keeps a matching
  no-Rust fallback definition beside the existing TOF_RSC_* fallback helpers.
  Validation passed for rustfmt, bash -n for the equivalence harness, strict
  Rust and C-fallback mckernel.img builds, full C-vs-Rust equivalence with
  the new tofu_uapi smoke, strict Rust-object symbol checks, whitespace
  checks, and the source-retirement audit. The Tofu compatibility constants
  and size/bit macros remain header debt, so the row stays partial.
- 2026-06-12: kernel/include/kmalloc.h moved from 86.4% to 93.5% for
  the source-retirement header audit. The lockless kmalloc cache helpers now
  live behind declarations instead of executable inline bodies:
  kmalloc_cache_free(), kmalloc_cache_prealloc(), and kmalloc_cache_alloc().
  Strict Rust exports the legacy wrapper names from kernel/rust/mem_helpers.rs
  and owns cache push/pop CAS sequencing, cache-pointer publication,
  empty-cache preallocation dispatch, no-cache warning routing, allocation
  failure logging, and result shaping through equivalence-tested
  kmalloc_cache_*_result() helpers. kernel/mem.c keeps matching C fallback
  definitions outside the strict Rust helper path and provides only narrow
  strict C callbacks for kmalloc allocation and exact legacy log strings.
  Validation passed for rustfmt, bash -n for the equivalence harness, strict
  Rust and C-fallback mckernel.img builds, full C-vs-Rust equivalence with
  expanded kmalloc cache coverage, strict Rust-object symbol checks, the
  updated 148-symbol Rust-object unresolved-boundary audit, whitespace checks,
  and the source-retirement audit. The source-location kmalloc/kfree/memcheck
  macros remain header debt, so the row stays partial.
- 2026-06-12: kernel/include/xpmem_private.h moved from 97.2% to
  100.0% for the source-retirement header audit. The final XPMEM private
  lookup and private-data helpers now live behind declarations instead of
  executable inline/macro bodies: xpmem_tg_ref_by_tgid(),
  xpmem_tg_ref_by_tgid_all(), xpmem_tg_ref_by_tgid_nolock(),
  xpmem_tg_ref_by_tgid_all_nolock(), and xpmem_is_private_data().
  Strict Rust exports the legacy wrapper names from
  kernel/rust/xpmem_helpers.rs and owns tgid hash selection, optional
  reader-lock dispatch, nolock lookup handoff, lookup debug-log sequencing,
  result shaping, and private-data offset checks through equivalence-tested
  xpmem_tg_ref_by_tgid_wrapper_result() and
  xpmem_is_private_data_result() helpers. kernel/xpmem.c keeps matching
  fallback definitions outside the strict Rust helper path and provides only
  narrow strict C callbacks/offsets for the remaining lock, refcount, log, and
  layout primitives. Validation passed for rustfmt, bash -n for the
  equivalence harness, strict Rust and C-fallback mckernel.img builds, full
  C-vs-Rust equivalence with expanded XPMEM lookup/private-data coverage,
  strict Rust-object and C-fallback object symbol checks, strict C bridge
  symbol checks, linked-image unresolved-symbol checks, focused header
  executable-logic scan, and the source-retirement audit.
- 2026-06-13: kernel/xpmem.c moved from 0.0% to 100.0% for the
  source-retirement audit. Strict Rust now exports the remaining public
  xpmem_open()/xpmem_openat() entry points and owns the final XPMEM
  open/ioctl/lifecycle wrapper layer in kernel/rust/xpmem_helpers.rs,
  including partition init/exit, thread-group open, mckfd callback
  publication, ioctl command dispatch, make/remove/get/release,
  attach/detach, flush, segment/AP/attachment cleanup, clear-PTE, unpin, and
  process-range free callback sequencing. kernel/xpmem.c remains available to
  C fallback builds, but it is removed from the strict Rust mckernel.img
  source list.
- 2026-06-12: kernel/include/xpmem_private.h moved from 95.2% to
  97.2% for the source-retirement header audit. The XPMEM private
  not-destroyable and refcount increment wrappers now live behind declarations
  instead of executable inline bodies: xpmem_tg_not_destroyable(),
  xpmem_seg_not_destroyable(), xpmem_ap_not_destroyable(),
  xpmem_att_not_destroyable(), xpmem_tg_ref(), xpmem_seg_ref(),
  xpmem_ap_ref(), and xpmem_att_ref(). Strict Rust exports the legacy wrapper
  names from kernel/rust/xpmem_helpers.rs and owns refcount pointer selection,
  atomic set/read/log sequencing, read/bug/inc sequencing, and invalid-input
  error shaping through equivalence-tested xpmem_not_destroyable_wrapper_result()
  and xpmem_ref_wrapper_result() helpers. kernel/xpmem.c keeps matching
  fallback definitions outside the strict Rust helper path and provides only
  narrow strict C callbacks for refcnt field-address, atomic, bug, and log
  primitives. Validation passed for rustfmt, bash -n for the equivalence
  harness, strict Rust and C-fallback mckernel.img builds, full C-vs-Rust
  equivalence with expanded XPMEM refcount wrapper coverage, strict Rust-object
  and C-fallback object symbol checks, strict C bridge symbol checks,
  linked-image unresolved-symbol checks, focused header executable-logic scan,
  and the source-retirement audit.
- 2026-06-12: kernel/include/xpmem_private.h moved from 94.0% to
  95.2% for the source-retirement header audit. The XPMEM private
  destroyable lifecycle wrappers now live behind declarations instead of
  executable inline bodies: xpmem_tg_destroyable(),
  xpmem_seg_destroyable(), xpmem_ap_destroyable(), and
  xpmem_att_destroyable(). Strict Rust exports the legacy wrapper names from
  kernel/rust/xpmem_helpers.rs and owns the call-log, deref-dispatch, and
  return-log sequencing through an equivalence-tested
  xpmem_destroyable_wrapper_result() helper. kernel/xpmem.c keeps matching
  fallback definitions outside the strict Rust helper path and provides only
  narrow strict C callbacks for the private deref/log primitives that remain
  C-owned. Validation passed for rustfmt, bash -n for the equivalence harness,
  strict Rust and C-fallback mckernel.img builds, full C-vs-Rust equivalence
  with expanded XPMEM destroyable sequencing coverage, strict Rust-object and
  C-fallback object symbol checks, strict C bridge symbol checks, linked-image
  unresolved-symbol checks, focused header executable-logic scan, and the
  source-retirement audit.
- 2026-06-12: kernel/include/xpmem_private.h moved from 92.7% to
  94.0% for the source-retirement header audit. The private ID/hash wrapper
  bodies now live behind declarations instead of executable inline bodies:
  xpmem_segid_to_tgid(), xpmem_apid_to_tgid(),
  xpmem_tg_hashtable_index(), and xpmem_ap_hashtable_index(). Strict Rust
  exports the legacy wrapper names from kernel/rust/xpmem_helpers.rs,
  preserving the old ID validation and hash debug sequencing through C
  panic/log bridge callbacks, while kernel/xpmem.c keeps matching fallback
  definitions outside the strict Rust helper path. Validation passed for
  rustfmt, bash -n for the equivalence harness, strict Rust and C-fallback
  mckernel.img builds, strict Rust-object and C-fallback object symbol checks,
  strict C bridge symbol checks, linked-image unresolved-symbol checks,
  focused header executable-logic scan, and full C-vs-Rust equivalence with
  expanded XPMEM wrapper parity coverage.
- 2026-06-12: kernel/include/rusage_private.h moved from 97.2% to
  100.0% for the source-retirement header audit. The final executable rusage
  memory-stat header helpers now live behind declarations instead of inline
  bodies: rusage_memory_stat_add() and
  rusage_memory_stat_add_with_page(). Strict Rust exports the resident-range
  skip gates, anonymous/pre-paging RSS accounting, dev/premap/XPMEM skip gates,
  zero-object RSS accounting, delayed phys_to_page() lookup ordering, COW
  RSS accounting for regular and hugetlb mappings, missing-page warning
  routing, mapped-page compare-exchange first-map detection, and SHM versus
  mapped-file counter dispatch from kernel/rust/mem_helpers.rs, while
  kernel/mem.c keeps matching C fallback definitions outside the strict Rust
  helper path. Validation passed for rustfmt, bash -n for the equivalence
  harness, strict Rust and C-fallback mckernel.img builds, strict Rust-object
  and C-fallback object symbol checks, strict object-set unresolved-reference
  inspection, linked-image unresolved-symbol checks, focused header
  executable-logic scan, and full C-vs-Rust equivalence with expanded
  rusage_private digest coverage for early skip/no-lookup paths, anonymous
  RSS accounting, COW accounting, hugetlb with-page warning behavior,
  SHM mapped-page accounting, regular mapped-file accounting, mapped-counter
  already-set behavior, and with-page no-lookup behavior.
- 2026-06-12: kernel/include/rusage_private.h moved from 96.0% to
  97.2% for the source-retirement header audit. The page-level rusage
  accounting helpers now live behind declarations instead of executable inline
  bodies: rusage_page_add() and rusage_page_sub(). Strict Rust exports the
  page-count to byte conversion, user versus kernel accounting dispatch,
  total-memory usage add/sub, total-memory max-usage compare-exchange update,
  and user/kernel subtract ordering from kernel/rust/mem_helpers.rs, while
  kernel/mem.c keeps matching C fallback definitions outside the strict Rust
  helper path. Validation passed for rustfmt, bash -n for the equivalence
  harness, strict Rust and C-fallback mckernel.img builds, strict Rust-object
  and C-fallback object symbol checks, strict object-set unresolved-reference
  inspection, linked-image unresolved-symbol checks, focused header
  executable-logic scan, and full C-vs-Rust equivalence with expanded
  rusage_private digest coverage for user page add/sub, kernel page add/sub,
  total-memory high-watermark behavior, NUMA dispatch, and kmem dispatch.
- 2026-06-12: kernel/include/rusage_private.h moved from 94.4% to
  96.0% for the source-retirement header audit. The RSS and NUMA accounting
  helpers now live behind declarations instead of executable inline bodies:
  rusage_rss_add(), rusage_rss_sub(), rusage_numa_add(), and
  rusage_numa_sub(). Strict Rust exports the global RSS current/max update,
  current-thread versus on-fork VM selection, per-VM currss mutation,
  process maxrss publication, NUMA counter add/sub ordering, and
  RSS-before-NUMA subtract ordering from kernel/rust/mem_helpers.rs, while
  kernel/mem.c keeps matching C fallback definitions outside the strict Rust
  helper path. Validation passed for rustfmt, bash -n for the equivalence
  harness, strict Rust and C-fallback mckernel.img builds, focused header
  executable-logic scan, and full C-vs-Rust equivalence with expanded
  rusage_private digest coverage for current VM, on-fork VM, subtract, and
  NUMA add/sub behavior. Two fixture failures during this slice were logged
  in kernel.log before the rusage_private fixture was corrected to provide a
  full strong rusage object and to model Rust-owned get_this_cpu_local_var()
  through the clv CPU-local base pointer.
- 2026-06-12: kernel/include/rusage_private.h moved from 94.0% to
  94.4% for the source-retirement header audit. The memory-stat decrement
  selector now lives behind a declaration instead of an executable inline
  body: rusage_memory_stat_sub(). Strict Rust exports the MF_SHM versus
  mapped-file dispatch from kernel/rust/mem_helpers.rs, reusing the already
  retired RSS and mapped-file counter helpers, while kernel/mem.c keeps
  matching C fallback definitions outside the strict Rust helper path.
  Validation passed for rustfmt, bash -n for the equivalence harness, strict
  Rust and C-fallback mckernel.img builds, strict Rust-object and C-fallback
  object symbol checks, strict object-set unresolved-reference inspection,
  linked-image unresolved-symbol checks, focused header executable-logic scan,
  and full C-vs-Rust equivalence with expanded rusage_private digest coverage
  for SHM, non-SHM, and mixed-flag memory-stat decrement dispatch.
- 2026-06-12: kernel/include/rusage_private.h moved from 93.4% to
  94.0% for the source-retirement header audit. The OOM and overmap policy
  helpers now live behind declarations instead of executable inline bodies:
  rusage_check_oom() and rusage_check_overmap(). Strict Rust exports the
  global rusage threshold arithmetic, OOM event/log dispatch, user-allocation
  -ENOMEM shaping, overcommit bypass, and overmap page-count comparison from
  kernel/rust/mem_helpers.rs, while kernel/mem.c keeps matching C fallback
  definitions outside the strict Rust helper path. Validation passed for
  rustfmt, bash -n for the equivalence harness, strict Rust and C-fallback
  mckernel.img builds, strict Rust-object and C-fallback object symbol checks,
  strict object-set unresolved-reference inspection, linked-image
  unresolved-symbol checks, focused header executable-logic scan, and full
  C-vs-Rust equivalence with expanded rusage_private digest coverage for
  OOM event/log/return behavior and overmap decisions. Two equivalence
  harness failures during this slice were logged in kernel.log before the
  harness stubs and unresolved-symbol allowlist were corrected.
- 2026-06-12: kernel/include/rusage_private.h moved from 92.4% to
  93.4% for the source-retirement header audit. The kernel-memory usage and
  thread-count accounting helpers now live behind declarations instead of
  executable inline bodies: rusage_kmem_add(), rusage_kmem_sub(),
  rusage_num_threads_inc(), and rusage_num_threads_dec(). Strict Rust exports
  the atomic add/sub and max-watermark compare-exchange sequencing from
  kernel/rust/mem_helpers.rs, while kernel/mem.c keeps matching C fallback
  definitions outside the strict Rust helper path. Validation passed for
  rustfmt, bash -n for the equivalence harness, strict Rust and C-fallback
  mckernel.img builds, strict Rust-object and C-fallback object symbol checks,
  strict object-set unresolved-reference inspection, linked-image
  unresolved-symbol checks, focused header executable-logic scan, and full
  C-vs-Rust equivalence with expanded rusage_private digest coverage for
  kmem high-watermark and thread high-watermark updates.
- 2026-06-12: kernel/include/rusage_private.h moved from 91.2% to
  92.4% for the source-retirement header audit. The RSS and mapped-file
  memory-stat page-size counter helpers now live behind declarations instead
  of executable inline bodies: memory_stat_rss_add(),
  memory_stat_rss_sub(), rusage_memory_stat_mapped_file_add(), and
  rusage_memory_stat_mapped_file_sub(). Strict Rust exports the atomic counter
  updates from kernel/rust/mem_helpers.rs using the same page-size classifier
  and sequentially consistent long-add semantics as the old ihk_atomic helper
  path, while kernel/mem.c keeps matching C fallback definitions outside the
  strict Rust helper path. Validation passed for rustfmt, bash -n for the
  equivalence harness, strict Rust and C-fallback mckernel.img builds, strict
  Rust-object and C-fallback object symbol checks, strict object-set
  unresolved-reference inspection, linked-image unresolved-symbol checks,
  focused header executable-logic scan, and full C-vs-Rust equivalence with
  expanded rusage_private digest coverage for RSS and mapped-file counters.
- 2026-06-12: kernel/include/rusage_private.h moved from 89.9% to
  91.2% for the source-retirement header audit. The header now declares the
  rusage page-size classifier and total/free/usage memory helpers instead of
  carrying those executable inline bodies. Strict Rust exports
  rusage_pgsize_to_pgtype(), rusage_total_memory_add(),
  rusage_get_total_memory(), rusage_get_free_memory(), and
  rusage_get_usage_memory() from kernel/rust/mem_helpers.rs, preserving
  ENABLE_RUSAGE and disabled-rusage behavior through Rust cfg selection.
  kernel/mem.c keeps matching C fallback definitions outside the strict Rust
  helper path. Validation passed for rustfmt, bash -n for the equivalence
  harness, strict Rust and C-fallback mckernel.img builds, strict/fallback
  object symbol checks, linked-image unresolved-symbol checks, focused header
  executable-logic scan, and full C-vs-Rust equivalence including the new
  rusage_private digest coverage.
- 2026-06-12: kernel/include/mc_jhash.h moved from 83.1% to
  100.0% for the source-retirement header audit. The hash compatibility
  header now declares mc_jhash2() instead of carrying the executable
  __mc_jhash_mix() macro and static inline mc_jhash2() body. Strict Rust
  exports mc_jhash2() from kernel/rust/futex.rs for kernel futex callers and
  from executer/kernel/mcctrl/rust/mcctrl_helpers.rs for mcctrl callers, while
  kernel/futex.c and executer/kernel/mcctrl/futex.c keep matching C fallback
  definitions outside the strict Rust paths. Validation passed for rustfmt,
  bash -n for the equivalence harness, strict Rust and C-fallback
  mckernel.img builds, Rust-enabled and no-Rust mcctrl_ko builds, strict
  Rust-object and C-fallback object symbol checks for mc_jhash2(), linked
  kernel/module unresolved-symbol checks, focused mc_jhash header
  executable-logic scan, full C-vs-Rust equivalence including the new jhash
  digest coverage, and the strict source-retirement audit.
- 2026-06-12: kernel/include/rbtree_augmented.h moved from 79.1% to
  95.2% for the source-retirement header audit. The augmented rbtree
  compatibility header now declares rb_insert_augmented(),
  rb_erase_augmented(), __rb_erase_augmented(), the parent/color query
  helpers, rb_set_parent(), rb_set_parent_color(), and __rb_change_child()
  instead of carrying those executable wrapper, scalar-helper, child-link, and
  augmented erase bodies. Strict Rust exports those ABI names from
  kernel/rust/rbtree.rs by reusing the existing Rust-owned rbtree insert,
  erase-color, augmented erase, parent/color, and child-change sequencing,
  while kernel/rbtree.c keeps matching C fallback definitions for no-Rust
  builds. The row remains partial because RB_DECLARE_CALLBACKS still generates
  type-specific propagate/copy/rotate callbacks at include sites and needs a
  separate generic-macro strategy. Validation passed for rustfmt, strict Rust
  and C-fallback mckernel.img builds, Rust-object and C-fallback object symbol
  checks for the augmented helper/export set, strict/fallback linked-image
  unresolved-symbol checks, bash -n for the equivalence harness, focused
  rbtree_augmented header executable-logic scan, full C-vs-Rust equivalence
  including the new augmented insert/erase and scalar helper coverage, and the
  strict source-retirement audit.
- 2026-06-12: kernel/include/plist.h moved from 82.9% to 88.0% for the
  source-retirement header audit. The header no longer carries the executable
  dynamic bodies for plist_head_init(), plist_head_init_raw(),
  plist_node_init(), plist_head_empty(), plist_node_empty(), or plist_first().
  Strict Rust exports those ABI names from kernel/rust/plist.rs using the same
  list-head initialization, priority publication, empty query, and first-node
  lookup semantics as the existing Rust-owned plist add/delete implementation.
  kernel/plist.c keeps matching C fallback bodies outside
  MCKERNEL_RUST_PLIST_CORE for no-Rust builds. The row remains partial because
  static initializer and iterator/container macros still live in the header
  and need a separate macro-erasure strategy. Validation passed for rustfmt,
  strict Rust and C-fallback mckernel.img builds, Rust-object and C-fallback
  object symbol checks for all plist exported helper names, strict/fallback
  linked-image unresolved-symbol checks, bash -n for the equivalence harness,
  focused plist header executable-logic scan, full C-vs-Rust equivalence
  including the new exported plist initializer/query calls, and the strict
  source-retirement audit.
- 2026-06-12: kernel/include/rbtree.h moved from 79.3% to 92.2% for the
  source-retirement header audit. The public rbtree compatibility header now
  declares rb_parent(), rb_empty_root(), rb_empty_node(), rb_clear_node(), and
  rb_link_node() instead of carrying their executable parent-mask,
  empty-root/node, clear-node, and link-node bodies. Strict Rust exports those
  ABI names from kernel/rust/rbtree.rs, while kernel/rbtree.c keeps matching C
  fallback definitions for no-Rust builds. RB_EMPTY_ROOT, RB_EMPTY_NODE, and
  RB_CLEAR_NODE remain as non-function-like compatibility aliases to the ABI
  helpers. The row remains partial because rb_entry(), rb_entry_safe(), and the
  postorder container iterator macro still needed a separate generic-macro
  strategy at this checkpoint. Validation passed for rustfmt, strict Rust and C-fallback
  mckernel.img builds, Rust-object and C-fallback object symbol checks for the
  rbtree exported helper set, strict/fallback linked-image unresolved-symbol
  checks, bash -n for the equivalence harness, focused rbtree header
  executable-logic scan, full C-vs-Rust equivalence including the new rbtree
  helper calls, and the strict source-retirement audit.
- 2026-06-12: arch/x86_64/kernel/include/arch/cpu.h moved from
  48.8% to 53.9% for the source-retirement header audit. The x86 CPU
  compatibility header now declares read_tsc() instead of carrying the
  executable rdtsc inline body. Strict Rust exports read_tsc() from
  kernel/rust/x86_cpu_helpers.rs by reusing the Rust-owned rdtsc()
  implementation, while arch/x86_64/kernel/cpu.c keeps the matching C fallback
  definition outside MCKERNEL_RUST_X86_CPU_HELPERS. The row remains partial
  because the memory-barrier and typed acquire/release macros still live in the
  header and need a separate primitive-coverage strategy. Validation passed for
  rustfmt, focused header scan confirming read_tsc() is declaration-only,
  strict Rust and C-fallback mckernel.img builds, strict Rust-object and
  C-fallback object symbol checks for read_tsc()/rdtsc(), strict and fallback
  linked-image unresolved-symbol checks, and the strict source-retirement audit.
- 2026-06-12: ihk/linux/include/ihk/arch/x86_64/ihk/rdtsc.h moved
  from 90.9% to 100.0% for the source-retirement header audit. The installed
  x86_64 host rdtsc compatibility header now declares rdtsc() instead of
  carrying the executable inline assembly body. The strict Rust maccess body
  exports rdtsc() from ihk/linux/user/rust/maccess.rs with the same rdtsc
  instruction and high/low-word combination, while ihk/linux/user/maccess.c
  keeps the matching C fallback definition for ENABLE_RUST_USER_TOOLS=OFF.
  Validation passed for rustfmt, focused executable-header scan showing the
  rdtsc header no longer has inline or function-like macro hits, strict Rust
  and no-Rust fallback maccess builds, strict link evidence showing only
  rust/maccess.o, Rust-object and C-fallback object rdtsc/main symbol checks,
  strict and fallback maccess CLI smokes preserving the pointer/time output
  shape and legacy exit status, and the strict source-retirement audit with
  compiled retired C sources still at 0.
- 2026-06-12: ihk/linux/user/pagetable.c, ihk/linux/user/memdump.c,
  ihk/linux/user/memdump2.c, and ihk/linux/user/memdump-mic.c moved to
  100.0% for the strict Rust source-retirement audit. Rust-enabled CMake now
  builds the pagetable, memdump, memdump2, and memdump-mic executables from
  their Rust bodies under ihk/linux/user/rust/ and omits the corresponding C
  files from those strict user-tool targets, while ENABLE_RUST_USER_TOOLS=OFF
  keeps the matching C fallback builds. The Rust bodies export the legacy main
  ABI names and own pagetable index/output formatting, memdump hex/decimal
  offset parsing, /dev/mem and /dev/kmem open/lseek/read/mmap sequencing,
  /dev/mcdN pread sequencing, dump header formatting, byte-line formatting,
  perror/error return behavior, and usage text. Validation passed for rustfmt,
  strict Rust builds, strict build-graph absence of the four C files, Rust
  object main symbol evidence, no-Rust C fallback builds, exact Rust-vs-C CLI
  parity smokes for usage, pagetable arithmetic output, and safe missing-device
  error paths, plus the strict source-retirement audit with compiled retired C
  sources still at 0.
- 2026-06-12: ihk/linux/user/maccess.c moved to 100.0% for the
  strict Rust source-retirement audit. Rust-enabled CMake now builds the
  maccess executable from ihk/linux/user/rust/maccess.rs and omits
  ihk/linux/user/maccess.c from that strict user-tool target, while the
  fallback C source remains available when ENABLE_RUST_USER_TOOLS is disabled.
  The Rust body exports the legacy main ABI name and owns the original memory
  access timing utility behavior: 256 MiB allocation and zeroing, 4 KiB pointer
  alignment, rdtsc-bracketed volatile unsigned-long sampling every 32 bytes,
  matching pointer/timing output shape, and the original exit status. Validation
  passed for rustfmt, strict Rust maccess build, strict build-graph absence of
  maccess.c, Rust-object main symbol evidence, no-Rust C fallback maccess build,
  Rust/C CLI smoke checks for output shape and exit status, and the strict
  source-retirement audit with compiled retired C sources still at 0.
- 2026-06-12: executer/kernel/mcctrl/include/mcctrl_rust.h moved from
  90.8% to 100.0% for the source-retirement header audit. The mcctrl Rust
  compatibility header is now declaration-only for this audit: the remaining
  executable static wrapper bodies for mapping lookup, sequential bitset fill,
  read-buffer status, long parsing, path predicates, pointer/hash matching,
  pidfd matching, Tofu path helpers, sysfs path errors, binfmt skip policy,
  pager path policy, filesystem predicates, device classification, decimal
  formatting, and futex command/label helpers were removed from the header.
  Strict Rust exports the legacy ABI names from
  executer/kernel/mcctrl/rust/mcctrl_helpers.rs through the existing
  Rust-owned result helpers, while executer/kernel/mcctrl/control.c keeps
  matching C fallback bodies outside MCCTRL_RUST_HELPERS. Validation passed
  for rustfmt, bash syntax checking of the equivalence harness, focused
  executable-header scan showing static-inline hits reduced from 62 to 0 and
  function-like macro hits at 0, strict Rust and no-Rust mcctrl_ko builds,
  strict Rust-object and C-fallback object symbol definition checks for the
  remaining wrapper batch, strict/no-Rust mcctrl.ko unresolved-symbol checks,
  the full equivalence harness, and the strict source-retirement audit with
  executable-header rows reduced by one and compiled retired C sources still
  at 0.
- 2026-06-12: executer/kernel/mcctrl/include/mcctrl_rust.h moved from
  87.5% to 90.8% for the source-retirement header audit. The mcctrl Rust
  compatibility header no longer carries executable static wrapper bodies for
  mcctrl_ikc_free_addrs_owner(), mcctrl_ikc_desc_free_at_put(),
  mcctrl_ikc_wait_mode(), mcctrl_ikc_busy_timeout_msecs(),
  mcctrl_ikc_wait_abort_return(), mcctrl_ikc_release_packet_after_handler(),
  mcctrl_ikc_cpu_nonnegative(), mcctrl_ikc_cpu_index_valid(),
  mcctrl_ikc_linux_cpu_valid(), mcctrl_ikc_init_uses_last_channel(), or
  mcctrl_ikc_cpu_count_valid(). Strict Rust exports the legacy ABI names from
  executer/kernel/mcctrl/rust/mcctrl_helpers.rs through the existing
  Rust-owned result helpers, while executer/kernel/mcctrl/control.c keeps
  matching C fallback bodies outside MCCTRL_RUST_HELPERS. The row remains
  partial because additional executable wrappers for mapping lookup, bitset,
  read/parse, path, Tofu, sysfs, pager, filesystem, formatting, and futex
  helper policies still live in the header. Validation passed for rustfmt,
  bash syntax checking of the equivalence harness, focused executable-header
  scan showing static-inline hits reduced from 84 to 62, strict Rust and
  no-Rust mcctrl_ko builds, strict Rust-object and C-fallback object symbol
  definition checks for the IKC wrapper batch, strict/no-Rust mcctrl.ko
  unresolved-symbol checks, the full equivalence harness, and the strict
  source-retirement audit.
- 2026-06-12: executer/kernel/mcctrl/include/mcctrl_rust.h moved from
  84.5% to 87.5% for the source-retirement header audit. The mcctrl Rust
  compatibility header no longer carries the executable static wrapper bodies
  for mcctrl_align_wait_buf(), mcctrl_partition_list_evict(),
  mcctrl_partition_count_mismatch(), mcctrl_partition_join_allowed(),
  mcctrl_partition_last_process(), mcctrl_partition_wait_required(),
  mcctrl_partition_wait_timeout_msecs(), mcctrl_partition_wake_next(),
  mcctrl_release_user_space_len(), or
  mcctrl_control_request_needs_root(). Strict Rust exports the legacy ABI
  names from executer/kernel/mcctrl/rust/mcctrl_helpers.rs through the
  existing Rust-owned result helpers, while executer/kernel/mcctrl/control.c
  keeps matching C fallback bodies outside MCCTRL_RUST_HELPERS. The row
  remains partial because the header still carries additional executable
  inline wrappers for IKC, sysfs, futex, pidfd, Tofu, and other mcctrl helper
  policies. Validation passed for rustfmt, bash syntax checking of the
  equivalence harness, focused executable-header scan showing the wrapper
  count reduction, strict Rust and no-Rust mcctrl_ko builds, strict Rust-object
  and C-fallback object symbol definition checks for the wrapper batch,
  strict/no-Rust mcctrl.ko unresolved-symbol checks, the full equivalence
  harness, and the strict source-retirement audit. Earlier symbol-check path
  and pipeline false-negative failures were recorded in kernel.log before the
  passing rerun.
- 2026-06-12: arch/x86_64/kernel/include/registers.h moved from
  83.7% to 84.7% for the source-retirement header audit. The x86 register
  compatibility header no longer carries the executable static body for
  rdtsc(); it now declares the ABI. Strict Rust exports rdtsc() from
  kernel/rust/x86_cpu_helpers.rs with the same rdtsc instruction and
  high/low-word combination, while arch/x86_64/kernel/cpu.c keeps the matching
  C fallback body outside MCKERNEL_RUST_X86_CPU_HELPERS. The row remains
  partial because the xgetbv/xsetbv, rdmsr/wrmsr/rdpmc, perf-counter helper,
  memory-barrier, register accessor, and perf-event value macros still live in
  the header. Validation passed for rustfmt, bash syntax checking of the
  equivalence harness, focused executable-header scan confirming rdtsc() was
  removed, strict Rust and C-fallback mckernel.img builds, strict Rust-object
  and C-fallback object symbol checks for rdtsc(), strict/fallback
  linked-image unresolved-symbol checks, the full equivalence harness, and the
  strict source-retirement audit.
- 2026-06-12: lib/include/bitops.h moved from 62.5% to 65.6%
  for the source-retirement header audit. The shared bitops header no longer
  carries the executable static body for hweight_long(); it now declares the
  ABI. Strict Rust exports hweight_long() from kernel/rust/bitops.rs using the
  Rust-owned __sw_hweight64() helper on the active x86_64 unsigned-long path,
  while lib/bitops.c keeps the matching C fallback body for non-Rust builds.
  The row remains partial because BITS_TO_LONGS/DECLARE_BITMAP,
  for_each_set_bit(), BIT/BIT_WORD, and ALIGN/IS_ALIGNED function-like macros
  still live in the header and require a broader source-level compatibility
  strategy. Validation passed for rustfmt, bash syntax checking of the
  equivalence harness, strict Rust and C-fallback mckernel.img builds, strict
  Rust-object and C-fallback object symbol definition checks for
  hweight_long(), strict/fallback linked-image unresolved-symbol checks, the
  full equivalence harness with direct bitops and bitops-header hweight_long()
  coverage, and the strict source-retirement audit.
- 2026-06-12: kernel/include/cls.h moved from 94.9% to 97.4%
  for the source-retirement header audit. The CPU-local storage header no
  longer carries the executable static body for get_this_cpu_local_var(); it
  now declares the ABI. Strict Rust exports get_this_cpu_local_var() from
  kernel/rust/cls.rs using the existing ihk_mc_get_processor_id() plus
  get_cpu_local_var() path, while kernel/cls.c keeps the matching C fallback
  body for non-Rust builds. The CLS smoke fixture now checks the exported
  accessor, and C-side equivalence fixtures that link fallback objects gained
  local weak providers where they intentionally avoid the full CLS object. The
  row remains partial because cpu_local_var() and
  cpu_local_var_with_override() field-access macros still live in the header.
  Validation passed for rustfmt, header static-inline detector no-hit on
  cls.h, bash syntax checking of the equivalence harness, strict Rust and
  C-fallback mckernel.img builds, strict Rust-object and C-fallback object
  symbol definition checks, strict/fallback linked-image unresolved-symbol
  checks, and the full equivalence harness after two fixture-provider link
  gaps were recorded in kernel.log and fixed.
- 2026-06-12: kernel/include/llist.h moved from 88.8% to 93.1%
  for the source-retirement header audit. The lockless-list header no longer
  carries executable static inline bodies for init_llist_head(),
  llist_empty(), llist_next(), llist_add(), or llist_del_all(). Strict Rust
  exports those ABI names from kernel/rust/llist.rs, using an atomic swap for
  llist_del_all() to preserve the old xchg-based delete-all semantics, while
  kernel/llist.c keeps matching C fallback definitions for non-Rust builds.
  The existing llist equivalence fixture now covers the externalized helpers
  in addition to llist_add_batch(), llist_del_first(), and
  llist_reverse_order(). The row remains partial because declaration-style
  initialization, container, and traversal macros still live in the header.
  Validation passed for rustfmt, bash syntax checking of the equivalence
  harness, strict Rust and C-fallback mckernel.img builds, strict Rust-object
  and C-fallback object symbol definition checks, strict/fallback
  linked-image unresolved-symbol checks, and the full equivalence harness.
- 2026-06-12: arch/x86_64/kernel/include/arch/mm.h moved to 100.0%
  for the source-retirement header audit. The x86 NFO TLB compatibility
  header no longer carries executable static inline no-op bodies for
  flush_nfo_tlb() or flush_nfo_tlb_mm(); it now exposes declarations only.
  The strict Rust x86 memory helper object exports both ABI names through
  kernel/rust/x86_memory_helpers.rs, while arch/x86_64/kernel/memory_helpers.c
  keeps equivalent no-op bodies only for C fallback builds outside
  MCKERNEL_RUST_X86_MEMORY_HELPERS. Validation passed for rustfmt, focused
  executable-header detector no-hit on arch/mm.h, strict Rust and C-fallback
  mckernel.img builds, strict Rust-object and C-fallback object symbol
  definition checks, strict/fallback linked-image unresolved-symbol checks,
  and the full equivalence harness. The source-retirement audit after code
  movement reports executable-header hits reduced to 44 and compiled retired C
  sources still at 0.
- 2026-06-12: ihk/ikc/include/ikc/ihk.h moved to 100.0% for the
  source-retirement header audit. The shared IKC compatibility header no
  longer carries executable ihk_ikc_map_virtual(), ihk_ikc_unmap_virtual(),
  ihk_os_to_dev(), IHK_EXPORT_SYMBOL(), host spinlock-save, or host
  processor-id function-like macro bodies. Those compatibility surfaces are
  now declared ABIs. The strict host IHK Rust helper object exports
  ihk_ikc_spinlock_lock, ihk_ikc_spinlock_unlock, ihk_ikc_spinlock_init,
  ihk_ikc_map_virtual, ihk_ikc_unmap_virtual, and
  ihk_ikc_get_processor_id through ihk/linux/core/rust/host_ikc.rs, with
  mikc.c exporting the symbols from ihk.ko for dependent modules. The strict
  kernel Rust IKC queue module exports the manycore ihk_os_to_dev,
  ihk_ikc_map_virtual, and ihk_ikc_unmap_virtual ABIs through
  kernel/rust/ikc_queue.rs. ihk/ikc/queue.c keeps equivalent C fallback
  bodies outside the strict Rust paths, and manycore IHK_EXPORT_SYMBOL uses in
  the fallback IKC C files are now host-only export markers, matching the old
  manycore no-op behavior. Validation passed for rustfmt, focused
  executable-header detector no-hit on the IKC header, strict Rust and pure
  C-fallback mckernel.img builds, strict Rust and pure no-Rust ihk_ko and
  mcctrl_ko builds, Rust-object and no-Rust C-object wrapper definition
  checks, strict/no-Rust Module.symvers export checks, the full equivalence
  harness, and the strict source-retirement audit with executable-header hits
  reduced to 45 and compiled retired C sources still at 0. Two intermediate
  validation failures were recorded in kernel.log and fixed/rerun: the
  manycore lock alias collision in the C-fallback kernel build, and a
  concurrent-use no-Rust Kbuild temporary-object failure. No guest runtime
  validation, module-load validation, QEMU validation, or host reboot path was
  run for this checkpoint.
- 2026-06-12: ihk/linux/include/ihk/arch/x86_64/ihk/msr.h moved to
  100.0% for the source-retirement header audit. The x86_64 host MSR
  compatibility header no longer carries the executable function-like
  ihk_get_tsc(x) macro body; it now exposes ihk_get_tsc as an object-like
  compatibility alias for the Linux rdtscll primitive, preserving the
  downstream ihk_get_tsc(x) expansion without retaining McKernel-owned
  preprocessor call logic. No in-tree non-test code currently calls the
  ihk_get_tsc alias, and rdtscll users continue to resolve through the
  existing Linux/kernel compatibility surfaces. Validation passed for a
  focused executable-header detector no-hit on the MSR header, strict Rust
  ihk_ko build, pure no-Rust ihk_ko fallback build, and the strict
  source-retirement audit with executable-header hits reduced to 46 and
  compiled retired C sources still at 0. No guest runtime validation,
  module-load validation, QEMU validation, or host reboot path was run for
  this checkpoint.
- 2026-06-12: ihk/cokernel/smp/x86_64/bootparam.h moved to
  100.0% for the source-retirement header audit. The active x86_64 SMP
  bootparam header now keeps the coreset layout and helper prototypes but no
  longer carries executable CORE_SET/CORE_CLR/CORE_ISSET/CORE_ZERO macros or
  the CORE_ISSET_ANY inline scan. The strict ihk-smp-x86_64 Rust helper object
  exports ihk_smp_core_set, ihk_smp_core_clear, ihk_smp_core_isset,
  ihk_smp_core_zero, and ihk_smp_core_isset_any from
  ihk/linux/driver/smp/rust/smp_driver_helpers.rs, preserving the old
  unchecked word/bit arithmetic without introducing Rust panic-bound paths.
  smp-driver.c keeps equivalent C fallback definitions outside
  IHK_SMP_RUST_HELPERS, and built x86 SMP call sites now dispatch through the
  declared helper ABI. Validation passed for rustfmt, bash syntax,
  executable-header detector no-hit on the x86_64 SMP bootparam header,
  strict Rust ihk-smp-x86_64_ko build, pure no-Rust ihk-smp-x86_64_ko fallback
  build, Rust-object and no-Rust C-object exported-symbol checks, linked
  strict/no-Rust module no-undefined checks for the coreset helper names, the
  full equivalence harness including direct coreset set/clear/isset/zero/any
  smoke coverage in ihk_module_helpers, and the strict source-retirement audit
  with executable-header hits reduced to 47 and compiled retired C sources
  still at 0. No guest runtime validation, module-load validation, QEMU
  validation, or host reboot path was run for this checkpoint.
- 2026-06-12: executer/kernel/mcctrl/include/futex.h moved to
  100.0% for the source-retirement header audit. The mcctrl futex
  compatibility header now declares get_futex_value_locked instead of carrying
  the executable pagefault-disable/__get_user/pagefault-enable inline body.
  The strict mcctrl Rust helper object exports get_futex_value_locked through
  executer/kernel/mcctrl/rust/mcctrl_helpers.rs and owns the ordering and
  return shaping: disable page faults, call the Linux get-user primitive
  bridge, re-enable page faults, and return -EFAULT for nonzero primitive
  failures. The actual Linux user access and pagefault primitives remain C
  bridge boundaries in futex.c, and futex.c keeps the original no-Rust
  fallback body outside MCCTRL_RUST_HELPERS. Validation passed for rustfmt,
  bash syntax, executable-header detector no-hit on the mcctrl futex header,
  strict Rust mcctrl_ko build, pure no-Rust mcctrl_ko fallback build,
  Rust-object and C-fallback object definition checks, linked strict and
  no-Rust mcctrl.ko no-undefined checks for get_futex_value_locked and the
  primitive bridge names, the full equivalence harness including direct
  success and fault-shaping get_futex_value_locked smoke coverage in
  ihk_module_helpers, and the strict source-retirement audit with
  executable-header hits reduced to 48 and compiled retired C sources still
  at 0. No guest runtime validation, module-load validation, QEMU validation,
  or host reboot path was run for this checkpoint.
- 2026-06-12: executer/kernel/mcctrl/include/refcount.h moved to
  100.0% for the source-retirement header audit. The copied mcctrl
  compatibility header now declares the refcount_set/read/add/inc/sub/dec
  helper ABI instead of carrying REFCOUNT_INIT and executable static inline
  bodies. The strict mcctrl Rust helper object exports the nine local-header
  refcount ABI names through executer/kernel/mcctrl/rust/mcctrl_helpers.rs,
  preserving the Linux atomic_add_unless-style nonzero add semantics and
  wrapping atomic add/sub behavior over the refs.counter word. The current
  Rocky build normally uses Linux's native refcount header before this copied
  legacy header is included; the retained no-Rust C fallback definitions in
  driver.c are therefore gated to the pre-4.11 Linux compatibility path that
  actually includes the copied header. Validation passed for rustfmt, bash
  syntax, executable-header detector no-hit on the mcctrl refcount header,
  strict Rust mcctrl_ko build, pure no-Rust mcctrl_ko fallback build,
  Rust-object exported-symbol checks, linked strict and no-Rust mcctrl.ko
  no-undefined checks for the refcount ABI names, the full equivalence harness
  including direct mcctrl refcount smoke coverage in ihk_module_helpers, and
  the strict source-retirement audit with executable-header hits reduced to 49
  and compiled retired C sources still at 0. An earlier same-slice no-Rust
  build failure from colliding unguarded fallback definitions was logged in
  kernel.log and fixed by matching the fallback guard to mcctrl.h's pre-4.11
  local-header include path. No guest runtime validation, module-load
  validation, QEMU validation, or host reboot path was run for this checkpoint.
- 2026-06-12: executer/kernel/mcctrl/arch/x86_64/include/arch-lock.h
  moved to 100.0% for the source-retirement header audit. The mcctrl x86 lock
  compatibility header now declares the ticket spinlock and MCS-rwlock helper
  ABI instead of carrying executable static inline bodies. The strict mcctrl
  Rust helper object resolves ihk_mc_spinlock_init,
  __ihk_mc_spinlock_lock_noirq, __ihk_mc_spinlock_unlock_noirq,
  __ihk_mc_spinlock_lock, __ihk_mc_spinlock_unlock,
  mcs_rwlock_writer_lock_noirq, and mcs_rwlock_writer_unlock_noirq through
  executer/kernel/mcctrl/rust/mcctrl_helpers.rs, with tiny retained Linux
  preempt bridge callbacks in driver.c. Non-Rust fallback bodies live in
  executer/kernel/mcctrl/arch/x86_64/cpu.c outside MCCTRL_RUST_HELPERS.
  Validation passed for rustfmt, bash syntax, executable-header detector
  no-hit on the mcctrl arch-lock header, strict Rust mcctrl_ko build, pure
  no-Rust mcctrl_ko fallback build, Rust-object and C-fallback object symbol
  definition checks, linked mcctrl.ko no-undefined checks for the externalized
  lock ABI names, the full equivalence harness including direct mcctrl
  spinlock and MCS-rwlock noirq smoke coverage, and the strict
  source-retirement audit with executable-header hits reduced to 50 and
  compiled retired C sources still at 0. No guest runtime validation,
  module-load validation, QEMU validation, or host reboot path was run for
  this checkpoint.
- 2026-06-12: arch/x86_64/kernel/include/arch-lock.h moved to
  100.0% for the source-retirement header audit. The x86 lock compatibility
  header now declares the ticket spinlock, MCS-rwlock, low-level rwlock, and
  irqflags_can_interrupt helpers instead of carrying executable static inline
  bodies and debug statement macros. The strict x86_64 Rust path resolves the
  spinlock helpers through kernel/rust/spinlock_helpers.rs and the rwlock/
  MCS-rwlock helpers through kernel/rust/lock_helpers.rs. Non-Rust fallback
  bodies live in arch/x86_64/kernel/lock.c, which is compiled only by the C
  fallback image path and omitted from the strict Rust image path. Validation
  passed for rustfmt, bash syntax, executable-header detector no-hit on
  arch-lock.h, strict and C-fallback mckernel.img builds, Rust-object and
  C-fallback object definition checks, linked-image no-undefined checks for
  the externalized lock ABI names, the full equivalence harness including
  direct irqflags_can_interrupt coverage in the spinlock_helpers digest, and
  the strict source-retirement audit with executable-header hits reduced to 51
  and compiled retired C sources still at 0. One same-slice waitq fixture link
  failure was logged and fixed by adding explicit C spinlock fixture bodies.
  No guest runtime validation, module-load validation, QEMU validation, or host
  reboot path was run for this checkpoint.
- 2026-06-12: lib/include/ihk/lock.h and kernel/lock_helpers.c
  moved to 100.0% for the strict Rust source-retirement audit. The header now
  exposes declarations for the generic rwspinlock, MCS lock, and linux_spin_*
  helpers instead of embedding fallback executable bodies or statement macros.
  The strict Rust build resolves those ABI names through
  kernel/rust/lock_helpers.rs, while kernel/lock_helpers.c carries the C
  fallback bodies only for non-Rust builds. The Tofu linux_spin_irqsave call
  sites now pass explicit saved-state pointers to the exported helper ABI.
  Validation passed for rustfmt, bash syntax, executable-header detector no-hit
  on lock.h, strict and C-fallback mckernel.img builds, Rust-object and
  C-fallback object definition checks, linked-image no-undefined checks for
  linux_spin_* names, the full equivalence harness including direct
  linux_spin_lock/unlock/irqsave/irqrestore coverage in the lock_helpers
  digest, and the strict source-retirement audit with executable-header hits
  reduced to 52 and compiled retired C sources still at 0. No guest runtime
  validation, module-load validation, QEMU validation, or host reboot path was
  run for this checkpoint.
- 2026-06-12: kernel/include/process.h moved to 100.0% for the
  source-retirement header audit. The Process/VM compatibility header now
  declares PROT_TO_VR_FLAG, VRFLAG_PROT_TO_MAXPROT,
  VRFLAG_MAXPROT_TO_PROT, the six __W* wait-status helpers, process_hash,
  thread_hash, has_cap_ipc_lock, and has_cap_sys_admin instead of carrying
  executable function-like macros and static inline bodies. The strict x86_64
  Rust path resolves those helper ABI names through
  kernel/rust/process_helpers.rs. Non-Rust fallback bodies live in
  kernel/process_helpers.c outside MCKERNEL_RUST_PROCESS_HELPERS. Validation
  passed for rustfmt, bash syntax, executable-header detector no-hit on
  process.h, strict and C-fallback mckernel.img builds, Rust-object and
  linked-image exported-symbol plus linked-image no-undefined checks,
  C-fallback object and linked-image definition/no-undefined checks, the full
  equivalence harness including direct VR-flag/wait-status/hash coverage in
  the process_helpers digest, and the strict source-retirement audit with
  executable-header hits reduced to 53 and compiled retired C sources still
  at 0. During validation three fixture/link failures were logged and fixed:
  host_helpers C fixture linkage, libc __W* macro collision, and syscall
  policy C fixture linkage. No guest runtime validation or module-load
  validation was run for this checkpoint.
- 2026-06-12: kernel/include/prio.h moved to 100.0% for the
  source-retirement header audit. The scheduler priority compatibility header
  now declares NICE_TO_PRIO, PRIO_TO_NICE, USER_PRIO, nice_to_rlimit, and
  rlimit_to_nice instead of carrying executable function-like priority
  conversion macros and static inline bodies. MAX_USER_PRIO is retained as a
  constant expression, and the stale unused TASK_USER_PRIO function-like macro
  was removed because the current thread layout has no static_prio field and a
  repository-wide use scan found no users. The strict x86_64 Rust path resolves
  the conversion helpers through kernel/rust/sched_helpers.rs. Non-Rust
  fallback bodies live in kernel/syscall.c outside
  MCKERNEL_RUST_SCHED_PRIO_HELPERS. Validation passed for rustfmt, bash
  syntax, executable-header detector no-hit on prio.h, strict and C-fallback
  mckernel.img builds, Rust-object and linked-image exported-symbol plus
  linked-image no-undefined checks, C-fallback object and linked-image
  definition/no-undefined checks, the full equivalence harness including
  direct priority conversion coverage in the sched_helpers digest, and the
  strict source-retirement audit with executable-header hits reduced to 54 and
  compiled retired C sources still at 0. No guest runtime validation or
  module-load validation was run for this checkpoint.
- 2026-06-12: kernel/include/memobj.h moved to 100.0% for the
  source-retirement header audit. The memobj compatibility header now declares
  memobj_ref/unref, memobj ops dispatch wrappers, memobj flag predicates, and
  flush/free/remap predicate wrappers instead of carrying executable static
  inline bodies. The strict x86_64 Rust path resolves the ops
  dispatch/predicate wrappers through kernel/rust/object_helpers.rs and
  resolves memobj_ref/unref through kernel/rust/refcount_helpers.rs. Non-Rust
  fallback bodies live in kernel/object_helpers.c and kernel/mem.c outside
  MCKERNEL_RUST_OBJECT_HELPERS / MCKERNEL_RUST_REFCOUNT_HELPERS. Validation
  passed for rustfmt, bash syntax, executable-header detector no-hit on
  memobj.h, strict and C-fallback mckernel.img builds, Rust-object and
  linked-image exported-symbol plus linked-image no-undefined checks,
  C-fallback object and linked-image definition/no-undefined checks, the full
  equivalence harness including direct memobj ops-table dispatch/missing-op/
  predicate coverage in the object_helpers digest, and the strict
  source-retirement audit with executable-header hits reduced to 55 and
  compiled retired C sources still at 0. No guest runtime validation or
  module-load validation was run for this checkpoint.
- 2026-06-11: executer/user/arch/x86_64/arch_args.h moved to
  100.0% for the source-retirement header audit. The x86_64 mcexec syscall
  argument compatibility header now declares the ptrace register fetch/store,
  syscall-number/result, syscall-argument, RIP, setter, and syscall-enter
  helpers instead of carrying executable static inline bodies and the
  syscall_enter function-like macro. The strict x86_64 mcexec path resolves
  those ABI names through Rust-owned executer/user/rust/mcexec_helpers.rs,
  preserving the verified struct user_regs_struct field order and the legacy
  unsigned comparison against -ENOSYS. The non-Rust fallback bodies live in
  executer/user/mcexec.c outside MCEXEC_RUST_HELPERS. This validation also
  exposed and fixed mcexec's existing fallout from declaration-only bitops
  headers by exporting set_bit, clear_bit, and test_bit from the same mcexec
  Rust helper object, with matching non-Rust fallback bodies in mcexec.c.
  Validation passed for rustfmt, bash syntax, executable-header detector
  no-hit on arch_args.h, strict and configured fallback mcexec builds,
  Rust-object and linked-binary exported-symbol checks plus linked-binary
  no-undefined checks for the arch-args and bitops ABI names, a direct
  no-MCEXEC_RUST_HELPERS mcexec.c compile with fallback-object definition and
  no-undefined checks, the full equivalence harness including direct
  arch-args field access/syscall_enter and bitops coverage in the mcexec smoke
  digest, and the strict source-retirement audit with executable-header hits
  reduced to 56 and compiled retired C sources still at 0. One strict mcexec
  link failure from missing set_bit/test_bit/clear_bit was logged in
  kernel.log before the bitops provider fix. No guest runtime validation or
  module-load validation was run for this checkpoint.
- 2026-06-11: executer/user/eclair.h moved to 100.0% for the
  source-retirement header audit. The eclair compatibility header now keeps
  DEBUG builds aliasing dprintf to printf but removes the normal-build
  executable function-like no-op macro. Normal builds instead declare
  eclair_dprintf and alias dprintf to that exported helper. The strict x86_64
  user-tool path resolves the variadic no-op through Rust-owned
  executer/user/rust/eclair_helpers.rs, and the non-Rust fallback body lives in
  executer/user/eclair.c outside ECLAIR_RUST_HELPERS. Validation passed for
  rustfmt, the executable-header detector no-hit on executer/user/eclair.h,
  strict and configured fallback eclair builds, Rust-object and linked-binary
  exported-symbol checks plus linked-binary no-undefined checks, a direct
  no-ECLAIR_RUST_HELPERS eclair.c compile and fallback-object symbol check,
  the full equivalence harness including direct eclair_dprintf coverage in the
  eclair smoke digest, and the strict source-retirement audit with
  executable-header hits reduced to 57 and compiled retired C sources still at
  0. No guest runtime validation or module-load validation was run for this
  checkpoint.
- 2026-06-11: kernel/include/hash.h moved to 100.0% for the
  source-retirement header audit. The hash compatibility header now declares
  hash_64, hash_32, hash_long, and hash_ptr instead of carrying executable
  static inline bodies and the hash_long function-like macro. The retained
  GOLDEN_RATIO_PRIME selector is object-like and now uses __SIZEOF_LONG__ so
  it remains preprocessor-safe with this tree's sizeof-based BITS_PER_LONG.
  The strict x86_64 Rust kernel path resolves all four ABI names through
  Rust-owned kernel/rust/hash.rs exports, preserving the legacy unsigned
  wraparound hash arithmetic and high-bit selection. The non-Rust fallback
  bodies live in kernel/mem.c outside MCKERNEL_RUST_HASH_HELPERS. Validation
  passed for rustfmt, executable-header detector no-hit on kernel/include/hash.h,
  strict and C-fallback mckernel.img builds, Rust-object and linked-image
  exported-symbol checks plus linked-image no-undefined checks, C-fallback
  object and linked-image definition checks plus linked-image no-undefined
  checks, the full equivalence harness including direct hash helper coverage in
  the mem-init helper digest, and the strict source-retirement audit with
  executable-header hits reduced to 58 and compiled retired C sources still at
  0. No guest runtime validation or module-load validation was run for this
  checkpoint.
- 2026-06-11: kernel/include/mman.h moved to 100.0% for the
  source-retirement header audit. The mman compatibility header now declares
  round_up and round_down instead of carrying executable function-like rounding
  macros. The strict x86_64 Rust kernel path resolves both ABI names through
  Rust-owned kernel/rust/mem_helpers.rs exports, preserving the legacy
  unsigned-long wraparound formulas. The non-Rust fallback bodies live in
  kernel/mem.c outside MCKERNEL_RUST_MMAN_HELPERS. Validation passed for
  rustfmt, executable-header detector no-hit on kernel/include/mman.h, strict
  and C-fallback mckernel.img builds, Rust-object and linked-image
  exported-symbol checks plus linked-image no-undefined checks, C-fallback
  object and linked-image definition checks plus linked-image no-undefined
  checks, the full equivalence harness including direct round_up and
  round_down coverage in the mem-init helper digest, and the strict
  source-retirement audit with executable-header hits reduced to 59 and
  compiled retired C sources still at 0. No guest runtime validation or
  module-load validation was run for this checkpoint.
- 2026-06-11: kernel/include/futex.h moved to 100.0% for the
  source-retirement header audit. The main kernel futex header now declares
  FUTEX_OP and get_futex_value_locked instead of carrying the executable
  function-like macro and static inline volatile load helper. The strict
  x86_64 Rust kernel path resolves both legacy ABI names through
  kernel/rust/futex.rs, preserving the FUTEX_WAKE_OP bit packing and the
  direct volatile 32-bit load/store return convention. The non-Rust fallback
  bodies live in kernel/futex.c outside the strict Rust object path.
  Validation passed for rustfmt, executable-header detector no-hit on
  kernel/include/futex.h, strict and C-fallback mckernel.img builds,
  Rust-object and linked-image exported-symbol checks plus linked-image
  no-undefined checks for both ABI names, C-fallback object and linked-image
  definition checks plus linked-image no-undefined checks, the full equivalence
  harness including direct FUTEX_OP and get_futex_value_locked coverage in the
  scheduler/futex helper digest, and the strict source-retirement audit with
  executable-header hits reduced to 60 and compiled retired C sources still at
  0. No guest runtime validation or module-load validation was run for this
  checkpoint.
- 2026-06-11: kernel/include/lwk/futex.h moved to 100.0% for the
  source-retirement header audit. The LWK futex compatibility header now
  declares the FUTEX_OP encoder instead of carrying the executable
  function-like macro body. The strict x86_64 Rust kernel path resolves the
  legacy FUTEX_OP ABI name through the Rust-owned kernel/rust/futex.rs export,
  preserving the original FUTEX_WAKE_OP bit packing with explicit unsigned
  masking before returning the legacy signed int value. The non-Rust fallback
  body lives in kernel/futex.c outside the strict Rust object path. Validation
  passed for rustfmt, executable-header detector no-hit on lwk/futex.h, strict
  and C-fallback mckernel.img builds, Rust-object and linked-image
  exported-symbol checks plus linked-image no-undefined checks, C-fallback
  object and linked-image definition checks plus linked-image no-undefined
  checks, the full equivalence harness including direct FUTEX_OP encoder
  coverage in the scheduler/futex helper digest, and the strict
  source-retirement audit with executable-header hits reduced to 61 and
  compiled retired C sources still at 0. No guest runtime validation or
  module-load validation was run for this checkpoint.
- 2026-06-11: arch/x86_64/kernel/include/cas.h moved to 100.0%
  for the source-retirement header audit. The x86 compare-and-swap header now
  declares compare_and_swap instead of carrying the executable lock-cmpxchg
  static inline body, and the disabled compare_and_swap16 inline block was
  removed from the compatibility header. The strict x86_64 Rust kernel path
  resolves compare_and_swap through the Rust-owned
  kernel/rust/atomic_helpers.rs export, preserving the legacy 0-on-fail and
  1-on-success convention over an unsigned-long compare-exchange. The
  non-Rust fallback body lives in arch/x86_64/kernel/cpu.c outside
  MCKERNEL_RUST_ATOMIC_HELPERS and preserves the original lock cmpxchgq
  sequence. Validation passed for rustfmt, executable-header detector no-hit
  on cas.h, strict and C-fallback mckernel.img builds, Rust-object and
  linked-image exported-symbol checks plus linked-image no-undefined checks,
  C-fallback object and linked-image definition checks plus linked-image
  no-undefined checks, the full equivalence harness including direct
  compare_and_swap success and failure coverage in atomic_helpers, and the
  strict source-retirement audit with executable-header hits reduced to 62
  and compiled retired C sources still at 0. No guest runtime validation or
  module-load validation was run for this checkpoint.
- 2026-06-11: arch/x86_64/kernel/include/arch-string.h moved to
  100.0% for the source-retirement header audit. The x86 string header now
  declares __inline_memcpy and __inline_memset instead of carrying executable
  inline assembly bodies. The strict x86_64 Rust kernel path resolves those
  ABI names through Rust-owned kernel/rust/string.rs exports, while
  lib/string.c keeps matching fallback bodies for non-Rust builds.
  __inline_memset preserves the old x86 stosl/stosw/stosb low-32,
  low-16, and low-8 value semantics rather than byte-pattern memset
  semantics. The equivalence harness now links focused C-model binaries with
  the string C fallback object where declaration-only headers leave external
  __inline_memset references, and direct string coverage checks both
  __inline_memcpy and __inline_memset. Validation passed for rustfmt,
  executable-header detector no-hit on arch-string.h, strict and C-fallback
  mckernel.img builds, Rust-object/C-fallback-object and linked-image
  exported-symbol checks plus linked-image no-undefined checks for the two
  ABI names, the full equivalence harness, and the strict source-retirement
  audit with compiled retired C sources still at 0. Five harness/link or
  expected-value setup failures were logged in kernel.log before the corrected
  C-model links and expected fill pattern were used. No guest runtime
  validation or module-load validation was run for this checkpoint.
- 2026-06-11: kernel/include/tofu/tof_uapi.h moved to 100.0% for the
  source-retirement header audit. The header now declares the seven public
  TOF_RSC_* resource-index helpers instead of carrying executable
  function-like macros. The strict x86_64 Rust kernel path resolves
  TOF_RSC_TOQ, TOF_RSC_TCQ, TOF_RSC_MRQ, TOF_RSC_PBQ, TOF_RSC_PRQ,
  TOF_RSC_STT, and TOF_RSC_MBT through Rust-owned kernel/rust/tofu_uapi.rs
  exports, while kernel/tofu/tof_utofu_main.c keeps matching fallback bodies
  for non-Rust Tofu builds. Validation passed for pre-edit value extraction,
  post-edit Rust helper value checks, executable-header detector no-hit on
  tof_uapi.h, strict and C-fallback mckernel.img builds, strict Rust-object
  and linked-image exported-symbol checks plus linked-image no-undefined
  checks for the seven ABI names, the full equivalence harness, and the
  strict source-retirement audit with compiled retired C sources still at 0.
  ENABLE_TOFU was OFF in the default strict and fallback build directories, so
  the Tofu C fallback block was retained but not compiled in those default
  build validations. During validation two value-check harness setup failures
  were logged in kernel.log before the corrected checks were used. No guest
  runtime validation or module-load validation was run for this checkpoint.
- 2026-06-11: lib/include/mc_xpmem.h and lib/include/mc_perf_event.h moved
  to 100.0% for the source-retirement header audit. Both headers now expose
  fixed ioctl command constants instead of carrying local _IOC/_IO
  function-like constructor macros. The XPMEM command constants preserve the
  legacy _IO('x', 0..6) encoding at 0x7800..0x7806, and the perf event
  command constants preserve the legacy _IO('$', 0..3) encoding at
  0x2400..0x2403. Validation passed for a pre-edit freestanding compile-time
  value assertion over the old macros, post-edit executable-header detector
  no-hits on both headers, post-edit freestanding compile-time value
  assertions over the fixed constants, strict and C-fallback mckernel.img
  builds, the full equivalence harness, and the strict source-retirement
  audit with compiled retired C sources still at 0. During validation two
  value-check harness setup failures were logged in kernel.log before the
  freestanding assertion form was used. No guest runtime validation or
  module-load validation was run for this checkpoint.
- 2026-06-11: lib/include/bitmap.h moved to 100.0% for the
  source-retirement header audit. The bitmap header now declares the public
  bitmap wrapper layer instead of carrying executable mask macros,
  small-constant predicates, static inline bitmap operations, and the
  bitmap_parse inline trampoline. The strict x86_64 Rust kernel path resolves
  bitmap_zero, bitmap_fill, bitmap_copy, bitmap_and, bitmap_or, bitmap_xor,
  bitmap_andnot, bitmap_complement, bitmap_equal, bitmap_intersects,
  bitmap_subset, bitmap_empty, bitmap_full, bitmap_weight,
  bitmap_shift_right, bitmap_shift_left, and bitmap_parse through
  Rust-owned kernel/rust/bitmap.rs exports, while lib/bitmap.c keeps matching
  fallback bodies outside MCKERNEL_RUST_BITMAP_CORE/PARSE. Validation passed
  for rustfmt, Python audit syntax, executable-header detector no-hits on
  bitmap.h, strict and C-fallback mckernel.img builds, Rust-object and linked
  image exported-symbol checks plus linked-image no-undefined checks for the
  bitmap wrapper ABI names, C-fallback bitmap.o definition checks, the full
  equivalence harness including bitmap and bitmap_parse digests, and the
  strict source-retirement audit with compiled retired C sources still at 0.
  During validation one command-selection failure was logged in kernel.log
  because the Python audit script was accidentally passed to bash -n; the
  correct Python py_compile and shell syntax checks passed before crediting
  this row. No guest runtime validation or module-load validation was run for
  this checkpoint.
- 2026-06-11: lib/include/bitops-__ffs.h,
  lib/include/bitops-clear_bit.h, lib/include/bitops-ffz.h,
  lib/include/bitops-fls.h, and lib/include/bitops-set_bit.h moved to
  100.0% for the source-retirement header audit. These generic bitops wrapper
  headers now declare __ffs, clear_bit, ffz, fls, and set_bit instead of
  carrying executable inline or macro fallback bodies. The strict x86_64 Rust
  kernel path continues resolving those ABI names through Rust-owned
  kernel/rust/bitops.rs exports, while lib/bitops.c keeps the x86 assembly C
  fallback bodies and now also carries the old generic non-x86 fallback bodies
  for deferred architecture builds. Validation passed for executable-header
  detector no-hits over all five headers, strict and C-fallback mckernel.img
  builds, Rust-object and linked-image exported-symbol checks plus
  linked-image no-undefined checks for the five ABI names, C-fallback
  bitops.o definition checks, the full equivalence harness including bitops
  and bitops_header digests, and diff hygiene checks. No guest runtime
  validation or module-load validation was run for this checkpoint.
- 2026-06-11: lib/include/ctype.h moved to 100.0% for the
  source-retirement header audit. The ctype header now declares the public
  predicate, ASCII, and case-conversion entry points instead of carrying
  executable classification macros and __tolower/__toupper static inline
  bodies. Rust-owned kernel/rust/numparse.rs exports __ismask, isalnum,
  isalpha, iscntrl, isdigit, isgraph, islower, isprint, ispunct, isspace,
  isupper, isxdigit, isascii, toascii, __tolower, __toupper, tolower, and
  toupper for the strict x86_64 Rust kernel path, while lib/vsprintf.c keeps
  matching fallback definitions beside the legacy _ctype table for non-Rust
  builds. Validation passed for rustfmt, executable-header detector no-hit on
  ctype.h, focused strict mckernel.img build, Rust-object and linked-image
  exported-symbol checks plus linked-image no-undefined checks for the ctype
  ABI names, the full equivalence harness, and the C fallback mckernel.img
  build. During validation the first strict build failed because __tolower
  used an overflowing u8 subtraction; that failure was logged in kernel.log
  before the conversion was corrected and validation rerun. No guest runtime
  validation or module-load validation was run for this checkpoint.
- 2026-06-03: executer/kernel/mcctrl/include/mc_plist.h moved to
  100.0% for the source-retirement header audit. The mcctrl plist header now
  declares dynamic init, empty, first, add, and delete entry points instead
  of carrying executable initializer, predicate, first-entry, and iterator
  macro/static-inline logic. Strict mcctrl futex users now call the Rust-owned
  exported helper bodies and use the underlying list iterator directly at the
  call sites; executer/kernel/mcctrl/mc_plist.c keeps the original C plist
  helper bodies as the non-Rust fallback implementation. Validation passed
  for rustfmt, executable-header detector no-hit on mc_plist.h, focused
  mcctrl_rust_helpers and mcctrl_ko builds, strict futex.o undefined-symbol
  plus Rust-object and linked-module definition checks for the plist helper
  ABI names, bash -n on the equivalence harness, focused mckernel.img build,
  the full equivalence harness including expanded mcctrl module-helper plist
  smoke coverage, and the active source-retirement audit reporting debt rows
  reduced from 136 to 135, executable header count reduced from 75 to 74,
  host/mcctrl retired rows increased from 56 to 57, and compiled retired C
  sources: 0. No guest runtime validation was run for this checkpoint.
- 2026-06-03: executer/kernel/mcctrl/arch/x86_64/include/archdeps.h
  moved to 100.0% for the source-retirement header audit. The x86 mcctrl
  archdeps header now declares xchg4 and
  mcctrl_pte_is_write_combined_result instead of carrying executable inline
  PTE flag logic and an x86 inline-assembly xchg4 macro. Rust-owned
  executer/kernel/mcctrl/rust/mcctrl_helpers.rs exports the strict mcctrl
  x86_64 xchg4 and write-combined PTE flag result ABI names, while
  executer/kernel/mcctrl/arch/x86_64/archdeps.c keeps the original C/asm
  fallback bodies outside MCCTRL_RUST_HELPERS for non-Rust mcctrl builds.
  Validation passed for rustfmt, executable-header detector no-hit on the
  x86 mcctrl archdeps header, focused mcctrl_rust_helpers and mcctrl_ko
  builds, strict futex.o/syscall.o undefined-symbol plus Rust-object and
  linked-module definition checks for xchg4 and
  mcctrl_pte_is_write_combined_result, bash -n on the equivalence harness,
  focused mckernel.img build, the full equivalence harness including new
  mcctrl module-helper boundary checks and Rust object undefined-symbol/SIMD
  gates, and the active source-retirement audit reporting debt rows reduced
  from 137 to 136, executable header count reduced from 76 to 75,
  host/mcctrl retired rows increased from 55 to 56, and compiled retired C
  sources: 0. No guest runtime validation was run for this checkpoint.
- 2026-06-03: executer/kernel/mcctrl/mcctrl.h moved to 100.0% for the
  source-retirement header audit. The mcctrl state header no longer carries
  the executable sysfs_inited inline body; the strict mcctrl sysfs paths now
  route sysfs buffer initialization checks through Rust-owned
  mcctrl_sysfs_inited_result, while executer/kernel/mcctrl/sysfs.c keeps the
  original boolean fallback body outside MCCTRL_RUST_HELPERS. Validation
  passed for rustfmt, executable-header detector no-hit on mcctrl.h, focused
  mcctrl_rust_helpers and mcctrl_ko builds, strict sysfs.o undefined-symbol
  plus Rust-object and linked-module definition checks for
  mcctrl_sysfs_inited_result, bash -n on the equivalence harness, focused
  mckernel.img build, the full equivalence harness including the new mcctrl
  module-helper boundary checks and Rust object undefined-symbol/SIMD gates,
  and the active source-retirement audit reporting debt rows reduced from 138
  to 137, executable header count reduced from 77 to 76, host/mcctrl retired
  rows increased from 54 to 55, and compiled retired C sources: 0. No guest
  runtime validation was run for this checkpoint.
- 2026-06-03: executer/kernel/mcctrl/sysfs.h moved to 100.0% for the
  source-retirement header audit. The mcctrl sysfs header now declares
  is_special_sysfs_ops instead of carrying the executable special-ops range
  predicate inline body. Rust-owned
  executer/kernel/mcctrl/rust/mcctrl_helpers.rs exports the strict mcctrl
  x86_64 is_special_sysfs_ops ABI name over the same
  SYSFS_SPECIAL_OPS_MIN..SYSFS_SPECIAL_OPS_MAX comparison used by the mcctrl
  sysfs create paths, while executer/kernel/mcctrl/sysfs.c keeps the original
  predicate as a non-Rust fallback body outside MCCTRL_RUST_HELPERS. Validation
  passed for rustfmt, executable-header detector no-hit on the mcctrl sysfs
  header, focused mcctrl_rust_helpers and mcctrl_ko builds, strict sysfs.o
  undefined-symbol plus Rust-object and linked-module definition checks for
  is_special_sysfs_ops, bash -n on the equivalence harness, focused
  mckernel.img build, the full equivalence harness including the new mcctrl
  module-helper boundary checks and Rust object undefined-symbol/SIMD gates,
  and the active source-retirement audit reporting debt rows reduced from 139
  to 138, executable header count reduced from 78 to 77, host/mcctrl retired
  rows increased from 53 to 54, and compiled retired C sources: 0. No guest
  runtime validation was run for this checkpoint.
- 2026-06-03: arch/x86_64/kernel/include/arch-bitops.h moved to 100.0%
  for the source-retirement header audit. The x86 arch bitops header now
  declares fls, ffs, __ffs, ffz, set_bit, and clear_bit instead of carrying
  executable inline x86 assembly fallback bodies. Rust-owned
  kernel/rust/bitops.rs exports those strict x86_64 ABI names, while
  lib/bitops.c now carries the original x86 C fallback bodies outside
  MCKERNEL_RUST_BITOPS for non-Rust builds. The equivalence harness links the
  page_alloc_bitmap C-model binary with bitops_c.o because page allocator
  headers now reach external fls through the declaration-only arch bitops
  header. Validation passed for focused mckernel.img build, strict build-graph
  absence check for lib/bitops.c, exported-symbol and linked-image resolution
  checks for the six bitops helpers, bash -n on the equivalence harness after
  the logged page_alloc_bitmap C-model link failure was fixed, the full
  equivalence harness including bitops, bitops_header, page_alloc_bitmap, and
  Rust object undefined-symbol/SIMD gates, and the active source-retirement
  audit reporting executable header count reduced from 79 to 78 and compiled
  retired C sources: 0. No guest runtime validation was run for this
  checkpoint.
- 2026-06-03: lib/include/bitops-test_bit.h moved to 100.0% for the
  source-retirement header audit. The header now declares test_bit instead of
  carrying the executable fallback bit-test inline body. Rust-owned
  kernel/rust/bitops.rs exports the strict x86_64 test_bit ABI name, while
  lib/bitops.c now carries the original non-Rust fallback body outside
  MCKERNEL_RUST_BITOPS. The equivalence harness links the focused C-model
  bitops header binary with bitops_c.o so the C side uses that fallback body,
  while the Rust side resolves test_bit from mckernel_rust.o. Validation passed
  for focused mckernel.img build, strict build-graph absence check for
  lib/bitops.c, exported-symbol and linked-image resolution checks for
  test_bit, bash -n on the equivalence harness after the logged C-model link
  failure was fixed, the full equivalence harness including bitops_header and
  Rust object undefined-symbol/SIMD gates, and the active source-retirement
  audit reporting executable header count reduced from 80 to 79 and compiled
  retired C sources: 0. No guest runtime validation was run for this
  checkpoint.
- 2026-06-03: kernel/include/kref.h moved to 100.0% for the
  source-retirement header audit. The kref header now declares kref_init,
  kref_read, kref_is_mckernel, kref_get, and kref_put unconditionally instead
  of carrying non-Rust fallback inline bodies, and the unused KREF_INIT(n)
  function-like macro was removed after repository-wide use scan found no
  users. Rust-owned kernel/rust/refcount_helpers.rs continues exporting the
  strict x86_64 kref ABI names, while kernel/mem.c now carries the original C
  fallback bodies outside MCKERNEL_RUST_REFCOUNT_HELPERS for non-Rust builds.
  Validation passed for focused mckernel.img build, exported-symbol and
  no-undefined checks for the five kref helpers in mckernel_rust.o, strict
  mem.c no-definition check, the full equivalence harness including
  refcount_helpers and Rust object undefined-symbol/SIMD gates, and the active
  source-retirement audit reporting executable header count reduced from 81 to
  80 and compiled retired C sources: 0. No guest runtime validation was run
  for this checkpoint.
- 2026-06-03: kernel/include/sysfs.h moved to 100.0% for the
  source-retirement header audit. The sysfs header now declares
  is_special_sysfs_ops instead of carrying the executable special-ops range
  predicate inline body. Rust-owned kernel/rust/sysfs.rs exports the strict
  x86_64 is_special_sysfs_ops ABI name over the same
  SYSFS_SPECIAL_OPS_MIN..SYSFS_SPECIAL_OPS_MAX comparison used internally by
  sysfs_createf, while kernel/sysfs.c keeps the original predicate as a
  non-Rust fallback body outside MCKERNEL_RUST_OBJECT_HELPERS. Validation
  passed for rustfmt, focused mckernel.img build, exported-symbol and
  no-undefined checks for is_special_sysfs_ops in mckernel_rust.o, the full
  equivalence harness including Rust object undefined-symbol/SIMD gates, and
  the active source-retirement audit reporting executable header count reduced
  from 82 to 81 and compiled retired C sources: 0. No guest runtime validation
  was run for this checkpoint.
- 2026-06-03: corrected 17 stale partial header rows to 100.0% after the
  active executable-header detector found no static inline, extern inline, or
  function-like macro bodies in those headers. This tracker-only correction
  covers arch mman/shm/cpulocal/elf/types declaration headers, executer
  uprotocol, ELF core/pager/sysfs message headers, lib limits/types, mcctrl
  sysfs message declarations, and retained IHK MIC/SMP/debug/monitor
  declaration or constant headers. No source files changed; retained content is
  constants, declarations, layouts, or non-executable object-like macros.
  Validation passed for active audit reporting debt rows reduced from 160 to
  143, executable header hits remaining 82, and compiled retired C sources: 0.
  No guest runtime validation was run for this checkpoint.
- 2026-06-03: kernel/include/time.h moved to 100.0% for the
  source-retirement header audit. The time header now declares ts_add,
  ts_sub, tv_add, tv_sub, tv_to_ts, and ts_to_tv instead of carrying
  executable timespec/timeval arithmetic and conversion inline bodies.
  Rust-owned kernel/rust/timer.rs exports the strict x86_64 ABI names and
  preserves nanosecond/microsecond normalization, while kernel/timer.c keeps
  the original helpers as fallback definitions for non-Rust builds. The
  equivalence harness now supplies matching C-model definitions for the
  syscall-policy C binary because that focused C model links syscall.c without
  timer.c. Validation passed for rustfmt, focused mckernel.img build,
  exported-symbol and no-undefined checks for the six time helpers in
  mckernel_rust.o, bash -n on the equivalence harness, the full equivalence
  harness including Rust object undefined-symbol/SIMD gates after the logged
  C-model unresolved-helper crash was fixed, and the active source-retirement
  audit reporting executable header count reduced from 83 to 82 and compiled
  retired C sources: 0. No guest runtime validation was run for this
  checkpoint.
- 2026-06-03: kernel/include/shm.h moved to 100.0% for the
  source-retirement header audit. The SysV shared-memory header now declares
  shmlock_users_lock and shmlock_users_unlock instead of carrying executable
  inline spinlock wrappers. Rust-owned kernel/rust/shmobj.rs exports the
  strict x86_64 shmlock_users_lock and shmlock_users_unlock ABI names over the
  existing Rust-owned shmlock_users_lock_body, while kernel/shmobj.c keeps the
  original wrappers as non-Rust fallback definitions outside
  MCKERNEL_RUST_OBJECT_HELPERS. Validation passed for rustfmt, focused
  mckernel.img build, exported-symbol and no-undefined checks for the two
  shmlock_users wrappers in mckernel_rust.o, the full equivalence harness
  including Rust object undefined-symbol/SIMD gates, and the active
  source-retirement audit reporting executable header count reduced from 84
  to 83 and compiled retired C sources: 0. No guest runtime validation was run
  for this checkpoint.
- 2026-06-03: kernel/include/uio.h moved to 100.0% for the
  source-retirement header audit. The UIO header now declares iov_length
  instead of carrying the executable iovec length accumulation loop.
  Rust-owned kernel/rust/syscall_policy.rs exports the strict x86_64
  iov_length ABI name and reads ABI-checked iovec lengths with wrapping
  size_t accumulation, while kernel/syscall.c keeps the original loop as a
  non-Rust fallback body outside MCKERNEL_RUST_SYSCALL_POLICY_HELPERS.
  Validation passed for rustfmt, focused mckernel.img build, exported-symbol
  and no-undefined checks for iov_length in mckernel_rust.o, the full
  equivalence harness including Rust object undefined-symbol/SIMD gates, and
  the active source-retirement audit reporting executable header count
  reduced from 85 to 84 and compiled retired C sources: 0. No guest runtime
  validation was run for this checkpoint.
- 2026-06-03: kernel/include/page.h moved to 100.0% for the
  source-retirement header audit. The page header now declares page_map,
  page_is_in_memobj, and page_is_multi_mapped instead of carrying executable
  public inline trampolines. Rust-owned kernel/rust/page_helpers.rs exports
  the strict x86_64 page_map, page_is_in_memobj, and page_is_multi_mapped ABI
  names, while kernel/mem.c keeps non-Rust fallback wrappers outside
  MCKERNEL_RUST_PAGE_HELPERS. Validation passed for rustfmt, focused
  mckernel.img build, exported-symbol and no-undefined checks for the three
  public page helpers in mckernel_rust.o, the full equivalence harness
  including Rust object undefined-symbol/SIMD gates, and the active
  source-retirement audit reporting executable header count reduced from 86
  to 85 and compiled retired C sources: 0. No guest runtime validation was
  run for this checkpoint.
- 2026-06-03: ihk/ikc/include/ikc/queue.h moved to 100.0% for
  the source-retirement header audit. The shared IKC queue header now
  declares ihk_ikc_channel_enabled instead of carrying executable inline
  channel-flag logic. Rust-owned kernel/rust/ikc_queue.rs and
  ihk/linux/core/rust/host_ikc.rs export the strict x86_64 kernel and IHK
  core-module ihk_ikc_channel_enabled ABI name, while ihk/ikc/queue.c keeps
  the original C predicate for non-Rust fallback builds and exports it through
  IHK_EXPORT_SYMBOL. ihk/linux/core/mikc.c exports the Rust-owned host-module
  symbol through EXPORT_SYMBOL. Validation passed for rustfmt, focused
  mckernel.img and ihk_ko builds, exported-symbol checks in mckernel_rust.o,
  core_helpers.o, ihk.ko, and Module.symvers, no-undefined checks for the Rust
  objects, the full equivalence harness including Rust object
  undefined-symbol/SIMD gates, and the active source-retirement audit reporting
  executable header count reduced from 87 to 86 and compiled retired C
  sources: 0. No guest runtime validation was run for this checkpoint.
- 2026-06-03: ihk/linux/include/ihk/ihk_rusage.h moved to
  100.0% for the source-retirement header audit. The installed rusage header
  now declares rusage_pgtype_to_pgsize instead of carrying executable inline
  switch logic. Rust-owned ihk/linux/user/rust/ihklib_helpers.rs exports the
  strict x86_64 user-library rusage_pgtype_to_pgsize ABI name, while
  ihk/linux/user/ihklib.c keeps the original C switch body outside
  IHKLIB_RUST_HELPERS for non-Rust builds. Validation passed for rustfmt,
  focused ihklib build, Rust-object and libihk.so exported-symbol checks,
  no-undefined check for the Rust object, the full equivalence harness
  including Rust object undefined-symbol/SIMD gates, and the active
  source-retirement audit reporting executable header count reduced from 88
  to 87 and compiled retired C sources: 0. No guest runtime validation was
  run for this checkpoint.
- 2026-06-03: lib/include/ihk/cpu.h moved to 100.0% for the
  source-retirement header audit. The shared CPU header now declares
  is_sampling_event instead of carrying executable inline logic. Rust-owned
  kernel/rust/x86_perfctr.rs exports the strict x86_64 is_sampling_event ABI
  name and reads the perf_event_attr sample_period field through an
  ABI-checked prefix offset, while arch/x86_64/kernel/perfctr.c and
  arch/arm64/kernel/perfctr.c keep the original C expression as fallback
  bodies outside the strict x86 Rust path. Validation passed for rustfmt,
  focused mckernel.img build, exported-symbol and no-undefined checks for
  is_sampling_event in mckernel_rust.o, the full equivalence harness including
  Rust object undefined-symbol/SIMD gates, and the active source-retirement
  audit reporting executable header count reduced from 89 to 88 and compiled
  retired C sources: 0. No guest runtime validation was run for this
  checkpoint.
- 2026-06-03: ihk/linux/include/ihk/ihk_host_driver.h moved to
  100.0% for the source-retirement header audit. The installed host-driver
  header now declares ihk_device_t_failed instead of carrying executable
  macro logic. Rust-owned ihk/linux/core/rust/core_helpers.rs exports the
  strict-build ihk_device_t_failed ABI name, and ihk/linux/core/host_driver.c
  keeps the non-Rust fallback definition outside IHK_CORE_RUST_HELPERS while
  exporting the helper from the IHK core module. Validation passed for
  rustfmt, focused ihk_core_rust_helpers and ihk_ko builds, Rust-object and
  ihk.ko exported-symbol checks, Module.symvers export verification, and the
  active source-retirement audit reporting executable header count reduced
  from 90 to 89 and compiled retired C sources: 0. No guest runtime
  validation was run for this checkpoint.
- 2026-06-03: arch/x86_64/kernel/include/signal.h moved to 100.0%
  for the source-retirement header audit. The header now contains declarations
  for valid_signal and __sigmask instead of executable inline/macro logic.
  Rust-owned syscall_policy.rs exports the strict-build valid_signal and
  __sigmask ABI names, while kernel/syscall.c keeps non-Rust fallback
  definitions outside MCKERNEL_RUST_SIGNAL_HELPERS. This preserves the public
  signal constants, sigset_t layout, and call sites in kernel/syscall.c and
  arch/x86_64/kernel/syscall.c while removing the x86_64 header's executable
  helper bodies from the strict Rust path. Validation passed for rustfmt,
  bash -n, focused mckernel.img build, equivalence including Rust object
  undefined-symbol/SIMD gates, exported-symbol checks for valid_signal and
  __sigmask, and the active source-retirement audit reporting executable
  header count reduced from 91 to 90 and compiled retired C sources: 0. No
  guest runtime validation was run for this checkpoint.
- 2026-06-03: tools/mcstat/runtest.c moved to 100.0% for the focused
  Rust-enabled x86_64 mcstat runtest target. Rust-enabled tools/mcstat CMake
  now builds tools/mcstat/rust/runtest.rs into runtest.o and uses that object
  for the mcstat_runtest target while keeping runtest.c available for
  non-Rust fallback builds. The Rust body exports the legacy main plus the
  global a, b, and c arrays and owns the invoked/wakeup/getpid/double-division
  loop/done sequencing from the original standalone test program. Retained
  boundaries are libc printf, sleep, getpid, and compiler-emitted libc helpers
  used by the user-space executable link. Validation passed for rustfmt,
  bash -n, standalone rustc object build, focused mcstat_runtest CMake build,
  build-graph absence of runtest.c in that target, exported-symbol and
  expected-undefined-symbol checks, object SIMD scan, and a non-reboot
  mcstat_runtest smoke run that printed the expected invoked/wakeup/getpid/done
  sequence. The default strict source-retirement audit reported compiled
  retired C sources: 0. No guest runtime validation was run for this
  checkpoint.
- 2026-06-03: executer/user/arch/x86_64/archdep_c.c moved to 100.0%
  for the Rust-enabled optional x86_64 UTI target. Rust-enabled user-tool
  CMake now builds executer/user/rust/archdep_uti_x86_64.rs into
  archdep_uti_x86_64.o and omits arch/x86_64/archdep_c.c from
  mck_syscall_intercept while keeping the C source available for non-Rust and
  non-x86_64 fallback builds. The Rust object exports the legacy
  uti_syscall6, uti_syscall3, uti_syscall1, and uti_syscall0 ABI names and
  owns x86_64 raw syscall register setup, syscall instruction dispatch, and
  return-value capture for the 0-, 1-, 3-, and 6-argument wrappers. Retained
  boundaries are the hardware syscall ABI itself and the surrounding bundled
  syscall_intercept preload/runtime behavior. Validation passed for rustfmt,
  bash -n, standalone rustc object build, exported-symbol/no-undefined/SIMD
  checks, separate ENABLE_UTI=ON Rust user-tools configure, focused
  mck_syscall_intercept build, build-graph absence of archdep_c.c, shared
  object export checks, and the default strict source-retirement audit
  reporting compiled retired C sources: 0. During validation a bundled
  syscall_intercept capstone include path was corrected after the first
  optional UTI build failed. No runtime UTI preload or syscall workload was run
  for this checkpoint.
- 2026-06-03: kernel/futex.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled kernel CMake now omits futex.c from
  the strict mckernel.img source list while keeping the C source available for
  fallback builds. The Rust kernel object exports the legacy get_futex_queues,
  futex_init, and futex ABI names and owns futex table allocation,
  publication, and hash initialization; futex command dispatch; private and
  shared key construction with vtop fault retry and logging; wake, requeue,
  wake-op, and wait wrapper sequencing; queue lock, enqueue, unqueue, wait
  setup, and wait body dispatch; Linux/McKernel wake target selection and IKC
  packet sequencing; plus invalid-command and vtop-error kprintf paths.
  Retained C boundaries are the x86 futex atomic-op bridge, spinlock
  primitives, current CPU/thread lookup, page-table vtop and page-fault
  primitives, user-value load and atomic side effects, sched wake/schedule and
  hassigpending, IKC send/channel-table state, allocation, virt_to_phys, and
  kprintf. Validation passed for rustfmt, bash -n, focused mckernel.img build,
  equivalence with updated undefined-symbol/SIMD gates, and clean non-reboot
  Rocky Rust build-only validation with --skip-ihk-patch; the active audit
  reported compiled retired C sources: 0. During validation the harness was
  corrected by adding weak stubs for the new primitive boundaries and removing
  a misplaced public futex smoke from the paired sched-helper C/Rust digest.
  No module-load, guest boot, or live futex workload validation was run for
  this checkpoint.
- 2026-06-03: executer/kernel/mcctrl/binfmt_mcexec.c moved to 100.0%
  for the strict Rust source-retirement audit. Rust-enabled mcctrl CMake now
  omits binfmt_mcexec.c from the strict mcctrl.ko source list while keeping
  the C source available for fallback builds. The Rust mcctrl helper object
  exports the legacy load_elf, binfmt_mcexec_init, and binfmt_mcexec_exit ABI
  names and owns ELF64 executable validation, mcctrl-alive and env-count
  gates, binfmt skip-path rejection, two-pass MCEXEC_WL argv/env scanning,
  whitelist path decision, mcexec open/argument rewrite/interpreter-change
  sequencing, registration wrapper dispatch, and final loader handoff
  selection. Retained C boundaries are Linux binfmt registration storage,
  randomized linux_binprm field access, d_path, get_user_pages/kmap/put_page,
  kmalloc/kfree, open_exec/fput, copy_strings_kernel, bprm_change_interp,
  prepare_binprm or Rocky probe-buffer refresh, and search_binary_handler.
  Validation passed for rustfmt, bash -n, focused mcctrl_ko build,
  equivalence including binfmt loader success/rejection smoke paths and
  undefined-symbol/SIMD gates, and clean non-reboot Rocky Rust build-only
  validation with --skip-ihk-patch. No module-load, live exec/binfmt workload,
  or guest runtime validation was run for this checkpoint.
- 2026-06-03: executer/kernel/mcctrl/arch/x86_64/archdeps.c moved to
  100.0% for the strict Rust source-retirement audit. Rust-enabled mcctrl
  CMake now omits arch/x86_64/archdeps.c from the strict mcctrl.ko source
  list while keeping the C source available for fallback builds. The Rust
  mcctrl helper object exports the legacy arch_symbols_init,
  reserve_user_space, get_vdso_info, get_user_sp, set_user_sp, restore_tls,
  save_tls_ctx, get_tls_ctx, get_rsp_ctx, translate_rva_to_rpa, and
  arch_switch_ctx ABI names and owns x86_64 VDSO symbol lookup publication,
  user-space reservation lock/map sequencing, VDSO/vvar/hpet/pvclock/vgtod
  descriptor population, user stack pointer dispatch, TLS save/query/restore
  wrapper sequencing, remote page-table walk translation including huge-page
  handling, and arch-switch no-op return shaping. Retained C boundaries are
  Linux kallsyms/vdso/mm locking/user-copy primitives, device map/unmap
  callbacks, privileged MSR/GS stack-pointer instructions, EXPORT_SYMBOL
  declarations, and Linux version-specific bridge constants. Validation passed
  for rustfmt, bash -n, focused mcctrl_ko build, equivalence including mcctrl
  archdeps smoke paths and undefined-symbol/SIMD gates, clean non-reboot Rocky
  Rust build-only validation with --skip-ihk-patch, and active
  source-retirement audit reporting compiled retired C sources: 0. During
  validation the x86_64 context layout mirror was corrected to the 160-byte C
  layout and TransUctx copy-in paths were changed to avoid emitting a memset
  dependency. No module-load, futex workload, or guest runtime validation was
  run for this checkpoint.
- 2026-06-03: ihk/ikc/linux.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled ihk/linux/core CMake now omits all
  ihk/ikc/*.c sources from the strict ihk.ko source list while keeping the C
  sources available for fallback builds. The Rust IHK core helper object
  exports the legacy ihk_ikc_get_master_channel, ihk_ikc_system_init,
  ihk_ikc_system_exit, ihk_ikc_alloc_queue, ihk_ikc_free_queue,
  ihk_ikc_malloc, ihk_ikc_free, call_arch_master_packet_handler,
  ihk_ikc_wait_init, ihk_ikc_wait_master, ihk_ikc_wake_master, and
  ihk_ikc_send ABI names and owns master/regular-channel receive dispatch,
  interrupt-handler descriptor publication, work-data callback publication,
  queue allocation/free sizing, atomic malloc/free dispatch, wait-wrapper
  dispatch, architecture master-packet call-through, and send retry/notify
  sequencing. Retained C boundaries are Linux GFP_ATOMIC page/kmalloc/free,
  waitqueue sleep/wake primitives, current-CPU lookup, interrupt handler
  registration, OS/channel accessors, interrupt issue callbacks, printk, and
  EXPORT_SYMBOL declarations. Validation passed for rustfmt, bash -n, focused
  ihk_ko build, equivalence including host IKC wrapper smoke paths, clean
  non-reboot Rocky Rust build-only validation with --skip-ihk-patch, active
  build-graph absence, exported-symbol checks, module-object SIMD scan, and
  active source-retirement audit reporting compiled retired C sources: 0. No
  module-load or guest runtime validation was run for this checkpoint. During
  smoke validation an x86 divide helper in the Rust IKC queue path was also
  fixed to keep the divisor out of rdx before div.
- 2026-06-03: ihk/linux/core/mem_alloc.c moved to 100.0% for the
  strict Rust source-retirement audit. Rust-enabled ihk/linux/core CMake now
  omits mem_alloc.c from the strict ihk.ko source list while keeping the C
  source available for fallback builds. The Rust IHK core helper object
  exports the legacy ihk_pagealloc_init, ihk_pagealloc_destroy,
  ihk_pagealloc_alloc, ihk_pagealloc_free, ihk_pagealloc_alloc_size, and
  ihk_pagealloc_free_size ABI names and owns descriptor sizing, allocation
  source selection, descriptor field publication, padding reservation,
  small/large allocation dispatch, locked map mutation, size wrappers, and
  destroy dispatch. Retained C boundaries are Linux page/kzalloc/free,
  spin-lock/IRQ bridges, printk, and EXPORT_SYMBOL declarations. Validation
  passed for rustfmt, bash -n, focused ihk_ko build, equivalence including
  host page-allocator smoke paths, active build-graph absence, exported-symbol
  checks, module-object SIMD scan, and active source-retirement audit
  reporting compiled retired C sources: 0. No module-load or guest runtime
  validation was run for this checkpoint.
- 2026-06-03: executer/kernel/mcctrl/arch/x86_64/cpu.c moved to 100.0%
  for the strict Rust source-retirement audit. Rust-enabled mcctrl CMake now
  omits arch/x86_64/cpu.c from the strict mcctrl.ko source list while keeping
  the C source available for fallback builds. The Rust mcctrl helper object
  exports the legacy cpu_restore_interrupt, cpu_pause,
  cpu_disable_interrupt_save, and cpu_enable_interrupt_save ABI names and owns
  the x86_64 pushfq/popfq/cli/sti/pause instruction sequencing. Validation
  passed for rustfmt, bash -n, focused mcctrl_ko build, equivalence
  module-object checks, active build-graph absence, exported-symbol checks,
  module-object SIMD scan, active source-retirement audit reporting compiled
  retired C sources: 0, tracker summary math, and whitespace checks. The
  privileged interrupt instructions were not invoked from the user-space smoke
  binary; no module-load, futex workload, or guest runtime validation was run
  for this checkpoint.
- 2026-06-03: ihk/linux/core/mm.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled ihk/linux/core CMake now omits mm.c
  from the strict ihk.ko source list while keeping the C source available for
  fallback builds. The Rust IHK core helper object exports the legacy
  ihk_host_map_generic and ihk_host_unmap_generic ABI names and preserves the
  existing not-implemented printk plus NULL/-ENOSYS return behavior.
  Validation passed for rustfmt, bash -n, focused ihk_ko build after adding
  export prototypes, equivalence including host-mm smoke checks, active
  build-graph absence, exported-symbol checks, module-object SIMD scan, active
  source-retirement audit reporting compiled retired C sources: 0, tracker
  summary math, and whitespace checks. No module-load or guest runtime
  validation was run for this checkpoint.
- 2026-06-03: executer/kernel/mcctrl/mc_plist.c moved to 100.0% for the
  strict Rust source-retirement audit. Rust-enabled mcctrl CMake now omits
  mc_plist.c from the strict mcctrl.ko source list while keeping the C source
  available for fallback builds. The Rust mcctrl helper object exports the
  legacy mc_plist_add and mc_plist_del ABI names and owns priority-list
  insertion, equal-priority FIFO node insertion, prio-list replacement on
  delete, and node/prio list unlink/reinit sequencing. Validation passed for
  rustfmt, bash -n, focused mcctrl_ko build, clean non-reboot Rocky
  Rust build-only validation with --skip-ihk-patch, active source-retirement
  audit reporting compiled retired C sources: 0, build-graph absence checks,
  exported-symbol checks, module-object SIMD scan, equivalence including the
  mcctrl plist smoke path, tracker summary math, and whitespace checks. No
  module-load, futex workload, or guest runtime validation was run for this
  checkpoint.
- 2026-06-03: the active source-retirement gate now enforces retired-row build
  graph absence with --fail-on-retired-compiled-c, and the strict Rust-enabled
  host ihk module no longer compiles ihk/ikc/master.c or ihk/ikc/queue.c as C.
  This does not change scored percentages because both rows were already
  100.0%, but it corrects the strict host-module evidence for those retired
  rows. Rust-enabled ihk/linux/core CMake now links rust/core_helpers.o into
  the ihk module and, when Rust helpers are enabled on x86_64, limits retained
  IKC C sources to ihk/ikc/linux.c. The new Rust host IKC body exports the
  legacy queue/master/channel ABI names for queue init/read/write, packet pool
  allocation/release, channel create/free/recv/enable/disable/find, listener
  listen/accept/connect/disconnect/destroy, master packet dispatch, wait-reply
  prepare/finish, and reply matching. Retained primitive boundaries remain
  Linux IKC send/interrupt, queue allocation/free, map/unmap, waitqueue,
  spin/IRQ, virt_to_phys, listener/channel accessors, architecture master
  packet fallback, and printk. Validation passed for rustfmt, bash -n,
  py_compile, ihk_ko build, clean non-reboot
  Rocky Rust build-only validation with --skip-ihk-patch, active
  source-retirement audit reporting compiled retired C sources: 0, exported
  symbol checks, build-graph absence checks, equivalence, module-object
  unresolved-symbol and SIMD checks, and smoke-test link stubs. The default
  validation path without --skip-ihk-patch still fails before configure because
  the local IHK compatibility patch no longer applies to this submodule state.
  No module-load or guest runtime validation was run for this checkpoint.
- 2026-06-03: ihk/linux/user/ihkmond.c moved to 100.0% for the
  strict Rust source-retirement audit. Rust-enabled CMake now builds the
  ihkmond executable from ihk/linux/user/rust/ihkmond.rs and omits
  ihk/linux/user/ihkmond.c from that strict user-tool target, while the
  fallback C source remains available outside the Rust-enabled path. The Rust
  body exports the legacy main ABI name. It owns option parsing, facility
  selection, daemon setup, pthread/eventfd/epoll initialization, udev monitor
  event dispatch, add/remove notification routing, hungup detection polling,
  kmsg buffer acquisition/release, file-buffer rotation, syslog transfer, and
  cleanup sequencing through retained libihk, udev, pthread, eventfd, ioctl,
  file I/O, syslog, and libc boundaries. Validation passed for rustfmt,
  strict ihkmond build, strict mckernel.img link, equivalence, active
  build-graph absence, Rust object graph evidence, exported-symbol checks,
  pre-daemon invalid-facility CLI smoke, and source-retirement audit. No
  module-load, daemon runtime, or device event validation was run for this
  batch.
- 2026-06-03: ihk/linux/user/ihkosctl.c moved to 100.0% for the
  strict Rust source-retirement audit. Rust-enabled CMake now builds the
  ihkosctl executable from ihk/linux/user/rust/ihkosctl.rs and omits
  ihk/linux/user/ihkosctl.c from that strict user-tool target, while the
  fallback C source remains available outside the Rust-enabled path. The Rust
  body exports the legacy main, __argc, and __argv ABI names. It owns usage
  and action dispatch, get/status/ikc_map handling, load/boot/shutdown,
  alloc/reserve/assign/release/set/query/query_free_mem/kargs/kmsg,
  clear_kmsg/intr/ioctl/dump sequencing, request allocation/free cleanup,
  device-path construction, parser/stringifier handoff, and retained libihk,
  ioctl, libc, libbfd, and makedumpfile boundaries. Validation passed for
  rustfmt, strict ihkosctl build, strict mckernel.img link, equivalence,
  active build-graph absence, Rust object graph evidence, exported-symbol
  checks, usage and read-only get CLI smokes, source-retirement audit, and
  git diff whitespace checks. No module-load or runtime device validation was
  run for this batch.
- 2026-06-03: ihk/linux/user/ihkconfig.c moved to 100.0% for the
  strict Rust source-retirement audit. Rust-enabled CMake now builds the
  ihkconfig executable from ihk/linux/user/rust/ihklib_helpers.rs with the
  ihkconfig_full_body cfg and omits ihk/linux/user/ihkconfig.c from that
  strict user-tool target, while the fallback C source remains available
  outside the Rust-enabled path. The Rust body exports the legacy main,
  __argc, __argv, and ihkconfig_usage_result ABI names. It owns usage and
  action dispatch, index-only get handling, reserve/release/query parsing,
  device-path construction, create/destroy/scratch/sbox/read/mmap/ioctl,
  clear_kmsg, clear_kmsg_write, and get buildid/os_instances sequencing
  through retained libihk, ioctl, parser, and libc boundaries. Validation
  passed for rustfmt, strict ihkconfig build, strict mckernel.img link,
  equivalence, active build-graph absence, Rust object/cfg graph evidence,
  exported-symbol checks, usage and read-only get CLI smokes,
  source-retirement audit, and git diff whitespace checks. No module-load or
  runtime device validation was run for this batch.
- 2026-06-03: executer/user/ldump2mcdump.c moved to 100.0% for the
  strict Rust source-retirement audit. Rust-enabled CMake now builds the
  ldump2mcdump crash extension shared object from
  executer/user/rust/ldump2mcdump_helpers.rs with a full-body cfg and omits
  executer/user/ldump2mcdump.c from that strict target, while the fallback C
  source remains available outside the Rust user-tool path. The Rust body
  exports the legacy cmd_ldump2mcdump, ldump2mcdump_init,
  ldump2mcdump_fini, help_ldump2mcdump, ldump2_count_chunks_result,
  ldump2_fill_chunks_result, and ldump2_physmem_name_result ABI names. It
  owns crash command registration, option parsing, dump-page map reads,
  chunk counting/filling, kernel-base publication, BFD section creation and
  content writes, physmem section naming, date/hostname/user metadata
  emission, and chunked physical-memory read/write sequencing through
  retained crash/libbfd/libc boundaries. Validation passed for rustfmt,
  strict ldump2mcdump shared-object build, strict mckernel.img link,
  active build-graph absence, exported-symbol checks, focused equivalence,
  source-retirement audit, and git diff whitespace checks. No crash-session
  runtime validation was run for this batch.
- 2026-06-03: lib/page_alloc.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now omits lib/page_alloc.c from
  the strict mckernel.img source list while the fallback C source remains
  available outside RUST_KERNEL_ENABLED. The Rust page allocator exports the
  legacy zero_at_free, deferred_zero_at_free, __ihk_pagealloc_init,
  ihk_pagealloc_init/destroy/alloc/reserve/free/count/query_free,
  __ihk_pagealloc_zero_free_pages, ihk_numa_add_free_pages,
  __ihk_numa_zero_free_pages, ihk_numa_zero_free_pages,
  ihk_numa_alloc_pages, and ihk_numa_free_pages ABI names. It owns the public
  bitmap allocator wrapper layer, descriptor init/destroy dispatch, lock
  callback handoff, double-free error routing, zero-free logging, NUMA
  add/alloc/free/zero wrapper dispatch, deferred-zero packet setup handoff,
  CPU-local cache selection, and retained primitive boundary calls into
  allocator, lock, CPU-local, IKC, kprintf, panic, and phys_to_virt providers.
  Validation passed for rustfmt, strict mckernel.img link, active build-graph
  absence, exported-symbol checks, focused equivalence with the updated
  119-symbol retained-boundary audit, source-retirement audit, and git diff
  whitespace checks. No guest-only runtime validation was run for this batch.
- 2026-06-03: kernel/procfs.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now includes
  kernel/rust/procfs.rs in the Rust kernel object and omits kernel/procfs.c
  from the strict mckernel.img source list, while the fallback C source remains
  available outside RUST_KERNEL_ENABLED. The Rust body exports the legacy
  process_procfs_request, procfs_create_thread, and procfs_delete_thread ABI
  names. It owns procfs request map/unmap orchestration, release handling,
  pbuf/default-buffer selection, mcos/PID/TID path dispatch, root mckernel/stat
  output dispatch, mem/maps/pagemap/status/auxv/cmdline/comm/stat entry
  sequencing, bitmask buffer construction, procfs backlog retry handoff,
  finish-request publication, and cleanup/ref-release ordering through retained
  primitive boundaries. Process lookup/refcount, thread lookup/refcount,
  memory-range locking, page-table/pagemap, user memory copy/page fault,
  allocator, IKC answer send, map/unmap, bitmap formatting, and kprintf bodies
  remain audited C boundaries. Validation passed for rustfmt, strict
  mckernel.img link, active build-graph absence, exported-symbol checks,
  focused equivalence with the updated 121-symbol retained-boundary audit,
  source-retirement audit, and git diff whitespace checks.
- 2026-06-02: kernel/init.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now includes kernel/rust/init.rs
  in the Rust kernel object and omits kernel/init.c from the strict
  mckernel.img source list, while the fallback C source remains available
  outside RUST_KERNEL_ENABLED. The Rust body exports the legacy main,
  monitor_init, monitor, rusage, mck_num_processors, data, multi_intr_mode,
  nmi_mode, and host_ikc_inited ABI names. It owns boot command-line parsing,
  dump-level and scheduler option publication, time-of-day origin setup,
  monitor allocation/publication, multi-interrupt/NMI address publication,
  rest-init ordering, post-init IKC/sysfs/AP startup sequencing, and top-level
  main boot sequencing through retained primitive boundaries. Architecture
  init/ready/status, memory/proc/futex/scheduler/sysfs setup, allocator,
  CPU/interrupt, IKC, syscall registration, and boot-time provider bodies remain
  audited boundaries. Validation passed for rustfmt, strict mckernel.img link,
  active build-graph absence, exported-symbol checks, focused equivalence with
  the updated 110-symbol retained-boundary audit, source-retirement audit, and
  git diff whitespace checks.
- 2026-06-02: kernel/ap.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now includes kernel/rust/ap.rs
  in the Rust kernel object and omits kernel/ap.c from the strict mckernel.img
  source list, while the fallback C source remains available outside
  RUST_KERNEL_ENABLED. The Rust body exports the legacy num_processors,
  ap_syscall_semaphore, show_int_ops, ap_start, ap_init, cpu_sysfs_setup,
  smp_func_call_handler, and smp_call_func ABI names. It owns AP wait/start/init
  sequencing, BSP/AP CPU count publication, CPU sysfs show/store/setup,
  SMP request queue drain, SMP call request allocation/publication, local
  callback dispatch, remote interrupt dispatch, atomic wait/completion, and
  result aggregation through retained primitive boundaries. Boot/tick,
  scheduler, IKC, sysfs, lock/list/atomic, allocator, kprintf, and CPU
  primitive bodies remain audited boundaries. Validation passed for rustfmt,
  strict mckernel.img link, active build-graph absence, exported-symbol checks,
  focused equivalence with the updated 93-symbol retained-boundary audit,
  source-retirement audit, and git diff whitespace checks.
- 2026-06-02: kernel/debug.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now includes
  kernel/rust/debug.rs in the Rust kernel object and omits kernel/debug.c from
  the strict mckernel.img source list, while the fallback C source remains
  available outside RUST_KERNEL_ENABLED. The Rust body exports the legacy
  kmsg_buf, kprintf_lock, kprintf_unlock, kputs, __kprintf, kprintf,
  kmsg_init, and dynamic_debug_sysfs_setup ABI names. It owns kmsg ring-buffer
  append/head/tail/overflow sequencing, kprintf prefix/format publication,
  high-water eventfd dispatch selection, dynamic-debug sysfs show/store
  parsing, ddebug flag mutation, and sysfs setup through retained primitive
  boundaries. Spinlock and interrupt primitives, eventfd, delay, sysfs_createf,
  verbose-section linker symbols, CPU-id lookup, and the Rust-owned
  snprintf/vsnprintf/string parser ABI remain audited boundaries. Validation
  passed for rustfmt, strict mckernel.img link, active build-graph absence,
  exported-symbol checks, focused equivalence with the updated 81-symbol
  retained-boundary audit, source-retirement audit, bash syntax check for the
  harness, and git diff whitespace checks.
- 2026-06-02: arch/x86_64/kernel/local.c, ihk/cokernel/smp/ikc.c, and
  ihk/cokernel/smp/x86_64/dma.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now includes
  kernel/rust/x86_local.rs, kernel/rust/smp_ikc.rs, and
  kernel/rust/builtin_dma.rs in the Rust kernel object and omits the three C
  objects from the strict mckernel.img source list, while fallback C sources
  remain outside RUST_KERNEL_ENABLED. The Rust bodies export the legacy
  CPU-local, processor-id, SMP IKC interrupt/init/send, and DMA init/request
  ABI names. Focused validation passed for rustfmt, strict mckernel.img link,
  exported-symbol checks, active build-graph absence checks, the combined
  equivalence harness, unresolved-symbol/SIMD audit, source-retirement audit,
  and git diff whitespace checks. Retained boundaries include boot_param,
  map_fixed_area, host IPI issue, kprintf, allocator, CPU pause, spinlock, and
  low-level map/queue primitives.
- 2026-06-02: kernel/timer.c, arch/x86_64/kernel/perfctr.c,
  arch/x86_64/kernel/vsyscall.c, arch/x86_64/kernel/lock.c, and kernel/ubsan.c
  moved to 100.0% for the strict Rust source-retirement audit. Rust-enabled
  CMake now includes
  kernel/rust/timer.rs, kernel/rust/x86_perfctr.rs,
  kernel/rust/x86_vsyscall.rs, and kernel/rust/ubsan.rs in the Rust kernel
  object and omits timer.c, x86_64 perfctr.c, x86_64 vsyscall.c, and the
  conditional UBSAN C handler source from strict Rust builds. The inactive
  x86_64 lock.c #if-0 spinlock bodies were deleted, leaving no executable C
  implementation in that source. The Rust bodies export the legacy timer,
  vsyscall, performance-counter, and UBSAN ABI names; own timer
  initialization/sleep/wake-loop dispatch, vsyscall section/data publication,
  x86 perf-counter discovery/MSR/PMC/allocation entry points, and UBSAN
  diagnostic handler formatting; and retain schedule, waitqueue,
  CPU-local, NUMA-zeroing, kprintf/panic, gettime, and setup-provided
  perf-extra-register lookups as audited boundary calls. The focused
  equivalence harness now stubs and audits the newly intentional retained
  ihk_numa_zero_free_pages, ihk_mc_get_extra_reg_event, and
  calculate_time_from_tsc boundary symbols, raising the retained unresolved
  primitive audit from 77 to 80. Strict mckernel.img build, rustfmt,
  exported-symbol, build-graph absence, focused equivalence, and
  source-retirement audit validation passed.
- 2026-06-02: kernel/cls.c, kernel/copy.c, kernel/freeze.c,
  kernel/listeners.c, and kernel/profile.c moved to 100.0% for the strict
  Rust source-retirement audit. Rust-enabled CMake now includes
  kernel/rust/cls.rs, kernel/rust/freeze.rs, kernel/rust/listeners.rs, and
  kernel/rust/profile.rs in the Rust kernel object and omits these five C
  objects from the strict mckernel.img source list; kernel/copy.c remains only
  fallback-source debt outside RUST_KERNEL_ENABLED because the strict non-DMA
  path has no active implementation body. The Rust body exports the legacy
  CPU-local/preempt/backlog, freeze/thaw, IKC listener test, and profile ABI
  names while retaining low-level timer, print, map/unmap, NMI, IKC, and
  allocator primitives as audited boundary calls. The focused equivalence
  harness now initializes Rust-owned CPU-local preempt depth for Rust-side
  spinlock/rwlock checks and audits the current 77 retained unresolved
  primitive symbols. Strict mckernel.img build, rustfmt, exported-symbol,
  build-graph absence, focused equivalence, source-retirement audit, and
  git diff whitespace validation passed.
- 2026-06-02: kernel/gencore.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now includes
  kernel/rust/gencore.rs in the Rust kernel object and omits gencore.c from
  the strict mckernel.img source list, while the fallback C source remains
  available outside RUST_KERNEL_ENABLED. The Rust body exports the legacy
  fill_elf_header, get_prstatus_size, get_prpsinfo_size, get_auxv_size,
  fill_prstatus, fill_prpsinfo, fill_auxv, get_note_size, fill_note,
  gencore, and freecore ABI names. The wrapper owns public core-image
  generation sequencing, NOTE thread-list traversal under the existing
  reader-lock boundary, allocation/free/zero callbacks, VM-range scan dispatch,
  program-header/coretable publication dispatch, and failure cleanup through
  the already verified gencore Rust helper bodies and retained VM/page-table
  primitive ABI. Strict mckernel.img build, symbol, build-graph absence,
  rustfmt, focused equivalence, and source-retirement audit validation passed.
- 2026-06-02: arch/x86_64/kernel/coredump.c moved to 100.0% for the
  strict Rust source-retirement audit. Rust-enabled CMake now includes
  kernel/rust/x86_coredump.rs in the Rust kernel object and omits the x86_64
  coredump C object from the strict mckernel.img source list. The Rust body
  exports arch_fill_prstatus, arch_fill_thread_core_info, and
  arch_get_thread_core_info_size, and owns ELF prstatus PID/PPID/signal,
  general-register, FS/GS base, and fpvalid publication plus the legacy
  no-extra-thread-core-info behavior. The focused coredump smoke validates the
  deterministic field-mapping body, parent/no-parent PPID behavior,
  unchanged unrelated prstatus fields, the no-op thread-core hook, and zero
  thread-core-info size without executing privileged RDMSR in userspace.
  Strict mckernel.img build, symbol, absence, rustfmt, focused equivalence,
  and source-retirement audit validation passed.
- 2026-06-02: arch/x86_64/kernel/mikc.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now omits the x86_64 arch MIKC
  C object from the strict mckernel.img source list while kernel/rust/mikc.rs
  exports ihk_mc_ikc_init_first_local and the arch_master_channel_packet_handler
  data symbol expected by the IKC manycore layer. The Rust body owns MIKC
  system-init dispatch, channel zeroing, queue-page calculation, receive/send
  queue page allocation, queue initialization, arch handler publication,
  descriptor initialization, channel enablement, and boot-parameter queue
  publication through the retained IKC, allocator, and arch queue primitive
  ABI. The MIKC equivalence smoke now validates queue-page arithmetic,
  allocation flags and page counts, queue init arguments, descriptor init
  arguments, enable sequencing, boot queue publication, channel zeroing, and
  arch handler storage. Strict mckernel.img build, symbol, absence, rustfmt,
  focused equivalence, and source-retirement audit validation passed.
- 2026-06-02: kernel/mikc.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now includes kernel/rust/mikc.rs
  in the Rust object and omits kernel/mikc.c from the strict mckernel.img
  source list, while the Rust body exports the legacy ihk_ikc_master_init and
  ihk_mc_get_master_channel ABI names. The Rust body owns master-channel
  allocation, initial IKC channel setup dispatch, master-channel pointer
  publication, and INIT_ACK packet handling that publishes host_ikc_inited
  through the retained IKC and allocator primitive ABI. The equivalence harness
  now stubs and audits ihk_mc_ikc_init_first and host_ikc_inited as intentional
  retained primitives and includes a MIKC smoke that validates allocator
  size/flag selection, init-handler registration, channel publication, ACK
  handling, and non-ACK no-op behavior. Strict mckernel.img build, symbol,
  absence, rustfmt, focused equivalence, and source-retirement audit validation
  passed.
- 2026-06-02: executer/user/arch/x86_64/arch-eclair.c moved to 100.0%
  for the strict Rust user-tools source-retirement audit. Rust-enabled x86_64
  CMake now builds executer/user/rust/arch_eclair_x86_64.rs as the eclair
  architecture-glue object and omits arch/x86_64/arch-eclair.c from the
  strict eclair source list, while the Rust object exports the legacy
  linux_page_offset, print_kregs, virt_to_phys, arch_setup_constants, and
  arch_read_kregs ABI names. The Rust body owns x86 kernel-register remote
  packet formatting, x86 virtual-to-physical address classification,
  architecture constant setup, Linux page-offset publication, and kernel-register
  read dispatch through the existing common eclair symbol/memory/read helpers.
  Strict eclair build, C-fallback eclair build, -h CLI smoke, exported-symbol
  check, build-graph absence check, rustfmt, strict mckernel.img build, and
  source-retirement audit validation passed.
- 2026-06-02: executer/user/libsched_yield.c moved to 100.0% for the strict
  Rust user-tools source-retirement audit. Rust-enabled x86_64 CMake now
  builds executer/user/rust/libsched_yield.rs as the sched_yield shared
  library object and omits libsched_yield.c from the strict sched_yield source
  list, while the Rust object exports the legacy sched_yield and pthread_create
  interposition ABI names. The Rust body owns sched_yield return behavior,
  RTLD_NEXT pthread_create lookup caching, McKernel clone syscall notification
  dispatch, and original pthread_create handoff through the existing libc/dl
  ABI. Strict sched_yield build, exported-symbol check, build-graph absence
  check, LD_PRELOAD loader smoke, ldd -r, rustfmt, strict mckernel.img build,
  and source-retirement audit validation passed.
- 2026-06-02: tools/mcstat/mcstat.c moved to 100.0% for the strict Rust
  user-tools source-retirement audit. Rust-enabled x86_64 CMake now builds
  tools/mcstat/rust/mcstat.rs as the mcstat object and omits mcstat.c from the
  strict mcstat executable source list, while the Rust object exports the
  legacy main ABI and links through the existing C/libihk path. The Rust body
  owns option parsing, usage/status/statistics/CPU output sequencing,
  /dev/mcos path construction, ihklib rusage aggregation, memory scale/unit
  selection, ioctl status and CPU monitor dispatch, loop/count/header
  sequencing, and legacy error text through the existing libc/ihklib ABI.
  Strict mcstat build, -h CLI smoke, Rust main symbol check, build-graph
  absence check, rustfmt, strict mckernel.img build, and source-retirement
  audit validation passed.
- 2026-06-02: kernel/pager.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now includes
  kernel/rust/pager.rs in the Rust object and omits pager.c from the strict
  mckernel.img source list. The Rust body exports the legacy do_pagein,
  do_pageout, myalloc_finalize, myalloc, myfree, and print_region ABI names
  and owns swapfile header/area bookkeeping, pager allocation-window
  management, Linux syscall forwarding loops, mlock-list request sequencing,
  pageout range classification, page-write/remove/munmap ordering, pagein
  read/fault/copy sequencing, and swapinfo publication/cleanup through the
  existing primitive ABI. The equivalence harness was updated to stub and audit
  the newly intentional retained pager primitives: copy_to_user,
  copy_from_user, ihk_mc_pt_virt_to_phys, ihk_mc_pt_virt_to_phys_size,
  ihk_mc_pt_free_range, page_fault_process_vm, lookup_process_memory_range,
  next_process_memory_range, sys_mlock, and sys_munlock. Strict mckernel.img
  build, symbol, absence, rustfmt, source-retirement audit, focused
  equivalence, and bash -n validation passed.
- 2026-06-02: kernel/sysfs.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now includes
  kernel/rust/sysfs.rs in the Rust object and omits sysfs.c from the strict
  mckernel.img source list. The Rust body exports the legacy sysfs_createf,
  sysfs_mkdirf, sysfs_symlinkf, sysfs_lookupf, sysfs_unlinkf,
  sysfss_packet_handler, and sysfs_init ABI names and owns public sysfs
  request allocation/path-format/IKC-send sequencing, special sysfs setup,
  sysfss packet dispatch, show/store/release callback dispatch, and sysfs init
  request setup through the existing primitive ABI. The equivalence harness was
  updated to stub and audit ihk_ikc_send as an intentional retained primitive.
  Strict mckernel.img build, symbol, absence, rustfmt, source-retirement audit,
  focused equivalence, C fallback mckernel.img, bash -n, and git diff --check
  validation passed.
- 2026-06-02: ihk/ikc/master.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now includes
  kernel/rust/ikc_master.rs in the Rust kernel object and omits master.c from
  the strict mckernel.img source list, while the C source remains available
  only as fallback source outside the strict Rust object path. The Rust body
  exports the legacy listen, accept, master packet handler, wait-reply
  prepare/finish, reply handler, connect, disconnect, disconnect-ack helper,
  and destroy-channel ABI names. It owns listener-slot publication,
  connect-accept validation, remote queue/channel reply shaping, packet-on-
  channel dispatch, disconnect-ack flag publication, wait-list insertion and
  reply matching, synchronous connect result publication, and disconnect
  state transitions through the existing IKC primitive ABI. Retained
  boundaries remain spinlock primitives, queue/channel allocation/free,
  regular-channel publication, queue empty/recv-handler dispatch, master send,
  architecture master packet handling, virt_to_phys, kprintf, and wait
  primitive behavior. Strict mckernel.img build/link, active build-graph
  absence, exported-symbol checks, focused equivalence with the updated
  81-symbol unresolved-boundary audit, source-retirement audit, rustfmt,
  bash -n, and whitespace validation passed.
- 2026-06-02: ihk/ikc/queue.c and ihk/ikc/manycore.c moved to 100.0%
  for the strict Rust source-retirement audit. Rust-enabled CMake now includes
  kernel/rust/ikc_queue.rs and kernel/rust/ikc_manycore.rs in the Rust kernel
  object and omits both C objects from the strict mckernel.img source list,
  while the C sources remain available only as fallback source outside the
  strict Rust object path. The Rust bodies export the legacy IKC queue,
  channel, and manycore ABI names and own queue init/read/write/empty/full,
  channel descriptor init, packet pool alloc/release, channel
  create/free/recv/recv-handler/enable/disable/find, manycore channel-list and
  regular-channel state, IKC send, system init/exit, queue allocation/free
  wrappers, master wait-list/wake, and unique-channel ID dispatch. Allocation,
  page allocation/free, map/unmap, virt_to_phys, interrupt
  register/unregister/vector lookup, architecture master packet handling,
  arch_set_mikc_queue, kprintf, CPU interrupt primitives, and
  smp_func_call_handler remain explicit retained primitive ABI boundaries.
  Rustfmt, strict mckernel.img build/link, active build-graph absence,
  exported-symbol checks, focused equivalence including updated MIKC smoke and
  83-symbol unresolved-boundary audit, source-retirement audit, and
  whitespace validation passed.
- 2026-06-02: kernel/fileobj.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now omits fileobj.c from the
  strict mckernel.img source list, while kernel/rust/fileobj.rs exports the
  legacy fileobj_create and fileobj_list_lock ABI names and owns the
  file-backed memobj ops table plus create, free, page I/O, get, flush,
  invalidate, lookup, page-hash, path, RSS-accounting, and premap/regular
  page sequencing through the existing primitive ABI. Focused equivalence,
  strict mckernel.img build, symbol, absence, source-retirement audit, and C
  fallback mckernel.img validation passed.
- 2026-06-02: kernel/shmobj.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake now omits shmobj.c from the
  strict mckernel.img source list, while kernel/rust/shmobj.rs exports the
  legacy to_shmobj, shmobj_list_lock, shmobj_list_unlock, shmlock_user_free,
  shmlock_user_get, shmobj_create, shmobj_create_indexed, the_seq, and
  shmlock_users_lock_body ABI names. The Rust body owns the shared-memory
  memobj ops table and callbacks, page-list lookup/update/get sequencing,
  user-page allocation/free, shmlock user lookup/free lifecycle, indexed
  object cleanup, shmid bitmap release, and RSS accounting inline behavior
  through the existing primitive ABI. Strict mckernel.img build, symbol,
  absence, rustfmt, and source-retirement audit checks passed.
- 2026-06-02: kernel/hugefileobj.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled non-FUGAKU CMake now omits
  hugefileobj.c from the strict mckernel.img source list, while
  kernel/rust/hugefileobj.rs exports the legacy hugefileobj_cleanup,
  hugefileobj_pre_create, hugefileobj_create, and hugefileobj_ops ABI names.
  The Rust body preserves the normal hugefile object list lifecycle, page array
  allocation and zeroing, user-page allocation/free callbacks, PFN lookup
  callback dispatch, mapper result publication, and pre-create pager callback
  sequencing through the existing primitive ABI. The FUGAKU-specific
  eager-prepopulation branch remains C fallback debt outside the normal strict
  Rust object path. Focused equivalence, strict mckernel.img build, symbol,
  absence, source-retirement audit, final hygiene, and C fallback
  mckernel.img validation passed.
- 2026-06-02: kernel/devobj.c and kernel/zeroobj.c moved to 100.0%
  for the strict Rust source-retirement audit. Rust-enabled CMake omits
  zeroobj.c from all strict Rust kernel builds and omits devobj.c for the
  non-FUGAKU strict Rust target. kernel/rust/zeroobj.rs and
  kernel/rust/devobj.rs now export the legacy zeroobj_create and devobj_create
  ABI names and own the private memobj ops callbacks/object-private storage
  needed by those create paths. The Rust devobj body preserves pager
  map/PFN/unmap forwarding, PFN-table allocation and locking, path publication,
  write-combined flag propagation, and local physical mapping dispatch through
  the existing kernel primitive ABI. The Rust zeroobj body preserves singleton
  publication, zero-page allocation,
  page-hash insertion, page metadata initialization, list insertion, refcount
  publication, and get_page/free ops behavior. Focused equivalence, strict
  mckernel.img build, symbol, absence, source-retirement audit, and C fallback
  mckernel.img validation passed. The C sources remain available only as
  fallback/FUGAKU-specific source outside the normal strict Rust object path.
- 2026-06-02: kernel/host_helpers.c and
  arch/x86_64/kernel/memory_helpers.c moved to 100.0% for the strict Rust
  source-retirement audit. Both files compiled as empty Rust-dispatched
  fallback carriers in the strict build; Rust-enabled CMake now omits their C
  objects entirely from mckernel.img. The Rust object exports the corresponding
  host helper and x86 memory helper ABI surfaces, while the C sources remain
  available only as fallback source outside the strict Rust object path.
- 2026-06-02: kernel/object_helpers.c, kernel/process_helpers.c, and
  kernel/sched_helpers.c moved to 100.0% for the strict Rust source-retirement
  audit. These files already compiled as Rust-dispatched empty fallback
  carriers in the strict build; Rust-enabled CMake now omits their C objects
  entirely from mckernel.img. The Rust object exports the corresponding object,
  process, and scheduler/futex helper ABI surfaces, while the C sources remain
  available only as fallback source outside the strict Rust object path.
- 2026-06-02: lib/abort.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake omits the C object from the
  strict mckernel.img source list, while kernel/rust/abort.rs exports the
  legacy panic and ihk_mc_debug_show_interrupt_context ABI names directly.
  The Rust panic body preserves monitor panic-status publication, status
  eventfd dispatch, interrupt disable, message print, stack print, and
  architecture stop/halt sequencing through the existing callback ABI. Focused
  equivalence, strict mckernel.img build, symbol, absence, and
  source-retirement audit validation passed. The C source remains available
  only as fallback source outside the strict Rust object path.
- 2026-06-02: lib/bitops.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake omits the C object from the
  strict mckernel.img source list, while kernel/rust/bitops.rs exports the
  legacy find-bit, hweight, fls/ffs/__ffs/ffz, set_bit, clear_bit, and test_bit
  ABI names. Focused equivalence, strict mckernel.img build, symbol, absence,
  and source-retirement audit validation passed. The C source remains available
  only as fallback source outside the strict Rust object path.
- 2026-06-02: lib/vsprintf.c moved to 100.0% for the strict Rust
  source-retirement audit. Rust-enabled CMake omits the C object from the
  strict mckernel.img source list, and Rust now exports _ctype plus the legacy
  vsnprintf, vscnprintf, snprintf, scnprintf, vsprintf, sprintf, vsscanf, and
  sscanf ABI names directly. Focused equivalence, strict mckernel.img build,
  symbol, absence, and source-retirement audit validation passed. The C source
  remains available only as fallback source outside the strict Rust object path.
- 2026-06-02: kernel/waitq.c, lib/bitmap.c, and lib/string.c moved to
  100.0% for the strict Rust source-retirement audit. Rust-enabled CMake omits
  waitq.c plus lib/bitmap.c and lib/string.c from the strict mckernel.img
  source list, Rust exports the legacy waitq, bitmap, string, and
  flatten_strings_from_user ABI names, and focused equivalence plus
  mckernel.img build validation passed. The C sources remain available only as
  fallback source outside the strict Rust object path.
- 2026-06-02: kernel/rbtree.c, kernel/llist.c, and kernel/plist.c moved to
  100.0% for the strict Rust source-retirement audit. Rust-enabled CMake omits
  these C objects from MCKERNEL_SRCS, Rust exports the legacy ABI names, and
  focused equivalence plus mckernel.img build validation passed. The C sources
  remain available only as fallback source debt outside the strict Rust object
  path.
- 2026-06-18: arch/x86_64/kernel/memory.c moved from 99.5% to 99.55%.
  Rust-enabled builds now route ihk_mc_reserve_arch_pages() through Rust while
  preserving the no-Rust C fallback definition and keeping reserve callback,
  virt_to_phys(), and panic side effects behind C bridges. Focused
  equivalence, strict/fallback mckernel.img builds, symbol checks, broad
  Rocky build-only validation, and the source-retirement audit passed.

Master category completion table:

+---------------------------------+------+--------+------+---------+------+-----+------------+-------+
| Category                        | Rows | Scored | 0.0% | Partial | 100% | n/a | Completion |  Left |
+---------------------------------+------+--------+------+---------+------+-----+------------+-------+
| ABI/layout foundation           |    2 |      0 |    0 |       0 |    0 |   2 |        n/a |   n/a |
| Other in-scope code             |  104 |     97 |    0 |       0 |   97 |   7 |     100.0% |  0.0% |
| Page allocator                  |    1 |      1 |    0 |       0 |    1 |   0 |     100.0% |  0.0% |
| Process/VM management           |    4 |      3 |    0 |       0 |    3 |   1 |     100.0% |  0.0% |
| Scheduler/timers/wait/futex     |    3 |      3 |    0 |       0 |    3 |   0 |     100.0% |  0.0% |
| Shared primitives               |   37 |     36 |    0 |       0 |   36 |   1 |     100.0% |  0.0% |
| Syscall core                    |    3 |      1 |    0 |       0 |    1 |   2 |     100.0% |  0.0% |
| User tools                      |   35 |     28 |    0 |       0 |   28 |   7 |     100.0% |  0.0% |
| host/IKC/mcctrl/IHK modules     |  110 |     70 |    0 |       0 |   70 |  40 |     100.0% |  0.0% |
| procfs/sysfs/xpmem/file objects |    9 |      9 |    0 |       0 |    9 |   0 |     100.0% |  0.0% |
| x86_64 memory management        |    5 |      3 |    0 |       0 |    3 |   2 |     100.0% |  0.0% |
+---------------------------------+------+--------+------+---------+------+-----+------------+-------+

Total percentage points left: 0.0%

Campaign progress since source-retirement baseline:

Baseline is the first master table for this campaign. Current is the master
table above. Delta points are completion percentage-point movement.

+---------------------------------+----------+----------+--------+----------+----------+
| Category                        | Baseline |  Current | Delta  | 0.0% row | 100% row |
+---------------------------------+----------+----------+--------+----------+----------+
| ABI/layout foundation           |      n/a |      n/a |    n/a |        0 |        0 |
| Other in-scope code             |    71.4% |   100.0% | +28.6% |      -23 |      +68 |
| Page allocator                  |     0.0% |   100.0% | +100.0% |       -1 |       +1 |
| Process/VM management           |    48.9% |   100.0% | +51.1% |       -2 |       +2 |
| Scheduler/timers/wait/futex     |     0.0% |   100.0% | +100.0% |       -3 |       +3 |
| Shared primitives               |    73.8% |   100.0% | +26.2% |       -6 |      +25 |
| Syscall core                    |    30.3% |   100.0% | +69.7% |       -2 |       +1 |
| User tools                      |    22.7% |   100.0% | +77.3% |      -23 |      +21 |
| host/IKC/mcctrl/IHK modules     |    49.8% |   100.0% | +50.2% |      -51 |      +42 |
| procfs/sysfs/xpmem/file objects |     0.0% |   100.0% | +100.0% |       -8 |       +9 |
| x86_64 memory management        |    35.7% |   100.0% | +64.3% |       -3 |       +2 |
+---------------------------------+----------+----------+--------+----------+----------+
| Overall scored categories       |    54.4% |   100.0% | +45.6% |     -122 |     +167 |
+---------------------------------+----------+----------+--------+----------+----------+

Fixed-width inventory table:

+--------------------------------------------------------+---------------------------------+------------+
| File                                                   | Category                        | Completion |
+--------------------------------------------------------+---------------------------------+------------+
| ihk/linux/core/abi_checks.c                            | ABI/layout foundation           |        n/a |
| kernel/rust/abi_checks.c                               | ABI/layout foundation           |        n/a |
| arch/x86_64/elfboot/elfboot.c                          | Other in-scope code             |        n/a |
| arch/x86_64/kboot/main.c                               | Other in-scope code             |        n/a |
| arch/x86_64/kernel/coredump.c                          | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/cpu.c                               | Other in-scope code             |        n/a |
| arch/x86_64/kernel/include/arch-bitops.h               | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/arch-futex.h                | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/arch-lock.h                 | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/arch-string.h               | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/arch/auxvec.h               | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/arch/cpu.h                  | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/arch/mm.h                   | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/arch/mman.h                 | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/arch/shm.h                  | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/cas.h                       | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/cpulocal.h                  | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/elf.h                       | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/errno.h                     | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/hwcap.h                     | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/ihk/atomic.h                | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/ihk/context.h               | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/ihk/ikc.h                   | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/ihk/types.h                 | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/prctl.h                     | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/registers.h                 | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/rlimit.h                    | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/signal.h                    | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/include/syscall_list.h              | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/local.c                             | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/lock.c                              | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/mikc.c                              | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/perfctr.c                           | Other in-scope code             |     100.0% |
| arch/x86_64/kernel/vsyscall.c                          | Other in-scope code             |     100.0% |
| executer/include/defs.h                                | Other in-scope code             |        n/a |
| executer/include/md5.h                                 | Other in-scope code             |        n/a |
| executer/include/pmi.h                                 | Other in-scope code             |     100.0% |
| executer/include/qlmpi.h                               | Other in-scope code             |     100.0% |
| executer/include/qlmpilib.h                            | Other in-scope code             |     100.0% |
| executer/include/uprotocol.h                           | Other in-scope code             |     100.0% |
| executer/include/uti.h                                 | Other in-scope code             |     100.0% |
| kernel/ap.c                                            | Other in-scope code             |     100.0% |
| kernel/cls.c                                           | Other in-scope code             |     100.0% |
| kernel/copy.c                                          | Other in-scope code             |     100.0% |
| kernel/debug.c                                         | Other in-scope code             |     100.0% |
| kernel/freeze.c                                        | Other in-scope code             |     100.0% |
| kernel/gencore.c                                       | Other in-scope code             |     100.0% |
| kernel/host.c                                          | Other in-scope code             |     100.0% |
| kernel/host_helpers.c                                  | Other in-scope code             |     100.0% |
| kernel/include/affinity.h                              | Other in-scope code             |     100.0% |
| kernel/include/amemcpy.h                               | Other in-scope code             |     100.0% |
| kernel/include/asm.h                                   | Other in-scope code             |     100.0% |
| kernel/include/auxvec.h                                | Other in-scope code             |     100.0% |
| kernel/include/cls.h                                   | Other in-scope code             |     100.0% |
| kernel/include/elfcore.h                               | Other in-scope code             |     100.0% |
| kernel/include/elfcoregpl.h                            | Other in-scope code             |     100.0% |
| kernel/include/futex.h                                 | Other in-scope code             |     100.0% |
| kernel/include/generic-rlimit.h                        | Other in-scope code             |     100.0% |
| kernel/include/hash.h                                  | Other in-scope code             |     100.0% |
| kernel/include/host_helpers.h                          | Other in-scope code             |     100.0% |
| kernel/include/init.h                                  | Other in-scope code             |     100.0% |
| kernel/include/kmalloc.h                               | Other in-scope code             |     100.0% |
| kernel/include/kmsg.h                                  | Other in-scope code             |     100.0% |
| kernel/include/kref.h                                  | Other in-scope code             |     100.0% |
| kernel/include/llist.h                                 | Other in-scope code             |     100.0% |
| kernel/include/lwk/compiler-gcc.h                      | Other in-scope code             |        n/a |
| kernel/include/lwk/compiler.h                          | Other in-scope code             |        n/a |
| kernel/include/lwk/futex.h                             | Other in-scope code             |     100.0% |
| kernel/include/lwk/stddef.h                            | Other in-scope code             |     100.0% |
| kernel/include/mc_jhash.h                              | Other in-scope code             |     100.0% |
| kernel/include/memobj.h                                | Other in-scope code             |     100.0% |
| kernel/include/mman.h                                  | Other in-scope code             |     100.0% |
| kernel/include/object_helpers.h                        | Other in-scope code             |     100.0% |
| kernel/include/page.h                                  | Other in-scope code             |     100.0% |
| kernel/include/pager.h                                 | Other in-scope code             |     100.0% |
| kernel/include/plist.h                                 | Other in-scope code             |     100.0% |
| kernel/include/prio.h                                  | Other in-scope code             |     100.0% |
| kernel/include/profile.h                               | Other in-scope code             |     100.0% |
| kernel/include/rbtree.h                                | Other in-scope code             |     100.0% |
| kernel/include/rbtree_augmented.h                      | Other in-scope code             |     100.0% |
| kernel/include/rusage.h                                | Other in-scope code             |     100.0% |
| kernel/include/rusage_private.h                        | Other in-scope code             |     100.0% |
| kernel/include/sched_helpers.h                         | Other in-scope code             |     100.0% |
| kernel/include/shm.h                                   | Other in-scope code             |     100.0% |
| kernel/include/sysfs.h                                 | Other in-scope code             |     100.0% |
| kernel/include/sysfs_msg.h                             | Other in-scope code             |     100.0% |
| kernel/include/time.h                                  | Other in-scope code             |     100.0% |
| kernel/include/timer.h                                 | Other in-scope code             |     100.0% |
| kernel/include/tofu/tof_core_bg_reg.h                  | Other in-scope code             |     100.0% |
| kernel/include/tofu/tof_core_cq_reg.h                  | Other in-scope code             |     100.0% |
| kernel/include/tofu/tof_icc.h                          | Other in-scope code             |     100.0% |
| kernel/include/tofu/tof_uapi.h                         | Other in-scope code             |     100.0% |
| kernel/include/tofu/tof_utofu_bg_bch.h                 | Other in-scope code             |     100.0% |
| kernel/include/tofu/tof_utofu_cq_trans.h               | Other in-scope code             |     100.0% |
| kernel/include/tofu/tofu_stag_range.h                  | Other in-scope code             |     100.0% |
| kernel/include/uio.h                                   | Other in-scope code             |     100.0% |
| kernel/include/waitq.h                                 | Other in-scope code             |     100.0% |
| kernel/include/xpmem.h                                 | Other in-scope code             |     100.0% |
| kernel/include/xpmem_private.h                         | Other in-scope code             |     100.0% |
| kernel/init.c                                          | Other in-scope code             |     100.0% |
| kernel/listeners.c                                     | Other in-scope code             |     100.0% |
| kernel/mikc.c                                          | Other in-scope code             |     100.0% |
| kernel/object_helpers.c                                | Other in-scope code             |     100.0% |
| kernel/profile.c                                       | Other in-scope code             |     100.0% |
| kernel/tofu/tof_utofu_main.c                           | Other in-scope code             |     100.0% |
| kernel/ubsan.c                                         | Other in-scope code             |     100.0% |
| lib/page_alloc.c                                       | Page allocator                  |     100.0% |
| kernel/include/process.h                               | Process/VM management           |     100.0% |
| kernel/include/process_helpers.h                       | Process/VM management           |     100.0% |
| kernel/process.c                                       | Process/VM management           |        n/a |
| kernel/process_helpers.c                               | Process/VM management           |     100.0% |
| kernel/futex.c                                         | Scheduler/timers/wait/futex     |     100.0% |
| kernel/sched_helpers.c                                 | Scheduler/timers/wait/futex     |     100.0% |
| kernel/timer.c                                         | Scheduler/timers/wait/futex     |     100.0% |
| kernel/llist.c                                         | Shared primitives               |     100.0% |
| kernel/lock_helpers.c                                  | Shared primitives               |     100.0% |
| kernel/plist.c                                         | Shared primitives               |     100.0% |
| kernel/rbtree.c                                        | Shared primitives               |     100.0% |
| kernel/waitq.c                                         | Shared primitives               |     100.0% |
| lib/abort.c                                            | Shared primitives               |     100.0% |
| lib/bitmap.c                                           | Shared primitives               |     100.0% |
| lib/bitops.c                                           | Shared primitives               |     100.0% |
| lib/include/bitmap.h                                   | Shared primitives               |     100.0% |
| lib/include/bitops-__ffs.h                             | Shared primitives               |     100.0% |
| lib/include/bitops-clear_bit.h                         | Shared primitives               |     100.0% |
| lib/include/bitops-ffz.h                               | Shared primitives               |     100.0% |
| lib/include/bitops-fls.h                               | Shared primitives               |     100.0% |
| lib/include/bitops-set_bit.h                           | Shared primitives               |     100.0% |
| lib/include/bitops-test_bit.h                          | Shared primitives               |     100.0% |
| lib/include/bitops.h                                   | Shared primitives               |     100.0% |
| lib/include/ctype.h                                    | Shared primitives               |     100.0% |
| lib/include/generic-errno.h                            | Shared primitives               |     100.0% |
| lib/include/ihk/cpu.h                                  | Shared primitives               |     100.0% |
| lib/include/ihk/debug.h                                | Shared primitives               |        n/a |
| lib/include/ihk/dma.h                                  | Shared primitives               |     100.0% |
| lib/include/ihk/lock.h                                 | Shared primitives               |     100.0% |
| lib/include/ihk/mm.h                                   | Shared primitives               |     100.0% |
| lib/include/ihk/monitor.h                              | Shared primitives               |     100.0% |
| lib/include/ihk/page_alloc.h                           | Shared primitives               |     100.0% |
| lib/include/ihk/perfctr.h                              | Shared primitives               |     100.0% |
| lib/include/limits.h                                   | Shared primitives               |     100.0% |
| lib/include/list.h                                     | Shared primitives               |     100.0% |
| lib/list.c                                             | Shared primitives               |     100.0% |
| lib/include/mc_perf_event.h                            | Shared primitives               |     100.0% |
| lib/include/mc_xpmem.h                                 | Shared primitives               |     100.0% |
| lib/include/memory.h                                   | Shared primitives               |     100.0% |
| lib/include/string.h                                   | Shared primitives               |     100.0% |
| lib/include/types.h                                    | Shared primitives               |     100.0% |
| lib/include/vsprintf.h                                 | Shared primitives               |     100.0% |
| lib/string.c                                           | Shared primitives               |     100.0% |
| lib/vsprintf.c                                         | Shared primitives               |     100.0% |
| arch/x86_64/kernel/syscall.c                           | Syscall core                    |        n/a |
| kernel/include/syscall.h                               | Syscall core                    |     100.0% |
| kernel/syscall.c                                       | Syscall core                    |        n/a |
| executer/user/arch/x86_64/arch-eclair.c                | User tools                      |     100.0% |
| executer/user/arch/x86_64/arch_args.h                  | User tools                      |     100.0% |
| executer/user/arch/x86_64/archdep_c.c                  | User tools                      |     100.0% |
| executer/user/arch/x86_64/include/arch-eclair.h        | User tools                      |     100.0% |
| executer/user/arch/x86_64/include/arch-ldump2mcdump.h  | User tools                      |     100.0% |
| executer/user/archdep.h                                | User tools                      |     100.0% |
| executer/user/archdep_uti.h                            | User tools                      |     100.0% |
| executer/user/eclair.c                                 | User tools                      |     100.0% |
| executer/user/eclair_bfd_shim.c                        | User tools                      |        n/a |
| executer/user/eclair.h                                 | User tools                      |     100.0% |
| executer/user/ldump2mcdump.c                           | User tools                      |     100.0% |
| executer/user/lib/uti/src/common.c                     | User tools                      |     100.0% |
| executer/user/lib/uti/src/include/uti.h                | User tools                      |     100.0% |
| executer/user/lib/uti/src/include/uti_impl.h           | User tools                      |     100.0% |
| executer/user/lib/uti/src/linux/uti.c                  | User tools                      |     100.0% |
| executer/user/lib/uti/src/mckernel/uti.c               | User tools                      |     100.0% |
| executer/user/lib/uti/test/src/attr.c                  | User tools                      |     100.0% |
| executer/user/lib/uti/test/src/create.c                | User tools                      |     100.0% |
| executer/user/lib/uti/test/src/include/util.h          | User tools                      |     100.0% |
| executer/user/libqlfort.c                              | User tools                      |     100.0% |
| executer/user/libsched_yield.c                         | User tools                      |     100.0% |
| executer/user/mcexec.c                                 | User tools                      |        n/a |
| executer/user/mcinspect.c                              | User tools                      |     100.0% |
| executer/user/mcinspect_bfd_shim.c                     | User tools                      |        n/a |
| executer/user/md5.c                                    | User tools                      |        n/a |
| executer/user/ql_mpiexec_start.c                       | User tools                      |        n/a |
| executer/user/qlmpi_mpi_abi.c                          | User tools                      |       n/a |
| executer/user/ql_server.c                              | User tools                      |     100.0% |
| executer/user/ql_talker.c                              | User tools                      |     100.0% |
| executer/user/qlmpilib.c                               | User tools                      |     100.0% |
| executer/user/syscall_intercept.c                      | User tools                      |     100.0% |
| tools/crash/mckernel.c                                 | User tools                      |     100.0% |
| tools/dwarf-extract-struct/dwarf-extract-struct.c      | User tools                      |        n/a |
| tools/mcstat/mcstat.c                                  | User tools                      |     100.0% |
| tools/mcstat/runtest.c                                 | User tools                      |     100.0% |
| executer/kernel/mcctrl/arch/x86_64/archdeps.c          | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/arch/x86_64/cpu.c               | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/arch/x86_64/include/arch-lock.h | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/arch/x86_64/include/archdeps.h  | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/binfmt_mcexec.c                 | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/control.c                       | host/IKC/mcctrl/IHK modules     |        n/a |
| executer/kernel/mcctrl/driver.c                        | host/IKC/mcctrl/IHK modules     |        n/a |
| executer/kernel/mcctrl/futex.c                         | host/IKC/mcctrl/IHK modules     |        n/a |
| executer/kernel/mcctrl/ikc.c                           | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/include/cpu.h                   | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/include/futex.h                 | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/include/mc_plist.h              | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/include/mcctrl_rust.h           | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/include/refcount.h              | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/mc_plist.c                      | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/mcctrl.h                        | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/procfs.c                        | host/IKC/mcctrl/IHK modules     |        n/a |
| executer/kernel/mcctrl/syscall.c                       | host/IKC/mcctrl/IHK modules     |        n/a |
| executer/kernel/mcctrl/sysfs.c                         | host/IKC/mcctrl/IHK modules     |        n/a |
| executer/kernel/mcctrl/sysfs.h                         | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/sysfs_files.c                   | host/IKC/mcctrl/IHK modules     |     100.0% |
| executer/kernel/mcctrl/sysfs_msg.h                     | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/attached/mic/dma.c                        | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/attached/mic/ikc.c                        | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/attached/mic/include/ihk/memconst.h       | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/attached/mic/include/march.h              | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/attached/mic/mic.h                        | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/attached/mic/setup.c                      | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/attached/mic/sfi.c                        | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/builtin/mic/bootparam.h                   | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/builtin/mic/builtin_dma.h                 | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/builtin/mic/dma.c                         | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/builtin/mic/ikc.c                         | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/builtin/mic/include/ihk/memconst.h        | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/builtin/mic/include/march.h               | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/builtin/mic/setup.c                       | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/builtin/x86/bootparam.h                   | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/builtin/x86/builtin_dma.h                 | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/builtin/x86/dma.c                         | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/builtin/x86/ikc.c                         | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/builtin/x86/include/ihk/memconst.h        | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/builtin/x86/include/march.h               | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/builtin/x86/setup.c                       | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/cokernel/smp/ikc.c                                 | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/smp/x86_64/bootparam.h                    | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/smp/x86_64/builtin_dma.h                  | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/smp/x86_64/dma.c                          | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/smp/x86_64/include/ihk/memconst.h         | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/smp/x86_64/include/march.h                | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/smp/x86_64/perf_event.h                   | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/cokernel/smp/x86_64/setup.c                        | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/ikc/include/ikc/ihk.h                              | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/ikc/include/ikc/master.h                           | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/ikc/include/ikc/msg.h                              | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/ikc/include/ikc/queue.h                            | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/ikc/linux.c                                        | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/ikc/manycore.c                                     | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/ikc/master.c                                       | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/ikc/queue.c                                        | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/include/sysdeps/mic/mic/mic_dma.h                  | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/include/sysdeps/mic/mic/mic_type.h                 | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/include/sysdeps/mic/mic/micconst.h                 | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/include/sysdeps/mic/mic/micsboxdefine.h            | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/include/sysdeps/mic/mic_host.h                     | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/core/host_driver.c                           | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/core/host_linux.h                            | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/core/mem_alloc.c                             | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/core/mikc.c                                  | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/core/mm.c                                    | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/core/ops_wrappers.h                          | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/driver/attached/mic/ihk_mic_driver.c         | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/driver/attached/mic/mic.h                    | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/driver/attached/mic/mic_device.c             | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/driver/attached/mic/mic_dma.c                | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/driver/attached/mic/mic_mmio.h               | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/driver/attached/mic/mic_user.h               | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/driver/builtin/builtin_dma.h                 | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/driver/builtin/dmacore.c                     | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/driver/builtin/ihk_builtin_driver.c          | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c     | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/driver/smp/arch/x86_64/smp-defines-driver.h  | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/driver/smp/bootparam.h                       | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/driver/smp/smp-arch-driver.h                 | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/driver/smp/smp-driver.c                      | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/driver/smp/smp-driver.h                      | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/affinity.h                       | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/include/ihk/arch/x86_64/ihk/archdefs.h       | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/arch/x86_64/ihk/msr.h            | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/arch/x86_64/ihk/rdtsc.h          | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/ihk_debug.h                      | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/ihk_host_driver.h                | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/ihk_host_misc.h                  | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/ihk_host_user.h                  | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/ihk_monitor.h                    | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/ihk_rusage.h                     | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/ihklib.h                         | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/ihklib_private.h                 | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/ikc.h                            | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/include/ihk/misc/debug.h                     | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/include/ihk/status.h                         | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/user/devtest.c                               | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/user/ihkconfig.c                             | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/user/ihklib.c                                | host/IKC/mcctrl/IHK modules     |        n/a |
| ihk/linux/user/ihkmond.c                               | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/user/ihkosctl.c                              | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/user/maccess.c                               | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/user/memdump-mic.c                           | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/user/memdump.c                               | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/user/memdump2.c                              | host/IKC/mcctrl/IHK modules     |     100.0% |
| ihk/linux/user/pagetable.c                             | host/IKC/mcctrl/IHK modules     |     100.0% |
| kernel/devobj.c                                        | procfs/sysfs/xpmem/file objects |     100.0% |
| kernel/fileobj.c                                       | procfs/sysfs/xpmem/file objects |     100.0% |
| kernel/hugefileobj.c                                   | procfs/sysfs/xpmem/file objects |     100.0% |
| kernel/pager.c                                         | procfs/sysfs/xpmem/file objects |     100.0% |
| kernel/procfs.c                                        | procfs/sysfs/xpmem/file objects |     100.0% |
| kernel/shmobj.c                                        | procfs/sysfs/xpmem/file objects |     100.0% |
| kernel/sysfs.c                                         | procfs/sysfs/xpmem/file objects |     100.0% |
| kernel/xpmem.c                                         | procfs/sysfs/xpmem/file objects |     100.0% |
| kernel/zeroobj.c                                       | procfs/sysfs/xpmem/file objects |     100.0% |
| arch/x86_64/kernel/include/arch-memory-helpers.h       | x86_64 memory management        |     100.0% |
| arch/x86_64/kernel/include/arch-memory.h               | x86_64 memory management        |     100.0% |
| arch/x86_64/kernel/memory.c                            | x86_64 memory management        |        n/a |
| arch/x86_64/kernel/memory_helpers.c                    | x86_64 memory management        |     100.0% |
| kernel/mem.c                                           | x86_64 memory management        |        n/a |
+--------------------------------------------------------+---------------------------------+------------+

Allowlisted n/a boundary justifications:
- `ihk/linux/core/abi_checks.c`: ABI/layout verifier used to prove C/Rust layout parity; not a McKernel runtime implementation body.
- `kernel/rust/abi_checks.c`: ABI/layout verifier used to prove Rust/C layout parity; not a McKernel runtime implementation body.
- `arch/x86_64/elfboot/elfboot.c`: x86 loader handoff surface outside the strict smp-x86 McKernel runtime image; retained as a boot compatibility boundary.
- `arch/x86_64/kboot/main.c`: x86 kboot loader handoff surface outside the strict smp-x86 McKernel runtime image; retained as a boot compatibility boundary.
- `executer/include/defs.h`: upstream crash-suite compatibility header surface; retained for external interface compatibility, not McKernel runtime behavior.
- `executer/include/md5.h`: third-party MD5 compatibility header; retained as external provenance, not McKernel-owned implementation debt.
- `kernel/include/lwk/compiler-gcc.h`: compiler compatibility surface for attributes/builtins; retained as a toolchain boundary, not McKernel runtime behavior.
- `kernel/include/lwk/compiler.h`: compiler compatibility surface for attributes/builtins; retained as a toolchain boundary, not McKernel runtime behavior.
- `lib/include/ihk/debug.h`: public IHK diagnostic compatibility surface; retained at the external API boundary, not as a strict McKernel implementation object.
- `executer/user/md5.c`: third-party MD5 implementation provenance; retained as external source, not McKernel-owned implementation debt.
- `executer/user/ql_mpiexec_start.c`: QL MPI entry path is outside the active Rocky smp-x86 validation configuration; retained as a deferred interface boundary.
- `executer/user/qlmpi_mpi_abi.c`: QL MPI ABI shim is outside the active Rocky smp-x86 validation configuration; retained as a deferred interface boundary.
- `executer/user/eclair_bfd_shim.c`: BFD macro compatibility shim for Rust-owned eclair; retained only because bfd_get_symtab_upper_bound() and bfd_canonicalize_symtab() are header-dispatched BFD function-table calls rather than linkable libbfd symbols.
- `executer/user/mcinspect_bfd_shim.c`: BFD macro compatibility shim for Rust-owned mcinspect; retained only because bfd_get_symtab_upper_bound() and bfd_canonicalize_symtab() are header-dispatched BFD function-table calls rather than linkable libbfd symbols.
- `executer/user/mcexec.c`: user-space launcher/libc/ioctl/ptrace boundary for mcexec; Rust owns top-level main sequencing, option parsing dispatch, descriptor load/preparation ordering, environment collection, stack-limit publication, topology/thread-plan/DMA setup, partitioned CPU setup, runtime policy publication, and final image start sequencing, while C is retained for the public main shim, getopt/static option storage, libc personality/re-exec and bind-mount bridges, mcctrl ioctl/ptrace/syscall/file/path/signal/pthread/numa callbacks, diagnostics, and non-Rust fallback source.
- `ihk/linux/user/ihklib.c`: public libihk user-space ABI/libc/ioctl/sysfs/BFD boundary; Rust owns CPU, memory, IKC, environment, OS lifecycle/status/kmsg/pagesize/rusage/perf/freeze/thaw, string-dispatch, topology/path, and ihkconfig/ihkosctl policy helpers, while C is retained for exported ABI wrappers, libc varargs/getopt/path and directory iteration, open/access/ioctl/eventfd/usleep callbacks, sysfs meminfo reads, libbfd dump-file construction, KRM cpuset compatibility, diagnostics, global reserve-memory configuration storage, and non-Rust fallback source.
- `tools/dwarf-extract-struct/dwarf-extract-struct.c`: developer layout-extraction tool, not a McKernel runtime/control-plane implementation object.
- `executer/kernel/mcctrl/procfs.c`: Linux procfs/file-operations ABI boundary for mcctrl_ko; Rust owns procfs packet, entry lifecycle, traversal, lseek, buffer open/release/read, and read/write request sequencing, while C is retained for version-dependent proc_ops/file_operations structs, procfs registration/removal, PDE data access, Linux credential/RCU/semaphore/list/page/user-copy/map/unmap primitive callbacks, and Linux kernel API coupling.
- `executer/kernel/mcctrl/driver.c`: Linux module/notifier/binfmt/kallsym/file-operation ABI boundary for mcctrl_ko; Rust owns ioctl dispatch, OS slot lookup, OS-alive scan, boot/shutdown notifier sequencing, symbol lookup ordering, module init/exit ordering, and control-plane callback dispatch, while C is retained for module_init/module_exit publication, kernel-version-specific binfmt/mmap helpers, kallsym compatibility globals, refcount/waitqueue/list/spinlock/allocation/uaccess/page/kmap primitives, IHK host callback registration, and bridge callbacks into Linux kernel APIs.
- `executer/kernel/mcctrl/futex.c`: Linux futex/user-copy/exception-table/wait-event boundary for mcctrl_ko; Rust owns futex key derivation, RVA-to-RPA cache search/insert orchestration, wait setup, wait queue sleep sequencing, wake scan, wake-op, requeue, queue/unqueue/list mutation, wake-state publication, McKernel wakeup-thread status sequencing, top-level dispatch, timeout selection, and process-cache cleanup, while C is retained for x86 exception-table atomics, `__get_user`, pagefault disable/enable, `copy_from_user`, Linux time/wait-event calls, `phys_to_virt`, IHK interrupt issue, rbtree primitive callbacks, kmalloc/free callbacks, per-process lookup/refcount callbacks, and non-Rust fallback source.
- `executer/kernel/mcctrl/syscall.c`: Linux VFS/MM/page-fault/core-dump and IHK remote-memory callback boundary for mcctrl_ko; Rust owns per-thread data lifecycle sequencing, optional Tofu file-to-pidfd hash sequencing, pager process lifecycle, pager request dispatch, remote page-fault packet publication/send/log sequencing, clear-PTE traversal/action selection, release-user-space traversal, zero-page list movement, and in-kernel syscall dispatch/return-release policy, while C is retained for Linux file/stat/path/mmap/munmap/fault/PTE/kernel_read/kernel_write/core-file APIs, IHK map/unmap/IKC packet wakeup callbacks, scheduler credential primitive checks, Tofu PDE/ioctl Linux callbacks, and non-Rust fallback source.
- `executer/kernel/mcctrl/sysfs.c`: Linux sysfs/kobject and IHK sysfs IKC-exchange boundary for mcctrl_ko; Rust owns packet/work dispatch, remote show/store/release sequencing, response completion, lookup traversal, local show/store/release and snooping format helpers, post-format public entry routing, remote parameter map/publish sequencing, sysfs topology/setup-file sequencing, and path validation, while C is retained for Linux kobject/sysfs create/remove/link callbacks, sysfs operation tables, IHK remote-buffer map/unmap callbacks, sysfs request exchange synchronization, remote snooping lifetime and kernel API callbacks, allocation/free primitives, and non-Rust fallback source.
- `arch/x86_64/kernel/memory.c`: x86_64 page-table, TLB, physical/virtual mapping, and user-copy primitive boundary for mckernel.img; Rust owns page-size classification, attribute conversion, early allocation policy, page-table walk/visit dispatch, set/clear/change/free range orchestration, lookup/split/move-PTE sequencing, page-table init area setup, user string/copy wrappers, and map/direct-copy selection, while C is retained for direct PTE mutation, page-table allocation/free callbacks, physical/virtual conversion callbacks, TLB flush instructions, init page-table publication, user-copy fault/map callbacks, tracked allocation/free bridges, log/panic side effects, and non-Rust fallback source.
- `arch/x86_64/kernel/cpu.c`: x86_64 CPU bring-up, interrupt, trap, APIC, FPU, descriptor-table, and context-switch primitive boundary for mckernel.img; Rust owns the surrounding x86 CPU ABI/layout helpers, register-state policy helpers, and CPU/control sequencing already exported from x86_cpu_helpers.rs, while C is retained for assembly-adjacent control-register/MSR/APIC/FPU instructions, IDT/GDT/TSS publication, exception-vector tables, low-level context entry/return glue, panic/log callbacks, and non-Rust fallback source.
- `arch/x86_64/kernel/syscall.c`: x86_64 syscall, signal-frame, ptrace register, TLS/vDSO, and process-VM architecture ABI boundary for mckernel.img; Rust owns syscall, signal, ptrace, wait, credential, process-VM, and migration policy sequencing in the shared Rust syscall/x86 helper layer, while C is retained for architecture register-frame access, debug-register and fpreg/regset primitives, signal-frame user-copy glue, vDSO/TLS callbacks, SMP move-pages handler integration, exception-return coupling, and non-Rust fallback source.
- `kernel/process.c`: process, thread, VM-range, wait, ptrace, scheduler, and lifecycle primitive callback boundary for mckernel.img; Rust owns the broad process/VM/wait/ptrace/range/page-fault/release sequencing and exported helper bodies in process_helpers.rs, while C is retained for exported ABI shims, procfs/profile/scheduler bridge callbacks, allocation/free/refcount/list/lock/waitqueue primitive side effects, architecture callback glue, diagnostics, and non-Rust fallback source.
- `kernel/syscall.c`: top-level syscall table, dispatch, Linux-forwarding, user-copy, and primitive callback boundary for mckernel.img; Rust owns the syscall policy bodies for memory policy, ptrace, wait, futex, signal, credential, resource-limit, scheduling, mckfd, and related wrappers in syscall_policy.rs, while C is retained for syscall table publication, low-level dispatch entry coupling, user-copy and Linux-forwarding primitive callbacks, signal/time/scheduler primitive side effects, diagnostics, and non-Rust fallback source.
- `kernel/mem.c`: core memory, page-fault, coredump, dump-query, page-allocator, and sysfs memory primitive boundary for mckernel.img; Rust owns page allocator lifecycle, page-table lookup/fault sequencing, memory-policy syscalls, query-free interrupt sequencing, dump bitmap/page-set helpers, and allocation wrapper policy in mem_helpers.rs, while C is retained for page-fault handler wiring, coredump/file emission callbacks, sysfs meminfo callbacks, page allocation/free/user-copy/lock primitive bodies, panic/log callbacks, and non-Rust fallback source.
- `executer/kernel/mcctrl/control.c`: Linux mcctrl ioctl, process proxy, per-process/per-thread data, waitqueue, IKC, and exec-file boundary for mcctrl_ko; Rust owns ioctl dispatch, image transfer/start/signal/syscall return/load, credential, region, mount/unmount/unshare, perf, rusage, UTI, release-user-space, and partition-policy helper sequencing through mcctrl_helpers.rs, while C is retained for Linux task/current/file/proc/open_exec/user-copy callbacks, PPD/PTD hash/list/refcount/waitqueue side effects, cpumask/topology callback integration, IKC send/release callbacks, diagnostics, and non-Rust fallback source.
- `ihk/linux/core/host_driver.c`: Linux IHK host device, OS object, file-operation, ioctl, kmsg, resource, and module-init boundary; Rust owns host-driver ioctl/action selection, kmsg buffer lifecycle, OS/device resource/status/debug/read/write/kargs/dump/freeze/thaw/rusage/build-id operation sequencing through Rust helpers, while C is retained for Linux cdev/file/module APIs, copy_to_user/copy_from_user, host OS/device object lifetime callbacks, resource reservation primitive mutation, kmsg buffer mapping, file read/write callbacks, diagnostics, and non-Rust fallback source.
- `ihk/linux/driver/smp/arch/x86_64/smp-arch-driver.c`: x86_64 SMP host-driver AP startup, trampoline, identity-map, interrupt, topology, APIC, and dump primitive boundary; Rust owns status/query, TSC/APIC policy, startup/trampoline/identity-map sequencing, interrupt/broadcast selection, perf-event mapping, topology collection, and symbol-init sequencing through smp_driver_helpers.rs, while C is retained for Linux/x86 APIC/MSR/MMIO/ioremap/page-table/trampoline instructions, irq/vector integration, topology callback reads, dump register capture primitives, diagnostics, and non-Rust fallback source.
- `ihk/linux/driver/smp/smp-driver.c`: Linux SMP IHK driver device/OS, CPU, memory, IKC, boot, map/unmap, topology, and module-operation boundary; Rust owns CPU/memory request validation, OS/device lifecycle, status/mode/debug/resource/query/load/boot/map/unmap/topology/file-read/module-init/exit sequencing through smp_driver_helpers.rs, while C is retained for Linux driver/module/file/sysfs APIs, memory chunk list mutation callbacks, CPU hotplug and cpumask callbacks, boot-parameter assembly bridges, IHK host object callbacks, copy_to_user/copy_from_user, diagnostics, and non-Rust fallback source.
- `ihk/cokernel/attached/mic/dma.c`: MIC/attached target code is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/attached/mic/ikc.c`: MIC/attached target code is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/attached/mic/include/ihk/memconst.h`: MIC/attached target header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/attached/mic/include/march.h`: MIC/attached target header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/attached/mic/mic.h`: MIC/attached target header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/attached/mic/setup.c`: MIC/attached target code is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/attached/mic/sfi.c`: MIC/attached target code is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/builtin/mic/bootparam.h`: MIC/builtin target header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/builtin/mic/builtin_dma.h`: MIC/builtin target header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/builtin/mic/dma.c`: MIC/builtin target code is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/builtin/mic/ikc.c`: MIC/builtin target code is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/builtin/mic/include/ihk/memconst.h`: MIC/builtin target header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/builtin/mic/include/march.h`: MIC/builtin target header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/builtin/mic/setup.c`: MIC/builtin target code is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/cokernel/builtin/x86/bootparam.h`: non-smp builtin x86 target header is outside the active smp-x86 source-retirement build.
- `ihk/cokernel/builtin/x86/setup.c`: non-smp builtin x86 target setup code is outside the active smp-x86 source-retirement build.
- `ihk/include/sysdeps/mic/mic/mic_dma.h`: MIC sysdeps header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/include/sysdeps/mic/mic/mic_type.h`: MIC sysdeps header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/include/sysdeps/mic/mic/micconst.h`: MIC sysdeps header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/include/sysdeps/mic/mic/micsboxdefine.h`: MIC sysdeps header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/linux/driver/attached/mic/ihk_mic_driver.c`: Linux MIC attached driver is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/linux/driver/attached/mic/mic.h`: Linux MIC attached driver header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/linux/driver/attached/mic/mic_device.c`: Linux MIC attached driver code is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/linux/driver/attached/mic/mic_dma.c`: Linux MIC attached driver code is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/linux/driver/attached/mic/mic_mmio.h`: Linux MIC attached driver header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/linux/driver/attached/mic/mic_user.h`: Linux MIC attached driver UAPI header is outside the active x86_64 smp-x86 source-retirement scope.
- `ihk/linux/driver/builtin/dmacore.c`: non-smp builtin Linux driver code is outside the active smp-x86 source-retirement build.
- `ihk/linux/driver/builtin/ihk_builtin_driver.c`: non-smp builtin Linux driver code is outside the active smp-x86 source-retirement build.
- `ihk/linux/include/ihk/affinity.h`: public IHK affinity compatibility header; retained at the external API boundary, not as a strict implementation object.
- `ihk/linux/include/ihk/misc/debug.h`: public IHK diagnostic compatibility header; retained at the external API boundary, not as a strict implementation object.

Instructions for completing the full Rust port

1. Work category by category, starting with rows at 0.0%. Treat each McKernel-owned .c implementation file as incomplete until its behavior is owned by Rust and the C body is removed from the strict build.
2. For each .c file, create or extend the nearest Rust replacement module listed by the category owner. Preserve public C ABI names with stable #[no_mangle] extern "C" exports when external callers still require those symbols.
3. Move behavior, not just call routing. Validation, allocation/free decisions, locking/list mutation, refcount mutation, page-table/TLB changes, user-copy shaping, IKC packet shaping, signal/wait/futex behavior, file/object lifecycle, CLI parsing, output shaping, and error shaping must live in Rust before the C body counts as retired.
4. Remove fallback C bodies after Rust equivalence is proven. Update CMake/Kbuild so the strict Rust build does not compile the retired C object, and delete or quarantine the old C source instead of leaving it as an alternate implementation.
5. Reduce headers to ABI surfaces. For every header below 100.0%, move executable static inline bodies and function-like macro behavior into Rust helpers or Rust ABI exports. Leave only constants, layouts, typedefs, prototypes, syscall/ioctl numbers, and other compatibility declarations.
6. Recheck n/a rows. Allowlisted boundary files may remain only when they are ABI/layout, loader, external-interface, generated, test, third-party, or assembly-adjacent surfaces with no McKernel-owned implementation behavior.
7. Keep compatibility stable. Do not change exported symbol names, syscall numbers, ioctl values, IKC packet formats, public struct layouts, command-line flags, output formats, or exit-code behavior while moving bodies to Rust.
8. Validate each retirement slice before moving on. Use focused equivalence tests first, then non-reboot Rust/C builds, ownership/source-audit gates, header executable-logic gates, and module-load smoke. Stop on the first validation failure and record the failing command and short error summary in kernel.log.
9. Add enforcement gates before the final sweep. The build should fail if a non-allowlisted McKernel-owned .c object is compiled, if a retained C fallback body exists, or if a compatibility header contains executable McKernel-owned inline/macro logic.
10. Finish only when all scored rows are 100.0%, all remaining n/a rows are explicitly allowlisted boundaries, the strict Rust build compiles no McKernel-owned C implementation objects, and runtime validation passes without requiring host reboot paths.

Total percentage points left: 0.0%
